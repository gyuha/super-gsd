---
phase: 42-smart-default-lens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/sg-retro/SKILL.md
autonomous: true
requirements: [LENS-01, LENS-02]
tags: [sg-retro, lens-consolidation, smart-default, agents, codex, gemini]

must_haves:
  truths:
    - "Codex/Gemini/Antigravity 환경에서 인자 없이 sg-retro를 호출하면 numbered-list 없이 dspm → ssc 순서로 즉시 실행된다"
    - "ssc/dspm/analyze 인자는 그대로 매칭되어 단독 또는 멀티 실행된다"
    - "4ls/sail/5why 인자는 stderr 에러 + exit 1로 거부되고 lessons 파일이 생성되지 않는다"
    - "멀티 인자 중 첫 dropped lens 발견 즉시 거부되며 후속 lens는 실행되지 않는다"
    - "<lens_templates> 블록에서 4ls/sail/5why 템플릿이 완전히 삭제되었고, ssc/dspm/analyze 템플릿만 보존된다"
    - "skills/sg-retro/SKILL.md와 의미적으로 동등하다 (헤더·<constraints>·<lens_templates>·argument-hint·success_criteria 항목 수 차이만 허용)"
  artifacts:
    - path: ".agents/skills/sg-retro/SKILL.md"
      provides: "Smart default lens 자동 진입 + 3-lens consolidation + dropped-lens reject (Codex/Gemini/Antigravity 변종)"
      contains: 'LENS_CODES_ARRAY="dspm ssc"'
  key_links:
    - from: ".agents/skills/sg-retro/SKILL.md Step 2 case-statement"
      to: "Step 5 sub-block / Step 6 LENS_NAME 매핑 / <lens_templates>"
      via: "LENS_CODE 변수 흐름"
      pattern: 'case "\$LENS_LC" in'
    - from: ".agents/skills/sg-retro/SKILL.md Step 2 smart default 분기"
      to: "Step 3+ LENS_CODES_ARRAY 순차 실행"
      via: "LENS_CODES_ARRAY 변수"
      pattern: "LENS_CODES_ARRAY"
    - from: ".agents/skills/sg-retro/SKILL.md"
      to: "skills/sg-retro/SKILL.md (pairwise mirror)"
      via: "D-10 동일 commit staging + 의미적 동등"
      pattern: "no longer supported (removed in v2.9)"
---

<objective>
`.agents/skills/sg-retro/SKILL.md`를 surgical edit 하여 plan 42-01이 `skills/sg-retro/SKILL.md`에 적용한 것과 **의미적으로 동등한** 변경을 적용한다. 두 파일은 구조적 차이가 있으므로(AskUserQuestion vs numbered-list + `<constraints>` + `<lens_templates>` + argument-hint frontmatter) 단순 `cp` 미러링이 아니라 **동일 의미 단위**의 변경을 각 파일 구조에 맞게 적용한다.

Phase 42의 핵심 변경:
1. **Smart default 자동 진입 (D-01, D-02, D-08)** — `LENS_RAW`와 `EXTRA_LENS_CODES`가 모두 비어 있을 때 `LENS_CODES_ARRAY="dspm ssc"`로 즉시 세팅. Step 2의 numbered-list 출력 블록과 `map_num_to_code()` 헬퍼를 **통째로 삭제**한다 (plan 42-01의 AskUserQuestion 삭제와 동등한 surgical 제거).
2. **Lens 6→3 통합 (D-03, D-04, D-05, D-09)** — `ssc`/`dspm`/`analyze` 3개만 유지. `4ls`/`sail`/`5why` 관련 코드를 (a) Step 2 case 매핑, (b) Step 2 VALID_EXTRAS inner case, (c) Step 5 sub-block 본문, (d) Step 6 LENS_NAME 매핑, (e) `<lens_templates>` 블록, (f) `<success_criteria>` 본문, (g) frontmatter `description` + `argument-hint`에서 모두 제거한다.
3. **Dropped lens reject (D-06, D-07)** — Step 2의 case 매핑에서 `4ls`/`sail`/`5why`(또는 미지의 코드)가 들어오면 stderr에 영문 에러 메시지를 emit하고 `exit 1`. 멀티 인자도 첫 dropped 발견 즉시 거부.

Purpose: pairwise convention(D-10 + CLAUDE.md "skills/ + .agents/ 쌍 커버")을 충족하여 Codex/Gemini/Antigravity 환경 사용자도 동일한 lens 마찰 제거 UX를 받는다.

Output: 의미적으로 동등한 단일 파일 (`.agents/skills/sg-retro/SKILL.md`) — 라인 수는 감소하지만 Step 번호/구조/`<constraints>`/argument-hint frontmatter는 보존.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-smart-default-lens/42-CONTEXT.md
@.planning/phases/42-smart-default-lens/42-PATTERNS.md
@CLAUDE.md
@.agents/skills/sg-retro/SKILL.md
</context>

<interfaces>
<!-- Phase 42에서 변경 대상이 되는 식별자와 정확한 토큰. .agents/ 버전 고유 구조 포함. -->

**유지 lens 코드 (3개):** `ssc`, `dspm`, `analyze`
**제거 lens 코드 (3개):** `4ls`, `sail`, `5why`

**유지 LENS_NAME 매핑 (Step 6):**
- `ssc` → `"Start/Stop/Continue"`
- `dspm` → `"Decisions/Surprises/Patterns/Mistakes"`
- `analyze` → `"Conversation Analyzer"`

**유지 `<lens_templates>` 블록:**
- `Start/Stop/Continue (ssc)` 템플릿
- `Decisions/Surprises/Patterns/Mistakes (dspm)` 템플릿
- `Conversation Analyzer (analyze)` 템플릿

**삭제 `<lens_templates>` 블록 (D-05):**
- `4Ls (4ls)` 템플릿 (lines ~353-374)
- `Sailboat (sail)` 템플릿 (lines ~399-420)
- `Five Whys (5why)` 템플릿 (lines ~422-452)

**Smart default 진입 조건:** `[ -z "$LENS_CODES_ARRAY" ] && [ -z "$LENS_RAW" ] && [ -z "$EXTRA_LENS_CODES" ]`
**Smart default 세팅:** `LENS_CODES_ARRAY="dspm ssc"` (dspm을 앞에)

**Dropped-lens 에러 메시지 (영문 fallback — Claude가 출력 시점에 사용자 언어로 렌더링):**
```
Lens '{code}' is no longer supported (removed in v2.9).
Available lenses: ssc, dspm, analyze.
Run without lens argument to use smart default (dspm+ssc).
```
이어서 `exit 1`. plan 42-01과 동일 문구·동일 종료 코드.

**현재 6-케이스 case-statement (Step 2, 86-98줄):** plan 42-01의 interfaces 블록과 동일한 6-케이스 구조.

**현재 numbered-list 출력 블록 (lines 101-131) — 삭제 대상:**
```
Select a lens:
1) ssc     — Start/Stop/Continue
2) 4ls     — 4Ls (Like/Learned/Lacked/Longed for)
3) dspm    — Decisions/Surprises/Patterns/Mistakes
4) sail    — Sailboat
5) 5why    — Five Whys
6) analyze — Conversation Analyzer
```
+ 직후 `map_num_to_code()` 헬퍼:
```bash
map_num_to_code() {
  case "$1" in
    1) echo "ssc"     ;;
    2) echo "4ls"     ;;
    3) echo "dspm"    ;;
    4) echo "sail"    ;;
    5) echo "5why"    ;;
    6) echo "analyze" ;;
    ssc|4ls|dspm|sail|5why|analyze) echo "$1" ;;
    *) echo "" ;;
  esac
}
```
+ 두 블록 사이의 안내 산문(있다면) 전체 삭제.

**현재 VALID_EXTRAS inner case (Step 2, lines ~136-149):** plan 42-01과 동일한 패턴 + 동일한 변경 방향(3-lens + dropped reject).

**현재 LENS_NAME 매핑 (Step 6, lines 271-278):** plan 42-01의 Step 5 LENS_NAME 매핑과 동일 6-케이스 구조 → 3-케이스로 축소.

**현재 frontmatter (lines 1-5):**
```
---
name: sg-retro
description: Run a structured retrospective on a GSD phase with one of six lenses — select multiple lenses in one call — and append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md. AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.
argument-hint: "[phase] [lens] - e.g. '14 ssc' or '14 ssc dspm'. lens: ssc|4ls|dspm|sail|5why|analyze"
---
```
→ 변경 후:
```
---
name: sg-retro
description: Run a structured retrospective on a GSD phase with one of three lenses (ssc, dspm, analyze) — select multiple lenses in one call or omit lens argument for smart default (dspm+ssc) — and append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md. AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.
argument-hint: "[phase] [lens] - e.g. '14 ssc' or '14 ssc dspm'. lens: ssc|dspm|analyze. Omit lens for smart default (dspm+ssc)."
---
```

**보존 금지 영역 (절대 손대지 않는다):**
- Step 1 STATE.md parsing 블록 (`# --- BEGIN STATE.md Phase parsing block ---` ~ END 사이) — `.agents/` 버전은 `(macOS-compatible grep + sed + awk pipeline)` 부속 텍스트가 없음. 그 차이는 보존(통일하지 않음 — surgical only).
- `<constraints>` 블록 (Codex/Gemini/Antigravity 제약 명시) — `.agents/` 고유 블록.
- `<language>` 블록
- `<objective>` 블록의 lens 개수 외 본문
- Step 3 artifact collection
- Step 5 ssc/dspm/analyze sub-block 본문 (DSPM explicit guard 포함)
- Step 5 analyze 끝의 `ANALYZE_LENS_RAN=true`
- Step 6 append 로직
- `<lens_templates>`의 ssc/dspm/analyze 템플릿 본문
- `<success_criteria>` 항목 #1, #4, #5, #6, #7 (dropped lens 미참조)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: frontmatter + Step 2 매핑 축소 + numbered-list/map_num_to_code 삭제 + smart default + dropped reject</name>
  <files>.agents/skills/sg-retro/SKILL.md</files>
  <read_first>
    - .agents/skills/sg-retro/SKILL.md (전체 — frontmatter 1-5, Step 2 86-149, 인접 보존 블록 확인)
    - .planning/phases/42-smart-default-lens/42-CONTEXT.md (D-01, D-02, D-05, D-06, D-07, D-08, D-09, D-10 결정)
    - .planning/phases/42-smart-default-lens/42-PATTERNS.md §2.A, §2.B, §2.D, §2.E, §2.F (변경 지시 본문)
    - skills/sg-retro/SKILL.md (plan 42-01 변경 적용 후의 참조 — 의미적 동등 기준)
    - CLAUDE.md §"사용자 언어 메시지" + §"macOS 셸 이식성"
  </read_first>
  <action>
**A. frontmatter 교체 (D-01, D-04, D-05):**
Line 3 `description` 본문에서 `one of six lenses` → `one of three lenses (ssc, dspm, analyze)`, 그리고 `or omit lens argument for smart default (dspm+ssc)` 절을 multi-call 안내(`select multiple lenses in one call`) 뒤에 추가한다. `AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.` 문장은 그대로 보존.

Line 4 `argument-hint` 본문에서 `lens: ssc|4ls|dspm|sail|5why|analyze` → `lens: ssc|dspm|analyze. Omit lens for smart default (dspm+ssc).` 로 교체.

`name` 필드와 `<language>` 블록(이어지는 줄들)은 손대지 않는다.

**B. `<objective>` 본문 축소:**
`<objective>` 블록 내부의 "six lenses" 또는 dropped lens 코드 나열 문구만 "three lenses" + ssc/dspm/analyze 나열로 축소. `<objective>` 블록의 lens-언급 외 본문은 보존.

**C. `<constraints>` 블록 보존:**
Lines 18-24의 `<constraints>` 블록은 Codex/Gemini/Antigravity 제약 명시 — Phase 42에서 의미 변경 없음. **byte-identical로 보존**.

**D. Step 1 STATE.md parsing 블록 — 보존:**
Lines 33-48의 `# --- BEGIN STATE.md Phase parsing block ---` ~ `# --- END ... ---` 사이는 절대 손대지 않는다. `.agents/` 버전은 부속 텍스트 `(macOS-compatible grep + sed + awk pipeline)`이 없는데, 이 차이도 그대로 보존(통일 시도 금지 — surgical only).

**E. Step 2 — `LENS_CODE` case-statement 축소 + dropped reject (D-05, D-06, D-09):**
Lines ~86-98의 6-케이스 case-statement에서:
- 보존: `ssc)`, `dspm)`, `analyze)` 세 줄 (들여쓰기·정렬 그대로).
- 삭제: `4ls)`, `sail)`, `5why)` 세 줄 완전 제거.
- `*)` fallback을 dropped/unknown 에러 분기로 교체. plan 42-01과 **byte-identical 동일한 에러 메시지**를 emit:

```bash
case "$LENS_LC" in
  ssc)     LENS_CODE="ssc"     ;;
  dspm)    LENS_CODE="dspm"    ;;
  analyze) LENS_CODE="analyze" ;;
  4ls|sail|5why)
    printf "Lens '%s' is no longer supported (removed in v2.9).\nAvailable lenses: ssc, dspm, analyze.\nRun without lens argument to use smart default (dspm+ssc).\n" "$LENS_LC" >&2
    exit 1
    ;;
  *)
    printf "Lens '%s' is no longer supported (removed in v2.9).\nAvailable lenses: ssc, dspm, analyze.\nRun without lens argument to use smart default (dspm+ssc).\n" "$LENS_LC" >&2
    exit 1
    ;;
esac
```

(들여쓰기는 기존 case-statement와 동일 2-space. `;;` 위치 일관 유지. 에러 메시지 본문·`exit 1`은 plan 42-01과 정확히 동일해야 D-12 시나리오 3·4 정합 검증을 통과한다.)

**F. Step 2 — Numbered-list 출력 블록 + `map_num_to_code()` 헬퍼 완전 삭제 (D-08):**
PATTERNS.md §2.E에 기술된 다음 블록 전체를 삭제:

1. `Select a lens:` 안내 + `1) ssc — ...` ~ `6) analyze — ...` 6줄 + 그 사이의 안내 산문
2. `map_num_to_code()` 함수 정의 전체 (`map_num_to_code() { case ... esac }`)
3. 두 블록 사이의 안내 산문(있다면)

이는 plan 42-01의 AskUserQuestion 6-옵션 multiSelect 블록 삭제와 의미적으로 동등한 surgical 제거다. 삭제 후 위/아래 보존 블록 사이에 빈 줄 1개만 남도록 한다.

**G. Step 2 — VALID_EXTRAS inner case 축소 + dropped reject (D-05, D-07):**
PATTERNS.md §2.F에 기술된 inner case
```bash
ssc|4ls|dspm|sail|5why|analyze) VALID_EXTRAS="${VALID_EXTRAS} ${EC_LC}" ;;
```
의 패턴을 plan 42-01과 동일한 방식으로 축소:

```bash
case "$EC_LC" in
  ssc|dspm|analyze) VALID_EXTRAS="${VALID_EXTRAS} ${EC_LC}" ;;
  4ls|sail|5why)
    printf "Lens '%s' is no longer supported (removed in v2.9).\nAvailable lenses: ssc, dspm, analyze.\nRun without lens argument to use smart default (dspm+ssc).\n" "$EC_LC" >&2
    exit 1
    ;;
  *)
    printf "Lens '%s' is no longer supported (removed in v2.9).\nAvailable lenses: ssc, dspm, analyze.\nRun without lens argument to use smart default (dspm+ssc).\n" "$EC_LC" >&2
    exit 1
    ;;
esac
```

이로써 `sg-retro 42 dspm sail` 같은 multi-arg가 첫 dropped 발견 즉시 거부된다 (D-07).

**H. Step 2 — Smart default 신규 분기 (D-02):**
LENS_CODES_ARRAY 구성 if-elif 직후, Step 3로 넘어가기 전 위치에 다음 분기를 추가:

```bash
# Phase 42 (D-02): smart default — no args → dspm+ssc, dspm first (technical core → behavior recommendation)
if [ -z "$LENS_CODES_ARRAY" ] && [ -z "$LENS_RAW" ] && [ -z "$EXTRA_LENS_CODES" ]; then
  LENS_CODES_ARRAY="dspm ssc"
fi
```

stderr 안내 로그 출력은 plan 42-01과 동일하게 **출력하지 않음** (잡음 최소화, 두 파일 의미적 동등 보장).

**I. macOS 셸 이식성 (CLAUDE.md):**
- `grep -P` 금지. `printf` + `>&2` 패턴만 사용.
- 변수 확장은 항상 큰따옴표.
- Step 1 STATE.md parsing 블록은 절대 손대지 않는다.
  </action>
  <verify>
    <automated>
# A) frontmatter description + argument-hint 업데이트 검증
grep -q 'three lenses (ssc, dspm, analyze)' .agents/skills/sg-retro/SKILL.md && \
grep -q 'smart default (dspm+ssc)' .agents/skills/sg-retro/SKILL.md && \
grep -qE 'argument-hint:.*ssc\|dspm\|analyze' .agents/skills/sg-retro/SKILL.md && \
! grep -q 'six lenses' .agents/skills/sg-retro/SKILL.md && \
! grep -qE 'argument-hint:.*ssc\|4ls\|dspm\|sail\|5why\|analyze' .agents/skills/sg-retro/SKILL.md && \

# B) Step 2 case-statement — ssc/dspm/analyze 보존
grep -c '^\s*ssc)\s*LENS_CODE="ssc"' .agents/skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \
grep -c '^\s*dspm)\s*LENS_CODE="dspm"' .agents/skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \
grep -c '^\s*analyze)\s*LENS_CODE="analyze"' .agents/skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \

# C) dropped lens 줄 제거 (case-statement)
! grep -E '^\s*4ls\)\s*LENS_CODE=' .agents/skills/sg-retro/SKILL.md && \
! grep -E '^\s*sail\)\s*LENS_CODE=' .agents/skills/sg-retro/SKILL.md && \
! grep -E '^\s*5why\)\s*LENS_CODE=' .agents/skills/sg-retro/SKILL.md && \

# D) dropped reject 메시지 존재 (plan 42-01과 byte-identical)
grep -q "no longer supported (removed in v2.9)" .agents/skills/sg-retro/SKILL.md && \
grep -q "Available lenses: ssc, dspm, analyze" .agents/skills/sg-retro/SKILL.md && \
grep -q "Run without lens argument to use smart default (dspm+ssc)" .agents/skills/sg-retro/SKILL.md && \

# E) Numbered-list + map_num_to_code 완전 삭제
! grep -qE '^\s*1\) ssc' .agents/skills/sg-retro/SKILL.md && \
! grep -qE '^\s*2\) 4ls' .agents/skills/sg-retro/SKILL.md && \
! grep -qE '^\s*4\) sail' .agents/skills/sg-retro/SKILL.md && \
! grep -q 'map_num_to_code' .agents/skills/sg-retro/SKILL.md && \

# F) Smart default 분기 신규 삽입
grep -q 'LENS_CODES_ARRAY="dspm ssc"' .agents/skills/sg-retro/SKILL.md && \
grep -q 'D-02.*smart default' .agents/skills/sg-retro/SKILL.md && \

# G) VALID_EXTRAS inner case 축소
grep -qE 'ssc\|dspm\|analyze\) VALID_EXTRAS=' .agents/skills/sg-retro/SKILL.md && \
! grep -qE 'ssc\|4ls\|dspm\|sail\|5why\|analyze\) VALID_EXTRAS=' .agents/skills/sg-retro/SKILL.md && \

# H) <constraints> 블록 보존
grep -q '<constraints>' .agents/skills/sg-retro/SKILL.md && \
grep -q '</constraints>' .agents/skills/sg-retro/SKILL.md && \

# I) Step 1 STATE.md parsing 블록 보존 (.agents/ 버전은 부속 텍스트 없음)
grep -q 'BEGIN STATE.md Phase parsing block' .agents/skills/sg-retro/SKILL.md && \

echo "Task 1 (.agents) PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'three lenses (ssc, dspm, analyze)' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'six lenses' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -cE 'argument-hint:.*ssc\|dspm\|analyze' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -cE 'argument-hint:.*ssc\|4ls\|dspm\|sail\|5why\|analyze' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -E '^\s*4ls\)' .agents/skills/sg-retro/SKILL.md` returns no match
    - Source: `grep -E '^\s*sail\)' .agents/skills/sg-retro/SKILL.md` returns no match
    - Source: `grep -E '^\s*5why\)' .agents/skills/sg-retro/SKILL.md` returns no match
    - Source: `grep -c "no longer supported (removed in v2.9)" .agents/skills/sg-retro/SKILL.md` returns ≥ 1
    - Source: `grep -c 'LENS_CODES_ARRAY="dspm ssc"' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'map_num_to_code' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -cE '^\s*[1-6]\) (ssc|4ls|dspm|sail|5why|analyze)' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -c '<constraints>' .agents/skills/sg-retro/SKILL.md` returns `1` AND `grep -c '</constraints>' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'BEGIN STATE.md Phase parsing block' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Behavior assertion (Task 3 검증): plan 42-01과 동일한 dropped lens 에러 문자열을 emit (byte-identical)
  </acceptance_criteria>
  <done>
frontmatter `description`+`argument-hint`가 3-lens + smart default 안내로 교체되었고, Step 2의 case-statement는 ssc/dspm/analyze만 매칭하며 4ls/sail/5why와 미지 코드는 stderr 에러 + exit 1로 거부된다. Numbered-list 출력 블록과 `map_num_to_code()` 헬퍼가 완전히 삭제되었고, Smart default 분기(`LENS_CODES_ARRAY="dspm ssc"`)가 신규 삽입되었다. VALID_EXTRAS inner case도 3-lens + dropped reject로 축소되었다. `<constraints>` 블록과 Step 1 STATE.md parsing 블록은 byte-identical로 보존되었다. 에러 메시지 본문은 plan 42-01과 byte-identical이다.
  </done>
</task>

<task type="auto">
  <name>Task 2: Step 5 sub-block 삭제 + Step 6 LENS_NAME 축소 + <lens_templates> 3개 템플릿 삭제 + success_criteria 재작성</name>
  <files>.agents/skills/sg-retro/SKILL.md</files>
  <read_first>
    - .agents/skills/sg-retro/SKILL.md (Task 1 변경 적용 후 — Step 5 223-259 + Step 6 271-278 + <lens_templates> 331-475 + success_criteria 477-485)
    - .planning/phases/42-smart-default-lens/42-CONTEXT.md (D-05, D-06, D-07)
    - .planning/phases/42-smart-default-lens/42-PATTERNS.md §2.G, §2.H, §2.I, §2.J
    - skills/sg-retro/SKILL.md (plan 42-01 변경 후 — success_criteria 재작성 본문의 의미적 동등 참조)
  </read_first>
  <action>
**A. Step 5 — Sub-block 삭제 (D-05):**
Step 5의 lens facilitation sub-block에서 다음 세 블록을 **완전히 삭제**한다 (PATTERNS.md §2.G):

1. **`4ls` sub-block** (lines ~227-229) — Liked/Learned/Lacked/Longed For + facilitation 본문.
2. **`sail` sub-block** (lines ~235-237) — Wind/Anchor/Sun/Rock + facilitation.
3. **`5why` sub-block** (lines ~239-248) — Problem Statement + Why 1~5 + Root Cause + 7-step AskUserQuestion 절차. (주의: `.agents/` 버전은 AskUserQuestion-free constraint와 충돌하지만 본 phase는 이 충돌을 단순히 sub-block 삭제로 해소.)

**보존:**
- `ssc` sub-block (lines 223-225)
- `dspm` sub-block + **DSPM explicit guard** (lines 231-233) — byte-identical 유지.
- `analyze` sub-block + `ANALYZE_LENS_RAN=true` flag (lines 250-259) — byte-identical 유지.

**B. Step 6 — LENS_NAME case-statement 축소 (D-05):**
PATTERNS.md §2.H에 기술된 LENS_NAME 매핑(lines 271-278)에서 `4ls)`/`sail)`/`5why)` 세 줄을 삭제하여 다음 3-케이스만 남긴다:

```bash
case "$LENS_CODE" in
  ssc)     LENS_NAME="Start/Stop/Continue" ;;
  dspm)    LENS_NAME="Decisions/Surprises/Patterns/Mistakes" ;;
  analyze) LENS_NAME="Conversation Analyzer" ;;
esac
```

들여쓰기·공백 패딩은 기존과 동일 유지 (가장 긴 토큰 `analyze`(7자)에 맞춘 정렬).

**C. `<lens_templates>` 블록에서 3개 lens 템플릿 삭제 (D-05) — `.agents/` 고유:**
PATTERNS.md §2.I에 기술된 다음 세 마크다운 템플릿 블록을 **완전히 삭제**한다:

1. **`4Ls (4ls)` 템플릿** (lines ~353-374) — 헤더 + 4-카테고리 마크다운 템플릿.
2. **`Sailboat (sail)` 템플릿** (lines ~399-420) — 헤더 + Wind/Anchor/Sun/Rock 템플릿.
3. **`Five Whys (5why)` 템플릿** (lines ~422-452) — 헤더 + Why 1~5 + Root Cause 템플릿.

**보존:**
- `Start/Stop/Continue (ssc)` 템플릿 블록
- `Decisions/Surprises/Patterns/Mistakes (dspm)` 템플릿 블록
- `Conversation Analyzer (analyze)` 템플릿 블록

`<lens_templates>` 블록 전체(`<lens_templates>` ~ `</lens_templates>` 태그 자체)는 보존하되, 그 안에서 3개 템플릿만 제거. 삭제 후 인접 보존 템플릿 사이에 빈 줄 1개만 남도록 정리(PATTERNS.md §2.I 가이드).

**D. `<success_criteria>` 재작성 (D-02, D-05, D-06, D-07):**
PATTERNS.md §2.J 표에 따라 `.agents/` 버전의 7개 항목 중 다음을 수정:

- **#2** (현재: "second argument is `ssc`/`4ls`/`dspm`/`sail`/`5why`/`analyze`, skip the numbered list")
  → 재작성:
  ```
  Second argument (if provided) is one of `ssc`/`dspm`/`analyze` (case-insensitive). When omitted along with extra-lens arguments, smart default `LENS_CODES_ARRAY="dspm ssc"` applies automatically (dspm first, then ssc) without prompting.
  ```

- **#3** (현재: "If no argument or unrecognized input, output the numbered list...")
  → 재작성 (numbered-list 삭제 + dropped reject 동작 명시):
  ```
  No argument → smart default (dspm+ssc) applies without prompting. Removed lens codes (`4ls`/`sail`/`5why`) or any unknown code emit a stderr error message containing "no longer supported (removed in v2.9)" and exit 1 without creating or appending to a lessons file. Multi-lens invocations reject on the first dropped code encountered — no partial execution (D-07).
  ```

**손대지 않는 항목 (보존):** #1, #4, #5, #6, #7 (dropped lens 미참조).

**E. 추가 신설 항목 — plan 42-01과 의미적 동등:**
`.agents/` 버전의 success_criteria는 7개이고 plan 42-01의 `skills/` 버전은 11개로 항목 수가 원래 다르다 (PATTERNS.md `<lens_templates>` 차이 참조). **항목 수 동기화는 시도하지 않는다** — 의미적 동등성만 보장하면 충분. #2/#3 재작성으로 smart default + dropped reject 동작이 명시되었으므로 추가 신설 불필요.

**F. 손대지 말 것:**
- Step 1 STATE.md parsing 블록 (byte-identical)
- `<constraints>` 블록 (byte-identical)
- `<language>` 블록
- Step 3, 4, 6 (LENS_NAME 매핑 외 줄들)
- Step 5의 ssc/dspm/analyze sub-block 본문 + DSPM explicit guard
- `<lens_templates>` 블록 안의 ssc/dspm/analyze 템플릿 본문
- `<success_criteria>` 항목 #1, #4, #5, #6, #7
- frontmatter `name` 필드 + (Task 1에서 이미 수정된) `description`/`argument-hint`
- `<objective>` 블록의 lens-언급 외 본문
  </action>
  <verify>
    <automated>
# A) Step 5 sub-block 본문 키워드 부재
! grep -qE '^### Lens: 4Ls' .agents/skills/sg-retro/SKILL.md && \
! grep -qE '^### Lens: Sailboat' .agents/skills/sg-retro/SKILL.md && \
! grep -qE '^### Lens: Five Whys' .agents/skills/sg-retro/SKILL.md && \
! grep -qE 'Wind.*Anchor.*Sun.*Rock' .agents/skills/sg-retro/SKILL.md && \
! grep -qE 'Why 1.*Why 2.*Why 3' .agents/skills/sg-retro/SKILL.md && \

# B) Step 5 ssc/dspm/analyze sub-block 보존
grep -qE 'Start/Stop/Continue' .agents/skills/sg-retro/SKILL.md && \
grep -qE 'Decisions/Surprises/Patterns/Mistakes' .agents/skills/sg-retro/SKILL.md && \
grep -qE 'Conversation Analyzer' .agents/skills/sg-retro/SKILL.md && \
grep -q 'ANALYZE_LENS_RAN=true' .agents/skills/sg-retro/SKILL.md && \
grep -q 'Explicit guard' .agents/skills/sg-retro/SKILL.md && \

# C) Step 6 LENS_NAME 3-케이스만
grep -c '^\s*ssc)\s*LENS_NAME="Start/Stop/Continue"' .agents/skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \
grep -c '^\s*dspm)\s*LENS_NAME="Decisions/Surprises/Patterns/Mistakes"' .agents/skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \
grep -c '^\s*analyze)\s*LENS_NAME="Conversation Analyzer"' .agents/skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \
! grep -qE '^\s*4ls\)\s*LENS_NAME=' .agents/skills/sg-retro/SKILL.md && \
! grep -qE '^\s*sail\)\s*LENS_NAME=' .agents/skills/sg-retro/SKILL.md && \
! grep -qE '^\s*5why\)\s*LENS_NAME=' .agents/skills/sg-retro/SKILL.md && \

# D) <lens_templates> 블록 자체 보존
grep -q '<lens_templates>' .agents/skills/sg-retro/SKILL.md && \
grep -q '</lens_templates>' .agents/skills/sg-retro/SKILL.md && \

# E) <lens_templates> 내 3개 dropped 템플릿 삭제 (헤더 + 본문 키워드 부재)
! grep -qE '4Ls \(4ls\)' .agents/skills/sg-retro/SKILL.md && \
! grep -qE 'Sailboat \(sail\)' .agents/skills/sg-retro/SKILL.md && \
! grep -qE 'Five Whys \(5why\)' .agents/skills/sg-retro/SKILL.md && \

# F) <lens_templates> 내 3개 유지 템플릿 보존
grep -qE 'Start/Stop/Continue \(ssc\)' .agents/skills/sg-retro/SKILL.md && \
grep -qE 'Decisions/Surprises/Patterns/Mistakes \(dspm\)' .agents/skills/sg-retro/SKILL.md && \
grep -qE 'Conversation Analyzer \(analyze\)' .agents/skills/sg-retro/SKILL.md && \

# G) success_criteria 재작성
grep -q 'smart default' .agents/skills/sg-retro/SKILL.md && \
grep -q 'no partial execution' .agents/skills/sg-retro/SKILL.md && \
! grep -q 'output the numbered list' .agents/skills/sg-retro/SKILL.md && \

# H) <success_criteria> 블록 보존
test "$(grep -c '<success_criteria>' .agents/skills/sg-retro/SKILL.md)" = "1" && \
test "$(grep -c '</success_criteria>' .agents/skills/sg-retro/SKILL.md)" = "1" && \

echo "Task 2 (.agents) PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '^### Lens: 4Ls' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -c '^### Lens: Sailboat' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -c '^### Lens: Five Whys' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -E '^\s*4ls\)\s*LENS_NAME=' .agents/skills/sg-retro/SKILL.md` returns no match
    - Source: `grep -E '^\s*sail\)\s*LENS_NAME=' .agents/skills/sg-retro/SKILL.md` returns no match
    - Source: `grep -E '^\s*5why\)\s*LENS_NAME=' .agents/skills/sg-retro/SKILL.md` returns no match
    - Source: `grep -cE '4Ls \(4ls\)' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -cE 'Sailboat \(sail\)' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -cE 'Five Whys \(5why\)' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -cE 'Start/Stop/Continue \(ssc\)' .agents/skills/sg-retro/SKILL.md` returns ≥ 1
    - Source: `grep -cE 'Decisions/Surprises/Patterns/Mistakes \(dspm\)' .agents/skills/sg-retro/SKILL.md` returns ≥ 1
    - Source: `grep -cE 'Conversation Analyzer \(analyze\)' .agents/skills/sg-retro/SKILL.md` returns ≥ 1
    - Source: `grep -c '<lens_templates>' .agents/skills/sg-retro/SKILL.md` returns `1` AND `grep -c '</lens_templates>' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '<constraints>' .agents/skills/sg-retro/SKILL.md` returns `1` AND `grep -c '</constraints>' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'output the numbered list' .agents/skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -c 'ANALYZE_LENS_RAN=true' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'Explicit guard' .agents/skills/sg-retro/SKILL.md` returns ≥ 1
    - Test command: `wc -l .agents/skills/sg-retro/SKILL.md` returns a value strictly less than `485`
  </acceptance_criteria>
  <done>
Step 5의 4ls/sail/5why sub-block + Step 6의 LENS_NAME 매핑 3-케이스 축소 + `<lens_templates>`의 3개 dropped 템플릿(4Ls/Sailboat/Five Whys) 삭제 + `<success_criteria>` #2/#3 재작성이 완료되었다. `<constraints>`/Step 1 STATE.md parsing 블록/ssc/dspm/analyze sub-block + 템플릿 + DSPM explicit guard + `ANALYZE_LENS_RAN=true`는 byte-identical로 보존되었다.
  </done>
</task>

<task type="auto">
  <name>Task 3: pairwise 의미적 동등성 검증 (D-10, D-12 시나리오 6) + D-12 시나리오 1-5 .agents 측 검증</name>
  <files></files>
  <read_first>
    - .agents/skills/sg-retro/SKILL.md (Task 1+2 변경 적용 후)
    - skills/sg-retro/SKILL.md (plan 42-01 완료 후 상태)
    - .planning/phases/42-smart-default-lens/42-CONTEXT.md (D-10, D-12)
  </read_first>
  <action>
**A. D-12 시나리오 6 — Pairwise 의미적 동등성 검증:**

두 파일의 **의미 단위**가 동일하게 적용되었는지 명시 키워드 grep으로 검증한다. byte-identical은 요구하지 않는다 — 구조 차이(`<constraints>`/`<lens_templates>`/argument-hint/success_criteria 항목 수)는 PATTERNS.md에서 명시적으로 허용된 차이다.

**핵심 의미 단위 6개 (양쪽 파일에서 동일 키워드 존재 확인):**

```bash
# 1. Smart default 분기 — 동일 한 줄 (LENS_CODES_ARRAY="dspm ssc")
test "$(grep -c 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md)" = "1" && \
test "$(grep -c 'LENS_CODES_ARRAY="dspm ssc"' .agents/skills/sg-retro/SKILL.md)" = "1" && \

# 2. Smart default 진입 주석 마커
test "$(grep -c 'D-02.*smart default' skills/sg-retro/SKILL.md)" = "1" && \
test "$(grep -c 'D-02.*smart default' .agents/skills/sg-retro/SKILL.md)" = "1" && \

# 3. Dropped lens 에러 메시지 (byte-identical 문자열)
grep -q "Lens '%s' is no longer supported (removed in v2.9)" skills/sg-retro/SKILL.md && \
grep -q "Lens '%s' is no longer supported (removed in v2.9)" .agents/skills/sg-retro/SKILL.md && \
grep -q "Available lenses: ssc, dspm, analyze" skills/sg-retro/SKILL.md && \
grep -q "Available lenses: ssc, dspm, analyze" .agents/skills/sg-retro/SKILL.md && \
grep -q "Run without lens argument to use smart default (dspm+ssc)" skills/sg-retro/SKILL.md && \
grep -q "Run without lens argument to use smart default (dspm+ssc)" .agents/skills/sg-retro/SKILL.md && \

# 4. 3-lens 케이스 매핑 (양쪽 동일)
grep -qE '^\s*ssc\)\s*LENS_CODE="ssc"' skills/sg-retro/SKILL.md && \
grep -qE '^\s*ssc\)\s*LENS_CODE="ssc"' .agents/skills/sg-retro/SKILL.md && \
grep -qE '^\s*dspm\)\s*LENS_CODE="dspm"' skills/sg-retro/SKILL.md && \
grep -qE '^\s*dspm\)\s*LENS_CODE="dspm"' .agents/skills/sg-retro/SKILL.md && \
grep -qE '^\s*analyze\)\s*LENS_CODE="analyze"' skills/sg-retro/SKILL.md && \
grep -qE '^\s*analyze\)\s*LENS_CODE="analyze"' .agents/skills/sg-retro/SKILL.md && \

# 5. Dropped lens case 부재 (양쪽 동일)
! grep -qE '^\s*4ls\)\s*LENS_CODE=' skills/sg-retro/SKILL.md && \
! grep -qE '^\s*4ls\)\s*LENS_CODE=' .agents/skills/sg-retro/SKILL.md && \
! grep -qE '^\s*sail\)\s*LENS_CODE=' skills/sg-retro/SKILL.md && \
! grep -qE '^\s*sail\)\s*LENS_CODE=' .agents/skills/sg-retro/SKILL.md && \
! grep -qE '^\s*5why\)\s*LENS_CODE=' skills/sg-retro/SKILL.md && \
! grep -qE '^\s*5why\)\s*LENS_CODE=' .agents/skills/sg-retro/SKILL.md && \

# 6. LENS_NAME 매핑 — 3-케이스만 양쪽 동일
grep -qE '^\s*ssc\)\s*LENS_NAME="Start/Stop/Continue"' skills/sg-retro/SKILL.md && \
grep -qE '^\s*ssc\)\s*LENS_NAME="Start/Stop/Continue"' .agents/skills/sg-retro/SKILL.md
```

**B. D-12 시나리오 1-5 — `.agents/` 측 정적 검증:**
plan 42-01 Task 3과 동일한 시나리오를 `.agents/` 파일에 대해 반복:

- **시나리오 1 (smart default 진입):** `LENS_CODES_ARRAY="dspm ssc"` 신규 분기 존재 + 진입 조건 if 절 존재.
- **시나리오 2 (단일 lens):** `ssc) LENS_CODE="ssc"` 매핑 보존.
- **시나리오 3 (dropped 단독 reject):** `4ls|sail|5why)` 분기 또는 `*)` 분기에 `exit 1` 포함.
- **시나리오 4 (multi-arg 부분 dropped):** VALID_EXTRAS inner case에 dropped 분기 + `exit 1`.
- **시나리오 5 (sg-learn pass-through):** `.agents/skills/sg-learn/SKILL.md` 미변경 (`git diff` 결과 0 줄).

**C. CLAUDE.md "skills/ + .agents/ 쌍 커버" 검증 (Phase 32 Medium-1 컨벤션):**
두 파일 모두에서 의미 단위 6개가 일관되게 적용되었는지 grep으로 확인한 위 (A) 결과가 본 컨벤션 충족의 증거. plan 42-01 Task 3의 시나리오 5에서 `skills/sg-learn` 미변경을 확인했으므로, 본 task에서는 `.agents/skills/sg-learn` 미변경 추가 확인.

```bash
test "$(git diff --stat HEAD -- .agents/skills/sg-learn/SKILL.md 2>/dev/null | wc -l)" = "0"
```

**D. 라인 수 감소 검증:**
`wc -l .agents/skills/sg-retro/SKILL.md` < 485 (sub-block 3개 + numbered-list + map_num_to_code + `<lens_templates>` 3개 템플릿 삭제 효과).

**E. Out of scope 재확인:**
- `skills/sg-retro/SKILL.md`는 plan 42-01의 책임 — 본 plan에서 손대지 않음.
- `skills/sg-learn/SKILL.md` / `.agents/skills/sg-learn/SKILL.md` 미변경 (D-11).
- README/TEAM.md (Phase 44).
- DISPLAY-01/02 (Phase 43).
- `--pick` flag (Phase 43).
  </action>
  <verify>
    <automated>
# A) 의미 단위 6개 양쪽 일치
test "$(grep -c 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md)" = "1" && \
test "$(grep -c 'LENS_CODES_ARRAY="dspm ssc"' .agents/skills/sg-retro/SKILL.md)" = "1" && \
grep -q "no longer supported (removed in v2.9)" skills/sg-retro/SKILL.md && \
grep -q "no longer supported (removed in v2.9)" .agents/skills/sg-retro/SKILL.md && \
grep -q "Available lenses: ssc, dspm, analyze" skills/sg-retro/SKILL.md && \
grep -q "Available lenses: ssc, dspm, analyze" .agents/skills/sg-retro/SKILL.md && \

# B) 시나리오 1-4 .agents 측 정적 검증
grep -A 3 'D-02.*smart default' .agents/skills/sg-retro/SKILL.md | grep -q 'LENS_CODES_ARRAY="dspm ssc"' && \
grep -qE '^\s*ssc\)\s*LENS_CODE="ssc"' .agents/skills/sg-retro/SKILL.md && \
grep -A 4 -E '4ls\|sail\|5why\)' .agents/skills/sg-retro/SKILL.md | grep -q 'exit 1' && \
grep -A 6 -E 'ssc\|dspm\|analyze\) VALID_EXTRAS' .agents/skills/sg-retro/SKILL.md | grep -q 'exit 1' && \

# C) .agents/skills/sg-learn 미변경
test "$(git diff --stat HEAD -- .agents/skills/sg-learn/SKILL.md 2>/dev/null | wc -l)" = "0" && \

# D) 라인 수 감소
test "$(wc -l < .agents/skills/sg-retro/SKILL.md)" -lt 485 && \

echo "Task 3 (.agents) PASS — pairwise equivalence + D-12 scenarios verified"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md` returns `1` AND `grep -c 'LENS_CODES_ARRAY="dspm ssc"' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c "no longer supported (removed in v2.9)" skills/sg-retro/SKILL.md` returns ≥ 1 AND `grep -c "no longer supported (removed in v2.9)" .agents/skills/sg-retro/SKILL.md` returns ≥ 1
    - Source: `grep -c "Available lenses: ssc, dspm, analyze" skills/sg-retro/SKILL.md` returns ≥ 1 AND `grep -c "Available lenses: ssc, dspm, analyze" .agents/skills/sg-retro/SKILL.md` returns ≥ 1
    - Source: `grep -A 4 -E '4ls\|sail\|5why\)' .agents/skills/sg-retro/SKILL.md` output contains `exit 1`
    - Source: `grep -A 6 -E 'ssc\|dspm\|analyze\) VALID_EXTRAS' .agents/skills/sg-retro/SKILL.md` output contains `exit 1`
    - CLI output: `git diff --stat HEAD -- .agents/skills/sg-learn/SKILL.md | wc -l` returns `0`
    - CLI output: `wc -l < .agents/skills/sg-retro/SKILL.md` returns a value strictly less than `485`
    - Behavior assertion: 두 파일의 의미 단위 6개(smart default, dropped reject 메시지, 3-lens case, dropped case 부재, LENS_NAME 3-케이스, VALID_EXTRAS 3-lens) 모두 일관 적용됨
  </acceptance_criteria>
  <done>
두 파일 (`skills/sg-retro/SKILL.md`와 `.agents/skills/sg-retro/SKILL.md`)이 의미적으로 동등하게 변경되었음이 grep으로 확인되었다. 구조적 차이(`<constraints>`/`<lens_templates>`/argument-hint/success_criteria 항목 수)는 PATTERNS.md에서 명시적으로 허용된 차이로 유지된다. `.agents/skills/sg-learn/SKILL.md`는 변경되지 않았다(D-11). 라인 수는 485보다 작게 감소했다. D-12 시나리오 1-5의 `.agents/` 측 정적 검증이 모두 통과한다. D-12 시나리오 6(pairwise diff) 검증이 완료된다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 인자 → Bash 변수 | `$LENS_RAW` / `$EXTRA_LENS_CODES`는 Skill 호출 시 인자에서 유래. 내부 신뢰 영역. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02 | (N/A) | .agents/skills/sg-retro/SKILL.md text edits | accept | Threat model: N/A for SKILL.md text edits. No new external input surfaces introduced. Phase modifies only local skill markdown files consumed by Codex/Gemini/Antigravity CLI runtimes. No auth/credential/PII surfaces. |
</threat_model>

<verification_steps>
## D-12 검증 시나리오 (Task 3에서 실행)

1. **Smart default 자동 진입 (.agents 측)** — `Skill(skill="sg-retro", args="42")` (Codex/Gemini 환경): numbered-list 출력 없음, lessons 파일에 dspm → ssc 순서로 lens 헤더 append.
2. **단일 lens 인자 (.agents 측)** — `Skill(skill="sg-retro", args="42 ssc")`: ssc 단독 실행.
3. **Dropped lens 단독 인자 (.agents 측)** — `Skill(skill="sg-retro", args="42 sail")`: stderr에 plan 42-01과 byte-identical 에러 메시지, exit 1, lessons 미생성.
4. **Multi-lens 부분 dropped (.agents 측)** — `Skill(skill="sg-retro", args="42 dspm sail")`: 첫 dropped 발견 즉시 거부, dspm 실행 없음.
5. **sg-learn pass-through (.agents 측)** — `.agents/skills/sg-learn/SKILL.md` 미변경 확인.
6. **Pairwise diff (D-10, D-12 시나리오 6)** — 두 파일의 의미 단위 6개(smart default 분기, dropped 에러 메시지, 3-lens case, dropped case 부재, LENS_NAME 3-케이스, VALID_EXTRAS 3-lens reject) 양쪽 일치 확인.
</verification_steps>

<success_criteria>
- `.agents/skills/sg-retro/SKILL.md`에서 lens 코드 `4ls`/`sail`/`5why`가 case-statement, LENS_NAME 매핑, sub-block, `<lens_templates>` 템플릿, frontmatter description/argument-hint, success_criteria 본문에서 모두 제거되었다.
- Smart default 분기(`LENS_CODES_ARRAY="dspm ssc"`)가 D-02 조건 하에 동작하도록 신규 삽입되었다.
- Numbered-list 출력 블록과 `map_num_to_code()` 헬퍼가 완전히 삭제되었다 (D-08, plan 42-01의 AskUserQuestion 삭제와 의미적 동등).
- Dropped lens 인자에 대해 stderr 에러 메시지 + exit 1 동작이 case-statement와 VALID_EXTRAS inner case 양쪽에 일관되게 구현되었다 (D-06, D-07). 에러 메시지 본문은 plan 42-01과 byte-identical이다.
- `<constraints>` 블록과 Step 1 STATE.md parsing 블록(부속 텍스트 차이는 그대로 유지)이 byte-identical로 보존되었다.
- Step 5의 ssc/dspm/analyze sub-block 본문, `<lens_templates>` 내 ssc/dspm/analyze 템플릿, DSPM explicit guard, `ANALYZE_LENS_RAN=true` flag가 모두 byte-identical로 보존되었다.
- `<success_criteria>` 항목 #1/#4/#5/#6/#7이 byte-identical로 보존되었고, #2/#3이 새 동작(smart default + 3-lens + dropped reject)을 정확히 반영하도록 재작성되었다.
- `.agents/skills/sg-learn/SKILL.md`는 변경되지 않았다 (D-11 thin pass-through 보존).
- 두 파일(`skills/sg-retro/SKILL.md`와 `.agents/skills/sg-retro/SKILL.md`)의 의미 단위 6개가 일관되게 적용되었다 (D-10 + D-12 시나리오 6 통과).
</success_criteria>

<output>
Create `.planning/phases/42-smart-default-lens/42-02-SUMMARY.md` when done.

**중요 (D-10 staging 제약):** 본 plan(42-02)과 plan 42-01은 동일 commit에 staged 되어야 한다. 단독 commit 금지. GSD execute-plan은 phase 단위 commit을 사용하므로 자연스럽게 충족되지만, 실행자는 자신의 task가 끝나도 `git commit`을 단독으로 호출하지 않는다.
</output>
