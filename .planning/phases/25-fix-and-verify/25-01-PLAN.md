---
phase: 25-fix-and-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-complete/SKILL.md
  - skills/sg-execute/SKILL.md
  - skills/sg-explore/SKILL.md
  - skills/sg-health/SKILL.md
  - skills/sg-learn/SKILL.md
  - skills/sg-lessons/SKILL.md
  - skills/sg-new/SKILL.md
  - skills/sg-parallel-execute/SKILL.md
  - skills/sg-plan/SKILL.md
  - skills/sg-quick/SKILL.md
  - skills/sg-retro/SKILL.md
  - skills/sg-review/SKILL.md
  - skills/sg-ship/SKILL.md
  - skills/sg-start/SKILL.md
  - skills/sg-status/SKILL.md
  - skills/sg-ui-plan/SKILL.md
  - skills/sg-update/SKILL.md
  - .planning/phases/25-fix-and-verify/25-VERIFICATION.md
autonomous: true
requirements:
  - QUAL-06

must_haves:
  truths:
    - "17개 SKILL.md의 description 필드가 모두 'Use this when [상황] — [동작].' 패턴으로 교체된다 (D-01, D-03)"
    - "각 description은 단일 줄이다 — 멀티라인 YAML 없음 (D-02)"
    - "트리거 표현이 상황 기반이다 — 사용자 발화 예시(execute, run 등)가 없다 (D-04)"
    - "삭제 전 sg-retro <process> Step 5와 lens_templates 내용 중복 여부를 확인한다 (D-09)"
    - "sg-retro SKILL.md에서 378~534행 <lens_templates> 블록(157줄)이 삭제된다 (D-07)"
    - "삭제 후 sg-retro가 391줄 이하가 된다 (D-08)"
    - "25-VERIFICATION.md에 17개 스킬의 수정 전/후 description 비교 테이블이 생성된다 (D-05, D-06)"
  artifacts:
    - path: "skills/sg-complete/SKILL.md"
      provides: "QUAL-02 GOOD 등급 description"
      contains: "Use this when"
    - path: "skills/sg-retro/SKILL.md"
      provides: "500줄 이하로 축소된 sg-retro"
      max_lines: 391
    - path: ".planning/phases/25-fix-and-verify/25-VERIFICATION.md"
      provides: "수정 전/후 비교 테이블"
      contains: "POOR → GOOD"
  key_links:
    - from: "description 필드"
      to: "skill-creator 트리거링 기준"
      via: "Use this when 패턴"
      pattern: "^description: Use this when"
---

<objective>
Phase 24에서 감사된 17개 SKILL.md의 QUAL-02 이슈(description POOR/FAIR)를 수정하고, sg-retro의 중복 블록을 삭제하며, 검증 문서를 생성한다.

Purpose: skill-creator 기준의 상황 기반 description으로 교체하여 Claude의 undertrigger 위험을 제거한다.
Output: 17개 수정된 SKILL.md + 삭제된 <lens_templates> 블록 + 25-VERIFICATION.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-fix-and-verify/25-CONTEXT.md
@.planning/phases/24-skills/24-SUMMARY.md

<interfaces>
<!-- 수정 대상 description 현재값 — Phase 24 감사 결과 -->
<!-- 이 값들은 수정 전(before) 값이며 acceptance_criteria 검증에 사용된다 -->

현재 description (BEFORE):
- sg-complete:        "Complete the current milestone — invokes gsd-complete-milestone Skill."
- sg-execute:         "Hand off the current GSD phase to Superpowers — package PLAN/REQ/SC into a single prompt and auto-invoke superpowers:executing-plans."
- sg-explore:         "Explore and map the codebase — invokes gsd-map-codebase Skill."
- sg-health:          "Diagnose super-gsd installation status — GSD, Superpowers, Hookify (optional), hooks, HANDOFF.md, STATE.md"
- sg-learn:           "Run a retrospective via sg-retro to extract patterns and generate hooks from this session."
- sg-lessons:         "List prior lessons from .planning/lessons/ (written by sg-retro) and inject them as context for the next GSD phase."
- sg-new:             "Start a new milestone — invokes gsd-new-milestone Skill."
- sg-parallel-execute:"Reads parallel_groups.json and dispatches up to 3 parallel Task() agents — one per independent group — to execute PLAN.md tasks directly without calling superpowers:executing-plans."
- sg-plan:            "Gather context (injects .planning/lessons/) and create a phase plan — chains gsd-discuss-phase → gsd-plan-phase automatically."
- sg-quick:           "Execute a small, ad-hoc task with GSD guarantees (atomic commits, STATE.md tracking). Quick mode for one-off tasks that don't need a full phase plan."
- sg-retro:           "Run a structured retrospective on a GSD phase with one of six lenses ..."
- sg-review:          "Request a code review via Superpowers — derives git range, collects description, then invokes superpowers:requesting-code-review Skill."
- sg-ship:            "Complete and ship the current milestone — invokes gsd-ship Skill."
- sg-start:           "Start or resume a project — detects existing session, prompts Resume / Start new milestone / Cancel; falls back to gsd-new-project when no session is detected."
- sg-status:          "Show the current super-gsd workflow stage, last handoff timestamp, and the next recommended command."
- sg-ui-plan:         "Run UI design brainstorming for a phase — resolves phase context from ROADMAP.md and invokes superpowers:brainstorming."
- sg-update:          "Check, install, or update GSD, superpowers, and super-gsd to their latest versions."

목표 description (AFTER) — D-03 포맷 "Use this when [상황] — [동작].":
- sg-complete:        "Use this when the current milestone is done and needs to be closed — invokes gsd-complete-milestone to finalize and archive the milestone."
- sg-execute:         "Use this when the phase plan is ready and implementation should begin — packages PLAN/REQ/SC and hands off to Superpowers via superpowers:executing-plans."
- sg-explore:         "Use this when you need a map of the current codebase structure — invokes gsd-map-codebase to analyse and document the project layout."
- sg-health:          "Use this when something feels broken or before onboarding — diagnoses GSD, Superpowers, hooks, HANDOFF.md, and STATE.md installation line by line."
- sg-learn:           "Use this when a phase is complete and you want to extract patterns and lessons from the session — delegates to sg-retro for structured retrospective."
- sg-lessons:         "Use this when starting a new phase and you want to review prior lessons before planning — lists weighted lessons from .planning/lessons/ as context."
- sg-new:             "Use this when a milestone is complete and a new one should begin — invokes gsd-new-milestone to set up the next milestone."
- sg-parallel-execute:"Use this when parallel_groups.json exists and independent plan groups should run concurrently — dispatches up to 3 Task() agents, one per group, without calling superpowers:executing-plans."
- sg-plan:            "Use this when a new phase needs to be planned — injects prior lessons, then chains gsd-discuss-phase and gsd-plan-phase automatically."
- sg-quick:           "Use this when a small ad-hoc task needs to be done without a full phase plan — runs gsd-planner then superpowers:executing-plans with atomic commits and STATE.md tracking."
- sg-retro:           "Use this when a phase is complete and a structured retrospective is needed — collects phase artifacts and git context, then facilitates one or more of six lenses (ssc, 4ls, dspm, sail, 5why, analyze) and appends results to .planning/lessons/."
- sg-review:          "Use this when implementation is complete and a code review is needed — derives the git range automatically and invokes superpowers:requesting-code-review."
- sg-ship:            "Use this when the milestone is ready to be shipped — resolves the current phase and invokes gsd-ship to complete delivery."
- sg-start:           "Use this when starting or resuming work on a project — detects an existing session via STATE.md and prompts Resume, Start new milestone, or Cancel; falls back to gsd-new-project if no session exists."
- sg-status:          "Use this when you want to know where you are in the workflow — reads HANDOFF.md and STATE.md to show the current stage and the next recommended command."
- sg-ui-plan:         "Use this when UI design brainstorming is needed before planning a phase — reads phase context from ROADMAP.md and runs superpowers:brainstorming."
- sg-update:          "Use this when GSD, Superpowers, or super-gsd may be outdated — checks each tool's installation status and installs or updates to the latest version."

sg-retro 삭제 대상:
- 378행: <lens_templates>
- 379-533행: 6개 렌즈 마크다운 스켈레톤
- 534행: </lens_templates>
- 총 157줄 삭제 → 548 - 157 = 391줄
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 17개 SKILL.md description 일괄 rewrite</name>
  <files>
    skills/sg-complete/SKILL.md,
    skills/sg-execute/SKILL.md,
    skills/sg-explore/SKILL.md,
    skills/sg-health/SKILL.md,
    skills/sg-learn/SKILL.md,
    skills/sg-lessons/SKILL.md,
    skills/sg-new/SKILL.md,
    skills/sg-parallel-execute/SKILL.md,
    skills/sg-plan/SKILL.md,
    skills/sg-quick/SKILL.md,
    skills/sg-retro/SKILL.md,
    skills/sg-review/SKILL.md,
    skills/sg-ship/SKILL.md,
    skills/sg-start/SKILL.md,
    skills/sg-status/SKILL.md,
    skills/sg-ui-plan/SKILL.md,
    skills/sg-update/SKILL.md
  </files>
  <read_first>
    각 파일을 수정 전에 반드시 Read 도구로 읽는다. description 필드는 YAML frontmatter 2~3번째 줄에 위치한다.
    수정 범위: description 값만 교체. name, argument-hint, <objective>, <process>, <success_criteria> 블록은 일절 변경하지 않는다.
  </read_first>
  <action>
    각 SKILL.md의 YAML frontmatter에서 `description:` 줄만 아래의 목표값으로 교체한다. 줄 수, 들여쓰기, 따옴표 없음 형식을 그대로 유지한다 (D-01, D-02, D-03, D-04).

    교체 대상 (파일 → 새 description 값):

    skills/sg-complete/SKILL.md:
      description: Use this when the current milestone is done and needs to be closed — invokes gsd-complete-milestone to finalize and archive the milestone.

    skills/sg-execute/SKILL.md:
      description: Use this when the phase plan is ready and implementation should begin — packages PLAN/REQ/SC and hands off to Superpowers via superpowers:executing-plans.

    skills/sg-explore/SKILL.md:
      description: Use this when you need a map of the current codebase structure — invokes gsd-map-codebase to analyse and document the project layout.

    skills/sg-health/SKILL.md:
      description: Use this when something feels broken or before onboarding — diagnoses GSD, Superpowers, hooks, HANDOFF.md, and STATE.md installation line by line.

    skills/sg-learn/SKILL.md:
      description: Use this when a phase is complete and you want to extract patterns and lessons from the session — delegates to sg-retro for structured retrospective.

    skills/sg-lessons/SKILL.md:
      description: Use this when starting a new phase and you want to review prior lessons before planning — lists weighted lessons from .planning/lessons/ as context.

    skills/sg-new/SKILL.md:
      description: Use this when a milestone is complete and a new one should begin — invokes gsd-new-milestone to set up the next milestone.

    skills/sg-parallel-execute/SKILL.md:
      description: Use this when parallel_groups.json exists and independent plan groups should run concurrently — dispatches up to 3 Task() agents, one per group, without calling superpowers:executing-plans.

    skills/sg-plan/SKILL.md:
      description: Use this when a new phase needs to be planned — injects prior lessons, then chains gsd-discuss-phase and gsd-plan-phase automatically.

    skills/sg-quick/SKILL.md:
      description: Use this when a small ad-hoc task needs to be done without a full phase plan — runs gsd-planner then superpowers:executing-plans with atomic commits and STATE.md tracking.

    skills/sg-retro/SKILL.md:
      description: Use this when a phase is complete and a structured retrospective is needed — collects phase artifacts and git context, then facilitates one or more of six lenses (ssc, 4ls, dspm, sail, 5why, analyze) and appends results to .planning/lessons/.

    skills/sg-review/SKILL.md:
      description: Use this when implementation is complete and a code review is needed — derives the git range automatically and invokes superpowers:requesting-code-review.

    skills/sg-ship/SKILL.md:
      description: Use this when the milestone is ready to be shipped — resolves the current phase and invokes gsd-ship to complete delivery.

    skills/sg-start/SKILL.md:
      description: Use this when starting or resuming work on a project — detects an existing session via STATE.md and prompts Resume, Start new milestone, or Cancel; falls back to gsd-new-project if no session exists.

    skills/sg-status/SKILL.md:
      description: Use this when you want to know where you are in the workflow — reads HANDOFF.md and STATE.md to show the current stage and the next recommended command.

    skills/sg-ui-plan/SKILL.md:
      description: Use this when UI design brainstorming is needed before planning a phase — reads phase context from ROADMAP.md and runs superpowers:brainstorming.

    skills/sg-update/SKILL.md:
      description: Use this when GSD, Superpowers, or super-gsd may be outdated — checks each tool's installation status and installs or updates to the latest version.

    주의: Edit 도구로 description 줄만 교체한다. 파일당 1번의 Edit 호출. 인접 줄(name, argument-hint) 수정 금지.
  </action>
  <verify>
    <automated>grep -r "^description: Use this when" skills/sg-*/SKILL.md | wc -l | tr -d ' ' | grep -q '^17$' && echo "PASS: 17개 모두 Use this when 패턴" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - 17개 파일 모두에서 `grep "^description: Use this when"` 이 1줄 매칭된다
    - `grep "^description:" skills/sg-*/SKILL.md | grep -v "Use this when"` 출력이 없다
    - `grep -c "^description:" skills/sg-complete/SKILL.md` 는 1이다 (단일 줄 유지, D-02)
    - `grep "execute\|run the plan\|실행해"` 가 어떤 description에도 없다 (발화 기반 트리거 없음, D-04)
    - description 변경 외 각 SKILL.md의 줄 수 변화가 없다 (name, argument-hint, 블록 보존)
  </acceptance_criteria>
  <done>17개 SKILL.md의 description이 모두 "Use this when [상황] — [동작]." 패턴 단일 줄로 교체된다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: sg-retro lens_templates 블록 삭제</name>
  <files>skills/sg-retro/SKILL.md</files>
  <read_first>
    수정 전 반드시 Read 도구로 `skills/sg-retro/SKILL.md` 전체를 읽는다.
    삭제 대상 라인 확인:
    - 378행: `<lens_templates>` 태그 줄
    - 534행: `</lens_templates>` 태그 줄
    삭제 전 확인 사항 (D-09): `<process>` Step 5 서브블록(221~267행)과 lens_templates 내용이 실제로 중복인지 확인한다.
    확인 방법: Step 5의 "Fixed subheadings" 목록 6개 렌즈(ssc, 4ls, dspm, sail, 5why, analyze)가 lens_templates의 6개 마크다운 스켈레톤과 1:1 대응하는지 확인한다.
    중복이 확인되면 삭제를 진행한다. 만약 lens_templates에 Step 5에 없는 고유 정보가 있으면 삭제를 중단하고 SUMMARY에 기록한다.
  </read_first>
  <action>
    사전 확인 완료 후, `skills/sg-retro/SKILL.md`에서 378행 `<lens_templates>` 부터 534행 `</lens_templates>` 까지 157줄을 삭제한다 (D-07).

    삭제 방법: Edit 도구를 사용하여 `<lens_templates>` 줄부터 `</lens_templates>` 줄(포함)까지 해당 블록 전체를 제거한다. 337행의 빈 줄(`</process>` 이후 ~ `<lens_templates>` 이전)은 보존한다.

    삭제 후 `</process>` 와 `<success_criteria>` 사이에 불필요한 빈 줄이 2개 이상 남지 않도록 조정한다 (최대 1개 빈 줄 허용).

    수정하지 않는 부분: `</process>` 태그(376행), `<success_criteria>` ~ `</success_criteria>` 블록 전체.
  </action>
  <verify>
    <automated>wc -l skills/sg-retro/SKILL.md | awk '{print $1}' | xargs -I{} python3 -c "n={}; print('PASS: {}줄'.format(n)) if n <= 391 else print('FAIL: {}줄 (391 초과)'.format(n))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lens_templates" skills/sg-retro/SKILL.md` 출력이 0이다
    - `wc -l skills/sg-retro/SKILL.md` 가 391 이하이다
    - `grep -c "success_criteria" skills/sg-retro/SKILL.md` 가 2이다 (여는 태그 + 닫는 태그 보존)
    - `grep -c "Step 5" skills/sg-retro/SKILL.md` 가 1 이상이다 (Step 5 블록이 삭제되지 않음)
  </acceptance_criteria>
  <done>sg-retro SKILL.md에서 lens_templates 블록이 삭제되어 391줄 이하가 된다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 25-VERIFICATION.md 생성</name>
  <files>.planning/phases/25-fix-and-verify/25-VERIFICATION.md</files>
  <read_first>
    `.planning/phases/24-skills/24-SUMMARY.md` 의 QUAL-02 이슈 상세 테이블을 확인한다 (수정 전 값의 출처).
    수정된 17개 SKILL.md에서 각 파일의 현재 description 값을 읽어 수정 후 값으로 사용한다.
  </read_first>
  <action>
    `.planning/phases/25-fix-and-verify/25-VERIFICATION.md`를 새로 생성한다. 내용은 한국어로 작성한다 (CLAUDE.md GSD 문서 작성 지침).

    파일 구조:
    1. 제목 + 날짜
    2. QUAL-02 수정 결과 섹션: 17개 스킬별 수정 전/후 description 4컬럼 테이블 (스킬 슬럿 | 수정 전 | 수정 후 | 등급 변화)
    3. sg-retro 리팩토링 결과 섹션: 삭제 전/후 줄 수 확인 (548줄 → 실제 줄 수)
    4. 결론: QUAL-06 충족 여부

    테이블 형식 (D-05, D-06):

    수정 전 값은 24-SUMMARY.md QUAL-02 테이블에서 가져온다.
    수정 후 값은 Task 1에서 적용된 목표 description 값이다.
    등급 변화는 모두 "POOR → GOOD" (sg-start는 "FAIR → GOOD").

    17행 전체를 포함해야 한다. 생략 없음.
  </action>
  <verify>
    <automated>test -f .planning/phases/25-fix-and-verify/25-VERIFICATION.md && grep -c "POOR → GOOD\|FAIR → GOOD" .planning/phases/25-fix-and-verify/25-VERIFICATION.md | grep -qE "^17$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - 파일이 `.planning/phases/25-fix-and-verify/25-VERIFICATION.md` 경로에 존재한다
    - 파일에 "POOR → GOOD" 문자열이 16회 이상 등장한다 (16개 POOR 스킬)
    - 파일에 "FAIR → GOOD" 문자열이 1회 등장한다 (sg-start)
    - 파일에 17개 스킬 슬럿 이름이 모두 포함된다 (sg-complete, sg-execute, ..., sg-update)
    - 파일에 "Use this when" 패턴의 수정 후 description이 17행 모두에 기재된다
    - sg-retro 리팩토링 결과 섹션이 있고 실제 줄 수가 기재된다
  </acceptance_criteria>
  <done>25-VERIFICATION.md가 생성되어 17개 스킬의 수정 전/후 description 비교 테이블과 sg-retro 리팩토링 결과를 포함한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 파일 시스템 → YAML frontmatter | Edit 도구가 description 줄만 수정해야 하며 인접 필드(name, argument-hint)를 변경하면 skill 동작이 깨진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | YAML frontmatter | mitigate | description 줄만 Edit. 수정 후 grep으로 name/argument-hint 보존 확인 |
| T-25-02 | Tampering | sg-retro process 블록 | mitigate | 삭제 전 Step 5와 lens_templates 중복 비교 확인 (D-09). success_criteria 보존 확인 |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜에는 패키지 설치 없음 |
</threat_model>

<verification>
전체 phase 검증:

1. `grep -r "^description: Use this when" skills/sg-*/SKILL.md | wc -l` → 17
2. `grep -c "lens_templates" skills/sg-retro/SKILL.md` → 0
3. `wc -l skills/sg-retro/SKILL.md` → 391 이하
4. `test -f .planning/phases/25-fix-and-verify/25-VERIFICATION.md && echo EXISTS`
5. `grep -c "POOR → GOOD\|FAIR → GOOD" .planning/phases/25-fix-and-verify/25-VERIFICATION.md` → 17
</verification>

<success_criteria>
1. 17개 SKILL.md 모두에서 description이 "Use this when [상황] — [동작]." 단일 줄 패턴으로 교체된다
2. sg-retro SKILL.md가 391줄 이하가 되고 lens_templates 블록이 존재하지 않는다
3. 25-VERIFICATION.md가 생성되어 17행 비교 테이블을 포함한다
4. QUAL-06: 발견된 문제점(QUAL-02 16건 + sg-retro 500줄 초과 1건)이 모두 수정된다
</success_criteria>

<output>
완료 후 `.planning/phases/25-fix-and-verify/25-01-SUMMARY.md`를 생성한다.
</output>
