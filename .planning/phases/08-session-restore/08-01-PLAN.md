---
phase: 08-session-restore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-start.md
autonomous: false
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04

must_haves:
  truths:
    - "sg-start 실행 시 `.planning/STATE.md` 존재 + `^Phase:` 라인 캡처 가능 여부로 기존 세션을 감지한다 (D-01; SESS-01)"
    - "HANDOFF.md 데이터 행이 0개여도 STATE.md 조건이 충족되면 기존 세션으로 분류된다 (D-02; SESS-01)"
    - "기존 세션 감지 시 5개 라인(Milestone / Phase / Stage / Last activity / Next)을 안내 헤더 뒤에 emit한다 (D-04; SESS-02)"
    - "STATE.md `Phase:` 파싱은 Phase 7 D-04~D-06의 bash 블록을 인라인 복제한다 — `^Phase:[[:space:]]*` 제거 + 끝 공백 trim, `\\S+` 토큰 캡처 금지 (D-03; Phase 7 D-07 lock)"
    - "Stage 표시는 storage→display 매핑(`init|gsd-plan→gsd|superpowers|review→superpowers|hookify|ship|complete`)을 sg-status.md에서 인라인 복제한다 (D-04 Stage 라인)"
    - "Last activity는 HANDOFF.md 마지막 데이터 행의 Timestamp 우선, 없으면 STATE.md frontmatter `last_updated` 또는 `last_activity`, 둘 다 없으면 `(unknown)` (D-06)"
    - "Milestone 표시는 STATE.md frontmatter `milestone` + `milestone_name` 결합 (예: `v1.1 Reliability`); 파싱 실패 시 `(unknown)` (D-07)"
    - "Next 명령은 sg-status.md의 storage 기준 case 블록을 그대로 복제 (D-04 Next 라인; Phase 2 D-28 lock)"
    - "AskUserQuestion 호출 시 header=`Session`, question=`Existing session detected. What do you want to do?`, 3 옵션(`Resume` / `Start new milestone` / `Cancel`) (D-10, D-12)"
    - "Resume 선택 시 5개 라인만 emit하고 종료. 자동 Skill invoke 없음. 사용자가 Next 명령을 직접 실행한다 (D-08, D-09)"
    - "Start new milestone 선택 시 `Skill(skill=\"gsd-new-milestone\", args=\"\")`로 위임 — args는 빈 문자열 (D-14)"
    - "Cancel 선택 시 `Cancelled. No changes made.` 단일 라인 emit 후 종료. 파일 read/write 없음 (D-15)"
    - "세 옵션 모두 HANDOFF.md를 read-only로만 접근 — 삭제/수정/append 없음 (D-16; SESS-04)"
    - "STATE.md가 없거나 `^Phase:` 라인을 캡처할 수 없으면 기존 동작 그대로 `Skill(skill=\"gsd-new-project\", args=\"$ARGUMENTS\")` 호출 (D-17; SESS-03 후방 호환)"
    - "`commands/sg-start.md` 한 파일만 수정한다 — 헬퍼 스크립트, Python 모듈, 테스트 fixture 추가 없음 (Phase 6 D-04 + Phase 7 D-08 lock)"
  artifacts:
    - path: "commands/sg-start.md"
      provides: "sg-start 명령 정의 — 단일 수정 대상"
      contains: "BEGIN STATE.md Phase parsing block"
  key_links:
    - from: "commands/sg-start.md (Phase 파싱 블록)"
      to: ".planning/STATE.md `Phase:` 라인"
      via: "grep + sed 인라인 복제 (Phase 7 D-07 lock)"
      pattern: "sed -E 's/\\^Phase:\\[\\[:space:\\]\\]\\*//'"
    - from: "commands/sg-start.md (Stage 매핑)"
      to: "commands/sg-status.md storage→display case 블록"
      via: "동일 case 블록 인라인 복제 (drift 시 양쪽 동시 수정)"
      pattern: "gsd-plan).*STAGE_DISPLAY=\"gsd\""
    - from: "commands/sg-start.md (Next 분기)"
      to: "commands/sg-status.md `case \"$STAGE_RAW\" in` Next 블록"
      via: "동일 case 블록 인라인 복제 (Phase 2 D-28 lock)"
      pattern: "complete.*sg-new"
    - from: "commands/sg-start.md (AskUserQuestion 옵션 분기)"
      to: "Skill 위임 또는 emit-only 종료"
      via: "if/case 블록 — Resume=emit, Start new milestone=Skill(gsd-new-milestone), Cancel=single line"
      pattern: "Skill\\(skill=\"gsd-new-milestone\""
    - from: "commands/sg-start.md (D-17 fallback 분기)"
      to: "Skill(gsd-new-project) 기존 호출"
      via: "STATE.md 미감지 시 $ARGUMENTS 그대로 전달"
      pattern: "Skill\\(skill=\"gsd-new-project\", args=\"\\$ARGUMENTS\"\\)"
---

<objective>
`commands/sg-start.md` 의 `<process>` 블록을 확장하여, STATE.md/HANDOFF.md를 읽어 기존 세션을 감지하고 AskUserQuestion으로 Resume/Start new milestone/Cancel 3-옵션을 제시한다. Resume이면 5개 라인 안내 후 종료, Start new milestone이면 sg-new-milestone Skill에 위임, Cancel이면 단일 라인 emit 후 종료. STATE.md가 없으면 기존 `gsd-new-project` 호출 동작을 그대로 유지한다.

Purpose:
- SESS-01/02/03/04 충족 — 기존 세션 감지/표시/재개/append-only 감사 로그 보존
- Phase 7 D-07이 사전 lock한 "STATE.md Phase 파싱 블록 인라인 복제" 계약 이행
- `commands/sg-status.md` 의 storage→display 매핑 + Next 명령 case 블록을 동일하게 인라인 복제 (drift 시 양쪽 동시 수정)

Output: `commands/sg-start.md` 한 파일만 수정. 헬퍼 스크립트, Python 모듈, 테스트 fixture, 다른 명령 파일 변경 없음 (Phase 6 D-04 + Phase 7 D-08 lock).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-session-restore/08-CONTEXT.md
@.planning/phases/07-status-accuracy/07-CONTEXT.md
@.planning/phases/07-status-accuracy/07-01-PLAN.md
@.planning/HANDOFF.md
@commands/sg-start.md
@commands/sg-status.md
@commands/sg-new.md

<interfaces>
<!-- sg-status.md 의 인라인 복제 대상 블록. 실행자는 추가 탐색 없이 아래 두 블록을 sg-start.md 에 그대로 복제한다. -->
<!-- drift 시 sg-status.md 와 sg-start.md 를 동시에 수정해야 한다 (Phase 7 D-07 lock). -->

A. STATE.md `Phase:` 파싱 블록 (commands/sg-status.md lines 17-21 그대로):
```bash
# --- BEGIN STATE.md Phase parsing block (D-07: Phase 8 sg-start이 동일 블록을 복제) ---
PHASE_LINE=$(grep -E '^Phase:' .planning/STATE.md 2>/dev/null | head -1 | sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//')
[ -z "$PHASE_LINE" ] && PHASE_LINE="(none)"
PHASE_NUM=$(echo "$PHASE_LINE" | grep -oE '^[0-9]+' || echo "")
# --- END STATE.md Phase parsing block ---
```

B. HANDOFF.md 마지막 데이터 행 + Stage 매핑 블록 (commands/sg-status.md lines 26-48 그대로):
```bash
LAST_ROW=$(grep -E '^\| [0-9]{4}-' .planning/HANDOFF.md 2>/dev/null | tail -1)
if [ -z "$LAST_ROW" ]; then
  STAGE_RAW="init"
  TS=""
else
  STAGE_RAW=$(echo "$LAST_ROW" | awk -F'|' '{gsub(/ /,"",$5); print $5}')
  TS=$(echo "$LAST_ROW" | awk -F'|' '{gsub(/ /,"",$2); print $2}')
  case "$STAGE_RAW" in
    gsd-plan|superpowers|review|hookify|ship|complete) ;;
    *) echo "Unknown stage '${STAGE_RAW}' in .planning/HANDOFF.md last row. Schema may be corrupted." >&2; exit 1 ;;
  esac
fi

# Storage → Display enum mapping (D-01, D-02)
case "$STAGE_RAW" in
  init)         STAGE_DISPLAY="init" ;;
  gsd-plan)     STAGE_DISPLAY="gsd" ;;
  superpowers)  STAGE_DISPLAY="superpowers" ;;
  review)       STAGE_DISPLAY="superpowers" ;;
  hookify)      STAGE_DISPLAY="hookify" ;;
  ship)         STAGE_DISPLAY="ship" ;;
  complete)     STAGE_DISPLAY="complete" ;;
esac
```

C. Next 명령 case 블록 (commands/sg-status.md lines 78-99 그대로):
```bash
case "$STAGE_RAW" in
  init)
    if [ -n "$PHASE_NUM" ]; then
      NEXT_CMD="/super-gsd:sg-plan $PHASE_NUM"
    else
      NEXT_CMD="/super-gsd:sg-plan"
    fi
    ;;
  gsd-plan)    NEXT_CMD="/super-gsd:sg-execute" ;;
  superpowers) NEXT_CMD="/super-gsd:sg-review" ;;
  review)      NEXT_CMD="/super-gsd:sg-learn" ;;
  hookify)     NEXT_CMD="/super-gsd:sg-ship" ;;
  ship)
    if [ "$NEXT_PHASE_EXISTS" = "1" ]; then
      NEXT_CMD="/super-gsd:sg-plan $NEXT_PHASE"
    else
      NEXT_CMD="/super-gsd:sg-complete"
    fi
    ;;
  complete) NEXT_CMD="/super-gsd:sg-new" ;;
  *) NEXT_CMD="(unknown stage: $STAGE_RAW)" ;;
esac
```

NEXT_PHASE 계산 블록도 sg-status.md lines 62-74 그대로 복제 필요 (ship/hookify 분기 의존):
```bash
if [ "$STAGE_RAW" = "hookify" ] || [ "$STAGE_RAW" = "ship" ]; then
  if echo "$PHASE_NUM" | grep -qE '^[0-9]+$'; then
    NEXT_PHASE=$((PHASE_NUM + 1))
    if grep -qE "^### Phase ${NEXT_PHASE}:" .planning/ROADMAP.md 2>/dev/null; then
      NEXT_PHASE_EXISTS=1
    else
      NEXT_PHASE_EXISTS=0
    fi
  else
    NEXT_PHASE_EXISTS=0
  fi
fi
```

D. STATE.md frontmatter 파싱 (Phase 8 신규 — milestone, last_updated, last_activity):
STATE.md frontmatter 실제 포맷 (lines 1-15 of .planning/STATE.md):
```yaml
---
gsd_state_version: 1.0
milestone: v1.1
milestone_name: Reliability
status: executing
stopped_at: Phase 8 context gathered
last_updated: "2026-05-19T15:43:38.395Z"
last_activity: 2026-05-18 -- Phase 07 planning complete
progress:
  ...
---
```

Bash 추출 (라인 단위 grep+sed, yq 미사용 — Phase 6 D-04 lock):
```bash
MILESTONE=$(grep -E '^milestone:' .planning/STATE.md 2>/dev/null | head -1 | sed -E 's/^milestone:[[:space:]]*//' | sed -E 's/[[:space:]]+$//' | sed -E 's/^"//;s/"$//')
MILESTONE_NAME=$(grep -E '^milestone_name:' .planning/STATE.md 2>/dev/null | head -1 | sed -E 's/^milestone_name:[[:space:]]*//' | sed -E 's/[[:space:]]+$//' | sed -E 's/^"//;s/"$//')
LAST_UPDATED=$(grep -E '^last_updated:' .planning/STATE.md 2>/dev/null | head -1 | sed -E 's/^last_updated:[[:space:]]*//' | sed -E 's/[[:space:]]+$//' | sed -E 's/^"//;s/"$//')
LAST_ACTIVITY=$(grep -E '^last_activity:' .planning/STATE.md 2>/dev/null | head -1 | sed -E 's/^last_activity:[[:space:]]*//' | sed -E 's/[[:space:]]+$//' | sed -E 's/^"//;s/"$//')
```

HANDOFF.md 데이터 행 포맷 (Phase 2 D-22 lock — `| ` leading 공백 포함 5컬럼 markdown table):
```
| 2026-05-19T15:44:46Z | 08-session-restore | complete | gsd-plan | - |
```
awk `-F'|'` 컬럼 인덱스: $2=Timestamp, $3=Phase, $4=From, $5=To, $6=Plan Hash.

STATE.md 미감지 조건 (D-01):
- `.planning/STATE.md` 파일이 없거나
- 파일은 있어도 `^Phase:` 라인이 없어 `PHASE_LINE`이 `(none)`로 fallback된 경우

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sg-start.md `<process>` 블록 확장 — STATE.md 감지 + 5-라인 표시 + AskUserQuestion 3-옵션 분기</name>
  <files>commands/sg-start.md</files>
  <read_first>
    - commands/sg-start.md (전체 — 현재 5-line `<process>` 블록 구조)
    - commands/sg-status.md (lines 14-100 — 인라인 복제 원본: STATE.md Phase 파싱 블록 + HANDOFF.md 마지막 행 + storage→display 매핑 + NEXT_PHASE 계산 + Next 명령 case 블록)
    - commands/sg-new.md (Skill 위임 패턴 참조 — `Skill(skill="gsd-new-milestone", args="$ARGUMENTS")`)
    - .planning/phases/08-session-restore/08-CONTEXT.md (locked decisions D-01~D-17)
    - .planning/phases/07-status-accuracy/07-CONTEXT.md (locked decisions D-04~D-08 — 인라인 복제 계약)
    - .planning/phases/07-status-accuracy/07-01-PLAN.md (Task 1 영역 A의 인라인 복제 형식 참조)
    - .planning/STATE.md (frontmatter 실제 포맷 + Phase 라인 실제 포맷)
    - .planning/HANDOFF.md (5컬럼 데이터 행 실제 포맷)
  </read_first>
  <action>
    `commands/sg-start.md` 의 `<process>` 블록을 한 줄짜리 Skill invoke에서 다음 6-step 구조로 확장한다. 다른 파일은 절대 건드리지 않는다 (`commands/sg-status.md`, `commands/sg-new.md` 등 포함). frontmatter (`name`, `description`, `argument-hint`)와 `<objective>`/`<execution_context>`/`<success_criteria>` 섹션은 본 task 범위에서 최소한으로 갱신한다(D-17 후방 호환 표현 + AskUserQuestion 호출 사실 반영). 전체 구조는 다음과 같다:

    ---

    **Step 1 — STATE.md Phase 파싱 (D-01, D-03; Phase 7 D-07 lock 인라인 복제).**

    `commands/sg-status.md` lines 17-21의 BEGIN/END 주석 demarcate된 블록을 **글자 그대로** 복제한다. 변수명 변경 금지. 주석 문자열도 그대로 유지(`# --- BEGIN STATE.md Phase parsing block (D-07: Phase 8 sg-start이 동일 블록을 복제) ---`로 시작, 동일 END 주석으로 종료). 결과: `PHASE_LINE`, `PHASE_NUM` 두 변수 설정.

    파싱 후 즉시 D-01 트리거 판정:
    ```bash
    if [ ! -f .planning/STATE.md ] || [ "$PHASE_LINE" = "(none)" ]; then
      EXISTING_SESSION=0
    else
      EXISTING_SESSION=1
    fi
    ```

    `EXISTING_SESSION=0`이면 Step 6의 D-17 fallback 분기로 바로 점프 (Step 2~5 건너뜀). 구현은 if 블록으로 감싼다.

    ---

    **Step 2 — STATE.md frontmatter 추가 파싱 + HANDOFF.md 마지막 행 파싱 (D-04, D-06, D-07).**

    EXISTING_SESSION=1 분기 안에서만 실행.

    (2a) STATE.md frontmatter 4개 필드 추출 — `interfaces` 섹션 블록 D를 그대로 사용:
    - `MILESTONE`, `MILESTONE_NAME`, `LAST_UPDATED`, `LAST_ACTIVITY` 4개 변수.
    - 각 변수는 line-by-line grep + sed 패턴 (yq 등 외부 도구 금지 — Phase 6 D-04 lock).

    Milestone 표시 문자열 조립 (D-07):
    ```bash
    if [ -n "$MILESTONE" ] && [ -n "$MILESTONE_NAME" ]; then
      MILESTONE_DISPLAY="${MILESTONE} ${MILESTONE_NAME}"
    elif [ -n "$MILESTONE" ]; then
      MILESTONE_DISPLAY="$MILESTONE"
    else
      MILESTONE_DISPLAY="(unknown)"
    fi
    ```

    (2b) HANDOFF.md 마지막 데이터 행 + Stage 매핑 — `interfaces` 섹션 블록 B를 그대로 복제. 변수명 (`LAST_ROW`, `STAGE_RAW`, `STAGE_DISPLAY`, `TS`) 변경 금지. case 블록 7개 분기 (`init|gsd-plan|superpowers|review|hookify|ship|complete`)와 스키마 검증 case 모두 그대로 유지.

    ---

    **Step 3 — Last activity 시각 결정 (D-06).**

    HANDOFF.md Timestamp 우선, 없으면 STATE.md frontmatter, 둘 다 없으면 `(unknown)`:
    ```bash
    if [ -n "$TS" ]; then
      LAST_ACTIVITY_DISPLAY="$TS"
    elif [ -n "$LAST_UPDATED" ]; then
      LAST_ACTIVITY_DISPLAY="$LAST_UPDATED"
    elif [ -n "$LAST_ACTIVITY" ]; then
      LAST_ACTIVITY_DISPLAY="$LAST_ACTIVITY"
    else
      LAST_ACTIVITY_DISPLAY="(unknown)"
    fi
    ```

    상대시각 환산 (`3 days ago` 등) 절대 금지 — D-06 lock. 추출된 문자열 그대로 출력한다.

    ---

    **Step 4 — NEXT_PHASE 계산 + Next 명령 매핑 (D-04 Next 라인; Phase 2 D-28 lock 인라인 복제).**

    `commands/sg-status.md` lines 62-99 의 NEXT_PHASE 블록 + 8-분기 case 블록 (`init/gsd-plan/superpowers/review/hookify/ship/complete/*`) 둘 다 그대로 복제. 변수명 (`NEXT_PHASE`, `NEXT_PHASE_EXISTS`, `NEXT_CMD`) 변경 금지. `interfaces` 섹션 블록 C의 두 코드 블록 모두 그대로 인라인.

    ---

    **Step 5 — 5개 라인 emit + AskUserQuestion 3-옵션 분기 (D-04, D-05, D-10, D-12, D-13, D-14, D-15, D-16; SESS-02/03/04).**

    (5a) 안내 헤더 + 5개 라인 출력. 정렬은 좌측 정렬 단일 공백 (sg-health 도트 채움 미강제 — D-claude-discretion 영역):
    ```
    Existing session detected.

    Milestone: <MILESTONE_DISPLAY>
    Phase: <PHASE_LINE>
    Stage: <STAGE_DISPLAY>
    Last activity: <LAST_ACTIVITY_DISPLAY>
    Next: <NEXT_CMD>
    ```
    안내 헤더 문자열은 정확히 `Existing session detected.` (영문). 5개 라인 사이 빈 줄 없음. 헤더와 5-라인 사이 한 줄 공백 1개.

    (5b) AskUserQuestion 호출 — `header="Session"`, `question="Existing session detected. What do you want to do?"`, options 3개 (D-12 정확한 라벨):
    - 라벨 `"Resume"` — description `"Show next command and exit. You will run the next command yourself."`
    - 라벨 `"Start new milestone"` — description `"Delegate to gsd-new-milestone Skill."`
    - 라벨 `"Cancel"` — description `"Exit without changes."`

    AskUserQuestion은 명령 파일에서 직접 도구 호출 형태(`AskUserQuestion(...)`)를 코드 블록에 명시한다 — `commands/sg-execute.md` 등 다른 명령에서 사용되는 동일 invocation 스타일을 따른다(자체 구문 발명 금지; 단, sg-start의 frontmatter에 명시적 tool whitelist는 없어도 무방 — discuss-phase workflow와 동일).

    (5c) AskUserQuestion 응답 분기 — if/case 블록 한 단계 안에 3개 분기:
    - **Resume** (D-08, D-16): 추가 출력 없음, 종료. (사용자가 위 Next 라인을 보고 직접 실행.) 자동 Skill invoke 절대 금지(D-09 hybrid handoff 차용 안 함).
    - **Start new milestone** (D-14): 즉시 `Skill(skill="gsd-new-milestone", args="")` 호출. args는 **빈 문자열**(`$ARGUMENTS` 사용 금지 — D-14). 호출 후 추가 출력 없음.
    - **Cancel** (D-15): 단일 라인 `Cancelled. No changes made.` emit 후 종료. 어떤 파일도 read/write 하지 않는다(이미 read한 STATE/HANDOFF는 read-only이므로 충족).

    세 분기 모두 `.planning/HANDOFF.md` 를 read-only로만 접근 (D-16; SESS-04 자연 충족). HANDOFF.md 삭제, 수정, append 절대 금지.

    ---

    **Step 6 — D-17 fallback 분기 (EXISTING_SESSION=0).**

    Step 1의 if 가드 else 분기 (또는 별도 `if [ "$EXISTING_SESSION" = "0" ]; then ... fi`)에서 기존 동작을 그대로 호출:
    ```
    Skill(skill="gsd-new-project", args="$ARGUMENTS")
    ```
    `$ARGUMENTS` 패스스루 유지 (D-17 — 후방 호환). 추가 출력 없음.

    ---

    **부속 갱신 (한 줄짜리, scope 최소):**

    - `<objective>` 1-2줄 추가: "Detect existing session via STATE.md + HANDOFF.md; if found, show 5-line summary and ask user (Resume / Start new milestone / Cancel) via AskUserQuestion. If no STATE.md, delegate to gsd-new-project as before." 정도. 영문 유지(기존 영문 톤).
    - `<execution_context>` 1줄 갱신: "Reads .planning/STATE.md, .planning/HANDOFF.md, .planning/ROADMAP.md (next-phase existence check). Writes nothing. Delegates to gsd-new-milestone or gsd-new-project Skill depending on user choice / detection result."
    - `<success_criteria>` 갱신 — 4개 항목으로 확장 (SESS-01/02/03/04 각각 1개):
      1. STATE.md/HANDOFF.md 감지 + 5-라인 표시 + AskUserQuestion 3-옵션 (SESS-01, SESS-02)
      2. Resume 선택 시 emit-only 종료 (자동 Skill invoke 없음) (SESS-03)
      3. Start new milestone 시 gsd-new-milestone Skill 위임, Cancel 시 단일 라인 emit 후 종료 (D-14, D-15)
      4. 세 옵션 모두 HANDOFF.md read-only — 삭제/수정/append 없음 (SESS-04, D-16)
    - frontmatter `argument-hint` 갱신: 기존 의미 유지하되 "fallback when no existing session detected" 한 줄 추가. 예: `"[project-name] - optional. Used only when no existing .planning/STATE.md is detected; passed through to gsd-new-project."`

    **하지 말아야 할 것 (lock 위반):**
    - 새 파일 생성 (helper script, test fixture, parse_handoff.sh 등) — Phase 6 D-04 + Phase 7 D-08 lock 위반.
    - `commands/sg-status.md`, `commands/sg-new.md`, `hooks/*` 등 다른 파일 수정 — Phase 8 scope 위반.
    - STATE.md / HANDOFF.md 에 write — D-16 + SESS-04 위반.
    - HANDOFF.md 삭제 또는 행 제거 — SESS-04 위반.
    - Resume 분기에서 자동으로 Next 명령 invoke — D-08, D-09 위반 (사용자 의사결정 권한 보존).
    - Start new milestone에 `$ARGUMENTS` 전달 — D-14 위반 (빈 문자열 lock).
    - 상대시각 표시 (`3 days ago` 등) — D-06 위반 (absolute timestamp lock).
    - sg-status의 D-29 5-라인 출력 형식을 sg-start에 적용 — D-05 위반 (sg-start는 자체 포맷).
    - storage→display 매핑 또는 Next case 블록을 sg-status.md와 다르게 변경 — Phase 7 D-07 drift lock 위반 (양쪽 동시 수정 강제).
    - `\S+` 같은 토큰 1개 캡처 정규식으로 Phase 라인 파싱 — Phase 7 D-06 회귀 위반.
    - Python/외부 셸 의존 인라인 추가 (yq, jq for STATE.md 등) — Phase 6 D-04 lock 위반.
  </action>
  <verify>
    <automated>
    cd /Users/gyuha/workspace/super-gsd \
    && grep -q 'BEGIN STATE.md Phase parsing block (D-07' commands/sg-start.md \
    && grep -q 'PHASE_LINE=$(grep -E ' commands/sg-start.md \
    && grep -q "sed -E 's/\\^Phase:\\[\\[:space:\\]\\]\\*//'" commands/sg-start.md \
    && grep -q 'case "$STAGE_RAW" in' commands/sg-start.md \
    && grep -q 'gsd-plan)' commands/sg-start.md \
    && grep -q 'STAGE_DISPLAY="gsd"' commands/sg-start.md \
    && grep -q 'review)' commands/sg-start.md \
    && grep -q 'STAGE_DISPLAY="superpowers"' commands/sg-start.md \
    && grep -q 'MILESTONE=' commands/sg-start.md \
    && grep -q 'MILESTONE_NAME=' commands/sg-start.md \
    && grep -q 'LAST_UPDATED=' commands/sg-start.md \
    && grep -q 'Existing session detected' commands/sg-start.md \
    && grep -q 'Milestone:' commands/sg-start.md \
    && grep -q 'Last activity:' commands/sg-start.md \
    && grep -q 'Next:' commands/sg-start.md \
    && grep -q 'AskUserQuestion' commands/sg-start.md \
    && grep -q 'Resume' commands/sg-start.md \
    && grep -q 'Start new milestone' commands/sg-start.md \
    && grep -q 'Cancel' commands/sg-start.md \
    && grep -q 'Cancelled. No changes made.' commands/sg-start.md \
    && grep -q 'Skill(skill="gsd-new-milestone", args="")' commands/sg-start.md \
    && grep -q 'Skill(skill="gsd-new-project", args="$ARGUMENTS")' commands/sg-start.md \
    && ! grep -q 'days ago' commands/sg-start.md \
    && ! grep -q "Skill(skill=\"gsd-new-milestone\", args=\"\\\$ARGUMENTS\")" commands/sg-start.md \
    && PARSED1=$(printf 'Phase: 6 (sg-health) — Not started\n' | grep -E '^Phase:' | head -1 | sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//') \
    && [ "$PARSED1" = "6 (sg-health) — Not started" ] \
    && PARSED2=$(printf 'Phase: Not started\n' | grep -E '^Phase:' | head -1 | sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//') \
    && [ "$PARSED2" = "Not started" ] \
    && ls commands/sg-start.md hooks/ 2>/dev/null \
    && ! ls tests/sg-start* 2>/dev/null \
    && ! ls bin/parse_handoff* hooks/parse_handoff* 2>/dev/null \
    && echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    1. `commands/sg-start.md` 에 정확한 BEGIN/END demarcate 주석 (`# --- BEGIN STATE.md Phase parsing block (D-07: Phase 8 sg-start이 동일 블록을 복제) ---`)이 포함된 STATE.md Phase 파싱 블록이 존재한다 (Phase 7 D-07 인라인 복제 계약).
    2. `commands/sg-start.md` 의 `^Phase:` 파싱 sed 패턴이 `sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//'` 형식 그대로이며, `\S+` 단일 토큰 캡처 정규식이 존재하지 않는다 (Phase 7 D-06 회귀 방지).
    3. `commands/sg-start.md` 에 storage→display 매핑 case 블록(`gsd-plan)*STAGE_DISPLAY="gsd"` + `review)*STAGE_DISPLAY="superpowers"` 포함)이 sg-status.md와 글자 그대로 일치한다 (drift 없음).
    4. `commands/sg-start.md` 에 Next 명령 case 블록(`complete) NEXT_CMD="/super-gsd:sg-new"` + `hookify)*NEXT_CMD="/super-gsd:sg-ship"` 포함)이 sg-status.md와 글자 그대로 일치한다.
    5. 5개 라인 안내 라벨(`Milestone:`, `Phase:`, `Stage:`, `Last activity:`, `Next:`) + 헤더 `Existing session detected.` 가 파일에 존재한다 (D-04, D-05).
    6. AskUserQuestion 호출이 명시되어 있고, 3개 옵션 라벨(`Resume`, `Start new milestone`, `Cancel`)이 모두 등장한다 (D-10, D-12).
    7. `Skill(skill="gsd-new-milestone", args="")` 호출이 Start new milestone 분기 안에 있다 — args는 빈 문자열(`$ARGUMENTS` 금지) (D-14).
    8. `Skill(skill="gsd-new-project", args="$ARGUMENTS")` 호출이 D-17 fallback 분기(STATE.md 미감지)에 그대로 남아 있다 (SESS-03 후방 호환).
    9. `Cancelled. No changes made.` 단일 라인이 Cancel 분기 출력으로 명시되어 있다 (D-15).
    10. 상대시각 표현 (`days ago`, `hours ago`, `minutes ago` 등) 이 파일에 등장하지 않는다 (D-06 absolute timestamp lock).
    11. `git status --short` 결과가 정확히 `M commands/sg-start.md` 1줄 + `.planning/` 산출물뿐이다 — `tests/`, `bin/`, `hooks/parse_handoff*` 같은 새 파일이 존재하지 않는다 (Phase 6 D-04 + Phase 7 D-08 lock).
    12. `commands/sg-status.md`, `commands/sg-new.md`, `hooks/*` 파일들은 본 task로 인해 변경되지 않았다 (Phase 8 scope).
  </acceptance_criteria>
  <done>
    `commands/sg-start.md` 한 파일만 수정되었고, 12개 acceptance criteria 가 모두 충족된다. STATE.md Phase 파싱 블록은 sg-status.md 와 글자 그대로 동기화되었으며, storage→display 매핑/Next case 블록/NEXT_PHASE 계산 블록 3개도 sg-status.md 인라인 복제로 완료. 5개 라인 안내 + AskUserQuestion 3-옵션 분기(Resume emit-only / Start new milestone Skill / Cancel 단일 라인) 모두 코드에 반영. D-17 fallback (STATE.md 미감지 시 gsd-new-project + $ARGUMENTS) 보존.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 7-시나리오 수동 회귀 검증 + HANDOFF.md append-only 불변량 확인 (SESS-04)</name>
  <files>.planning/HANDOFF.md (백업/복원 대상; 수정 후 원복), .planning/STATE.md (백업/복원 대상; 수정 후 원복) — 작업이 끝나면 두 파일 모두 원래 상태로 복원되어야 한다 (read-only 효과)</files>
  <read_first>
    - .planning/phases/08-session-restore/08-CONTEXT.md (specifics §"수동 검증 시나리오 체크리스트" 7개 행)
    - commands/sg-start.md (Task 1 수정 결과물)
    - .planning/HANDOFF.md (백업 대상 + SESS-04 행-개수 불변량 확인 baseline)
    - .planning/STATE.md (백업 대상)
  </read_first>
  <what-built>
    Task 1에서 `commands/sg-start.md` 의 `<process>` 블록을 6-step 구조로 확장했다: STATE.md Phase 파싱 인라인 복제 + STATE.md frontmatter 파싱 + HANDOFF.md 마지막 행 파싱 + Stage 매핑 + NEXT_PHASE/Next 매핑 + 5-라인 emit + AskUserQuestion 3-옵션 분기 + D-17 fallback. 자동화 테스트 자산을 추가하지 않았으므로(Phase 7 D-08 일관성 유지), 사람이 직접 7개 회귀 시나리오를 실행해 동작과 SESS-04 append-only 감사 로그 불변량을 확인한다.
  </what-built>
  <action>
    사람이 CONTEXT.md `<specifics>` 테이블의 7개 회귀 시나리오를 순서대로 실행하고, 매 시나리오마다 (a) sg-start 출력이 기대값과 정확히 일치하는지 (b) 실행 전/후 `.planning/HANDOFF.md` 행 개수가 변하지 않는지(SESS-04 핵심 검증) 확인한다. 자동화 자산 추가 없음(Phase 7 D-08 일관성).
  </action>
  <how-to-verify>
    1. 백업 (절대 잊지 말 것 — 이 phase는 시나리오마다 STATE.md/HANDOFF.md를 손상시킨다):
       ```
       cp .planning/HANDOFF.md .planning/HANDOFF.md.bak
       cp .planning/STATE.md .planning/STATE.md.bak
       HANDOFF_BASELINE=$(grep -cE '^\| [0-9]{4}-' .planning/HANDOFF.md)
       echo "HANDOFF.md baseline data-row count: $HANDOFF_BASELINE"
       ```

    2. CONTEXT.md `<specifics>` 테이블 7개 시나리오를 순서대로 실행. 매 시나리오마다:
       - STATE.md / HANDOFF.md 를 시나리오 fixture로 수동 편집.
       - Claude 세션에서 `/super-gsd:sg-start` 실행.
       - 출력이 기대 동작과 일치하는지 확인 (아래 시나리오별 기대값 참조).
       - **HANDOFF.md 데이터 행 개수가 baseline 또는 시나리오 fixture에서 정한 값과 동일한지 확인:**
         ```
         AFTER=$(grep -cE '^\| [0-9]{4}-' .planning/HANDOFF.md 2>/dev/null || echo 0)
         echo "after scenario N: $AFTER rows (expected: 변화 없음)"
         ```

    3. 시나리오별 기대 동작:

       **시나리오 1 — STATE.md/HANDOFF.md 모두 없음 (D-17 fallback):**
       - 사전 작업: `rm .planning/STATE.md .planning/HANDOFF.md`
       - sg-start 실행 → AskUserQuestion 없음, 즉시 `Skill(skill="gsd-new-project", args="$ARGUMENTS")` 호출 (기존 동작).
       - HANDOFF.md 행 개수: 0 → 0 (파일 자체 없음, 그대로 유지).

       **시나리오 2 — STATE.md `Phase: 1 (...)` + HANDOFF.md 데이터 행 0개 + Resume 선택 (D-02, D-08, D-16; SESS-01/02/03):**
       - 사전 작업: STATE.md `Phase: 1 (foundation) — Not started` 라인 + HANDOFF.md 헤더+구분자만(데이터 행 0개).
       - sg-start 실행 → 5개 라인 emit (Stage=`init`, Next=`/super-gsd:sg-plan 1`, Last activity=STATE.md last_updated 또는 last_activity), AskUserQuestion 3옵션 표시.
       - "Resume" 선택 → 추가 출력 없음, 즉시 종료. **Skill 자동 invoke 없음(D-08).**
       - HANDOFF.md 행 개수: 0 → 0 (SESS-04 검증 핵심).

       **시나리오 3 — STATE.md `Phase: 6 ...` + HANDOFF.md 마지막 `To=hookify` + Resume 선택 (D-04 Next 라인):**
       - 사전 작업: STATE.md `Phase: 6 (sg-health) — Not started`, HANDOFF.md 마지막 행 `| 2026-05-20T10:00:00Z | 06-sg-health | review | hookify | - |`.
       - sg-start 실행 → 5개 라인 (Stage=`hookify`, Next=`/super-gsd:sg-ship`, Last activity=`2026-05-20T10:00:00Z`).
       - "Resume" 선택 → emit-only 종료.
       - HANDOFF.md 행 개수: N → N (변화 없음).

       **시나리오 4 — STATE.md `Phase: 6 ...` + HANDOFF.md 마지막 `To=complete` + Start new milestone 선택 (D-14):**
       - 사전 작업: STATE.md `Phase: 6 (sg-health) — Not started`, HANDOFF.md 마지막 행 `| 2026-05-20T11:00:00Z | 06-sg-health | hookify | complete | - |`.
       - sg-start 실행 → 5개 라인 (Stage=`complete`, Next=`/super-gsd:sg-new`).
       - "Start new milestone" 선택 → 즉시 `Skill(skill="gsd-new-milestone", args="")` 호출 (args 빈 문자열, **$ARGUMENTS 금지**).
       - HANDOFF.md 행 개수: N → N (Skill 호출 자체는 HANDOFF.md에 행을 추가하지 않음. 만약 추가된다면 SESS-04 위반이지만 이는 gsd-new-milestone 책임 영역. sg-start의 책임은 자체적으로 행을 추가하지 않는 것 — 확인).

       **시나리오 5 — STATE.md `Phase: 7 ...` + HANDOFF.md 마지막 `To=review` + Cancel 선택 (D-15):**
       - 사전 작업: STATE.md `Phase: 7 (status-accuracy) — Not started`, HANDOFF.md 마지막 행 `| 2026-05-20T12:00:00Z | 07-status-accuracy | superpowers | review | - |`.
       - sg-start 실행 → 5개 라인 (Stage=`superpowers` 표시지만 Next=`/super-gsd:sg-learn` — D-03/D-04 라우팅 lock).
       - "Cancel" 선택 → `Cancelled. No changes made.` 단일 라인 emit, 즉시 종료. **Skill 자동 invoke 없음.**
       - HANDOFF.md 행 개수: N → N (SESS-04 검증).

       **시나리오 6 — STATE.md `Phase: Not started` (숫자 없음) + HANDOFF.md 없음 + Resume 선택 (D-03 회귀 방지 + scenario 6 fallback):**
       - 사전 작업: STATE.md `Phase: Not started` (D-03 인라인 복제 회귀 fixture), HANDOFF.md 삭제.
       - sg-start 실행 → 5개 라인 (Phase=`Not started` — `Not`만 잘리지 않음, Stage=`init`, Next=`/super-gsd:sg-plan` — 인자 생략 fallback, PHASE_NUM 추출 실패).
       - "Resume" 선택 → emit-only 종료.
       - HANDOFF.md 행 개수: 0 → 0 (파일 없음 그대로).

       **시나리오 7 — STATE.md 있음 + HANDOFF.md 스키마 손상 (마지막 행이 알 수 없는 stage):**
       - 사전 작업: STATE.md 정상, HANDOFF.md 마지막 행 `| 2026-05-20T13:00:00Z | 99-bogus | init | corrupted_stage | - |`.
       - sg-start 실행 → sg-status와 동일하게 stderr 메시지 `Unknown stage 'corrupted_stage' in .planning/HANDOFF.md last row. Schema may be corrupted.` 출력 후 exit 1. AskUserQuestion 호출 없음. 진단 책임은 `sg-health`로 위임 (CONTEXT code_context known risk sites 일관).
       - HANDOFF.md 행 개수: N → N.

    4. **SESS-04 종합 검증** — 모든 시나리오 종료 시점에서 (백업 복원 직전):
       ```
       FINAL=$(grep -cE '^\| [0-9]{4}-' .planning/HANDOFF.md 2>/dev/null || echo 0)
       echo "final data-row count vs baseline: $FINAL vs $HANDOFF_BASELINE"
       ```
       Baseline 과 비교: 시나리오 fixture에서 의도적으로 행을 변경한 경우 baseline 과 다를 수 있지만, **sg-start 자체가 추가/삭제한 행은 0이어야 한다**. 시나리오별로 행 개수가 fixture와 동일하면 sg-start의 read-only 책임이 충족된 것.

    5. 복원 (필수):
       ```
       mv .planning/HANDOFF.md.bak .planning/HANDOFF.md
       mv .planning/STATE.md.bak .planning/STATE.md
       RESTORED=$(grep -cE '^\| [0-9]{4}-' .planning/HANDOFF.md)
       echo "restored data-row count: $RESTORED (must equal baseline $HANDOFF_BASELINE)"
       [ "$RESTORED" = "$HANDOFF_BASELINE" ] && echo "RESTORE OK" || echo "RESTORE FAIL"
       ```

    6. 백업 복원 후 한 번 더 `/super-gsd:sg-start` 실행해 현재 프로젝트 상태(`v1.1 Reliability`, Phase 8 진행 중)에서 정상 출력하는지 확인 — 이 단계에서는 옵션 선택 없이 출력만 확인하고 Cancel 종료.
  </how-to-verify>
  <verify>
    <human-check>
    7개 시나리오 모두 기대 동작과 정확히 일치하고, 각 시나리오에서 sg-start 자체가 HANDOFF.md 행을 추가/삭제하지 않았으며(SESS-04), 최종적으로 `git status --short .planning/HANDOFF.md .planning/STATE.md` 결과가 빈 출력(원복됨)이어야 한다.
    </human-check>
  </verify>
  <acceptance_criteria>
    1. 시나리오 1 (D-17 fallback): STATE.md 없음 → AskUserQuestion 호출 없이 즉시 `gsd-new-project` Skill invoke. 출력에 `Existing session detected.` 헤더가 없음.
    2. 시나리오 2 (Resume + 0 데이터 행): 5개 라인 emit, Stage=`init`, Resume 선택 시 추가 Skill invoke 없이 종료. HANDOFF.md 행 개수 변화 0.
    3. 시나리오 3 (Resume + hookify stage): Stage=`hookify`, Next=`/super-gsd:sg-ship`, Resume 선택 시 emit-only 종료.
    4. 시나리오 4 (Start new milestone + complete stage): "Start new milestone" 선택 시 `Skill(skill="gsd-new-milestone", args="")` 호출 (args 빈 문자열 확인). HANDOFF.md 행 개수 변화 0 (sg-start 자체 책임 영역).
    5. 시나리오 5 (Cancel + review stage 라우팅 lock): Stage=`superpowers`(표시 매핑), Next=`/super-gsd:sg-learn`(storage 기준 라우팅), Cancel 선택 시 `Cancelled. No changes made.` 단일 라인 emit 후 종료.
    6. 시나리오 6 (D-03 회귀 방지): STATE.md `Phase: Not started` 입력에서 Phase 라인이 `Phase: Not started` 그대로 출력되고 `Not`만 잘리지 않음. PHASE_NUM 미추출 시 Next=`/super-gsd:sg-plan`(인자 생략 fallback).
    7. 시나리오 7 (스키마 손상): stderr 메시지 + exit 1, AskUserQuestion 호출 없음.
    8. SESS-04 불변량: 7개 시나리오 모두에서 sg-start 자체가 HANDOFF.md 행을 추가/삭제하지 않았다 (행-개수 baseline 비교).
    9. 백업 복원 후 `git status --short .planning/HANDOFF.md .planning/STATE.md` 결과가 빈 출력 (원복 완료).
    10. 백업 복원 후 실 환경에서 `/super-gsd:sg-start` 1회 실행 시 5개 라인이 정상 출력되고 (현재 STATE.md `Phase: 6 (sg-health) — Not started`, HANDOFF.md 마지막 `To=gsd-plan` 기준), Cancel 종료 후 HANDOFF.md 행 개수 변화 없음.
  </acceptance_criteria>
  <done>
    7개 시나리오 모두 기대 동작과 일치하고, SESS-04 행-개수 불변량이 8/8 위치에서 (시나리오 1-7 + 시나리오 종료 후 실환경 1회) 충족되었으며, HANDOFF.md / STATE.md 가 백업으로부터 원복되었다.
  </done>
  <resume-signal>
    7개 시나리오 모두 통과 + SESS-04 불변량 충족 + 백업 원복 확인되면 "approved" 입력. 시나리오별 출력 불일치나 행-개수 변화가 있으면 시나리오 번호와 실제 출력/행 개수를 보고 — Task 1로 돌아가 수정.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (no untrusted boundary crossed) | `sg-start`는 로컬 `.planning/STATE.md` / `.planning/HANDOFF.md` / `.planning/ROADMAP.md` 만 읽고, AskUserQuestion으로 사용자에게 선택을 받은 뒤 stdout으로 출력하거나 Skill에 위임한다. 네트워크 없음, 파일 쓰기 없음. `$ARGUMENTS` 패스스루는 D-17 분기에서만 사용되며 gsd-new-project Skill에 그대로 전달된다 (Skill 내부에서 자체 검증). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `.planning/STATE.md` `Phase:` 라인 / frontmatter (악성 콘텐츠) | accept | sg-start는 표시만 하고 실행하지 않음. 셸 인젝션 surface는 bash 변수 인용(`"$PHASE_LINE"`, `"$MILESTONE_DISPLAY"` 등)으로 차단. `eval` 미사용. Repo가 이미 침해된 상태에서만 발생 가능 — 별도 mitigation 불필요. Phase 7 T-07-01과 동일 disposition. |
| T-08-02 | Tampering | `.planning/HANDOFF.md` `To` 컬럼 (악성 stage 문자열) | mitigate | 기존 `case "$STAGE_RAW" in gsd-plan\|superpowers\|review\|hookify\|ship\|complete) ;; *) ... exit 1 ;; esac` 스키마 검증 블록을 그대로 인라인 복제 — 알려진 7-state 외 값은 stderr 메시지 후 exit 1 (시나리오 7). |
| T-08-03 | Tampering | `$ARGUMENTS` (D-17 fallback에서 gsd-new-project로 전달) | accept | sg-start는 `$ARGUMENTS`를 직접 해석하지 않고 gsd-new-project Skill에 패스스루만 함. 검증 책임은 gsd-new-project 내부 (CONTEXT canonical_refs §"Skill Reference" — `$HOME/.claude/get-shit-done/workflows/new-project.md`). |
| T-08-04 | Repudiation | HANDOFF.md append-only 감사 로그 (SESS-04) | mitigate | 세 옵션 분기 모두 HANDOFF.md를 read-only로만 접근하도록 Task 1 action에 명시적 금지 (`HANDOFF.md 삭제 또는 행 제거 — SESS-04 위반`). Task 2 시나리오별 행-개수 baseline 비교로 사후 검증. |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | Phase 8은 패키지 설치 작업 없음 — RESEARCH.md Package Legitimacy Audit 불필요. |

요약: 읽기 전용 명령(쓰기 분기는 Skill 위임이며 Skill 내부 책임). 사용자 입력은 AskUserQuestion 3-옵션 라벨로 한정. 네트워크 없음. bash 변수 인용 위생 + 스키마 검증 case 블록 인라인 복제 + 시나리오별 SESS-04 행-개수 baseline 검증으로 mitigation 완료.
</threat_model>

<verification>
1. Task 1 자동 verify (`<automated>` 블록의 모든 `grep -q` / `! grep -q` / sed pipeline 회귀 체크)가 `PASS`로 종료한다.
2. Task 2 수동 검증 7개 시나리오 모두 기대 동작과 정확히 일치 — 특히:
   - 시나리오 1: D-17 fallback (STATE.md 없음 → gsd-new-project + $ARGUMENTS)
   - 시나리오 2: D-02 (HANDOFF.md 데이터 행 0개도 기존 세션)
   - 시나리오 4: D-14 (Start new milestone args 빈 문자열, $ARGUMENTS 금지)
   - 시나리오 5: D-04/D-03 라우팅 lock (review 표시는 superpowers, Next는 sg-learn)
   - 시나리오 6: D-03 회귀 방지 (`Phase: Not started` 잘림 없음)
   - 시나리오 7: 스키마 손상 stderr + exit 1
3. SESS-04 행-개수 불변량: sg-start 자체가 어떤 시나리오에서도 HANDOFF.md 데이터 행을 추가/삭제하지 않았다.
4. `git diff --stat commands/sg-start.md` 결과: 한 파일만 변경됨.
5. `.planning/` 내 다른 파일은 PLAN/SUMMARY 외 변경되지 않는다.
6. `tests/sg-start/`, `bin/parse_handoff*`, `hooks/parse_handoff*` 같은 새 파일이 존재하지 않는다 (Phase 6 D-04 + Phase 7 D-08 lock).
</verification>

<success_criteria>
- SESS-01 충족: sg-start 실행 시 `.planning/STATE.md` 존재 + `^Phase:` 라인 캡처 가능 여부로 기존 세션을 감지한다 (D-01 단일 트리거).
- SESS-02 충족: 기존 세션 감지 시 5개 라인(Milestone / Phase / Stage / Last activity / Next)을 안내 헤더 뒤에 표시하고 AskUserQuestion 3-옵션을 제시한다 (D-04, D-10, D-12).
- SESS-03 충족: Resume 선택 시 5개 라인 emit 후 즉시 종료 — `gsd-new-project` 호출을 건너뛰며, 자동 Skill invoke 없이 사용자가 Next 명령을 직접 실행한다 (D-08, D-09). STATE.md 미감지 시(D-17 fallback)에는 기존 `Skill(skill="gsd-new-project", args="$ARGUMENTS")` 동작 그대로 유지 (후방 호환).
- SESS-04 충족: 세 옵션(Resume / Start new milestone / Cancel) 모두 `.planning/HANDOFF.md` 를 read-only로만 접근 — 삭제/수정/append 없음 (D-16). Task 2 시나리오별 행-개수 baseline 비교로 사후 검증.
- Phase 7 D-07 lock 이행: STATE.md `Phase:` 파싱 블록을 BEGIN/END 주석 demarcate된 형태로 글자 그대로 sg-status.md에서 복제 (drift 시 양쪽 동시 수정).
- Phase 2 D-28 lock 유지: Next 명령 매핑은 sg-status.md의 storage-state-기준 case 블록을 그대로 복제 — `review` stage 표시는 `superpowers`이지만 Next는 `/super-gsd:sg-learn`, `hookify` stage Next는 `/super-gsd:sg-ship`, `complete` stage Next는 `/super-gsd:sg-new`.
- Phase 6 D-04 + Phase 7 D-08 lock 유지: helper 스크립트, 테스트 fixture, parse_handoff.{py,sh} 같은 새 파일 추가 없음. `commands/sg-start.md` 한 파일만 수정.
- D-06 absolute timestamp lock 유지: 상대시각 표시 (`days ago` 등) 미도입.
- D-13/D-14/D-15 옵션 라벨 lock 유지: "Fresh" 라벨 미사용, Start new milestone은 args 빈 문자열, Cancel은 단일 라인 emit.
</success_criteria>

<output>
Create `.planning/phases/08-session-restore/08-01-SUMMARY.md` when done.
</output>
