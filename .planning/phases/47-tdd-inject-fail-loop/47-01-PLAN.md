---
phase: 47-tdd-inject-fail-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-execute/SKILL.md
  - .agents/skills/sg-execute/SKILL.md
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03]

must_haves:
  truths:
    - "When .planning/USE-TDD exists, sg-execute's sequential handoff prompt instructs the use of superpowers:test-driven-development and Red-first (failing test before implementation) ordering"
    - "When .planning/USE-TDD is absent, sg-execute's sequential prompt blob is byte-identical to current behavior (no TDD lines added)"
    - "When .planning/USE-TDD-RETRY exists, sg-execute skips the Step 7 same-hash short-circuit (re-execution allowed) and injects a '## Previous Test Failures — Fix First' section read from that file"
    - "The parallel routing path (sg-parallel-execute) is untouched — TDD injection is sequential-only"
    - "The .agents/skills/sg-execute mirror reflects identical marker-gated behavior using its inline direct-implementation path (no Skill handoff)"
  artifacts:
    - path: "skills/sg-execute/SKILL.md"
      provides: "TDD injection in Step 9 sequential blob + retry-bypass guard in Step 7"
      contains: "USE-TDD"
    - path: ".agents/skills/sg-execute/SKILL.md"
      provides: "Mirror: TDD injection in direct-implementation prose + retry-bypass guard"
      contains: "USE-TDD"
  key_links:
    - from: "skills/sg-execute/SKILL.md Step 7"
      to: ".planning/USE-TDD-RETRY"
      via: "presence check guarding the same-hash exit 0 short-circuit"
      pattern: "USE-TDD-RETRY"
    - from: "skills/sg-execute/SKILL.md Step 9 (after parallel return)"
      to: "superpowers:test-driven-development"
      via: "marker-gated instruction injected into the ## Instruction to Superpowers section"
      pattern: "test-driven-development"
---

<objective>
Make sg-execute consume the `.planning/USE-TDD` marker (Phase 46) so that, when TDD mode is ON, the sequential Superpowers handoff prompt instructs Red-first test ordering via `superpowers:test-driven-development`. Additionally, consume `.planning/USE-TDD-RETRY` (written by sg-review's failure loop in Plan 47-02) to (a) bypass the Step 7 idempotency short-circuit on re-execution and (b) inject prior FAIL feedback as a fix-first section.

Purpose: This is the producer half of the two-skill TDD handshake. EXEC-01 (inject), EXEC-02 (non-invasive when markers absent), EXEC-03 (.agents mirror).
Output: Modified `skills/sg-execute/SKILL.md` and its `.agents/skills/sg-execute/SKILL.md` mirror, changed together (pairwise sync, D-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-tdd-inject-fail-loop/47-CONTEXT.md
@.planning/phases/47-tdd-inject-fail-loop/47-PATTERNS.md

<interfaces>
<!-- Marker contract established in Phase 46. Both files are plain-text, human-readable, presence-tested with [ -f <path> ]. -->

`.planning/USE-TDD` — presence-only. EXISTS = TDD mode ON. Phase 46 writes a 2-line body (e.g. "TDD mode enabled\nActivated: <iso8601>"). Plan 47-01 only reads presence: `if [ -f .planning/USE-TDD ]; then ... ; fi`.

`.planning/USE-TDD-RETRY` — written by sg-review (Plan 47-02) on each FAIL re-execution. Plain-text, human-readable (NOT JSON). Carries TWO pieces of state: retry count + last review FAIL feedback. Serialization is implementer discretion but MUST be parseable with portable shell (no grep -P): the recommended convention is line 1 = integer count, lines 2+ = feedback body. Plan 47-01 reads: (a) presence (to bypass Step 7), (b) the feedback body (to inject the fix-first section). Plan 47-02 owns writing it; Plan 47-01 must read whatever serialization Plan 47-02 settles on — coordinate via the line-1-count / lines-2+-body convention.

Canonical marker-presence pattern (from skills/sg-use-tdd/SKILL.md:36):
  if [ -f .planning/USE-TDD ]; then TDD_ON=true; else TDD_ON=false; fi
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject TDD + retry handling into skills/sg-execute/SKILL.md (sequential path only)</name>
  <files>skills/sg-execute/SKILL.md</files>
  <read_first>
    - skills/sg-execute/SKILL.md:135-143 (Step 7 idempotency check — the same-hash `exit 0` short-circuit; retry-bypass target per D-02)
    - skills/sg-execute/SKILL.md:282-331 (Step 9 routing branch + sequential prompt blob; parallel `return` at :289-293; `## Instruction to Superpowers` at :323-324; sequential Skill invocation at :328-331)
    - skills/sg-use-tdd/SKILL.md:36 (canonical `[ -f .planning/USE-TDD ]` presence-check pattern)
    - .planning/phases/47-tdd-inject-fail-loop/47-PATTERNS.md (Injection sites A and B; Shared Patterns)
  </read_first>
  <action>
    Two marker-gated edits to skills/sg-execute/SKILL.md, both strictly additive so the marker-absent path stays byte-identical (EXEC-02 / D-03b regression guard):

    (1) Step 7 retry-bypass (D-02): Before the existing same-hash short-circuit `if`, add a presence check on `.planning/USE-TDD-RETRY`. When that file exists, set a bypass flag and guard the existing `if` so the `exit 0` short-circuit does NOT fire (re-execution of the same plan hash is the intent during a retry). Use `[ -f .planning/USE-TDD-RETRY ]` (no grep -P). When the retry file is absent, the guard adds zero behavioral change. Do NOT alter the existing grep -E / awk -F'|' extraction line.

    (2) Step 9 sequential injection (D-03 + D-02): The injection MUST happen AFTER the parallel routing `return` block (:289-293) so the parallel path (sg-parallel-execute) is never touched — sequential-only per D-03. In the sequential prompt-blob assembly: when `.planning/USE-TDD` is present, augment the `## Instruction to Superpowers` section with an explicit instruction to use the `superpowers:test-driven-development` skill and write a failing (Red) test for each behavior BEFORE implementing it (then make it pass / Green). When `.planning/USE-TDD-RETRY` is present, ALSO prepend a `## Previous Test Failures — Fix First` section whose body is the feedback read from `.planning/USE-TDD-RETRY` (per the line-1-count / lines-2+-body convention in <interfaces>). The two existing `## Instruction to Superpowers` lines (:323-324) must remain verbatim when USE-TDD is absent. CRITICAL (EXEC-02 / D-03b): structure both injections so the TDD directive and the `## Previous Test Failures — Fix First` section render ONLY inside their `[ -f .planning/USE-TDD ]` / `[ -f .planning/USE-TDD-RETRY ]` presence-conditional branches — never unconditionally / at top level. The presence test must syntactically precede the injected directive so the directive cannot be emitted when the marker is absent. Exact markdown wording of the injected directives is implementer discretion (D-03).

    Surface any user-facing prose in the user's input language per the file's <language> block; machine tokens `USE-TDD`, `USE-TDD-RETRY`, `superpowers:test-driven-development`, `superpowers:executing-plans`, paths stay verbatim English.
  </action>
  <verify>
    <automated>grep -c "test-driven-development" skills/sg-execute/SKILL.md | grep -qv '^0$' && grep -q "USE-TDD-RETRY" skills/sg-execute/SKILL.md && grep -q "Previous Test Failures" skills/sg-execute/SKILL.md && grep -q "USE-TDD" skills/sg-execute/SKILL.md && ! grep -q "grep -P" skills/sg-execute/SKILL.md && echo PASS</automated>
    <automated># Marker-ABSENT path (EXEC-02 load-bearing): the TDD directive must NOT render unconditionally — assert the first `[ -f .planning/USE-TDD ]` presence gate appears at a LOWER line number than the first `test-driven-development` directive it guards (gate precedes directive ⇒ directive is inside the conditional, cannot emit when marker absent). Run: TDD_LINE=$(grep -n "test-driven-development" skills/sg-execute/SKILL.md | head -1 | cut -d: -f1); GATE_LINE=$(grep -nE '\[ -f \.planning/USE-TDD \]' skills/sg-execute/SKILL.md | head -1 | cut -d: -f1); test -n "$TDD_LINE" && test -n "$GATE_LINE" && [ "$GATE_LINE" -lt "$TDD_LINE" ] && echo GATED-PASS</automated>
    <automated># Same for the retry section: the `## Previous Test Failures — Fix First` header must sit inside a `[ -f .planning/USE-TDD-RETRY ]` conditional. Run: PTF_LINE=$(grep -n "Previous Test Failures" skills/sg-execute/SKILL.md | head -1 | cut -d: -f1); RETRY_GATE=$(grep -nE '\[ -f \.planning/USE-TDD-RETRY \]' skills/sg-execute/SKILL.md | head -1 | cut -d: -f1); test -n "$PTF_LINE" && test -n "$RETRY_GATE" && [ "$RETRY_GATE" -lt "$PTF_LINE" ] && echo RETRY-GATED-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "superpowers:test-driven-development" skills/sg-execute/SKILL.md` returns at least one hit, located AFTER line 293 (the parallel `return`) — verify via `grep -n` line number > 293.
    - `grep -n "USE-TDD-RETRY" skills/sg-execute/SKILL.md` returns a hit inside the Step 7 region (near :135-143) guarding the short-circuit.
    - `grep -q "Previous Test Failures" skills/sg-execute/SKILL.md` succeeds (the fix-first section header is present).
    - `grep -q "if \[ -f .planning/USE-TDD \]" skills/sg-execute/SKILL.md` OR equivalent `[ -f .planning/USE-TDD ]` presence test is present.
    - MARKER-ABSENT GUARD (EXEC-02): the TDD `test-driven-development` directive is enclosed by a `[ -f .planning/USE-TDD ]` presence-conditional — the gate's line number precedes the directive's line number, so the directive cannot render when the marker is absent. An unconditional/top-level TDD directive FAILS this criterion (see the GATED-PASS verify above).
    - MARKER-ABSENT GUARD (retry): the `## Previous Test Failures — Fix First` header is enclosed by a `[ -f .planning/USE-TDD-RETRY ]` presence-conditional (RETRY-GATED-PASS verify above) — never top-level.
    - The existing literal line "Execute the plans above using the superpowers:executing-plans skill." (:324) is still present unchanged.
    - The parallel block lines (`Skill(skill="sg-parallel-execute"` and the `return`) are unchanged — `grep -q 'Skill(skill="sg-parallel-execute"' skills/sg-execute/SKILL.md` still succeeds.
    - No `grep -P` introduced; any awk uses `-F'|'`.
  </acceptance_criteria>
  <done>USE-TDD-present sequential handoff carries a Red-first + test-driven-development directive; USE-TDD-RETRY-present run bypasses the Step 7 short-circuit and prepends a fix-first feedback section; marker-absent path and the parallel path are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror TDD + retry handling into .agents/skills/sg-execute/SKILL.md (prose / inline path)</name>
  <files>.agents/skills/sg-execute/SKILL.md</files>
  <read_first>
    - .agents/skills/sg-execute/SKILL.md:18-23 (Platform Constraints block — declares AskUserQuestion not supported, Superpowers unavailable, direct implementation mode)
    - .agents/skills/sg-execute/SKILL.md:125-132 (mirror Step 7 idempotency check — matches `execute` To-stage, not `(superpowers|parallel)`; retry-bypass target)
    - .agents/skills/sg-execute/SKILL.md:143-172 (Step 9 "Direct implementation mode"; Direct Implementation Instructions block at :147-167 — TDD injection target since no Skill handoff exists here)
    - .agents/skills/sg-execute/SKILL.md:59-91 (existing interactive-gate → prose-halt fallback analog)
    - skills/sg-execute/SKILL.md (the primary, as just edited in Task 1 — mirror MUST reflect identical marker-gated behavior)
    - .planning/phases/47-tdd-inject-fail-loop/47-PATTERNS.md (`.agents/skills/sg-execute/SKILL.md` section)
  </read_first>
  <action>
    Reflect the same two marker-gated behaviors into the mirror, adapted to the mirror's inline direct-implementation model (no `superpowers:*` Skill handoff exists on this platform — D-07):

    (1) Step 7 retry-bypass (D-02): same `[ -f .planning/USE-TDD-RETRY ]` guard around the mirror's same-hash `exit 0` short-circuit at :125-132 (note: mirror matches the `execute` To-stage, not `(superpowers|parallel)` — preserve that grep verbatim, only add the bypass guard).

    (2) Direct-implementation injection (D-03 adapted): when `.planning/USE-TDD` is present, inject into the Direct Implementation Instructions prose (:147-167) an instruction to write a failing (Red) test for each behavior BEFORE implementing it, then make it pass (Green) — phrased for inline execution since `superpowers:test-driven-development` is unavailable here; reference the TDD discipline (Red-first ordering) in prose rather than invoking the skill. When `.planning/USE-TDD-RETRY` is present, ALSO surface the prior FAIL feedback (read from the file) as a "Previous Test Failures — Fix First" prose section the inline implementer must address first. CRITICAL (EXEC-02 / D-03b): structure the injection so the Red-first directive and the "Previous Test Failures — Fix First" section render ONLY inside their `[ -f .planning/USE-TDD ]` / `[ -f .planning/USE-TDD-RETRY ]` presence-conditional branches — the presence test must precede the directive so it cannot emit when the marker is absent.

    User-facing prose in user's input language; machine tokens (`USE-TDD`, `USE-TDD-RETRY`, paths, `$sg-execute`) verbatim English. Keep consistent with the existing Platform Constraints block (:18-23) — do not claim Skill handoff.
  </action>
  <verify>
    <automated>grep -q "USE-TDD-RETRY" .agents/skills/sg-execute/SKILL.md && grep -q "USE-TDD" .agents/skills/sg-execute/SKILL.md && grep -qiE "failing \(red\) test|red-first|red test" .agents/skills/sg-execute/SKILL.md && grep -q "Previous Test Failures" .agents/skills/sg-execute/SKILL.md && ! grep -q "grep -P" .agents/skills/sg-execute/SKILL.md && echo PASS</automated>
    <automated># Marker-ABSENT path (EXEC-02): the Red-first directive must sit inside a `[ -f .planning/USE-TDD ]` conditional. Run: RED_LINE=$(grep -niE "failing \(red\) test|red-first|red test" .agents/skills/sg-execute/SKILL.md | head -1 | cut -d: -f1); GATE_LINE=$(grep -nE '\[ -f \.planning/USE-TDD \]' .agents/skills/sg-execute/SKILL.md | head -1 | cut -d: -f1); test -n "$RED_LINE" && test -n "$GATE_LINE" && [ "$GATE_LINE" -lt "$RED_LINE" ] && echo GATED-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "USE-TDD-RETRY" .agents/skills/sg-execute/SKILL.md` succeeds inside the Step 7 region (near :125-132).
    - `grep -q "USE-TDD" .agents/skills/sg-execute/SKILL.md` succeeds and a `[ -f .planning/USE-TDD ]` presence test is present.
    - A Red-first / failing-test-before-implementation directive is present in the Direct Implementation Instructions region (grep -E matches `failing \(red\) test|red-first|red test` case-insensitively after line 143). NOTE: a bare `grep -qi "red"` is forbidden — "red" already pre-exists inside "ROADMAP" (line ~107), so it would pass before any edit; use the discriminating phrase match.
    - MARKER-ABSENT GUARD (EXEC-02): the Red-first directive is enclosed by a `[ -f .planning/USE-TDD ]` presence-conditional — the gate's line number precedes the directive's line number (GATED-PASS verify above). An unconditional Red-first directive FAILS this criterion.
    - `grep -q "Previous Test Failures" .agents/skills/sg-execute/SKILL.md` succeeds.
    - The mirror does NOT introduce a `superpowers:test-driven-development` Skill() invocation (consistent with Platform Constraints :18-23) — `grep -q 'Skill(skill="superpowers:test-driven-development"' .agents/skills/sg-execute/SKILL.md` returns nothing.
    - The mirror Step 7 grep still matches the `execute` stage (unchanged): `grep -q 'execute\[\[:space:\]\]' .agents/skills/sg-execute/SKILL.md` or the original line is intact.
    - No `grep -P` introduced.
  </acceptance_criteria>
  <done>The .agents mirror reflects identical marker-gated TDD injection and retry-bypass via inline/prose execution (no Skill handoff), consistent with its Platform Constraints block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → skill logic | sg-execute reads `.planning/USE-TDD` and `.planning/USE-TDD-RETRY` (local files, no network/auth, no user-input-as-code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | `.planning/USE-TDD-RETRY` content (read into the handoff prompt body) | accept | Local single-developer file under `.planning/` (gitignored, not network-exposed). Feedback body is injected as prose context for a code-review/implementer, not executed as code. No privilege boundary crossed. |
| T-47-02 | Tampering | retry count enabling unbounded re-execution | mitigate | Count cap (max 2) is enforced by Plan 47-02 (the writer); Plan 47-01 only reads presence to bypass the idempotency short-circuit. The bound lives at the producer of the counter. |
| T-47-03 | Information disclosure / Injection | marker file paths | mitigate | Paths are hardcoded string constants (`.planning/USE-TDD`, `.planning/USE-TDD-RETRY`) — never derived from `$ARGUMENTS` or any user-supplied value. No path-injection surface. |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — only Markdown SKILL.md edits. No supply-chain surface. |
</threat_model>

<verification>
- `grep -c "test-driven-development" skills/sg-execute/SKILL.md` > 0 and located after the parallel `return` (line > 293).
- Both files contain `USE-TDD` and `USE-TDD-RETRY` references.
- `! grep -q "grep -P" skills/sg-execute/SKILL.md .agents/skills/sg-execute/SKILL.md` (portability).
- Parallel routing block in the primary is unchanged.
- Both files changed in a single commit (pairwise sync, D-08).
</verification>

<success_criteria>
- EXEC-01: USE-TDD present → sequential handoff instructs superpowers:test-driven-development + Red-first ordering.
- EXEC-02: USE-TDD absent → sequential prompt blob byte-identical to current; parallel path untouched.
- EXEC-03: .agents mirror reflects identical marker-gated behavior (inline/prose, no Skill handoff).
- D-01: TDD-mode detection uses `.planning/USE-TDD` (presence-only, Phase 46, unchanged); the retry count + last FAIL feedback live in the SEPARATE file `.planning/USE-TDD-RETRY` so the presence-only marker semantics are not polluted. This plan reads `USE-TDD-RETRY` presence (Step 7 bypass) and `USE-TDD` presence (Step 9 injection) but never writes to or alters `USE-TDD`.
- D-02: USE-TDD-RETRY present → Step 7 short-circuit bypassed + fix-first feedback section injected.
- Pairwise sync: both files modified together.
</success_criteria>

<output>
Create `.planning/phases/47-tdd-inject-fail-loop/47-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
