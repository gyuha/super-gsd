---
phase: 18-sg-parallel-execute
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-parallel-execute/SKILL.md
autonomous: true
requirements:
  - TE-02a
  - TE-02b
  - TE-03a

must_haves:
  truths:
    - "skills/sg-parallel-execute/SKILL.md 파일이 존재한다"
    - "스킬이 $ARGUMENTS로 전달된 parallel_groups.json 경로를 Read tool로 읽는다"
    - "GROUP_COUNT = JSON 배열 길이로 자동 결정된다 (에이전트 수 전달 불필요)"
    - "실행할 에이전트 수는 min(GROUP_COUNT, 3)이며 wave 오름차순으로 선택된다"
    - "각 Task() 프롬프트에 superpowers:executing-plans 호출 금지 지시가 포함된다"
    - "각 Task() 프롬프트에 HANDOFF.md 기록 금지 지시가 포함된다"
    - "각 Task() 프롬프트에 STATE.md 업데이트 금지 지시가 포함된다"
    - "parallel_groups.json이 없거나 파싱 실패 시 오류 메시지를 출력하고 종료한다 (자동 폴백 없음)"
    - "GROUP_COUNT > 3이면 앞 3개 그룹 병렬 실행 후 나머지를 순차 처리한다"
  artifacts:
    - path: "skills/sg-parallel-execute/SKILL.md"
      provides: "병렬 에이전트 디스패치 스킬"
      contains: "sg-parallel-execute"
  key_links:
    - from: "sg-execute.md Step 9"
      to: "skills/sg-parallel-execute/SKILL.md"
      via: "Skill(skill=\"sg-parallel-execute\", args=\"$GROUPS_JSON_FILE\")"
    - from: "skills/sg-parallel-execute/SKILL.md"
      to: "parallel_groups.json"
      via: "Read tool on $ARGUMENTS path"
---

<objective>
`skills/sg-parallel-execute/SKILL.md`를 신규 생성한다. 이 스킬은 `$ARGUMENTS`로 `parallel_groups.json` 파일 경로를 받아, 배열 길이로 GROUP_COUNT를 결정하고, min(GROUP_COUNT, 3)개의 Task()를 동시 실행한다. 각 Task()는 bare 실행 방식(superpowers:executing-plans 호출 없음)으로 해당 그룹의 PLAN.md를 직접 처리한다.

Purpose: TE-02a, TE-02b, TE-03a 요건을 충족하는 병렬 실행 스킬 구현.
Output: `skills/sg-parallel-execute/SKILL.md` (신규)
</objective>

<execution_context>
Self-contained plan — no external workflow files imported. Follow the SKILL.md structure pattern from `skills/sg-retro/SKILL.md` (frontmatter + `<objective>` + `<process>`).
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-sg-parallel-execute/18-CONTEXT.md
@skills/sg-retro/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: skills/sg-parallel-execute/SKILL.md 신규 생성</name>
  <files>skills/sg-parallel-execute/SKILL.md</files>

  <read_first>
    - skills/sg-retro/SKILL.md — SKILL.md frontmatter + `<objective>` + `<process>` 구조 패턴 확인
    - .planning/phases/18-sg-parallel-execute/18-CONTEXT.md — D-01~D-07 결정사항, Task() 프롬프트 구조
    - .planning/REQUIREMENTS.md — TE-02a, TE-02b, TE-03a 원문
  </read_first>

  <action>
  `skills/sg-parallel-execute/` 디렉토리를 생성하고 `SKILL.md`를 작성한다.

  **frontmatter:**
  - name: sg-parallel-execute
  - description: Reads parallel_groups.json and dispatches up to 3 parallel Task() agents — one per independent group — to execute PLAN.md tasks directly without calling superpowers:executing-plans.

  **`<objective>` 블록:**
  `$ARGUMENTS`로 전달된 `parallel_groups.json` 파일 경로를 Read tool로 읽어 그룹 목록을 파악한다. GROUP_COUNT(배열 길이)를 계산하고, min(GROUP_COUNT, 3)개의 Task()를 동시(병렬)로 실행한다. 각 Task()는 해당 그룹의 PLAN.md를 직접 읽어 태스크를 실행하며 superpowers:executing-plans를 호출하지 않는다. GROUP_COUNT > 3이면 wave 번호 오름차순 앞 3개 그룹을 병렬 실행한 뒤 나머지를 순차 처리한다.

  **`<execution_context>` 블록:**
  Self-contained. Reads $ARGUMENTS (parallel_groups.json path), then reads each group's PLAN.md files. Writes nothing to .planning/HANDOFF.md, .planning/STATE.md, or any GSD/Superpowers files. Parallel Task() agents are dispatched in the same response.

  **`<process>` 블록 (6단계):**

  Step 1 — 입력 검증.
  `$ARGUMENTS`가 비어 있으면 에러 메시지 출력 후 종료:
  `[sg-parallel-execute] Error: $ARGUMENTS must be the path to parallel_groups.json. Got empty.`

  Step 2 — parallel_groups.json 읽기.
  Read tool로 `$ARGUMENTS` 경로를 읽는다. 파일이 없거나 JSON 파싱 실패 시:
  `[sg-parallel-execute] Error: Cannot read parallel_groups.json at <path>. Ensure sg-execute Step 8.5 ran successfully.`
  출력 후 종료. 자동 폴백 없음 (D-07).

  Step 3 — GROUP_COUNT 계산 및 실행 그룹 결정.
  파싱된 배열의 길이 = GROUP_COUNT.
  EXEC_COUNT = min(GROUP_COUNT, 3).
  wave 번호 오름차순으로 정렬한 뒤 앞 EXEC_COUNT개를 병렬 실행 대상으로 선택 (D-02).
  GROUP_COUNT > 3이면 나머지 (EXEC_COUNT 이후) 그룹은 순차 처리 대상으로 분리.
  출력: `[sg-parallel-execute] GROUP_COUNT=N, EXEC_COUNT=M (wave-ascending order)`

  Step 4 — 각 병렬 그룹의 PLAN.md 읽기.
  각 그룹의 `plans` 배열에서 파일명을 순회하여 Read tool로 PLAN.md 본문을 읽는다.
  PHASE_DIR는 `$ARGUMENTS` 경로의 디렉토리 부분으로 추론한다 (예: `dirname "$ARGUMENTS"`).
  PLAN.md 경로 = `{PHASE_DIR}/{plan_filename}`.

  Step 5 — 병렬 Task() 디스패치.
  EXEC_COUNT개의 Task()를 동일 응답 내에서 병렬로 실행한다 (D-02, TE-02a).
  각 Task()의 프롬프트 구조 (D-03):

  ```
  Execute the following plan(s) for Phase {PHASE_NUM}.

  CRITICAL constraints — do NOT violate these:
  - Do NOT call superpowers:executing-plans (bare task execution only)
  - Do NOT write to .planning/HANDOFF.md
  - Do NOT update .planning/STATE.md
  - Do NOT modify any GSD or Superpowers internal files (non-invasive rule)

  Plans to execute:
  === {plan_filename} ===
  {full PLAN.md body}

  Execute all tasks in the plan(s) above. Follow each task's <action>, <verify>, and <done> fields. Report completion status for each task.
  ```

  PHASE_NUM은 PHASE_DIR의 디렉토리명 앞 두 자리 숫자로 추론한다 (예: `18-sg-parallel-execute` → `18`).

  Step 6 — GROUP_COUNT > 3일 경우 순차 처리.
  병렬 배치(Step 5) 완료 후, 나머지 그룹을 wave 오름차순으로 순차 처리한다.
  각 그룹에 대해 Step 4~5와 동일한 PLAN.md 읽기 + Task() 실행을 반복하되, 1개씩 순차적으로 실행한다.
  출력: `[sg-parallel-execute] Sequential group {N}: executing {plan_filename}`

  **`<success_criteria>` 블록:**
  1. $ARGUMENTS로 유효한 parallel_groups.json 경로가 전달되면 Read tool로 파일을 읽고 GROUP_COUNT를 출력한다.
  2. EXEC_COUNT개의 Task()가 동일 응답 내에서 병렬로 호출된다.
  3. 각 Task() 프롬프트에 superpowers:executing-plans 호출 금지, HANDOFF.md 쓰기 금지, STATE.md 업데이트 금지 지시가 포함된다.
  4. $ARGUMENTS가 빈 문자열이거나 파일이 없으면 오류 메시지를 출력하고 종료한다 (자동 폴백 없음).
  5. GROUP_COUNT > 3이면 앞 3개 그룹 병렬 실행 후 나머지를 순차 처리한다.
  </action>

  <verify>
  <automated>
    # 파일 존재 확인
    test -f skills/sg-parallel-execute/SKILL.md && echo "PASS: SKILL.md exists" || echo "FAIL: SKILL.md missing"

    # frontmatter name 필드 확인
    grep -c 'name: sg-parallel-execute' skills/sg-parallel-execute/SKILL.md

    # superpowers:executing-plans 호출 금지 지시 포함 확인 (프롬프트 내)
    grep -c 'superpowers:executing-plans' skills/sg-parallel-execute/SKILL.md

    # HANDOFF.md 금지 지시 포함 확인
    grep -cF 'HANDOFF.md' skills/sg-parallel-execute/SKILL.md

    # STATE.md 금지 지시 포함 확인
    grep -c 'STATE.md' skills/sg-parallel-execute/SKILL.md

    # process 블록 존재 확인
    grep -c '<process>' skills/sg-parallel-execute/SKILL.md

    # min(GROUP_COUNT, 3) 상한 로직 확인
    grep -cE '(EXEC_COUNT|min.*3|3개)' skills/sg-parallel-execute/SKILL.md
  </automated>
  </verify>

  <acceptance_criteria>
  - `skills/sg-parallel-execute/SKILL.md` 파일이 존재한다
  - frontmatter에 `name: sg-parallel-execute` 필드가 있다
  - `<objective>` 블록이 존재하고 `parallel_groups.json`, `Task()`, `min(GROUP_COUNT, 3)` 개념을 포함한다
  - `<process>` 블록이 존재하고 6단계(입력 검증, JSON 읽기, GROUP_COUNT 계산, PLAN.md 읽기, 병렬 Task() 디스패치, 순차 처리)를 포함한다
  - 각 Task() 프롬프트 구조에 `superpowers:executing-plans` 호출 금지 문구가 포함된다
  - 각 Task() 프롬프트 구조에 `HANDOFF.md` 쓰기 금지 문구가 포함된다
  - 각 Task() 프롬프트 구조에 `STATE.md` 업데이트 금지 문구가 포함된다
  - GROUP_COUNT > 3 처리 (순차 폴오버) 로직이 process에 명시된다
  - 파일이 없거나 $ARGUMENTS 비어있을 때의 오류 처리가 명시된다
  </acceptance_criteria>

  <done>skills/sg-parallel-execute/SKILL.md가 생성되어 있고, sg-retro/SKILL.md와 동일한 구조(frontmatter + objective + execution_context + process + success_criteria)를 따르며, 위 acceptance_criteria를 모두 충족한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| $ARGUMENTS → SKILL.md | 스킬 호출자가 전달하는 파일 경로 (sg-execute가 생성한 JSON) |
| SKILL.md → Task() 에이전트 | 스킬이 주입하는 PLAN.md 본문 + 제약 프롬프트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | Task() 에이전트 프롬프트 | mitigate | HANDOFF.md/STATE.md 쓰기 금지 지시를 프롬프트 최상단에 CRITICAL 레이블로 명시 |
| T-18-02 | Repudiation | parallel_groups.json 소비 | accept | JSON 파일은 sg-execute Step 8.5가 이미 생성 — 출처 추적 가능, 스킬 재생성 불필요 |
| T-18-03 | Denial of Service | GROUP_COUNT > 3 무한 에이전트 | mitigate | EXEC_COUNT = min(GROUP_COUNT, 3) 상한 강제 |
</threat_model>

<verification>
1. `skills/sg-parallel-execute/SKILL.md` 존재: `test -f skills/sg-parallel-execute/SKILL.md`
2. frontmatter name 필드: `grep -c 'name: sg-parallel-execute' skills/sg-parallel-execute/SKILL.md` → 1 이상
3. Task() 프롬프트 내 3개 금지 지시 모두 포함: `grep -cE '(superpowers:executing-plans|HANDOFF\.md|STATE\.md)' skills/sg-parallel-execute/SKILL.md` → 3 이상
4. process 블록 존재: `grep -c '<process>' skills/sg-parallel-execute/SKILL.md` → 1
</verification>

<success_criteria>
1. `skills/sg-parallel-execute/SKILL.md`가 sg-retro/SKILL.md와 동일한 구조(frontmatter + objective + execution_context + process + success_criteria)로 생성된다.
2. $ARGUMENTS로 parallel_groups.json 경로를 받아 GROUP_COUNT를 계산하고 min(GROUP_COUNT, 3)개의 Task()를 병렬 디스패치하는 과정이 process에 명시된다.
3. 각 Task() 프롬프트에 superpowers:executing-plans 호출 금지, HANDOFF.md 쓰기 금지, STATE.md 업데이트 금지 지시가 포함된다.
4. GROUP_COUNT > 3인 경우 나머지 그룹을 순차 처리하는 로직이 명시된다.
5. 오류 처리(파일 없음, 빈 $ARGUMENTS)가 명시되고 자동 폴백이 없다.
</success_criteria>

<output>
Create `.planning/phases/18-sg-parallel-execute/18-01-SUMMARY.md` when done.
</output>
