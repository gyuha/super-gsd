---
phase: 32-skill-superpowers-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-complete/SKILL.md
  - skills/sg-execute/SKILL.md
  - skills/sg-plan/SKILL.md
  - skills/sg-review/SKILL.md
  - skills/sg-ship/SKILL.md
  - skills/sg-ui-plan/SKILL.md
  - skills/sg-lessons/SKILL.md
  - skills/sg-quick/SKILL.md
autonomous: true
requirements:
  - PARSE-01
must_haves:
  truths:
    - "skills/sg-{complete,execute,plan,review,ship,ui-plan}에서 STATE.md Phase 파싱이 Read 도구 + Claude 해석으로 교체된다"
    - "skills/sg-{complete,execute,plan,review,ship,ui-plan}에서 HANDOFF.md 마지막 행 파싱이 Read 도구 + Claude 해석으로 교체된다"
    - "skills/sg-execute에서 ROADMAP.md Phase 섹션 파싱이 Read 도구 + Claude 해석으로 교체된다"
    - "skills/sg-review에서 PLAN.md objective 섹션 파싱이 Read 도구 + Claude 해석으로 교체된다"
    - "skills/sg-lessons에서 grep -oE milestone= 패턴이 node -e 방식으로 교체된다"
    - "skills/sg-quick에서 sed flag strip과 awk STATE.md 업데이트가 node -e / Read+Edit 방식으로 교체된다"
    - "sg-execute Step 8.5의 wave/files_modified awk 파싱은 변경되지 않는다 (명시적 제외)"
  artifacts:
    - path: "skills/sg-complete/SKILL.md"
      provides: "STATE.md Phase + milestone + HANDOFF 파싱 → Read 도구"
    - path: "skills/sg-execute/SKILL.md"
      provides: "STATE.md + ROADMAP + HANDOFF 파싱 → Read 도구"
    - path: "skills/sg-plan/SKILL.md"
      provides: "STATE.md Phase + HANDOFF 파싱 → Read 도구"
    - path: "skills/sg-review/SKILL.md"
      provides: "STATE.md + PLAN.md objective + HANDOFF 파싱 → Read 도구"
    - path: "skills/sg-ship/SKILL.md"
      provides: "STATE.md Phase + HANDOFF 파싱 → Read 도구"
    - path: "skills/sg-ui-plan/SKILL.md"
      provides: "STATE.md Phase + HANDOFF 파싱 → Read 도구"
    - path: "skills/sg-lessons/SKILL.md"
      provides: "argument 파싱 → node -e"
    - path: "skills/sg-quick/SKILL.md"
      provides: "flag strip → node -e, STATE.md 업데이트 → Read+Edit"
  key_links:
    - from: "skills/sg-*/SKILL.md (process 섹션)"
      to: ".planning/STATE.md"
      via: "Read 도구 직접 호출"
      pattern: "Read .planning/STATE.md"
    - from: "skills/sg-*/SKILL.md (process 섹션)"
      to: ".planning/HANDOFF.md"
      via: "Read 도구 직접 호출"
      pattern: "Read .planning/HANDOFF.md"
---

<objective>
`skills/sg-*/SKILL.md` 8개 파일에서 bash 파이프라인(grep|sed|awk) 기반 파일 파싱을 Superpowers 방식(Read 도구 + Claude 해석)으로 전환한다. Pattern E/F(argument 파싱)는 node -e로, Pattern G(STATE.md 테이블 업데이트)는 Read+Edit 도구로 전환한다.

Purpose: bash 파이프라인은 Windows에서 동작하지 않고 Superpowers 철학과 맞지 않는다. Read 도구 + Claude 해석으로 전환하면 플랫폼 독립성이 확보된다.
Output: 8개 SKILL.md 파일에서 bash 파싱 코드가 제거되고 Read 도구 지시문으로 교체된다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/32-skill-superpowers-migration/32-CONTEXT.md
@.planning/phases/32-skill-superpowers-migration/32-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: sg-complete / sg-plan / sg-ship / sg-ui-plan — Pattern A+B 전환</name>
  <files>
    skills/sg-complete/SKILL.md
    skills/sg-plan/SKILL.md
    skills/sg-ship/SKILL.md
    skills/sg-ui-plan/SKILL.md
  </files>
  <read_first>
    - skills/sg-complete/SKILL.md — 전체 파일 읽기 (Step 1 Phase 파싱, Step 1.3 milestone 파싱, Step 1.5 HANDOFF 파싱 위치 확인)
    - skills/sg-plan/SKILL.md — Step 1 Phase 파싱, Step 2.5 HANDOFF 파싱 위치 확인
    - skills/sg-ship/SKILL.md — Step 1 Phase 파싱, Step 1.5 HANDOFF 파싱 위치 확인
    - skills/sg-ui-plan/SKILL.md — Step 1 Phase 파싱, Step 4 HANDOFF 파싱 위치 확인
    - .planning/phases/32-skill-superpowers-migration/32-PATTERNS.md — 패턴 A/B Before/After 확인
  </read_first>
  <steps>
    1. `skills/sg-complete/SKILL.md`를 읽고 다음 세 패턴을 교체한다:
       - Step 1: `grep -E '^Phase:' .planning/STATE.md | head -1 | sed -E 's/^Phase:[[:space:]]*//' | awk '{print $1}'` 블록 전체를 제거하고 else 분기를 `Read .planning/STATE.md, then extract the Phase: value from the YAML frontmatter (first token after "Phase: ").`로 교체
       - Step 1.3: `grep -E '^milestone:'...awk '{print $2}'` → `Read .planning/STATE.md, then extract the milestone: value from the YAML frontmatter.`
       - Step 1.5 FROM_STAGE: `grep -E '^\| [0-9]{4}-' "$HANDOFF_FILE" | tail -1 | awk -F'|' '{gsub(/ /,"",$5); print $5}'` → `Read .planning/HANDOFF.md, then extract the To column (5th pipe-delimited field) from the last row that starts with "| " followed by a 4-digit year.`
       주의: HANDOFF.md 초기화 블록(`if [ ! -f "$HANDOFF_FILE" ]...printf ...`)은 변경하지 않는다. Step 1 else 분기의 bash 변수 할당 형식을 제거하되, `PHASE_NUM`이 남은 단계에서 여전히 사용되므로 Read 지시문 바로 다음에 `Set PHASE_NUM to the extracted value.` 문장을 추가한다.

    2. `skills/sg-plan/SKILL.md`를 읽고 두 패턴을 교체한다:
       - Step 1: STATE.md Phase 파싱 bash 블록 → `Read .planning/STATE.md, then extract the Phase: value from the YAML frontmatter. Set PHASE_NUM to the extracted value.`
       - Step 2.5 PREV_STAGE: `grep -E '^\| [0-9]{4}-' "$HANDOFF_FILE" | tail -1 | awk -F'|' ...` → `Read .planning/HANDOFF.md, then extract the To column (5th pipe-delimited field) from the last row that starts with "| " followed by a 4-digit year. Set PREV_STAGE to the extracted value (empty string if no data rows exist).`

    3. `skills/sg-ship/SKILL.md`를 읽고 두 패턴을 교체한다:
       - Step 1: STATE.md Phase 파싱 bash 블록 → `Read .planning/STATE.md, then extract the Phase: value from the YAML frontmatter. Set PHASE_NUM to the extracted value.`
       - Step 1.5 FROM_STAGE: HANDOFF bash 파싱 → `Read .planning/HANDOFF.md, then extract the To column (5th pipe-delimited field) from the last row that starts with "| " followed by a 4-digit year. Set FROM_STAGE to the extracted value (default "review" if empty).`

    4. `skills/sg-ui-plan/SKILL.md`를 읽고 두 패턴을 교체한다:
       - Step 1: STATE.md Phase 파싱 bash 블록 → `Read .planning/STATE.md, then extract the Phase: value from the YAML frontmatter. Set PHASE_NUM to the extracted value.`
       - Step 4 FROM_STAGE: HANDOFF bash 파싱 → `Read .planning/HANDOFF.md, then extract the To column (5th pipe-delimited field) from the last row that starts with "| " followed by a 4-digit year. Set FROM_STAGE to the extracted value.`
  </steps>
  <verify>
    <automated>grep -c 'grep -E.*Phase.*sed\|awk.*print.*Phase\|grep -E.*milestone.*awk' skills/sg-complete/SKILL.md skills/sg-plan/SKILL.md skills/sg-ship/SKILL.md skills/sg-ui-plan/SKILL.md || true</automated>
    <automated>grep -rn "grep -E '\\^\\\| \[0-9\]" skills/sg-complete/SKILL.md skills/sg-plan/SKILL.md skills/sg-ship/SKILL.md skills/sg-ui-plan/SKILL.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "grep -E '\^Phase:'" skills/sg-complete/SKILL.md skills/sg-plan/SKILL.md skills/sg-ship/SKILL.md skills/sg-ui-plan/SKILL.md` 결과 0건
    - `grep -rn "awk -F'\|'" skills/sg-complete/SKILL.md skills/sg-plan/SKILL.md skills/sg-ship/SKILL.md skills/sg-ui-plan/SKILL.md` 결과 0건
    - `grep -c "Read .planning/STATE.md" skills/sg-complete/SKILL.md` 결과 2 이상 (Phase + milestone 두 곳)
    - `grep -c "Read .planning/HANDOFF.md" skills/sg-complete/SKILL.md` 결과 1
    - HANDOFF 초기화 블록(`if [ ! -f "$HANDOFF_FILE" ]`)은 변경 없이 존재
  </acceptance_criteria>
  <done>4개 파일에서 STATE.md Phase/milestone 파싱과 HANDOFF.md 마지막 행 파싱이 모두 Read 도구 지시문으로 교체된다.</done>
</task>

<task type="auto">
  <name>Task 2: sg-execute / sg-review — Pattern A+B+C+D 전환</name>
  <files>
    skills/sg-execute/SKILL.md
    skills/sg-review/SKILL.md
  </files>
  <read_first>
    - skills/sg-execute/SKILL.md — Step 1(Phase 파싱), Step 3(ROADMAP 파싱), Step 8(HANDOFF 파싱), Step 8.5(wave/files_modified awk — 유지 대상) 위치 확인
    - skills/sg-review/SKILL.md — Step 3(Phase + objective 파싱), Step 3.9(Phase + HANDOFF 파싱) 위치 확인
    - .planning/phases/32-skill-superpowers-migration/32-PATTERNS.md — 패턴 C/D Before/After 확인
  </read_first>
  <steps>
    1. `skills/sg-execute/SKILL.md` Step 1: STATE.md Phase 파싱 bash 블록 교체:
       `grep -E '^Phase:' .planning/STATE.md | head -1 | sed -E 's/^Phase:[[:space:]]*//' | awk '{print $1}'`
       → `Read .planning/STATE.md, then extract the Phase: value from the YAML frontmatter. Set PHASE_NUM to the extracted value.`

    2. `skills/sg-execute/SKILL.md` Step 3: ROADMAP.md 파싱 bash 블록 전체(PHASE_HEADER/PHASE_NAME/HEADER_LINE/GOAL/REQ_IDS/REQ_IDS_CLEAN/SC_TEXT 추출 코드) 교체:
       ```
       Read .planning/ROADMAP.md, then:
       - Find the `### Phase <PHASE_NUM>:` section header; extract PHASE_NAME (text after "Phase <PHASE_NUM>: " on that line).
       - Extract the **Goal**: line value immediately following the header. Set GOAL.
       - Extract the requirement IDs from the **Requirements**: line (comma-separated, strip parenthetical labels). Set REQ_IDS_CLEAN as a space-separated list.
       - Extract numbered items under **Success Criteria** until the next `**` section. Set SC_TEXT.
       ```

    3. `skills/sg-execute/SKILL.md` Step 8 FROM_STAGE: HANDOFF bash 파싱 교체:
       `grep -E '^\| [0-9]{4}-' .planning/HANDOFF.md | tail -1 | awk -F'|' '{gsub(/ /,"",$5); print $5}'`
       → `Read .planning/HANDOFF.md, then extract the To column (5th pipe-delimited field) from the last row starting with "| " followed by a 4-digit year. Set FROM_STAGE (default "init" if empty).`

       주의: Step 8의 EXISTING_HASH `grep -E "^\| [^|]+ \|...` 줄은 변경하지 않는다 — 해시 비교용 grep으로 HANDOFF 파싱과 다른 목적이다.
       주의: Step 8.5의 `grep -rl '^wave:'` 및 wave/files_modified awk 파싱 블록 전체는 변경하지 않는다 (32-PATTERNS.md "No Analog Found" 항목).

    4. `skills/sg-review/SKILL.md` Step 3: 두 개의 Phase 파싱 bash 블록 교체:
       - Step 3 PHASE_NUM: `grep -E '^Phase:'...sed...awk` → `Read .planning/STATE.md, then extract the Phase: value from the YAML frontmatter. Set PHASE_NUM.`
       - Step 3 PLAN_REQUIREMENTS: `sed -n '/<objective>/,/<\/objective>/p' "$PLAN_FILE" 2>/dev/null | grep -v 'objective>'` → `Read the PLAN_FILE path, then extract the text content between <objective> and </objective> tags. Set PLAN_REQUIREMENTS.`

    5. `skills/sg-review/SKILL.md` Step 3.9: Phase 파싱 + HANDOFF 파싱 교체:
       - PHASE_NUM_R: `grep -E '^Phase:'...sed...awk` → `Read .planning/STATE.md (already read above — reuse the extracted Phase value). Set PHASE_NUM_R.` (이미 읽은 파일이므로 "reuse" 지시)
       - FROM_STAGE_R: `grep -E '^\| [0-9]{4}-' "$HANDOFF_FILE" | tail -1 | awk -F'|' ...` → `Read .planning/HANDOFF.md, then extract the To column (5th pipe-delimited field) from the last row starting with "| " followed by a 4-digit year. Set FROM_STAGE_R (default "init" if empty).`
  </steps>
  <verify>
    <automated>grep -n "grep -E '\\^Phase:'" skills/sg-execute/SKILL.md skills/sg-review/SKILL.md | wc -l</automated>
    <automated>grep -n "PHASE_HEADER\|REQ_IDS_CLEAN.*sed\|SC_TEXT.*awk" skills/sg-execute/SKILL.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "grep -E '\^Phase:'" skills/sg-execute/SKILL.md` 결과 0건
    - `grep -n "grep -E '\^Phase:'" skills/sg-review/SKILL.md` 결과 0건
    - `grep -n "sed -n '/<objective>/,/<.objective>/p'" skills/sg-review/SKILL.md` 결과 0건
    - `grep -n "PHASE_HEADER\|HEADER_LINE.*awk\|REQ_IDS.*sed.*//g" skills/sg-execute/SKILL.md` 결과 0건
    - `grep -n "grep -rl '\^wave:'" skills/sg-execute/SKILL.md` 결과 1건 (Step 8.5 보존)
    - Step 8의 EXISTING_HASH grep 줄은 변경 없이 존재
  </acceptance_criteria>
  <done>sg-execute의 STATE.md/ROADMAP.md/HANDOFF.md 파싱과 sg-review의 STATE.md/PLAN.md/HANDOFF.md 파싱이 모두 Read 도구 지시문으로 교체된다. Step 8.5 wave awk 블록은 변경되지 않는다.</done>
</task>

<task type="auto">
  <name>Task 3: sg-lessons / sg-quick — Pattern E/F/G 전환</name>
  <files>
    skills/sg-lessons/SKILL.md
    skills/sg-quick/SKILL.md
  </files>
  <read_first>
    - skills/sg-lessons/SKILL.md — Step 0 MILESTONE_ARG, Step 0 ARG_NUM grep -oE 패턴 위치 확인
    - skills/sg-quick/SKILL.md — Step 1 DESCRIPTION sed flag strip, Step 7 awk STATE.md 업데이트 위치 확인
    - .planning/phases/32-skill-superpowers-migration/32-PATTERNS.md — 패턴 E/F/G Before/After 확인 (sg-new SKILL.md의 node -e 예시 형식)
  </read_first>
  <steps>
    1. `skills/sg-lessons/SKILL.md` Step 0 MILESTONE_ARG 교체:
       `echo "$ARGUMENTS" | grep -oE 'milestone=[^ ]+' | head -1 | sed 's/milestone=//'`
       → node -e 방식:
       ```bash
       MILESTONE_ARG=$(node -e "
         const args = process.env.ARGUMENTS || '';
         const m = args.match(/milestone=([^ ]+)/);
         process.stdout.write(m ? m[1] : '');
       " 2>/dev/null)
       ```
       주의: `ARGUMENTS` 환경변수로 전달. `2>/dev/null` 포함.

    2. `skills/sg-lessons/SKILL.md` Step 0 ARG_NUM도 같은 방식으로 교체:
       `echo "$ARGUMENTS" | grep -oE '[0-9]+' | head -1`
       → node -e 방식:
       ```bash
       ARG_NUM=$(node -e "
         const args = process.env.ARGUMENTS || '';
         const m = args.match(/([0-9]+)/);
         process.stdout.write(m ? m[1] : '');
       " 2>/dev/null)
       ```

    3. `skills/sg-quick/SKILL.md` Step 1 DESCRIPTION sed flag strip 교체:
       `echo "$ARGS" | sed 's/--discuss//g; s/--research//g; s/--validate//g; s/--full//g' | sed 's/^[[:space:]]*//; s/[[:space:]]*$//'`
       → node -e 방식:
       ```bash
       DESCRIPTION=$(node -e "
         const a = process.env.ARGUMENTS || '';
         process.stdout.write(a.replace(/--discuss|--research|--validate|--full/g,'').trim());
       " 2>/dev/null)
       ```
       주의: `ARGS` 변수 선언(Step 1 첫 줄 `ARGS="$ARGUMENTS"`)은 유지한다 — flag 파싱 루프에서 사용됨.
       단, DESCRIPTION 할당만 위 node -e 방식으로 교체한다.

    4. `skills/sg-quick/SKILL.md` Step 7 awk STATE.md 테이블 업데이트 교체:
       전체 awk 블록(`awk -v row="$NEW_ROW" '...' .planning/STATE.md > .planning/STATE.md.tmp && mv ...`)을 다음 지시문으로 교체:
       ```
       Read .planning/STATE.md.
       Find the ### Quick Tasks Completed table section.
       Append NEW_ROW after the last existing table row in that section (insert after the last line starting with "|" within the section).
       Write the updated content back using the Edit tool.
       If the section is not found, print exactly: `ERROR: ### Quick Tasks Completed section not found in STATE.md` and exit.
       ```
       주의: NEW_ROW 변수 조립 코드(`DIR_NAME`, `SAFE_DESCRIPTION`, `NEW_ROW` 할당 bash)는 변경하지 않는다 — 변수 값 자체를 지시문에서 참조해야 하므로.
  </steps>
  <verify>
    <automated>grep -n "grep -oE\|sed 's/milestone" skills/sg-lessons/SKILL.md | wc -l</automated>
    <automated>grep -n "sed 's/--discuss//g" skills/sg-quick/SKILL.md | wc -l</automated>
    <automated>grep -n "awk -v row=" skills/sg-quick/SKILL.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "grep -oE 'milestone" skills/sg-lessons/SKILL.md` 결과 0건
    - `grep -n "grep -oE '\[0-9\]" skills/sg-lessons/SKILL.md` 결과 0건
    - `grep -n "sed 's/--discuss//g" skills/sg-quick/SKILL.md` 결과 0건
    - `grep -n "awk -v row=" skills/sg-quick/SKILL.md` 결과 0건
    - `grep -n "node -e" skills/sg-lessons/SKILL.md` 결과 2건 (MILESTONE_ARG, ARG_NUM)
    - `grep -n "node -e" skills/sg-quick/SKILL.md` 결과 1건 이상 (DESCRIPTION)
    - `grep -n "Read .planning/STATE.md" skills/sg-quick/SKILL.md` 결과 1건 이상 (Step 7)
    - `ARGS="$ARGUMENTS"` 줄은 sg-quick SKILL.md에 여전히 존재
  </acceptance_criteria>
  <done>sg-lessons의 argument 파싱 두 곳이 node -e로, sg-quick의 flag strip과 STATE.md 테이블 업데이트가 각각 node -e / Read+Edit 지시문으로 교체된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md 텍스트 편집 | 지시문 오류로 인한 동작 변경 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | Step 8.5 wave/files_modified awk 블록 | mitigate | accept_criteria에서 `grep -rl '^wave:'` 줄 보존 명시적 검증 |
| T-32-02 | Tampering | HANDOFF 초기화 bash 블록 | mitigate | acceptance_criteria에서 `if [ ! -f "$HANDOFF_FILE" ]` 보존 확인 |
| T-32-03 | Tampering | EXISTING_HASH grep 줄 (sg-execute Step 8) | mitigate | acceptance_criteria에서 명시적 보존 확인 |
</threat_model>

<verification>
```bash
# 전체 변환 검증: 8개 파일에서 bash Phase 파싱 0건
grep -rn "grep -E '\^Phase:'" \
  skills/sg-complete/SKILL.md \
  skills/sg-execute/SKILL.md \
  skills/sg-plan/SKILL.md \
  skills/sg-review/SKILL.md \
  skills/sg-ship/SKILL.md \
  skills/sg-ui-plan/SKILL.md \
  skills/sg-lessons/SKILL.md \
  skills/sg-quick/SKILL.md

# 보존 확인: sg-execute Step 8.5 wave 파싱 유지
grep -n "grep -rl '\^wave:'" skills/sg-execute/SKILL.md

# 보존 확인: sg-status, sg-next, sg-start 변경 없음
git diff HEAD -- skills/sg-status/SKILL.md skills/sg-next/SKILL.md skills/sg-start/SKILL.md | wc -c
```
</verification>

<success_criteria>
1. `grep -rn "grep -E '\^Phase:'" skills/sg-{complete,execute,plan,review,ship,ui-plan}/SKILL.md` 결과 0건
2. `grep -rn "awk -F'\|'.*gsub.*print.*5" skills/sg-{complete,execute,plan,review,ship,ui-plan}/SKILL.md` 결과 0건
3. `grep -n "sed -n '/<objective>" skills/sg-review/SKILL.md` 결과 0건
4. `grep -n "awk -v row=" skills/sg-quick/SKILL.md` 결과 0건
5. `grep -n "grep -oE 'milestone" skills/sg-lessons/SKILL.md` 결과 0건
6. `grep -n "grep -rl '\^wave:'" skills/sg-execute/SKILL.md` 결과 1건 이상 (Step 8.5 보존)
7. `git diff HEAD -- skills/sg-status/SKILL.md skills/sg-next/SKILL.md skills/sg-start/SKILL.md` 빈 출력
</success_criteria>

<output>
Create `.planning/phases/32-skill-superpowers-migration/32-01-SUMMARY.md` when done.
</output>
