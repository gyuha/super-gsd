# Roadmap Archive — v2.10 Plan-Phase Ambiguity Grilling

**Completed:** 2026-05-31
**Phases:** 45
**Status:** Shipped

---

## Goal

`sg-plan`이 discuss subagent를 호출하기 전, 메인 컨텍스트에서 grill-me 원칙으로 사용자를 모호함이 해소될 때까지 질문하여 계획 입력의 불확실성을 제거한다.

**참고 모델:** mattpocock grill-me skill — "합의된 이해에 도달할 때까지 집요하게 인터뷰, 설계 트리 분기를 따라 의존성을 하나씩 해소, 질문마다 권장 답변 제시, 한 번에 하나씩, 코드베이스로 답할 수 있으면 묻지 말고 탐색."

---

## Phase 45: sg-plan Grilling Step

**Goal**: sg-plan을 실행한 사용자가 gsd-discuss-phase subagent 호출 전에 모호함이 완전히 해소된 상태에서 계획을 시작한다
**Depends on**: Phase 44 (v2.10 신규 시작)
**Requirements**: GRILL-01, GRILL-02, GRILL-03, GRILL-04, GRILL-05, GRILL-06
**Scope note**: gsd-discuss-phase는 수정하지 않는다 (non-invasive 제약). grilling은 sg-plan 메인 컨텍스트의 선행 단계로만 구현한다. 쌍 파일 규칙: `skills/sg-plan/SKILL.md`와 `.agents/skills/sg-plan/SKILL.md` 모두 변경한다.
**Status**: Complete (2026-05-31)

**Success Criteria** (achieved):

  1. `/super-gsd:sg-plan` 실행 시 gsd-discuss-phase 호출 전에 Claude가 사용자 언어로 질문을 한 번에 하나씩 제시하며, 각 질문에 권장 답변이 함께 표시된다 (GRILL-01, GRILL-02)
  2. 코드베이스 탐색으로 답할 수 있는 질문(파일 존재 여부, 현재 구현 방식 등)은 사용자에게 묻지 않고 Claude가 직접 Read/Bash로 탐색해 해소한다 (GRILL-03)
  3. 이전 답변이 다음 질문의 분기를 결정하며, 설계 트리 의존성이 순서대로 해소된다 (GRILL-04)
  4. Claude가 모든 모호함이 해소됐다고 판단하면 합의 요약을 제시하고, 사용자가 "확정" 또는 "추가 질문"으로 최종 결정한다 — Claude가 단독으로 종료하지 않는다 (GRILL-05)
  5. 합의 요약(결정·제약 목록)이 gsd-discuss-phase의 CONTEXT 입력으로 전달되어 후속 plan에 반영된다 (GRILL-06)

**Plans**: 1 plan

- [x] 45-01-PLAN.md — skills/sg-plan/SKILL.md + .agents/skills/sg-plan/SKILL.md에 grill 단계 삽입 (GRILL-01~06)

**Mechanism**:

- `skills/sg-plan/SKILL.md` — grill = **Step 1.5** (AskUserQuestion, Step 1 직후 / Step 2 직전), +25줄
- `.agents/skills/sg-plan/SKILL.md` — grill = **Step 1.2** (프로즈 번호 선택 폴백, Visual Companion Step 1.5 직전), +43줄
- 양쪽 모두 grill 합의를 gsd-discuss-phase Agent 프롬프트에 "locked context — do NOT re-ask"로 inline 주입 (Non-invasive)

**Commits**: 1eb8e2e (plan), 3ed7221 (feat), a39f18e (merge), 207a569 (state)

---

## Milestone Patterns

- **단일 phase 마일스톤 (coarse granularity)**: v2.10은 Phase 45 하나로 GRILL-01~06 6개 요구사항을 모두 커버. `granularity: coarse` 설정과 일관.
- **Pairwise mirror discipline 유지**: `skills/sg-plan` + `.agents/skills/sg-plan` 동일 커밋 변경 (D-15) — 동작 의미는 동일, 질문 표면화 메커니즘만 플랫폼별로 분기 (AskUserQuestion vs 프로즈 폴백).
- **Non-invasive prompt injection**: gsd-discuss-phase를 수정하지 않고 Agent 프롬프트 문단 주입으로 grill 합의를 전달 — 기존 orchestration 패턴 답습.
- **Plan-spec deviation 투명성**: plan이 `.agents` grill을 "Step 1.6"으로 라벨했으나 "Step 1.5 이전" 요구와 산술 모순(1.6 > 1.5) → 실행 중 사용자 확인 후 Step 1.2로 정정, SUMMARY에 deviation 명시.

## Carried Over to Next Milestone

- **Plan-lint 도입** — step 번호 산술-순서 일관성 자동 점검 (v2.9에서도 누적된 미도입 항목)
- **두 sg-plan SKILL.md 구조 divergence 정합** — `.claude`에 Visual Companion 부재 등, 본 마일스톤 scope 외 deferred
- **미러 파일 분량 비대칭 시 divergence 검토** — plan/review 체크리스트 항목 후보
- **STATE.md `Phase:` field auto-sync** — 다수 마일스톤 연속 defer
