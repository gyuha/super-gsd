---
phase: quick-260516-2qm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [quick-task]
must_haves:
  truths:
    - "Commands 섹션 바로 아래에 'Usage Examples' 섹션이 존재한다"
    - "신규 프로젝트 시작부터 ship까지 end-to-end 예시 워크플로우가 서술되어 있다"
    - "각 sg-* 명령어에 대해 구체적인 사용 시나리오가 포함되어 있다"
    - "sg-quick 같은 메인 흐름 외 명령어에도 단독 사용 예시가 있다"
  artifacts:
    - path: "README.md"
      provides: "Usage Examples 섹션 포함한 README"
      contains: "## Usage Examples"
  key_links:
    - from: "Commands 섹션"
      to: "Usage Examples 섹션"
      via: "문서 순서 — Commands 표 직후에 위치"
---

<objective>
README.md의 Commands 섹션 아래에 "Usage Examples" 섹션을 추가한다.

Purpose: 사용자가 sg-* 명령어를 처음 보고 "그래서 실제로 어떻게 쓰는 거지?"라는 질문에 바로 답할 수 있도록, 신규 프로젝트 시작부터 ship까지의 end-to-end 시나리오와 개별 명령어 사용 맥락을 예시로 보여준다.
Output: Usage Examples 섹션이 추가된 README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/super-gsd/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md에 Usage Examples 섹션 추가</name>
  <files>README.md</files>
  <action>
    Commands 섹션과 Prerequisites 섹션 사이에 "## Usage Examples" 섹션을 삽입한다.

    섹션 구성:
    1. **End-to-End Workflow** — 신규 프로젝트 시작부터 ship까지 전체 흐름을 보여주는 단일 예시 블록.
       결제 기능을 추가하는 가상 시나리오로, 각 명령어 실행 시점과 그 이유를 인라인 주석(# ...)으로 설명한다.
       예시 블록 형식은 셸 코드 블록(`shell`)으로 작성한다.

    2. **Individual Command Examples** — 메인 흐름에 없는 명령어와 선택적 사용 시나리오를 짧게 서술한다.
       포함할 명령어:
       - `sg-status`: 중간 확인 (언제든 실행 가능)
       - `sg-lessons`: sg-plan 전에 이전 회고 내용 검토
       - `sg-update`: 도구 일괄 업데이트
       - `sg-quick`: 메인 워크플로우 밖의 소규모 단발성 작업 (예: 버그 픽스, 문서 수정)
       각각 1-2줄의 셸 코드 블록으로 보여준다.

    End-to-End 예시에서 사용할 시나리오:
    - 프로젝트명: `my-saas-app` (이미 Claude Code에서 열려 있는 상태)
    - 작업: "결제 모듈 추가" 마일스톤
    - 흐름: sg-start → sg-explore → sg-plan → sg-execute → (구현) → sg-review → sg-learn → sg-ship

    삽입 위치: `See [docs/COMMANDS.md]...` 줄 직후, `## Prerequisites` 줄 직전.

    문서 언어: 영어 (기존 README 언어 일치). 한국어 요약 섹션은 수정하지 않는다.
  </action>
  <verify>
    <automated>grep -n "## Usage Examples" /Users/gyuha/workspace/super-gsd/README.md</automated>
  </verify>
  <done>
    - "## Usage Examples" 섹션이 Commands 섹션과 Prerequisites 섹션 사이에 존재한다
    - End-to-End Workflow 코드 블록에 sg-start부터 sg-ship까지 전체 명령어 시퀀스가 있다
    - sg-status, sg-lessons, sg-update, sg-quick 각각의 단독 예시가 있다
    - 기존 섹션(Prerequisites, Installation 등)은 수정되지 않았다
  </done>
</task>

</tasks>

<verification>
grep -n "## Usage Examples\|sg-start\|sg-ship\|sg-quick\|sg-lessons" /Users/gyuha/workspace/super-gsd/README.md
</verification>

<success_criteria>
README.md를 열었을 때 Commands 표 바로 아래에 실제 사용 예시를 볼 수 있다. 신규 사용자가 처음 읽어도 "어떤 순서로 명령을 쓰면 되는지" 파악할 수 있는 수준의 구체성을 갖춘다.
</success_criteria>

<output>
완료 후 `.planning/quick/260516-2qm-readme-md/260516-2qm-SUMMARY.md` 생성
</output>
