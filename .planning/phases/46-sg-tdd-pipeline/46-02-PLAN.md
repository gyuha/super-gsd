---
phase: 46-sg-tdd-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - hooks/transcript_matcher.cjs
  - hooks/stop_hook.cjs
  - skills/sg-next/SKILL.md
  - skills/sg-status/SKILL.md
  - .agents/skills/sg-next/SKILL.md
  - .agents/skills/sg-status/SKILL.md
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03

must_haves:
  truths:
    - "transcript_matcher.cjs가 'TDD verification complete' 문자열을 감지하면 'tdd-complete' 신호를 반환한다"
    - "stop_hook.cjs가 tdd stage에서 'tdd-complete' 신호를 매핑하고, sg-review 안내를 systemMessage로 출력한다"
    - "sg-next가 execute 완료 상태에서 tdd_mode=true이면 sg-tdd로, false/미설정이면 sg-review로 라우팅한다"
    - "sg-status가 sg-next와 동일한 execute→tdd 조건 분기를 가진다 (D-07 inline-replication)"
    - "HANDOFF.md stage enum에 tdd가 추가되어 Unknown stage 오류가 발생하지 않는다"
    - ".agents/skills/sg-next와 sg-status 미러가 skills/ 버전과 동기화된다"
  artifacts:
    - path: "hooks/transcript_matcher.cjs"
      provides: "TDD_SIGNALS 배열 + detectSignal() tdd-complete 분기"
      contains: "tdd-complete"
    - path: "hooks/stop_hook.cjs"
      provides: "stageToSignal() tdd 케이스 + main() tdd-complete 분기"
      contains: "tdd-complete"
    - path: "skills/sg-next/SKILL.md"
      provides: "execute→tdd 조건 라우팅 + tdd enum"
      contains: "tdd_mode"
    - path: "skills/sg-status/SKILL.md"
      provides: "D-07 sg-next 동시 갱신"
      contains: "tdd_mode"
  key_links:
    - from: "hooks/stop_hook.cjs stageToSignal()"
      to: "hooks/transcript_matcher.cjs detectSignal()"
      via: "signal string 'tdd-complete'"
      pattern: "tdd-complete"
    - from: "skills/sg-next/SKILL.md execute) branch"
      to: ".planning/config.json super_gsd.tdd_mode"
      via: "node -e inline read"
      pattern: "tdd_mode"
---

<objective>
hooks/transcript_matcher.cjs, hooks/stop_hook.cjs, skills/sg-next/SKILL.md, skills/sg-status/SKILL.md, .agents/skills/sg-next/SKILL.md, .agents/skills/sg-status/SKILL.md 6개 파일을 수정하여 tdd stage를 파이프라인에 통합한다.

Purpose: sg-tdd가 Wave 1에서 생성된 이후, 기존 파이프라인(stop_hook 신호 감지, sg-next/sg-status 라우팅)이 tdd stage를 인식하도록 연결한다.
Output: 6개 파일 수정 — 모두 외과적(surgical) 삽입이며 기존 로직 변경 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-sg-tdd-pipeline/46-CONTEXT.md
@.planning/phases/46-sg-tdd-pipeline/46-PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
<!-- 아래 시그니처를 수정 대상 파일에 적용한다. 탐색 불필요. -->

From hooks/transcript_matcher.cjs (현재 상태, 전체 파일):
- const SG_RETRO_SIGNALS = ['lessons file:', '## Lens:', 'Retrospective complete']; (line 21-25)
- detectSignal() 끝 분기: if (SG_RETRO_SIGNALS.some(...)) return 'sg-retro-complete'; return ''; (line 49-50)
- 삽입 위치: SG_RETRO_SIGNALS 배열 바로 뒤에 TDD_SIGNALS 배열 추가, detectSignal() 끝에서 return '' 앞에 tdd 분기 추가

From hooks/stop_hook.cjs (현재 상태):
- stageToSignal() switch: case 'execute': return 'superpowers-implementation-complete'; / case 'review': return 'superpowers-review-complete'; (lines 133-136)
- main() cmd 변수: let cmdExecute, cmdReview, cmdLearn, cmdShip; (line 184)
- main() if/else-if 체인: } else if (signal === 'superpowers-implementation-complete') { ... } else if (signal === 'superpowers-review-complete') { (lines 200-202)
- 삽입 위치: stageToSignal()에 case 'tdd': return 'tdd-complete'; (execute와 review 사이), main()에 cmdTdd 변수 추가, tdd-complete if/else-if 분기 추가

From skills/sg-next/SKILL.md (현재 상태):
- case 열거 line 45: gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete|sg-next
- scan-back case line 60: init|gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete
- execute) 단일 분기 line 110: execute)     NEXT_CMD="/super-gsd:sg-review" ;;
- Skill() 매핑 Step 6 목록 (lines 200-207)
- success_criteria 목록

From skills/sg-status/SKILL.md (현재 상태):
- case 열거 line 101: gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete|sg-next
- scan-back case line 116: init|gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete
- display enum case line 129: execute)      STAGE_DISPLAY="superpowers" ;; / review)       STAGE_DISPLAY="superpowers" ;;
- execute) 단일 분기 line 177: execute)     NEXT_CMD="/super-gsd:sg-review" ;;

From .agents/skills/sg-next/SKILL.md (현재 상태):
- case 열거 line 53: gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete|sg-next
- scan-back case line 68: init|gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete
- execute) 분기 line 104: execute)     NEXT_CMD="\$sg-review" ;;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: hooks/transcript_matcher.cjs — TDD_SIGNALS 추가 + detectSignal() 분기</name>
  <files>hooks/transcript_matcher.cjs</files>
  <read_first>
    - hooks/transcript_matcher.cjs — 전체 파일 (53행). TDD_SIGNALS 삽입 위치(line 25 뒤)와 detectSignal() return '' 위치(line 50) 확인
    - .planning/phases/46-sg-tdd-pipeline/46-PATTERNS.md §transcript_matcher.cjs — 정확한 삽입 패턴
  </read_first>
  <action>
    두 곳에 외과적 삽입만 한다. 기존 코드는 수정하지 않는다.

    삽입 1 — SG_RETRO_SIGNALS 배열 닫는 세미콜론 바로 뒤에 빈 줄 + TDD_SIGNALS 배열 추가:
    const TDD_SIGNALS = [
      'TDD verification complete',
    ];

    삽입 2 — detectSignal() 함수 내부에서 SG_RETRO_SIGNALS 분기 바로 뒤, return '' 바로 앞에 추가:
    if (TDD_SIGNALS.some(sig => recent.includes(sig))) return 'tdd-complete';

    이 두 삽입 외 파일의 어떤 부분도 수정하지 않는다.
  </action>
  <verify>
    <automated>node -e "const m=require('./hooks/transcript_matcher.cjs'); console.log(m.detectSignal === typeof m.detectSignal ? 'ok' : 'fail')" && grep -c "tdd-complete" hooks/transcript_matcher.cjs && grep -c "TDD verification complete" hooks/transcript_matcher.cjs</automated>
  </verify>
  <acceptance_criteria>
    - const TDD_SIGNALS = ['TDD verification complete']; 배열이 SG_RETRO_SIGNALS 뒤에 존재한다
    - detectSignal() 함수 내부에 "return 'tdd-complete'" 분기가 있다
    - 분기 순서: GSD_PLAN → IMPLEMENTATION → REVIEW → SG_RETRO → TDD → return '' (TDD 분기가 SG_RETRO 뒤, return '' 앞에 위치)
    - node -e "const m=require('./hooks/transcript_matcher.cjs');" 실행 시 오류 없음
    - grep -c "tdd-complete" hooks/transcript_matcher.cjs 결과가 1 이상
  </acceptance_criteria>
  <done>transcript_matcher.cjs가 "TDD verification complete" 신호를 감지하면 "tdd-complete"를 반환한다</done>
</task>

<task type="auto">
  <name>Task 2: hooks/stop_hook.cjs — stageToSignal() + main() tdd 분기 추가</name>
  <files>hooks/stop_hook.cjs</files>
  <read_first>
    - hooks/stop_hook.cjs — 전체 파일 (221행). stageToSignal() switch(line 124-142), main() cmd vars(line 184-195), if/else-if 체인(line 197-208) 위치 확인
    - .planning/phases/46-sg-tdd-pipeline/46-PATTERNS.md §stop_hook.cjs — 두 삽입 지점의 정확한 after-state
  </read_first>
  <action>
    세 곳에 외과적 삽입/수정만 한다. 기존 코드 로직은 변경하지 않는다.

    수정 1 — stageToSignal() switch 문 (per PATTERNS.md):
    현재 case 'execute': return 'superpowers-implementation-complete'; 줄 다음, case 'review' 줄 이전에 다음 케이스를 추가한다:
    case 'tdd':
      return 'tdd-complete';                            // → "Run sg-review"

    수정 2 — main() 함수 내 cmdTdd 변수 추가:
    현재 let cmdExecute, cmdReview, cmdLearn, cmdShip; 선언을 let cmdExecute, cmdReview, cmdLearn, cmdShip, cmdTdd; 로 변경한다.
    platform === 'claude-code' 분기에 cmdTdd = '/super-gsd:sg-tdd'; 추가.
    else 분기에 cmdTdd = '$sg-tdd'; 추가.

    수정 3 — main() if/else-if 체인에 tdd-complete 분기 추가:
    현재 "superpowers-implementation-complete" 분기의 닫는 중괄호 다음, "superpowers-review-complete" 분기 이전에 삽입:
    } else if (signal === 'tdd-complete') {
      response = { systemMessage: `TDD verification complete. Run ${cmdReview} to request a code review.` };
    }

    이 세 수정 외 파일의 어떤 부분도 변경하지 않는다.
  </action>
  <verify>
    <automated>echo '{"cwd":"/tmp","stop_hook_active":true}' | node hooks/stop_hook.cjs | grep -c "{}" ; grep -c "tdd-complete" hooks/stop_hook.cjs ; grep -c "cmdTdd" hooks/stop_hook.cjs</automated>
  </verify>
  <acceptance_criteria>
    - stageToSignal() switch 문에 case 'tdd': return 'tdd-complete'; 케이스가 있다 (execute와 review 케이스 사이)
    - main() 함수에 cmdTdd 변수가 선언되어 있다
    - platform === 'claude-code' 분기에 cmdTdd = '/super-gsd:sg-tdd' 가 있다
    - else 분기에 cmdTdd = '$sg-tdd' 가 있다
    - if/else-if 체인에 signal === 'tdd-complete' 분기가 있다
    - 분기 메시지: "TDD verification complete. Run ${cmdReview} to request a code review."
    - grep -c "tdd-complete" hooks/stop_hook.cjs 결과가 2 이상 (stageToSignal + main 각 1건)
    - node -e "require('./hooks/stop_hook.cjs');" 실행 시 오류 없음 (모듈 로딩 검증)
  </acceptance_criteria>
  <done>stop_hook.cjs가 tdd stage를 인식하고 sg-review 안내 systemMessage를 출력한다</done>
</task>

<task type="auto">
  <name>Task 3: sg-next + sg-status + .agents/ 미러 — tdd 라우팅 + enum 갱신 (D-07 inline-replication)</name>
  <files>
    skills/sg-next/SKILL.md
    skills/sg-status/SKILL.md
    .agents/skills/sg-next/SKILL.md
    .agents/skills/sg-status/SKILL.md
  </files>
  <read_first>
    - skills/sg-next/SKILL.md — 전체 파일. D-07 block 경계 주석 위치, case 열거(line 45), scan-back case(line 60), execute) 분기(line 110), Skill() 매핑 목록(Step 6), success_criteria
    - skills/sg-status/SKILL.md — 전체 파일. case 열거(line 101), scan-back case(line 116), display enum(lines 122-135), execute) 분기(line 177)
    - .agents/skills/sg-next/SKILL.md — 전체 파일. case 열거(line 53), scan-back case(line 68), execute) 분기(line 104)
    - .agents/skills/sg-status/SKILL.md — 전체 파일. 동일 패턴 위치 확인
    - .planning/phases/46-sg-tdd-pipeline/46-PATTERNS.md §sg-next §sg-status §.agents 미러 — after-state 패턴
  </read_first>
  <action>
    4개 파일에 동시에 외과적 수정을 적용한다 (D-07 D-04 inline-replication 규칙 — 어느 한 파일만 수정하는 것은 코드 리뷰 블로커).
    각 파일의 기존 로직(Step 0 --team, HANDOFF 초기화, git 관련 코드 등)은 일체 수정하지 않는다.

    === skills/sg-next/SKILL.md ===

    수정 A1 — Step 2 case 열거 (D-05, per PATTERNS.md):
    아래 패턴 찾아 교체:
    찾기: gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete|sg-next) ;;
    교체: gsd-plan|ui-plan|superpowers|parallel|execute|tdd|review|sg-retro|ship|complete|sg-next) ;;

    수정 A2 — Step 2 scan-back re-validate case:
    찾기: init|gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete) ;;
    교체: init|gsd-plan|ui-plan|superpowers|parallel|execute|tdd|review|sg-retro|ship|complete) ;;

    수정 A3 — Step 3 next-command routing block 내 execute) 분기 (D-03, per PATTERNS.md):
    찾기(단일 행): execute)     NEXT_CMD="/super-gsd:sg-review" ;;
    교체(여러 행):
    execute)
      TDD_MODE=$(node -e "try{const c=require('./.planning/config.json');console.log(c.super_gsd&&c.super_gsd.tdd_mode?'true':'false')}catch(e){console.log('false')}" 2>/dev/null || echo "false")
      if [ "$TDD_MODE" = "true" ]; then
        NEXT_CMD="/super-gsd:sg-tdd"
      else
        NEXT_CMD="/super-gsd:sg-review"
      fi
      ;;
    tdd)         NEXT_CMD="/super-gsd:sg-review" ;;

    수정 A4 — Step 6 Skill() 매핑 목록에 sg-tdd 항목 추가:
    기존 /super-gsd:sg-execute → Skill(...) 목록 항목들 다음에 추가:
    - `/super-gsd:sg-tdd` → `Skill(skill="super-gsd:sg-tdd", args="")`

    수정 A5 — success_criteria에 tdd 라우팅 항목 추가:
    기존 success_criteria 목록 끝에 추가:
    6. When STAGE_RAW is tdd, routes to /super-gsd:sg-review.

    === skills/sg-status/SKILL.md ===

    수정 B1 — Step 2 case 열거 (A1과 동일 변경):
    찾기: gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete|sg-next) ;;
    교체: gsd-plan|ui-plan|superpowers|parallel|execute|tdd|review|sg-retro|ship|complete|sg-next) ;;

    수정 B2 — Step 2 scan-back case (A2와 동일 변경):
    찾기: init|gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete) ;;
    교체: init|gsd-plan|ui-plan|superpowers|parallel|execute|tdd|review|sg-retro|ship|complete) ;;

    수정 B3 — Step 2 display enum mapping에 tdd 추가:
    찾기:      execute)      STAGE_DISPLAY="superpowers" ;;
               review)       STAGE_DISPLAY="superpowers" ;;
    교체:      execute)      STAGE_DISPLAY="superpowers" ;;
               tdd)          STAGE_DISPLAY="superpowers" ;;
               review)       STAGE_DISPLAY="superpowers" ;;

    수정 B4 — Step 5 next-command routing execute) 분기 (A3과 동일 로직, $PHASE_NUM 들여쓰기 맞춤):
    찾기(단일 행): execute)     NEXT_CMD="/super-gsd:sg-review" ;;
    교체(여러 행, sg-status 들여쓰기 스타일 유지):
       execute)
         TDD_MODE=$(node -e "try{const c=require('./.planning/config.json');console.log(c.super_gsd&&c.super_gsd.tdd_mode?'true':'false')}catch(e){console.log('false')}" 2>/dev/null || echo "false")
         if [ "$TDD_MODE" = "true" ]; then
           NEXT_CMD="/super-gsd:sg-tdd"
         else
           NEXT_CMD="/super-gsd:sg-review"
         fi
         ;;
       tdd)         NEXT_CMD="/super-gsd:sg-review" ;;

    === .agents/skills/sg-next/SKILL.md ===

    수정 C1 — case 열거 (A1과 동일, $sg-* prefix 유지):
    찾기: gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete|sg-next) ;;
    교체: gsd-plan|ui-plan|superpowers|parallel|execute|tdd|review|sg-retro|ship|complete|sg-next) ;;

    수정 C2 — scan-back case (A2와 동일):
    찾기: init|gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|ship|complete) ;;
    교체: init|gsd-plan|ui-plan|superpowers|parallel|execute|tdd|review|sg-retro|ship|complete) ;;

    수정 C3 — execute) 분기 ($sg-* prefix, per PATTERNS.md):
    찾기(단일 행): execute)     NEXT_CMD="\$sg-review" ;;
    교체(여러 행):
    execute)
      TDD_MODE=$(node -e "try{const c=require('./.planning/config.json');console.log(c.super_gsd&&c.super_gsd.tdd_mode?'true':'false')}catch(e){console.log('false')}" 2>/dev/null || echo "false")
      if [ "$TDD_MODE" = "true" ]; then
        NEXT_CMD="\$sg-tdd"
      else
        NEXT_CMD="\$sg-review"
      fi
      ;;
    tdd)         NEXT_CMD="\$sg-review" ;;

    === .agents/skills/sg-status/SKILL.md ===

    수정 D1 ~ D4: .agents/skills/sg-next 변경과 동일한 패턴을 적용한다.
    - case 열거에 tdd 추가 (execute와 review 사이)
    - scan-back case에 tdd 추가
    - display enum에 tdd) STAGE_DISPLAY="superpowers" ;; 추가 (execute와 review 사이)
    - execute) 분기를 config 읽기 + 조건 분기로 교체 ($sg-* prefix 사용)
    - tdd) NEXT_CMD="\$sg-review" ;; 추가
  </action>
  <verify>
    <automated>grep -c "tdd" skills/sg-next/SKILL.md && grep -c "tdd_mode" skills/sg-next/SKILL.md && grep -c "tdd_mode" skills/sg-status/SKILL.md && grep -c "tdd" .agents/skills/sg-next/SKILL.md && grep -c "tdd" .agents/skills/sg-status/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/sg-next/SKILL.md: case 열거에 "execute|tdd|review" 패턴이 있다
    - skills/sg-next/SKILL.md: execute) 분기 내에 "tdd_mode" 읽기 로직이 있다
    - skills/sg-next/SKILL.md: "tdd)         NEXT_CMD="/super-gsd:sg-review"" 분기가 있다
    - skills/sg-next/SKILL.md: Step 6 Skill() 목록에 /super-gsd:sg-tdd 항목이 있다
    - skills/sg-status/SKILL.md: case 열거에 "execute|tdd|review" 패턴이 있다
    - skills/sg-status/SKILL.md: display enum에 tdd) STAGE_DISPLAY="superpowers" ;; 가 있다
    - skills/sg-status/SKILL.md: execute) 분기 내에 "tdd_mode" 읽기 로직이 있다
    - .agents/skills/sg-next/SKILL.md: case 열거에 "execute|tdd|review" 패턴이 있다
    - .agents/skills/sg-next/SKILL.md: execute) 분기 내에 NEXT_CMD="\$sg-tdd" 가 있다
    - .agents/skills/sg-status/SKILL.md: case 열거에 "execute|tdd|review" 패턴이 있다
    - 4개 파일 모두 grep -c "execute|tdd|review" 결과가 1 이상
    - grep -c "tdd_mode" skills/sg-next/SKILL.md 결과가 1 이상
    - grep -c "tdd_mode" skills/sg-status/SKILL.md 결과가 1 이상
  </acceptance_criteria>
  <done>sg-next, sg-status, .agents/ 미러 4개 파일이 모두 tdd stage를 인식하고 tdd_mode 조건 라우팅을 구현한다 (D-07 inline-replication 충족)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/config.json → sg-next/sg-status | tdd_mode 값은 사용자 제어 파일에서 읽음 |
| transcript → detectSignal() | 마지막 200줄 스캔 — 신호 문자열 스푸핑 가능성 있음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Spoofing | transcript_matcher.cjs detectSignal() | accept | 내부 도구 — 외부 공격 면적 없음. 신호 스푸핑 시 잘못된 stage 안내만 발생, 데이터 손실 없음 |
| T-46-05 | Tampering | config.json tdd_mode 임의 값 | accept | try/catch fallback → false. 악의적 값이 들어와도 최악은 false로 처리 |
| T-46-06 | Denial of Service | node -e inline 실행 실패 | accept | 2>/dev/null || echo "false" 폴백으로 기본값 false 보장 |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜에 패키지 설치 없음 |
</threat_model>

<verification>
Phase 46 Plan 02 완료 확인:
- grep -c "tdd-complete" hooks/transcript_matcher.cjs → 1 이상
- grep -c "tdd-complete" hooks/stop_hook.cjs → 2 이상 (stageToSignal + main)
- grep "execute|tdd|review" skills/sg-next/SKILL.md → 매칭 2건 이상 (case 열거 + scan-back)
- grep "execute|tdd|review" skills/sg-status/SKILL.md → 매칭 2건 이상
- grep "tdd_mode" skills/sg-next/SKILL.md → 매칭 1건 이상
- grep "tdd_mode" skills/sg-status/SKILL.md → 매칭 1건 이상
- grep "tdd_mode" .agents/skills/sg-next/SKILL.md → 매칭 1건 이상
- grep "tdd_mode" .agents/skills/sg-status/SKILL.md → 매칭 1건 이상
</verification>

<success_criteria>
- hooks/transcript_matcher.cjs: "TDD verification complete" → "tdd-complete" 신호 감지
- hooks/stop_hook.cjs: tdd stage → "TDD verification complete. Run {cmdReview}..." systemMessage
- skills/sg-next + skills/sg-status: execute→tdd (tdd_mode=true) 또는 execute→review (false) 조건 라우팅
- 4개 파일 모두 tdd enum이 execute와 review 사이에 배치됨
- D-07 inline-replication: sg-next와 sg-status가 동일 플랜에서 동시 갱신됨
- .agents/ 미러 2개 파일이 skills/ 버전과 동기화됨 ($sg-* prefix 사용)
</success_criteria>

<output>
완료 시 .planning/phases/46-sg-tdd-pipeline/46-02-SUMMARY.md를 생성한다
</output>
