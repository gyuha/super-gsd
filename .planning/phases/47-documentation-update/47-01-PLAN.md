---
phase: 47-documentation-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.ko.md
  - CLAUDE.md
autonomous: true
requirements: [DOC-01, DOC-02]

must_haves:
  truths:
    - "README.md Commands table has sg-tdd row between sg-execute and sg-review rows"
    - "README.ko.md Commands table has sg-tdd row between sg-execute and sg-review rows"
    - "Both READMEs pipeline string includes sg-tdd with tdd_mode=true conditional annotation"
    - "Both READMEs sg-next chain description mentions sg-tdd routing"
    - "CLAUDE.md data flow block has sg-tdd line after sg-execute line"
    - "CLAUDE.md Skills layer description says 22 instead of 21"
    - "CLAUDE.md has tdd_mode flag explanation paragraph after stop_hook.cjs bullet"
  artifacts:
    - path: "README.md"
      provides: "English command reference with sg-tdd row"
      contains: "sg-tdd"
    - path: "README.ko.md"
      provides: "Korean command reference with sg-tdd row"
      contains: "sg-tdd"
    - path: "CLAUDE.md"
      provides: "Architecture section updated for sg-tdd"
      contains: "super_gsd.tdd_mode"
  key_links:
    - from: "README.md"
      to: "Commands table"
      via: "sg-tdd row insertion"
      pattern: "sg-tdd.*superpowers:test-driven-development"
    - from: "CLAUDE.md"
      to: "data flow block"
      via: "sg-tdd line insertion"
      pattern: "sg-tdd.*Superpowers:test-driven-development"
---

<objective>
Update README.md, README.ko.md, and CLAUDE.md to document the sg-tdd command and tdd_mode config flag added in Phase 46. All changes are surgical text insertions — no structural changes.

Purpose: Users and downstream agents reading these files need accurate documentation of the full sg-execute → sg-tdd → sg-review pipeline including the conditional tdd_mode activation.
Output: Three updated documentation files with sg-tdd reflected in Commands tables, pipeline descriptions, data flow block, and architecture notes.
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/super-gsd/.planning/PROJECT.md
@/Users/gyuha/workspace/super-gsd/.planning/ROADMAP.md
@/Users/gyuha/workspace/super-gsd/.planning/STATE.md
@/Users/gyuha/workspace/super-gsd/.planning/phases/47-documentation-update/47-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md — sg-tdd row + pipeline + sg-next chain</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
Make three surgical insertions in README.md:

1. Line ~12: In the sentence "All twenty-one slash commands...", change `twenty-one` to `twenty-two` (per D-07 parity — README says 21 to match CLAUDE.md count).

   NOTE: README.md line 12 currently reads "All twenty-one slash commands..." — update to "All twenty-two slash commands...".

2. Line ~19 (Workflow diagram pipeline string): Replace the pipeline string
   `sg-new/sg-start → sg-explore → sg-plan → sg-execute → sg-review → sg-learn → sg-ship → sg-complete`
   with
   `sg-new/sg-start → sg-explore → sg-plan → sg-execute → sg-tdd (tdd_mode=true) → sg-review → sg-learn → sg-ship → sg-complete`
   (per D-04)

3. Line ~25 (sg-next description): Update the chain description inside the parentheses. The current text contains `parallel/execute → sg-review`. Replace that segment with `parallel/execute → sg-tdd (tdd_mode=true) → sg-review` (per D-05).

4. Lines ~39-40 (Commands table): Insert a new row between the `/super-gsd:sg-execute` row and the `/super-gsd:sg-review` row (per D-01, D-02):
   ```
   | `/super-gsd:sg-tdd` | Run a red-green-refactor TDD verification gate via `superpowers:test-driven-development` — only active when `super_gsd.tdd_mode: true` in `.planning/config.json` | After `sg-execute` completes, when `tdd_mode` is enabled |
   ```

Do NOT modify any other rows, formatting, or content. Match the pipe-table alignment style of existing rows exactly.
  </action>
  <verify>
    <automated>grep -n "sg-tdd" /Users/gyuha/workspace/super-gsd/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sg-tdd" README.md` returns 4 or more (pipeline line, sg-next line, table row command cell, table row description cell)
    - `grep "sg-tdd (tdd_mode=true)" README.md` returns 2 lines (pipeline + sg-next chain)
    - `grep "twenty-two" README.md` returns 1 line
    - `grep "/super-gsd:sg-tdd" README.md` returns 1 line (the table row)
    - `grep "superpowers:test-driven-development" README.md` returns 1 line (table row description)
    - The sg-tdd table row appears between the sg-execute row and the sg-review row (verify with grep -n context)
  </acceptance_criteria>
  <done>README.md Commands table has sg-tdd row in correct position, pipeline and sg-next chain descriptions include sg-tdd with tdd_mode=true annotation, and command count is updated to twenty-two.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.ko.md — sg-tdd row + pipeline + sg-next chain</name>
  <files>README.ko.md</files>
  <read_first>README.ko.md</read_first>
  <action>
Make three surgical insertions in README.ko.md, mirroring exactly what was done in Task 1 but in Korean (per D-03, D-04, D-05, D-09):

1. Line ~12: In the sentence "21개 슬래시 명령이 제공된다", change `21개` to `22개`.

   NOTE: README.ko.md line 12 currently reads "새 마일스톤 시작부터 완료 및 다음 마일스톤 시작까지 전체 GSD → Superpowers → sg-retro 사이클을 커버하는 21개 슬래시 명령이 제공된다" — update `21개` to `22개`.

2. Line ~19 (워크플로우 diagram pipeline string): Replace the pipeline string
   `sg-new/sg-start → sg-explore → sg-plan → sg-execute → sg-review → sg-learn → sg-ship → sg-complete`
   with
   `sg-new/sg-start → sg-explore → sg-plan → sg-execute → sg-tdd (tdd_mode=true) → sg-review → sg-learn → sg-ship → sg-complete`

3. Line ~25 (sg-next description): Update the chain description inside the Korean parenthesized text. The current text contains `parallel/execute → sg-review`. Replace that segment with `parallel/execute → sg-tdd (tdd_mode=true) → sg-review`.

4. Lines ~39-40 (명령어 table): Insert a new row between the `/super-gsd:sg-execute` row and the `/super-gsd:sg-review` row (per D-03):
   ```
   | `/super-gsd:sg-tdd` | `superpowers:test-driven-development`를 통해 red-green-refactor TDD 검증 게이트 실행 — `.planning/config.json`의 `super_gsd.tdd_mode: true`일 때만 활성화 | `tdd_mode`가 활성화된 상태에서 `sg-execute` 완료 후 |
   ```

Do NOT modify any other rows, formatting, or content. Match the pipe-table alignment style of existing rows exactly.
  </action>
  <verify>
    <automated>grep -n "sg-tdd" /Users/gyuha/workspace/super-gsd/README.ko.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sg-tdd" README.ko.md` returns 4 or more
    - `grep "sg-tdd (tdd_mode=true)" README.ko.md` returns 2 lines (pipeline + sg-next chain)
    - `grep "22개" README.ko.md` returns 1 line
    - `grep "/super-gsd:sg-tdd" README.ko.md` returns 1 line (the table row)
    - `grep "superpowers:test-driven-development" README.ko.md` returns 1 line
    - The sg-tdd table row appears between the sg-execute row and the sg-review row
  </acceptance_criteria>
  <done>README.ko.md Commands table has sg-tdd row in correct position with Korean description, pipeline and sg-next chain updated, command count changed to 22개.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md — data flow, skill count, tdd_mode flag description</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md</read_first>
  <action>
Make three surgical changes in CLAUDE.md (per D-06, D-07, D-08):

1. Line ~126 (Skills 레이어 설명 first sentence): Change `21개의 SKILL.md 파일이` to `22개의 SKILL.md 파일이` (per D-07).

2. Lines ~139-140 (data flow code block): After the line `sg-execute → Superpowers:executing-plans`, insert a new line:
   `sg-tdd → Superpowers:test-driven-development  # active when tdd_mode=true`
   (per D-06)

   The data flow block currently reads:
   ```
   sg-execute → Superpowers:executing-plans
   sg-review → Superpowers:requesting-code-review
   ```
   After the change it must read:
   ```
   sg-execute → Superpowers:executing-plans
   sg-tdd → Superpowers:test-driven-development  # active when tdd_mode=true
   sg-review → Superpowers:requesting-code-review
   ```

3. After the `stop_hook.cjs` bullet point (line ~130) and before the `rule_runner.cjs` bullet — NO, correct position per D-08: after ALL three bullet points in the Hooks 레이어 section, before the **데이터 흐름** heading. Insert a new paragraph:

   **`super_gsd.tdd_mode` 플래그**

   `.planning/config.json`의 `super_gsd.tdd_mode` 플래그가 `true`일 때 sg-next/sg-status가 execute 완료 후 sg-review 대신 sg-tdd로 라우팅한다. 부재 또는 `false`이면 기존 동작 유지 (기본 off).

   Insert this paragraph between the `lessons_ranker.cjs` bullet and the `**데이터 흐름**` heading.

Do NOT change any other content.
  </action>
  <verify>
    <automated>grep -n "sg-tdd\|tdd_mode\|22개" /Users/gyuha/workspace/super-gsd/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "22개의 SKILL.md 파일이" CLAUDE.md` returns 1 line (was 21개)
    - `grep "sg-tdd → Superpowers:test-driven-development" CLAUDE.md` returns 1 line in the data flow block
    - `grep "active when tdd_mode=true" CLAUDE.md` returns 1 line
    - `grep "super_gsd.tdd_mode" CLAUDE.md` returns at least 2 lines (the new flag description paragraph + the data flow comment)
    - The new flag description paragraph contains the text "sg-next/sg-status가 execute 완료 후 sg-review 대신 sg-tdd로 라우팅한다"
    - `grep "21개의" CLAUDE.md` returns 0 lines (the old count is gone)
  </acceptance_criteria>
  <done>CLAUDE.md Skills layer description updated to 22개, data flow block includes sg-tdd line after sg-execute line, and tdd_mode flag explanation paragraph inserted before the data flow heading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → filesystem | Markdown files written directly; no user input, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | README.md / README.ko.md | accept | Documentation-only changes; no executable content, no secrets |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After all three tasks complete:

```bash
# Verify sg-tdd appears in all three files
grep -c "sg-tdd" /Users/gyuha/workspace/super-gsd/README.md
grep -c "sg-tdd" /Users/gyuha/workspace/super-gsd/README.ko.md
grep -c "sg-tdd" /Users/gyuha/workspace/super-gsd/CLAUDE.md

# Verify pipeline strings updated
grep "tdd_mode=true" /Users/gyuha/workspace/super-gsd/README.md
grep "tdd_mode=true" /Users/gyuha/workspace/super-gsd/README.ko.md

# Verify count updated
grep "twenty-two" /Users/gyuha/workspace/super-gsd/README.md
grep "22개" /Users/gyuha/workspace/super-gsd/README.ko.md
grep "22개의 SKILL.md" /Users/gyuha/workspace/super-gsd/CLAUDE.md

# Verify CLAUDE.md data flow
grep "sg-tdd → Superpowers:test-driven-development" /Users/gyuha/workspace/super-gsd/CLAUDE.md

# Verify tdd_mode flag section
grep "super_gsd.tdd_mode" /Users/gyuha/workspace/super-gsd/CLAUDE.md
```
</verification>

<success_criteria>
- README.md Commands table has `/super-gsd:sg-tdd` row between sg-execute and sg-review rows with English description per D-02
- README.ko.md Commands table has `/super-gsd:sg-tdd` row with Korean description per D-03
- Both READMEs pipeline string reads `sg-execute → sg-tdd (tdd_mode=true) → sg-review` per D-04
- Both READMEs sg-next chain description updated per D-05
- Both READMEs command count updated (twenty-two / 22개)
- CLAUDE.md data flow block contains `sg-tdd → Superpowers:test-driven-development  # active when tdd_mode=true` per D-06
- CLAUDE.md Skills layer says `22개의` per D-07
- CLAUDE.md has `super_gsd.tdd_mode` flag paragraph explaining default-off behavior per D-08
- D-09 satisfied: README.md and README.ko.md updated atomically in same plan
</success_criteria>

<output>
Create `.planning/phases/47-documentation-update/47-01-SUMMARY.md` when done
</output>
