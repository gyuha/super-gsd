---
phase: 41-team-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/TEAM.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - ".planning/TEAM.md가 존재한다"
    - "브랜치 전략(phase/{N}-{slug} 명명 규칙)이 명시되어 있다"
    - "파일 소유권 테이블(STATE.md / ROADMAP.md / HANDOFF.md)이 있다"
    - "Merge 순서 컨벤션(PR 생성 방법 포함)이 기술되어 있다"
    - "Quick Start(git user.name 설정 1-liner)가 포함되어 있다"
  artifacts:
    - path: ".planning/TEAM.md"
      provides: "팀원 온보딩용 워크플로우 가이드"
      contains: "Quick Start, Branch strategy, File ownership, Merge order"
  key_links:
    - from: ".planning/TEAM.md"
      to: "skills/sg-execute/SKILL.md Step 1.5"
      via: "브랜치 전략 서술이 실제 구현을 정확히 반영"
    - from: ".planning/TEAM.md"
      to: "skills/sg-phase/SKILL.md Step 4i"
      via: "Merge 순서 PR 안내가 실제 명령과 일치"
---

<objective>
`.planning/TEAM.md`를 신규 생성한다. 팀원이 super-gsd 팀 워크플로우(브랜치 생성, 파일 소유권, merge 순서)를 독립적으로 온보딩할 수 있는 최소한의 영문 가이드다.

Purpose: Phase 40(브랜치 워크플로우)과 Phase 39(HANDOFF User 컬럼 + sg-status --team)의 구현을 문서화하여 팀원이 해당 기능을 올바르게 사용할 수 있도록 한다.
Output: `.planning/TEAM.md` 신규 파일 1개
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-team-documentation/41-CONTEXT.md
@.planning/phases/40-sg-execute-branch-workflow/40-CONTEXT.md
@.planning/phases/39-handoff-user-tracking/39-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: .planning/TEAM.md 신규 생성</name>
  <files>.planning/TEAM.md</files>
  <read_first>
    - `.planning/phases/41-team-documentation/41-CONTEXT.md` §C (콘텐츠 범위 결정), §specifics (Quick Start 예시, 파일 소유권 테이블 형식)
    - `.planning/phases/40-sg-execute-branch-workflow/40-CONTEXT.md` §C (브랜치명 계산: `phase/{PHASE_PAD}-{slug}`) §E (PR 안내 방식: gh CLI 있으면 `gh pr create --base main`, 없으면 `git push -u origin HEAD`)
    - `.planning/phases/39-handoff-user-tracking/39-CONTEXT.md` §A (HANDOFF.md 6열 스키마), §B (sg-status --team 출력 형식)
    - `skills/sg-execute/SKILL.md` — Step 1.5 브랜치 제안 로직 확인 (실제 구현과 서술 일치)
    - `skills/sg-status/SKILL.md` — --team 플래그 처리 확인 (출력 형식 일치)
  </read_first>
  <action>
    `.planning/TEAM.md`를 영문으로 신규 생성한다. 4개 섹션 순서: Quick Start → Branch strategy → File ownership → Merge order.

    **Quick Start 섹션:**
    git user.name이 설정되어야 HANDOFF.md User 컬럼에 올바르게 기록된다는 사실을 1-liner로 안내한다:
    ```
    git config user.name
    # Expected: your name. If empty: git config --global user.name "Your Name"
    ```
    sg-status --team 실행 방법 1줄도 포함한다.

    **Branch strategy 섹션:**
    - `sg-execute`가 main/master 감지 시 AskUserQuestion으로 브랜치 생성을 제안함을 설명한다.
    - 브랜치 명명 규칙: `phase/{PHASE_PAD}-{slug}` (예: `phase/41-team-documentation`)
    - PHASE_PAD는 2자리 zero-padded 정수, slug는 ROADMAP.md phase 이름을 소문자+하이픈으로 정규화

    **File ownership 섹션:**
    3개 파일의 소유권 테이블:
    | File | Owner | When modified |
    |------|-------|---------------|
    | `.planning/STATE.md` | GSD (auto) | After each sg-* command via gsd-sdk |
    | `.planning/ROADMAP.md` | Human | Phase planning decisions only |
    | `.planning/HANDOFF.md` | super-gsd (auto) | Append-only at each sg-* command |

    HANDOFF.md 6열 스키마도 명시한다: `| Timestamp | Phase | From | To | Plan Hash | User |`

    **Merge order 섹션:**
    - phase 완료 시(`sg-complete N` 또는 `sg-phase complete N`) PR 안내가 자동 출력됨을 설명한다.
    - gh CLI 있을 때: `gh pr create --base main --title "phase/{N}-{slug}"` 출력
    - gh CLI 없을 때: `git push -u origin HEAD` + GitHub URL에서 PR 생성 안내 출력
    - main 브랜치로의 squash merge를 권장하되, 강제하지 않는다.

    파일 상단에 제목과 1문장 소개를 넣는다. 외부 링크는 `skills/sg-execute/SKILL.md`, `skills/sg-status/SKILL.md` 경로 참조만 허용한다.
  </action>
  <verify>
    <automated>test -f /Users/gyuha/workspace/super-gsd/.planning/TEAM.md && grep -c "Branch strategy" /Users/gyuha/workspace/super-gsd/.planning/TEAM.md | grep -v "^0$" && grep -c "File ownership" /Users/gyuha/workspace/super-gsd/.planning/TEAM.md | grep -v "^0$" && grep -c "Merge order" /Users/gyuha/workspace/super-gsd/.planning/TEAM.md | grep -v "^0$" && echo "PASS"</automated>
  </verify>
  <done>
    - `.planning/TEAM.md` 파일이 존재한다
    - Quick Start, Branch strategy, File ownership, Merge order 4개 섹션이 모두 있다
    - 브랜치명 예시 `phase/41-team-documentation` 또는 동등한 패턴이 포함되어 있다
    - 파일 소유권 테이블에 STATE.md / ROADMAP.md / HANDOFF.md 3행이 있다
    - gh CLI 유무에 따른 PR 안내 두 경우가 모두 기술되어 있다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문서 → 독자 | 문서 내용이 실제 구현(SKILL.md)과 불일치하면 팀원이 잘못된 명령을 사용할 수 있음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | .planning/TEAM.md | accept | 문서 파일 — 코드 실행 없음. git history로 추적 가능 |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜에 패키지 설치 없음 |
</threat_model>

<verification>
- `.planning/TEAM.md` 존재 확인: `test -f .planning/TEAM.md`
- 4개 섹션 헤더 존재 확인: `grep -E "## (Quick Start|Branch strategy|File ownership|Merge order)" .planning/TEAM.md`
- 파일 소유권 테이블 확인: `grep "HANDOFF.md" .planning/TEAM.md`
- 브랜치명 패턴 확인: `grep "phase/" .planning/TEAM.md`
</verification>

<success_criteria>
- `.planning/TEAM.md`가 존재하고 4개 섹션을 모두 포함한다
- git user.name 설정 방법이 Quick Start에 명시되어 있다
- 브랜치 명명 규칙 `phase/{N}-{slug}`가 서술되어 있다
- 파일 소유권 테이블 3행(STATE.md / ROADMAP.md / HANDOFF.md)이 있다
- PR 생성 안내(gh CLI 있는 경우 / 없는 경우)가 Merge order 섹션에 포함되어 있다
</success_criteria>

<output>
완료 시 `.planning/phases/41-team-documentation/41-01-SUMMARY.md` 생성
</output>
