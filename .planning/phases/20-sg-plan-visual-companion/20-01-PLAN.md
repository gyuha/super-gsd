---
phase: 20-sg-plan-visual-companion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-plan.md
autonomous: true
requirements:
  - VC-01
  - VC-02

must_haves:
  truths:
    - "UI 키워드가 없는 phase에서 sg-plan을 실행하면 AskUserQuestion 없이 바로 Step 2(gsd-discuss-phase)로 진행된다"
    - "UI 키워드가 있는 phase에서 sg-plan을 실행하면 AskUserQuestion이 표시된다"
    - "'Visual Companion 포함' 선택 시 superpowers:brainstorming Agent가 실행된 뒤 gsd-discuss-phase로 이어진다"
    - "'UI 없음' 선택 시 한 줄 출력 후 gsd-discuss-phase로 이어진다"
    - "brainstorming Agent가 에러로 종료되어도 gsd-discuss-phase 실행이 abort되지 않는다"
    - "brainstorming Agent 프롬프트에 writing-plans 억제 지시가 있다"
  artifacts:
    - path: "commands/sg-plan.md"
      provides: "Step 1.5 Visual Companion 분기 (process 블록 + success_criteria 블록)"
      contains: "Step 1.5"
    - path: "commands/sg-plan.md"
      provides: "success_criteria 항목 1.5"
      contains: "UI_DETECTED"
  key_links:
    - from: "commands/sg-plan.md Step 1.5"
      to: "superpowers:brainstorming"
      via: "Agent(subagent_type='general-purpose')"
      pattern: "superpowers:brainstorming"
    - from: "commands/sg-plan.md Step 1.5"
      to: "commands/sg-plan.md Step 2"
      via: "모든 분기가 Step 2로 합류"
      pattern: "Step 2"
---

<objective>
`commands/sg-plan.md`의 `<process>` 블록에 Step 1.5 (Visual Companion 판단 분기)를 삽입하고, `<success_criteria>` 블록에 대응하는 검증 항목을 추가한다.

Purpose: sg-plan이 UI 관련 phase에서 자동으로 brainstorming을 제안하여, 사용자가 UI 설계를 gsd-discuss-phase 이전에 완료할 수 있게 한다.
Output: commands/sg-plan.md — Step 1.5 블록 삽입, success_criteria 항목 1.5 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-sg-plan-visual-companion/20-CONTEXT.md
@.planning/phases/20-sg-plan-visual-companion/20-RESEARCH.md
@.planning/phases/20-sg-plan-visual-companion/20-PATTERNS.md
</context>

<interfaces>
<!-- commands/sg-plan.md 현재 구조 (executor가 수정 전 파일을 직접 읽어야 함) -->
<!--
<process> 블록 구조:
  Step 0: Prior lessons 주입 (lines 16–36)
  Step 1: Resolve phase — PHASE_NUM 결정 (lines 38–50)
  Step 2: Print + gsd-discuss-phase Agent 호출 (lines 52–62)
  Step 2.5: HANDOFF.md 기록 (lines 64–80)
  Step 3: gsd-plan-phase Skill 호출 (lines 82–88)

<success_criteria> 블록 구조:
  0. lessons 출력 확인
  1. PHASE_NUM 비어 있을 때 에러 메시지
  2. gsd-discuss-phase Agent 실행 확인
  3. gsd-discuss-phase 에러 시 abort 확인
  4. HANDOFF.md To=gsd-plan 기록 확인
  5. gsd-plan-phase Skill 단일 호출 확인
  6. 진행 메시지 출력 확인

삽입 위치: Step 1 블록(line 38–50) 닫는 줄 바로 다음, Step 2 "2. Print:" 줄 바로 이전
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: sg-plan.md process 블록에 Step 1.5 삽입</name>
  <files>commands/sg-plan.md</files>
  <read_first>
    commands/sg-plan.md — 수정 대상 파일 전체. 삽입 위치(Step 1 종료 ~ Step 2 시작 사이)와 기존 Agent 패턴, 변수 치환 지시 문구를 확인한다.
    .planning/phases/20-sg-plan-visual-companion/20-RESEARCH.md — Code Examples 섹션의 Step 1.5 전체 구조 초안과 anti-pattern 목록.
    .planning/phases/20-sg-plan-visual-companion/20-PATTERNS.md — 패턴 1~5 (AskUserQuestion 문법, Agent 호출, 에러 처리, grep 이식성, 진행 메시지 형식).
  </read_first>
  <action>
commands/sg-plan.md의 `<process>` 블록에서 Step 1 종료 줄(`Do not proceed to Step 2.` 줄) 바로 다음, Step 2 (`2. Print: \`[sg-plan] Step 1/2:`) 줄 바로 이전에 아래 Step 1.5 블록을 삽입한다.

**삽입할 텍스트 (빈 줄 포함, 들여쓰기 없이):**

```
1.5. **Visual Companion 판단 (D-01~D-06).** Phase goal에 UI 관련 키워드가 있을 때만 실행한다:
   ```bash
   PHASE_SECTION=$(gsd-sdk query roadmap.get-phase "$PHASE_NUM" --pick section 2>/dev/null)
   UI_DETECTED=""
   if [ -n "$PHASE_SECTION" ] && echo "$PHASE_SECTION" | grep -iE "UI|화면|design|Visual|frontend|interface|component" > /dev/null 2>&1; then
     UI_DETECTED="1"
   fi
   ```
   **UI 키워드가 없거나 PHASE_SECTION이 비어 있으면** 이 단계를 조용히 건너뛰고 Step 2로 이동한다.

   **UI 키워드가 감지되면** AskUserQuestion을 표시한다:
   ```
   AskUserQuestion(
     questions: [{
       question: "이 phase에 UI 설계가 포함되어 있나요?",
       header: "Visual Companion",
       multiSelect: false,
       options: [
         { label: "Visual Companion 포함", description: "superpowers:brainstorming을 실행하여 UI를 먼저 설계합니다." },
         { label: "UI 없음", description: "UI 설계 없이 기존 흐름을 진행합니다." }
       ]
     }]
   )
   ```

   **"UI 없음" 선택 시:** `[sg-plan] UI 설계 없이 진행합니다.` 출력 후 Step 2로 이동한다.

   **"Visual Companion 포함" 선택 시:** brainstorming Agent를 실행하고 완료를 기다린다.
   **Before calling Agent, replace every occurrence of `$PHASE_NUM` and `$PHASE_SECTION` with actual resolved values. `$PHASE_SECTION` contains multi-line text — insert it as literal text in the prompt string:**
   ```
   Agent(
     description="superpowers:brainstorming for Phase $PHASE_NUM UI design",
     prompt="Do NOT invoke writing-plans Skill after brainstorming completes. Your task is to run the superpowers brainstorming skill for Phase $PHASE_NUM UI design. The project root is the current working directory. Phase context:\n\n$PHASE_SECTION\n\nInvoke Skill(skill='superpowers:brainstorming', args='Phase $PHASE_NUM UI 설계를 진행합니다. 위 컨텍스트를 참고하십시오. 중요: brainstorming 완료 후 writing-plans Skill을 호출하지 마십시오. brainstorming 대화만 진행하고 종료하십시오.') and follow its instructions to completion. Do NOT invoke writing-plans after brainstorming finishes.",
     subagent_type="general-purpose"
   )
   ```
   Agent가 에러로 종료되면: `[sg-plan] brainstorming 실패, 기존 흐름으로 계속...` 출력 후 Step 2로 이동한다 (중단 없음).
```

**준수 사항:**
- 기존 Step 2, 2.5, 3 번호와 내용은 변경하지 않는다 (per D-06).
- brainstorming Agent 프롬프트에 writing-plans 억제 지시가 프롬프트 **첫 줄**과 **마지막 줄** 양쪽에 있어야 한다 (RESEARCH.md Open Questions 1 권고).
- `grep -iE` + `> /dev/null 2>&1` 패턴 사용 (macOS BSD grep 이식성, per CLAUDE.md + PATTERNS.md 패턴 4).
- AskUserQuestion 레이블: "Visual Companion 포함" / "UI 없음" (per Claude's Discretion).
- PHASE_SECTION 읽기: `gsd-sdk query roadmap.get-phase "$PHASE_NUM" --pick section 2>/dev/null` (per D-02).
  </action>
  <verify>
    <automated>grep -c "Step 1.5" /Users/gyuha/workspace/super-gsd/commands/sg-plan.md</automated>
    <automated>grep -c "UI_DETECTED" /Users/gyuha/workspace/super-gsd/commands/sg-plan.md</automated>
    <automated>grep -c "Visual Companion 포함" /Users/gyuha/workspace/super-gsd/commands/sg-plan.md</automated>
    <automated>grep -c "UI 없음" /Users/gyuha/workspace/super-gsd/commands/sg-plan.md</automated>
    <automated>grep -c "brainstorming 실패, 기존 흐름으로 계속" /Users/gyuha/workspace/super-gsd/commands/sg-plan.md</automated>
    <automated>grep -c "Do NOT invoke writing-plans" /Users/gyuha/workspace/super-gsd/commands/sg-plan.md</automated>
    <automated>grep -c "UI 설계 없이 진행합니다" /Users/gyuha/workspace/super-gsd/commands/sg-plan.md</automated>
  </verify>
  <acceptance_criteria>
    - commands/sg-plan.md에 "Step 1.5" 문자열이 1회 이상 존재한다: `grep -c "Step 1.5" commands/sg-plan.md` > 0
    - commands/sg-plan.md에 "UI_DETECTED" 변수 참조가 존재한다: `grep -c "UI_DETECTED" commands/sg-plan.md` > 0
    - commands/sg-plan.md에 "Visual Companion 포함" 레이블이 존재한다: `grep -c "Visual Companion 포함" commands/sg-plan.md` > 0
    - commands/sg-plan.md에 "UI 없음" 레이블이 존재한다: `grep -c "UI 없음" commands/sg-plan.md` > 0
    - commands/sg-plan.md에 "[sg-plan] brainstorming 실패, 기존 흐름으로 계속..." 문자열이 존재한다: `grep -c "brainstorming 실패, 기존 흐름으로 계속" commands/sg-plan.md` > 0
    - commands/sg-plan.md에 "[sg-plan] UI 설계 없이 진행합니다." 문자열이 존재한다: `grep -c "UI 설계 없이 진행합니다" commands/sg-plan.md` > 0
    - writing-plans 억제 지시가 Agent prompt에 2회 이상 존재한다 (첫 줄 + 마지막 줄): `grep -c "Do NOT invoke writing-plans" commands/sg-plan.md` >= 2
    - `grep -iE "UI\|화면\|design\|Visual\|frontend\|interface\|component"` 패턴이 process 블록에 존재한다: `grep -c 'grep -iE' commands/sg-plan.md` > 0
    - 기존 Step 2 "gsd-discuss-phase for Phase" 문자열이 그대로 존재한다 (변경 없음): `grep -c "gsd-discuss-phase for Phase" commands/sg-plan.md` > 0
    - 기존 Step 2.5 "HANDOFF_FILE" 문자열이 그대로 존재한다 (변경 없음): `grep -c "HANDOFF_FILE" commands/sg-plan.md` > 0
  </acceptance_criteria>
  <done>commands/sg-plan.md process 블록에 Step 1.5가 삽입되어 있고, AskUserQuestion, brainstorming Agent, 에러 처리, UI 없음 분기 모두 grep으로 확인 가능하다. 기존 Step 2~3은 변경 없음.</done>
</task>

<task type="auto">
  <name>Task 2: sg-plan.md success_criteria 블록에 항목 1.5 추가</name>
  <files>commands/sg-plan.md</files>
  <read_first>
    commands/sg-plan.md — Task 1 완료 후의 파일. `<success_criteria>` 블록 전체를 확인하여 기존 항목 0~6의 번호와 내용을 파악한다.
    .planning/phases/20-sg-plan-visual-companion/20-RESEARCH.md — Pitfall 3 (success_criteria 불일치 방지) 섹션.
  </read_first>
  <action>
commands/sg-plan.md의 `<success_criteria>` 블록에서 항목 `1. PHASE_NUM이 비어 있으면...` 줄 바로 다음, 항목 `2. gsd-discuss-phase는 Agent()로...` 줄 바로 이전에 아래 항목 1.5를 삽입한다.

**삽입할 텍스트:**

```
1.5. Phase goal에 UI 키워드(UI|화면|design|Visual|frontend|interface|component)가 없거나 PHASE_SECTION이 비어 있으면 Step 1.5를 조용히 건너뛰고 Step 2로 진행한다. UI 키워드가 감지되면 AskUserQuestion("Visual Companion 포함" / "UI 없음")이 표시된다. "Visual Companion 포함" 선택 시 superpowers:brainstorming Agent가 실행되고 완료 후 Step 2로 이동한다. "UI 없음" 선택 시 `[sg-plan] UI 설계 없이 진행합니다.` 출력 후 Step 2로 이동한다. brainstorming Agent 에러 시 `[sg-plan] brainstorming 실패, 기존 흐름으로 계속...` 출력 후 Step 2로 이동한다 (abort 없음). brainstorming Agent 프롬프트에는 writing-plans 미호출 억제 지시가 포함된다.
```

**준수 사항:**
- 기존 항목 0~6의 번호, 내용, 순서를 변경하지 않는다.
- 기존 항목 `2. gsd-discuss-phase는 Agent()로...`의 번호를 3으로 올리지 않는다 — 1.5를 삽입하되 2는 그대로 유지한다.
  </action>
  <verify>
    <automated>grep -c "1.5\." /Users/gyuha/workspace/super-gsd/commands/sg-plan.md</automated>
    <automated>grep -n "success_criteria" /Users/gyuha/workspace/super-gsd/commands/sg-plan.md</automated>
  </verify>
  <acceptance_criteria>
    - `<success_criteria>` 블록 내에 "1.5." 문자열이 존재한다: `grep -c "1\.5\." commands/sg-plan.md` > 0
    - success_criteria 내 항목 1.5에 "writing-plans 미호출" 또는 "writing-plans" 문자열이 포함된다: `grep -A 5 "1\.5\." commands/sg-plan.md | grep -c "writing-plans"` > 0
    - success_criteria 내 기존 항목 "2. gsd-discuss-phase는 Agent()로"가 그대로 존재한다: `grep -c "2\. gsd-discuss-phase는 Agent()로" commands/sg-plan.md` > 0
    - success_criteria 내 기존 항목 "6. Progress messages"가 그대로 존재한다: `grep -c "6\. Progress messages" commands/sg-plan.md` > 0
    - success_criteria 블록이 `</success_criteria>` 태그로 닫힌다: `grep -c "</success_criteria>" commands/sg-plan.md` == 1
  </acceptance_criteria>
  <done>commands/sg-plan.md success_criteria 블록에 항목 1.5가 삽입되어 있고, brainstorming writing-plans 억제 검증 항목이 포함되며, 기존 항목 0~6이 변경 없이 유지된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROADMAP.md → gsd-sdk → PHASE_SECTION | 외부 입력 없음 — 자체 파일 읽기만 수행 |
| PHASE_SECTION → grep | 내부 텍스트를 grep에 파이프. 공격 벡터 없음 (ROADMAP.md는 신뢰 소스) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | brainstorming Agent prompt | mitigate | writing-plans 억제 지시를 prompt 첫 줄과 마지막 줄 양쪽에 명시하여 Skill의 HARD-GATE 재정의 |
| T-20-02 | Denial of Service | PHASE_SECTION 빈 값 | accept | PHASE_SECTION이 비어 있으면 grep을 건너뛰고 조용히 Step 2로 진행 — 기능 손실 없음 |
| T-20-03 | Spoofing | 없음 | accept | 이 phase는 인증/권한 없음 |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 없음 — 파일 수정만 수행 |
</threat_model>

<verification>
## Phase 20 전체 검증

```bash
# 1. Step 1.5 삽입 확인
grep -c "Step 1.5" commands/sg-plan.md

# 2. 핵심 구성 요소 일괄 확인
grep -E "UI_DETECTED|Visual Companion 포함|UI 없음|brainstorming 실패|UI 설계 없이 진행" commands/sg-plan.md

# 3. writing-plans 억제 지시 2회 이상 확인
grep -c "Do NOT invoke writing-plans" commands/sg-plan.md

# 4. macOS 이식성 패턴 확인 (grep -iE, > /dev/null)
grep -c "grep -iE" commands/sg-plan.md

# 5. success_criteria 항목 1.5 확인
grep -c "1\.5\." commands/sg-plan.md

# 6. 기존 Step 2, 2.5 변경 없음 확인
grep -c "gsd-discuss-phase for Phase" commands/sg-plan.md
grep -c "HANDOFF_FILE" commands/sg-plan.md

# 7. success_criteria 블록 무결성
grep -c "</success_criteria>" commands/sg-plan.md
```
</verification>

<success_criteria>
1. `grep -c "Step 1.5" commands/sg-plan.md` 결과 > 0
2. `grep -c "UI_DETECTED" commands/sg-plan.md` 결과 > 0
3. `grep -c "Visual Companion 포함" commands/sg-plan.md` 결과 > 0
4. `grep -c "UI 없음" commands/sg-plan.md` 결과 > 0
5. `grep -c "brainstorming 실패, 기존 흐름으로 계속" commands/sg-plan.md` 결과 > 0
6. `grep -c "UI 설계 없이 진행합니다" commands/sg-plan.md` 결과 > 0
7. `grep -c "Do NOT invoke writing-plans" commands/sg-plan.md` 결과 >= 2
8. `grep -c "grep -iE" commands/sg-plan.md` 결과 > 0
9. `grep -c "1\.5\." commands/sg-plan.md` 결과 > 0 (success_criteria 항목 1.5)
10. `grep -c "gsd-discuss-phase for Phase" commands/sg-plan.md` 결과 > 0 (기존 Step 2 유지)
11. `grep -c "HANDOFF_FILE" commands/sg-plan.md` 결과 > 0 (기존 Step 2.5 유지)
12. `grep -c "</success_criteria>" commands/sg-plan.md` 결과 == 1
</success_criteria>

<output>
완료 후 `.planning/phases/20-sg-plan-visual-companion/20-01-SUMMARY.md`를 생성하라.
</output>
