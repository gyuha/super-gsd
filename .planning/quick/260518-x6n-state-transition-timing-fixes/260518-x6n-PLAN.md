---
phase: quick-260518-x6n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-plan.md
  - commands/sg-review.md
  - commands/sg-learn.md
  - commands/sg-health.md
  - docs/COMMANDS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "sg-plan 완료 후 HANDOFF.md에 gsd-plan 행이 기록되어 sg-status가 plan 이후 단계를 올바르게 안내한다"
    - "sg-review는 Skill 호출 이전에 HANDOFF.md를 기록하지 않는다"
    - "sg-learn은 Skill 호출 이전에 HANDOFF.md를 기록하지 않는다"
    - "sg-health가 플러그인 설치 디렉토리 기준으로 hooks.json을 검사하여 오탐을 내지 않는다"
    - "docs/COMMANDS.md의 Maps to 값과 샘플 출력이 실제 구현과 일치한다"
  artifacts:
    - path: "commands/sg-plan.md"
      provides: "gsd-plan 완료 후 HANDOFF.md idempotent 기록 로직"
    - path: "commands/sg-review.md"
      provides: "Step 3.5 제거 — HANDOFF 기록이 Skill 호출 이후로 이동"
    - path: "commands/sg-learn.md"
      provides: "Step 0.5 제거 — HANDOFF 기록이 Skill 호출 이후로 이동"
    - path: "commands/sg-health.md"
      provides: "hooks/hooks.json 경로를 ${CLAUDE_PLUGIN_ROOT}/hooks/hooks.json으로 교체"
    - path: "docs/COMMANDS.md"
      provides: "stale 이름 3건 수정"
  key_links:
    - from: "commands/sg-plan.md Step 3"
      to: ".planning/HANDOFF.md"
      via: "Skill 호출 완료 후 bash 기록 블록"
    - from: "commands/sg-review.md Step 4"
      to: ".planning/HANDOFF.md"
      via: "Skill 호출 완료 후 bash 기록 블록 (Step 3.5 위치 이동)"
    - from: "commands/sg-learn.md Step 1"
      to: ".planning/HANDOFF.md"
      via: "Skill 호출 완료 후 bash 기록 블록 (Step 0.5 위치 이동)"
---

<objective>
상태 전이 타이밍 버그 5건을 수정한다. P1 버그 3건은 HANDOFF.md 기록 순서 오류(Skill 호출 전 기록)이고, P2 버그 1건은 sg-health의 상대 경로 오탐이며, P3 버그 1건은 docs/COMMANDS.md의 stale 이름이다.

Purpose: sg-status가 실제 실행 결과와 일치하는 단계를 표시하도록 한다.
Output: 수정된 commands/sg-plan.md, commands/sg-review.md, commands/sg-learn.md, commands/sg-health.md, docs/COMMANDS.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@commands/sg-plan.md
@commands/sg-review.md
@commands/sg-learn.md
@commands/sg-health.md
@docs/COMMANDS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: P1 — sg-plan HANDOFF 기록 추가 / sg-review Step 3.5 제거 및 이동 / sg-learn Step 0.5 제거 및 이동</name>
  <files>commands/sg-plan.md, commands/sg-review.md, commands/sg-learn.md</files>
  <action>
세 파일의 HANDOFF.md 기록 타이밍을 Skill 호출 이후로 교정한다.

**commands/sg-plan.md:**
- 현재 Step 3는 gsd-plan-phase Skill 호출로 끝난다 (line 52-57). 이 Skill은 terminal action이라 제어가 반환되지 않는다.
- 따라서 Skill 호출 직전에 HANDOFF.md를 idempotent하게 기록하는 bash 블록을 삽입한다 (Step 2.5로 번호 부여). "idempotent"는 동일 phase+To 조합이 이미 존재하면 중복 기록하지 않음을 의미한다.
- 삽입 위치: Step 3의 `Print: [sg-plan] Step 2/2:` 출력 직후, Skill 호출 직전.
- 기록 형식: `| $TS | $PHASE_SLUG | gsd-discuss-phase | gsd-plan | - |`
- idempotent 조건: `grep -q "| gsd-plan |" "$HANDOFF_FILE"` 가 참이면 기록 건너뜀.
- HANDOFF_FILE, PHASE_NUM, PHASE_PAD, PHASE_SLUG 변수 도출 패턴은 sg-review.md Step 3.5의 기존 bash 블록을 그대로 참조한다.

**commands/sg-review.md:**
- Step 3.5 전체(line 60-73)를 제거한다.
- 대신 Step 4의 Skill 호출 직전에 동일 bash 블록을 삽입한다 (Step 3.5 → Step 3.9로 재배치).
- 기록 형식은 기존과 동일: `| $TS | $PHASE_SLUG_R | superpowers | review | - |`
- superpowers:requesting-code-review는 terminal Skill이므로 호출 직전이 가능한 최후 시점이다.

**commands/sg-learn.md:**
- Step 0.5 전체(line 15-28)를 제거한다.
- 대신 Step 1의 Skill 호출 직전에 동일 bash 블록을 삽입한다 (Step 0.5 → Step 0.9로 재배치).
- 기록 형식: `| $TS | $PHASE_SLUG_L | review | hookify | - |`
- hookify:hookify는 terminal Skill이므로 호출 직전이 가능한 최후 시점이다.

세 파일 모두 success_criteria 섹션도 HANDOFF 기록 조건을 반영하여 업데이트한다.
  </action>
  <verify>
    <automated>
grep -n "HANDOFF" /Users/gyuha/workspace/super-gsd/commands/sg-plan.md | grep -v "^#"
grep -n "3\.5\|0\.5" /Users/gyuha/workspace/super-gsd/commands/sg-review.md
grep -n "3\.5\|0\.5" /Users/gyuha/workspace/super-gsd/commands/sg-learn.md
    </automated>
  </verify>
  <done>
- sg-plan.md에 HANDOFF 기록 블록이 존재하고 gsd-plan Skill 호출 직전에 위치한다.
- sg-review.md에 Step 3.5가 없고, review Skill 호출 직전(Step 3.9)에 HANDOFF 기록 블록이 있다.
- sg-learn.md에 Step 0.5가 없고, hookify Skill 호출 직전(Step 0.9)에 HANDOFF 기록 블록이 있다.
  </done>
</task>

<task type="auto">
  <name>Task 2: P2 — sg-health hooks.json 경로 수정 / P3 — COMMANDS.md stale 이름 3건 수정</name>
  <files>commands/sg-health.md, docs/COMMANDS.md</files>
  <action>
**commands/sg-health.md (line 44-57, Stop hook / SubagentStop hook 검사):**
- `grep -q '"Stop"[[:space:]]*:' hooks/hooks.json` → `grep -q '"Stop"[[:space:]]*:' "${CLAUDE_PLUGIN_ROOT}/hooks/hooks.json"`
- `grep -q '"SubagentStop"' hooks/hooks.json` → `grep -q '"SubagentStop"' "${CLAUDE_PLUGIN_ROOT}/hooks/hooks.json"`
- execution_context 섹션의 설명도 `hooks/hooks.json` → `${CLAUDE_PLUGIN_ROOT}/hooks/hooks.json`으로 업데이트한다.
- CLAUDE_PLUGIN_ROOT가 미설정일 경우의 폴백은 추가하지 않는다 (플러그인 환경에서는 항상 설정됨).

**docs/COMMANDS.md:**
- Quick Reference 테이블 (line 23): sg-explore 행의 `gsd-explore` → `gsd-map-codebase`
- Quick Reference 테이블 (line 25): sg-execute 행의 `sg-executing-plans` → `superpowers:executing-plans`
- sg-status 샘플 출력 (line 257): `Next: /hookify` → `Next: /super-gsd:sg-review`
- sg-explore 상세 섹션(line 60, 64)의 `gsd-explore` 참조도 `gsd-map-codebase`로 수정한다.
- sg-execute 상세 섹션(line 105, 109)의 `sg-executing-plans` 참조도 `superpowers:executing-plans`로 수정한다.
  </action>
  <verify>
    <automated>
grep -n "hooks/hooks.json" /Users/gyuha/workspace/super-gsd/commands/sg-health.md | grep -v "CLAUDE_PLUGIN_ROOT"
grep -n "gsd-explore\|sg-executing-plans\|/hookify" /Users/gyuha/workspace/super-gsd/docs/COMMANDS.md
    </automated>
  </verify>
  <done>
- sg-health.md의 모든 hooks.json 참조가 ${CLAUDE_PLUGIN_ROOT}/hooks/hooks.json 형식이다 (상대 경로 없음).
- COMMANDS.md Quick Reference 테이블에서 sg-explore는 gsd-map-codebase, sg-execute는 superpowers:executing-plans를 표시한다.
- sg-status 샘플 출력의 Next 줄이 /super-gsd:sg-review를 표시한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 repo → plugin commands | 명령 파일은 사용자 repo에 설치된 후 Claude Code가 실행 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-x6n-01 | Tampering | HANDOFF.md 기록 | accept | HANDOFF.md는 단순 audit log이며 보안 결정에 사용되지 않음 |
| T-x6n-02 | Tampering | CLAUDE_PLUGIN_ROOT 환경 변수 | accept | 플러그인 런타임이 제공하는 값이므로 외부 조작 불가 |
</threat_model>

<verification>
두 태스크 완료 후 전체 검증:

```bash
# P1: sg-plan HANDOFF 기록 존재 확인
grep -c "HANDOFF" /Users/gyuha/workspace/super-gsd/commands/sg-plan.md

# P1: sg-review/sg-learn에 조기 기록 스텝 없음
grep -c "3\.5\." /Users/gyuha/workspace/super-gsd/commands/sg-review.md
grep -c "0\.5\." /Users/gyuha/workspace/super-gsd/commands/sg-learn.md

# P2: 상대 경로 잔존 없음
grep -c "^[^$]*hooks/hooks\.json" /Users/gyuha/workspace/super-gsd/commands/sg-health.md

# P3: stale 이름 잔존 없음
grep -c "gsd-explore\b" /Users/gyuha/workspace/super-gsd/docs/COMMANDS.md
grep -c "sg-executing-plans" /Users/gyuha/workspace/super-gsd/docs/COMMANDS.md
grep -c '"/hookify"' /Users/gyuha/workspace/super-gsd/docs/COMMANDS.md
```

P1 카운트 >= 1, P1 step 카운트 == 0, P2/P3 카운트 == 0이면 통과.
</verification>

<success_criteria>
1. sg-plan 완료 후 HANDOFF.md에 To=gsd-plan 행이 기록되고, sg-status가 올바른 다음 단계를 표시한다.
2. sg-review와 sg-learn은 Skill 호출 이전에 HANDOFF.md를 기록하지 않는다.
3. sg-health가 임의의 타겟 repo에서 실행되어도 hooks.json 오탐을 내지 않는다.
4. docs/COMMANDS.md의 Maps to 값과 샘플 출력이 현재 구현과 일치한다.
</success_criteria>

<output>
완료 후 `.planning/quick/260518-x6n-state-transition-timing-fixes/260518-x6n-SUMMARY.md` 생성
</output>
