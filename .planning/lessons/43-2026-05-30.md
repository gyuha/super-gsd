# Lessons: Phase 43 (2026-05-30)

## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: 2026-05-30T15:20:00Z_

### Decisions

- **`--pick` flag을 sg-retro Step 1에서 token-position-free하게 파싱** (D-01, D-02 — `.planning/phases/43-pick-display-polish/43-CONTEXT.md:25-30`). `sg-learn`은 D-11 thin pass-through 유지(미수정). `--pick` + positional lens 충돌은 stderr error + exit 1로 거부 (D-03, silent override 금지 — Phase 42 D-06 일관성).
- **D-08 P1 emphasis: `🔴 P1` emoji prefix in single Action Items table**. 별도 분리된 P1 섹션 대신 단일 테이블 + emoji prefix 선택 — 기존 lessons 파일 마이그레이션 불필요(D-10). DISPLAY-02 intent line은 static 3 fixed sentences(D-12), 위치는 `_Captured:` 직후(D-13), italic 단일 라인(D-14), English source(D-15).
- **D-17 옵션 (b) 채택: `.agents/<constraints>` 적극적 갱신**. Phase 42 retro의 prose drift 교훈을 plan 단계에서 직접 반영 — `.agents/` `<constraints>` 첫 줄을 새 `--pick` graceful-exit 동작을 명시하도록 갱신. `argument-hint` frontmatter도 동시에 새 동작 반영.
- **lessons_ranker.cjs 변경 없음 (Option B)**. Planner가 `hooks/lessons_ranker.cjs` 정독 후 priority cell을 ranker가 파싱하지 않음을 grep 검증 (`grep -cE '(\| P1 \||"P1"|P\[123\])'` → 0). 별도 plan 또는 quick task 불필요. Milestone "Out of Scope: lessons_ranker.cjs 알고리즘 변경" 제약을 정확히 해석한 결과.
- **`.agents/` `--pick` fallback: graceful exit 1 with valid alternative**. Codex/Gemini/Antigravity의 `AskUserQuestion not supported` 제약 하에서 silent fallback to smart default 대신 명시적 실패 + valid alternative 안내. Phase 42 D-06 dropped-lens reject와 일관된 정책.

### Surprises

- **Phase 42 retro에서 도출한 P1 #1 lesson(plan-lint preserve vs grep acceptance)이 Phase 43 plan에서도 같은 형태로 재발했고, plan-spec deviation으로 surface됨**: `grep -c 'PICK_MODE=true'` literal substring 검증과 `<interfaces>`의 character-exact 토큰 spec(`[ "$PICK_MODE" = "true" ]`)이 직접 충돌. 양쪽 SUMMARY 모두 이 deviation을 정직하게 surface (Phase 42 SUMMARY 패턴 답습). 같은 lesson을 두 phase 연속으로 인용하면서도 plan-time 차단 메커니즘이 아직 부재함이 명확히 드러남.
- **Phase 43 plan이 prose drift를 4개 enumerate token으로 예방하려 했으나 code review에서 3건의 같은-class 드리프트가 추가 발견됨**: `skills/sg-retro/SKILL.md:290` (Step 6 description prose), `skills/sg-retro/SKILL.md:422` (SC #3 "Each lens output follows exactly"), `.agents/skills/sg-retro/SKILL.md:411` (SC #4). 모두 "변경되는 구조를 묘사하는 보존 블록" — plan의 "변경되는 산문 토큰" enumerate에 빠진 새로운 sub-class. Phase 42에서 도출한 lesson을 한 단계 더 정밀화할 필요.
- **2회 연속 commit 누락이 sg-review 진입에서 노출됨**: Phase 42에서 처음 발생(sg-parallel-execute가 commit 안 함 → sg-review BASE==HEAD 막힘)했는데 Phase 43에서 정확히 같은 시점에 재발생. Phase 42 retro Action Item P1 #2가 "다음 phase 전 자동화 필요"였으나 그 자체가 deferred 상태로 Phase 43 진입 → 같은 사용자 확인 단계 반복.

### Patterns

- **Surfacing deviations honestly in SUMMARY § "Plan-spec Deviations"** — Phase 42에서 시작된 패턴이 Phase 43에서 자연스럽게 답습되어 reviewer가 즉시 의도성·심각도 판단 가능. 두 plan 모두 동일한 PICK_MODE deviation을 같은 형식으로 보고. burying 없이 trust 유지.
- **Pairwise parallel execute 패턴이 안정적**: `wave:1` + `depends_on:[]` + non-overlapping `files_modified` + 한 응답 2 Task() dispatch → Phase 42에 이어 Phase 43에서도 conflict 0건. ~5-6분/agent. 두 plan agents의 plan-spec deviation도 동일 형태로 보고됨(`<interfaces>` 문법 일관성 효과).
- **Code review가 Phase 42→43 두 phase 연속으로 같은 class의 prose drift를 잡음**: Critical 0 + Important 2-3건 일관 발견. review가 plan-time validation의 last line of defense로 안정적 기능. Phase 43은 양적 감소(Phase 42 3건 → Phase 43 3건, 동일하지만 severity는 더 낮음)는 있었으나 class는 살아남음.

### Mistakes

- **Plan 43이 "변경되는 산문 토큰" enumerate를 4개로만 작성, "변경되는 구조를 묘사하는 보존 블록"을 missing 카테고리로 간과**. 결과적으로 Phase 42와 같은 class의 drift 3건이 review-time에 발견됨 (`a6bf7bc fix(42)` → `b7831a4 fix(43)`, 2회 연속 같은 fix 패턴). Phase 42 lesson의 정밀화 부족.
- **sg-parallel-execute → sg-review 사이의 commit 누락 자동화가 deferred 상태로 Phase 43 진입**. Phase 42 retro에서 P1로 도출되었으나 실행되지 않아 같은 사용자 확인 단계를 또 거침. P1 action item이 다음 phase 사이클에서 closure 안 됨 — Action Items가 실행되지 않으면 같은 mistake가 자동 재발생함이 명백히 입증됨.
- **STATE.md `Phase:` 필드 stale 문제가 milestone 내 3번째(36→42→43)로 재발생**. sg-execute/sg-retro 양쪽에서 매번 PHASE_NUM 수동 입력 필요. Phase 42 retro P2 action으로 deferred, Phase 43에서 또 재발 → P2 우선순위가 너무 낮았을 가능성. P1로 격상 필요.

### Action Items

| priority | item | next step |
|----------|------|-----------|
| 🔴 P1 | "변경되는 구조를 묘사하는 보존 블록" sub-class를 plan template `<interfaces>` enumerate 카테고리에 추가 — `<success_criteria>` 항목 중 structural token(`_Captured:`, `### {name}`, header pattern) 포함 블록은 byte-identical 보존과 동시에 변경 acceptance 충돌 가능성 명시 검증 | gsd-plan-phase template 또는 sg-plan SKILL.md 보강 — Phase 44 plan 작성 전 도입 필수 |
| 🔴 P1 | sg-parallel-execute → sg-review 사이 commit 자동화 (Phase 42 retro P1 #2의 closure) | `skills/sg-parallel-execute/SKILL.md` 종료 단계 또는 `skills/sg-review/SKILL.md` Step 1 보강. Phase 44 또는 별도 quick task로 즉시 실행 — 더 이상 deferred 금지 |
| 🔴 P1 | STATE.md `Phase:` 필드 자동 sync (Phase 42 P2의 P1 격상) | `skills/sg-plan/SKILL.md` 종료 시 또는 `hooks/state_sync.cjs` 신설. milestone 내 3번 연속 재발 → 우선순위 격상 |
| P2 | Plan lint script: "preserve/byte-identical/손대지 마" 영역과 grep acceptance 패턴 cross-check (Phase 42 P1 #1의 continued evidence) | `hooks/plan_lint.cjs` 신설 또는 sg-plan 내장. Phase 42·43의 PICK_MODE deviation을 회귀 테스트 케이스로 |

## Lens: Start/Stop/Continue
_Captured: 2026-05-30T15:20:30Z_

### Start

- **plan template `<interfaces>`에 "구조를 묘사하는 보존 블록" 명시 enumerate 카테고리 추가**. Phase 43에서 발견된 새로운 sub-class — `<success_criteria>` #3/#4 같은 항목이 `_Captured:`/`### {name}`/`{ISO}` 같은 structural token을 enumerate하면 변경 acceptance와 자동 충돌 가능성을 plan-time에 표시.
- **commit gate를 sg-parallel-execute 또는 sg-review에 내장**. 두 phase 연속 같은 manual commit 확인 단계를 거쳤음 — 자동화 deferred를 더 이상 허용 말 것.
- **action item P1 closure tracking**. Phase 42 P1 #2가 Phase 43까지 deferred로 살아남아 같은 mistake 재발 → milestone 내 retrospective action items의 closure 상태를 STATE.md 또는 ROADMAP에 명시적으로 추적.

### Stop

- **plan에서 "변경되는 산문 토큰" enumerate를 단일 카테고리로만 작성하기**. Phase 43이 4개 token enumerate했지만 3개 추가 sub-class 드리프트 발견 — enumerate의 정밀도가 lesson을 매 phase 정련해야 함을 인지.
- **P2 우선순위로 STATE.md sync 처리하기**. 같은 mistake가 3번 연속 재발 → 우선순위 부족 신호.
- **retro action items를 deferred로 남기고 다음 phase로 진입하기**. Phase 42 P1이 Phase 43에 deferred 상태로 진입 → 같은 mistake 재발생. P1은 다음 phase 진입 전 closure 또는 명시적 deferral with justification 필요.

### Continue

- **wave:1 + non-overlapping files + 2 Task() agent 동시 dispatch 패턴**. Phase 42·43 모두 깔끔히 작동, ~5-6분/agent, conflict 0건. 향후 pairwise 변경 default 패턴 유지.
- **SUMMARY § "Plan-spec Deviations" 섹션으로 plan-spec 충돌 surface**. Phase 42에서 정립된 패턴이 Phase 43에서 자연스럽게 답습 — reviewer가 즉시 의도성·심각도 판단 가능. 두 plan agents가 동일 deviation을 동일 형식으로 보고하는 일관성도 확인됨.
- **Code review를 D-10/D-16 pairwise 동등성 last-line-of-defense로 활용**. Phase 42·43 모두 plan-time에 못 잡은 prose drift를 review-time에 잡음 — review가 안정적으로 기능. Critical 0 + Important 2-3건 일관 발견.
- **CONTEXT.md의 18 D-XX decision lock-in**. Phase 42·43 모두 plan-time에 ambiguity 최소화 — planner agent가 명확한 spec으로 작업하여 충돌·재작업 없음.

### Action Items

| priority | item | next step |
|----------|------|-----------|
| 🔴 P1 | retro Action Items P1 closure 추적 강제 — milestone 내 다음 phase 진입 전 P1 closure 또는 명시적 justified deferral 의무 | sg-plan SKILL.md Step 0에 P1 backlog 표시 (lessons_ranker 활용). 또는 ROADMAP에 "Action Item Backlog" 섹션 신설 |
| P2 | Pairwise mirror plan template에 "변경되는 구조 묘사 블록" 카테고리 추가 (P1 action #1과 통합 가능) | gsd-plan-phase template enhancement |
| P2 | Code review가 last-line-of-defense로 잡고 있는 prose drift 패턴을 자동화 — 변경된 token이 보존 블록에서 사용되는지 grep cross-check | `hooks/plan_drift_check.cjs` 또는 review-time pre-grep |
