---
phase: 03-sg-command-set-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-execute.md
  - commands/sg-status.md
autonomous: true
requirements:
  - PLUGIN-02
must_haves:
  truths:
    - "User can invoke /super-gsd:sg-execute to hand off GSD phase to Superpowers"
    - "User can invoke /super-gsd:sg-status to inspect workflow state"
    - "commands/to-superpowers.md and commands/status.md no longer exist (replaced)"
    - "sg-status NEXT_CMD references /super-gsd:sg-execute, not /super-gsd:to-superpowers"
  artifacts:
    - path: "commands/sg-execute.md"
      provides: "GSD→Superpowers handoff command"
      contains: "name: sg-execute"
    - path: "commands/sg-status.md"
      provides: "Workflow status display command"
      contains: "name: sg-status"
  key_links:
    - from: "commands/sg-status.md"
      to: "commands/sg-execute.md"
      via: "NEXT_CMD in gsd-plan branch"
      pattern: "/super-gsd:sg-execute"
---

<objective>
Replace commands/to-superpowers.md with commands/sg-execute.md and commands/status.md with commands/sg-status.md.

Purpose: Rename the Phase 2 commands to the sg- naming convention (D-36, D-37). Logic is unchanged — only frontmatter `name` changes and one internal cross-reference updates.
Output: Two new command files with sg- names; two old files deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sg-command-set-readme/03-CONTEXT.md
@.planning/phases/03-sg-command-set-readme/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sg-execute.md (replace to-superpowers.md)</name>
  <files>commands/sg-execute.md</files>
  <read_first>
    - commands/to-superpowers.md — full source; copy verbatim then apply changes below
  </read_first>
  <action>
    Create commands/sg-execute.md as an exact copy of commands/to-superpowers.md with the following two targeted changes:

    1. Frontmatter: change `name: to-superpowers` to `name: sg-execute` (per D-36).
    2. Frontmatter: update `description` to "Hand off the current GSD phase to Superpowers — package PLAN/REQ/SC into a single prompt and auto-invoke sg-executing-plans." (description stays functionally the same, only command name context updates if the existing description already mentions to-superpowers by name — check before changing).
    3. In the `<process>` section Step 10 final user message, change `/super-gsd:status` to `/super-gsd:sg-status`.

    All other content — argument-hint, objective, execution_context, all 9 process steps, success_criteria — must be copied without modification.

    After creating sg-execute.md, delete commands/to-superpowers.md using Bash rm.
  </action>
  <verify>
    <automated>grep -c "name: sg-execute" /Users/gyuha/workspace/super-gsd/commands/sg-execute.md && ! test -f /Users/gyuha/workspace/super-gsd/commands/to-superpowers.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - commands/sg-execute.md contains `name: sg-execute` in frontmatter
    - commands/sg-execute.md contains `argument-hint:` field (carried over from to-superpowers.md)
    - commands/sg-execute.md contains exactly 9 numbered steps in the process section
    - commands/sg-execute.md contains `Skill(skill="sg-executing-plans"` in the process section
    - commands/sg-execute.md contains `/super-gsd:sg-status` in the Step 10 final user message (not /super-gsd:status)
    - commands/to-superpowers.md does not exist
  </acceptance_criteria>
  <done>sg-execute.md exists with name: sg-execute and sg-status cross-reference; to-superpowers.md deleted</done>
</task>

<task type="auto">
  <name>Task 2: Create sg-status.md (replace status.md)</name>
  <files>commands/sg-status.md</files>
  <read_first>
    - commands/status.md — full source; copy verbatim then apply changes below
  </read_first>
  <action>
    Create commands/sg-status.md as an exact copy of commands/status.md with the following targeted changes:

    1. Frontmatter: change `name: status` to `name: sg-status` (per D-37).
    2. In the `<process>` section Step 5, in the `gsd-plan` case of the stage→next-command mapping, change `NEXT_CMD="/super-gsd:to-superpowers"` to `NEXT_CMD="/super-gsd:sg-execute"`.

    All other content — description, objective, execution_context, all 5 process steps, success_criteria — must be copied without modification.

    After creating sg-status.md, delete commands/status.md using Bash rm.
  </action>
  <verify>
    <automated>grep -c "name: sg-status" /Users/gyuha/workspace/super-gsd/commands/sg-status.md && grep -c "sg-execute" /Users/gyuha/workspace/super-gsd/commands/sg-status.md && ! test -f /Users/gyuha/workspace/super-gsd/commands/status.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - commands/sg-status.md contains `name: sg-status` in frontmatter
    - commands/sg-status.md contains `NEXT_CMD="/super-gsd:sg-execute"` in the gsd-plan stage branch
    - commands/sg-status.md contains `NEXT_CMD="/hookify"` for superpowers and review stages (unchanged)
    - commands/sg-status.md outputs exactly the 4-line format: Phase / Stage / Last handoff / blank / Next (unchanged behavior)
    - commands/status.md does not exist
  </acceptance_criteria>
  <done>sg-status.md exists with name: sg-status and sg-execute cross-reference; status.md deleted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → command file | Command file contents are read by Claude Code as instructions; malicious content could redirect behavior |
| command → .planning/ files | Commands read STATE.md, HANDOFF.md, ROADMAP.md — path traversal not applicable (paths are hardcoded) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | commands/sg-execute.md | mitigate | Copy source verbatim; diff against to-superpowers.md after creation to confirm only name field changed |
| T-03-01-02 | Tampering | commands/sg-status.md | mitigate | Copy source verbatim; verify only name and NEXT_CMD gsd-plan branch changed |
| T-03-01-03 | Information Disclosure | HANDOFF.md append | accept | HANDOFF.md is an append-only planning artifact in a local repo; no PII, low-value target |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. Both sg-execute.md and sg-status.md exist in commands/
2. to-superpowers.md and status.md are deleted
3. sg-execute.md frontmatter name is sg-execute
4. sg-status.md frontmatter name is sg-status
5. sg-status.md gsd-plan branch references /super-gsd:sg-execute
6. sg-execute.md Step 10 references /super-gsd:sg-status
</verification>

<success_criteria>
- /super-gsd:sg-execute is a valid Claude Code slash command (commands/sg-execute.md with name: sg-execute)
- /super-gsd:sg-status is a valid Claude Code slash command (commands/sg-status.md with name: sg-status)
- Old commands/to-superpowers.md and commands/status.md are deleted
- Internal cross-references between the two commands are consistent (sg-execute ↔ sg-status)
</success_criteria>

<output>
Create `.planning/phases/03-sg-command-set-readme/03-01-SUMMARY.md` when done
</output>
