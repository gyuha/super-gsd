---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/hooks.json
  - hooks/stop-hook.py
  - hooks/transcript-matcher.py
  - .planning/config.json
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
  - HOOK-04

must_haves:
  truths:
    - "GSD plan-phase 완료 직후 Stop 훅이 /super-gsd:sg-execute 안내 메시지를 출력한다"
    - "Superpowers code-reviewer 완료 직후 SubagentStop 훅이 /super-gsd:sg-learn 안내 메시지를 출력한다"
    - ".planning/config.json에 super_gsd.auto_advance: false를 설정하면 두 훅 모두 아무것도 출력하지 않는다"
    - "관련 없는 명령 종료 시 훅이 발화하지 않는다"
  artifacts:
    - path: "hooks/hooks.json"
      provides: "Stop + SubagentStop 훅 이벤트 등록"
      contains: "SubagentStop"
    - path: "hooks/stop-hook.py"
      provides: "훅 공통 진입점 — config guard + 신호 감지 + systemMessage 출력"
      exports: ["main"]
    - path: "hooks/transcript-matcher.py"
      provides: "transcript 파일에서 단계 완료 신호를 감지하는 유틸리티"
      exports: ["detect_signal"]
    - path: ".planning/config.json"
      provides: "super_gsd.auto_advance 설정 키"
      contains: "super_gsd"
  key_links:
    - from: "hooks/hooks.json"
      to: "hooks/stop-hook.py"
      via: "command: python3 ${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.py"
    - from: "hooks/stop-hook.py"
      to: "hooks/transcript-matcher.py"
      via: "from transcript_matcher import detect_signal"
    - from: "hooks/stop-hook.py"
      to: ".planning/config.json"
      via: "load_config() reads super_gsd.auto_advance"
---

<objective>
Stop/SubagentStop 훅을 구현한다. GSD plan-phase 또는 Superpowers code-reviewer가 완료되면 사용자에게 다음 단계 명령을 안내하는 메시지를 자동으로 표시한다.

Purpose: 사용자가 다음 명령을 직접 기억할 필요 없이 단계 전환이 자동으로 안내된다.
Output: hooks/ 디렉토리(hooks.json, stop-hook.py, transcript-matcher.py) + config.json super_gsd 키
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-auto-advance-hooks/04-PATTERNS.md

<interfaces>
<!-- 04-PATTERNS.md에서 추출한 핵심 계약. 아래 인터페이스를 그대로 사용한다. -->

hooks/hooks.json 목표 구조:
```json
{
  "description": "super-gsd auto-advance hooks — GSD plan-phase 완료 및 Superpowers review 완료를 감지해 다음 단계를 안내한다",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.py\"",
            "timeout": 10
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
```

Stop 훅 입출력 계약:
- stdin: JSON (transcript_path 필드 포함)
- stdout: JSON (systemMessage 키 또는 빈 {})
- exit: 항상 0 (finally: sys.exit(0))
- 결코 decision: block을 사용하지 않는다

detect_signal() 반환값:
- 'gsd-plan-complete' : GSD plan-phase 완료 감지
- 'superpowers-review-complete' : Superpowers code-reviewer 완료 감지
- '' : 신호 없음

config guard 계약:
- .planning/config.json의 super_gsd.auto_advance 가 false 이면 {} 반환
- 파일이 없거나 파싱 실패 시 기본값 true 적용
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: hooks/hooks.json — Stop + SubagentStop 훅 등록</name>
  <files>hooks/hooks.json</files>
  <action>
    hooks/ 디렉토리를 생성하고 hooks.json을 작성한다. 04-PATTERNS.md의 "Phase 4용 hooks.json 목표 구조"를 그대로 사용한다.

    핵심 규칙:
    - Stop과 SubagentStop 두 이벤트를 등록한다 (HOOK-01, HOOK-02 각각).
    - command 값은 python3 "${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.py" — 절대경로 하드코딩 금지.
    - timeout: 10 (초과 시 훅이 행 걸려 세션을 막지 않도록).
    - matcher 없음 — 필터링은 스크립트 내부(transcript-matcher.py)에서 수행.
    - description 필드에 한 줄 설명 포함.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('hooks/hooks.json')); assert 'Stop' in d['hooks']; assert 'SubagentStop' in d['hooks']; print('hooks.json OK')" && echo "PASS"</automated>
  </verify>
  <done>hooks/hooks.json이 Stop + SubagentStop 두 이벤트를 등록하고 JSON 파싱이 성공한다</done>
</task>

<task type="auto">
  <name>Task 2: hooks/transcript-matcher.py — 신호 감지 유틸리티</name>
  <files>hooks/transcript-matcher.py</files>
  <action>
    04-PATTERNS.md의 "Phase 4 detect_signal() 구현 패턴"을 기반으로 transcript-matcher.py를 작성한다.

    detect_signal(transcript_path: str) -> str 함수를 구현한다:
    - transcript_path가 비어 있으면 '' 반환.
    - 파일을 열어 splitlines()[-200:]로 최근 200줄만 검사한다.
    - GSD_PLAN_SIGNALS = ['PLANNING COMPLETE', 'gsd-plan-phase', 'plan-phase complete', 'PLAN.md'] — 이 중 하나라도 recent에 있으면 'gsd-plan-complete' 반환.
    - REVIEW_SIGNALS = ['code-reviewer', 'requesting-code-review', 'review complete', 'CODE REVIEW'] — 이 중 하나라도 recent에 있으면 'superpowers-review-complete' 반환.
    - 신호 없으면 '' 반환.
    - 파일 읽기 실패(FileNotFoundError, IOError, PermissionError, UnicodeDecodeError) 시 '' 반환.

    spurious firing 방지 원칙 (HOOK-04):
    - 최근 200줄만 검사 — 오래된 기록에서 발화하지 않는다.
    - GSD 신호와 Review 신호가 동시에 존재하면 gsd-plan-complete를 우선한다(GSD 체크를 먼저 수행).

    파일 상단에 모듈 docstring을 포함시킨다. `if __name__ == '__main__':` 블록은 작성하지 않는다.
  </action>
  <verify>
    <automated>python3 -c "
import sys; sys.path.insert(0, 'hooks')
from transcript_matcher import detect_signal
assert detect_signal('') == ''
assert detect_signal('/nonexistent/path') == ''
print('transcript-matcher.py import OK')
" && echo "PASS"</automated>
  </verify>
  <done>detect_signal()이 import 가능하고, 빈 경로 및 존재하지 않는 경로에 대해 '' 를 반환한다</done>
</task>

<task type="auto">
  <name>Task 3: hooks/stop-hook.py + config.json 패치</name>
  <files>hooks/stop-hook.py, .planning/config.json</files>
  <action>
    [stop-hook.py]

    04-PATTERNS.md의 "Phase 4 stop-hook.py 골격"을 기반으로 작성한다.

    구조:
    1. load_config() — .planning/config.json 에서 super_gsd 딕셔너리 반환. 파일 없음/파싱 실패 시 {} 반환.
    2. main() — stdin에서 JSON을 읽고, try/except/finally 패턴으로 항상 exit 0을 보장.

    main() 흐름:
    a. json.load(sys.stdin) 으로 input_data 읽기.
    b. load_config() 호출 — super_gsd.auto_advance 가 False 면 {} 출력 후 즉시 sys.exit(0).
    c. input_data.get('transcript_path', '') 로 경로 추출.
    d. from transcript_matcher import detect_signal 으로 신호 감지.
       (Python import 경로가 hooks/ 안에서 실행된다. import 실패 시 except에서 처리.)
    e. 신호에 따라 systemMessage 출력:
       - 'gsd-plan-complete': "GSD plan-phase complete. Run /super-gsd:sg-execute to hand off to Superpowers."
       - 'superpowers-review-complete': "Superpowers review complete. Run /super-gsd:sg-learn to capture lessons with Hookify."
       - 기타: {} 출력 (아무 메시지도 없음).
    f. except Exception as e: → {"systemMessage": f"super-gsd hook error: {e}"} 출력.
    g. finally: sys.exit(0).

    파일 첫 줄은 #!/usr/bin/env python3.
    파일을 실행 가능하게 설정하지 않아도 됨 — hooks.json이 python3로 명시 호출.

    [.planning/config.json 패치]

    기존 config.json에 super_gsd 최상위 키를 추가한다. 기존 키를 변경하지 않는다.
    추가할 내용:
    ```json
    "super_gsd": {
      "auto_advance": true
    }
    ```
    jq 명령 사용: jq '. + {"super_gsd": {"auto_advance": true}}' .planning/config.json > /tmp/config_tmp.json && mv /tmp/config_tmp.json .planning/config.json

    jq 없는 환경 대비: Python으로 fallback — json.load → dict update → json.dump (indent=2).
  </action>
  <verify>
    <automated>
      python3 -c "
import json, sys, os
# 1. config.json에 super_gsd 키 확인
cfg = json.load(open('.planning/config.json'))
assert 'super_gsd' in cfg, 'super_gsd key missing'
assert cfg['super_gsd'].get('auto_advance') is True, 'auto_advance should be true'

# 2. stop-hook.py import 확인
sys.path.insert(0, 'hooks')
import stop_hook
print('stop-hook.py import OK')
print('config.json super_gsd OK')
" &amp;&amp; echo "PASS"

      echo '{"transcript_path": ""}' | python3 hooks/stop-hook.py | python3 -c "
import json,sys
d=json.load(sys.stdin)
assert d == {}, f'Expected empty dict for no signal, got: {d}'
print('no-signal path OK')
"
    </automated>
  </verify>
  <done>
    1. .planning/config.json에 super_gsd.auto_advance: true 키가 존재하고 기존 키는 그대로다.
    2. stop-hook.py가 Python import 가능하다.
    3. 빈 transcript_path 입력 시 {} 를 출력한다.
    4. auto_advance: false 설정 시 {} 를 출력한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code → hook script | 훅 이벤트 JSON이 stdin으로 전달됨 — transcript_path는 로컬 파일 경로 |
| hook script → transcript file | transcript_path 경로를 파일 시스템에서 읽음 |
| hook script → config.json | .planning/config.json을 읽어 auto_advance 여부 결정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | transcript_path field | accept | 로컬 파일 경로이며 훅은 읽기만 수행. 파일 쓰기 없음. |
| T-04-02 | Denial of Service | stop-hook.py 무한 루프 | mitigate | hooks.json timeout: 10 설정으로 10초 초과 시 강제 종료 |
| T-04-03 | Information Disclosure | systemMessage 출력 | accept | 출력은 고정 문자열 — 파일 내용이나 환경변수를 노출하지 않음 |
| T-04-04 | Elevation of Privilege | config.json 파싱 실패 | mitigate | 예외 발생 시 기본값 true 적용(fail-open) — 훅 비활성화는 사용자 의도여야 하므로 적합 |
| T-04-SC | Tampering | (해당 없음 — 이 플랜에서 npm/pip/cargo 패키지 설치 없음) | accept | 외부 패키지 설치 없음 — 표준 라이브러리(json, sys, os)만 사용 |
</threat_model>

<verification>
Phase 4 전체 검증:

1. Stop 훅 동작: `echo '{"transcript_path": ""}' | python3 hooks/stop-hook.py` → `{}` 출력
2. GSD 신호 감지: transcript 파일에 'PLANNING COMPLETE' 포함 시 gsd-plan-complete 신호 반환
3. Review 신호 감지: transcript 파일에 'code-reviewer' 포함 시 superpowers-review-complete 신호 반환
4. config guard: super_gsd.auto_advance: false 시 항상 {} 출력
5. 무관 명령 종료: 어떤 신호도 없는 transcript → {} 출력
6. hooks.json 유효성: Stop + SubagentStop 두 이벤트 등록 확인
7. config.json 기존 키 보존: workflow.auto_advance 등 기존 키 변경 없음

```bash
# 전체 검증 커맨드
python3 -c "
import json
# hooks.json
d = json.load(open('hooks/hooks.json'))
assert 'Stop' in d['hooks']
assert 'SubagentStop' in d['hooks']

# config.json
cfg = json.load(open('.planning/config.json'))
assert cfg['super_gsd']['auto_advance'] is True
assert 'workflow' in cfg  # 기존 키 보존

print('All structural checks PASS')
"

echo '{"transcript_path": ""}' | python3 hooks/stop-hook.py
```
</verification>

<success_criteria>
1. hooks/hooks.json이 Stop + SubagentStop 두 이벤트를 등록하고 JSON 파싱 성공
2. hooks/stop-hook.py가 stdin JSON을 받아 systemMessage 또는 {} 를 stdout에 출력하고 exit 0으로 종료
3. hooks/transcript-matcher.py의 detect_signal()이 transcript 내용 기반으로 신호를 정확히 반환
4. .planning/config.json에 super_gsd.auto_advance: true 키 추가, 기존 키 미변경
5. auto_advance: false 설정 시 stop-hook.py가 {} 출력 (HOOK-03)
6. 관련 없는 transcript 입력 시 {} 출력 (HOOK-04)
</success_criteria>

<output>
완료 후 `.planning/phases/04-auto-advance-hooks/04-01-SUMMARY.md`를 생성한다.
</output>
