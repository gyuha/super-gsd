---
phase: 40
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-execute/SKILL.md
  - .agents/skills/sg-execute/SKILL.md
autonomous: true
requirements:
  - TEAM-03

must_haves:
  truths:
    - "sg-execute가 main 또는 master 브랜치에서 실행되면 phase/{N}-{slug} 브랜치 생성을 AskUserQuestion으로 제안한다"
    - "사용자가 동의하면 git checkout -b phase/{N}-{slug}가 실행된다"
    - "feature 브랜치(main/master 이외)에서 실행 중이면 제안 없이 기존 흐름이 그대로 진행된다"
    - "git 미설치/non-git 환경에서는 제안 없이 기존 흐름이 유지된다"
  artifacts:
    - path: "skills/sg-execute/SKILL.md"
      provides: "Step 1.5 브랜치 감지 + AskUserQuestion 제안 로직"
      contains: "Step 1.5"
    - path: ".agents/skills/sg-execute/SKILL.md"
      provides: "Step 1.5 브랜치 감지 로직 (AskUserQuestion 미지원 — 메시지 출력 후 계속)"
      contains: "Step 1.5"
  key_links:
    - from: "skills/sg-execute/SKILL.md Step 1"
      to: "Step 1.5"
      via: "PHASE_NUM, PHASE_PAD 확정 후 삽입"
      pattern: "Step 1\\.5"
    - from: "Step 1.5"
      to: "Step 2"
      via: "브랜치 생성 or 스킵 후 계속"
      pattern: "CURRENT_BRANCH"
---

<objective>
`skills/sg-execute/SKILL.md`의 Step 1 (phase resolve) 완료 직후에 Step 1.5를 삽입하여 main/master 브랜치 감지 + `phase/{N}-{slug}` 브랜치 생성 제안을 구현한다. `.agents/skills/sg-execute/SKILL.md`에도 동일 로직을 추가하되, AskUserQuestion 미지원 플랫폼이므로 분기 없이 메시지 출력 후 계속 진행하도록 처리한다.

Purpose: TEAM-03 — 팀원이 실수로 main 브랜치에서 작업하는 것을 방지하고, phase 브랜치 워크플로우를 자연스럽게 유도한다.
Output: 수정된 skills/sg-execute/SKILL.md, .agents/skills/sg-execute/SKILL.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-sg-execute-branch-workflow/40-CONTEXT.md
</context>

<interfaces>
<!-- Phase 40 CONTEXT.md에서 추출한 핵심 패턴 -->

Step 1.5 삽입 위치: skills/sg-execute/SKILL.md의 Step 1 블록 닫힘("If extraction fails..." 이후) ~ Step 2 블록 시작("2. **Locate phase directory.**") 사이.

브랜치 감지 로직 (CONTEXT.md §B):
```bash
CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "unknown")
if [ "$CURRENT_BRANCH" = "main" ] || [ "$CURRENT_BRANCH" = "master" ]; then
  # → 브랜치 생성 제안
else
  # → 제안 없이 기존 흐름 그대로 진행
fi
```

브랜치명 계산 (CONTEXT.md §C + §specifics):
- `BRANCH_NAME="phase/${PHASE_PAD}-${SLUG}"`
- `PHASE_PAD=$(printf "%02d" $PHASE_NUM)` — 이미 Step 1에서 확정됨
- slug: ROADMAP.md Read 도구로 읽어 `### Phase N:` 헤더의 이름 부분을 소문자+하이픈 정규화 (bash tr/sed 파이프라인 대신 Claude가 직접 정규화)
- 예: `sg-execute 브랜치 워크플로우 + PR 안내` → `sg-execute-branch-workflow`

AskUserQuestion 패턴 (CONTEXT.md §C):
```
AskUserQuestion(
  header: "Branch",
  question: "현재 main 브랜치입니다. phase/{N}-{slug} 브랜치를 생성하고 전환할까요?",
  options: ["Create branch (권장)", "Skip — main에서 계속"]
)
```
- "Create branch" 선택 시: `git checkout -b {BRANCH_NAME}` 실행 후 계속
- "Skip" 선택 시: 제안 없이 기존 흐름 그대로

.agents/ 플랫폼 제약 (현재 .agents/skills/sg-execute/SKILL.md <constraints> 블록):
"AskUserQuestion not supported" — 따라서 .agents 버전은 AskUserQuestion 없이 메시지만 출력하고 계속 진행.
예시 대안: 브랜치명을 계산하여 "On main branch — suggested branch: phase/{N}-{slug}. Create it with: git checkout -b phase/{N}-{slug}" 메시지 출력 후 계속.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: skills/sg-execute/SKILL.md에 Step 1.5 삽입</name>
  <files>skills/sg-execute/SKILL.md</files>
  <read_first>
    - skills/sg-execute/SKILL.md — Step 1 블록 끝 위치(PHASE_NUM 검증 종료)와 Step 2 시작 위치 확인. PHASE_PAD 변수가 Step 2에서 계산됨을 확인 (Step 1.5에서 먼저 계산 필요).
    - .planning/phases/40-sg-execute-branch-workflow/40-CONTEXT.md — §A(삽입 위치), §B(감지 로직), §C(제안 방식), §specifics(브랜치명 계산)
  </read_first>
  <action>
    skills/sg-execute/SKILL.md의 Step 1 검증 블록(PHASE_NUM empty/invalid 체크 끝) 바로 다음, Step 2 헤더 바로 위에 Step 1.5 블록을 삽입한다.

    삽입할 내용 (per D-A, D-B, D-C):

    **Step 1.5 헤더:** `1.5. **Branch detection (TEAM-03).** Check if current git branch is main or master; if so, offer to create a phase branch.`

    **Step 1.5 로직:**
    1. PHASE_PAD 계산: Step 2에서 PHASE_PAD를 계산하지만 Step 1.5는 그 이전이므로, 여기서 먼저 계산한다 — `PHASE_PAD=$(printf "%02d" "$PHASE_NUM")` (decimal 처리는 Step 2와 동일하게).
    2. ROADMAP.md를 Read 도구로 읽어 `### Phase {PHASE_NUM}:` 헤더에서 phase 이름을 추출하고, 소문자+공백→하이픈+특수문자 제거로 BRANCH_SLUG를 생성한다. 예: `sg-execute 브랜치 워크플로우 + PR 안내` → `sg-execute-branch-workflow`. 이 정규화는 bash 파이프라인이 아니라 Claude가 직접 처리한다.
    3. `BRANCH_NAME="phase/${PHASE_PAD}-${BRANCH_SLUG}"`
    4. git 브랜치 감지:
       ```bash
       CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "unknown")
       ```
    5. main/master이면: AskUserQuestion으로 브랜치 생성 제안
       - header: "Branch"
       - question: 사용자 언어로 "현재 {CURRENT_BRANCH} 브랜치입니다. {BRANCH_NAME} 브랜치를 생성하고 전환할까요?" (언어 감지 지침 준수 — 산문은 사용자 언어, 브랜치명은 영문 그대로)
       - options: ["Create branch (권장)", "Skip — continue on {CURRENT_BRANCH}"]
       - "Create branch" 선택 시: `git checkout -b {BRANCH_NAME}` 실행 후 Step 2로 진행
       - "Skip" 선택 시: Step 2로 진행 (변경 없음)
    6. main/master 이외이면 (`develop`, `release/*`, `phase/*`, `hotfix/*`, `unknown` 등): 이 step 전체 건너뜀, Step 2로 바로 진행

    **주의사항:**
    - Step 2에서 PHASE_PAD를 다시 계산하는 기존 코드는 그대로 유지한다 (중복이지만 Step 2 블록 자체를 수정하지 않음 — 외과적 변경).
    - ROADMAP.md slug 추출은 Read 도구 기반 (v2.5 컨벤션 — bash grep/awk 파이프라인 금지).
  </action>
  <verify>
    <automated>grep -n "Step 1\.5" skills/sg-execute/SKILL.md</automated>
    <automated>grep -n "CURRENT_BRANCH" skills/sg-execute/SKILL.md</automated>
    <automated>grep -n "BRANCH_NAME" skills/sg-execute/SKILL.md</automated>
    <automated>grep -n "AskUserQuestion" skills/sg-execute/SKILL.md</automated>
  </verify>
  <done>
    - skills/sg-execute/SKILL.md에 "1.5." 또는 "Step 1.5" 헤더가 존재한다
    - CURRENT_BRANCH, BRANCH_NAME, BRANCH_SLUG 변수가 Step 1.5 블록 내에 있다
    - AskUserQuestion이 Step 1.5 내에 존재한다
    - Step 1과 Step 2 사이에 삽입되어 있다 (line number 기준으로 Step 1.5 < Step 2)
    - 기존 Step 2~9 번호는 변경되지 않았다
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: .agents/skills/sg-execute/SKILL.md에 Step 1.5 삽입 (pairwise)</name>
  <files>.agents/skills/sg-execute/SKILL.md</files>
  <read_first>
    - .agents/skills/sg-execute/SKILL.md — Step 1 블록 끝 위치와 Step 2 시작 위치, <constraints> 블록("AskUserQuestion not supported") 확인
    - CLAUDE.md §skills/ + .agents/ 쌍 커버 컨벤션
  </read_first>
  <action>
    .agents/skills/sg-execute/SKILL.md의 Step 1 바로 다음, Step 2 헤더 바로 위에 Step 1.5 블록을 삽입한다. AskUserQuestion 미지원 플랫폼이므로 다음과 같이 처리한다:

    **AskUserQuestion 대신 메시지 출력:**
    - main/master 감지 시, AskUserQuestion 없이 메시지 텍스트로 출력:
      "On main branch — to create a phase branch, run: `git checkout -b {BRANCH_NAME}`"
    - 사용자 언어 지침 준수: 산문은 사용자 언어, 명령어(`git checkout -b ...`)는 영문 그대로
    - 메시지 출력 후 자동으로 Step 2로 진행 (브랜치 생성은 사용자가 직접 선택)

    **로직 구조:**
    1. PHASE_PAD 계산 (`printf "%02d" "$PHASE_NUM"`)
    2. ROADMAP.md Read 도구로 읽어 slug 추출 → BRANCH_NAME 계산
    3. `CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "unknown")`
    4. main/master이면 메시지 출력 후 계속
    5. main/master 이외이면 건너뜀

    **중요:** <constraints> 블록의 "AskUserQuestion not supported" 문구는 그대로 유지한다.
  </action>
  <verify>
    <automated>grep -n "Step 1\.5" .agents/skills/sg-execute/SKILL.md</automated>
    <automated>grep -n "CURRENT_BRANCH" .agents/skills/sg-execute/SKILL.md</automated>
    <automated>grep -n "BRANCH_NAME" .agents/skills/sg-execute/SKILL.md</automated>
  </verify>
  <done>
    - .agents/skills/sg-execute/SKILL.md에 Step 1.5 블록이 삽입되어 있다
    - AskUserQuestion이 없다 (constraints 블록 준수)
    - CURRENT_BRANCH 변수와 main/master 감지 로직이 있다
    - BRANCH_NAME 계산 로직이 있다
    - 기존 <constraints> 블록 내용("AskUserQuestion not supported")이 유지된다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill → git CLI | SKILL.md 내 `git rev-parse`/`git checkout -b` 명령 실행 |
| skill → ROADMAP.md | Read 도구로 phase slug 추출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Spoofing | git rev-parse HEAD | accept | 출력은 브랜치명 제안에만 사용, 시스템 영향 없음 |
| T-40-02 | Tampering | git checkout -b | accept | 사용자가 AskUserQuestion으로 명시적 동의 후 실행, non-destructive 명령 |
| T-40-03 | Information Disclosure | ROADMAP.md slug | accept | 로컬 파일, 민감 정보 없음 |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | 이 plan에 패키지 설치 없음 |
</threat_model>

<verification>
```bash
# Step 1.5가 Step 1 이후 Step 2 이전에 삽입되었는지 확인
grep -n "Resolve phase\|1\.5\|Locate phase" skills/sg-execute/SKILL.md | head -10

# .agents 쌍도 동일하게 확인
grep -n "Resolve phase\|1\.5\|Locate phase" .agents/skills/sg-execute/SKILL.md | head -10

# CURRENT_BRANCH, BRANCH_NAME, AskUserQuestion 존재 확인 (skills/)
grep -c "CURRENT_BRANCH\|BRANCH_NAME\|AskUserQuestion" skills/sg-execute/SKILL.md

# .agents/에는 AskUserQuestion이 없어야 함 (constraints 준수)
grep "AskUserQuestion" .agents/skills/sg-execute/SKILL.md | grep -v "not supported" | wc -l
# 위 명령 결과가 0이어야 함
```
</verification>

<success_criteria>
1. `skills/sg-execute/SKILL.md`에 Step 1.5 블록이 삽입되고, main/master 감지 시 AskUserQuestion으로 브랜치 생성을 제안한다
2. `git checkout -b phase/{N}-{slug}` 실행 로직이 "Create branch" 선택 경로에 존재한다
3. main/master 이외 브랜치에서는 Step 1.5 전체가 건너뛰어진다
4. `.agents/skills/sg-execute/SKILL.md`에도 Step 1.5가 삽입되고, AskUserQuestion 없이 메시지 출력 후 계속 진행한다
5. 기존 Step 2~9 로직은 수정 없이 유지된다
</success_criteria>

<output>
Create `.planning/phases/40-sg-execute-branch-workflow/40-01-SUMMARY.md` when done
</output>
