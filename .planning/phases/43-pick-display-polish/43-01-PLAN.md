---
phase: 43-pick-display-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-retro/SKILL.md
autonomous: true
requirements: [LENS-03, DISPLAY-01, DISPLAY-02]
tags: [sg-retro, pick, display, askuserquestion, skills]

must_haves:
  truths:
    - "sg-retro 호출 시 `--pick` 토큰이 어느 위치에 있어도(`43 --pick` / `--pick 43` / `43 ssc --pick`) 동일하게 인식된다"
    - "`--pick` + positional lens 인자가 동시에 들어오면 stderr 에러 + exit 1로 거부되고 AskUserQuestion은 호출되지 않는다"
    - "`--pick` 모드에서는 정확히 1회의 AskUserQuestion(multiSelect, 3 옵션) 호출만으로 lens 선택이 종료된다"
    - "사용자가 0개 lens를 선택하면 silent exit(0) + stderr 안내, lessons 파일 미생성, HANDOFF append 없음"
    - "`--pick` 없이 호출 시 Phase 42 smart default(`dspm ssc`)는 변경 없이 동일하게 동작한다 (no regression)"
    - "lessons 파일의 모든 P1 행이 priority 셀에서 `🔴 P1`로 시작한다 (P2/P3는 prefix 없음)"
    - "각 lens section의 `_Captured: {ISO}_` 줄 바로 다음 줄에 `_Intent: ..._` italic single-line이 emit된다"
  artifacts:
    - path: "skills/sg-retro/SKILL.md"
      provides: "--pick AskUserQuestion 분기 + DISPLAY-01 P1 emoji prefix + DISPLAY-02 lens intent line"
      contains: 'PICK_MODE=true'
  key_links:
    - from: "skills/sg-retro/SKILL.md Step 1 PICK_MODE detection"
      to: "Step 2 conflict reject + Step 5 AskUserQuestion 분기"
      via: "PICK_MODE 변수"
      pattern: "PICK_MODE"
    - from: "skills/sg-retro/SKILL.md Step 6 LENS_NAME case"
      to: "Step 6 INTENT_LINE case + BODY_PRINTF emit"
      via: "LENS_CODE 변수"
      pattern: "INTENT_LINE"
    - from: "skills/sg-retro/SKILL.md Step 6 ACTION_ITEMS_PRINTF 가이드"
      to: "lessons 파일 priority 셀 `🔴 P1` prefix"
      via: "Claude execute-time printf 라인"
      pattern: "🔴 P1"
---

<objective>
`skills/sg-retro/SKILL.md`를 surgical edit 하여 Phase 43의 세 가지 변경을 적용한다:

1. **LENS-03 `--pick` 명시적 lens 선택 UI (D-01~D-07)** — Step 1에 `--pick` 토큰 탐지 분기를 추가하고, Step 2에 `--pick` + positional lens 충돌 reject 분기를 신설하며, Step 5 직전(또는 lens-loop 진입 직전)에 AskUserQuestion multiSelect 호출 블록을 신설한다. `sg-learn`은 변경 없음(D-11 carry-forward).
2. **DISPLAY-01 P1 visual emphasis (D-08~D-11)** — Step 6의 `ACTION_ITEMS_PRINTF` placeholder 주석 가이드에 "P1 행은 priority 셀에 `🔴 P1`로 emit, P2/P3는 변경 없음" 지시를 명시한다 (Claude가 execute 시점에 printf 라인 생성).
3. **DISPLAY-02 lens intent line (D-12~D-15)** — Step 6에 `INTENT_LINE` case 매핑(3개 lens 고정 영문 문구)을 추가하고, `_Captured:` printf 직후에 `printf '%s\n\n' "$INTENT_LINE"` 한 줄을 single-redirect block 내부에 삽입한다 (D-21 단일 redirect 보존).

추가로:
- `<success_criteria>` 블록에 `--pick` 동작·P1 prefix·intent line 동작을 명시하는 항목을 추가한다 (총 11개 → 14개 권장).
- `<objective>` 블록의 산문을 `--pick` flag 존재를 반영하도록 surgical 갱신한다 (Phase 42 retro의 prose drift 교훈 — `<objective>`도 변경 토큰 대상).
- frontmatter `description`은 본 phase에서는 변경하지 않는다(이미 Phase 42에서 "Smart default (dspm+ssc) runs when no lens argument is provided"로 갱신됨 — `--pick` 언급 추가는 Phase 44 DOC-01 영역).

Purpose: 사용자가 명시적으로 lens를 고르고 싶을 때 정확히 1회의 AskUserQuestion으로 종료되고(LENS-03), lessons 파일에서 P1과 lens 의도가 한눈에 보이도록(DISPLAY-01, DISPLAY-02) UX를 폴리시한다.

Output: 의미적으로 동등한 단일 파일(`skills/sg-retro/SKILL.md`) — 라인 수는 증가하지만 Step 번호/기존 보존 블록은 byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-pick-display-polish/43-CONTEXT.md
@.planning/phases/42-smart-default-lens/42-CONTEXT.md
@.planning/lessons/42-2026-05-30.md
@CLAUDE.md
@skills/sg-retro/SKILL.md
</context>

<interfaces>
<!-- Phase 43에서 변경되는 식별자와 정확한 토큰. 실행자는 추가 탐색 없이 이 값들을 그대로 사용한다. -->

**--pick 토큰:** `--pick` (long flag, lowercase, hyphenated). short alias 없음.
**PICK_MODE 변수명:** `PICK_MODE` (bash boolean — `true`/`false` 문자열 비교 사용).
**충돌 reject 조건:** `[ "$PICK_MODE" = "true" ] && [ -n "$LENS_RAW$EXTRA_LENS_CODES" ]`

**충돌 reject 에러 메시지 (영문 source — Claude가 출력 시점에 사용자 언어로 렌더링; 머신 토큰 `--pick`, `ssc`, `dspm`, `analyze`는 원문 그대로):**
```
Cannot combine --pick with positional lens argument.
Use either: sg-retro {phase} {lens...}  (explicit args)
Or:         sg-retro {phase} --pick     (interactive picker)
```
이어서 `exit 1`. lessons 파일 미생성. HANDOFF append 없음.

**AskUserQuestion 호출 형태 (D-05, D-07):**
- `header`: `"Pick Lens"` (≤12 chars)
- `question`: `"Which lens(es) do you want to run for Phase ${PHASE_RAW}?"`
- `multiSelect`: `true`
- options (3개, label + description):
  - `label: "ssc"`, `description: "surface behavior changes — what to start, stop, or continue doing next phase."`
  - `label: "dspm"`, `description: "capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase."`
  - `label: "analyze"`, `description: "scan session transcript for frustration, correction, repetition, and validated-success signals; propose sg-rule drafts."`
- description 문구는 D-12의 intent line과 **동일 문구 재사용**(중복 정의 방지) — `_Intent: ` prefix와 trailing italic 마커는 제거하고 본문만.

**사용자 0개 선택 시 동작 (D-06):**
- stderr: `--pick cancelled — no lens selected, no retrospective recorded.`
- `exit 0` (silent exit — exit 1 아님, 실패가 아닌 사용자 의도적 취소)
- lessons 파일 미생성, HANDOFF append 없음

**INTENT_LINE 고정 문구 (D-12, D-15 — 영문 source 고정, 사용자 언어 번역 없음):**
- `ssc`: `_Intent: surface behavior changes — what to start, stop, or continue doing next phase._`
- `dspm`: `_Intent: capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase._`
- `analyze`: `_Intent: scan session transcript for frustration, correction, repetition, and validated-success signals; propose sg-rule drafts._`

(em-dash `—` U+2014 사용 — ASCII `--` 아님. CLAUDE.md "em-dash 보존" 컨벤션과 일관.)

**INTENT_LINE case 매핑 위치 (Step 6):**
LENS_NAME case (현재 263-267줄) **직후**에 동일 case 패턴으로 추가한다:
```bash
case "$LENS_CODE" in
  ssc)     INTENT_LINE='_Intent: surface behavior changes — what to start, stop, or continue doing next phase._' ;;
  dspm)    INTENT_LINE='_Intent: capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase._' ;;
  analyze) INTENT_LINE='_Intent: scan session transcript for frustration, correction, repetition, and validated-success signals; propose sg-rule drafts._' ;;
esac
```
들여쓰기/정렬은 인접 LENS_NAME case와 동일(2-space 들여쓰기, `analyze` 7자에 맞춘 패딩). single-quote(`'...'`) 사용 — em-dash와 italic underscore가 bash expansion에 영향 받지 않도록.

**INTENT_LINE emit 위치 (Step 6 BODY_PRINTF 직전, single redirect block 내부 — D-21 보존):**
현재 (line ~313):
```bash
printf '%s\n' "$LENS_HEADER"
printf '_Captured: %s_\n\n' "$NOW_ISO"
# BODY_PRINTF — Claude inserts subheading + bullet `printf` lines here.
```
변경 후:
```bash
printf '%s\n' "$LENS_HEADER"
printf '_Captured: %s_\n' "$NOW_ISO"
printf '%s\n\n' "$INTENT_LINE"
# BODY_PRINTF — Claude inserts subheading + bullet `printf` lines here.
```
(주의: 현재 `_Captured:` printf의 `\n\n`을 `\n`으로 줄이고, intent line printf가 `\n\n`을 emit하여 _Captured + Intent 두 줄 + 빈 줄 1개의 패턴을 만든다. 빈 줄 총 개수는 보존.)

**ACTION_ITEMS_PRINTF 주석 가이드 갱신 (Step 6, line ~325):**
현재 주석:
```
# ACTION_ITEMS_PRINTF — Claude inserts row `printf` lines here, e.g.:
#   printf '| P1 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"
```
변경 후:
```
# ACTION_ITEMS_PRINTF — Claude inserts row `printf` lines here.
# DISPLAY-01 (D-08, D-09): P1 행은 priority 셀에 `🔴 P1` prefix로 emit. P2/P3는 prefix 없음.
#   printf '| 🔴 P1 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"   # P1 only
#   printf '| P2 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"      # P2 unchanged
#   printf '| P3 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"      # P3 unchanged
```

**Step 1 PICK_MODE 탐지 블록 (Step 1 시작부, `set -- $ARGUMENTS` 직후):**
```bash
# D-01, D-02: --pick 토큰을 어느 위치에서든 탐지하고 제거 (positional 파싱 보존)
PICK_MODE=false
NEW_ARGS=""
for ARG in "$@"; do
  case "$ARG" in
    --pick)
      PICK_MODE=true
      ;;
    *)
      NEW_ARGS="${NEW_ARGS}${NEW_ARGS:+ }${ARG}"
      ;;
  esac
done
# shellcheck disable=SC2086 — intentional word splitting for positional re-set
set -- $NEW_ARGS
PHASE_RAW="${1:-}"
LENS_RAW="${2:-}"
```
(주의: 현재 Step 1의 `set -- $ARGUMENTS` + `PHASE_RAW="${1:-}"` + `LENS_RAW="${2:-}"` 3줄은 위 블록으로 **확장 교체**된다. 이후 PHASE_RAW empty check + EXTRA_LENS_CODES 파싱 로직은 byte-identical 보존.)

**Step 2 conflict reject 분기 (Step 2 lens case 매핑 직후, smart default 분기 직전):**
```bash
# D-03: --pick + positional lens 충돌 reject
if [ "$PICK_MODE" = "true" ] && { [ -n "$LENS_RAW" ] || [ -n "$EXTRA_LENS_CODES" ]; }; then
  printf 'Cannot combine --pick with positional lens argument.\nUse either: sg-retro {phase} {lens...}  (explicit args)\nOr:         sg-retro {phase} --pick     (interactive picker)\n' >&2
  exit 1
fi
```

**Step 5 AskUserQuestion 호출 블록 (현재 Step 5 `ANALYZE_LENS_RAN=false` 직후, lens loop 진입 직전):**
```bash
# D-05, D-06: --pick 모드 → AskUserQuestion multiSelect 1회 호출
if [ "$PICK_MODE" = "true" ]; then
  # Claude: invoke AskUserQuestion with the spec defined in <interfaces>.
  # The user's selection (1+ of: ssc, dspm, analyze) replaces LENS_CODES_ARRAY.
  # 0-selection → stderr "--pick cancelled — no lens selected, no retrospective recorded." + exit 0
  # See plan 43-01-PLAN.md <interfaces> for the exact AskUserQuestion shape.
  : # placeholder — Claude execute-time inserts AskUserQuestion call here
fi
```
(주의: bash 자체로는 AskUserQuestion을 호출할 수 없다. 위 블록은 Claude가 execute 시점에 AskUserQuestion tool call로 치환하고, 결과를 `LENS_CODES_ARRAY="ssc dspm"` 같은 공백 구분 문자열로 변환한다. 0개 선택 시 분기 처리도 Claude가 담당. SKILL.md는 "여기서 Claude가 무엇을 해야 하는지" 지시문만 명시.)

**변경되는 산문 토큰 (Phase 42 retro lessons P1 #1 적용 — prose drift 방지를 위해 명시적 enumerate):**
- `<objective>` (line 14): 현재 `... — Start/Stop/Continue (ssc), Decisions/Surprises/Patterns/Mistakes (dspm), Conversation Analyzer (analyze) — via direct arguments, or omit the lens argument to apply the smart default (dspm+ssc).` → 변경 후 `... via direct arguments, the `--pick` flag (interactive multiSelect), or omit the lens argument to apply the smart default (dspm+ssc).` (─pick 진입점 명시 추가)
- Step 1 본문 산문 (line ~24): 현재 `Parse \`$ARGUMENTS\` into \`PHASE_RAW\` and \`LENS_RAW\`.` → 변경 후 `Parse \`$ARGUMENTS\` into \`PHASE_RAW\` and \`LENS_RAW\`. Detect \`--pick\` token anywhere in the argument list and strip it before positional parsing (D-01, D-02).`
- Step 5 본문 산문 — `Common flow for ssc/dspm lenses` 항목 #5 (line ~214): 현재 `Assemble the final lens section (header + \`_Captured: {NOW_ISO}_\` + subheadings + Action Items) and append to \`${LESSONS_FILE}\` ...` → 변경 후 `Assemble the final lens section (header + \`_Captured: {NOW_ISO}_\` + \`_Intent: ..._\` + subheadings + Action Items) and append to \`${LESSONS_FILE}\` ...` (intent line 명시 추가)
- `<success_criteria>` 항목 추가 — D-18 시나리오를 반영한 신규 항목 3개 (#12 --pick + AskUserQuestion 1회, #13 P1 emoji prefix, #14 lens intent line).

**보존되는 산문 토큰 (절대 변경 금지):**
- `<language>` 블록 전체 (lines 6-11)
- frontmatter `name`/`description` 필드 (lines 1-3) — 본 phase out of scope
- Step 1 STATE.md parsing 블록 (`# --- BEGIN STATE.md Phase parsing block ... ---` ~ `# --- END ---` 사이의 모든 줄, lines 33-38) — macOS-compatible grep+sed+awk pipeline
- Step 2 case 매핑 3-lens 본문 (ssc/dspm/analyze) + dropped reject 분기 (4ls/sail/5why + *)
- Step 2 VALID_EXTRAS inner case (Phase 42에서 정착) — byte-identical
- Step 2 smart default 분기 (`if [ -z "$LENS_CODES_ARRAY" ] && [ -z "$LENS_RAW" ] && [ -z "$EXTRA_LENS_CODES" ]; then LENS_CODES_ARRAY="dspm ssc"; fi`) — byte-identical
- Step 3, 3b, 4 전체 — byte-identical
- Step 5 sub-block 본문 (ssc/dspm/analyze facilitation + DSPM explicit guard + analyze 7-step + ANALYZE_LENS_RAN=true) — byte-identical
- Step 6 `LENS_NAME` case 매핑 — byte-identical (INTENT_LINE는 그 **직후** 새 case로 삽입; LENS_NAME case는 손대지 않음)
- Step 6 `RUN_SUFFIX` 분기 + `LENS_HEADER` 조립 + 파일 존재 체크 + 신규 파일 헤더 작성 — byte-identical
- Step 6 single `>>` redirect block의 단일성(D-21) — INTENT_LINE printf 한 줄만 _Captured printf와 BODY_PRINTF 사이에 삽입; 별도 redirect block 신설 금지
- Step 6 auto-suggest 블록 + HANDOFF.md append 블록 — byte-identical
- `<success_criteria>` 기존 11개 항목 (#1~#11) 본문 — byte-identical (3개 항목만 추가)

**보존 블록과 변경 acceptance의 충돌 회피 (Phase 42 mistake #1 + #2 직접 적용):**
- `<objective>`는 위 "변경되는 산문 토큰"에 명시되어 있으므로 byte-identical 검증 acceptance를 두지 않는다.
- Step 1 본문 산문은 위 "변경되는 산문 토큰"에 명시되어 있으므로 byte-identical 검증 acceptance를 두지 않는다.
- Step 5 "Common flow for ssc/dspm lenses" 항목 #5는 위 "변경되는 산문 토큰"에 명시되어 있으므로 byte-identical 검증 acceptance를 두지 않는다.
- 위 토큰 외의 모든 산문은 "보존"으로 처리하고, 신규 acceptance가 해당 산문을 가리키지 않도록 한다.

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Step 1 PICK_MODE 탐지 + Step 2 conflict reject + Step 5 AskUserQuestion 분기 (LENS-03)</name>
  <files>skills/sg-retro/SKILL.md</files>
  <read_first>
    - skills/sg-retro/SKILL.md (전체 — 변경 영역 Step 1, Step 2 smart default 분기, Step 5 시작부 + ANALYZE_LENS_RAN 초기화)
    - .planning/phases/43-pick-display-polish/43-CONTEXT.md (D-01~D-07)
    - CLAUDE.md §"macOS 셸 이식성" + §"사용자 언어 메시지"
  </read_first>
  <action>
**A. Step 1 — PICK_MODE 탐지 블록 신설 (D-01, D-02):**

Step 1의 첫 bash 블록 (현재 lines 28-30):
```bash
set -- $ARGUMENTS
PHASE_RAW="${1:-}"
LENS_RAW="${2:-}"
```
을 다음으로 **교체**한다 (PICK_MODE 탐지 + 토큰 제거 + 정상 positional 파싱):

```bash
set -- $ARGUMENTS

# D-01, D-02: --pick 토큰을 어느 위치에서든 탐지하고 제거 (positional 파싱 보존)
PICK_MODE=false
NEW_ARGS=""
for ARG in "$@"; do
  case "$ARG" in
    --pick)
      PICK_MODE=true
      ;;
    *)
      NEW_ARGS="${NEW_ARGS}${NEW_ARGS:+ }${ARG}"
      ;;
  esac
done
# shellcheck disable=SC2086 — intentional word splitting for positional re-set
set -- $NEW_ARGS
PHASE_RAW="${1:-}"
LENS_RAW="${2:-}"
```

이후 Step 1의 PHASE_RAW empty check / STATE.md fallback / phase validation / PHASE_PAD / PHASE_DIR / EXTRA_LENS_CODES 파싱은 **byte-identical 보존**.

**B. Step 1 본문 산문 갱신 (interfaces "변경되는 산문 토큰" 적용):**

Step 1 첫 문단(line ~24, "Parse `$ARGUMENTS` into `PHASE_RAW` and `LENS_RAW`."로 시작):
- 변경 전: `Parse \`$ARGUMENTS\` into \`PHASE_RAW\` and \`LENS_RAW\`. If \`PHASE_RAW\` is empty, fall back to ...`
- 변경 후: `Parse \`$ARGUMENTS\` into \`PHASE_RAW\` and \`LENS_RAW\`. Detect \`--pick\` token anywhere in the argument list and strip it before positional parsing (D-01, D-02). If \`PHASE_RAW\` is empty, fall back to ...`

(첫 문장 끝에 "Detect ... (D-01, D-02)." 한 문장만 삽입. 나머지 문단 산문은 byte-identical.)

**C. Step 2 — 충돌 reject 분기 신설 (D-03):**

Step 2 lens case 매핑 + VALID_EXTRAS inner case + smart default 분기(현재 lines 80-131)는 **byte-identical 보존**. smart default 분기(`if [ -z "$LENS_CODES_ARRAY" ] && [ -z "$LENS_RAW" ] && [ -z "$EXTRA_LENS_CODES" ]; then LENS_CODES_ARRAY="dspm ssc"; fi`) 바로 **위**에 다음 분기를 추가한다:

```bash
# D-03: --pick + positional lens 충돌 reject (silent override 금지)
if [ "$PICK_MODE" = "true" ] && { [ -n "$LENS_RAW" ] || [ -n "$EXTRA_LENS_CODES" ]; }; then
  printf 'Cannot combine --pick with positional lens argument.\nUse either: sg-retro {phase} {lens...}  (explicit args)\nOr:         sg-retro {phase} --pick     (interactive picker)\n' >&2
  exit 1
fi
```

(분기 위치 critical: smart default 분기 위에 두어야 PICK_MODE+positional 케이스가 smart default보다 먼저 탐지된다. PICK_MODE=true + positional이 들어오면 smart default 진입 조건 `LENS_RAW empty AND EXTRA_LENS_CODES empty`가 false이므로 race는 없지만, 명시적 위치 보장 권장.)

**D. Step 5 — AskUserQuestion 호출 블록 신설 (D-05, D-06):**

Step 5의 `ANALYZE_LENS_RAN=false` 초기화 (현재 lines 192-195) 직후, lens loop 진입 텍스트 직전에 다음 블록을 추가한다:

```bash
# D-05, D-06: --pick 모드 → AskUserQuestion multiSelect 1회 호출
if [ "$PICK_MODE" = "true" ]; then
  # Claude execute-time: invoke AskUserQuestion with the spec below.
  #   header: "Pick Lens"
  #   question: "Which lens(es) do you want to run for Phase ${PHASE_RAW}?"
  #   multiSelect: true
  #   options:
  #     - label "ssc",     description "surface behavior changes — what to start, stop, or continue doing next phase."
  #     - label "dspm",    description "capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase."
  #     - label "analyze", description "scan session transcript for frustration, correction, repetition, and validated-success signals; propose sg-rule drafts."
  # Selected labels → LENS_CODES_ARRAY (space-separated, in user-selected order).
  # 0-selection (cancel / empty) → emit stderr message and silent exit(0):
  #   "--pick cancelled — no lens selected, no retrospective recorded."
  # The exit(0) MUST occur before the lens loop, lessons file creation, and HANDOFF append.
  :  # placeholder — Claude inserts the AskUserQuestion tool call here at execute time
fi
```

(bash 자체는 AskUserQuestion을 호출할 수 없으므로 `:` no-op + 주석 가이드로 표현. Claude가 SKILL.md를 해석 실행할 때 이 placeholder를 AskUserQuestion tool call로 치환한다. 0-selection 시 `exit 0`이 lens loop 진입 **전**에 발생하도록 위치 critical — lens loop는 line ~200의 `for LENS_CODE in $LENS_CODES_ARRAY; do` 이므로 그 **전**에 이 블록이 있어야 함.)

**E. <objective> 산문 갱신 (interfaces "변경되는 산문 토큰" 적용):**

`<objective>` 블록(line 13-15)의 본문에서:
- 변경 전: `... — Start/Stop/Continue (ssc), Decisions/Surprises/Patterns/Mistakes (dspm), Conversation Analyzer (analyze) — via direct arguments, or omit the lens argument to apply the smart default (dspm+ssc).`
- 변경 후: `... — Start/Stop/Continue (ssc), Decisions/Surprises/Patterns/Mistakes (dspm), Conversation Analyzer (analyze) — via direct arguments, the \`--pick\` flag (interactive multiSelect), or omit the lens argument to apply the smart default (dspm+ssc).`

(`via direct arguments,` 뒤에 `the \`--pick\` flag (interactive multiSelect),` 한 절만 삽입. 나머지 문장은 byte-identical.)

**F. macOS 셸 이식성 (CLAUDE.md):**
- `grep -P` 금지. 모든 분기에서 `printf '...' >&2` 패턴만 사용 (다중 줄 `echo` 회피).
- 변수 확장 시 큰따옴표(`"$PICK_MODE"`) 사용.
- `case "$ARG" in` 패턴은 BSD/GNU 양립.
- Step 1 STATE.md parsing 블록은 **절대 손대지 않는다**.

**G. 보존 검증:**
- Step 1 STATE.md parsing 블록, Step 2 lens case 매핑(3-lens + dropped reject + VALID_EXTRAS inner case + smart default 분기), Step 3, 3b, 4 전체, Step 5 sub-block 본문(ssc/dspm/analyze), Step 5 ANALYZE_LENS_RAN flag setter, Step 6 전체(LENS_NAME case, RUN_SUFFIX, LENS_HEADER, redirect block, auto-suggest, HANDOFF append)는 본 Task에서 손대지 않는다. INTENT_LINE + ACTION_ITEMS_PRINTF 가이드 + success_criteria 항목 추가는 Task 2가 담당.
  </action>
  <verify>
    <automated>
# A) PICK_MODE 탐지 블록 신설 검증
grep -q 'PICK_MODE=false' skills/sg-retro/SKILL.md && \
grep -q 'PICK_MODE=true' skills/sg-retro/SKILL.md && \
grep -qE 'for ARG in "\$@"; do' skills/sg-retro/SKILL.md && \
grep -q 'D-01, D-02: --pick 토큰을' skills/sg-retro/SKILL.md && \

# B) Step 1 본문 산문 갱신 (--pick 토큰 언급 추가)
grep -q 'Detect `--pick` token anywhere in the argument list' skills/sg-retro/SKILL.md && \

# C) Step 2 충돌 reject 분기 신설
grep -q 'D-03: --pick + positional lens 충돌 reject' skills/sg-retro/SKILL.md && \
grep -q 'Cannot combine --pick with positional lens argument' skills/sg-retro/SKILL.md && \

# D) Step 5 AskUserQuestion 호출 블록 신설
grep -q 'D-05, D-06: --pick 모드' skills/sg-retro/SKILL.md && \
grep -q 'AskUserQuestion with the spec below' skills/sg-retro/SKILL.md && \
grep -q '\-\-pick cancelled — no lens selected, no retrospective recorded' skills/sg-retro/SKILL.md && \

# E) <objective> 산문 갱신
grep -q '`--pick` flag (interactive multiSelect)' skills/sg-retro/SKILL.md && \

# F) smart default 분기 보존 (Phase 42 carry-forward)
grep -q 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md && \
grep -q 'BEGIN STATE.md Phase parsing block (macOS-compatible grep + sed + awk pipeline)' skills/sg-retro/SKILL.md && \

# G) Phase 42 보존: dropped lens reject 보존
grep -q 'no longer supported (removed in v2.9)' skills/sg-retro/SKILL.md && \

# H) sg-learn 미변경 (D-11 carry-forward)
test "$(git diff --stat HEAD -- skills/sg-learn/SKILL.md 2>/dev/null | wc -l)" = "0" && \

echo "Task 1 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'PICK_MODE=false' skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'PICK_MODE=true' skills/sg-retro/SKILL.md` returns ≥ 1 (탐지 분기 1회 + Step 2 충돌 분기 + Step 5 분기 — case 패턴에서 등장)
    - Source: `grep -c 'Cannot combine --pick with positional lens argument' skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '\-\-pick cancelled — no lens selected' skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '`--pick` flag (interactive multiSelect)' skills/sg-retro/SKILL.md` returns `1` (objective 산문 갱신)
    - Source: `grep -c 'Detect `--pick` token anywhere in the argument list' skills/sg-retro/SKILL.md` returns `1` (Step 1 산문 갱신)
    - Source: `grep -c 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md` returns `1` (Phase 42 smart default 보존)
    - Source: `grep -c 'BEGIN STATE.md Phase parsing block (macOS-compatible grep + sed + awk pipeline)' skills/sg-retro/SKILL.md` returns `1` (STATE.md 블록 보존)
    - Source: `grep -c 'no longer supported (removed in v2.9)' skills/sg-retro/SKILL.md` returns ≥ 1 (Phase 42 dropped reject 보존)
    - CLI: `git diff --stat HEAD -- skills/sg-learn/SKILL.md | wc -l` returns `0` (D-11 sg-learn 미변경)
    - Behavior assertion (Task 3에서 실행): `Skill(skill="sg-retro", args="43 --pick ssc")` → stderr `Cannot combine --pick ...` 출력 + exit 1 + lessons 파일 미생성
  </acceptance_criteria>
  <done>
Step 1의 첫 bash 블록이 PICK_MODE 탐지 + 토큰 제거 + 정상 positional 파싱으로 확장되었다. Step 2 smart default 분기 위에 `--pick` + positional lens 충돌 reject 분기가 신설되었다. Step 5의 `ANALYZE_LENS_RAN=false` 직후에 AskUserQuestion multiSelect 호출 placeholder + 0-selection silent exit 가이드가 삽입되었다. `<objective>` 본문과 Step 1 첫 문단 산문이 `--pick` 진입점을 언급하도록 갱신되었다. 보존 영역(STATE.md 블록, Phase 42 smart default 분기, Phase 42 dropped reject 분기, Step 3/3b/4 전체, Step 5 sub-block, Step 6 전체)은 byte-identical로 유지되었다. sg-learn은 변경되지 않았다.
  </done>
</task>

<task type="auto">
  <name>Task 2: Step 6 INTENT_LINE case + body printf + ACTION_ITEMS_PRINTF 가이드 + success_criteria 추가 (DISPLAY-01, DISPLAY-02)</name>
  <files>skills/sg-retro/SKILL.md</files>
  <read_first>
    - skills/sg-retro/SKILL.md (Task 1 적용 후 — Step 5 Common flow 항목 #5, Step 6 LENS_NAME case + redirect block 본문, <success_criteria> 11개 항목)
    - .planning/phases/43-pick-display-polish/43-CONTEXT.md (D-08~D-15)
    - .planning/lessons/42-2026-05-30.md (DISPLAY-01 적용 전 baseline — P1 평문 행)
  </read_first>
  <action>
**A. Step 6 — INTENT_LINE case 매핑 신설 (D-12, D-14):**

Step 6의 LENS_NAME case (현재 lines 263-267)는 **byte-identical 보존**. 그 **직후**에 INTENT_LINE case를 추가한다:

```bash
case "$LENS_CODE" in
  ssc)     LENS_NAME="Start/Stop/Continue" ;;
  dspm)    LENS_NAME="Decisions/Surprises/Patterns/Mistakes" ;;
  analyze) LENS_NAME="Conversation Analyzer" ;;
esac

# D-12, D-14: lens intent line (static, English, italic single-line — emitted after _Captured: line)
case "$LENS_CODE" in
  ssc)     INTENT_LINE='_Intent: surface behavior changes — what to start, stop, or continue doing next phase._' ;;
  dspm)    INTENT_LINE='_Intent: capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase._' ;;
  analyze) INTENT_LINE='_Intent: scan session transcript for frustration, correction, repetition, and validated-success signals; propose sg-rule drafts._' ;;
esac
```

(2-space 들여쓰기, `analyze` 7자 패딩 정렬은 LENS_NAME case와 동일. em-dash `—` U+2014 사용 — ASCII `--` 아님. single-quote `'...'` 사용 — bash expansion 방지 + 본문에 underscore 보존.)

**B. Step 6 — single redirect block 내부 INTENT_LINE printf 삽입 (D-13, D-14, D-21 보존):**

현재 (line ~311-313):
```bash
{
  printf '%s\n' "$LENS_HEADER"
  printf '_Captured: %s_\n\n' "$NOW_ISO"
  # BODY_PRINTF — Claude inserts subheading + bullet `printf` lines here.
```
변경 후:
```bash
{
  printf '%s\n' "$LENS_HEADER"
  printf '_Captured: %s_\n' "$NOW_ISO"
  printf '%s\n\n' "$INTENT_LINE"
  # BODY_PRINTF — Claude inserts subheading + bullet `printf` lines here.
```

변경 사항:
- 기존 `_Captured: %s_\n\n` (두 개의 `\n`)을 `_Captured: %s_\n` (한 개의 `\n`)으로 축소
- 신규 `printf '%s\n\n' "$INTENT_LINE"` 한 줄 삽입

결과 파일 형태:
```
## Lens: Start/Stop/Continue
_Captured: 2026-06-XXTXX:XX:XXZ_
_Intent: surface behavior changes — what to start, stop, or continue doing next phase._

### Start
...
```

(빈 줄 총 개수 보존: 기존 `_Captured` 다음 빈 줄 1개 → 변경 후 `_Intent` 다음 빈 줄 1개. 단일 `>>` redirect block 보존 — 별도 redirect block 신설 금지. D-21.)

**C. Step 6 — ACTION_ITEMS_PRINTF 주석 가이드 갱신 (D-08, D-09, D-11):**

현재 (line ~325-326):
```
# ACTION_ITEMS_PRINTF — Claude inserts row `printf` lines here, e.g.:
#   printf '| P1 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"
```
변경 후:
```
# ACTION_ITEMS_PRINTF — Claude inserts row `printf` lines here.
# DISPLAY-01 (D-08, D-09): P1 행은 priority 셀에 `🔴 P1` prefix로 emit. P2/P3는 prefix 없음.
#   printf '| 🔴 P1 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"   # P1 only
#   printf '| P2 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"      # P2 unchanged
#   printf '| P3 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"      # P3 unchanged
```

(주석 갱신 — 실행 동작 변경 없음. Claude가 execute 시점에 이 가이드를 읽고 printf 라인 생성. 기존 BODY_PRINTF 주석 가이드와 동일 패턴. 본 갱신은 lessons_ranker.cjs 호환성과 무관 — ranker는 Action Items priority 셀을 파싱하지 않음, lessons_ranker.cjs 분석 결과 카테고리 헤더 + `(high|medium|low)` 시그널만 사용.)

**D. Step 5 "Common flow for ssc/dspm lenses" 항목 #5 산문 갱신 (interfaces "변경되는 산문 토큰" 적용):**

Step 5의 numbered list 항목 #5 (line ~214):
- 변경 전: `Assemble the final lens section (header + \`_Captured: {NOW_ISO}_\` + subheadings + Action Items) and append to \`${LESSONS_FILE}\` ...`
- 변경 후: `Assemble the final lens section (header + \`_Captured: {NOW_ISO}_\` + \`_Intent: ..._\` + subheadings + Action Items) and append to \`${LESSONS_FILE}\` ...`

(`_Captured: {NOW_ISO}_` 뒤에 `+ \`_Intent: ..._\`` 한 토큰만 삽입. 나머지 문장은 byte-identical.)

**E. `<success_criteria>` 블록 — 신규 항목 3개 추가 (#12, #13, #14):**

기존 11개 항목(#1~#11)은 **byte-identical 보존**. 항목 #11 다음에 새 항목 3개를 추가한다:

```
12. The `--pick` flag (long form, lowercase, hyphenated) is detected anywhere in the argument list and triggers an interactive lens selection via AskUserQuestion multiSelect with exactly three options (ssc/dspm/analyze) — called exactly once per invocation (LENS-03, D-01, D-02, D-05). Combining `--pick` with a positional lens argument (e.g. `43 ssc --pick`) emits a stderr conflict error and exits 1 without invoking AskUserQuestion or creating a lessons file (D-03). 0-selection silently exits 0 with a `--pick cancelled` stderr message and creates no lessons file and no HANDOFF row (D-06).
13. Lessons file `### Action Items` rows whose priority is P1 are emitted with a `🔴 P1` prefix in the priority cell; P2 and P3 rows have no prefix (DISPLAY-01, D-08, D-09). The single 3-column table schema (`priority | item | next step`) is preserved.
14. Each lens section emits an italic single-line lens intent statement directly after the `_Captured: {ISO}_` line, sourced from the static `INTENT_LINE` case mapping in Step 6 (one fixed English sentence per lens — ssc/dspm/analyze) (DISPLAY-02, D-12, D-13, D-14, D-15).
```

(번호는 12/13/14. 기존 11개 항목 본문은 변경하지 않음. 신규 항목은 LENS-03/DISPLAY-01/DISPLAY-02 + 관련 D-xx 결정 ID를 명시.)

**F. 손대지 말 것:**
- Step 1, Step 2 (Task 1 변경분 제외), Step 3, Step 3b, Step 4 전체
- Step 5 sub-block 본문 + ANALYZE_LENS_RAN flag setter
- Step 6 LENS_NAME case (위 INTENT_LINE 추가 **이전**의 case는 byte-identical 보존)
- Step 6 RUN_SUFFIX 분기, LENS_HEADER 조립, 파일 존재 체크, 신규 파일 헤더 작성 (line 270-294)
- Step 6 BODY_PRINTF placeholder 주석 본문 (위치만 그대로, ACTION_ITEMS_PRINTF 갱신과 별개)
- Step 6 single `>>` redirect block의 단일성 — 새 redirect block 신설 금지
- Step 6 auto-suggest 블록 + HANDOFF append 블록 (line 335-365)
- `<success_criteria>` 항목 #1~#11 본문 — byte-identical
- `<language>` 블록, frontmatter `name`/`description` 필드
- `<execution_context>` 블록
  </action>
  <verify>
    <automated>
# A) INTENT_LINE case 매핑 신설 검증
grep -q 'D-12, D-14: lens intent line' skills/sg-retro/SKILL.md && \
grep -q "ssc)     INTENT_LINE='_Intent: surface behavior changes" skills/sg-retro/SKILL.md && \
grep -q "dspm)    INTENT_LINE='_Intent: capture technical decisions" skills/sg-retro/SKILL.md && \
grep -q "analyze) INTENT_LINE='_Intent: scan session transcript" skills/sg-retro/SKILL.md && \

# B) LENS_NAME case 보존 (Phase 42 carry-forward)
grep -q 'ssc)     LENS_NAME="Start/Stop/Continue"' skills/sg-retro/SKILL.md && \
grep -q 'dspm)    LENS_NAME="Decisions/Surprises/Patterns/Mistakes"' skills/sg-retro/SKILL.md && \
grep -q 'analyze) LENS_NAME="Conversation Analyzer"' skills/sg-retro/SKILL.md && \

# C) Step 6 single redirect block 내부 INTENT_LINE printf 삽입
grep -q "printf '%s\\\\n\\\\n' \"\$INTENT_LINE\"" skills/sg-retro/SKILL.md && \

# D) ACTION_ITEMS_PRINTF 가이드 갱신 — DISPLAY-01 명시
grep -q 'DISPLAY-01 (D-08, D-09): P1 행은 priority 셀에' skills/sg-retro/SKILL.md && \
grep -q "printf '| 🔴 P1 | %s | %s |" skills/sg-retro/SKILL.md && \

# E) Step 5 "Common flow" 항목 #5 산문 갱신 (intent line 명시)
grep -q '_Captured: {NOW_ISO}_` + `_Intent: ..._`' skills/sg-retro/SKILL.md && \

# F) success_criteria 신규 3개 항목 — 키워드 존재
grep -q 'LENS-03, D-01, D-02, D-05' skills/sg-retro/SKILL.md && \
grep -q 'DISPLAY-01, D-08, D-09' skills/sg-retro/SKILL.md && \
grep -q 'DISPLAY-02, D-12, D-13, D-14, D-15' skills/sg-retro/SKILL.md && \

# G) success_criteria 블록 단일성 보존 (열고 닫는 태그 1쌍)
test "$(grep -c '<success_criteria>' skills/sg-retro/SKILL.md)" = "1" && \
test "$(grep -c '</success_criteria>' skills/sg-retro/SKILL.md)" = "1" && \

# H) Step 6 single >> redirect block 단일성 (별도 redirect 신설 금지)
test "$(grep -c '} >> \"\$LESSONS_FILE\"' skills/sg-retro/SKILL.md)" = "1" && \

# I) D-21 single redirect block 내부에 INTENT_LINE printf가 있는지 (BODY_PRINTF 가이드 위)
grep -B 2 'BODY_PRINTF — Claude inserts' skills/sg-retro/SKILL.md | grep -q "INTENT_LINE" && \

# J) auto-suggest 블록 + HANDOFF append 블록 보존
grep -q 'sg-rule drafts were included in the Conversation Analyzer' skills/sg-retro/SKILL.md && \
grep -q 'HANDOFF.md record — after all lenses complete' skills/sg-retro/SKILL.md && \

echo "Task 2 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "INTENT_LINE='_Intent:" skills/sg-retro/SKILL.md` returns `3` (3개 lens 각각 1줄)
    - Source: `grep -c "D-12, D-14: lens intent line" skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c "printf '%s\\\\n\\\\n' \"\$INTENT_LINE\"" skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'DISPLAY-01 (D-08, D-09): P1 행은 priority 셀에' skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c "printf '| 🔴 P1 | %s | %s |" skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '_Intent: ..._' skills/sg-retro/SKILL.md` returns ≥ 1 (Step 5 산문 갱신 — Common flow 항목 #5)
    - Source: `grep -c 'LENS-03, D-01, D-02, D-05' skills/sg-retro/SKILL.md` returns `1` (success_criteria #12)
    - Source: `grep -c 'DISPLAY-01, D-08, D-09' skills/sg-retro/SKILL.md` returns `1` (success_criteria #13)
    - Source: `grep -c 'DISPLAY-02, D-12, D-13, D-14, D-15' skills/sg-retro/SKILL.md` returns `1` (success_criteria #14)
    - Source: `grep -c '<success_criteria>' skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '</success_criteria>' skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '} >> "\$LESSONS_FILE"' skills/sg-retro/SKILL.md` returns `1` (D-21 single redirect block 보존)
    - Source: `grep -c 'sg-rule drafts were included in the Conversation Analyzer' skills/sg-retro/SKILL.md` returns `1` (auto-suggest 보존)
    - Source: `grep -c 'HANDOFF.md record — after all lenses complete' skills/sg-retro/SKILL.md` returns `1` (HANDOFF append 보존)
    - Source: `grep -c 'ssc)     LENS_NAME="Start/Stop/Continue"' skills/sg-retro/SKILL.md` returns `1` (LENS_NAME case 보존)
    - Source: `grep -c 'analyze) LENS_NAME="Conversation Analyzer"' skills/sg-retro/SKILL.md` returns `1` (LENS_NAME case 보존)
  </acceptance_criteria>
  <done>
Step 6에 INTENT_LINE case 매핑이 LENS_NAME case 직후에 추가되었고, single `>>` redirect block 내부의 `_Captured:` printf 다음에 INTENT_LINE printf 한 줄이 삽입되었다. ACTION_ITEMS_PRINTF placeholder 주석이 `🔴 P1` prefix 가이드로 확장되었다. Step 5 "Common flow" 항목 #5 산문에 `_Intent: ..._` 토큰이 명시되었다. `<success_criteria>` 블록에 LENS-03/DISPLAY-01/DISPLAY-02를 각각 명시하는 항목 3개(#12/#13/#14)가 추가되었고, 기존 11개 항목 본문은 byte-identical로 보존되었다. Step 6 single redirect block의 단일성(D-21)이 유지되었고, auto-suggest + HANDOFF append 블록은 변경되지 않았다.
  </done>
</task>

<task type="auto">
  <name>Task 3: D-18 9개 검증 시나리오 정적·동적 점검 + pairwise diff 사전 점검</name>
  <files></files>
  <read_first>
    - skills/sg-retro/SKILL.md (Task 1+2 적용 후 상태)
    - .planning/phases/43-pick-display-polish/43-CONTEXT.md (D-18 9개 시나리오)
  </read_first>
  <action>
Task 1+2 변경이 D-18의 9개 시나리오를 충족하는지 정적·동적 검증한다. 본 plan은 `skills/sg-retro/SKILL.md`만 수정하므로 시나리오 9(pairwise diff)는 사전 점검만 수행하고 최종 검증은 plan 43-02 완료 후 별도로 실행한다.

**시나리오 1 — `--pick` 정상 진입 (D-05):**
`Skill(skill="sg-retro", args="43 --pick")` → AskUserQuestion 1회 호출 + 옵션 3개 multiSelect.
- 정적 검증: PICK_MODE 탐지 블록 존재, Step 5 AskUserQuestion 가이드 블록 존재, 0-selection silent exit 가이드 존재.
- 동적 검증(가능 시): 실제 호출 → AskUserQuestion 1회 발생 → 사용자가 `ssc dspm` 선택 → lessons 파일에 2개 lens section append + 각각 INTENT_LINE 존재.

**시나리오 2 — `--pick` + positional 충돌 (D-03):**
`Skill(skill="sg-retro", args="43 --pick ssc")` → stderr 에러 + exit 1, lessons 파일 미생성, AskUserQuestion 미호출.
- 정적 검증:
  ```bash
  grep -B 1 -A 3 'D-03: --pick + positional' skills/sg-retro/SKILL.md | grep -q 'exit 1'
  ```

**시나리오 3 — `--pick` 위치 무관 (D-02):**
`Skill(skill="sg-retro", args="43 ssc --pick")` → 동일 충돌 에러 + exit 1.
- 정적 검증: PICK_MODE 탐지 분기가 모든 위치의 `--pick`를 탐지하는지 확인 (`for ARG in "$@"` 루프 구조).

**시나리오 4 — smart default 보존 (D-04, no regression):**
`Skill(skill="sg-retro", args="43")` → AskUserQuestion 없이 `dspm ssc` 자동 진입.
- 정적 검증: smart default 분기 byte-identical 보존 확인.
  ```bash
  grep -A 3 'D-02.*smart default' skills/sg-retro/SKILL.md | grep -q 'LENS_CODES_ARRAY="dspm ssc"'
  ```

**시나리오 5 — sg-learn pass-through (D-11 carry-forward):**
`Skill(skill="sg-learn", args="43 --pick")` → sg-retro forward → `--pick` 처리.
- 정적 검증: `skills/sg-learn/SKILL.md` 미변경.
  ```bash
  test "$(git diff --stat HEAD -- skills/sg-learn/SKILL.md | wc -l)" = "0"
  ```

**시나리오 6 — 0-selection silent exit (D-06):**
`Skill(skill="sg-retro", args="43 --pick")` + 사용자 0개 선택 → silent exit(0), stderr 안내 메시지, lessons 미생성.
- 정적 검증: `--pick cancelled — no lens selected` 가이드 문자열 존재.
  ```bash
  grep -q '\-\-pick cancelled — no lens selected, no retrospective recorded' skills/sg-retro/SKILL.md
  ```

**시나리오 7 — DISPLAY-01 P1 emoji prefix:**
lessons 파일의 모든 P1 행이 `| 🔴 P1 |`로 시작하는지 grep 확인. P2/P3는 prefix 없음.
- 정적 검증: ACTION_ITEMS_PRINTF 주석 가이드가 `🔴 P1` 패턴을 P1 행에만 적용하도록 명시되어 있는지.
  ```bash
  grep -q "printf '| 🔴 P1 | %s | %s |" skills/sg-retro/SKILL.md && \
  grep -q "printf '| P2 | %s | %s |" skills/sg-retro/SKILL.md
  ```
- 동적 검증: 실제 lessons 파일 생성 후 — `grep -E '^\| 🔴 P1 \|' .planning/lessons/43-{YYYY-MM-DD}.md | wc -l` ≥ 1, `grep -E '^\| P1 \|' .planning/lessons/43-{YYYY-MM-DD}.md | wc -l` = 0.

**시나리오 8 — DISPLAY-02 intent line:**
모든 lens section에 `_Intent: ..._` italic 줄이 `_Captured:_` 바로 다음 줄에 존재.
- 정적 검증:
  ```bash
  grep -B 2 'BODY_PRINTF — Claude inserts' skills/sg-retro/SKILL.md | grep -q 'INTENT_LINE' && \
  test "$(grep -c "INTENT_LINE='_Intent:" skills/sg-retro/SKILL.md)" = "3"
  ```
- 동적 검증: 실제 lessons 파일 생성 후 — `awk '/^_Captured:/{c=1;next} c{print;c=0}' .planning/lessons/43-{YYYY-MM-DD}.md | grep -c '^_Intent:'`가 lens 개수와 일치.

**시나리오 9 — pairwise diff (D-16, plan 43-02 완료 후 별도 검증):**
`skills/sg-retro/SKILL.md`와 `.agents/skills/sg-retro/SKILL.md`의 의미적 동등성. 본 plan에서는 의미 변경 단위(`--pick` 탐지·충돌 reject·AskUserQuestion 가이드·INTENT_LINE case·INTENT_LINE printf·ACTION_ITEMS_PRINTF 가이드·success_criteria 신규 항목)를 enumerate하고, plan 43-02 완료 후 두 파일에 모두 적용되었는지 확인. 본 task에서는 `skills/` 측 변경만 검증.

**lessons_ranker.cjs 호환성 확인 (CONTEXT open ambiguity #2):**
DISPLAY-01의 `🔴 P1` prefix가 lessons_ranker.cjs의 priority 추출에 영향을 주는지 확인.
- 정적 검증 (`hooks/lessons_ranker.cjs` 읽기 결과 기반):
  - `_extractSeverity` 함수는 `(high|medium|low)` 또는 `severity:` 시그널만 추출 (priority `P1`/`P2`/`P3` 토큰을 파싱하지 않음).
  - `parseLessonsFiles` 함수는 카테고리 헤더(`## `) 기준 split + 첫 non-empty body line을 pattern으로 사용 (Action Items 테이블 셀을 파싱하지 않음).
  - 결론: `🔴 P1` prefix 추가는 ranker 동작에 영향 없음. 별도 ranker 갱신 plan 불필요 (Option B 채택).
- 검증 명령 (선택):
  ```bash
  # ranker가 priority 토큰을 직접 파싱하는지 확인
  grep -E 'P1|P2|P3|priority' hooks/lessons_ranker.cjs | wc -l
  # 결과가 0이면 영향 없음 확정
  ```
  </action>
  <verify>
    <automated>
# 시나리오 1: --pick 정상 진입 가이드 존재
grep -q 'PICK_MODE=true' skills/sg-retro/SKILL.md && \
grep -q 'AskUserQuestion with the spec below' skills/sg-retro/SKILL.md && \
grep -q '\-\-pick cancelled — no lens selected, no retrospective recorded' skills/sg-retro/SKILL.md && \

# 시나리오 2: --pick + positional 충돌 reject 분기
grep -B 1 -A 3 'D-03: --pick + positional' skills/sg-retro/SKILL.md | grep -q 'exit 1' && \

# 시나리오 3: --pick 위치 무관 (for ARG 루프)
grep -qE 'for ARG in "\$@"; do' skills/sg-retro/SKILL.md && \

# 시나리오 4: smart default 보존 (no regression)
grep -A 3 'D-02.*smart default' skills/sg-retro/SKILL.md | grep -q 'LENS_CODES_ARRAY="dspm ssc"' && \

# 시나리오 5: sg-learn 미변경 (D-11)
test "$(git diff --stat HEAD -- skills/sg-learn/SKILL.md 2>/dev/null | wc -l)" = "0" && \

# 시나리오 6: 0-selection silent exit 가이드
grep -q '\-\-pick cancelled — no lens selected' skills/sg-retro/SKILL.md && \

# 시나리오 7: DISPLAY-01 P1 emoji prefix 가이드
grep -q "printf '| 🔴 P1 | %s | %s |" skills/sg-retro/SKILL.md && \
grep -q "printf '| P2 | %s | %s |" skills/sg-retro/SKILL.md && \

# 시나리오 8: DISPLAY-02 INTENT_LINE 3개 + BODY_PRINTF 가이드 위에 위치
test "$(grep -c "INTENT_LINE='_Intent:" skills/sg-retro/SKILL.md)" = "3" && \
grep -B 2 'BODY_PRINTF — Claude inserts' skills/sg-retro/SKILL.md | grep -q 'INTENT_LINE' && \

# lessons_ranker.cjs 호환성: priority 토큰 미파싱 확인
test "$(grep -cE '(\\| P1 \\||"P1"|P\\[123\\])' hooks/lessons_ranker.cjs 2>/dev/null)" = "0" && \

# success_criteria 14개 항목 (#1~#11 보존 + #12/#13/#14 신규)
test "$(grep -cE '^[0-9]+\. ' skills/sg-retro/SKILL.md)" -ge 14 && \

echo "Task 3 PASS — D-18 scenarios 1-8 verified statically + lessons_ranker.cjs compatibility confirmed"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'PICK_MODE=true' skills/sg-retro/SKILL.md` returns ≥ 2 (탐지 분기 + Step 5 분기 + Step 2 충돌 분기)
    - Source: `grep -B 1 -A 3 'D-03: --pick + positional' skills/sg-retro/SKILL.md | grep -c 'exit 1'` returns `1`
    - Source: `grep -E 'for ARG in "\$@"; do' skills/sg-retro/SKILL.md` returns ≥ 1 (PICK_MODE 탐지 루프)
    - Source: `grep -A 3 'D-02.*smart default' skills/sg-retro/SKILL.md | grep -c 'LENS_CODES_ARRAY="dspm ssc"'` returns `1` (Phase 42 smart default 보존)
    - CLI: `git diff --stat HEAD -- skills/sg-learn/SKILL.md | wc -l` returns `0` (D-11 sg-learn 미변경)
    - Source: `grep -c '\-\-pick cancelled — no lens selected' skills/sg-retro/SKILL.md` returns `1` (0-selection 가이드)
    - Source: `grep -c "printf '| 🔴 P1 | %s | %s |" skills/sg-retro/SKILL.md` returns `1` (DISPLAY-01 가이드)
    - Source: `grep -c "INTENT_LINE='_Intent:" skills/sg-retro/SKILL.md` returns `3` (DISPLAY-02 3개 lens 매핑)
    - CLI: `grep -cE '(\\| P1 \\||"P1"|P\\[123\\])' hooks/lessons_ranker.cjs` returns `0` (lessons_ranker.cjs는 priority 토큰을 파싱하지 않음 — Option B 정당화)
    - Behavior assertion (pairwise diff): plan 43-02 완료 후 두 파일에 의미 단위(`--pick` 탐지·충돌 reject·AskUserQuestion 가이드·INTENT_LINE case·INTENT_LINE printf·ACTION_ITEMS_PRINTF 가이드)가 모두 적용되었는지 별도 확인 (시나리오 9)
  </acceptance_criteria>
  <done>
D-18의 시나리오 1-8이 정적 검증으로 통과한다. `skills/sg-learn/SKILL.md`는 변경되지 않았다(D-11 보존). `hooks/lessons_ranker.cjs`는 priority 토큰을 파싱하지 않으므로 DISPLAY-01의 `🔴 P1` prefix가 ranker에 영향을 주지 않는다(별도 plan 불필요, Option B 확정). 시나리오 9(pairwise diff)는 plan 43-02 완료 후 별도 검증한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 인자 → Bash 변수 | `$ARGUMENTS` / `$LENS_RAW` / `$EXTRA_LENS_CODES`는 Skill 호출 시 인자에서 유래. 내부 신뢰 영역 (Claude Code 런타임이 호출). |
| AskUserQuestion 응답 → LENS_CODES_ARRAY | AskUserQuestion이 enum-bound multiSelect로 옵션을 강제하므로 사용자 응답은 `ssc`/`dspm`/`analyze` 중 하나로 보장됨. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | (N/A) | skills/sg-retro/SKILL.md text edits | accept | Threat model: N/A for SKILL.md text edits. Phase 42 T-42-01과 동일 — 새로운 외부 입력 surface 없음. `--pick` 토큰은 기존 `$ARGUMENTS` 파이프라인에 합류하며, AskUserQuestion 응답은 Claude Code 런타임이 enum으로 검증. 인증·자격증명·PII surface 없음. |
</threat_model>

<verification_steps>
## D-18 검증 시나리오 (Task 3에서 실행)

1. **`--pick` 정상 진입 (D-05)** — `Skill(skill="sg-retro", args="43 --pick")`: AskUserQuestion 1회 호출, multiSelect 3개 옵션(ssc/dspm/analyze), 사용자가 `ssc dspm` 선택 → lessons 파일에 2개 lens section append, 각각 INTENT_LINE 포함.
2. **`--pick` + positional 충돌 (D-03)** — `Skill(skill="sg-retro", args="43 --pick ssc")`: stderr `Cannot combine --pick with positional lens argument.` 출력, exit 1, lessons 파일 미생성, AskUserQuestion 미호출.
3. **`--pick` 위치 무관 (D-02)** — `Skill(skill="sg-retro", args="43 ssc --pick")`: 위와 동일(`--pick` 위치 무관).
4. **smart default 보존 (D-04)** — `Skill(skill="sg-retro", args="43")`: AskUserQuestion 없이 `dspm ssc` 자동 진입 (behavior preserved, no regression).
5. **sg-learn pass-through (D-11)** — `Skill(skill="sg-learn", args="43 --pick")`: sg-learn은 변경 없이 forward → sg-retro에서 `--pick` 처리. `skills/sg-learn/SKILL.md` 미변경 확인.
6. **0-selection silent exit (D-06)** — `Skill(skill="sg-retro", args="43 --pick")` + 사용자 0개 선택: stderr `--pick cancelled — no lens selected, no retrospective recorded.`, exit 0, lessons 파일 미생성, HANDOFF append 없음.
7. **DISPLAY-01 P1 emoji prefix (D-08, D-09)** — lessons 파일의 모든 P1 행이 `| 🔴 P1 |`로 시작. P2/P3는 prefix 없음. 단일 3-컬럼 테이블 스키마 보존.
8. **DISPLAY-02 intent line (D-12~D-15)** — 모든 lens section에 `_Intent: ..._` italic 줄이 `_Captured:_` 바로 다음 줄에 존재. 3개 lens 각각 고정 영문 문구.
9. **pairwise diff (D-16, 플랜 43-02 완료 후)** — `diff skills/sg-retro/SKILL.md .agents/skills/sg-retro/SKILL.md` 비교 시 의미적 동등(파일 헤더·`<constraints>` 블록·`<lens_templates>` 블록·argument-hint frontmatter만 차이).

## lessons_ranker.cjs 호환성 (CONTEXT open ambiguity #2 — Task 3 정적 확인)

`hooks/lessons_ranker.cjs`는 `_extractSeverity`에서 `(high|medium|low)`만, `parseLessonsFiles`에서 `## ` 카테고리 헤더 + 첫 non-empty body line만 파싱한다. Action Items 테이블의 priority 셀(`P1`/`P2`/`P3` 토큰)은 파싱하지 않으므로 DISPLAY-01의 `🔴 P1` prefix는 ranker 동작에 영향 없음. 별도 ranker 갱신 plan **불필요** (Option B 확정).
</verification_steps>

<success_criteria>
- `skills/sg-retro/SKILL.md`의 Step 1 첫 bash 블록이 PICK_MODE 탐지(`for ARG in "$@"` 루프) + `--pick` 토큰 제거 + 정상 positional 파싱으로 확장되었다 (D-01, D-02).
- Step 2 smart default 분기 **위**에 `--pick` + positional lens 충돌 reject 분기(`Cannot combine --pick with positional lens argument.` + exit 1)가 신설되었다 (D-03).
- Step 5의 `ANALYZE_LENS_RAN=false` 초기화 직후에 AskUserQuestion multiSelect 호출 placeholder(3개 옵션 ssc/dspm/analyze + 0-selection silent exit 가이드)가 삽입되었다 (D-05, D-06).
- Step 6의 LENS_NAME case 직후에 INTENT_LINE case(3개 lens 각각 고정 영문 italic single-line)가 추가되었다 (D-12, D-14).
- Step 6의 single `>>` redirect block 내부에서 `_Captured:` printf 직후에 INTENT_LINE printf 한 줄이 삽입되었다 (D-13, D-21 보존).
- Step 6의 ACTION_ITEMS_PRINTF placeholder 주석이 `🔴 P1` prefix 가이드로 확장되었다. P2/P3는 prefix 없음 명시 (D-08, D-09, D-11).
- `<success_criteria>` 블록에 신규 항목 3개(#12 `--pick` + AskUserQuestion 1회, #13 P1 emoji prefix, #14 lens intent line)가 추가되었고, 기존 11개 항목(#1~#11) 본문은 byte-identical로 보존되었다.
- `<objective>` 본문에 `--pick` flag 진입점 언급이 추가되었고, Step 1 첫 문단 산문에 `--pick` 토큰 탐지 안내가 추가되었다. Step 5 "Common flow" 항목 #5 산문에 `_Intent: ..._` 토큰이 명시되었다 (Phase 42 retro P1 #1 — prose drift 방지 적용).
- Phase 42 carry-forward 영역(Step 1 STATE.md parsing 블록, Step 2 lens case 매핑 + dropped reject 분기 + VALID_EXTRAS inner case + smart default 분기, Step 3/3b/4 전체, Step 5 sub-block 본문 + DSPM explicit guard + ANALYZE_LENS_RAN flag, Step 6 LENS_NAME case + RUN_SUFFIX + LENS_HEADER + 신규 파일 헤더 + auto-suggest + HANDOFF append 블록)은 byte-identical로 보존되었다.
- `skills/sg-learn/SKILL.md`는 변경되지 않았다 (D-11 thin pass-through 보존).
- `hooks/lessons_ranker.cjs`는 priority 토큰을 파싱하지 않으므로 DISPLAY-01의 `🔴 P1` prefix가 ranker 동작에 영향을 주지 않는다 (Option B — 별도 ranker plan 불필요).
- frontmatter `description` 필드는 본 phase에서 변경하지 않는다 (Phase 44 DOC-01 영역).
</success_criteria>

<output>
Create `.planning/phases/43-pick-display-polish/43-01-SUMMARY.md` when done.

**중요 (D-16 + Phase 42 D-10 carry-forward staging 제약):** 본 plan(43-01)과 plan 43-02는 **동일 commit에 staged** 되어야 한다. 단독 commit 금지. plan 43-02가 완료될 때까지 commit 보류하거나, 두 plan을 별도 커밋으로 만들지 않고 마지막에 합쳐 staging한다. 실행자는 자신의 task가 끝나도 `git commit`을 단독으로 호출하지 말고, phase 전체가 완료된 후 commit하도록 한다. (참고: GSD execute-plan은 phase 단위 commit을 사용하므로 자연스럽게 충족됨.)
</output>
