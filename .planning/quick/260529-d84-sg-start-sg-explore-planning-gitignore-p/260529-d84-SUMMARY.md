---
quick_id: 260529-d84
date: 2026-05-29
status: complete
---

# Quick Task Summary — 260529-d84

## 변경 내용

3개 SKILL.md 파일의 `<process>` 블록 맨 앞에 **Step 0** 삽입.

| 파일 | 변경 내용 |
|------|-----------|
| `skills/sg-start/SKILL.md` | Step 0 — `.planning/` gitignore 멱등 추가 |
| `.agents/skills/sg-start/SKILL.md` | 동일 (pairwise) |
| `skills/sg-explore/SKILL.md` | Step 0 삽입 + success_criteria 항목 2 추가 |

## Step 0 로직

```bash
if ! grep -qxF '.planning/' .gitignore 2>/dev/null; then
  printf '\n.planning/\n!.planning/codebase/\n' >> .gitignore
fi
```

- `.planning/` 항목이 없을 때만 추가 (멱등)
- `.planning/codebase/` 는 `!.planning/codebase/` 예외로 git 추적 유지
- `grep -P` 금지 → `-xF` 사용 (macOS 호환)
