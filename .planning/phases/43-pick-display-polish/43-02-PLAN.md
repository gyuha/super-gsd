---
phase: 43-pick-display-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/sg-retro/SKILL.md
autonomous: true
requirements: [LENS-03, DISPLAY-01, DISPLAY-02]
tags: [sg-retro, pick, display, agents, codex, gemini, pairwise]

must_haves:
  truths:
    - "`.agents/skills/sg-retro/SKILL.md`가 plan 43-01과 의미적으로 동등하게 갱신된다 (구조적 차이는 <constraints> + <lens_templates> + argument-hint frontmatter 영역만 허용)"
    - "Codex/Gemini/Antigravity 환경에서 `--pick` 호출 시 AskUserQuestion 미지원으로 인해 graceful early-exit + stderr 안내 메시지가 emit되고 lessons 파일이 생성되지 않는다 (D-17 fallback 정책)"
    - "`--pick` 없이 호출 시 Phase 42 smart default(`dspm ssc`)는 변경 없이 동일하게 동작한다 (no regression)"
    - "`--pick` + positional lens 충돌은 plan 43-01과 동일하게 stderr 에러 + exit 1로 거부된다"
    - "lessons 파일의 모든 P1 행이 priority 셀에서 `🔴 P1`로 시작한다 (P2/P3는 prefix 없음)"
    - "각 lens section의 `_Captured: {ISO}_` 줄 바로 다음 줄에 `_Intent: ..._` italic single-line이 emit된다"
    - "<constraints> 블록의 'AskUserQuestion not supported' 문구가 `--pick` 모드의 graceful exit 동작을 반영하도록 갱신된다 (D-17 옵션 b)"
    - "argument-hint frontmatter에 `--pick` 사용법이 추가된다"
  artifacts:
    - path: ".agents/skills/sg-retro/SKILL.md"
      provides: "--pick graceful fallback (Codex/Gemini/Antigravity) + DISPLAY-01 P1 emoji prefix + DISPLAY-02 lens intent line"
      contains: 'PICK_MODE=true'
  key_links:
    - from: ".agents/skills/sg-retro/SKILL.md Step 1 PICK_MODE detection"
      to: "Step 2 conflict reject + Step 5 graceful exit"
      via: "PICK_MODE 변수"
      pattern: "PICK_MODE"
    - from: ".agents/skills/sg-retro/SKILL.md Step 6 LENS_NAME case"
      to: "Step 6 INTENT_LINE case + body printf emit"
      via: "LENS_CODE 변수"
      pattern: "INTENT_LINE"
    - from: ".agents/skills/sg-retro/SKILL.md"
      to: "skills/sg-retro/SKILL.md (pairwise mirror)"
      via: "D-16 동일 commit staging + 의미적 동등"
      pattern: "PICK_MODE"
---

<objective>
`.agents/skills/sg-retro/SKILL.md`를 surgical edit 하여 plan 43-01이 `skills/sg-retro/SKILL.md`에 적용한 것과 **의미적으로 동등한** 변경을 적용한다. 두 파일은 구조적 차이가 있으므로(AskUserQuestion 가용성, `<constraints>` 블록, `<lens_templates>` 블록, argument-hint frontmatter) 단순 `cp` 미러링이 아니라 **동일 의미 단위**의 변경을 각 파일 구조에 맞게 적용한다.

Phase 43의 핵심 변경 + `.agents/` 변종 처리:

1. **LENS-03 `--pick` 명시적 lens 선택 UI + graceful fallback (D-01~D-07, D-17)** — Step 1에 PICK_MODE 탐지 분기를, Step 2에 충돌 reject 분기를 추가하는 것은 plan 43-01과 동일. 단, Codex/Gemini/Antigravity 환경은 AskUserQuestion을 지원하지 않으므로 Step 5의 `--pick` 분기는 **AskUserQuestion을 호출하지 않고 graceful early-exit**한다(stderr 안내 메시지 + exit 1). silent fallback to smart default는 명시적 사용자 의도를 무시하므로 금지(Phase 42 D-06 일관성).
2. **DISPLAY-01 P1 visual emphasis (D-08~D-11)** — plan 43-01과 동일: Step 6의 `ACTION_ITEMS_PRINTF` 가이드를 `🔴 P1` prefix로 갱신.
3. **DISPLAY-02 lens intent line (D-12~D-15)** — plan 43-01과 동일: Step 6에 INTENT_LINE case + single redirect block 내 printf 삽입. 추가로 `<lens_templates>` 블록의 ssc/dspm/analyze 템플릿 3개에도 `_Captured:` 다음 줄에 `_Intent: ..._` 예시 라인을 삽입한다(템플릿 일관성).

추가로 `.agents/` 변종 고유 갱신:
- **`<constraints>` 갱신 (D-17 옵션 b 채택 — Phase 42 retro prose drift 교훈 적용)** — `<constraints>` 블록의 첫 줄 "AskUserQuestion not supported: smart default (dspm+ssc) applies when no lens argument is provided; explicit lens arguments (ssc/dspm/analyze) are accepted directly" 문구를 갱신: `--pick` 모드는 graceful exit하고, smart default/positional args는 변경 없이 동작함을 명시.
- **argument-hint frontmatter 갱신 (Claude 재량 영역, 권장 문구 적용)** — 현재 `"[phase] [lens] - e.g. '14 ssc' or '14 ssc dspm'. lens: ssc|dspm|analyze. Omit lens for smart default (dspm+ssc)."` → 갱신 후 `--pick` 사용법 명시 (구체 문구는 interfaces 참조).
- **`<success_criteria>` 신규 항목 추가** — 7개 → 10개 권장 (`--pick` graceful exit, DISPLAY-01, DISPLAY-02).

Purpose: pairwise convention(D-16 + CLAUDE.md "skills/ + .agents/ 쌍 커버")을 충족하여 Codex/Gemini/Antigravity 환경 사용자도 `--pick` 동작(graceful fallback)·DISPLAY-01·DISPLAY-02를 동일하게 받는다.

Output: 의미적으로 동등한 단일 파일(`.agents/skills/sg-retro/SKILL.md`) — 라인 수는 증가하지만 Step 번호/`<constraints>`/`<lens_templates>`/argument-hint frontmatter 구조는 보존.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-pick-display-polish/43-CONTEXT.md
@.planning/phases/42-smart-default-lens/42-CONTEXT.md
@.planning/lessons/42-2026-05-30.md
@CLAUDE.md
@.agents/skills/sg-retro/SKILL.md
@skills/sg-retro/SKILL.md
</context>

<interfaces>
<!-- Phase 43에서 변경되는 식별자와 정확한 토큰. .agents/ 변종 고유 영역 포함. -->

**--pick 토큰:** `--pick` (plan 43-01과 동일)
**PICK_MODE 변수명:** `PICK_MODE` (bash boolean, plan 43-01과 동일)
**충돌 reject 조건/메시지:** plan 43-01과 byte-identical (`Cannot combine --pick with positional lens argument.\nUse either: sg-retro {phase} {lens...}  (explicit args)\nOr:         sg-retro {phase} --pick     (interactive picker)`).

**`.agents/` 변종 고유 — `--pick` graceful fallback (D-17, CONTEXT deferred ambiguity #3):**

Codex/Gemini/Antigravity 환경에서는 AskUserQuestion이 지원되지 않으므로 `--pick` 모드는 **graceful early-exit**한다:
- stderr: `--pick is not supported in this environment (AskUserQuestion required). Use positional lens args (e.g. 'sg-retro {phase} ssc dspm') or omit lens for smart default (dspm+ssc).`
- `exit 1` (graceful — silent fallback to smart default는 사용자 의도 무시이므로 금지; Phase 42 D-06 일관성)
- lessons 파일 미생성, HANDOFF append 없음

(D-17 옵션 (b) 권장에 따라 `<constraints>` 산문도 동시 갱신 — interfaces 하단 참조. Phase 42 retro에서 prose drift 3건이 잡힌 교훈에 따라 산문 일관성 lock-in.)

**INTENT_LINE 고정 문구 (D-12, D-15 — plan 43-01과 byte-identical):**
- `ssc`: `_Intent: surface behavior changes — what to start, stop, or continue doing next phase._`
- `dspm`: `_Intent: capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase._`
- `analyze`: `_Intent: scan session transcript for frustration, correction, repetition, and validated-success signals; propose sg-rule drafts._`

**INTENT_LINE case 매핑 위치 (Step 6):**
LENS_NAME case (현재 237-241줄) **직후**에 plan 43-01과 동일한 case 패턴 추가. byte-identical 구조.

**INTENT_LINE emit 위치 (Step 6 BODY_PRINTF 직전, single redirect block 내부 — D-21 보존):**
plan 43-01과 동일 — 현재 `_Captured: %s_\n\n`을 `_Captured: %s_\n`으로 줄이고 INTENT_LINE printf 한 줄 삽입.

**ACTION_ITEMS_PRINTF 주석 가이드 갱신 (Step 6, line ~273):**
plan 43-01과 동일 문구로 갱신. `🔴 P1` prefix 가이드 + P2/P3 unchanged 예시.

**Step 1 PICK_MODE 탐지 블록 (Step 1 시작부, `set -- $ARGUMENTS` 직후):**
plan 43-01과 byte-identical bash 블록 (for-loop + NEW_ARGS + set -- $NEW_ARGS).

**Step 2 conflict reject 분기 (Step 2 lens case 매핑 직후, smart default 분기 직전):**
plan 43-01과 byte-identical 분기.

**Step 5 graceful fallback 블록 (Step 5 `ANALYZE_LENS_RAN=false` 직후, lens loop 진입 직전):**

plan 43-01의 AskUserQuestion 호출 placeholder 대신, Codex/Gemini/Antigravity용 graceful early-exit 분기:

```bash
# D-05, D-06, D-17: --pick 모드 — AskUserQuestion 미지원 환경 graceful fallback
if [ "$PICK_MODE" = "true" ]; then
  printf -- '--pick is not supported in this environment (AskUserQuestion required).\nUse positional lens args (e.g. '\''sg-retro %s ssc dspm'\'') or omit lens for smart default (dspm+ssc).\n' "${PHASE_RAW}" >&2
  exit 1
fi
```

(`printf --` 사용 이유: 첫 인자가 `-`로 시작하면 printf가 옵션으로 해석할 수 있어 `--` 종결자 사용. `--pick`가 메시지 본문에 포함되므로 안전망. single-quote escape `'\''` 패턴은 BSD/GNU 양립. 메시지에 phase 번호 보간하여 사용자가 즉시 valid alternative를 복사 사용 가능.)

**`<lens_templates>` 블록 갱신 (DISPLAY-02 템플릿 일관성):**

현재 `<lens_templates>` 블록의 3개 템플릿(`Start/Stop/Continue (ssc)` line ~298-314, `Decisions/Surprises/Patterns/Mistakes (dspm)` line ~318-337, `Conversation Analyzer (analyze)` line ~341-358)에서 각 템플릿의 `_Captured: {ISO-8601 UTC}_` 줄 **직후**에 해당 lens의 `_Intent: ..._` 줄을 추가한다.

예시 (ssc 템플릿):
```markdown
## Lens: Start/Stop/Continue
_Captured: {ISO-8601 UTC}_
_Intent: surface behavior changes — what to start, stop, or continue doing next phase._

### Start
- [item]
...
```

(dspm/analyze 템플릿도 동일 패턴 — 각각 D-12의 고정 INTENT_LINE 본문 사용.)

**`<constraints>` 블록 갱신 (D-17 옵션 b — Phase 42 retro prose drift 교훈 적용):**

현재 (line 18-23):
```
<constraints>
## Platform Constraints (Codex / Gemini CLI / Antigravity CLI)
- AskUserQuestion not supported: smart default (dspm+ssc) applies when no lens argument is provided; explicit lens arguments (ssc/dspm/analyze) are accepted directly
- SubagentStop not supported: no automatic trigger on stage completion
- Superpowers integration unavailable: this skill is fully self-contained
</constraints>
```

변경 후:
```
<constraints>
## Platform Constraints (Codex / Gemini CLI / Antigravity CLI)
- AskUserQuestion not supported: smart default (dspm+ssc) applies when no lens argument is provided; explicit lens arguments (ssc/dspm/analyze) are accepted directly. The `--pick` flag triggers a graceful early-exit (stderr message + exit 1) since the interactive picker requires AskUserQuestion.
- SubagentStop not supported: no automatic trigger on stage completion
- Superpowers integration unavailable: this skill is fully self-contained
</constraints>
```

(첫 줄 본문에 `--pick` 동작 명시 한 문장만 추가. 나머지 줄은 byte-identical. SubagentStop / Superpowers 줄은 손대지 않음.)

**argument-hint frontmatter 갱신 (Claude 재량 — 권장 문구 적용):**

현재 (line 4):
```
argument-hint: "[phase] [lens] - e.g. '14 ssc' or '14 ssc dspm'. lens: ssc|dspm|analyze. Omit lens for smart default (dspm+ssc)."
```

변경 후:
```
argument-hint: "[phase] [lens...|--pick] - e.g. '14 ssc' or '14 --pick'. lens: ssc|dspm|analyze. Omit lens for smart default (dspm+ssc). --pick is not supported in this environment (AskUserQuestion required) — use positional args instead."
```

(`[lens]` → `[lens...|--pick]`로 확장 + `--pick` 예시 추가 + `--pick` 미지원 안내 한 문장 추가. 사용자가 hint만 봐도 .agents/ 환경에서 `--pick`가 동작하지 않음을 알 수 있게 함.)

**변경되는 산문 토큰 (Phase 42 retro lessons P1 #1 적용 — prose drift 방지를 위해 명시적 enumerate):**
- `<objective>` (line 14-15): 현재 `... — Start/Stop/Continue (ssc), Decisions/Surprises/Patterns/Mistakes (dspm), Conversation Analyzer (analyze) — or omit the lens argument to apply the smart default (dspm+ssc).` → 변경 후 `... — Start/Stop/Continue (ssc), Decisions/Surprises/Patterns/Mistakes (dspm), Conversation Analyzer (analyze) — or omit the lens argument to apply the smart default (dspm+ssc). The \`--pick\` flag is accepted for argument compatibility but triggers a graceful early-exit since AskUserQuestion is unavailable in this environment.`
- `<constraints>` 첫 항목 (위 갱신 참조)
- Step 1 본문 산문 (line ~33): 현재 `Parse \`$ARGUMENTS\` into \`PHASE_RAW\` and \`LENS_RAW\`. If \`PHASE_RAW\` is empty, fall back to ...` → 변경 후 `Parse \`$ARGUMENTS\` into \`PHASE_RAW\` and \`LENS_RAW\`. Detect \`--pick\` token anywhere in the argument list and strip it before positional parsing (D-01, D-02). If \`PHASE_RAW\` is empty, fall back to ...`
- Step 5 본문 산문 — "Common flow for ssc/dspm lenses" 항목 #5 (line ~206): 현재 `Assemble the final lens section and append to \`${LESSONS_FILE}\` ...` → 변경 후 `Assemble the final lens section (header + \`_Captured: {NOW_ISO}_\` + \`_Intent: ..._\` + subheadings + Action Items) and append to \`${LESSONS_FILE}\` ...`
- argument-hint frontmatter (line 4 — 위 갱신 참조)
- `<lens_templates>` 3개 템플릿 (`_Intent:` 줄 추가)
- `<success_criteria>` 항목 추가 — D-18 시나리오 반영 신규 3개 (#8 `--pick` graceful exit, #9 P1 emoji prefix, #10 lens intent line).

**보존되는 산문 토큰 (절대 변경 금지):**
- `<language>` 블록 전체
- frontmatter `name`/`description` 필드 (argument-hint만 갱신)
- Step 1 STATE.md parsing 블록 (`# --- BEGIN STATE.md Phase parsing block ---` ~ `# --- END ---` 사이의 모든 줄)
- Step 2 lens case 매핑 3-lens 본문 (ssc/dspm/analyze) + dropped reject 분기 (4ls/sail/5why + *) — Phase 42에서 정착, byte-identical
- Step 2 VALID_EXTRAS inner case — byte-identical
- Step 2 smart default 분기 (`if [ -z "$LENS_CODES_ARRAY" ] ... LENS_CODES_ARRAY="dspm ssc"`) — byte-identical
- Step 3, 3b, 4 전체 — byte-identical
- Step 5 sub-block 본문 (ssc/dspm/analyze facilitation 본문, 단 "Common flow" 항목 #5만 산문 갱신)
- Step 5 ANALYZE_LENS_RAN flag setter
- Step 6 LENS_NAME case (INTENT_LINE 추가는 그 **직후** 새 case로 삽입; LENS_NAME case는 byte-identical)
- Step 6 RUN_SUFFIX 분기 + LENS_HEADER 조립 + 파일 존재 체크 + 신규 파일 헤더 작성
- Step 6 single `>>` redirect block의 단일성 (D-21) — INTENT_LINE printf 한 줄만 _Captured printf와 BODY_PRINTF 사이에 삽입
- Step 6 auto-suggest 블록
- `<constraints>` 블록 SubagentStop / Superpowers 줄 (첫 줄만 갱신)
- `<lens_templates>` 블록의 Action Items 테이블 헤더·구분자 줄 (단, P1 예시 행은 `🔴 P1` prefix 적용 — 아래 추가 액션 참조)
- `<success_criteria>` 기존 7개 항목 (#1~#7) 본문 — byte-identical (3개 항목만 추가)

**`<lens_templates>` Action Items 예시 행 갱신 (DISPLAY-01 일관성):**

각 템플릿의 Action Items 예시 행:
- 현재: `| P1 | [summary] | [concrete step] |`
- 변경 후: `| 🔴 P1 | [summary] | [concrete step] |`

(3개 템플릿 모두 동일. P1 예시 행만 변경 — 다른 priority 예시는 없으므로 변경 대상도 없음.)

**보존 블록과 변경 acceptance의 충돌 회피 (Phase 42 mistake #1 + #2 직접 적용):**
- `<objective>`, `<constraints>` 첫 항목, Step 1 본문 산문, Step 5 항목 #5, argument-hint frontmatter, `<lens_templates>` Intent 줄/P1 행은 위 "변경되는 산문 토큰"에 명시되어 있으므로 byte-identical 검증 acceptance를 두지 않는다.
- 위 토큰 외의 모든 산문은 "보존"으로 처리하고, 신규 acceptance가 해당 산문을 가리키지 않도록 한다.

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Step 1 PICK_MODE 탐지 + Step 2 conflict reject + Step 5 graceful fallback (LENS-03 + D-17)</name>
  <files>.agents/skills/sg-retro/SKILL.md</files>
  <read_first>
    - .agents/skills/sg-retro/SKILL.md (전체 — 변경 영역 frontmatter, Step 1, Step 2 smart default 분기, Step 5 ANALYZE_LENS_RAN 초기화 + lens loop 진입, <constraints>)
    - skills/sg-retro/SKILL.md (plan 43-01 적용 후 참조 — pairwise 의미 단위 일관성 확인)
    - .planning/phases/43-pick-display-polish/43-CONTEXT.md (D-01~D-07, D-17)
    - CLAUDE.md §"macOS 셸 이식성" + §"사용자 언어 메시지" + §"skills/ + .agents/ 쌍 커버"
  </read_first>
  <action>
**A. argument-hint frontmatter 갱신 (Claude 재량, 권장 문구):**

frontmatter line 4를 다음으로 교체:
- 변경 전: `argument-hint: "[phase] [lens] - e.g. '14 ssc' or '14 ssc dspm'. lens: ssc|dspm|analyze. Omit lens for smart default (dspm+ssc)."`
- 변경 후: `argument-hint: "[phase] [lens...|--pick] - e.g. '14 ssc' or '14 --pick'. lens: ssc|dspm|analyze. Omit lens for smart default (dspm+ssc). --pick is not supported in this environment (AskUserQuestion required) — use positional args instead."`

(frontmatter `name`/`description` 필드는 손대지 않는다.)

**B. `<constraints>` 블록 갱신 (D-17 옵션 b — Phase 42 retro prose drift 교훈 적용):**

`<constraints>` 블록의 첫 줄(line ~20):
- 변경 전: `- AskUserQuestion not supported: smart default (dspm+ssc) applies when no lens argument is provided; explicit lens arguments (ssc/dspm/analyze) are accepted directly`
- 변경 후: `- AskUserQuestion not supported: smart default (dspm+ssc) applies when no lens argument is provided; explicit lens arguments (ssc/dspm/analyze) are accepted directly. The \`--pick\` flag triggers a graceful early-exit (stderr message + exit 1) since the interactive picker requires AskUserQuestion.`

(첫 줄 본문 끝에 한 문장만 추가. SubagentStop / Superpowers 줄은 손대지 않는다.)

**C. `<objective>` 산문 갱신 (interfaces "변경되는 산문 토큰" 적용):**

`<objective>` 블록(line 14-15)의 본문 끝에 graceful exit 안내 한 문장 추가:
- 변경 전: `... Conversation Analyzer (analyze) — or omit the lens argument to apply the smart default (dspm+ssc). Facilitate each lens ...`
- 변경 후: `... Conversation Analyzer (analyze) — or omit the lens argument to apply the smart default (dspm+ssc). The \`--pick\` flag is accepted for argument compatibility but triggers a graceful early-exit since AskUserQuestion is unavailable in this environment. Facilitate each lens ...`

(`Facilitate each lens` 문장 시작 **앞**에 graceful exit 한 문장 삽입.)

**D. Step 1 — PICK_MODE 탐지 블록 신설 (D-01, D-02):**

Step 1의 첫 bash 블록 (현재 lines 36-38):
```bash
set -- $ARGUMENTS
PHASE_RAW="${1:-}"
LENS_RAW="${2:-}"
```
을 plan 43-01과 byte-identical bash 블록으로 교체:

```bash
set -- $ARGUMENTS

# D-01, D-02: --pick 토큰을 어느 위치에서든 탐지하고 제거 (positional 파싱 보존)
PICK_MODE=false
NEW_ARGS=""
for ARG in "$@"; do
  case "$ARG" in
    --pick)
      PICK_MODE=true
      ;;
    *)
      NEW_ARGS="${NEW_ARGS}${NEW_ARGS:+ }${ARG}"
      ;;
  esac
done
# shellcheck disable=SC2086 — intentional word splitting for positional re-set
set -- $NEW_ARGS
PHASE_RAW="${1:-}"
LENS_RAW="${2:-}"
```

이후 Step 1의 PHASE_RAW empty check / STATE.md fallback / phase validation / PHASE_PAD / PHASE_DIR / EXTRA_LENS_CODES 파싱은 **byte-identical 보존**.

**E. Step 1 본문 산문 갱신 (interfaces "변경되는 산문 토큰" 적용):**

Step 1 첫 문단(line ~33, "Parse `$ARGUMENTS` into `PHASE_RAW` and `LENS_RAW`."로 시작):
- 변경 전: `Parse \`$ARGUMENTS\` into \`PHASE_RAW\` and \`LENS_RAW\`. If \`PHASE_RAW\` is empty, fall back to ...`
- 변경 후: `Parse \`$ARGUMENTS\` into \`PHASE_RAW\` and \`LENS_RAW\`. Detect \`--pick\` token anywhere in the argument list and strip it before positional parsing (D-01, D-02). If \`PHASE_RAW\` is empty, fall back to ...`

**F. Step 2 — 충돌 reject 분기 신설 (D-03):**

Step 2 lens case 매핑 + VALID_EXTRAS inner case + smart default 분기(현재 lines 86-135)는 **byte-identical 보존**. smart default 분기 바로 **위**에 plan 43-01과 byte-identical 분기 추가:

```bash
# D-03: --pick + positional lens 충돌 reject (silent override 금지)
if [ "$PICK_MODE" = "true" ] && { [ -n "$LENS_RAW" ] || [ -n "$EXTRA_LENS_CODES" ]; }; then
  printf 'Cannot combine --pick with positional lens argument.\nUse either: sg-retro {phase} {lens...}  (explicit args)\nOr:         sg-retro {phase} --pick     (interactive picker)\n' >&2
  exit 1
fi
```

**G. Step 5 — graceful fallback 분기 신설 (D-05, D-06, D-17 — .agents/ 변종 고유):**

Step 5의 `ANALYZE_LENS_RAN=false` 초기화 (현재 line 188) 직후, lens loop 진입 텍스트 직전에 graceful early-exit 분기 추가:

```bash
# D-05, D-06, D-17: --pick 모드 — AskUserQuestion 미지원 환경 graceful fallback
if [ "$PICK_MODE" = "true" ]; then
  printf -- '--pick is not supported in this environment (AskUserQuestion required).\nUse positional lens args (e.g. '\''sg-retro %s ssc dspm'\'') or omit lens for smart default (dspm+ssc).\n' "${PHASE_RAW}" >&2
  exit 1
fi
```

(plan 43-01의 AskUserQuestion placeholder 대신 graceful exit. `printf --`로 `--pick` 메시지가 옵션으로 해석되지 않도록 보호. exit 1이 lens loop 진입 **전**에 발생 — lens loop는 `for LENS_CODE in $LENS_CODES_ARRAY; do` 이므로 그 **전** 위치 critical.)

**H. macOS 셸 이식성 (CLAUDE.md):**
- `grep -P` 금지. `printf` + `>&2` 패턴만 사용.
- 변수 확장 시 큰따옴표 사용.
- `case "$ARG" in` 패턴은 BSD/GNU 양립.
- single-quote escape `'\''` 패턴 (`printf -- '... '\''sg-retro %s ssc dspm'\'' ...'`)은 BSD/GNU 양립 — 외부 quote는 single, 내부 quote 표현은 `'\''` 패턴.
- Step 1 STATE.md parsing 블록 **절대 손대지 않음**.

**I. 보존 검증:**
- Step 1 STATE.md parsing 블록, Step 2 lens case 매핑(3-lens + dropped reject + VALID_EXTRAS inner case + smart default 분기), Step 3, 3b, 4 전체, Step 5 sub-block 본문(ssc/dspm/analyze), Step 5 ANALYZE_LENS_RAN flag setter, Step 6 전체(LENS_NAME case, RUN_SUFFIX, LENS_HEADER, redirect block, auto-suggest)는 본 Task에서 손대지 않는다. INTENT_LINE + ACTION_ITEMS_PRINTF 가이드 + `<lens_templates>` 갱신 + success_criteria 항목 추가는 Task 2가 담당.
  </action>
  <verify>
    <automated>
# A) argument-hint frontmatter 갱신
grep -q 'argument-hint:.*\[lens\.\.\.|--pick\]' .agents/skills/sg-retro/SKILL.md && \
grep -q '\-\-pick is not supported in this environment' .agents/skills/sg-retro/SKILL.md && \

# B) <constraints> 첫 줄 갱신 (--pick graceful exit 명시)
grep -q '`--pick` flag triggers a graceful early-exit' .agents/skills/sg-retro/SKILL.md && \

# C) <objective> 산문 갱신
grep -q '`--pick` flag is accepted for argument compatibility but triggers a graceful early-exit' .agents/skills/sg-retro/SKILL.md && \

# D) Step 1 PICK_MODE 탐지 블록 신설
grep -q 'PICK_MODE=false' .agents/skills/sg-retro/SKILL.md && \
grep -q 'PICK_MODE=true' .agents/skills/sg-retro/SKILL.md && \
grep -qE 'for ARG in "\$@"; do' .agents/skills/sg-retro/SKILL.md && \
grep -q 'D-01, D-02: --pick 토큰을' .agents/skills/sg-retro/SKILL.md && \

# E) Step 1 본문 산문 갱신
grep -q 'Detect `--pick` token anywhere in the argument list' .agents/skills/sg-retro/SKILL.md && \

# F) Step 2 충돌 reject 분기 신설
grep -q 'D-03: --pick + positional lens 충돌 reject' .agents/skills/sg-retro/SKILL.md && \
grep -q 'Cannot combine --pick with positional lens argument' .agents/skills/sg-retro/SKILL.md && \

# G) Step 5 graceful fallback 분기 신설 (.agents/ 변종 고유)
grep -q 'D-05, D-06, D-17: --pick 모드' .agents/skills/sg-retro/SKILL.md && \
grep -q '\-\-pick is not supported in this environment (AskUserQuestion required)' .agents/skills/sg-retro/SKILL.md && \

# H) smart default 분기 보존 (Phase 42 carry-forward)
grep -q 'LENS_CODES_ARRAY="dspm ssc"' .agents/skills/sg-retro/SKILL.md && \
grep -q 'BEGIN STATE.md Phase parsing block' .agents/skills/sg-retro/SKILL.md && \

# I) Phase 42 보존: dropped lens reject 보존
grep -q 'no longer supported (removed in v2.9)' .agents/skills/sg-retro/SKILL.md && \

# J) sg-learn 미변경 (D-11 carry-forward) — .agents/ 측
test "$(git diff --stat HEAD -- .agents/skills/sg-learn/SKILL.md 2>/dev/null | wc -l)" = "0" && \

# K) AskUserQuestion 호출 패턴 부재 (.agents/ 변종은 AskUserQuestion을 호출하지 않음)
! grep -q 'AskUserQuestion with the spec below' .agents/skills/sg-retro/SKILL.md && \

echo "Task 1 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'argument-hint:.*\[lens\.\.\.|--pick\]' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '`--pick` flag triggers a graceful early-exit' .agents/skills/sg-retro/SKILL.md` returns `1` (constraints 갱신)
    - Source: `grep -c '`--pick` flag is accepted for argument compatibility' .agents/skills/sg-retro/SKILL.md` returns `1` (objective 갱신)
    - Source: `grep -c 'PICK_MODE=false' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'Cannot combine --pick with positional lens argument' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '\-\-pick is not supported in this environment (AskUserQuestion required)' .agents/skills/sg-retro/SKILL.md` returns ≥ 2 (argument-hint + Step 5 graceful exit)
    - Source: `grep -c 'Detect `--pick` token anywhere in the argument list' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'LENS_CODES_ARRAY="dspm ssc"' .agents/skills/sg-retro/SKILL.md` returns `1` (Phase 42 smart default 보존)
    - Source: `grep -c 'no longer supported (removed in v2.9)' .agents/skills/sg-retro/SKILL.md` returns ≥ 1 (Phase 42 dropped reject 보존)
    - Source: `grep -c 'AskUserQuestion with the spec below' .agents/skills/sg-retro/SKILL.md` returns `0` (.agents/ 변종은 AskUserQuestion 호출 안 함)
    - CLI: `git diff --stat HEAD -- .agents/skills/sg-learn/SKILL.md | wc -l` returns `0` (D-11 sg-learn 미변경)
    - Behavior assertion (Task 3에서 실행): `Skill(skill="sg-retro" via .agents, args="43 --pick")` → stderr `--pick is not supported in this environment ...` 출력 + exit 1 + lessons 파일 미생성
  </acceptance_criteria>
  <done>
argument-hint frontmatter가 `--pick` 사용법 + graceful exit 안내를 포함하도록 갱신되었다. `<constraints>` 첫 줄에 `--pick` graceful early-exit 동작이 명시되었다 (D-17 옵션 b). `<objective>` 산문에 `--pick` graceful exit 한 문장이 추가되었다. Step 1의 첫 bash 블록이 plan 43-01과 byte-identical PICK_MODE 탐지 + 토큰 제거 + 정상 positional 파싱으로 확장되었다. Step 2 smart default 분기 위에 `--pick` + positional lens 충돌 reject 분기가 신설되었다. Step 5의 `ANALYZE_LENS_RAN=false` 직후에 graceful early-exit 분기(.agents/ 변종 고유 — AskUserQuestion 호출 없음, exit 1)가 신설되었다. 보존 영역(STATE.md 블록, Phase 42 smart default 분기, Phase 42 dropped reject 분기, Step 3/3b/4 전체, Step 5 sub-block, Step 6 전체)은 byte-identical로 유지되었다. `.agents/skills/sg-learn/SKILL.md`는 변경되지 않았다.
  </done>
</task>

<task type="auto">
  <name>Task 2: Step 6 INTENT_LINE case + body printf + ACTION_ITEMS_PRINTF + <lens_templates> 갱신 + success_criteria 추가 (DISPLAY-01, DISPLAY-02)</name>
  <files>.agents/skills/sg-retro/SKILL.md</files>
  <read_first>
    - .agents/skills/sg-retro/SKILL.md (Task 1 적용 후 — Step 5 Common flow 항목 #5, Step 6 LENS_NAME case + redirect block 본문, <lens_templates> 3개 템플릿, <success_criteria> 7개 항목)
    - .planning/phases/43-pick-display-polish/43-CONTEXT.md (D-08~D-15)
    - skills/sg-retro/SKILL.md (plan 43-01 적용 후 — pairwise 의미 단위 일관성 확인)
  </read_first>
  <action>
**A. Step 6 — INTENT_LINE case 매핑 신설 (D-12, D-14):**

Step 6의 LENS_NAME case (현재 lines 237-241)는 **byte-identical 보존**. 그 **직후**에 plan 43-01과 byte-identical INTENT_LINE case 추가:

```bash
case "$LENS_CODE" in
  ssc)     LENS_NAME="Start/Stop/Continue" ;;
  dspm)    LENS_NAME="Decisions/Surprises/Patterns/Mistakes" ;;
  analyze) LENS_NAME="Conversation Analyzer" ;;
esac

# D-12, D-14: lens intent line (static, English, italic single-line — emitted after _Captured: line)
case "$LENS_CODE" in
  ssc)     INTENT_LINE='_Intent: surface behavior changes — what to start, stop, or continue doing next phase._' ;;
  dspm)    INTENT_LINE='_Intent: capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase._' ;;
  analyze) INTENT_LINE='_Intent: scan session transcript for frustration, correction, repetition, and validated-success signals; propose sg-rule drafts._' ;;
esac
```

(2-space 들여쓰기, `analyze` 7자 패딩 정렬. em-dash `—` U+2014 사용. single-quote `'...'` 사용. plan 43-01과 byte-identical.)

**B. Step 6 — single redirect block 내부 INTENT_LINE printf 삽입 (D-13, D-14, D-21 보존):**

현재 (line ~266-269):
```bash
{
  printf '%s\n' "$LENS_HEADER"
  printf '_Captured: %s_\n\n' "$NOW_ISO"
  # BODY_PRINTF — insert subheading + bullet printf lines here (confirmed content)
```
변경 후:
```bash
{
  printf '%s\n' "$LENS_HEADER"
  printf '_Captured: %s_\n' "$NOW_ISO"
  printf '%s\n\n' "$INTENT_LINE"
  # BODY_PRINTF — insert subheading + bullet printf lines here (confirmed content)
```

(plan 43-01과 byte-identical 변경. `_Captured: %s_\n\n` → `_Captured: %s_\n`으로 축소, INTENT_LINE printf 한 줄 삽입. single `>>` redirect block 보존 — D-21.)

**C. Step 6 — ACTION_ITEMS_PRINTF 주석 가이드 갱신 (D-08, D-09, D-11):**

현재 (line ~273):
```
# ACTION_ITEMS_PRINTF — insert row printf lines here
```
변경 후:
```
# ACTION_ITEMS_PRINTF — insert row printf lines here
# DISPLAY-01 (D-08, D-09): P1 행은 priority 셀에 `🔴 P1` prefix로 emit. P2/P3는 prefix 없음.
#   printf '| 🔴 P1 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"   # P1 only
#   printf '| P2 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"      # P2 unchanged
#   printf '| P3 | %s | %s |\n' "$ITEM_TEXT" "$NEXT_STEP_TEXT"      # P3 unchanged
```

(plan 43-01과 byte-identical 가이드 본문. 본 plan의 .agents/ 측 placeholder는 짧은 한 줄이지만 갱신 후 가이드는 동일.)

**D. Step 5 "Common flow for ssc/dspm lenses" 항목 #5 산문 갱신 (interfaces "변경되는 산문 토큰" 적용):**

Step 5의 numbered list 항목 #5 (line ~206):
- 변경 전: `Assemble the final lens section and append to \`${LESSONS_FILE}\` using the bash block in Step 6.`
- 변경 후: `Assemble the final lens section (header + \`_Captured: {NOW_ISO}_\` + \`_Intent: ..._\` + subheadings + Action Items) and append to \`${LESSONS_FILE}\` using the bash block in Step 6.`

(plan 43-01과 의미적으로 동일하지만 .agents/ 본문 구조에 맞게 surgical insertion.)

**E. `<lens_templates>` 갱신 — `_Intent:` 줄 추가 (DISPLAY-02 템플릿 일관성):**

세 템플릿(ssc/dspm/analyze)의 `_Captured: {ISO-8601 UTC}_` 줄 직후에 해당 lens의 `_Intent: ..._` 줄을 한 줄씩 추가한다.

**Start/Stop/Continue (ssc) 템플릿 (line ~298-314):**
변경 전:
```markdown
## Lens: Start/Stop/Continue
_Captured: {ISO-8601 UTC}_

### Start
```
변경 후:
```markdown
## Lens: Start/Stop/Continue
_Captured: {ISO-8601 UTC}_
_Intent: surface behavior changes — what to start, stop, or continue doing next phase._

### Start
```

**Decisions/Surprises/Patterns/Mistakes (dspm) 템플릿 (line ~318-337):**
변경 전:
```markdown
## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: {ISO-8601 UTC}_

### Decisions
```
변경 후:
```markdown
## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: {ISO-8601 UTC}_
_Intent: capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase._

### Decisions
```

**Conversation Analyzer (analyze) 템플릿 (line ~341-358):**
변경 전:
```markdown
## Lens: Conversation Analyzer
_Captured: {ISO-8601 UTC}_

### Analysis Findings
```
변경 후:
```markdown
## Lens: Conversation Analyzer
_Captured: {ISO-8601 UTC}_
_Intent: scan session transcript for frustration, correction, repetition, and validated-success signals; propose sg-rule drafts._

### Analysis Findings
```

(각 템플릿에서 `_Captured:` 다음 빈 줄 1개 → `_Intent:` 줄 + 빈 줄 1개로 교체. 빈 줄 총 개수 보존.)

**F. `<lens_templates>` Action Items 예시 행 갱신 (DISPLAY-01 일관성):**

세 템플릿의 Action Items 예시 행:
- 현재: `| P1 | [summary] | [concrete step] |`
- 변경 후: `| 🔴 P1 | [summary] | [concrete step] |`

(3개 템플릿 모두 동일. 각 템플릿마다 한 행만 변경 — 다른 priority 예시 행은 없음.)

**G. `<success_criteria>` 블록 — 신규 항목 3개 추가 (#8, #9, #10):**

기존 7개 항목(#1~#7)은 **byte-identical 보존**. 항목 #7 다음에 새 항목 3개를 추가한다:

```
8. The `--pick` flag (long form, lowercase, hyphenated) is detected anywhere in the argument list and triggers a graceful early-exit with a stderr message and exit 1, since AskUserQuestion is unavailable in this environment (LENS-03, D-01, D-02, D-05, D-17). Combining `--pick` with a positional lens argument (e.g. `43 ssc --pick`) emits a stderr conflict error and exits 1 (D-03). Smart default (dspm+ssc) and positional lens arguments continue to work without AskUserQuestion.
9. Lessons file `### Action Items` rows whose priority is P1 are emitted with a `🔴 P1` prefix in the priority cell; P2 and P3 rows have no prefix (DISPLAY-01, D-08, D-09). The single 3-column table schema (`priority | item | next step`) is preserved.
10. Each lens section emits an italic single-line lens intent statement directly after the `_Captured: {ISO}_` line, sourced from the static `INTENT_LINE` case mapping in Step 6 (one fixed English sentence per lens — ssc/dspm/analyze) (DISPLAY-02, D-12, D-13, D-14, D-15).
```

(번호는 8/9/10. 기존 7개 항목 본문은 변경하지 않음. 신규 항목은 LENS-03/DISPLAY-01/DISPLAY-02 + 관련 D-xx 결정 ID를 명시. .agents/ 변종 고유 D-17 graceful exit 명시.)

**H. 손대지 말 것:**
- Step 1, Step 2 (Task 1 변경분 제외), Step 3, Step 3b, Step 4 전체
- Step 5 sub-block 본문 (ssc/dspm/analyze facilitation) + ANALYZE_LENS_RAN flag setter
- Step 6 LENS_NAME case (위 INTENT_LINE 추가 **이전**의 case는 byte-identical 보존)
- Step 6 RUN_SUFFIX 분기, LENS_HEADER 조립, 파일 존재 체크, 신규 파일 헤더 작성
- Step 6 BODY_PRINTF placeholder 주석 본문 (위치만 그대로, ACTION_ITEMS_PRINTF 갱신과 별개)
- Step 6 single `>>` redirect block의 단일성 — 새 redirect block 신설 금지
- Step 6 auto-suggest 블록
- `<success_criteria>` 항목 #1~#7 본문 — byte-identical
- `<language>` 블록, `<execution_context>` 블록
- frontmatter `name`/`description` 필드 (argument-hint는 Task 1에서 갱신됨)
- `<constraints>` 블록의 SubagentStop / Superpowers 줄 (첫 줄만 Task 1에서 갱신)
  </action>
  <verify>
    <automated>
# A) INTENT_LINE case 매핑 신설 검증
grep -q 'D-12, D-14: lens intent line' .agents/skills/sg-retro/SKILL.md && \
grep -q "ssc)     INTENT_LINE='_Intent: surface behavior changes" .agents/skills/sg-retro/SKILL.md && \
grep -q "dspm)    INTENT_LINE='_Intent: capture technical decisions" .agents/skills/sg-retro/SKILL.md && \
grep -q "analyze) INTENT_LINE='_Intent: scan session transcript" .agents/skills/sg-retro/SKILL.md && \

# B) LENS_NAME case 보존 (Phase 42 carry-forward)
grep -q 'ssc)     LENS_NAME="Start/Stop/Continue"' .agents/skills/sg-retro/SKILL.md && \
grep -q 'dspm)    LENS_NAME="Decisions/Surprises/Patterns/Mistakes"' .agents/skills/sg-retro/SKILL.md && \
grep -q 'analyze) LENS_NAME="Conversation Analyzer"' .agents/skills/sg-retro/SKILL.md && \

# C) Step 6 single redirect block 내부 INTENT_LINE printf 삽입
grep -q "printf '%s\\\\n\\\\n' \"\$INTENT_LINE\"" .agents/skills/sg-retro/SKILL.md && \

# D) ACTION_ITEMS_PRINTF 가이드 갱신 — DISPLAY-01 명시
grep -q 'DISPLAY-01 (D-08, D-09): P1 행은 priority 셀에' .agents/skills/sg-retro/SKILL.md && \
grep -q "printf '| 🔴 P1 | %s | %s |" .agents/skills/sg-retro/SKILL.md && \

# E) Step 5 "Common flow" 항목 #5 산문 갱신 (intent line 명시)
grep -q '_Captured: {NOW_ISO}_` + `_Intent: ..._`' .agents/skills/sg-retro/SKILL.md && \

# F) <lens_templates> _Intent 줄 추가 (3개 템플릿)
test "$(grep -c '^_Intent: ' .agents/skills/sg-retro/SKILL.md)" -ge 3 && \
grep -q '^_Intent: surface behavior changes' .agents/skills/sg-retro/SKILL.md && \
grep -q '^_Intent: capture technical decisions' .agents/skills/sg-retro/SKILL.md && \
grep -q '^_Intent: scan session transcript' .agents/skills/sg-retro/SKILL.md && \

# G) <lens_templates> Action Items P1 예시 행 갱신 (3개 템플릿)
test "$(grep -c '| 🔴 P1 | \[summary\] | \[concrete step\] |' .agents/skills/sg-retro/SKILL.md)" = "3" && \
! grep -q '^| P1 | \[summary\] | \[concrete step\] |' .agents/skills/sg-retro/SKILL.md && \

# H) success_criteria 신규 3개 항목 — 키워드 존재
grep -q 'LENS-03, D-01, D-02, D-05, D-17' .agents/skills/sg-retro/SKILL.md && \
grep -q 'DISPLAY-01, D-08, D-09' .agents/skills/sg-retro/SKILL.md && \
grep -q 'DISPLAY-02, D-12, D-13, D-14, D-15' .agents/skills/sg-retro/SKILL.md && \

# I) success_criteria 블록 단일성 보존
test "$(grep -c '<success_criteria>' .agents/skills/sg-retro/SKILL.md)" = "1" && \
test "$(grep -c '</success_criteria>' .agents/skills/sg-retro/SKILL.md)" = "1" && \

# J) Step 6 single >> redirect block 단일성 (별도 redirect 신설 금지)
test "$(grep -c '} >> \"\$LESSONS_FILE\"' .agents/skills/sg-retro/SKILL.md)" = "1" && \

# K) auto-suggest 블록 보존
grep -q 'sg-rule drafts were included in the Conversation Analyzer' .agents/skills/sg-retro/SKILL.md && \

# L) <lens_templates> 블록 단일성 보존
test "$(grep -c '<lens_templates>' .agents/skills/sg-retro/SKILL.md)" = "1" && \
test "$(grep -c '</lens_templates>' .agents/skills/sg-retro/SKILL.md)" = "1" && \

echo "Task 2 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "INTENT_LINE='_Intent:" .agents/skills/sg-retro/SKILL.md` returns `3` (3개 lens 매핑)
    - Source: `grep -c 'D-12, D-14: lens intent line' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c "printf '%s\\\\n\\\\n' \"\$INTENT_LINE\"" .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'DISPLAY-01 (D-08, D-09): P1 행은 priority 셀에' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c "printf '| 🔴 P1 | %s | %s |" .agents/skills/sg-retro/SKILL.md` returns `1` (가이드 예시)
    - Source: `grep -c '^_Intent: ' .agents/skills/sg-retro/SKILL.md` returns ≥ 3 (lens_templates 3개 + Step 6 case 정의 라인은 들여쓰기 있어 별도 카운트)
    - Source: `grep -c '| 🔴 P1 | \[summary\] | \[concrete step\] |' .agents/skills/sg-retro/SKILL.md` returns `3` (3개 템플릿)
    - Source: `grep -cE '^\| P1 \| \[summary\]' .agents/skills/sg-retro/SKILL.md` returns `0` (기존 P1 예시 행 부재)
    - Source: `grep -c 'LENS-03, D-01, D-02, D-05, D-17' .agents/skills/sg-retro/SKILL.md` returns `1` (success_criteria #8)
    - Source: `grep -c 'DISPLAY-01, D-08, D-09' .agents/skills/sg-retro/SKILL.md` returns `1` (success_criteria #9)
    - Source: `grep -c 'DISPLAY-02, D-12, D-13, D-14, D-15' .agents/skills/sg-retro/SKILL.md` returns `1` (success_criteria #10)
    - Source: `grep -c '<success_criteria>' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '</success_criteria>' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '<lens_templates>' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '</lens_templates>' .agents/skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c '} >> "\$LESSONS_FILE"' .agents/skills/sg-retro/SKILL.md` returns `1` (D-21 single redirect 보존)
    - Source: `grep -c 'sg-rule drafts were included in the Conversation Analyzer' .agents/skills/sg-retro/SKILL.md` returns `1` (auto-suggest 보존)
    - Source: `grep -c 'ssc)     LENS_NAME="Start/Stop/Continue"' .agents/skills/sg-retro/SKILL.md` returns `1` (LENS_NAME case 보존)
  </acceptance_criteria>
  <done>
Step 6에 INTENT_LINE case 매핑이 LENS_NAME case 직후에 plan 43-01과 byte-identical로 추가되었다. single `>>` redirect block 내부의 `_Captured:` printf 다음에 INTENT_LINE printf 한 줄이 삽입되었다 (D-21 보존). ACTION_ITEMS_PRINTF placeholder 주석이 `🔴 P1` prefix 가이드로 확장되었다. Step 5 "Common flow" 항목 #5 산문에 `_Intent: ..._` 토큰이 명시되었다. `<lens_templates>` 3개 템플릿에 각각 `_Intent:` 줄이 추가되었고, Action Items P1 예시 행이 `| 🔴 P1 |`로 갱신되었다. `<success_criteria>` 블록에 신규 항목 3개(#8 `--pick` graceful exit, #9 P1 emoji prefix, #10 lens intent line)가 추가되었고, 기존 7개 항목(#1~#7) 본문은 byte-identical로 보존되었다. Step 6 single redirect block의 단일성(D-21)이 유지되었고, auto-suggest 블록 + `<lens_templates>`/`<success_criteria>` 블록 단일성도 보존되었다.
  </done>
</task>

<task type="auto">
  <name>Task 3: D-18 9개 검증 시나리오 정적·동적 점검 + pairwise diff 정합 확인</name>
  <files></files>
  <read_first>
    - .agents/skills/sg-retro/SKILL.md (Task 1+2 적용 후 상태)
    - skills/sg-retro/SKILL.md (plan 43-01 완료 후 상태)
    - .planning/phases/43-pick-display-polish/43-CONTEXT.md (D-18 9개 시나리오 + D-16 pairwise 요구사항)
  </read_first>
  <action>
Task 1+2 변경이 D-18 시나리오를 .agents/ 환경에 맞게 충족하는지, 그리고 plan 43-01과의 pairwise 의미적 동등성을 검증한다.

**시나리오 1 — `--pick` graceful early-exit (D-05, D-17 — .agents/ 변종 고유):**
`Skill(skill="sg-retro" via .agents, args="43 --pick")` → AskUserQuestion 호출 없음. stderr `--pick is not supported in this environment ...` + exit 1. lessons 파일 미생성.
- 정적 검증:
  ```bash
  grep -A 3 'D-05, D-06, D-17: --pick 모드' .agents/skills/sg-retro/SKILL.md | grep -q 'exit 1' && \
  grep -q '\-\-pick is not supported in this environment' .agents/skills/sg-retro/SKILL.md
  ```

**시나리오 2 — `--pick` + positional 충돌 (D-03):**
`Skill(skill="sg-retro" via .agents, args="43 --pick ssc")` → stderr `Cannot combine --pick with positional lens argument.` + exit 1. lessons 미생성.
- 정적 검증:
  ```bash
  grep -B 1 -A 3 'D-03: --pick + positional' .agents/skills/sg-retro/SKILL.md | grep -q 'exit 1'
  ```

**시나리오 3 — `--pick` 위치 무관 (D-02):**
`Skill(skill="sg-retro" via .agents, args="43 ssc --pick")` → 동일 충돌 reject (`--pick` + `ssc` 동시 존재).
- 정적 검증: PICK_MODE 탐지 분기의 `for ARG in "$@"` 루프 구조.

**시나리오 4 — smart default 보존 (D-04, no regression):**
`Skill(skill="sg-retro" via .agents, args="43")` → 자동으로 `dspm ssc` 진입.
- 정적 검증:
  ```bash
  grep -A 3 'D-02.*smart default' .agents/skills/sg-retro/SKILL.md | grep -q 'LENS_CODES_ARRAY="dspm ssc"'
  ```

**시나리오 5 — sg-learn pass-through (D-11):**
`Skill(skill="sg-learn" via .agents, args="43 --pick")` → sg-retro forward → graceful exit.
- 정적 검증:
  ```bash
  test "$(git diff --stat HEAD -- .agents/skills/sg-learn/SKILL.md | wc -l)" = "0"
  ```

**시나리오 6 — 0-selection silent exit (D-06):**
.agents/ 환경에서는 AskUserQuestion이 호출되지 않으므로 0-selection 시나리오는 발생하지 않음 — `--pick` 진입 시점에 즉시 graceful exit. 시나리오 1로 covered.

**시나리오 7 — DISPLAY-01 P1 emoji prefix:**
- 정적 검증: ACTION_ITEMS_PRINTF 가이드 + `<lens_templates>` 예시 행 모두 `🔴 P1` 사용.
  ```bash
  grep -q "printf '| 🔴 P1 | %s | %s |" .agents/skills/sg-retro/SKILL.md && \
  test "$(grep -c '| 🔴 P1 | \[summary\] | \[concrete step\] |' .agents/skills/sg-retro/SKILL.md)" = "3"
  ```

**시나리오 8 — DISPLAY-02 intent line:**
- 정적 검증:
  ```bash
  test "$(grep -c "INTENT_LINE='_Intent:" .agents/skills/sg-retro/SKILL.md)" = "3" && \
  test "$(grep -c '^_Intent: ' .agents/skills/sg-retro/SKILL.md)" -ge 3
  ```

**시나리오 9 — pairwise diff (D-16) — 의미적 동등성 검증:**

두 파일의 의미 단위(`--pick` 탐지, 충돌 reject, INTENT_LINE case + emit, ACTION_ITEMS_PRINTF 가이드)가 모두 적용되었는지 확인. 구조적 차이(AskUserQuestion 호출 vs graceful exit, `<constraints>` 블록 존재, `<lens_templates>` 블록 존재, argument-hint frontmatter)는 허용.

- 정적 검증 (key 토큰이 양쪽 파일에 모두 존재):
  ```bash
  # PICK_MODE 탐지 분기
  grep -q 'PICK_MODE=false' skills/sg-retro/SKILL.md && \
  grep -q 'PICK_MODE=false' .agents/skills/sg-retro/SKILL.md && \

  # 충돌 reject (동일 메시지)
  grep -q 'Cannot combine --pick with positional lens argument' skills/sg-retro/SKILL.md && \
  grep -q 'Cannot combine --pick with positional lens argument' .agents/skills/sg-retro/SKILL.md && \

  # INTENT_LINE 3개 동일
  test "$(grep -c "INTENT_LINE='_Intent:" skills/sg-retro/SKILL.md)" = "3" && \
  test "$(grep -c "INTENT_LINE='_Intent:" .agents/skills/sg-retro/SKILL.md)" = "3" && \

  # ACTION_ITEMS_PRINTF 가이드 동일
  grep -q "printf '| 🔴 P1 | %s | %s |" skills/sg-retro/SKILL.md && \
  grep -q "printf '| 🔴 P1 | %s | %s |" .agents/skills/sg-retro/SKILL.md
  ```

- 차이 허용 토큰 (양쪽이 의도적으로 다름):
  - `skills/sg-retro/SKILL.md`: `AskUserQuestion with the spec below` 가이드 존재
  - `.agents/skills/sg-retro/SKILL.md`: `--pick is not supported in this environment` graceful exit 존재
  - `.agents/skills/sg-retro/SKILL.md`: `<constraints>` 블록, `<lens_templates>` 블록, argument-hint frontmatter 존재
  ```bash
  grep -q 'AskUserQuestion with the spec below' skills/sg-retro/SKILL.md && \
  ! grep -q 'AskUserQuestion with the spec below' .agents/skills/sg-retro/SKILL.md && \
  grep -q '\-\-pick is not supported in this environment' .agents/skills/sg-retro/SKILL.md && \
  ! grep -q '\-\-pick is not supported in this environment' skills/sg-retro/SKILL.md && \
  grep -q '<constraints>' .agents/skills/sg-retro/SKILL.md && \
  ! grep -q '<constraints>' skills/sg-retro/SKILL.md && \
  grep -q '<lens_templates>' .agents/skills/sg-retro/SKILL.md && \
  ! grep -q '<lens_templates>' skills/sg-retro/SKILL.md
  ```
  </action>
  <verify>
    <automated>
# 시나리오 1: --pick graceful early-exit (.agents/ 변종 고유)
grep -A 3 'D-05, D-06, D-17: --pick 모드' .agents/skills/sg-retro/SKILL.md | grep -q 'exit 1' && \
grep -q '\-\-pick is not supported in this environment' .agents/skills/sg-retro/SKILL.md && \

# 시나리오 2: --pick + positional 충돌 reject
grep -B 1 -A 3 'D-03: --pick + positional' .agents/skills/sg-retro/SKILL.md | grep -q 'exit 1' && \

# 시나리오 3: --pick 위치 무관 (for ARG 루프)
grep -qE 'for ARG in "\$@"; do' .agents/skills/sg-retro/SKILL.md && \

# 시나리오 4: smart default 보존
grep -A 3 'D-02.*smart default' .agents/skills/sg-retro/SKILL.md | grep -q 'LENS_CODES_ARRAY="dspm ssc"' && \

# 시나리오 5: .agents/ sg-learn 미변경
test "$(git diff --stat HEAD -- .agents/skills/sg-learn/SKILL.md 2>/dev/null | wc -l)" = "0" && \

# 시나리오 7: DISPLAY-01 P1 emoji prefix 가이드 + 템플릿
grep -q "printf '| 🔴 P1 | %s | %s |" .agents/skills/sg-retro/SKILL.md && \
test "$(grep -c '| 🔴 P1 | \[summary\] | \[concrete step\] |' .agents/skills/sg-retro/SKILL.md)" = "3" && \

# 시나리오 8: DISPLAY-02 INTENT_LINE 3개 + lens_templates 3개
test "$(grep -c "INTENT_LINE='_Intent:" .agents/skills/sg-retro/SKILL.md)" = "3" && \
test "$(grep -c '^_Intent: ' .agents/skills/sg-retro/SKILL.md)" -ge 3 && \

# 시나리오 9: pairwise 의미 단위 동등성 (공통 토큰)
grep -q 'PICK_MODE=false' skills/sg-retro/SKILL.md && \
grep -q 'PICK_MODE=false' .agents/skills/sg-retro/SKILL.md && \
grep -q 'Cannot combine --pick with positional lens argument' skills/sg-retro/SKILL.md && \
grep -q 'Cannot combine --pick with positional lens argument' .agents/skills/sg-retro/SKILL.md && \
test "$(grep -c "INTENT_LINE='_Intent:" skills/sg-retro/SKILL.md)" = "3" && \
test "$(grep -c "INTENT_LINE='_Intent:" .agents/skills/sg-retro/SKILL.md)" = "3" && \
grep -q "printf '| 🔴 P1 | %s | %s |" skills/sg-retro/SKILL.md && \
grep -q "printf '| 🔴 P1 | %s | %s |" .agents/skills/sg-retro/SKILL.md && \

# 시나리오 9: pairwise 의미 단위 동등성 (의도적 차이)
grep -q 'AskUserQuestion with the spec below' skills/sg-retro/SKILL.md && \
! grep -q 'AskUserQuestion with the spec below' .agents/skills/sg-retro/SKILL.md && \
grep -q '\-\-pick is not supported in this environment' .agents/skills/sg-retro/SKILL.md && \
! grep -q '\-\-pick is not supported in this environment' skills/sg-retro/SKILL.md && \
grep -q '<constraints>' .agents/skills/sg-retro/SKILL.md && \
! grep -q '<constraints>' skills/sg-retro/SKILL.md && \

# success_criteria 10개 항목 (#1~#7 보존 + #8/#9/#10 신규)
test "$(grep -cE '^[0-9]+\. ' .agents/skills/sg-retro/SKILL.md)" -ge 10 && \

echo "Task 3 PASS — D-18 시나리오 1-5, 7-9 + pairwise 의미적 동등성 검증"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -A 3 'D-05, D-06, D-17: --pick 모드' .agents/skills/sg-retro/SKILL.md | grep -c 'exit 1'` returns `1` (graceful exit)
    - Source: `grep -c '\-\-pick is not supported in this environment' .agents/skills/sg-retro/SKILL.md` returns ≥ 2 (argument-hint + Step 5 + 가능한 constraints)
    - Source: `grep -B 1 -A 3 'D-03: --pick + positional' .agents/skills/sg-retro/SKILL.md | grep -c 'exit 1'` returns `1`
    - Source: `grep -E 'for ARG in "\$@"; do' .agents/skills/sg-retro/SKILL.md` returns ≥ 1
    - Source: `grep -A 3 'D-02.*smart default' .agents/skills/sg-retro/SKILL.md | grep -c 'LENS_CODES_ARRAY="dspm ssc"'` returns `1`
    - CLI: `git diff --stat HEAD -- .agents/skills/sg-learn/SKILL.md | wc -l` returns `0`
    - Source: `grep -c "INTENT_LINE='_Intent:" .agents/skills/sg-retro/SKILL.md` returns `3`
    - Source: `grep -c '| 🔴 P1 | \[summary\] | \[concrete step\] |' .agents/skills/sg-retro/SKILL.md` returns `3` (3 templates)
    - Source: `grep -c '^_Intent: ' .agents/skills/sg-retro/SKILL.md` returns ≥ 3 (lens_templates 3개 — Step 6 case는 들여쓰기 있음)
    - Behavior assertion: 두 파일에 동일한 의미 단위(PICK_MODE 탐지, 충돌 reject, INTENT_LINE case + emit, ACTION_ITEMS_PRINTF 가이드)가 적용되어 있으며, 의도적 차이(`.agents/` 측 graceful exit vs `skills/` 측 AskUserQuestion placeholder, `<constraints>`/`<lens_templates>`/`argument-hint` 존재 여부)는 명확히 식별됨
  </acceptance_criteria>
  <done>
D-18의 시나리오 1-5 + 7-9가 정적 검증으로 통과한다. .agents/ 변종 고유의 `--pick` graceful early-exit(D-17 옵션 b)가 정상 동작하도록 가이드되어 있다. `<lens_templates>` 3개 템플릿에 `_Intent:` 줄 + `🔴 P1` 예시 행이 적용되었다. pairwise 의미적 동등성: 공통 토큰(PICK_MODE 탐지, 충돌 reject, INTENT_LINE case + emit, ACTION_ITEMS_PRINTF 가이드)이 양쪽 파일에 모두 존재하며, 의도적 차이(graceful exit vs AskUserQuestion, `<constraints>`/`<lens_templates>`/`argument-hint`)는 plan 의도와 일치한다. `.agents/skills/sg-learn/SKILL.md`는 변경되지 않았다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 인자 → Bash 변수 | `$ARGUMENTS` / `$LENS_RAW` / `$EXTRA_LENS_CODES`는 Skill 호출 시 인자에서 유래. 내부 신뢰 영역. |
| `--pick` 토큰 → graceful early-exit | .agents/ 환경에서는 AskUserQuestion이 부재하므로 사용자 입력 surface가 추가되지 않음 (graceful exit 경로). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | (N/A) | .agents/skills/sg-retro/SKILL.md text edits | accept | Threat model: N/A for SKILL.md text edits. Phase 42 T-42-01과 동일 — 새로운 외부 입력 surface 없음. `--pick` 토큰은 기존 `$ARGUMENTS` 파이프라인에 합류하며, .agents/ 변종에서는 AskUserQuestion이 호출되지 않으므로 추가 입력 surface도 없음. 인증·자격증명·PII surface 없음. |
</threat_model>

<verification_steps>
## D-18 검증 시나리오 (.agents/ 환경 변종 — Task 3에서 실행)

1. **`--pick` graceful early-exit (D-05, D-17 — .agents/ 변종 고유)** — `Skill(skill="sg-retro" via .agents, args="43 --pick")`: AskUserQuestion 미호출, stderr `--pick is not supported in this environment ...` + exit 1, lessons 미생성.
2. **`--pick` + positional 충돌 (D-03)** — `Skill(skill="sg-retro" via .agents, args="43 --pick ssc")`: plan 43-01과 동일한 stderr 메시지 + exit 1.
3. **`--pick` 위치 무관 (D-02)** — `Skill(skill="sg-retro" via .agents, args="43 ssc --pick")`: 동일 충돌 reject.
4. **smart default 보존 (D-04)** — `Skill(skill="sg-retro" via .agents, args="43")`: `dspm ssc` 자동 진입, 변경 없음.
5. **sg-learn pass-through (D-11)** — `Skill(skill="sg-learn" via .agents, args="43 --pick")`: forward → sg-retro graceful exit. `.agents/skills/sg-learn/SKILL.md` 미변경.
6. **0-selection silent exit (D-06)** — .agents/ 환경에서는 AskUserQuestion이 호출되지 않으므로 0-selection 분기 자체가 발생하지 않음 (시나리오 1으로 covered).
7. **DISPLAY-01 P1 emoji prefix (D-08, D-09)** — `<lens_templates>` 예시 행 + ACTION_ITEMS_PRINTF 가이드 모두 `🔴 P1` 사용. lessons 파일 실제 생성 시 P1 행이 `| 🔴 P1 |`로 시작.
8. **DISPLAY-02 intent line (D-12~D-15)** — `<lens_templates>` 3개 템플릿 + Step 6 INTENT_LINE case + redirect block printf 모두 적용. lessons 파일 실제 생성 시 `_Intent:` 줄이 `_Captured:` 다음에 위치.
9. **pairwise diff 의미적 동등성 (D-16)** — 두 파일에 공통 토큰(PICK_MODE, 충돌 reject 메시지, INTENT_LINE 3개, ACTION_ITEMS_PRINTF `🔴 P1` 가이드)이 모두 존재. 의도적 차이(`skills/` AskUserQuestion vs `.agents/` graceful exit, `<constraints>`/`<lens_templates>`/`argument-hint` 존재)는 plan 의도와 일치.
</verification_steps>

<success_criteria>
- `.agents/skills/sg-retro/SKILL.md`의 frontmatter `argument-hint`가 `--pick` 사용법 + graceful exit 안내를 포함하도록 갱신되었다.
- `<constraints>` 블록 첫 줄이 `--pick` graceful early-exit 동작을 명시하도록 갱신되었다 (D-17 옵션 b — Phase 42 retro prose drift 교훈 적용).
- `<objective>` 블록 본문에 `--pick` graceful exit 한 문장이 추가되었다.
- Step 1의 첫 bash 블록이 plan 43-01과 byte-identical PICK_MODE 탐지 + 토큰 제거 + 정상 positional 파싱으로 확장되었다 (D-01, D-02).
- Step 2 smart default 분기 **위**에 `--pick` + positional lens 충돌 reject 분기가 신설되었다 (D-03, plan 43-01과 동일 메시지·동일 exit code).
- Step 5의 `ANALYZE_LENS_RAN=false` 직후에 .agents/ 변종 고유의 graceful early-exit 분기(AskUserQuestion 미호출, stderr 안내 + exit 1)가 신설되었다 (D-05, D-06, D-17).
- Step 6의 LENS_NAME case 직후에 INTENT_LINE case가 plan 43-01과 byte-identical로 추가되었다 (D-12, D-14).
- Step 6의 single `>>` redirect block 내부에서 `_Captured:` printf 직후에 INTENT_LINE printf 한 줄이 삽입되었다 (D-13, D-21 보존).
- Step 6의 ACTION_ITEMS_PRINTF 주석이 `🔴 P1` prefix 가이드로 확장되었다 (D-08, D-09, D-11).
- `<lens_templates>` 3개 템플릿 각각에 `_Intent:` 줄이 추가되었고, Action Items P1 예시 행이 `| 🔴 P1 |`로 갱신되었다.
- `<success_criteria>` 블록에 신규 항목 3개(#8 `--pick` graceful exit, #9 P1 emoji prefix, #10 lens intent line)가 추가되었고, 기존 7개 항목(#1~#7) 본문은 byte-identical로 보존되었다.
- Phase 42 carry-forward 영역(Step 1 STATE.md parsing 블록, Step 2 lens case 매핑 + dropped reject 분기 + VALID_EXTRAS inner case + smart default 분기, Step 3/3b/4 전체, Step 5 sub-block 본문 + ANALYZE_LENS_RAN flag, Step 6 LENS_NAME case + RUN_SUFFIX + LENS_HEADER + 신규 파일 헤더 + auto-suggest)은 byte-identical로 보존되었다.
- `.agents/skills/sg-learn/SKILL.md`는 변경되지 않았다 (D-11 thin pass-through 보존).
- `.agents/skills/sg-retro/SKILL.md`는 `skills/sg-retro/SKILL.md`와 의미적으로 동등하다: 공통 토큰(PICK_MODE 탐지, 충돌 reject, INTENT_LINE case + emit, ACTION_ITEMS_PRINTF 가이드)이 양쪽 파일에 모두 존재. 의도적 차이(graceful exit vs AskUserQuestion placeholder, `<constraints>`/`<lens_templates>`/`argument-hint` 존재 여부)는 plan 의도와 일치 (D-16 pairwise convention).
</success_criteria>

<output>
Create `.planning/phases/43-pick-display-polish/43-02-SUMMARY.md` when done.

**중요 (D-16 + Phase 42 D-10 carry-forward staging 제약):** 본 plan(43-02)과 plan 43-01은 **동일 commit에 staged** 되어야 한다. 단독 commit 금지. plan 43-01이 완료될 때까지 commit 보류하거나, 두 plan을 별도 커밋으로 만들지 않고 마지막에 합쳐 staging한다. 실행자는 자신의 task가 끝나도 `git commit`을 단독으로 호출하지 말고, phase 전체가 완료된 후 commit하도록 한다. (참고: GSD execute-plan은 phase 단위 commit을 사용하므로 자연스럽게 충족됨.)
</output>
