---
phase: 31-cleanup-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
  - README.ko.md
  - CHANGELOG.md
  - .claude-plugin/plugin.json
autonomous: true
requirements:
  - CLEAN-01
  - DOC-01
  - DOC-02
  - DOC-03
must_haves:
  truths:
    - "CLAUDE.md에 python3 또는 .py 참조가 0건이다 (hooks/skills 경로 제외)"
    - "README.md L201, L216의 'Python scripts' 표현이 'Node.js scripts (CommonJS .cjs)'로 교체되어 있다"
    - "README.ko.md L200, L215의 'Python 스크립트' 표현이 'Node.js 스크립트(CommonJS .cjs)'로 교체되어 있다"
    - "CHANGELOG.md 최상단에 ## [0.0.38] 섹션이 존재한다"
    - ".claude-plugin/plugin.json version 필드가 '0.0.38'이다"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Python 참조 제거, Node.js/.cjs 기반 서술로 갱신"
      contains: "hooks/*.cjs"
    - path: "README.md"
      provides: "Codex/Gemini 설치 섹션 Python 참조 제거"
      contains: "Node.js scripts (CommonJS .cjs)"
    - path: "README.ko.md"
      provides: "Codex/Gemini 설치 섹션 Python 참조 제거 (한국어)"
      contains: "Node.js 스크립트(CommonJS .cjs)"
    - path: "CHANGELOG.md"
      provides: "v2.4 마일스톤 요약 섹션"
      contains: "0.0.38"
    - path: ".claude-plugin/plugin.json"
      provides: "버전 0.0.38 bump"
      contains: "\"version\": \"0.0.38\""
  key_links:
    - from: "CHANGELOG.md"
      to: ".claude-plugin/plugin.json"
      via: "CLAUDE.md 배포 트리거 규칙 — 두 파일을 함께 업데이트해야 한다"
      pattern: "0\\.0\\.38"
---

<objective>
v2.4 Hooks Node Migration 완료에 따른 문서 정비. `hooks/*.py` 4개는 quick task 260525-vp6(commit 877a666)에서 이미 삭제 완료되어 CLEAN-01의 파일 삭제 부분은 처리됨.

본 플랜은 남은 문서 참조 갱신만 수행한다:
- CLAUDE.md: Python/`.py` 참조 7개 위치를 Node.js/`.cjs` 기반으로 교체 (D-01, per CLEAN-01)
- README.md: L201, L216 `Python scripts` → `Node.js scripts (CommonJS .cjs)` (D-02, per DOC-01)
- README.ko.md: L200, L215 `Python 스크립트` → `Node.js 스크립트(CommonJS .cjs)` (D-02, per DOC-01)
- CHANGELOG.md: ## [0.0.38] v2.4 마일스톤 요약 섹션 추가 (D-03/D-04/D-05, per DOC-02)
- .claude-plugin/plugin.json: version → "0.0.38" (D-05, per DOC-03)

Purpose: v2.4 마일스톤 종결. Python 런타임 의존성이 완전히 제거되었음을 문서가 정확히 반영해야 한다.
Output: 5개 파일 갱신, python3 grep 0건 달성, 버전 0.0.38 bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-cleanup-docs/31-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md Python 참조 → Node.js/.cjs 전면 교체 (D-01, CLEAN-01)</name>
  <files>CLAUDE.md</files>

  <read_first>
  CLAUDE.md 전체를 읽어 현재 상태를 확인한다. 교체 대상 7개 위치:
  - L16: `Bash/Python/Markdown`
  - L28-30: hooks 항목 (`hooks/*.py`, `rule_runner.py`, `lessons_ranker.py`)
  - L88: `rule_runner.py`는 `prompt` 이벤트는 지원하지 않는다
  - L106: `Python 스크립트를 호출해`
  - L110-112: stop_hook.py, rule_runner.py, lessons_ranker.py 3줄
  - L118-124: 데이터 흐름 코드블록 (lessons_ranker.py, stop_hook.py, rule_runner.py)
  - L135: `stop_hook.py`는 이 파일의 마지막 행을
  - L139: `__file__` 기반 경로로 폴백
  - L166-175: Development Commands 4개 python3 명령
  보존 대상: `<!-- GSD:*-start/end -->` 태그, Conventions 섹션의 `rule_runner.cjs` 교체 후 `prompt` 이벤트 언급 라인(L88 → rule_runner.cjs로만 파일명 교체)
  </read_first>

  <action>
  아래 교체를 정확히 적용한다. 각 항목은 before → after 쌍으로 지정된다.

  **[1] L16 — Tech stack 줄 (D-01)**
  Before: `- **Tech stack**: Claude Code 플러그인 시스템 (skills + hooks). Bash/Python/Markdown 위주.`
  After:  `- **Tech stack**: Claude Code 플러그인 시스템 (skills + hooks). Bash/Node.js/Markdown 위주.`

  **[2] L28 — Hooks 항목 첫 줄 (D-01)**
  Before: `- **Hooks**: \`hooks/*.py\` — Python 3. Claude Code의 PreToolUse/Stop/SubagentStop 이벤트에서 실행되며 JSON으로 응답`
  After:  `- **Hooks**: \`hooks/*.cjs\` — Node.js 18+. Claude Code의 PreToolUse/Stop/SubagentStop 이벤트에서 실행되며 JSON으로 응답`

  **[3] L29 — Rules 항목의 rule_runner.py (D-01)**
  Before: `- **Rules**: `.claude/sg-rule.*.local.md` — YAML frontmatter(name, enabled, event, pattern/conditions, action)가 있는 Markdown. `rule_runner.py`가 평가`
  After:  `- **Rules**: `.claude/sg-rule.*.local.md` — YAML frontmatter(name, enabled, event, pattern/conditions, action)가 있는 Markdown. `rule_runner.cjs`가 평가`

  **[4] L30 — Lessons 항목의 lessons_ranker.py (D-01)**
  Before: `- **Lessons**: `.planning/lessons/*.md` — phase 회고 결과. `lessons_ranker.py`가 frequency·recency·severity 기반 weighted score로 순위를 매겨 `sg-plan`에 주입`
  After:  `- **Lessons**: `.planning/lessons/*.md` — phase 회고 결과. `lessons_ranker.cjs`가 frequency·recency·severity 기반 weighted score로 순위를 매겨 `sg-plan`에 주입`

  **[5] L88 — Conventions 섹션 rule_runner.py (D-01)**
  Before: `` `rule_runner.py`는 `prompt` 이벤트는 지원하지 않는다. ``
  After:  `` `rule_runner.cjs`는 `prompt` 이벤트는 지원하지 않는다. ``

  **[6] L106 — Skills 레이어 Python 스크립트 언급 (D-01)**
  Before: `Claude Code가 SKILL.md를 실행 지시문으로 해석하며, GSD skill, Superpowers skill, 또는 Python 스크립트를 호출해 단계 간 인계를 수행한다.`
  After:  `Claude Code가 SKILL.md를 실행 지시문으로 해석하며, GSD skill, Superpowers skill, 또는 Node.js 스크립트(.cjs)를 호출해 단계 간 인계를 수행한다.`

  **[7] L110 — stop_hook.py (D-01)**
  Before: `- \`stop_hook.py\` — Stop/SubagentStop 이벤트에서 \`transcript_matcher.py\`로 신호를 감지해 \`systemMessage\`로 다음 단계를 안내한다. \`.planning/config.json\`의 \`super_gsd.auto_advance: false\`로 비활성화 가능.`
  After:  `- \`stop_hook.cjs\` — Stop/SubagentStop 이벤트에서 \`transcript_matcher.cjs\`로 신호를 감지해 \`systemMessage\`로 다음 단계를 안내한다. \`.planning/config.json\`의 \`super_gsd.auto_advance: false\`로 비활성화 가능.`

  **[8] L111 — rule_runner.py (D-01)**
  Before: `- \`rule_runner.py\` — PreToolUse 이벤트에서 \`.claude/sg-rule.*.local.md\` 규칙을 평가한다.`
  After:  `- \`rule_runner.cjs\` — PreToolUse 이벤트에서 \`.claude/sg-rule.*.local.md\` 규칙을 평가한다.`

  **[9] L112 — lessons_ranker.py (D-01)**
  Before: `- \`lessons_ranker.py\` — CLI 도구. \`--top N\`으로 weighted top-N 패턴을 JSON lines로 출력하거나 \`--archive\`로 마일스톤 아카이브를 생성한다.`
  After:  `- \`lessons_ranker.cjs\` — CLI 도구. \`--top N\`으로 weighted top-N 패턴을 JSON lines로 출력하거나 \`--archive\`로 마일스톤 아카이브를 생성한다.`

  **[10] L118-124 — 데이터 흐름 코드블록 (D-01)**
  교체 대상: 코드블록 내 3줄
  Before:
  ```
  sg-plan → lessons_ranker.py → 이전 교훈 주입
  ...
  stop_hook.py → transcript_matcher.py → systemMessage (다음 단계 안내)
  rule_runner.py → .claude/*.local.md 규칙 평가 → warn/block
  ```
  After:
  ```
  sg-plan → lessons_ranker.cjs → 이전 교훈 주입
  ...
  stop_hook.cjs → transcript_matcher.cjs → systemMessage (다음 단계 안내)
  rule_runner.cjs → .claude/*.local.md 규칙 평가 → warn/block
  ```
  (나머지 줄은 그대로 유지)

  **[11] L135 — HANDOFF.md 설명 내 stop_hook.py (D-01)**
  Before: `` `stop_hook.py`는 이 파일의 마지막 행을 읽어 현재 단계와 이전 상태를 파악한다. ``
  After:  `` `stop_hook.cjs`는 이 파일의 마지막 행을 읽어 현재 단계와 이전 상태를 파악한다. ``

  **[12] L139 — 환경 변수 __file__ 폴백 (D-01)**
  Before: `- \`CLAUDE_PLUGIN_ROOT\` — hooks에서 플러그인 루트 경로를 찾는 데 사용. 부재 시 \`__file__\` 기반 경로로 폴백.`
  After:  `- \`CLAUDE_PLUGIN_ROOT\` — hooks에서 플러그인 루트 경로를 찾는 데 사용. 부재 시 \`__dirname\` (CommonJS) 기반 경로로 폴백.`

  **[13] L164-175 — Development Commands 섹션 전체 교체 (D-01)**
  Before (코드블록 내용):
  ```bash
  # hooks 직접 테스트 (stdin에 JSON 입력)
  echo '{"tool_name":"Bash","tool_input":{"command":"awk \"{print $1}\""}}' | python3 hooks/rule_runner.py

  # stop_hook 직접 테스트 (Stop 이벤트 시뮬레이션)
  echo '{"session_id":"test","stop_hook_active":true}' | python3 hooks/stop_hook.py

  # lessons weighted ranking
  python3 hooks/lessons_ranker.py --top 5 .planning/lessons/*.md

  # 마일스톤 lessons 아카이브
  python3 hooks/lessons_ranker.py --archive --milestone v1.2 .planning/lessons/*.md
  ```
  After (코드블록 내용):
  ```bash
  # hooks 직접 테스트 (stdin에 JSON 입력)
  echo '{"tool_name":"Bash","tool_input":{"command":"awk \"{print $1}\""}}' | node hooks/rule_runner.cjs

  # stop_hook 직접 테스트 (Stop 이벤트 시뮬레이션)
  echo '{"session_id":"test","stop_hook_active":true}' | node hooks/stop_hook.cjs

  # lessons weighted ranking
  node hooks/lessons_ranker.cjs --top 5 .planning/lessons/*.md

  # 마일스톤 lessons 아카이브
  node hooks/lessons_ranker.cjs --archive --milestone v1.2 .planning/lessons/*.md
  ```
  </action>

  <verify>
    <automated>grep -n 'python3\|\.py"' /Users/gyuha/workspace/super-gsd/CLAUDE.md | grep -v 'GSD:' | grep -v '\.py\b.*hook.*설정' || echo "PASS: python3/.py 참조 없음"</automated>
    <automated>grep -c 'hooks/\*\.cjs' /Users/gyuha/workspace/super-gsd/CLAUDE.md</automated>
    <automated>grep -c 'Node\.js' /Users/gyuha/workspace/super-gsd/CLAUDE.md</automated>
  </verify>

  <done>
  - CLAUDE.md에 `python3` 또는 `hooks/*.py` 패턴이 0건
  - `hooks/*.cjs` 문자열이 Technology Stack 섹션에 존재
  - Development Commands 4개 명령이 `node hooks/*.cjs` 형태
  - `__dirname` (CommonJS) 폴백 표현이 환경 변수 섹션에 존재
  - `<!-- GSD:*-start/end -->` HTML 태그는 수정 없이 보존
  </done>
</task>

<task type="auto">
  <name>Task 2: README.md + README.ko.md Python 참조 교체 (D-02, DOC-01)</name>
  <files>README.md, README.ko.md</files>

  <read_first>
  README.md 현재 내용 중 L201, L216만 확인. L255(Roadmap Phase 15 과거 항목)는 보존.
  README.ko.md 현재 내용 중 L200, L215만 확인. L254(로드맵 Phase 15 과거 항목)는 보존.
  두 파일 모두 Roadmap/로드맵 섹션의 역사 기록은 수정하지 않는다.
  </read_first>

  <action>
  **README.md (D-02)**

  L201 — Codex 섹션 설명줄:
  Before: ``.codex/hooks.json` registers Stop and PreToolUse hooks automatically. The \`hooks/\` directory contains the Python scripts that the hooks invoke — omitting it causes all hooks to fail silently. Use \`$sg-retro\`, \`$sg-plan\`, \`$sg-execute\`, \`$sg-review\`, \`$sg-start\`, \`$sg-status\` skill syntax. See \`AGENTS.md\` for the full workflow.`
  After: ``.codex/hooks.json` registers Stop and PreToolUse hooks automatically. The \`hooks/\` directory contains the Node.js scripts (CommonJS .cjs) that the hooks invoke — omitting it causes all hooks to fail silently. Use \`$sg-retro\`, \`$sg-plan\`, \`$sg-execute\`, \`$sg-review\`, \`$sg-start\`, \`$sg-status\` skill syntax. See \`AGENTS.md\` for the full workflow.`

  L216 — Gemini 섹션 설명줄:
  Before: ``.gemini/settings.json` registers SessionEnd and BeforeTool hooks. The \`hooks/\` directory contains the Python scripts that the hooks invoke — omitting it causes all hooks to fail silently. Use \`.agents/skills/\` skills. See \`AGENTS.md\` for the full workflow.`
  After: ``.gemini/settings.json` registers SessionEnd and BeforeTool hooks. The \`hooks/\` directory contains the Node.js scripts (CommonJS .cjs) that the hooks invoke — omitting it causes all hooks to fail silently. Use \`.agents/skills/\` skills. See \`AGENTS.md\` for the full workflow.`

  L255 (Roadmap Phase 15) — 수정하지 않는다.

  **README.ko.md (D-02)**

  L200 — Codex 섹션 설명줄:
  Before: ``.codex/hooks.json`이 Stop 및 PreToolUse 훅을 자동 등록한다. `hooks/` 디렉터리에는 훅이 실행하는 Python 스크립트가 포함되어 있으며, 이를 생략하면 모든 훅이 자동으로 실패한다.`
  After: ``.codex/hooks.json`이 Stop 및 PreToolUse 훅을 자동 등록한다. `hooks/` 디렉터리에는 훅이 실행하는 Node.js 스크립트(CommonJS .cjs)가 포함되어 있으며, 이를 생략하면 모든 훅이 자동으로 실패한다.`
  (나머지 줄 내용 보존)

  L215 — Gemini 섹션 설명줄:
  Before: ``.gemini/settings.json`이 SessionEnd 및 BeforeTool 훅을 등록한다. `hooks/` 디렉터리에는 훅이 실행하는 Python 스크립트가 포함되어 있으며, 이를 생략하면 모든 훅이 자동으로 실패한다.`
  After: ``.gemini/settings.json`이 SessionEnd 및 BeforeTool 훅을 등록한다. `hooks/` 디렉터리에는 훅이 실행하는 Node.js 스크립트(CommonJS .cjs)가 포함되어 있으며, 이를 생략하면 모든 훅이 자동으로 실패한다.`
  (나머지 줄 내용 보존)

  L254 (로드맵 Phase 15) — 수정하지 않는다.
  </action>

  <verify>
    <automated>grep -n "Python scripts" /Users/gyuha/workspace/super-gsd/README.md || echo "PASS: README.md Python scripts 0건"</automated>
    <automated>grep -n "Python 스크립트" /Users/gyuha/workspace/super-gsd/README.ko.md | grep -v "Phase 15" || echo "PASS: README.ko.md Python 스크립트 0건 (Phase 15 제외)"</automated>
    <automated>grep -n "Node.js scripts (CommonJS .cjs)" /Users/gyuha/workspace/super-gsd/README.md</automated>
    <automated>grep -n "Node.js 스크립트(CommonJS .cjs)" /Users/gyuha/workspace/super-gsd/README.ko.md</automated>
  </verify>

  <done>
  - README.md L201, L216에 "Node.js scripts (CommonJS .cjs)" 표현이 존재
  - README.md L255 Roadmap 항목은 수정 없이 보존
  - README.ko.md L200, L215에 "Node.js 스크립트(CommonJS .cjs)" 표현이 존재
  - README.ko.md L254 로드맵 항목은 수정 없이 보존
  </done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG.md v2.4 요약 추가 + plugin.json 0.0.38 bump (D-03/D-04/D-05, DOC-02/DOC-03)</name>
  <files>CHANGELOG.md, .claude-plugin/plugin.json</files>

  <read_first>
  CHANGELOG.md 현재 최상단 (첫 번째 `## [0.0.37]` 섹션 위치 확인).
  .claude-plugin/plugin.json 현재 version 필드 확인 (현재 "0.0.37").
  </read_first>

  <action>
  **CHANGELOG.md (D-03/D-04/D-05)**

  파일 최상단(첫 번째 `## [` 블록 바로 위)에 다음 섹션을 삽입한다. 기존 내용은 그대로 보존한다.

  삽입 내용:
  ```
  ## [0.0.38] - 2026-05-26

  ### Changed (v2.4 Hooks Node Migration — milestone complete)

  - CLAUDE.md: Hooks 레이어 서술을 `hooks/*.cjs` / Node.js 18+ 기준으로 전면 갱신,
    Development Commands 예시 4개를 `node hooks/*.cjs` 기반으로 교체
  - README.md / README.ko.md: Codex·Gemini 설치 섹션의 "Python scripts" →
    "Node.js scripts (CommonJS .cjs)" 교체

  ### Summary: v2.4 Hooks Node Migration (Phases 28–31)

  - Phase 28: `hooks/{stop_hook,transcript_matcher,rule_runner,lessons_ranker}.cjs` 4개 신규 작성
  - Phase 29: `hooks/hooks.json`, `.codex/hooks.json`, `.gemini/settings.json` — python3 → node 전환
  - Phase 30: 8개 SKILL.md python3 → node 일괄 교체
  - Phase 31 / 260525-vp6: `.py` 삭제 + CLAUDE.md / README 문서 Node 기반 갱신

  ```

  위 블록 다음 줄이 `## [0.0.37]`로 시작하도록 한다. 기존 내용을 삭제하지 않는다.

  **.claude-plugin/plugin.json (D-05)**

  `"version"` 필드 값을 `"0.0.37"` → `"0.0.38"`로 교체한다. 파일의 다른 필드는 그대로 유지한다.
  </action>

  <verify>
    <automated>grep -n "0\.0\.38" /Users/gyuha/workspace/super-gsd/CHANGELOG.md | head -5</automated>
    <automated>grep -n "Summary: v2.4" /Users/gyuha/workspace/super-gsd/CHANGELOG.md</automated>
    <automated>grep '"version"' /Users/gyuha/workspace/super-gsd/.claude-plugin/plugin.json</automated>
    <automated>node -e "const p = require('/Users/gyuha/workspace/super-gsd/.claude-plugin/plugin.json'); console.log(p.version === '0.0.38' ? 'PASS' : 'FAIL: ' + p.version)"</automated>
  </verify>

  <done>
  - CHANGELOG.md 최상단에 `## [0.0.38] - 2026-05-26` 섹션이 존재
  - CHANGELOG.md에 `### Summary: v2.4 Hooks Node Migration (Phases 28–31)` 소섹션이 존재
  - CHANGELOG.md 기존 0.0.37 이하 내용은 보존
  - plugin.json `version` 필드가 `"0.0.38"`
  </done>
</task>

</tasks>

<threat_model>
## 신뢰 경계

| 경계 | 설명 |
|------|------|
| 문서 파일 → 개발자 읽기 | CLAUDE.md/README*.md는 신뢰된 개발자만 읽음. 외부 입력 없음. |
| plugin.json → Claude Code 파서 | version 필드만 변경. 구조 변경 없음. |

## STRIDE 위협 등록부

| 위협 ID | 카테고리 | 컴포넌트 | 처리 방침 | 완화 계획 |
|---------|---------|---------|----------|-----------|
| T-31-01 | Tampering | CHANGELOG.md 삽입 위치 | mitigate | 최상단 삽입 검증: `grep -n "0.0.38" CHANGELOG.md`로 첫 번째 `## [` 위에 위치 확인 |
| T-31-02 | Tampering | README.md L255 Roadmap 항목 보존 | mitigate | 교체 후 `grep -n "Phase 15.*Python" README.md`로 역사 항목 보존 확인 |
| T-31-03 | Tampering | plugin.json 구조 파괴 | mitigate | `node -e "require('./plugin.json')"` JSON 파싱 성공 확인 |
| T-31-04 | Information Disclosure | CLAUDE.md GSD:* HTML 태그 삭제 | accept | 태그는 GSD 도구가 섹션 경계 감지에 사용. 교체 대상이 아닌 줄이므로 위험 없음. |
| T-31-SC | Tampering | npm/pip/cargo 설치 | accept | 이 플랜은 패키지 설치 없음. 해당 없음. |
</threat_model>

<verification>
## Phase 31 전체 검증

Phase 31 완료 기준(ROADMAP.md SC #1~5):

**SC #1 — hooks/*.py 0건 (이미 완료, 877a666)**
```bash
ls /Users/gyuha/workspace/super-gsd/hooks/*.py 2>/dev/null | wc -l
# 기대값: 0
```

**SC #2 — python3 전역 grep 0건**
```bash
grep -rn 'python3' /Users/gyuha/workspace/super-gsd/hooks/ /Users/gyuha/workspace/super-gsd/skills/ /Users/gyuha/workspace/super-gsd/.agents/skills/ /Users/gyuha/workspace/super-gsd/.codex/ /Users/gyuha/workspace/super-gsd/.gemini/ /Users/gyuha/workspace/super-gsd/CLAUDE.md /Users/gyuha/workspace/super-gsd/README.md /Users/gyuha/workspace/super-gsd/README.ko.md 2>/dev/null
# 기대값: 0건
```

**SC #3 — CLAUDE.md Technology Stack/Architecture/Development Commands Node.js 기반**
```bash
grep -c 'hooks/\*\.cjs\|node hooks/' /Users/gyuha/workspace/super-gsd/CLAUDE.md
# 기대값: 2 이상
```

**SC #4 — README.md/README.ko.md Python 요구사항 제거**
```bash
grep -n "Python scripts" /Users/gyuha/workspace/super-gsd/README.md || echo "PASS"
grep -n "Python 스크립트" /Users/gyuha/workspace/super-gsd/README.ko.md | grep -v "Phase 15" || echo "PASS"
```

**SC #5 — CHANGELOG.md v2.4 마일스톤 항목 존재**
```bash
grep -c "0\.0\.38" /Users/gyuha/workspace/super-gsd/CHANGELOG.md
# 기대값: 1 이상
```
</verification>

<success_criteria>
- CLAUDE.md: `python3` 또는 `hooks/*.py` 참조 0건 (보존 대상 GSD HTML 태그 제외)
- CLAUDE.md: `hooks/*.cjs`, `Node.js 18+`, `node hooks/*.cjs`, `__dirname (CommonJS)` 표현 존재
- README.md: L201, L216에 "Node.js scripts (CommonJS .cjs)" 포함, L255 역사 항목 보존
- README.ko.md: L200, L215에 "Node.js 스크립트(CommonJS .cjs)" 포함, L254 역사 항목 보존
- CHANGELOG.md 최상단에 `## [0.0.38] - 2026-05-26` 섹션 및 `### Summary: v2.4` 소섹션 존재
- plugin.json `version` = `"0.0.38"`
- `grep -rn 'python3' hooks/ skills/ .agents/skills/ .codex/ .gemini/ CLAUDE.md README.md README.ko.md` → 0건
</success_criteria>

<output>
완료 후 `.planning/phases/31-cleanup-docs/31-01-SUMMARY.md`를 생성한다.
</output>
