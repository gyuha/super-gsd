# Lessons: Phase 45 (2026-05-31)

## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: 2026-05-31T13:15:45Z_
_Intent: capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase._

### Decisions
- grill = sg-plan 메인 컨텍스트, phase 해소 직후 / discuss Agent 호출 직전 배치 (D-01, `45-CONTEXT.md`)
- 합의 결과는 별도 파일 없이 gsd-discuss-phase Agent 프롬프트에 inline 주입 — Non-invasive 유지 (D-13/D-14)
- `skills/` + `.agents/skills/` 두 미러 파일을 동일 커밋(`3ed7221`)에서 변경 (D-15, pairwise sync)

### Surprises
- plan이 `.agents` grill을 "Step 1.6"으로 라벨했으나 동시에 "Step 1.5 이전" 배치를 요구 → `1.6 > 1.5` 산술 모순이 실행 중 발각, Step 1.2로 정정 (`45-01-PLAN.md:173`, `45-01-SUMMARY.md` deviation)
- 두 SKILL.md가 이미 구조적으로 divergent — Visual Companion이 `.agents`에만 존재해 grill 삽입 위치·번호가 파일마다 다름 (D-03)

### Patterns
- 동일 grill 동작을 두 메커니즘으로 표면화: AskUserQuestion(`.claude`) vs 프로즈 번호 선택 폴백(`.agents`) — 확립된 분기 패턴
- 하위 Agent 프롬프트에 "locked context — do NOT re-ask" 문구로 합의를 주입하는 패턴 (`45-01-SUMMARY.md`)

### Mistakes
- plan 단계에서 부여한 step 번호(1.6)가 요구 순서(1.5 이전)와 모순 — plan review가 산술 일관성을 못 잡고 실행 중 사용자 확인으로 수습
- 미러 파일 분량 비대칭(`.agents` +43줄 vs `.claude` +25줄, `git show 3ed7221`)이 divergence를 넓혔으나 plan에서 리스크로 플래그되지 않음

### Action Items
| priority | item | next step |
|----------|------|-----------|
| 🔴 P1 | 두 sg-plan SKILL.md 구조 divergence 정합 | 별도 phase/마일스톤으로 deferred (`45-CONTEXT.md` deferred 섹션) |
| P2 | plan-phase에 step 번호 산술-순서 일관성 체크 부재 | sg-rule draft 또는 plan-checker 항목 후보 |
| P3 | grill 단계 README/문서 미반영 | 배포 트리거 시 quick task |

## Lens: Start/Stop/Continue
_Captured: 2026-05-31T13:15:45Z_
_Intent: surface behavior changes — what to start, stop, or continue doing next phase._

### Start
- plan review에서 step 번호의 산술 순서가 요구 배치와 일치하는지 검증 (라벨 1.6 ↔ "1.5 이전" 모순 자동 점검)
- 고정 번호 대신 삽입 앵커 표현("X 직후/직전")을 우선 사용해 번호 충돌 회피

### Stop
- 요구 순서와 모순되는 step 번호 부여 중단 (1.6을 "1.5 이전"에 두는 식)
- 미러 파일에 비대칭 분량을 추가하면서 divergence 점검 없이 진행하는 것 중단

### Continue
- pairwise sync 동일 커밋 규칙(D-15) — code review 블로커 회피에 효과적
- Non-invasive 프롬프트 주입으로 gsd-discuss-phase 미수정 유지
- 실행 중 모순 발견 시 사용자 확인 게이트로 deviation 처리 (조용히 강행하지 않음)

### Action Items
| priority | item | next step |
|----------|------|-----------|
| 🔴 P1 | step 번호 순서 모순 방지 규칙 부재 | sg-rule draft 또는 CLAUDE.md 컨벤션 추가 |
| P2 | 미러 파일 분량 비대칭 시 divergence 검토 누락 | plan/review 체크리스트 항목 후보 |
