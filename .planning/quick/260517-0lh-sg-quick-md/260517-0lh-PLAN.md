---
phase: quick-260517-0lh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-quick.md
autonomous: true
requirements:
  - bug-fix-task-to-agent
  - bug-fix-step-reorder
  - bug-fix-pending-sha
  - bug-fix-state-dir-link
  - bug-fix-unimplemented-flags

must_haves:
  truths:
    - "Task() 호출이 Agent()로 교체되고 subagent_type이 gsd-planner로 설정된다"
    - "STATE.md 업데이트와 커밋이 Skill 호출 전에 실행된다"
    - "커밋 후 실제 SHA가 STATE.md에 기록된다"
    - "STATE.md 디렉토리 링크가 실제 디렉토리명(QUICK_ID-SLUG)과 일치한다"
    - "--discuss/--research/--validate/--full 플래그가 설정된 경우 gsd-quick Skill로 위임된다"
  artifacts:
    - path: "commands/sg-quick.md"
      provides: "5개 버그가 모두 수정된 sg-quick 커맨드 정의"
  key_links:
    - from: "commands/sg-quick.md Step 4"
      to: "Agent() 호출"
      via: "subagent_type=gsd-planner"
    - from: "commands/sg-quick.md STATE.md 업데이트"
      to: "Skill 호출 이전 위치"
      via: "단계 순서 재배치"
---

<objective>
sg-quick.md의 코드 리뷰에서 발견된 5개 버그를 수정한다.

Purpose: sg-quick 커맨드가 실제로 동작하도록 Critical 3건 + Important 2건을 모두 패치한다.
Output: 수정된 commands/sg-quick.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/super-gsd/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5개 버그 일괄 수정 — sg-quick.md 전면 재작성</name>
  <files>commands/sg-quick.md</files>
  <action>
현재 commands/sg-quick.md를 읽고 아래 5개 수정 사항을 적용하여 전체 파일을 교체한다.

**Bug 1 — Task() → Agent(), subagent_type 수정 (Step 4):**
- `Task(` → `Agent(`
- `subagent_type="general-purpose"` → `subagent_type="gsd-planner"`

**Bug 2 — 단계 순서 재배치:**
현재 순서: ... Step 7(Skill 호출) → Step 8(STATE.md 업데이트) → Step 9(커밋) → Step 10(완료)
새 순서:
1. 인자 파싱
2. gsd-sdk 초기화
3. 디렉토리 생성
4. gsd-planner Agent 스폰 → PLAN.md 생성
5. PLAN.md 읽기
6. handoff 프롬프트 빌드
7. Bug 5 플래그 위임 처리 (Skill 호출 전 분기)
8. STATE.md Quick Tasks Completed 테이블 업데이트 (Skill 호출 전!)
9. PLAN.md 커밋 → SHA 획득 → STATE.md 행 SHA 패치 → STATE.md 커밋
10. Skill(skill="superpowers:executing-plans", ...) 호출
11. 완료 메시지 출력

**Bug 3 — 실제 커밋 SHA 기록:**
Step 9를 두 단계로 분리한다:
```bash
# Step 9a: PLAN.md만 먼저 커밋
git add "$PLAN_PATH"
git commit -m "quick($QUICK_ID): $DESCRIPTION"
COMMIT_SHA=$(git rev-parse --short HEAD)

# Step 9b: STATE.md 행을 실제 SHA로 삽입 후 STATE.md 커밋
DIR_NAME=$(basename "$TASK_DIR")
NEW_ROW="| $QUICK_ID | $DESCRIPTION | $(date +%Y-%m-%d) | $COMMIT_SHA | [$DIR_NAME](.planning/quick/$DIR_NAME/) |"
# NEW_ROW를 Quick Tasks Completed 테이블 마지막 행 뒤에 삽입
git add .planning/STATE.md
git commit -m "quick($QUICK_ID): update STATE.md"
```

**Bug 4 — STATE.md 디렉토리 링크 수정:**
`DIR_NAME=$(basename "$TASK_DIR")`으로 실제 디렉토리명(QUICK_ID-SLUG 형태)을 추출하여
링크를 `[$DIR_NAME](.planning/quick/$DIR_NAME/)`으로 사용한다.
`$SLUG` 단독 사용 금지.

**Bug 5 — 미구현 플래그 위임:**
Step 1(인자 파싱) 직후, Step 2(gsd-sdk 초기화) 이전에 플래그 체크 분기를 삽입한다:
```bash
if [ -n "$DISCUSS_FLAG" ] || [ -n "$RESEARCH_FLAG" ] || [ -n "$VALIDATE_FLAG" ] || [ -n "$FULL_FLAG" ]; then
  Skill(skill="gsd-quick", args="$ARGUMENTS")
  exit 0
fi
```
이 분기가 실행되면 이후 모든 단계는 건너뛴다.

**<process> 섹션 전체를 위 순서에 맞게 재작성한다.** 각 단계 번호를 1~11로 갱신하고,
success_criteria의 3번 항목도 "단일 커밋이 아닌 두 커밋(PLAN.md 커밋 + STATE.md 커밋)"을 반영하도록 수정한다.
  </action>
  <verify>
    <automated>
cd /Users/gyuha/workspace/super-gsd && grep -n 'Agent(' commands/sg-quick.md | head -5
grep -n 'subagent_type="gsd-planner"' commands/sg-quick.md
grep -n 'gsd-quick.*ARGUMENTS\|DISCUSS_FLAG\|RESEARCH_FLAG' commands/sg-quick.md | head -5
grep -n 'rev-parse' commands/sg-quick.md
grep -n 'basename.*TASK_DIR\|DIR_NAME' commands/sg-quick.md
grep -c 'Task(' commands/sg-quick.md || echo "0"
    </automated>
  </verify>
  <done>
- commands/sg-quick.md에서 Task( 호출이 0건이어야 한다
- Agent(와 subagent_type="gsd-planner"가 존재한다
- DISCUSS_FLAG/RESEARCH_FLAG 플래그 위임 분기(gsd-quick Skill 위임)가 Skill(superpowers:executing-plans) 호출보다 앞에 위치한다
- git rev-parse --short HEAD로 실제 SHA를 읽는 코드가 존재한다
- basename "$TASK_DIR"로 DIR_NAME을 추출하는 코드가 존재한다
- STATE.md 업데이트가 Skill(superpowers:executing-plans) 호출보다 앞 단계에 위치한다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| $ARGUMENTS → bash 변수 | 사용자 입력이 sed/xargs로 처리됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0lh-01 | Tampering | STATE.md 행 삽입 | accept | STATE.md는 append-only 관리 파일, 낮은 위험 |
| T-0lh-02 | Spoofing | git commit SHA | mitigate | git rev-parse --short HEAD로 실제 SHA 사용 |
</threat_model>

<verification>
```bash
cd /Users/gyuha/workspace/super-gsd

# Bug 1: Task() 제거 확인
echo "=== Bug 1: Task() calls ==="
grep -c 'Task(' commands/sg-quick.md || echo "0"

# Bug 1: Agent() + gsd-planner 확인
echo "=== Bug 1: Agent() with gsd-planner ==="
grep -n 'Agent(' commands/sg-quick.md
grep -n 'gsd-planner' commands/sg-quick.md

# Bug 2: Skill 호출 위치 vs STATE.md 업데이트 위치
echo "=== Bug 2: Step ordering ==="
grep -n 'STATE\|superpowers:executing-plans\|rev-parse\|DIR_NAME\|DISCUSS_FLAG' commands/sg-quick.md | head -20

# Bug 3: 실제 SHA
echo "=== Bug 3: real SHA ==="
grep -n 'rev-parse' commands/sg-quick.md

# Bug 4: 디렉토리 링크
echo "=== Bug 4: DIR_NAME ==="
grep -n 'basename\|DIR_NAME' commands/sg-quick.md

# Bug 5: 플래그 위임
echo "=== Bug 5: flag delegation ==="
grep -n 'gsd-quick.*ARGUMENTS\|Skill.*gsd-quick' commands/sg-quick.md
```
</verification>

<success_criteria>
1. commands/sg-quick.md에서 `Task(` 호출이 0건
2. `Agent(` + `subagent_type="gsd-planner"` 존재
3. 플래그 위임 분기(Skill gsd-quick)가 Skill superpowers:executing-plans 보다 앞 단계에 위치
4. `git rev-parse --short HEAD`로 실제 커밋 SHA를 STATE.md에 기록
5. `basename "$TASK_DIR"`로 디렉토리명 추출 후 STATE.md 링크에 사용
6. STATE.md 업데이트 단계가 Skill superpowers:executing-plans 호출 단계보다 앞에 위치
</success_criteria>

<output>
완료 후 `.planning/quick/260517-0lh-sg-quick-md/260517-0lh-SUMMARY.md` 생성
</output>
