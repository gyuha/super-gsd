---
phase: 18-sg-parallel-execute
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - commands/sg-execute.md
autonomous: true
requirements:
  - TE-02a
  - TE-02b
  - TE-03a

must_haves:
  truths:
    - "sg-execute.md Step 9의 TODO 주석이 실제 Skill() 호출로 교체된다"
    - "임시 echo 플레이스홀더와 TODO 라인이 모두 제거된다"
    - "PARALLEL_GROUPS가 비어 있지 않을 때 Skill() 호출 후 return하여 순차 경로로 흘러가지 않는다"
    - "PARALLEL_GROUPS가 비어 있을 때 기존 순차 실행 경로(superpowers:executing-plans)가 그대로 실행된다"
  artifacts:
    - path: "commands/sg-execute.md"
      provides: "Step 9 병렬/순차 라우팅 분기 (활성화된 상태)"
      contains: "Skill(skill=\"sg-parallel-execute\""
  key_links:
    - from: "commands/sg-execute.md Step 9"
      to: "skills/sg-parallel-execute/SKILL.md"
      via: "Skill(skill=\"sg-parallel-execute\", args=\"$GROUPS_JSON_FILE\")"
---

<objective>
`commands/sg-execute.md` Step 9의 `if [ -n "$PARALLEL_GROUPS" ]` 블록을 수정하여 Phase 17이 삽입한 TODO 플레이스홀더를 실제 `Skill()` 호출로 활성화한다. 임시 echo 메시지와 TODO 주석을 제거하고, Skill() 호출 후 return하여 세션 제어가 sg-parallel-execute 스킬로 이전되고 순차 경로로 흘러가지 않도록 한다.

Purpose: TE-02a — sg-execute가 PARALLEL_GROUPS를 감지하면 sg-parallel-execute로 라우팅되는 경로를 완성한다.
Output: `commands/sg-execute.md` (수정)
</objective>

<execution_context>
이 플랜은 18-01-PLAN.md가 완료된 후(skills/sg-parallel-execute/SKILL.md 존재) 실행된다. sg-execute.md의 다른 로직(Step 0~8.5, idempotency 검사, HANDOFF.md 기록, lessons 주입)은 일체 수정하지 않는다.
</execution_context>

<context>
@commands/sg-execute.md
@.planning/phases/18-sg-parallel-execute/18-CONTEXT.md
</context>

<interfaces>
<!-- sg-execute.md Step 9의 현재 상태 (수정 대상 블록) -->

현재 Step 9 if 블록 (commands/sg-execute.md lines 245-255):
```bash
if [ -n "$PARALLEL_GROUPS" ]; then
  echo "=== 병렬 실행 경로 선택 ==="
  echo "PARALLEL_GROUPS: $PARALLEL_GROUPS"
  echo "sg-parallel-execute 스킬 라우팅 (Phase 18에서 구현 예정)"
  # TODO Phase 18: Skill(skill="sg-parallel-execute", args="$GROUPS_JSON_FILE")
  echo "현재 Phase 17에서는 병렬 그룹이 감지되었으나 sg-parallel-execute 스킬이 미구현 상태입니다."
  echo "parallel_groups.json이 저장되었습니다: $GROUPS_JSON_FILE"
  echo "Phase 18 완료 후 이 경로가 활성화됩니다."
  echo ""
  echo "임시로 기존 순차 실행 경로로 폴백합니다."
fi
```

목표 상태 (Phase 18 활성화 후):
```bash
if [ -n "$PARALLEL_GROUPS" ]; then
  echo "=== 병렬 실행 경로 선택: ${GROUP_COUNT}개 그룹 감지 ==="
  Skill(skill="sg-parallel-execute", args="$GROUPS_JSON_FILE")
  return
fi
```

Step 9의 나머지 (순차 경로): `if [ -n "$PARALLEL_GROUPS" ]` 블록 이후에 위치하는 기존 superpowers:executing-plans 조립 로직은 수정하지 않는다. PARALLEL_GROUPS가 비어 있을 때만 이 경로가 실행된다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: sg-execute.md Step 9 TODO 블록 활성화</name>
  <files>commands/sg-execute.md</files>

  <read_first>
    - commands/sg-execute.md — Step 9의 `if [ -n "$PARALLEL_GROUPS" ]` 블록 전체 현재 상태 확인 (lines 244-256 부근). 제거 대상 echo/TODO 라인 위치를 정확히 파악한다.
    - .planning/phases/18-sg-parallel-execute/18-CONTEXT.md — D-04 결정사항 (제거 대상 라인 목록 및 교체 방식, return 필수)
  </read_first>

  <action>
  `commands/sg-execute.md`의 Step 9 `if [ -n "$PARALLEL_GROUPS" ]` 블록을 아래와 같이 수정한다.

  **제거 대상 — 파일을 먼저 읽어 실제 라인을 확인한 뒤 모든 플레이스홀더 echo/TODO 라인 제거:**
  1. `  echo "=== 병렬 실행 경로 선택 ==="`
  2. `  echo "PARALLEL_GROUPS: $PARALLEL_GROUPS"`
  3. `  echo "sg-parallel-execute 스킬 라우팅 (Phase 18에서 구현 예정)"`
  4. `  # TODO Phase 18: Skill(skill="sg-parallel-execute", args="$GROUPS_JSON_FILE")`
  5. `  echo "현재 Phase 17에서는 병렬 그룹이 감지되었으나 sg-parallel-execute 스킬이 미구현 상태입니다."`
  6. `  echo "parallel_groups.json이 저장되었습니다: $GROUPS_JSON_FILE"`
  7. `  echo "Phase 18 완료 후 이 경로가 활성화됩니다."`
  8. `  echo ""`
  9. `  echo "임시로 기존 순차 실행 경로로 폴백합니다."`

  (파일을 읽은 뒤 존재하는 모든 플레이스홀더 echo/TODO 라인을 제거한다. 위 목록은 Phase 17 삽입 기준 참고값이므로 실제 파일 내용을 우선한다.)

  **교체 결과 — `if [ -n "$PARALLEL_GROUPS" ]` 블록이 다음으로 대체된다:**
  ```bash
  if [ -n "$PARALLEL_GROUPS" ]; then
    echo "=== 병렬 실행 경로 선택: ${GROUP_COUNT}개 그룹 감지 ==="
    Skill(skill="sg-parallel-execute", args="$GROUPS_JSON_FILE")
    return
  fi
  ```

  `return`은 D-04 결정사항이다. Skill() 호출 후 Step 9 이후의 순차 경로(superpowers:executing-plans 조립 로직)가 실행되지 않도록 반드시 포함한다.

  `GROUP_COUNT` 변수는 Step 8.5의 bash 스크립트에서 이미 설정된 변수이므로 별도 선언 없이 참조한다.

  **금지 사항:**
  - Step 9 블록 바깥의 어떤 라인도 수정하지 않는다
  - PARALLEL_GROUPS가 비어 있을 때의 순차 경로(superpowers:executing-plans 조립 로직)는 건드리지 않는다
  - idempotency 검사, HANDOFF.md 기록, lessons 주입 로직은 건드리지 않는다
  </action>

  <verify>
  <automated>
    # TODO 주석이 제거되었는지 확인
    REMAINING=$(grep -c 'TODO Phase 18' commands/sg-execute.md || echo "0")
    [ "$REMAINING" -eq 0 ] && echo "PASS: TODO removed" || echo "FAIL: TODO still present ($REMAINING occurrences)"

    # 임시 폴백 메시지가 제거되었는지 확인
    FALLBACK=$(grep -c '임시로 기존 순차 실행 경로로 폴백합니다' commands/sg-execute.md || echo "0")
    [ "$FALLBACK" -eq 0 ] && echo "PASS: fallback echo removed" || echo "FAIL: fallback echo present"

    # Phase 17 미구현 안내 메시지 제거 확인
    PHASE17=$(grep -c 'Phase 17에서는 병렬 그룹이' commands/sg-execute.md || echo "0")
    [ "$PHASE17" -eq 0 ] && echo "PASS: Phase 17 placeholder removed" || echo "FAIL: Phase 17 placeholder present"

    # 실제 Skill() 호출이 삽입되었는지 확인
    SKILL=$(grep -c 'Skill(skill="sg-parallel-execute"' commands/sg-execute.md || echo "0")
    [ "$SKILL" -ge 1 ] && echo "PASS: Skill() call present" || echo "FAIL: Skill() call missing"

    # PARALLEL_GROUPS 분기 블록이 여전히 존재하는지 확인
    BRANCH=$(grep -c 'if \[ -n "\$PARALLEL_GROUPS" \]' commands/sg-execute.md || echo "0")
    [ "$BRANCH" -ge 1 ] && echo "PASS: PARALLEL_GROUPS branch present" || echo "FAIL: PARALLEL_GROUPS branch missing"

    # superpowers:executing-plans 순차 경로가 여전히 존재하는지 확인 (제거하면 안 됨)
    SEQ=$(grep -c 'superpowers:executing-plans' commands/sg-execute.md || echo "0")
    [ "$SEQ" -ge 1 ] && echo "PASS: sequential path preserved" || echo "FAIL: sequential path removed"

    # Skill() 호출 다음 라인에 return이 존재하는지 확인
    RETURN=$(grep -A1 'Skill(skill="sg-parallel-execute"' commands/sg-execute.md | grep -c 'return' || echo "0")
    [ "$RETURN" -ge 1 ] && echo "PASS: return present after Skill() call" || echo "FAIL: return missing after Skill() call"
  </automated>
  </verify>

  <acceptance_criteria>
  - `grep -c 'TODO Phase 18' commands/sg-execute.md` 출력이 0이다
  - `grep -c '임시로 기존 순차 실행 경로로 폴백합니다' commands/sg-execute.md` 출력이 0이다
  - `grep -c 'Skill(skill="sg-parallel-execute"' commands/sg-execute.md` 출력이 1 이상이다
  - `grep -A1 'Skill(skill="sg-parallel-execute"' commands/sg-execute.md | grep -c 'return'` 출력이 1 이상이다 (Skill() 호출 다음 라인에 return 존재)
  - `grep -c 'if \[ -n "\$PARALLEL_GROUPS" \]' commands/sg-execute.md` 출력이 1 이상이다 (분기 블록 유지)
  - `grep -c 'superpowers:executing-plans' commands/sg-execute.md` 출력이 1 이상이다 (순차 경로 보존)
  - Step 9 블록 내부에 `echo "현재 Phase 17에서는 병렬 그룹이..."` 문자열이 없다
  - Step 9 블록 내부에 `echo "Phase 18 완료 후 이 경로가 활성화됩니다."` 문자열이 없다
  </acceptance_criteria>

  <done>commands/sg-execute.md의 Step 9 if 블록이 Skill(skill="sg-parallel-execute", args="$GROUPS_JSON_FILE") 호출과 그 다음 라인의 return으로 교체되어 있고, 임시 플레이스홀더 echo/TODO 라인이 모두 제거되어 있으며, 순차 경로(superpowers:executing-plans)는 그대로 보존되어 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sg-execute Step 9 → Skill() | Step 8.5 결과(GROUPS_JSON_FILE)를 Skill args로 전달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | sg-execute.md 편집 범위 | mitigate | Step 9 블록(if [ -n "$PARALLEL_GROUPS" ] 내부)만 수정, 다른 Step은 건드리지 않는다 — 파일을 읽은 뒤 정확한 라인 범위 확인 후 Edit |
| T-18-05 | Denial of Service | Skill() 이후 순차 경로 실행 | mitigate | Skill() 호출 직후 return 추가 (D-04) — 병렬 경로 진입 시 순차 경로가 실행되지 않음 |
</threat_model>

<verification>
1. TODO 주석 제거: `grep -c 'TODO Phase 18' commands/sg-execute.md` → 0
2. 폴백 echo 제거: `grep -c '임시로 기존 순차 실행 경로' commands/sg-execute.md` → 0
3. Skill() 삽입: `grep -c 'Skill(skill="sg-parallel-execute"' commands/sg-execute.md` → 1 이상
4. return 존재: `grep -A1 'Skill(skill="sg-parallel-execute"' commands/sg-execute.md | grep -c 'return'` → 1 이상
5. 순차 경로 보존: `grep -c 'superpowers:executing-plans' commands/sg-execute.md` → 1 이상
6. 전체 파일 구조 정합성: Step 0~8.5, HANDOFF.md 기록, idempotency 로직이 그대로인지 확인 (`grep -n 'Step' commands/sg-execute.md` 출력으로 Step 번호 순서 확인)
</verification>

<success_criteria>
1. `commands/sg-execute.md` Step 9에서 PARALLEL_GROUPS가 비어 있지 않을 때 `Skill(skill="sg-parallel-execute", args="$GROUPS_JSON_FILE")`이 호출되고, 그 다음 라인에 `return`이 있어 순차 경로로 흘러가지 않는다.
2. Phase 17이 삽입한 임시 echo/TODO 플레이스홀더 라인이 모두 제거된다.
3. PARALLEL_GROUPS가 비어 있을 때의 순차 경로(`superpowers:executing-plans`)가 그대로 보존된다.
4. idempotency 검사, HANDOFF.md 기록, lessons 주입 등 Step 9 외부 로직은 일체 변경되지 않는다.
</success_criteria>

<output>
Create `.planning/phases/18-sg-parallel-execute/18-02-SUMMARY.md` when done.
</output>
