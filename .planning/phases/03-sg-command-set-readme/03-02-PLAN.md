---
phase: 03-sg-command-set-readme
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-start.md
  - commands/sg-explore.md
autonomous: true
requirements:
  - PLUGIN-02
must_haves:
  truths:
    - "User can invoke /super-gsd:sg-start to scaffold a new project or milestone"
    - "User can invoke /super-gsd:sg-explore to map the codebase"
    - "sg-start delegates entirely to gsd-new-project Skill"
    - "sg-explore delegates entirely to gsd-explore Skill"
  artifacts:
    - path: "commands/sg-start.md"
      provides: "New project/milestone start command"
      contains: "name: sg-start"
    - path: "commands/sg-explore.md"
      provides: "Codebase exploration command"
      contains: "name: sg-explore"
  key_links:
    - from: "commands/sg-start.md"
      to: "gsd-new-project Skill"
      via: "Skill(skill=\"gsd-new-project\", args=\"$ARGUMENTS\")"
      pattern: "gsd-new-project"
    - from: "commands/sg-explore.md"
      to: "gsd-explore Skill"
      via: "Skill(skill=\"gsd-explore\", args=\"\")"
      pattern: "gsd-explore"
---

<objective>
Create commands/sg-start.md and commands/sg-explore.md — two simple single-Skill delegation commands.

Purpose: Provide the workflow entry points for starting a project and exploring a codebase (D-34). Both commands are thin wrappers: they invoke one Skill and print a next-step guide message.
Output: Two new command files following the XML 4-section structure established in Phase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sg-command-set-readme/03-CONTEXT.md
@.planning/phases/03-sg-command-set-readme/03-PATTERNS.md
</context>

<interfaces>
<!-- Established command file pattern from Phase 2. Executor uses this as the template. -->

Frontmatter structure (from commands/to-superpowers.md):
  name: sg-{name}
  description: <one sentence, shown in /help>
  argument-hint: "[arg] - optional. <description>"   # omit if no arguments

XML 4-section body structure:
  <objective>One paragraph describing the command's purpose.</objective>
  <execution_context>Self-contained — reads/writes X. Or: delegates to Y Skill.</execution_context>
  <process>Numbered steps. Bash blocks where needed.</process>
  <success_criteria>Numbered verifiable conditions.</success_criteria>

Skill invocation pattern:
  Skill(skill="<skill-name>", args="<args>")

User-facing strings: English only (D-41).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/sg-start.md</name>
  <files>commands/sg-start.md</files>
  <read_first>
    - commands/to-superpowers.md — reference for XML 4-section structure and frontmatter format (read before writing to confirm current pattern)
    - .planning/phases/03-sg-command-set-readme/03-PATTERNS.md — sg-start pattern definition (lines 116-148)
  </read_first>
  <action>
    Create commands/sg-start.md with the following exact content shape:

    Frontmatter fields:
    - name: sg-start
    - description: "Start a new project or milestone — invokes gsd-new-project to scaffold planning artifacts."
    - argument-hint: "[project-name] - optional. Passed through to gsd-new-project."

    XML 4-section body:
    - objective: Invoke gsd-new-project Skill to scaffold a new project or milestone. Pass $ARGUMENTS as-is; gsd-new-project handles new vs. existing project detection internally.
    - execution_context: Self-contained. Delegates all project detection and scaffolding to gsd-new-project Skill.
    - process: Two steps — (1) Invoke Skill: Skill(skill="gsd-new-project", args="$ARGUMENTS") (2) Print: "Project initialized. Run /super-gsd:sg-explore to map the codebase, then /super-gsd:sg-plan to create a phase plan."
    - success_criteria: Two items — (1) gsd-new-project Skill is invoked exactly once (2) $ARGUMENTS is forwarded unchanged
  </action>
  <verify>
    <automated>grep -c "name: sg-start" /Users/gyuha/workspace/super-gsd/commands/sg-start.md && grep -c "gsd-new-project" /Users/gyuha/workspace/super-gsd/commands/sg-start.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - commands/sg-start.md contains `name: sg-start` in frontmatter
    - commands/sg-start.md contains `argument-hint:` field
    - commands/sg-start.md contains `<objective>`, `<execution_context>`, `<process>`, `<success_criteria>` sections (all four)
    - commands/sg-start.md contains `Skill(skill="gsd-new-project", args="$ARGUMENTS")`
    - commands/sg-start.md process section mentions sg-explore and sg-plan as next steps in the printed guide message
  </acceptance_criteria>
  <done>commands/sg-start.md exists as a valid Claude Code slash command delegating to gsd-new-project</done>
</task>

<task type="auto">
  <name>Task 2: Create commands/sg-explore.md</name>
  <files>commands/sg-explore.md</files>
  <read_first>
    - commands/sg-start.md — just written; use as structural template to ensure consistency
    - .planning/phases/03-sg-command-set-readme/03-PATTERNS.md — sg-explore pattern definition (lines 152-182)
  </read_first>
  <action>
    Create commands/sg-explore.md with the following exact content shape:

    Frontmatter fields:
    - name: sg-explore
    - description: "Explore and map the codebase — invokes gsd-explore Skill."
    - (no argument-hint — sg-explore takes no arguments)

    XML 4-section body:
    - objective: Invoke the gsd-explore Skill to analyse and map the current codebase. No arguments required.
    - execution_context: Self-contained. Delegates entirely to gsd-explore Skill.
    - process: Two steps — (1) Invoke Skill: Skill(skill="gsd-explore", args="") (2) Print: "Exploration complete. Run /super-gsd:sg-plan <phase> to create a phase plan."
    - success_criteria: One item — gsd-explore Skill is invoked exactly once.
  </action>
  <verify>
    <automated>grep -c "name: sg-explore" /Users/gyuha/workspace/super-gsd/commands/sg-explore.md && grep -c "gsd-explore" /Users/gyuha/workspace/super-gsd/commands/sg-explore.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - commands/sg-explore.md contains `name: sg-explore` in frontmatter
    - commands/sg-explore.md does NOT contain `argument-hint:` field (no arguments accepted)
    - commands/sg-explore.md contains all four XML sections
    - commands/sg-explore.md contains `Skill(skill="gsd-explore", args="")`
    - commands/sg-explore.md process section mentions sg-plan as the next step in the guide message
  </acceptance_criteria>
  <done>commands/sg-explore.md exists as a valid Claude Code slash command delegating to gsd-explore</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → command file | Command file contents are instructions; no untrusted external input processed in these simple delegation commands |
| command → Skill | $ARGUMENTS is forwarded as-is to gsd-new-project; gsd-new-project is responsible for its own input handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | $ARGUMENTS forwarding in sg-start | accept | gsd-new-project Skill handles its own validation; sg-start is a thin pass-through with no parsing |
| T-03-02-02 | Spoofing | Skill name mismatch | mitigate | Verify Skill names match canonical PATTERNS.md: gsd-new-project, gsd-explore |
| T-03-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. commands/sg-start.md exists with name: sg-start
2. commands/sg-explore.md exists with name: sg-explore
3. Both files have all four XML sections
4. sg-start invokes gsd-new-project Skill with $ARGUMENTS
5. sg-explore invokes gsd-explore Skill with empty args
6. Guide messages in process steps reference correct next commands
</verification>

<success_criteria>
- /super-gsd:sg-start is a valid Claude Code slash command (commands/sg-start.md with name: sg-start)
- /super-gsd:sg-explore is a valid Claude Code slash command (commands/sg-explore.md with name: sg-explore)
- Both commands follow the XML 4-section structure established in Phase 2
- Both commands print a next-step guide message after Skill invocation
</success_criteria>

<output>
Create `.planning/phases/03-sg-command-set-readme/03-02-SUMMARY.md` when done
</output>
