---
phase: 23-plugin-commands
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
  - README.ko.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "CLAUDE.md Technology Stack 섹션에 'Commands' 항목이 없고 'Skills' 항목 하나로 통합됐다"
    - "CLAUDE.md Architecture 섹션에 '1. Commands 레이어' 항목이 없고 '1. Skills 레이어'로 교체됐다"
    - "CLAUDE.md의 GSD 마커(GSD:stack-start, GSD:architecture-start)가 보존됐다"
    - "README.md Roadmap에서 commands/ 기준 표현이 skills/ 기준으로 교체됐다"
    - "README.ko.md의 동일 섹션이 README.md와 동기화됐다"
  artifacts:
    - path: "CLAUDE.md"
      provides: "skills/ 기준으로 재서술된 Architecture 및 Technology Stack 섹션"
      contains: "Skills 레이어"
    - path: "README.md"
      provides: "commands/ 참조 없는 Roadmap 섹션"
    - path: "README.ko.md"
      provides: "README.md와 동기화된 한국어 문서"
  key_links:
    - from: "CLAUDE.md GSD:stack 마커 블록"
      to: "Skills 한 줄 통합 표현"
      via: "GSD:stack-start ... GSD:stack-end 내부 수정"
      pattern: "GSD:stack-start"
    - from: "CLAUDE.md GSD:architecture 마커 블록"
      to: "1. Skills 레이어 항목"
      via: "GSD:architecture-start ... GSD:architecture-end 내부 수정"
      pattern: "GSD:architecture-start"
---

<objective>
CLAUDE.md의 Technology Stack과 Architecture 섹션을 skills/ 기준으로 재서술하고, README.md와 README.ko.md의 Roadmap 섹션에서 commands/ 기준 표현을 정리한다.

Purpose: Phase 22-23 완료 후 코드베이스 실제 구조(commands/ 없음, skills/ 16개)가 문서에 반영되도록 한다. GSD 마커는 보존한다.
Output: commands/ 참조가 제거된 CLAUDE.md, README.md, README.ko.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-plugin-commands/23-CONTEXT.md
@.planning/phases/23-plugin-commands/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md Technology Stack + Architecture 섹션 재서술 (per D-03)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` — 전체 파일. 특히 GSD:stack-start~GSD:stack-end 블록(24-32행)과 GSD:architecture-start~GSD:architecture-end 블록(100-148행) 확인
  </read_first>
  <action>
    수정 범위는 GSD 마커 블록 내부로만 한정한다. 마커 라인 자체(HTML 주석)는 변경하지 않는다.

    **GSD:stack-start~GSD:stack-end 블록 내부 수정 (Technology Stack 섹션):**

    현재 두 줄:
    - `- **Plugin manifest**: ...` (유지)
    - `- **Commands**: \`commands/*.md\` — ...` (제거)
    - `- **Skills**: \`skills/sg-retro/SKILL.md\` — ...` (교체)
    - `- **Hooks**: ...` (유지)
    - `- **Rules**: ...` (유지)
    - `- **Lessons**: ...` (유지)

    변경 결과: `- **Commands**:` 줄을 삭제하고, `- **Skills**:` 줄을 아래 내용으로 교체한다:
    `- **Skills**: \`skills/sg-*/SKILL.md\` — YAML frontmatter + \`<objective>\` / \`<process>\` / \`<success_criteria>\` 블록 구조. Claude Code 슬래시 명령(\`/super-gsd:sg-*\`)으로 직접 호출되거나 Claude가 자동 발동한다`

    또한 Plugin manifest 설명의 "14개 명령 경로, skills 디렉토리"를 "skills 디렉토리, marketplace 메타데이터 정의"로 수정한다 (commands 키 제거에 따른 최소 수정).

    **GSD:architecture-start~GSD:architecture-end 블록 내부 수정 (Architecture 섹션):**

    현재 구조: "1. Commands 레이어", "2. Hooks 레이어", "3. Skills 레이어" 3개 항목.

    변경 결과:
    1. "1. Commands 레이어 (`commands/*.md`)" 항목 전체를 아래로 교체:
       `**1. Skills 레이어** (\`skills/sg-*/SKILL.md\`)\n\n16개의 SKILL.md 파일이 각각 하나의 \`/super-gsd:sg-*\` 슬래시 명령을 정의한다. 14개는 GSD → Superpowers → sg-retro 워크플로우 명령이고, sg-retro(회고)와 sg-parallel-execute(병렬 실행)가 추가로 포함된다. Claude Code가 SKILL.md를 실행 지시문으로 해석하며, GSD skill, Superpowers skill, 또는 Python 스크립트를 호출해 단계 간 인계를 수행한다.`
    2. 기존 "3. Skills 레이어" 항목 전체를 삭제 (위의 1번으로 통합됐으므로).
    3. 기존 "2. Hooks 레이어"는 "2. Hooks 레이어"로 그대로 유지 (번호 변경 없음).
    4. Architecture 도입부 "플러그인은 세 개의 레이어로 구성된다."를 "플러그인은 두 개의 레이어로 구성된다."로 수정한다.

    데이터 흐름 다이어그램에서 `sg-*` 명령 표기는 현재 그대로 유지한다 (skills 기반으로 동일하게 동작하므로 변경 불필요).

    Constraints 섹션(GSD:project-start 블록 내부)의 "Tech stack: Claude Code 플러그인 시스템 (skills + commands + hooks)" 표현은 GSD:project-start 마커 내부이므로 손대지 않는다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -c "Commands 레이어" CLAUDE.md | grep -q "^0$" && echo "PASS: Commands 레이어 없음" || echo "FAIL: Commands 레이어 여전히 존재"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "GSD:architecture-start" CLAUDE.md` 결과가 존재한다 (마커 보존)
    - `grep "GSD:stack-start" CLAUDE.md` 결과가 존재한다 (마커 보존)
    - `grep -c "Commands 레이어" CLAUDE.md` 출력이 `0`이다
    - `grep "Skills 레이어" CLAUDE.md` 결과가 존재한다
    - `grep "skills/sg-\*/SKILL.md" CLAUDE.md` 결과가 존재한다 (Technology Stack 업데이트 확인)
    - `grep "16개의 SKILL.md" CLAUDE.md` 결과가 존재한다 (Architecture 업데이트 확인)
    - `grep "두 개의 레이어" CLAUDE.md` 결과가 존재한다
    - `grep "3. Skills 레이어" CLAUDE.md` 결과가 없다 (통합됐으므로)
  </acceptance_criteria>
  <done>CLAUDE.md Technology Stack에 Commands 항목이 없고 Skills 항목이 sg-*/SKILL.md 기준으로 통합됐으며, Architecture에 "1. Skills 레이어"가 Commands 레이어를 대체했고 GSD 마커가 보존됐다</done>
</task>

<task type="auto">
  <name>Task 2: README.md + README.ko.md Roadmap 섹션 정리 (per D-04, D-05)</name>
  <files>README.md, README.ko.md</files>
  <read_first>
    - `README.md` — Roadmap 섹션(238행 이후) 전체. "Phase 3 — sg- Command Set & README" 표현 위치 확인
    - `README.ko.md` — 로드맵 섹션(238행 이후) 전체. 동일 표현 위치 확인
  </read_first>
  <action>
    RESEARCH.md Anti-Patterns 항목 준수: "README Roadmap 문단 텍스트 대폭 재작성 금지". Phase 설명은 역사적 기록이므로 최소한의 표현만 수정한다.

    **README.md Roadmap 섹션 수정:**

    "Phase 3 — sg- Command Set & README (shipped):" 줄의 설명 문구에서 "9-command `sg-` interface"로 이미 명확하므로 해당 줄은 그대로 유지한다. Phase 3 설명 본문("delivers the full 9-command...so the entire GSD → Superpowers → Hookify cycle has discoverable slash commands")에서 "Hookify"를 "sg-retro"로 교체한다 (이 표현은 Phase 13 이후 구식이므로 최소 수정).

    Phase 22와 Phase 23에 해당하는 항목은 현재 README.md Roadmap에 없다. DOC-02 범위는 "commands/ 참조 정리"이므로 Phase 22-23 Roadmap 항목 추가는 하지 않는다 (RESEARCH.md Open Questions #2 결론).

    README.md 상단 "All thirteen slash commands covering the full GSD → Superpowers → sg-retro cycle" 문구는 이미 sg-retro를 사용하므로 변경 불필요.

    **README.ko.md Roadmap 섹션 수정 (D-05):**

    README.md에서 수정한 것과 동일한 변경을 README.ko.md에 적용한다:
    "Phase 3 — sg- 명령 세트 및 README (완료):" 줄의 설명 문구에서 "Hookify"를 "sg-retro"로 교체한다.

    수정 범위 외 내용(Commands 테이블, 사용 예시, Installation, Multi-Platform Support 섹션 등)은 건드리지 않는다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep "Hookify cycle" README.md | wc -l | grep -q "^0$" && echo "PASS: README.md Hookify cycle 없음" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md Roadmap의 Phase 3 설명에 "Hookify cycle"이 없고 "sg-retro" 또는 "sg-retro cycle"로 표현된다
    - README.ko.md Roadmap의 동일 위치에서도 "Hookify"가 "sg-retro"로 교체됐다
    - README.md의 나머지 섹션(Commands 테이블, Workflow, Installation, Multi-Platform Support)이 변경되지 않았다
    - README.ko.md의 나머지 섹션이 변경되지 않았다
    - 두 파일 모두 Markdown 형식이 깨지지 않았다 (Phase 3 항목이 여전히 bullet point로 존재)
  </acceptance_criteria>
  <done>README.md와 README.ko.md의 Phase 3 설명에서 구식 Hookify 참조가 sg-retro로 교체됐고, 다른 섹션은 변경되지 않았다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GSD 마커 내부 편집 | 마커 블록 외부를 실수로 수정할 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | CLAUDE.md GSD 마커 | mitigate | 수정 전/후 `grep "GSD:architecture-start" CLAUDE.md` 실행 — 마커 보존 확인 |
| T-23-04 | Tampering | README Roadmap 과잉 수정 | accept | 최소 변경 원칙 적용 — Phase 3 설명의 Hookify 표현만 수정, 나머지 역사적 기록 보존 |
</threat_model>

<verification>
두 태스크 완료 후 순서대로 확인:

1. `grep "Commands 레이어" CLAUDE.md` → 결과 없음
2. `grep "GSD:architecture-start" CLAUDE.md` → 결과 존재
3. `grep "GSD:stack-start" CLAUDE.md` → 결과 존재
4. `grep "Skills 레이어" CLAUDE.md` → "1. Skills 레이어" 결과 존재
5. `grep "Hookify cycle" README.md` → 결과 없음
6. `grep "Hookify" README.ko.md` (Roadmap 섹션 한정) → sg-retro로 교체됐는지 확인
</verification>

<success_criteria>
- CLAUDE.md Technology Stack 섹션: Commands 항목 없음, Skills 항목이 `skills/sg-*/SKILL.md` 기준으로 통합됨
- CLAUDE.md Architecture 섹션: "1. Skills 레이어"가 "1. Commands 레이어"를 완전히 대체함, "3. Skills 레이어" 중복 항목 없음
- CLAUDE.md GSD 마커(GSD:stack-start, GSD:stack-end, GSD:architecture-start, GSD:architecture-end) 전부 보존
- README.md Phase 3 설명에서 Hookify → sg-retro 교체 완료
- README.ko.md 동일 변경 적용 완료
</success_criteria>

<output>
완료 후 `.planning/phases/23-plugin-commands/23-02-SUMMARY.md` 생성
</output>
