# Lessons: Phase 39 (2026-05-28)

## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: 2026-05-28T14:48:30Z_

### Decisions
- HANDOFF.md `User` 컬럼을 6번째 선택적 열로 추가 — 스키마 버전 없이 혼합 스키마 허용 (39-CONTEXT.md:A, D)
- `sg-status --team` primary source = HANDOFF.md `$7`, fallback = `git log` 작성자 (39-CONTEXT.md:B)
- `.gitignore` 수정 불필요 — HANDOFF.md의 user.name 기록은 git log 작성자 정보와 동등 수준 (39-CONTEXT.md:C)

### Surprises
- `grep -q "Timestamp.*Phase.*From.*To.*Plan Hash"` 가드가 부분 문자열 매칭이라 새 6열 헤더 사용 시 기존 HANDOFF.md가 재초기화됨 — 코드 리뷰에서 발견 (commit 7c2d8c1)
- Phase 도중 sg-parallel-execute adversarial review에서 [high] 이슈 3개 + [medium] 1개가 한꺼번에 발견됨 — 예상보다 많은 설계 결함

### Patterns
- GIT_USER 3-line 스니펫(할당 + 가드 + echo)을 13개 파일에 동일하게 적용하는 패턴 확립 (39-01-SUMMARY.md)
- Phase 내 버그를 sg-quick quick task로 즉시 수정하는 패턴 — 이번 phase에서 5개 quick task 실행

### Mistakes
- sg-phase에서 GIT_USER 누락, sg-health NF 버그, 스키마 문서 미포함 — 코드 리뷰 전까지 미발견 (7c2d8c1 fix commit)

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | HANDOFF.md 헤더 재초기화 버그 수정 — grep 가드를 정확한 6열 헤더 매칭으로 교체 | sg-quick으로 즉시 수정 |
| P1 | skill 변경 시 adversarial review를 workflow에 공식 포함 | sg-review 후 adversarial review 단계 추가 검토 |
| P2 | GIT_USER 적용 대상 13개 파일 목록을 CLAUDE.md에 명시 | 다음 phase 계획 시 체크리스트에 포함 |
| P3 | sg-parallel-execute .agents/ 미러 생성 확인 자동화 | CLAUDE.md 컨벤션에 체크 조건 추가 |

## Lens: Start/Stop/Continue
_Captured: 2026-05-28T14:48:40Z_

### Start
- 핵심 skill 변경 시 adversarial review 자동 실행 — sg-parallel-execute에서 고위험 이슈 다수 발견됨; sg-review만으로는 설계 결함을 잡기 어려움

### Stop
- 코드 리뷰 없이 phase를 ship하는 것 — 이번 review에서 GIT_USER 누락 등 중요 버그 발견 (7c2d8c1)

### Continue
- sg-quick을 활용한 즉시 버그 수정 패턴 — phase 흐름을 끊지 않고 소규모 수정 가능
- SKILL.md 수정 시 .agents/ 미러도 항상 함께 수정하는 컨벤션 (CLAUDE.md 명시)

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | sg-review 완료 후 adversarial review를 표준 단계로 추가 | sg-review SKILL.md에 adversarial review 권장 안내 추가 |
| P2 | GIT_USER 패턴을 적용하는 skill 목록을 CLAUDE.md에 체크리스트로 관리 | CLAUDE.md Conventions 섹션에 추가 |

