---
phase: quick
plan: 260516-2sw
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Each prerequisite plugin has concrete install commands in the README"
    - "Existing Prerequisites section text is preserved verbatim"
    - "Document language remains English throughout"
  artifacts:
    - path: "README.md"
      provides: "Updated Prerequisites section with install sub-sections"
      contains: "plugin install superpowers@claude-plugins-official"
  key_links:
    - from: "README.md Prerequisites"
      to: "README.md Installation"
      via: "logical ordering — install prereqs before installing super-gsd itself"
---

<objective>
Expand the Prerequisites section in README.md to include concrete installation commands for GSD, Superpowers, and Hookify.

Purpose: Currently the Prerequisites section tells readers WHAT to install but not HOW. New users must leave the README to discover install commands for all three plugins before they can use super-gsd.
Output: Prerequisites section restructured into three ### sub-sections (GSD, Superpowers, Hookify), each containing the exact install command(s) in a code block. All existing prose preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add install sub-sections to Prerequisites</name>
  <files>README.md</files>
  <action>
Edit the Prerequisites section (currently lines 110-118) to restructure it into three ### sub-sections. Keep the opening paragraph that begins "super-gsd is non-invasive..." and the closing paragraph that begins "If any of the three is missing..." exactly as they are. Between those two paragraphs, replace the bullet list with three ### sub-sections:

### GSD

Install via npm, then run the installer which configures Claude Code integration:

```shell
npm install -g get-shit-done-cc
get-shit-done-cc
```

### Superpowers

Install from the Claude Code official plugin marketplace:

```
/plugin install superpowers@claude-plugins-official
```

### Hookify

Install from the Claude Code official plugin marketplace:

```
/plugin install hookify@claude-plugins-official
```

The closing sentence about partial workflow must remain after these three sub-sections, before the ## Installation heading.

Do NOT change anything outside the Prerequisites section. Match surrounding markdown style exactly (blank line between sub-sections, backtick code fences, no trailing spaces).
  </action>
  <verify>
grep -c "plugin install superpowers@claude-plugins-official" /Users/gyuha/workspace/super-gsd/README.md && grep -c "plugin install hookify@claude-plugins-official" /Users/gyuha/workspace/super-gsd/README.md && grep -c "npm install -g get-shit-done-cc" /Users/gyuha/workspace/super-gsd/README.md
  </verify>
  <done>README.md Prerequisites section contains ### sub-sections for all three plugins, each with a runnable install command, and the document is otherwise unchanged.</done>
</task>

</tasks>

<verification>
grep -A 40 "^## Prerequisites" /Users/gyuha/workspace/super-gsd/README.md
</verification>

<success_criteria>
- `### GSD`, `### Superpowers`, `### Hookify` headings present under ## Prerequisites
- Each heading followed by a code block with install command(s)
- Original opening and closing paragraphs of Prerequisites section unchanged
- No changes outside the Prerequisites section
</success_criteria>

<output>
Create `.planning/quick/260516-2sw-readme-md-gsd-superpowers-hookify/260516-2sw-SUMMARY.md` when done.
</output>
