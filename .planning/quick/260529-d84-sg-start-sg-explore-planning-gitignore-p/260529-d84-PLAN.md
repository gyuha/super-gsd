---
phase: quick-260529-d84
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-start/SKILL.md
  - .agents/skills/sg-start/SKILL.md
  - skills/sg-explore/SKILL.md
autonomous: true
requirements:
  - gitignore-idempotent-planning-exclusion
must_haves:
  truths:
    - "sg-start 또는 sg-explore 실행 시 .planning/ 이 .gitignore에 추가된다"
    - ".planning/codebase/ 는 추가 예외로 git에 계속 추적된다"
    - "이미 항목이 있을 경우 중복 추가되지 않는다"
  artifacts:
    - path: "skills/sg-start/SKILL.md"
      provides: "gitignore 갱신 로직 (process 맨 앞 Step 0)"
    - path: ".agents/skills/sg-start/SKILL.md"
      provides: "동일 로직 (pairwise 동기화)"
    - path: "skills/sg-explore/SKILL.md"
      provides: "gitignore 갱신 로직 (process Step 0)"
  key_links:
    - from: "skills/sg-start/SKILL.md process"
      to: ".gitignore"
      via: "grep 존재 확인 → echo 추가 (멱등)"
    - from: "skills/sg-explore/SKILL.md process"
      to: ".gitignore"
      via: "동일 패턴"
---

<objective>
sg-start 또는 sg-explore가 실행될 때 프로젝트 루트의 .gitignore에
`.planning/` 제외 항목(+ `!.planning/codebase/` 예외)을 자동으로 추가한다.

목적: .planning/ 디렉터리가 워킹 디렉터리에 실수로 커밋되지 않도록 보호하면서,
     codebase 매핑 결과는 git 추적을 유지한다.
출력: skills/sg-start/SKILL.md, .agents/skills/sg-start/SKILL.md,
      skills/sg-explore/SKILL.md 세 파일에 멱등 gitignore 갱신 스텝 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/super-gsd/skills/sg-start/SKILL.md
@/Users/gyuha/workspace/super-gsd/.agents/skills/sg-start/SKILL.md
@/Users/gyuha/workspace/super-gsd/skills/sg-explore/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: sg-start/SKILL.md 두 쌍에 gitignore 갱신 스텝 추가</name>
  <files>
    skills/sg-start/SKILL.md
    .agents/skills/sg-start/SKILL.md
  </files>
  <action>
    두 파일 모두 `&lt;process&gt;` 블록의 맨 처음(Step 1 앞)에 **Step 0** 을 삽입한다.
    기존 Step 1~6 번호는 그대로 유지한다(번호 재부여 금지).

    삽입할 내용 (산문 지시 — 코드 블록은 참고용이므로 SKILL.md의 기존 코드 블록 서식에 맞춰 작성):

    Step 0 제목: `.gitignore에 .planning/ 제외 항목 추가 (멱등)`

    로직:
    - 프로젝트 루트의 `.gitignore` 파일에 `.planning/` 항목이 없을 때만 두 줄을 추가한다.
    - 추가할 두 줄: `.planning/` 와 `!.planning/codebase/`
    - 존재 여부 확인: `grep -qxF '.planning/' .gitignore 2>/dev/null`
      - 해당 패턴이 없으면(exit code != 0): 빈 줄 + `.planning/` + `!.planning/codebase/` 세 줄을 `.gitignore`에 추가
      - 이미 있으면: 아무것도 하지 않는다
    - `.gitignore` 자체가 없는 경우에도 동일 조건 분기가 새 파일을 생성한다

    bash 스니펫 (SKILL.md `&lt;process&gt;` 코드 블록 서식으로 삽입):
    ```bash
    if ! grep -qxF '.planning/' .gitignore 2>/dev/null; then
      printf '\n.planning/\n!.planning/codebase/\n' >> .gitignore
    fi
    ```

    주의:
    - `grep -P` 금지 (macOS 비호환). `-xF` 사용 (exact line, fixed string).
    - Step 0은 STATE.md 읽기 전에 실행되므로 .planning/ 부재 여부와 무관하게 항상 실행된다.
    - skills/sg-start/SKILL.md 와 .agents/skills/sg-start/SKILL.md 두 파일을 **동일하게** 수정한다.
      두 파일의 process 구조가 다를 경우(현재 .agents 버전은 constraint 섹션이 있음) 각 파일의
      `&lt;process&gt;` 태그 바로 다음 줄에 삽입한다.
  </action>
  <verify>
    grep -n 'grep -qxF' skills/sg-start/SKILL.md
    grep -n 'grep -qxF' .agents/skills/sg-start/SKILL.md
  </verify>
  <done>
    두 파일 모두 &lt;process&gt; 첫 번째 스텝으로 멱등 gitignore 갱신 로직이 포함된다.
    `.planning/codebase/` 예외 라인(`!.planning/codebase/`)도 함께 추가된다.
  </done>
</task>

<task type="auto">
  <name>Task 2: sg-explore/SKILL.md에 gitignore 갱신 스텝 추가</name>
  <files>
    skills/sg-explore/SKILL.md
  </files>
  <action>
    `&lt;process&gt;` 블록의 기존 Step 1(`Skill(skill="gsd-map-codebase", args="")`) 앞에
    **Step 0** 을 삽입한다. 기존 Step 1 번호는 유지한다.

    Step 0 제목: `.gitignore에 .planning/ 제외 항목 추가 (멱등)`

    로직은 Task 1과 동일한 bash 스니펫:
    ```bash
    if ! grep -qxF '.planning/' .gitignore 2>/dev/null; then
      printf '\n.planning/\n!.planning/codebase/\n' >> .gitignore
    fi
    ```

    sg-explore의 `&lt;success_criteria&gt;`에 항목을 하나 추가한다:
    `2. 실행 전 .gitignore에 .planning/ 항목이 없으면 자동으로 추가된다 (멱등).`
    기존 `1. gsd-map-codebase Skill is invoked exactly once.` 번호는 유지.

    주의: `.agents/skills/sg-explore/` 디렉터리는 존재하지 않으므로 수정 대상 없음.
  </action>
  <verify>
    grep -n 'grep -qxF' skills/sg-explore/SKILL.md
  </verify>
  <done>
    skills/sg-explore/SKILL.md의 &lt;process&gt;에 Step 0이 추가되고,
    &lt;success_criteria&gt;에 gitignore 갱신 항목이 명시된다.
  </done>
</task>

</tasks>

<verification>
세 파일 모두 수정 후 아래를 확인한다:

1. `grep -c 'grep -qxF' skills/sg-start/SKILL.md` → 1 이상
2. `grep -c 'grep -qxF' .agents/skills/sg-start/SKILL.md` → 1 이상
3. `grep -c 'grep -qxF' skills/sg-explore/SKILL.md` → 1 이상
4. `grep -c '!.planning/codebase/' skills/sg-start/SKILL.md` → 1 이상
5. `grep -c '!.planning/codebase/' skills/sg-explore/SKILL.md` → 1 이상
</verification>

<success_criteria>
- sg-start 또는 sg-explore 실행 시, 프로젝트 루트 .gitignore에 `.planning/` 줄이 없으면 자동 추가된다.
- `.planning/codebase/` 는 `!.planning/codebase/` 예외로 git 추적이 유지된다.
- 이미 항목이 있으면 중복 추가가 발생하지 않는다 (멱등).
- .agents/skills/sg-start/SKILL.md가 skills/sg-start/SKILL.md와 동기화된다.
</success_criteria>

<output>
완료 후 `.planning/quick/260529-d84-sg-start-sg-explore-planning-gitignore-p/260529-d84-SUMMARY.md` 를 생성한다.
</output>
