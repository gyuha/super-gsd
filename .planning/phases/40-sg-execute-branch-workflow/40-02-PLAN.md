---
phase: 40
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - skills/sg-phase/SKILL.md
autonomous: true
requirements:
  - TEAM-04

must_haves:
  truths:
    - "sg-complete [N] (phase 완료) 직후 PR 생성 안내 메시지가 출력된다"
    - "gh CLI가 있으면 gh pr create --base main 명령 텍스트를 출력한다"
    - "gh CLI가 없으면 git push -u origin HEAD + GitHub URL 패턴 안내를 출력한다"
    - "PR 자동 생성은 없다 — 명령 텍스트 출력만"
  artifacts:
    - path: "skills/sg-phase/SKILL.md"
      provides: "Step 4i PR 안내 로직 (Step 4h 직후)"
      contains: "Step 4i"
  key_links:
    - from: "skills/sg-phase/SKILL.md Step 4h"
      to: "Step 4i"
      via: "4h 확인 출력 후 PR 안내 출력"
      pattern: "Step 4i"
    - from: "Step 4i"
      to: "gh CLI 감지"
      via: "command -v gh"
      pattern: "command -v gh"
---

<objective>
`skills/sg-phase/SKILL.md`의 `complete` route에서 Step 4h (확인 출력) 이후 Step 4i를 삽입하여 PR 생성 안내를 구현한다. gh CLI 존재 여부에 따라 `gh pr create` 명령 또는 `git push -u origin HEAD` 안내를 텍스트로 출력한다. `.agents/skills/sg-phase/` 는 존재하지 않으므로 pairwise 처리 불필요.

Purpose: TEAM-04 — phase 완료 시 자연스럽게 PR 생성 흐름으로 유도한다. 자동 실행 없음 — 사용자가 직접 실행할 명령만 안내한다.
Output: 수정된 skills/sg-phase/SKILL.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-sg-execute-branch-workflow/40-CONTEXT.md
</context>

<interfaces>
<!-- Phase 40 CONTEXT.md에서 추출한 핵심 패턴 -->

삽입 위치: skills/sg-phase/SKILL.md의 Step 4h 블록 ("Print a confirmation summarizing what changed...") 닫힘 바로 뒤.

PR 안내 로직 (CONTEXT.md §D, §E):
```bash
# Step 4i (sg-phase complete 내부에 추가)
if command -v gh >/dev/null 2>&1; then
  echo "PR을 생성하려면:"
  echo "  gh pr create --base main --title \"phase/${PHASE_PAD}-${PHASE_SLUG_CLEAN}\""
else
  echo "PR을 생성하려면 현재 브랜치를 push한 뒤 GitHub에서 PR을 여세요:"
  echo "  git push -u origin HEAD"
fi
```
- 출력 메시지는 사용자 언어로 (CLAUDE.md 언어 지침 준수)
- 브랜치명(`phase/...`), 명령어(`gh pr create`, `git push -u origin HEAD`)는 영문 그대로
- 조건 분기 없이 항상 출력 (단순성 우선 — CONTEXT.md §E)

PHASE_SLUG 접근: sg-phase complete Step 4b에서 이미 PHASE_SLUG가 계산된다.
단, PHASE_SLUG = "40-sg-execute-branch-workflow" 형태의 full slug. 브랜치명에는 PHASE_PAD + 나머지 slug 일부를 사용하거나 그대로 사용 가능.
CONTEXT.md §specifics: `phase/{PHASE_PAD}-{slug}` — slug는 ROADMAP.md 기반 정규화된 값.

sg-phase/SKILL.md의 4b 블록:
```bash
PHASE_SLUG=$(printf '%s' "$PHASE_DIR" | xargs basename 2>/dev/null || echo "${PHASE_PAD}")
```
이 PHASE_SLUG 값은 `40-sg-execute-branch-workflow` 형태이므로 브랜치명에 `phase/${PHASE_SLUG}` 로 사용 가능 (이미 패딩+슬러그 포함).

pairwise 확인: `ls .agents/skills/sg-phase/` → 디렉토리 없음 → pairwise 처리 불필요.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: .agents/skills/sg-phase 존재 여부 재확인 후 pairwise 처리 결정</name>
  <files></files>
  <read_first>
    - CLAUDE.md §skills/ + .agents/ 쌍 커버 컨벤션
  </read_first>
  <action>
    다음 명령으로 .agents/skills/sg-phase/ 디렉토리 존재 여부를 확인한다:
    ```bash
    ls .agents/skills/sg-phase/ 2>/dev/null || echo "NOT FOUND"
    ```
    - "NOT FOUND"이면: pairwise 처리 불필요. Task 2로 바로 진행.
    - 존재하면: pairwise convention에 따라 Task 2와 동일한 변경을 .agents/skills/sg-phase/SKILL.md에도 적용한다. 이 경우 files_modified 목록에 .agents/skills/sg-phase/SKILL.md도 포함된다.

    (계획 시점에서 .agents/skills/sg-phase/가 없음을 확인했으나, 실행 시점에서 재확인하여 누락 방지.)
  </action>
  <verify>
    <automated>ls .agents/skills/sg-phase/ 2>/dev/null || echo "NOT FOUND"</automated>
  </verify>
  <done>
    - .agents/skills/sg-phase/ 존재 여부가 확인됨
    - 존재 시 pairwise 처리 계획이 수립됨 (Task 2 확장)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: skills/sg-phase/SKILL.md에 Step 4i 삽입</name>
  <files>skills/sg-phase/SKILL.md</files>
  <read_first>
    - skills/sg-phase/SKILL.md — Step 4h 블록 위치 (확인 출력 텍스트 끝)와 process 닫힘 태그 위치 확인. PHASE_SLUG, PHASE_PAD 변수가 이미 존재하는지 확인.
    - .planning/phases/40-sg-execute-branch-workflow/40-CONTEXT.md — §D(PR 안내 위치), §E(PR 안내 방식)
  </read_first>
  <action>
    skills/sg-phase/SKILL.md의 `complete` route에서 Step 4h 블록("Print a confirmation summarizing what changed...") 끝 바로 다음에 Step 4i를 삽입한다. `</process>` 태그 이전.

    **Step 4i 헤더:** `4i. **PR creation guidance (TEAM-04).** Check for gh CLI and output the PR creation command or git push guidance.`

    **Step 4i 로직:**
    ```bash
    if command -v gh >/dev/null 2>&1; then
      # gh CLI 있음: gh pr create 명령 텍스트 출력
      # 사용자 언어로 안내 산문 출력 (브랜치명, 명령어는 영문 그대로)
      echo "To create a PR:"
      echo "  gh pr create --base main --title \"phase/${PHASE_SLUG}\""
    else
      # gh CLI 없음: git push + GitHub URL 안내
      echo "To create a PR, push the current branch and open a PR on GitHub:"
      echo "  git push -u origin HEAD"
    fi
    ```
    - 출력 산문("To create a PR:" 등)은 사용자 언어로 표면화 (CLAUDE.md 사용자 언어 메시지 컨벤션)
    - 명령어 토큰(`gh pr create`, `git push -u origin HEAD`, `phase/${PHASE_SLUG}`)은 영문 그대로
    - PHASE_SLUG 변수: Step 4b에서 이미 `$(basename "$PHASE_DIR")` 형태로 계산된 값 사용

    **주의:**
    - Step 4h 블록 내용은 수정하지 않는다
    - `</process>` 태그 이전에 삽입
    - PR 자동 실행 없음 — 텍스트 출력만 (CONTEXT.md §E "자동 실행 없음")
    - 조건 분기(현재 브랜치가 phase/* 인지) 없이 항상 출력 (CONTEXT.md §E "단순성 우선")
  </action>
  <verify>
    <automated>grep -n "Step 4i\|4i\." skills/sg-phase/SKILL.md</automated>
    <automated>grep -n "command -v gh" skills/sg-phase/SKILL.md</automated>
    <automated>grep -n "gh pr create" skills/sg-phase/SKILL.md</automated>
    <automated>grep -n "git push -u origin HEAD" skills/sg-phase/SKILL.md</automated>
  </verify>
  <done>
    - skills/sg-phase/SKILL.md에 "4i." 또는 "Step 4i" 헤더가 존재한다
    - `command -v gh` 조건 분기가 존재한다
    - `gh pr create --base main` 텍스트가 gh CLI 있음 경로에 있다
    - `git push -u origin HEAD` 텍스트가 gh CLI 없음 경로에 있다
    - Step 4h와 `</process>` 태그 사이에 삽입되어 있다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill → gh CLI 감지 | `command -v gh` 실행 — 출력 없음, exit code만 사용 |
| skill → 출력 문자열 | `PHASE_SLUG` 변수가 브랜치명으로 출력에 포함됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Information Disclosure | PHASE_SLUG 출력 | accept | 로컬 phase 슬러그, 민감 정보 없음 |
| T-40-05 | Spoofing | command -v gh | accept | 존재 여부만 확인, gh 실행 없음 |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | 이 plan에 패키지 설치 없음 |
</threat_model>

<verification>
```bash
# Step 4i가 Step 4h 이후 </process> 이전에 존재하는지 확인
grep -n "4h\.\|4i\.\|</process>" skills/sg-phase/SKILL.md | head -10

# PR 안내 핵심 요소 확인
grep -c "command -v gh\|gh pr create\|git push -u origin HEAD" skills/sg-phase/SKILL.md
# 결과가 3 이어야 함

# 기존 Step 4h 확인 출력 로직이 유지되는지 확인
grep -n "4h\." skills/sg-phase/SKILL.md
```
</verification>

<success_criteria>
1. `skills/sg-phase/SKILL.md`의 `complete` route에 Step 4i가 삽입되어 있다
2. `command -v gh >/dev/null 2>&1` 조건 분기로 gh CLI 존재를 감지한다
3. gh CLI 있음: `gh pr create --base main --title "phase/${PHASE_SLUG}"` 명령 텍스트가 출력된다
4. gh CLI 없음: `git push -u origin HEAD` 안내가 출력된다
5. PR 자동 실행 없음 — 텍스트 출력만 (CONTEXT.md §E 준수)
6. 기존 Step 4h 로직은 수정 없이 유지된다
</success_criteria>

<output>
Create `.planning/phases/40-sg-execute-branch-workflow/40-02-SUMMARY.md` when done
</output>
