---
phase: 41-team-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.ko.md
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "README.md에 ## Team Workflow 섹션이 있다"
    - "README.ko.md에 ## 팀 워크플로우 섹션이 있다"
    - "두 섹션 모두 Usage Examples 뒤, Installation 앞에 위치한다"
    - "git user.name 설정 확인 방법이 기술되어 있다"
    - "sg-status --team 사용법과 출력 예시가 포함되어 있다"
    - "sg-execute 브랜치 생성 흐름이 간략히 설명되어 있다"
    - ".planning/TEAM.md로 향하는 링크가 있다"
  artifacts:
    - path: "README.md"
      provides: "## Team Workflow 섹션"
      contains: "git user.name, sg-status --team, sg-execute branch"
    - path: "README.ko.md"
      provides: "## 팀 워크플로우 섹션"
      contains: "git user.name, sg-status --team, sg-execute 브랜치"
  key_links:
    - from: "README.md ## Team Workflow"
      to: ".planning/TEAM.md"
      via: "See .planning/TEAM.md 링크"
    - from: "README.ko.md ## 팀 워크플로우"
      to: ".planning/TEAM.md"
      via: ".planning/TEAM.md 링크 동기화"
---

<objective>
`README.md`에 `## Team Workflow` 섹션을 추가하고, `README.ko.md`에 `## 팀 워크플로우` 섹션을 한국어로 동기화한다. 두 섹션 모두 Usage Examples 섹션 직후, Installation 섹션 직전에 삽입한다.

Purpose: 팀원이 README에서 바로 팀 워크플로우 진입점을 발견할 수 있도록 한다. 상세 내용은 `.planning/TEAM.md`로 안내한다.
Output: README.md + README.ko.md 각 1개 섹션 추가 (기존 내용 수정 없음)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/41-team-documentation/41-CONTEXT.md
@.planning/phases/39-handoff-user-tracking/39-CONTEXT.md

<interfaces>
<!-- README.md 현재 섹션 순서 (삽입 위치 확인용) -->
<!-- ## Usage Examples → (신규 ## Team Workflow) → ## Installation -->
<!-- README.ko.md: ## 사용 예시 → (신규 ## 팀 워크플로우) → ## 설치 -->

sg-status --team 출력 형식 (39-CONTEXT.md §B):
```
## 팀 현황

| 팀원 | 최근 Phase | 최근 Stage | 마지막 활동 |
| ---- | --------- | --------- | ---------- |
| Alice | 41-team-docs | plan | 2026-05-29T10:00Z |
```

브랜치명 패턴: phase/{PHASE_PAD}-{slug} (예: phase/41-team-documentation)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md에 ## Team Workflow 섹션 삽입</name>
  <files>README.md</files>
  <read_first>
    - `README.md` 전체 — 현재 `## Usage Examples` 섹션 끝과 `## Installation` 섹션 시작 위치를 정확히 파악한다 (삽입 지점)
    - `.planning/phases/41-team-documentation/41-CONTEXT.md` §D (섹션 제목 `## Team Workflow`, 삽입 위치 결정) §specifics (출력 예시)
    - `.planning/phases/39-handoff-user-tracking/39-CONTEXT.md` §B (sg-status --team 출력 형식)
  </read_first>
  <action>
    README.md의 `## Usage Examples` 섹션 마지막 줄과 `## Installation` 섹션 첫 줄 사이에 `## Team Workflow` 섹션을 삽입한다. 기존 내용은 한 글자도 수정하지 않는다.

    삽입할 섹션 내용:

    ```markdown
    ## Team Workflow

    super-gsd tracks who did what via the `User` column in `.planning/HANDOFF.md`. Set your git identity before starting:

    ```shell
    # Verify your git identity
    git config user.name
    # If empty: git config --global user.name "Your Name"
    ```

    **Check team status:**

    ```shell
    /super-gsd:sg-status --team
    ```

    Outputs a per-member table showing their most recent phase, stage, and last activity timestamp.

    **Branch workflow:**

    When you run `sg-execute` from `main` or `master`, super-gsd detects this and offers to create a `phase/{N}-{slug}` branch (e.g. `phase/41-team-documentation`) via `AskUserQuestion`. After the phase is complete, `sg-phase complete N` prints the `gh pr create` command so you can open a PR without leaving the terminal.

    For branch naming conventions, file ownership rules, and merge order, see [`.planning/TEAM.md`](.planning/TEAM.md).
    ```

    삽입 후 섹션 순서가 `## Usage Examples` → `## Team Workflow` → `## Installation` 순임을 확인한다.
  </action>
  <verify>
    <automated>grep -n "## Team Workflow" /Users/gyuha/workspace/super-gsd/README.md</automated>
  </verify>
  <done>
    - `## Team Workflow` 헤더가 README.md에 존재한다
    - 섹션이 `## Usage Examples` 뒤, `## Installation` 앞에 위치한다
    - git user.name 설정 코드 블록이 포함되어 있다
    - `sg-status --team` 명령이 언급되어 있다
    - `.planning/TEAM.md` 링크가 포함되어 있다
  </done>
</task>

<task type="auto">
  <name>Task 2: README.ko.md에 ## 팀 워크플로우 섹션 삽입</name>
  <files>README.ko.md</files>
  <read_first>
    - `README.ko.md` — `## 사용 예시` 섹션 끝과 `## 설치` 섹션 시작 위치를 파악한다 (삽입 지점)
  </read_first>
  <action>
    README.ko.md의 `## 사용 예시` 섹션 마지막 줄과 `## 설치` 섹션 첫 줄 사이에 `## 팀 워크플로우` 섹션을 삽입한다. Task 1에서 작성한 영문 섹션과 동일한 구조를 한국어로 작성한다. 기존 내용은 한 글자도 수정하지 않는다.

    삽입할 섹션 내용:

    ```markdown
    ## 팀 워크플로우

    super-gsd는 `.planning/HANDOFF.md`의 `User` 컬럼을 통해 팀원별 작업 이력을 자동으로 기록한다. 시작 전 git 사용자 정보를 설정한다:

    ```shell
    # git 사용자 확인
    git config user.name
    # 비어 있으면: git config --global user.name "이름"
    ```

    **팀 현황 확인:**

    ```shell
    /super-gsd:sg-status --team
    ```

    팀원별 최근 phase, stage, 마지막 활동 시각을 테이블로 출력한다.

    **브랜치 워크플로우:**

    `main` 또는 `master` 브랜치에서 `sg-execute`를 실행하면, super-gsd가 이를 감지하고 `AskUserQuestion`으로 `phase/{N}-{slug}` 브랜치(예: `phase/41-team-documentation`) 생성을 제안한다. phase 완료 후 `sg-phase complete N`을 실행하면 PR 생성 명령이 자동으로 출력된다.

    브랜치 명명 규칙, 파일 소유권 규칙, merge 순서는 [`.planning/TEAM.md`](.planning/TEAM.md)를 참고한다.
    ```

    삽입 후 섹션 순서가 `## 사용 예시` → `## 팀 워크플로우` → `## 설치` 순임을 확인한다.
  </action>
  <verify>
    <automated>grep -n "## 팀 워크플로우" /Users/gyuha/workspace/super-gsd/README.ko.md</automated>
  </verify>
  <done>
    - `## 팀 워크플로우` 헤더가 README.ko.md에 존재한다
    - 섹션이 `## 사용 예시` 뒤, `## 설치` 앞에 위치한다
    - git user.name 설정 코드 블록이 포함되어 있다
    - `sg-status --team` 명령이 언급되어 있다
    - `.planning/TEAM.md` 링크가 포함되어 있다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문서 → 독자 | README 섹션이 실제 구현과 불일치하면 혼란 발생 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02 | Tampering | README.md, README.ko.md | accept | 문서 파일 — 코드 실행 없음. git history로 추적 가능 |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜에 패키지 설치 없음 |
</threat_model>

<verification>
- README.md 섹션 위치 확인: `grep -n "## Team Workflow\|## Usage Examples\|## Installation" README.md`
- README.ko.md 섹션 위치 확인: `grep -n "## 팀 워크플로우\|## 사용 예시\|## 설치" README.ko.md`
- TEAM.md 링크 확인: `grep "TEAM.md" README.md README.ko.md`
</verification>

<success_criteria>
- README.md: `## Team Workflow` 섹션이 `## Usage Examples` 뒤, `## Installation` 앞에 있다
- README.ko.md: `## 팀 워크플로우` 섹션이 `## 사용 예시` 뒤, `## 설치` 앞에 있다
- 두 파일 모두: git user.name 설정, sg-status --team, 브랜치 워크플로우, TEAM.md 링크를 포함한다
- 기존 섹션 내용이 변경되지 않았다
</success_criteria>

<output>
완료 시 `.planning/phases/41-team-documentation/41-02-SUMMARY.md` 생성
</output>
