---
phase: quick-260527-w6z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-status/SKILL.md
  - .agents/skills/sg-status/SKILL.md
  - CLAUDE.md
autonomous: true
requirements: [QUICK-sg-status-milestones]
must_haves:
  truths:
    - "sg-status가 5줄 워크플로우 상태 위에 먼저 마일스톤 요약과 현재 마일스톤 단계 진행 테이블을 출력한다"
    - "산문과 테이블 헤더는 사용자 언어로, 머신 토큰은 영어로 유지된다"
    - "기존 5줄 상태 블록(Phase/Stage/Last handoff/Next 영어 라벨)이 맨 마지막에 보존된다"
    - ".agents/skills/sg-status/SKILL.md도 동일한 로직 변경을 받는다"
    - "CLAUDE.md Conventions에 사용자 언어 메시지 규약이 추가된다"
  artifacts:
    - path: "skills/sg-status/SKILL.md"
      provides: "마일스톤 요약 + 현재 마일스톤 단계 테이블 렌더링 단계(신규 Step 6)와 완화된 success_criteria"
      contains: "ROADMAP.md"
    - path: ".agents/skills/sg-status/SKILL.md"
      provides: "Codex/Gemini 미러의 동일 로직 변경"
      contains: "ROADMAP.md"
    - path: "CLAUDE.md"
      provides: "사용자 언어 메시지 규약"
      contains: "사용자 언어 메시지"
  key_links:
    - from: "skills/sg-status/SKILL.md"
      to: ".planning/ROADMAP.md"
      via: "Read 도구로 ## Milestones / ## Progress 파싱"
      pattern: "ROADMAP\\.md"
    - from: "skills/sg-status/SKILL.md"
      to: ".planning/STATE.md"
      via: "milestone: 필드로 현재 마일스톤 식별"
      pattern: "milestone:"
---

<objective>
`sg-status` 스킬이 기존 5줄 워크플로우 상태 출력에 더해, **먼저** 마일스톤 요약과 현재 마일스톤(STATE.md `milestone:`)에 한정된 단계 진행 테이블을 출력하도록 변경한다. 산문과 테이블 헤더는 사용자 언어로 지역화하되, 머신 토큰(명령명·enum·슬러그·날짜·타임스탬프)은 영어 그대로 유지한다.

Purpose: 워크플로우 위치만 보여주던 상태 출력에 마일스톤·단계 진행 맥락을 더해, 사용자가 한눈에 현재 마일스톤 안에서의 진척도를 파악할 수 있게 한다. 동시에 향후 스킬이 동일하게 동작하도록 CLAUDE.md에 사용자 언어 메시지 규약을 명문화한다.

Output:
- `skills/sg-status/SKILL.md` — 신규 마일스톤+단계 테이블 단계(Step 6 삽입, 기존 출력 단계는 Step 7로 재번호) + 완화된 success_criteria
- `.agents/skills/sg-status/SKILL.md` — 동일 로직 미러 변경
- `CLAUDE.md` — Conventions 블록에 `### 사용자 언어 메시지` 규약 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/sg-status/SKILL.md
@.agents/skills/sg-status/SKILL.md
@CLAUDE.md

확인된 사실(작업 시작 전 읽기로 검증됨):
- `STATE.md` 3번째 줄에 `milestone: v2.7`, 27번째 줄에 `Phase: 36 (Not started)`.
- `ROADMAP.md` 3번째 줄 `## Milestones` — 항목 형식 `- [x]/- [ ] **vX.Y Name** (date) — desc`.
- `ROADMAP.md` 436번째 줄 `## Progress`, 438번째 줄 헤더 `| Phase | Milestone | Plans Complete | Status | Completed |`. 셀 예: `| 9. sg-retro Skill scaffold | v1.2 | 1/1 | Complete | 2026-05-20 |`.
- 두 SKILL.md 모두 `<language>` 블록이 이미 존재한다(각 파일에 여는 태그 1줄).
- 이 작업은 Markdown 지시 문서(SKILL.md + CLAUDE.md) 편집이다. 런타임 코드 변경 없음, 실행할 테스트 없음.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 두 sg-status SKILL.md에 마일스톤+단계 테이블 단계 추가</name>
  <files>skills/sg-status/SKILL.md, .agents/skills/sg-status/SKILL.md</files>
  <action>
두 파일에 동일한 로직 변경을 적용한다. 단, `.agents/` 미러는 기존 플랫폼 매핑 도구 어휘(platform-mapped tool names)와 enum 매핑(예: `execute`/`hookify` display, `hookify` 스테이지 라우팅)을 그대로 보존하고, 추가되는 로직만 미러링한다. 두 파일 모두 기존 `<language>` 블록을 그대로 둔다.

(1) 신규 단계를 **Step 6으로 삽입**하고, 기존의 "Print output" 단계를 **Step 7로 재번호**한다. 신규 Step 6은 PHASE_LINE/STAGE_DISPLAY/LAST_TS/NEXT_CMD 계산 이후, 5줄 출력 **이전**에 위치한다.

(2) 신규 Step 6은 다음 두 가지를 렌더링한다. 이식성 규약("macOS 셸 이식성")에 따라 깨지기 쉬운 bash 파이프라인 대신 **Read 도구로 `.planning/ROADMAP.md`와 `.planning/STATE.md`를 읽고 Claude가 직접 해석하여 표를 렌더링**하도록 지시한다. `grep -P` 금지, 파이프 구분 마크다운 테이블을 awk로 분할 금지(불가피한 bash 파싱이 필요하면 `cut -d'|'` 또는 `awk -F'|'`만 허용 — 단 여기서는 bash 파싱을 쓰지 말고 Read+해석을 권장).
   (a) **Milestones 섹션**: ROADMAP.md `## Milestones`의 체크박스 목록(`- [x]`/`- [ ]`)을 읽어 완료 상태와 함께 보여준다. 마일스톤 버전 ID(`vX.Y`)와 날짜는 ROADMAP 원문 그대로 유지(verbatim). 섹션 제목과 산문은 사용자 언어로 지역화한다.
   (b) **현재 마일스톤 단계 테이블**: STATE.md `milestone:` 필드(예: `v2.7`)로 현재 마일스톤을 식별하고, ROADMAP.md `## Progress` 테이블(`| Phase | Milestone | Plans Complete | Status | Completed |`)에서 **Milestone 열이 현재 마일스톤과 일치하는 행만** 골라 간결한 테이블로 렌더링한다. 테이블 **헤더는 사용자 언어로 지역화**하되, 셀 값(phase 슬러그 텍스트, `2/3` 같은 플랜 수, 상태 enum 텍스트, 날짜)은 ROADMAP 원문 그대로 둔다.

(3) Step 7(기존 출력 단계)의 5줄 상태 블록은 **바이트 단위로 보존**한다: 라벨 `Phase:`/`Stage:`/`Last handoff:`/`Next:`는 영어로 유지하고, 모든 머신 토큰(명령명 `/super-gsd:sg-plan` 등, 스테이지 enum 값, phase 슬러그, 타임스탬프)도 영어 그대로 둔다. 신규 섹션과 이 블록 사이는 빈 줄 하나로만 구분한다.

(4) Step 6에 지역화 규칙을 명문화한다: 산문 + 테이블 헤더 → 사용자 언어(기존 `<language>` 블록 기준), 머신 토큰 → 영어 verbatim.

(5) `<success_criteria>`를 갱신한다. 기존 "정확히 다섯 줄(D-29 lock)" 잠금을 **완화**한다 — 기준 #1을 다음을 요구하도록 재작성: (a) Milestones 섹션 + 현재 마일스톤 단계 테이블이 **먼저** 나타나고, (b) 워크플로우 상태 블록이 **맨 마지막**에 나타나며 여전히 `Phase:/Stage:/Last handoff:/Next:` 네 줄을 영어 라벨 + 영어 머신 토큰으로 포함한다. 나머지 라우팅/enum 기준은 그대로 유지한다(`skills/` 파일의 #2·#3, `.agents/` 파일의 해당 enum/라우팅 기준 보존).

주의: `<action>` 안에 펜스 코드 블록을 넣지 말 것. 위 지시는 산문이며, 실행자는 각 파일의 기존 스타일(번호 매긴 Step + 필요한 경우 bash 블록)을 따라 직접 편집한다.
  </action>
  <verify>
    <automated>test "$(grep -c '<language>' skills/sg-status/SKILL.md)" = 1 && test "$(grep -c '<language>' .agents/skills/sg-status/SKILL.md)" = 1 && grep -q 'ROADMAP.md' skills/sg-status/SKILL.md && grep -q 'ROADMAP.md' .agents/skills/sg-status/SKILL.md && grep -qE 'milestone' skills/sg-status/SKILL.md && grep -qiE 'Milestones|마일스톤' skills/sg-status/SKILL.md && grep -qiE 'Milestones|마일스톤' .agents/skills/sg-status/SKILL.md && grep -qE '^7\.|7\. \*\*' skills/sg-status/SKILL.md && grep -q 'Phase: ' skills/sg-status/SKILL.md && grep -q 'Next: ' skills/sg-status/SKILL.md && echo PASS</automated>
  </verify>
  <done>두 SKILL.md 모두 (a) ROADMAP.md를 읽어 `## Milestones` 요약 + 현재 마일스톤 단계 테이블을 먼저 렌더링하는 신규 Step 6을 포함하고, (b) 기존 5줄 상태 블록이 Step 7로 보존되며 `Phase:`/`Stage:`/`Last handoff:`/`Next:` 영어 라벨과 영어 머신 토큰을 유지하고, (c) success_criteria #1이 "마일스톤+테이블 먼저, 워크플로우 블록 마지막"으로 완화되며 라우팅/enum 기준은 보존된다. `.agents/` 파일은 기존 플랫폼 도구 어휘를 유지한 채 동일 로직을 미러링한다.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md Conventions에 사용자 언어 메시지 규약 추가</name>
  <files>CLAUDE.md</files>
  <action>
`CLAUDE.md`의 `## Conventions` 블록(`&lt;!-- GSD:conventions-start --&gt;` ~ `&lt;!-- GSD:conventions-end --&gt;` 마커 사이) 안에 신규 하위 섹션 `### 사용자 언어 메시지`를 추가한다(기존 `### macOS 셸 이식성` 다음, `&lt;!-- GSD:conventions-end --&gt;` 마커 앞이 자연스러운 위치). 한국어로 작성한다.

규약 내용: 스킬 스크립트를 작성할 때, 사용자에게 보이는 최종 메시지는 하드코딩된 영어 echo/printf 문자열을 그대로 출력하는 대신 **각 SKILL.md의 `<language>` 지시에 따라 사용자 언어로 표면화**한다. 단, 머신 토큰(명령명, enum 값, 슬러그, 파일 경로, 타임스탬프)은 영어로 유지한다. 이는 v2.7 Internationalization 마일스톤(영어 소스 + 응답 시 `<language>` 자동 감지)과 일관된다는 점을 명시한다.

기존 마커 주석과 다른 하위 섹션은 건드리지 않는다(Surgical Change).
  </action>
  <verify>
    <automated>grep -q '### 사용자 언어 메시지' CLAUDE.md && awk '/GSD:conventions-start/{s=1} /GSD:conventions-end/{s=0} s &amp;&amp; /### 사용자 언어 메시지/{f=1} END{exit !f}' CLAUDE.md && grep -q 'GSD:conventions-start' CLAUDE.md && grep -q 'GSD:conventions-end' CLAUDE.md && echo PASS</automated>
  </verify>
  <done>`CLAUDE.md`의 conventions 마커 사이에 `### 사용자 언어 메시지` 하위 섹션이 존재하고, 사용자 언어 표면화 + 머신 토큰 영어 유지 + v2.7 i18n 일관성을 한국어로 설명한다. 기존 마커와 다른 규약 하위 섹션은 변경되지 않는다.</done>
</task>

</tasks>

<verification>
- `skills/sg-status/SKILL.md`와 `.agents/skills/sg-status/SKILL.md` 둘 다 ROADMAP.md를 참조하며 마일스톤 섹션과 현재 마일스톤 단계 테이블을 먼저 출력한다.
- 두 파일의 기존 `<language>` 블록이 그대로 유지된다(각 파일 여는 태그 1줄).
- 두 파일의 5줄 상태 블록이 보존되어 `Phase:`/`Stage:`/`Last handoff:`/`Next:` 영어 라벨을 포함한다.
- `CLAUDE.md` conventions 마커 사이에 `### 사용자 언어 메시지` 규약이 추가된다.
- 버전 범프는 이 작업 범위에 포함하지 않는다.
</verification>

<success_criteria>
1. 두 SKILL.md 모두 신규 Step 6(마일스톤 요약 + 현재 마일스톤 단계 테이블)을 가지며, 산문·테이블 헤더는 사용자 언어, 머신 토큰은 영어로 명시된 지역화 규칙을 포함한다.
2. 두 SKILL.md의 기존 5줄 상태 블록이 맨 마지막(Step 7)에 영어 라벨·영어 머신 토큰으로 보존되고, success_criteria #1이 "마일스톤+테이블 먼저, 워크플로우 블록 마지막"으로 완화되며 라우팅/enum 기준은 유지된다.
3. `.agents/` 미러는 기존 플랫폼 도구 어휘를 유지한 채 동일 로직을 받는다(누락 시 Phase 32 Medium-1 블로커).
4. `CLAUDE.md` conventions 마커 사이에 `### 사용자 언어 메시지` 규약이 한국어로 추가된다.
</success_criteria>

<output>
완료 시 `.planning/quick/260527-w6z-sg-status-milestones-phase-5-claude-md-s/260527-w6z-SUMMARY.md`를 생성한다.
</output>
