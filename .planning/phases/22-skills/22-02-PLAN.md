---
phase: 22
plan: 02
type: implementation
wave: 1
depends_on: []
files_modified:
  - skills/sg-start/SKILL.md
  - skills/sg-status/SKILL.md
  - skills/sg-health/SKILL.md
autonomous: true
requirements:
  - SC-02
  - SC-05
  - SC-06

must_haves:
  truths:
    - "skills/sg-start/SKILL.md 가 존재하고 name, description frontmatter를 포함한다"
    - "skills/sg-status/SKILL.md 가 존재하고 name, description frontmatter를 포함한다"
    - "skills/sg-health/SKILL.md 가 존재하고 name, description frontmatter를 포함한다"
    - "3개 파일 모두 <objective>, <process>, <success_criteria> 블록을 포함한다"
  artifacts:
    - path: "skills/sg-start/SKILL.md"
      provides: "sg-start 슬래시 명령 정의"
      contains: "name: sg-start"
    - path: "skills/sg-status/SKILL.md"
      provides: "sg-status 슬래시 명령 정의"
      contains: "name: sg-status"
    - path: "skills/sg-health/SKILL.md"
      provides: "sg-health 슬래시 명령 정의"
      contains: "name: sg-health"
---

<objective>
SC-02 구현: `skills/sg-start/SKILL.md`, `skills/sg-status/SKILL.md`, `skills/sg-health/SKILL.md` 를 생성한다.
세션·상태·진단 계열 3개 파일을 commands/ 에서 skills/ 로 복사한다.

Output artifacts:
- `skills/sg-start/SKILL.md`
- `skills/sg-status/SKILL.md`
- `skills/sg-health/SKILL.md`
</objective>

<tasks>
<task>
<name>skills/sg-start/SKILL.md 생성</name>
<files>skills/sg-start/SKILL.md</files>
<action>
1. `mkdir -p skills/sg-start` 실행
2. `commands/sg-start.md` 전체 내용을 읽는다
3. `skills/sg-start/SKILL.md` 를 동일 내용으로 생성한다
</action>
<verify>
- `ls skills/sg-start/SKILL.md` 가 존재
- `grep 'name: sg-start' skills/sg-start/SKILL.md` 가 매칭 (SC-05: name 키)
- `grep 'description:' skills/sg-start/SKILL.md` 가 매칭 (SC-05: description 키)
- `grep 'argument-hint' skills/sg-start/SKILL.md` 가 매칭 또는 commands/sg-start.md에 argument-hint 없으면 생략 허용 (SC-05: Discretion — sg-retro 선례에 따라 원본에 없으면 생략 가능)
- `grep '<objective>' skills/sg-start/SKILL.md` 가 매칭 (SC-06: objective 블록)
- `grep '<process>' skills/sg-start/SKILL.md` 가 매칭 (SC-06: process 블록)
- `grep '<success_criteria>' skills/sg-start/SKILL.md` 가 매칭 (SC-06: success_criteria 블록)
</verify>
<done>true</done>
</task>

<task>
<name>skills/sg-status/SKILL.md 생성</name>
<files>skills/sg-status/SKILL.md</files>
<action>
1. `mkdir -p skills/sg-status` 실행
2. `commands/sg-status.md` 전체 내용을 읽는다
3. `skills/sg-status/SKILL.md` 를 동일 내용으로 생성한다
</action>
<verify>
- `ls skills/sg-status/SKILL.md` 가 존재
- `grep 'name: sg-status' skills/sg-status/SKILL.md` 가 매칭 (SC-05: name 키)
- `grep 'description:' skills/sg-status/SKILL.md` 가 매칭 (SC-05: description 키)
- `grep 'argument-hint' skills/sg-status/SKILL.md` 가 매칭 또는 원본에 없으면 생략 허용 (SC-05 Discretion)
- `grep '<objective>' skills/sg-status/SKILL.md` 가 매칭 (SC-06: objective 블록)
- `grep '<process>' skills/sg-status/SKILL.md` 가 매칭 (SC-06: process 블록)
- `grep '<success_criteria>' skills/sg-status/SKILL.md` 가 매칭 (SC-06: success_criteria 블록)
</verify>
<done>true</done>
</task>

<task>
<name>skills/sg-health/SKILL.md 생성</name>
<files>skills/sg-health/SKILL.md</files>
<action>
1. `mkdir -p skills/sg-health` 실행
2. `commands/sg-health.md` 전체 내용을 읽는다
3. `skills/sg-health/SKILL.md` 를 동일 내용으로 생성한다
</action>
<verify>
- `ls skills/sg-health/SKILL.md` 가 존재
- `grep 'name: sg-health' skills/sg-health/SKILL.md` 가 매칭 (SC-05: name 키)
- `grep 'description:' skills/sg-health/SKILL.md` 가 매칭 (SC-05: description 키)
- `grep 'argument-hint' skills/sg-health/SKILL.md` 가 매칭 또는 원본에 없으면 생략 허용 (SC-05 Discretion)
- `grep '<objective>' skills/sg-health/SKILL.md` 가 매칭 (SC-06: objective 블록)
- `grep '<process>' skills/sg-health/SKILL.md` 가 매칭 (SC-06: process 블록)
- `grep '<success_criteria>' skills/sg-health/SKILL.md` 가 매칭 (SC-06: success_criteria 블록)
</verify>
<done>true</done>
</task>
</tasks>

<success_criteria>
1. `skills/sg-start/SKILL.md`, `skills/sg-status/SKILL.md`, `skills/sg-health/SKILL.md` 3개 파일이 모두 존재한다
2. 각 파일에 `name:`, `description:` frontmatter 키가 있다 (SC-05); `argument-hint`는 commands/ 원본에 있으면 유지, 없으면 생략 허용 (SC-05 Discretion)
3. 각 파일에 `<objective>`, `<process>`, `<success_criteria>` 블록이 있다 (SC-06)
</success_criteria>
</output>
