# Lessons: Phase 47 (2026-06-01)

## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: 2026-06-01T07:04:38Z_
_Intent: capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase._

### Decisions
- **D-09 동시 갱신 규칙:** README.md와 README.ko.md는 단일 플랜(47-01) 내 두 태스크로 동시 처리 — 단일 커밋 `a0426ef`에서 두 파일 함께 커밋 (47-CONTEXT.md D-09)
- **sg-tdd grep 카운트 3 (플랜 기대값 4와 불일치):** 테이블 행 구조상 명령명과 설명이 한 줄에 있어 `grep -c`는 3을 반환. PLAN acceptance criteria가 4 이상을 기대했으나 3이 정확. 플래너가 grep 카운트 기준을 테이블 구조 없이 추정한 것 (47-01-SUMMARY.md decisions 항목)

### Surprises
- **Feature Delta 표 카운트 누락 (commit `fab5c97`):** README.md/ko.md 232번째 줄의 스킬 커버리지 표 (`21/21` → `22/22`)가 플랜에 포함되지 않아 코드 리뷰에서 Important 이슈로 발견
- **Roadmap 주석 카운트 누락 (commit `fab5c97`):** README.md/ko.md 303번째 줄의 "Expanded to 21 commands" 문자열도 플랜 누락 → 리뷰에서 발견. 동일 커밋에서 수정

### Patterns
- **단일 커밋 다중 파일 원자적 처리:** 3개 파일 외과적 삽입이 `a0426ef` 단일 커밋으로 처리 — DOC-01, DOC-02 커버리지를 원자적 달성
- **코드 리뷰의 문서 커버리지 사각지대 발견:** 플래너가 주요 삽입 위치에 집중하면서 같은 파일 내 카운트 참조(Feature Delta 표, Roadmap 주석)를 놓침. 코드 리뷰 없으면 스테일 카운트 머지됨

### Mistakes
- **문서 파일 내 모든 숫자 참조 스캔 부재:** PLAN tasks가 주요 삽입 위치에 집중하면서 동일 파일 내 다른 위치(`21 of 21` Feature Delta 표, `21 commands` Roadmap 주석)를 누락. 코드 리뷰 Important 이슈 2건으로 발견
- **grep acceptance criteria의 카운트 오류:** `grep -c "sg-tdd" >= 4` 기준이 테이블 행 구조를 간과하여 False-fail 위험 (리뷰어 non-blocking 판정)

### Action Items
| priority | item | next step |
|----------|------|-----------|
| 🔴 P1 | 문서 갱신 플랜에 전체 파일 카운트 참조 스캔 태스크 추가 | 문서 업데이트 플랜 작성 시 `grep -rn "현재 N개"` 패턴으로 모든 숫자 참조 찾는 태스크 명시 |
| P2 | PLAN acceptance criteria의 grep -c 기준을 테이블 구조 반영하여 정확히 명시 | `grep -c` 대신 실제 구조를 고려한 기준 또는 `grep -l` (존재 여부)로 변경 검토 |
| P3 | "메타 카운트" 참조 위치를 CONTEXT.md canonical_refs에 명시 | 다음 문서 갱신 phase 계획 시 canonical_refs에 카운트 참조 위치 목록 추가 |


## Lens: Start/Stop/Continue
_Captured: 2026-06-01T07:05:03Z_
_Intent: surface behavior changes — what to start, stop, or continue doing next phase._

### Start
- 문서 갱신 플랜에 "전체 파일 카운트 참조 스캔" 태스크 명시 — 주요 삽입 외 동일 파일 내 Feature Delta 표, Roadmap 주석 등 파생 위치를 `grep -rn "N개\|N of N"` 패턴으로 찾는 별도 태스크 추가 (commit `fab5c97` 수정 사항 예방)
- PLAN acceptance criteria에 테이블 구조를 반영한 정확한 grep 기준 명시 — `grep -c`가 행 단위인지 설명

### Stop
- 문서 갱신 플랜에서 테이블 구조 분석 없이 `grep -c "pattern" >= N` 기준을 경험적으로 설정 — False-fail/False-pass 위험 (47-01-SUMMARY.md decisions 항목에서 발견)
- 주요 삽입 위치만 PLAN tasks에 열거하고 같은 파일 내 파생 참조 위치는 암묵적 가정으로 처리 — Feature Delta 표, Roadmap 주석 등 누락 패턴 재발 방지

### Continue
- 단일 커밋으로 다중 연관 파일 원자적 처리 패턴 — README.md + README.ko.md + CLAUDE.md를 `a0426ef` 단일 커밋으로 처리. 일관성 보장, 리뷰 추적 단순화
- 문서-only phase에도 코드 리뷰 적용 — 이번 phase에서 Important 이슈 2건 발견. 작은 phase에서 리뷰를 생략하지 않는 것이 효과적임을 검증

### Action Items
| priority | item | next step |
|----------|------|-----------|
| 🔴 P1 | 문서 갱신 플랜 template에 "카운트 참조 전체 스캔" 태스크 패턴 추가 | 다음 문서 갱신 phase의 PLAN 첫 태스크로 `grep -rn "현재값"` 스캔 태스크 작성 |
| P2 | grep acceptance criteria 작성 가이드 개선 | PLAN.md 작성 시 테이블 행 단위 카운트와 전체 히트 수를 구분하여 기술 |

