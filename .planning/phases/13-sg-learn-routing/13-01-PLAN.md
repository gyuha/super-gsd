---
phase: 13-sg-learn-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-learn.md
  - commands/sg-update.md
  - README.md
  - README.ko.md
  - docs/COMMANDS.md
  - .claude-plugin/plugin.json
autonomous: false
requirements:
  - MIGRATION-01
  - MIGRATION-02
  - MIGRATION-03
  - MIGRATION-04

must_haves:
  truths:
    - "sg-learn이 hookify:hookify 대신 super-gsd:sg-retro를 호출한다"
    - "hookify 미설치 환경에서 sg-learn이 오류 없이 동작한다"
    - "README/COMMANDS.md/plugin.json에서 hookify가 필수 의존성으로 표기되지 않는다"
    - "sg-update가 hookify를 설치/업데이트하지 않는다"
  artifacts:
    - path: "commands/sg-learn.md"
      provides: "sg-retro Skill 호출"
      contains: "super-gsd:sg-retro"
    - path: "commands/sg-update.md"
      provides: "hookify 블록 제거된 업데이트 커맨드"
    - path: "README.md"
      provides: "hookify Optional 섹션으로 demote된 영문 README"
    - path: "README.ko.md"
      provides: "hookify Optional 섹션으로 demote된 한글 README"
    - path: "docs/COMMANDS.md"
      provides: "sg-learn 항목이 sg-retro 기준으로 업데이트된 레퍼런스"
    - path: ".claude-plugin/plugin.json"
      provides: "hookify keyword 제거된 plugin.json"
  key_links:
    - from: "commands/sg-learn.md"
      to: "super-gsd:sg-retro"
      via: "Skill() 호출"
      pattern: "super-gsd:sg-retro"
---

<objective>
sg-learn 라우팅을 hookify:hookify에서 super-gsd:sg-retro로 전환하고, hookify를 필수 의존성 위치에서 제거하여 super-gsd가 hookify 없이 단독 동작하도록 한다.

Purpose: hookify 설치 없이 sg-learn이 동작하게 하여 super-gsd의 외부 의존성을 줄인다.
Output: sg-learn.md (Skill 교체), sg-update.md (hookify 블록 제거), README.md + README.ko.md (hookify demote), docs/COMMANDS.md (sg-learn 항목 갱신), plugin.json (keyword 정리)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5개 파일 수정 — sg-retro 라우팅 전환 및 hookify 의존성 제거</name>
  <files>
    commands/sg-learn.md,
    commands/sg-update.md,
    README.md,
    README.ko.md,
    docs/COMMANDS.md,
    .claude-plugin/plugin.json
  </files>
  <action>
다음 6개 파일을 아래 명세대로 수정한다.

**1. commands/sg-learn.md (D-01)**

frontmatter의 description을 변경한다:
- 변경 전: "Run a Hookify retrospective to extract patterns and generate hooks from this session."
- 변경 후: "Run a retrospective via sg-retro to extract patterns and generate hooks from this session."

objective 블록을 변경한다:
- 변경 전: "Invoke the hookify:hookify Skill to run a retrospective..."
- 변경 후: "Invoke the super-gsd:sg-retro Skill to run a retrospective..."

execution_context 블록을 변경한다:
- 변경 전: "Self-contained. Delegates entirely to hookify:hookify Skill (terminal action)."
- 변경 후: "Self-contained. Delegates entirely to super-gsd:sg-retro Skill (terminal action)."

process 블록의 step 0.9에서 HANDOFF.md에 기록하는 stage 값은 "hookify" 그대로 유지한다 (D-02, sg-status 라우팅 호환성).

step 1의 Skill 호출을 변경한다:
- 변경 전: `Skill(skill="hookify:hookify", args="$ARGUMENTS")`
- 변경 후: `Skill(skill="super-gsd:sg-retro", args="$ARGUMENTS")`

step 1의 설명 텍스트도 함께 업데이트한다:
- 변경 전: "Session control transfers to the skill; no steps execute after this point:"
- 변경 후: "Session control transfers to the skill; no steps execute after this point:" (동일 유지)

success_criteria를 변경한다:
- 변경 전: "hookify:hookify Skill is invoked exactly once."
- 변경 후: "super-gsd:sg-retro Skill is invoked exactly once."

**2. commands/sg-update.md (D-03/D-04)**

frontmatter description을 변경한다:
- 변경 전: "Check, install, or update GSD, superpowers, hookify, and super-gsd to their latest versions."
- 변경 후: "Check, install, or update GSD, superpowers, and super-gsd to their latest versions."

objective 블록에서 hookify 항목 제거:
- 제거 대상: `- hookify — Claude Code plugin`
- 목록을 GSD, superpowers, super-gsd 3개 항목으로 축소

bash 블록에서 hookify 관련 코드 전체 제거:
- `# hookify` 섹션 전체 (84행~106행, `# hookify`부터 `fi` 닫는 괄호까지) 삭제
- `HOOKIFY_STATUS` 변수를 참조하는 echo 라인 제거: `echo "- hookify: ${HOOKIFY_STATUS}"`
- echo "Updating workflow tools..." 라인의 내용은 유지

summary echo 출력에서 hookify 라인 제거:
- 변경 전:
  ```
  echo "- GSD (get-shit-done-cc): ${GSD_STATUS}"
  echo "- superpowers: ${SUPERPOWERS_STATUS}"
  echo "- hookify: ${HOOKIFY_STATUS}"
  echo "- super-gsd: ${SUPERGSD_STATUS}"
  ```
- 변경 후:
  ```
  echo "- GSD (get-shit-done-cc): ${GSD_STATUS}"
  echo "- superpowers: ${SUPERPOWERS_STATUS}"
  echo "- super-gsd: ${SUPERGSD_STATUS}"
  ```

success_criteria에서 hookify 언급 제거:
- 변경 전: "superpowers, hookify, super-gsd plugins installed or updated..."
- 변경 후: "superpowers, super-gsd plugins installed or updated..."

**3. README.md (D-05)**

Prerequisites 섹션에서 hookify를 필수 항목에서 제거하고 Optional 서브섹션으로 demote한다.

현재 Prerequisites 섹션:
```
- **GSD** (`get-shit-done-cc`) — provides the `/gsd-*` planning commands and the `.planning/` directory convention this plugin reads from.
- **Superpowers** (`claude-plugins-official/superpowers`) — provides the `superpowers:*` skill tree used during the build / review stage.
- **Hookify** (`claude-plugins-official/hookify`) — provides the `/hookify:*` commands used during the retrospection stage.
```

변경 후:
```
- **GSD** (`get-shit-done-cc`) — provides the `/gsd-*` planning commands and the `.planning/` directory convention this plugin reads from.
- **Superpowers** (`claude-plugins-official/superpowers`) — provides the `superpowers:*` skill tree used during the build / review stage.

### Optional

- **Hookify** (`claude-plugins-official/hookify`) — historical retrospection tool. `sg-learn` now routes to the built-in `sg-retro` skill; Hookify is no longer required.
```

Step 2 설치 설명에서 "GSD, Superpowers, and Hookify" 언급을 "GSD and Superpowers"로 변경한다:
- 현재 텍스트 (166행): "`sg-update` detects whether GSD, Superpowers, and Hookify are already installed and installs any that are missing. Running it on a fresh machine will install all three automatically. On an existing setup it updates them to their latest versions."
- 변경 후: "`sg-update` detects whether GSD and Superpowers are already installed and installs any that are missing. Running it on a fresh machine will install both automatically. On an existing setup it updates them to their latest versions."

Verify install 섹션의 step 4 제거 (hookify:help 확인 단계):
- 현재 step 4: "Run `/hookify:help` and confirm Hookify responds with its usual help output — this proves Hookify remains intact and unmodified."
- 제거 후 step 3에서 마무리. "If all three checks pass..." 로 결론 문구 수정.

Commands 테이블에서 sg-learn 행의 설명을 업데이트한다 (35행):
- 변경 전: "Run a Hookify retrospective to extract patterns and generate hooks (`hookify:hookify`)"
- 변경 후: "Run a retrospective via `sg-retro` to extract patterns and generate hooks"

sg-update 행의 설명도 업데이트한다 (41행):
- 변경 전: "Check, install, or update GSD, superpowers, hookify, and super-gsd (installs missing tools automatically)"
- 변경 후: "Check, install, or update GSD, superpowers, and super-gsd (installs missing tools automatically)"

**4. README.ko.md (D-05)**

영문 README와 동일한 변경을 한국어 문서에 적용한다.

필수 도구 섹션에서 Hookify 항목을 제거하고 Optional 서브섹션으로 demote:
```
- **GSD** (`get-shit-done-cc`) — 이 플러그인이 읽는 `/gsd-*` 계획 명령과 `.planning/` 디렉터리 컨벤션을 제공한다.
- **Superpowers** (`claude-plugins-official/superpowers`) — 빌드/리뷰 단계에서 사용하는 `superpowers:*` 스킬 트리를 제공한다.

### 선택적 도구

- **Hookify** (`claude-plugins-official/hookify`) — 구버전 회고 도구. `sg-learn`은 이제 내장 `sg-retro` 스킬로 라우팅되므로 Hookify는 더 이상 필수가 아니다.
```

sg-update 설명(166행 근처)에서 "GSD, Superpowers, Hookify" → "GSD, Superpowers"로 변경:
- 변경 전: "`sg-update`는 GSD, Superpowers, Hookify의 설치 여부를 감지하고 없는 것을 자동으로 설치한다. 새 머신에서 실행하면 세 가지를 모두 자동으로 설치한다."
- 변경 후: "`sg-update`는 GSD, Superpowers의 설치 여부를 감지하고 없는 것을 자동으로 설치한다. 새 머신에서 실행하면 두 가지를 모두 자동으로 설치한다."

설치 확인 섹션의 step 4 제거 (`/hookify:help` 확인 단계):
- step 4 제거 후 결론 문구를 "세 가지 확인이 모두 통과하면..." → "세 가지 확인이 모두 통과하면..." 으로 유지 (step이 3개로 줄어들었으므로 실제로는 "세 가지"가 자연스럽게 맞음). 단, step 번호가 1~3이 되도록 재정렬.

명령어 테이블에서 sg-learn 행 업데이트 (35행):
- 변경 전: "Hookify 회고 실행, 패턴 추출 및 훅 생성 (`hookify:hookify`)"
- 변경 후: "`sg-retro`를 통한 회고 실행, 패턴 추출 및 훅 생성"

sg-update 행 업데이트 (41행):
- 변경 전: "GSD, superpowers, hookify, super-gsd 설치 여부 확인 후 설치 또는 업데이트"
- 변경 후: "GSD, superpowers, super-gsd 설치 여부 확인 후 설치 또는 업데이트"

**5. docs/COMMANDS.md (D-06)**

Quick Reference 테이블에서 sg-learn 행 업데이트:
- 변경 전: `| `/super-gsd:sg-learn` | `hookify:hookify` | (none) | Run Hookify retrospective to capture patterns |`
- 변경 후: `| `/super-gsd:sg-learn` | `super-gsd:sg-retro` | (none) | Run retrospective via sg-retro to capture patterns |`

sg-learn 섹션 본문 업데이트:
- Maps to 라인: `hookify:hookify` → `super-gsd:sg-retro`
- What it does: "Invokes the `hookify:hookify` Skill..." → "Invokes the `super-gsd:sg-retro` Skill to run a retrospective and extract learnable patterns from the current session."

Workflow 다이어그램 (11행) 업데이트:
- 변경 전: `sg-start → ... → sg-learn → sg-ship` 줄에서 `(Hookify)` 레이블
- 변경 후: `(sg-retro)` 로 변경

Workflow Guide 하단 6번 항목 업데이트:
- 변경 전: "**`sg-learn`** — run a Hookify retrospective after the review."
- 변경 후: "**`sg-learn`** — run a retrospective via `sg-retro` after the review."

**6. .claude-plugin/plugin.json (D-07)**

keywords 배열에서 "hookify" 제거:
- 변경 전: `["gsd", "superpowers", "hookify", "orchestration", "workflow", "claude-code"]`
- 변경 후: `["gsd", "superpowers", "orchestration", "workflow", "claude-code"]`

description 필드에서 "Hookify" 를 "sg-retro" 로 교체:
- 변경 전: "Orchestrator plugin that auto-chains GSD -> Superpowers -> Hookify so planning, implementation, and retrospection stay connected as a single learning loop."
- 변경 후: "Orchestrator plugin that auto-chains GSD -> Superpowers -> sg-retro so planning, implementation, and retrospection stay connected as a single learning loop."
  </action>
  <verify>
    <automated>
grep -n "super-gsd:sg-retro" /Users/gyuha/workspace/super-gsd/commands/sg-learn.md | head -5
grep -c "hookify" /Users/gyuha/workspace/super-gsd/commands/sg-update.md
grep -c "hookify:hookify" /Users/gyuha/workspace/super-gsd/docs/COMMANDS.md
python3 -c "import json; d=json.load(open('/Users/gyuha/workspace/super-gsd/.claude-plugin/plugin.json')); print('hookify in keywords:', 'hookify' in d['keywords'])"
    </automated>
  </verify>
  <done>
- commands/sg-learn.md: Skill 호출이 `super-gsd:sg-retro`를 참조하고 hookify 언급이 없다 (HANDOFF stage 값 "hookify" 제외)
- commands/sg-update.md: hookify 설치/업데이트 블록이 없고, 요약 출력에 hookify 라인이 없다
- README.md + README.ko.md: Prerequisites에 hookify가 필수 항목이 아닌 Optional 서브섹션에만 존재한다
- docs/COMMANDS.md: sg-learn 항목이 super-gsd:sg-retro 기준으로 기술된다
- plugin.json: keywords에 "hookify"가 없고 description이 "sg-retro"를 참조한다
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: MIGRATION-02 e2e 체크리스트 — hookify 미설치 환경 검증 (D-08)</name>
  <what-built>
Task 1에서 sg-learn.md를 super-gsd:sg-retro로 교체하고, sg-update에서 hookify 블록을 제거했으며, README/COMMANDS.md/plugin.json에서 hookify 의존 표기를 제거했다.
  </what-built>
  <how-to-verify>
다음 체크리스트를 순서대로 확인한다. hookify가 설치된 환경이라면 hookify를 비활성화하거나 미설치 상태를 시뮬레이션한다.

**[A] sg-learn 라우팅 전환 확인**
1. `/super-gsd:sg-learn`을 실행한다.
2. 실행 흐름이 hookify:hookify가 아닌 super-gsd:sg-retro로 진입하는지 확인한다.
3. HANDOFF.md에 stage 값이 "hookify"로 기록되는지 확인한다 (sg-status 호환성 유지 여부).

**[B] sg-update hookify 제외 확인**
1. `/super-gsd:sg-update`를 실행한다.
2. 출력 요약에 hookify 라인이 없는지 확인한다.
   기대 출력 형식:
   ```
   Tools:
   - GSD (get-shit-done-cc): ...
   - superpowers: ...
   - super-gsd: ...
   ```
3. hookify 관련 "Installing hookify..." 또는 "Updating hookify..." 메시지가 없는지 확인한다.

**[C] README 확인**
1. README.md의 Prerequisites 섹션을 확인한다.
2. hookify가 필수 항목 목록에 없고 Optional 서브섹션에만 있는지 확인한다.
3. README.ko.md에서도 동일하게 적용됐는지 확인한다.

**[D] docs/COMMANDS.md 확인**
1. `docs/COMMANDS.md`를 열고 sg-learn 섹션을 확인한다.
2. "Maps to:" 항목이 `super-gsd:sg-retro`로 표기되는지 확인한다.

**[E] plugin.json 확인**
1. `.claude-plugin/plugin.json`을 열고 `keywords` 배열을 확인한다.
2. "hookify"가 없는지 확인한다.
  </how-to-verify>
  <resume-signal>모든 체크리스트 항목이 통과했으면 "approved"라고 입력한다. 실패한 항목이 있으면 어떤 항목이 실패했는지 설명한다.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill 호출 | commands/sg-learn.md에서 외부 Skill을 invoke하는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | commands/sg-learn.md Skill 교체 | accept | 내부 플러그인 스킬로 교체; super-gsd:sg-retro는 동일 플러그인 소유 |
| T-13-02 | Denial of Service | sg-retro Skill 미설치 환경 | mitigate | Task 2 e2e 체크리스트에서 sg-retro 정상 동작 확인 |
</threat_model>

<verification>
- `grep "super-gsd:sg-retro" commands/sg-learn.md` — Skill 교체 확인
- `grep -c "hookify" commands/sg-update.md` — 0이어야 함 (HANDOFF stage 값 제외)
- `grep "hookify:hookify" docs/COMMANDS.md` — 없어야 함
- `python3 -c "import json; d=json.load(open('.claude-plugin/plugin.json')); assert 'hookify' not in d['keywords']"` — keyword 제거 확인
</verification>

<success_criteria>
1. commands/sg-learn.md가 `Skill(skill="super-gsd:sg-retro", ...)` 호출을 포함하고 hookify 언급이 없다 (HANDOFF stage "hookify" 값 제외).
2. commands/sg-update.md에 hookify 설치/업데이트 블록이 없고 요약 출력에 hookify 라인이 없다.
3. README.md와 README.ko.md의 Prerequisites에서 hookify가 필수 항목이 아닌 Optional 서브섹션으로만 존재한다.
4. docs/COMMANDS.md의 sg-learn 항목이 super-gsd:sg-retro 기준으로 기술된다.
5. plugin.json의 keywords 배열에 "hookify"가 없다.
6. Task 2 e2e 체크리스트가 사람 손으로 검증되어 "approved" 신호가 수신된다.
</success_criteria>

<output>
완료 후 `.planning/phases/13-sg-learn-routing/13-01-SUMMARY.md`를 생성한다.
</output>
