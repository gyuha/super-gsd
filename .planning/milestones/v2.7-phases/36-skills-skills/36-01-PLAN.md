---
phase: 36-skills-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-retro/SKILL.md
  - skills/sg-next/SKILL.md
  - skills/sg-parallel-execute/SKILL.md
  - skills/sg-start/SKILL.md
  - skills/sg-health/SKILL.md
autonomous: true
requirements:
  - I18N-01

must_haves:
  truths:
    - "skills/sg-retro/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-next/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-parallel-execute/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-start/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-health/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "bash code block content (commands, flags, variable names) is unchanged in all 5 files"
    - "YAML frontmatter field names and structure are unchanged in all 5 files"
  artifacts:
    - path: "skills/sg-retro/SKILL.md"
      provides: "Translated sg-retro skill (53 Korean lines converted)"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-next/SKILL.md"
      provides: "Translated sg-next skill (32 Korean lines converted)"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-parallel-execute/SKILL.md"
      provides: "Translated sg-parallel-execute skill (31 Korean lines converted)"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-start/SKILL.md"
      provides: "Translated sg-start skill (30 Korean lines converted)"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-health/SKILL.md"
      provides: "Translated sg-health skill (30 Korean lines converted)"
      contains: "no Korean characters in prose sections"
  key_links:
    - from: "skills/sg-retro/SKILL.md"
      to: "bash pipeline comment lines"
      via: "text-only rule"
      pattern: "comments inside bash fences remain Korean-free after translation"
---

<objective>
Translate Korean prose content in the 5 highest-Korean-volume SKILL.md files to English. Per I18N-01, only prose text is translated — bash code block commands, flags, and variable names are never modified (text-only rule).

Purpose: Enable non-Korean users to understand and follow skill instructions without language barriers.
Output: 5 modified SKILL.md files with all Korean prose replaced by equivalent English text, bash code untouched, YAML frontmatter structure unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate sg-retro and sg-next SKILL.md files (I18N-01)</name>
  <files>skills/sg-retro/SKILL.md, skills/sg-next/SKILL.md</files>
  <read_first>
    skills/sg-retro/SKILL.md
    skills/sg-next/SKILL.md
  </read_first>
  <action>
Translate all Korean prose text in skills/sg-retro/SKILL.md and skills/sg-next/SKILL.md to English.

TEXT-ONLY RULE (non-negotiable): Content inside fenced bash code blocks (between triple backtick lines) is NEVER modified — no changes to commands, flags, variable names, string literals within bash, or comment text inside bash fences. The only exception: bash comment lines that contain pure Korean prose explanations may be translated, but the comment marker (#) and variable references remain unchanged.

For skills/sg-retro/SKILL.md, translate these specific prose locations:
- <objective> block: already in English — verify no Korean present
- <execution_context> block: verify no Korean present
- <process> Step headers and prose paragraphs between bash fences (e.g. "macOS 호환 파이프라인" → "macOS-compatible pipeline", "정수 또는 소수점 phase 허용" → "integer or decimal phase allowed", etc.)
- Inline bash comments that are pure Korean prose (e.g. "# D-02: 정수 또는 소수점 phase 허용 (예: 7, 7.1)" → "# D-02: integer or decimal phase allowed (e.g. 7 or 7.1)")
- Korean text in AskUserQuestion option descriptions and question strings
- Five Whys sub-block Korean facilitation instructions
- Sub-block sail Korean facilitation notes
- Sub-block analyze Korean signal keywords (frustration signals list: "왜", "안 돼", "다시", "틀렸" — translate these as English equivalents or keep as quoted examples with English explanation)
- Step 6 bash comment block explanations in Korean
- After-loop auto-suggest Korean comments
- HANDOFF.md record block Korean comments
- <success_criteria> items that have Korean text

For skills/sg-next/SKILL.md, translate these specific prose locations:
- Step 1 header "STATE.md Phase 파싱:" → "STATE.md Phase parsing:"
- Bash comment block markers (BEGIN/END blocks have Korean drift comments) → translate inline
- Step 2 header "HANDOFF.md stage 감지 + enum 매핑:" → "HANDOFF.md stage detection + enum mapping:"
- Step 3 header "NEXT_PHASE 계산 + stage→next-command 라우팅:" → "NEXT_PHASE computation + stage→next-command routing:"
- Inline bash comment "# sg-next는 메타-전환 행이므로 직전 FROM 컬럼..." → English
- Step 4 header + prose about complete/init append deferral
- Step 5 AskUserQuestion strings (question text, option labels, descriptions) — these are currently in Korean
- Step 6 bash comment "# NEXT_CMD 기준 Skill() 매핑:" → "# Skill() mapping by NEXT_CMD:"
- Bullet list of Skill() mappings with Korean comments (인자 없음 → "no argument")
- <success_criteria> items with Korean text

CRITICAL: Do NOT change any of the following in bash blocks:
- Variable names: PHASE_RAW, LENS_RAW, PHASE_PAD, PHASE_DIR, etc.
- Command names: grep, sed, awk, ls, printf, etc.
- String literal patterns: '^Phase:', '^[0-9]+(\.[0-9]+)?$', etc.
- Structural markers: BEGIN/END block comment markers (keep the D-XX labels)
  </action>
  <verify>
    <automated>grep -c '[가-힣]' /Users/gyuha/workspace/super-gsd/skills/sg-retro/SKILL.md && grep -c '[가-힣]' /Users/gyuha/workspace/super-gsd/skills/sg-next/SKILL.md</automated>
  </verify>
  <done>Both files return 0 from grep -c '[가-힣]'. YAML frontmatter field names unchanged. Bash code content (commands, flags, variable names) unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Translate sg-parallel-execute, sg-start, and sg-health SKILL.md files (I18N-01)</name>
  <files>skills/sg-parallel-execute/SKILL.md, skills/sg-start/SKILL.md, skills/sg-health/SKILL.md</files>
  <read_first>
    skills/sg-parallel-execute/SKILL.md
    skills/sg-start/SKILL.md
    skills/sg-health/SKILL.md
  </read_first>
  <action>
Translate all Korean prose text in skills/sg-parallel-execute/SKILL.md, skills/sg-start/SKILL.md, and skills/sg-health/SKILL.md to English. Apply the same TEXT-ONLY RULE as Task 1.

For skills/sg-parallel-execute/SKILL.md, translate:
- <objective> block: "전달된" → "passed via", "파악한다" → "to identify group list", "배열 길이" → "array length", etc. Translate the full objective prose to natural English.
- Step headers and prose: "입력 검증" → "Input validation", "읽기" → "Reading", "GROUP_COUNT 계산 및 실행 그룹 결정" → "Compute GROUP_COUNT and determine execution groups", etc.
- Step 3 prose: "파싱된 배열의 길이 = GROUP_COUNT" → "Length of parsed array = GROUP_COUNT", "각 항목은..." format description → English
- Step 5 prose description about Task() prompt structure
- Step 6 prose about sequential processing
- Inline bash comment "# wave 오름차순으로 정렬한 뒤 앞 EXEC_COUNT개를 병렬 실행" → English
- <success_criteria> items that are in Korean (items 1-5 look English, verify)

For skills/sg-start/SKILL.md, translate:
- Step 1 header "STATE.md Phase 파싱 (D-01, D-03; Phase 7 D-07 inline-replication lock)." → "STATE.md Phase parsing (D-01, D-03; Phase 7 D-07 inline-replication lock)."
- The prose "skills/sg-status/SKILL.md lines 17-21 블록을 글자 그대로 복제한다 (drift 시 양쪽 동시 수정):" → "Replicate the skills/sg-status/SKILL.md lines 17-21 block verbatim (if drift occurs, update both simultaneously):"
- Step 1 bash comment about D-01 trigger determination prose
- Step 2 header and subheader Korean prose
- Step 2 STATE.md frontmatter section: "4개 필드 (line-by-line grep + sed; yq 등 외부 도구 미사용 — Phase 6 D-04 lock):" → English
- Milestone display assembly comment prose
- Step 3 header and inline prose about absolute timestamp prohibition
- Step 4 header prose and copy instructions
- Step 5 header prose (AskUserQuestion 3-옵션 분기) and response branch descriptions
- Korean prose after Resume/Start new milestone/Cancel labels
- Step 6 D-17 fallback description
- Bash inline comment "상대시각 환산 ... 절대 금지" → "Relative time conversion ... strictly prohibited"
- <success_criteria> items with Korean

For skills/sg-health/SKILL.md, translate:
- <objective> block: "GSD/Superpowers 설치 여부..." → full English translation
- <process> preamble "아래 7개 항목을..." → "Check the following 7 items in order..."
- Each diagnostic item's label and message strings (e.g. "FAIL] ~/.claude/get-shit-done/ 디렉토리 없음" → "[FAIL] ~/.claude/get-shit-done/ directory not found")
- Item 3 heading "Hook 스크립트 존재 여부 (Codex/Gemini 설치 시 필수)" → "Hook script existence (required for Codex/Gemini install)"
- All WARN/FAIL message strings throughout items 1-7
- Item 8 "요약 출력" → "Summary output" and the summary strings: "모든 항목 정상입니다." → "All checks passed.", "[FAIL] ${FAIL}개, [WARN] ${WARN}개 — 위 항목을 확인하세요." → "[FAIL] ${FAIL} item(s), [WARN] ${WARN} item(s) — check the items above."
- <success_criteria> items with Korean

NOTE for sg-health: The diagnostic output strings like "[FAIL] ~/.claude/get-shit-done/ 디렉토리 없음" are user-visible messages printed to stdout/stderr. These are NOT bash commands — translate them to English. The surrounding bash commands (test, echo, grep) are NOT modified.
  </action>
  <verify>
    <automated>grep -c '[가-힣]' /Users/gyuha/workspace/super-gsd/skills/sg-parallel-execute/SKILL.md && grep -c '[가-힣]' /Users/gyuha/workspace/super-gsd/skills/sg-start/SKILL.md && grep -c '[가-힣]' /Users/gyuha/workspace/super-gsd/skills/sg-health/SKILL.md</automated>
  </verify>
  <done>All three files return 0 from grep -c '[가-힣]'. YAML frontmatter field names unchanged. Bash commands, flags, and variable names unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file edit → SKILL.md content | Translation must not alter functional instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | bash code blocks in SKILL.md | mitigate | text-only rule: verify with grep after each file edit |
| T-36-02 | Tampering | YAML frontmatter structure | mitigate | read file before edit; preserve all field names and values exactly |
</threat_model>

<verification>
After both tasks complete:
```bash
for f in skills/sg-retro/SKILL.md skills/sg-next/SKILL.md skills/sg-parallel-execute/SKILL.md skills/sg-start/SKILL.md skills/sg-health/SKILL.md; do
  count=$(grep -c '[가-힣]' "$f" 2>/dev/null || echo 0)
  echo "$f: $count Korean chars"
done
```
All files must report 0.
</verification>

<success_criteria>
- All 5 SKILL.md files contain zero Korean characters (grep -c '[가-힣]' returns 0 for each)
- bash code block content (commands, flags, variable names) is unchanged
- YAML frontmatter field names and structure are unchanged
- No functional behavior changes — only language of prose text replaced
</success_criteria>

<output>
Create `.planning/phases/36-skills-skills/36-01-SUMMARY.md` when done.
</output>
