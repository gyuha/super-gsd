---
phase: 09-sg-retro-skill-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-retro/SKILL.md
  - .claude-plugin/plugin.json
autonomous: false
requirements:
  - RETRO-01
  - RETRO-02
  - RETRO-03
  - RETRO-04

must_haves:
  truths:
    - "사용자가 `Skill(skill=\"sg-retro\", args=\"{phase} {lens?}\")` 형태로 호출하면 phase 토큰을 zero-pad하고 `.planning/phases/{NN}-*/` 디렉터리를 glob으로 찾는다 (D-02; RETRO-02)"
    - "`args`가 비어 있으면 `.planning/STATE.md`의 `^Phase:` 라인에서 phase 번호를 추출 — Phase 7 D-04~D-06 multi-line 패턴(`sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//' | awk '{print $1}'`)을 인라인 복제하며 단일 토큰 캡처 정규식 `\\S+`은 절대 사용하지 않는다 (D-01, D-08)"
    - "phase 디렉터리가 없으면 stderr에 `Phase {N} not found. Available phases:` + `ls .planning/phases/` 결과를 출력하고 exit 1로 종료한다 (D-04)"
    - "`args`의 두 번째 토큰이 `ssc`/`4ls`/`dspm` 중 하나이면 (case-insensitive) AskUserQuestion을 건너뛰고 해당 lens로 직행한다 (D-03)"
    - "두 번째 토큰이 없거나 매핑되지 않으면 AskUserQuestion(header=`Lens`, 3옵션 `Start/Stop/Continue (ssc)` / `4Ls (4ls)` / `Decisions/Surprises/Patterns/Mistakes (dspm)`)로 lens를 선택받는다 (D-03, D-13, D-14, D-15)"
    - "Skill은 phase 디렉터리의 `{NN}-CONTEXT.md` + 모든 `{NN}-*-PLAN.md` + 모든 `{NN}-*-SUMMARY.md` 세 종류만 자동 수집한다 — `{NN}-PATTERNS.md`, `{NN}-VERIFICATION.md`, `{NN}-RESEARCH.md`는 수집 대상에서 제외한다 (D-05)"
    - "git 컨텍스트 수집 범위는 `BASE=$(git log -1 --format=%H -- .planning/phases/{NN}-*/)`로 결정하며, `BASE`가 비면 `HEAD~10..HEAD`로 fallback한다 (D-06)"
    - "git diff는 1000줄 cap 적응형 — 초과 시 `[diff truncated — N lines exceeded 1000-line cap; showing --stat summary]` 라인 + `git diff --stat`만 출력하고, git log는 cap 없이 `--oneline` 전체 표시한다 (D-07)"
    - "각 lens는 artifact-grounded draft-then-confirm — Claude가 D-05 아티팩트와 D-06 git 산출물 근거로 항목 초안을 먼저 제시하고 사용자가 수정/확정한다. 단순 개방형 질문(`Liked는 뭐였나요?`) 미사용 (D-10)"
    - "DSPM lens는 phase 아티팩트 + git diff/log만 근거로 한다 — 세션 transcript 스캔 절대 도입하지 않음 (D-11; Phase 10 ANALYZER 경계 보존)"
    - "각 lens 출력은 결정적 마크다운 구조를 따른다: `## Lens: {English name}` 헤더 + `_Captured: {ISO-8601 UTC}_` italic 라인 + lens별 fixed subheading(`### Start`/`### Stop`/`### Continue`, `### Liked`/`### Learned`/`### Lacked`/`### Longed For`, `### Decisions`/`### Surprises`/`### Patterns`/`### Mistakes`) + `### Action Items` 표 (D-09)"
    - "Action Items 표는 3컬럼 — `priority | item | next step` — 모든 lens에서 동일하며 owner 컬럼 없음 (D-12; Phase 12 RECURRENCE-01 파서 입력)"
    - "lessons 파일 경로는 `.planning/lessons/{NN}-{YYYY-MM-DD}.md` (UTC 기준), 디렉터리가 없으면 `mkdir -p`로 생성 (D-17; Phase 5 PATTERNS lock)"
    - "파일이 없으면 최상위 헤더 `# Lessons: Phase {N} ({YYYY-MM-DD})`를 작성한 뒤 첫 lens 섹션 append, 파일이 있으면 최상위 헤더는 재작성 금지하고 새 lens 섹션만 파일 끝에 `>>` redirect로 한 번에 append한다 (D-18, D-19, D-21)"
    - "같은 날 같은 phase에서 같은 lens가 두 번 이상 실행되면 두 번째부터 `## Lens: {name} (run 2)`, `(run 3)`, ... disambiguating 접미사를 적용한다 (D-20)"
    - "Append 전후 `wc -l` 차이를 stderr에 로그 (`lessons file: +N lines`)하여 partial-write 검증 신호를 남긴다 (D-21)"
    - "Skill은 `hooks/stop_hook.py`를 호출하지도 수정하지도 않는다 — lessons 파일을 직접 작성한다 (D-08, D-17)"
    - "SKILL.md frontmatter, AskUserQuestion header/options/question, 상태/에러 메시지는 영문 고정. lens facilitation prompt 내 Claude의 사용자 대화 텍스트와 lens 본문 항목(`### Liked` 내용, Action Items의 item/next step 문장)은 사용자 입력 언어 auto-detect. 마크다운 구조 마커(`## Lens: ...`, `### Liked`, 컬럼 헤더)는 영문 결정적 (D-16)"
    - "`.claude-plugin/plugin.json`에 `\"skills\": \"./skills/\"` 한 줄을 `\"commands\"` 배열 직후, `\"homepage\"` 직전에 추가한다 — `jq` 미사용, 기존 키 순서 보존을 위해 Edit 도구로 직접 텍스트 편집 (specifics §plugin.json 정확한 형태)"
    - "`commands/sg-learn.md`, `hooks/stop_hook.py`, `commands/sg-status.md` 등 다른 파일은 본 phase에서 절대 수정하지 않는다 (canonical_refs lock; Phase 13 MIGRATION-01 경계 보존)"
    - "자동화 테스트 자산, Python/셸 helper, fixture 디렉터리 추가 없음 — `skills/sg-retro/SKILL.md` + `.claude-plugin/plugin.json` 정확히 두 파일만 변경 (Phase 6 D-04 + Phase 7 D-08 lock 계승)"
  artifacts:
    - path: "skills/sg-retro/SKILL.md"
      provides: "sg-retro Skill 정의 — argument 파싱 + 컨텍스트 수집 + lens 선택 + lens facilitation + lessons append 전체 로직"
      contains: "## Lens: Start/Stop/Continue"
      min_lines: 200
    - path: ".claude-plugin/plugin.json"
      provides: "skills 디렉터리 등록"
      contains: "\"skills\": \"./skills/\""
  key_links:
    - from: "skills/sg-retro/SKILL.md (Phase 파싱 블록)"
      to: ".planning/STATE.md `^Phase:` 라인"
      via: "grep + sed | awk 인라인 복제 (Phase 7 D-04~D-06 lock; D-08 인라인 복제 계약)"
      pattern: "sed -E 's/\\^Phase:\\[\\[:space:\\]\\]\\*//'"
    - from: "skills/sg-retro/SKILL.md (컨텍스트 수집)"
      to: ".planning/phases/{NN}-*/{NN}-CONTEXT.md, {NN}-*-PLAN.md, {NN}-*-SUMMARY.md"
      via: "shell glob (`ls -1 .planning/phases/${PHASE_PAD}-*/${PHASE_PAD}-*-PLAN.md`)"
      pattern: "PHASE_PAD-CONTEXT.md"
    - from: "skills/sg-retro/SKILL.md (git 컨텍스트)"
      to: "phase 디렉터리 마지막 commit 이후 git log + diff"
      via: "git log -1 --format=%H -- .planning/phases/{NN}-*/ → BASE..HEAD, fallback HEAD~10..HEAD"
      pattern: "git log -1 --format=%H -- \\.planning/phases/"
    - from: "skills/sg-retro/SKILL.md (lens 선택)"
      to: "AskUserQuestion(header=Lens, 3 옵션 `(ssc)`/`(4ls)`/`(dspm)` 코드 병기)"
      via: "두 번째 토큰 매칭 실패 시 fallback (case-insensitive)"
      pattern: "Start/Stop/Continue \\(ssc\\)"
    - from: "skills/sg-retro/SKILL.md (lens 출력)"
      to: ".planning/lessons/{NN}-{YYYY-MM-DD}.md (D-17)"
      via: ">> redirect로 한 번에 append, 디렉터리 없으면 mkdir -p"
      pattern: "\\.planning/lessons/\\$\\{PHASE_PAD\\}-"
    - from: "skills/sg-retro/SKILL.md (Action Items 표)"
      to: "Phase 12 RECURRENCE-01 weighted top-N 입력"
      via: "결정적 마크다운 구조 — `priority | item | next step` 3컬럼"
      pattern: "\\| priority \\| item \\| next step \\|"
    - from: ".claude-plugin/plugin.json (skills 필드)"
      to: "Claude Code Skill 로더가 `skills/sg-retro/SKILL.md` 자동 등록"
      via: "`\"skills\": \"./skills/\"` 디렉터리 path"
      pattern: "\"skills\": \"\\./skills/\""
---

<objective>
super-gsd 플러그인에 첫 자체 Skill인 `sg-retro`를 추가한다. `skills/sg-retro/SKILL.md` 한 파일이 argument 파싱(D-01~D-04) + 타겟 컨텍스트 수집(D-05~D-08) + lens 선택(D-13~D-16) + 3개 lens facilitation(D-09~D-12) + lessons append(D-17~D-21) 전체 동작을 자체 완결로 수행한다. `.claude-plugin/plugin.json`에 `"skills": "./skills/"` 한 줄을 추가해 Claude Code Skill 시스템에 등록한다.

Purpose:
- RETRO-01 충족 — `skills/sg-retro/SKILL.md` 존재 + Skill 시스템 로드 가능.
- RETRO-02 충족 — phase argument로 CONTEXT/PLAN/SUMMARY + git log/diff 자동 수집.
- RETRO-03 (부분) 충족 — Start/Stop/Continue / 4Ls / Decisions·Surprises·Patterns·Mistakes 3 lens. Sailboat/Five Whys는 Phase 10 책임이므로 SKILL.md 구조가 lens 추가에 plug-in 가능하게 유지.
- RETRO-04 충족 — 일관된 출력 구조(헤더 + 항목 리스트 + 우선순위 액션 아이템) + `.planning/lessons/{NN}-{YYYY-MM-DD}.md` 저장.
- Phase 12 LESSONS-02 / RECURRENCE-01 파서의 사전 계약: `^## Lens: ` 결정적 헤더, `### {fixed subheading}`, `priority | item | next step` 3컬럼 표.

Output: 정확히 두 파일만 변경 — `skills/sg-retro/SKILL.md` (신규) + `.claude-plugin/plugin.json` (1줄 추가). 헬퍼 스크립트, 테스트 fixture, 다른 명령/훅 파일 변경 없음 (Phase 6 D-04 + Phase 7 D-08 lock 계승).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-sg-retro-skill-scaffold/09-CONTEXT.md
@.claude-plugin/plugin.json
@commands/sg-status.md
@commands/sg-plan.md
@commands/sg-execute.md
@commands/sg-learn.md

<interfaces>
<!-- 실행자는 추가 코드베이스 탐색 없이 아래 블록들을 SKILL.md 본문에 그대로 인라인 복제한다. -->
<!-- drift 시 `commands/sg-status.md`의 동일 블록과 동시 수정해야 한다 (D-08 인라인 복제 계약). -->

A. STATE.md `^Phase:` 파싱 + 숫자 검증 + phase 디렉터리 resolve (D-01, D-02, D-04, D-08 — `commands/sg-status.md` lines 17-21 기반 + 본 Skill의 숫자-only 검증과 zero-pad는 specifics §"`args` 파싱 정확한 규칙" 기준):

```bash
set -- $ARGUMENTS
PHASE_RAW="${1:-}"
LENS_RAW="${2:-}"

if [ -z "$PHASE_RAW" ]; then
  # --- BEGIN STATE.md Phase parsing block (D-08: Phase 7 D-04~D-06 multi-line 패턴 인라인 복제) ---
  PHASE_RAW=$(grep -E '^Phase:' .planning/STATE.md 2>/dev/null | head -1 \
              | sed -E 's/^Phase:[[:space:]]*//' \
              | sed -E 's/[[:space:]]+$//' \
              | awk '{print $1}')
  # --- END STATE.md Phase parsing block ---
fi

# D-02: 숫자만 허용
if ! printf '%s' "$PHASE_RAW" | grep -qE '^[0-9]+$'; then
  echo "Phase token must be a number. Got: '${PHASE_RAW}'." >&2
  if [ -d .planning/phases ]; then
    echo "Available phases:" >&2
    ls .planning/phases/ 2>/dev/null >&2 || echo "  (no phases yet)" >&2
  fi
  exit 1
fi

PHASE_PAD=$(printf "%02d" "$PHASE_RAW")
PHASE_DIR=$(ls -d .planning/phases/${PHASE_PAD}-*/ 2>/dev/null | head -1)

# D-04: 디렉터리 미존재 에러
if [ -z "$PHASE_DIR" ]; then
  echo "Phase ${PHASE_RAW} not found. Available phases:" >&2
  ls .planning/phases/ 2>/dev/null >&2 || echo "  (no phases yet)" >&2
  exit 1
fi
PHASE_DIR="${PHASE_DIR%/}"  # trailing slash 제거
```

B. Lens 코드 매핑 (D-03 case-insensitive, fallback AskUserQuestion):

```bash
LENS_CODE=""
if [ -n "$LENS_RAW" ]; then
  LENS_LC=$(printf '%s' "$LENS_RAW" | tr '[:upper:]' '[:lower:]')
  case "$LENS_LC" in
    ssc)  LENS_CODE="ssc"  ;;
    4ls)  LENS_CODE="4ls"  ;;
    dspm) LENS_CODE="dspm" ;;
    *)    LENS_CODE=""     ;;  # 매핑 실패 → AskUserQuestion fallback
  esac
fi
```

`LENS_CODE`가 비어 있으면 AskUserQuestion (D-13~D-15) — header `"Lens"`, question `"Which retrospective lens do you want to run?"`, 3 옵션 라벨 정확히:
- `"Start/Stop/Continue (ssc)"`
- `"4Ls (4ls)"`
- `"Decisions/Surprises/Patterns/Mistakes (dspm)"`

default 옵션 지정 금지 (D-15). 응답 라벨에서 `(ssc)` / `(4ls)` / `(dspm)` 부분을 grep/sed로 추출해 `LENS_CODE`에 대입한다:
```bash
# 예: 사용자가 "Start/Stop/Continue (ssc)" 응답 → LENS_CODE="ssc"
LENS_CODE=$(printf '%s' "$ASKUSERQUESTION_RESPONSE" | grep -oE '\((ssc|4ls|dspm)\)' | head -1 | tr -d '()')
```

C. 타겟 아티팩트 수집 (D-05 — 정확히 세 종류, 다른 파일 자동 수집 금지):

```bash
CONTEXT_FILE="${PHASE_DIR}/${PHASE_PAD}-CONTEXT.md"
PLAN_FILES=$(ls -1 ${PHASE_DIR}/${PHASE_PAD}-*-PLAN.md 2>/dev/null)
SUMMARY_FILES=$(ls -1 ${PHASE_DIR}/${PHASE_PAD}-*-SUMMARY.md 2>/dev/null)

# 표시(stderr 진단용; lens facilitation은 본문에서 직접 Read 도구로 읽음)
echo "Collected artifacts:" >&2
[ -f "$CONTEXT_FILE" ] && echo "  $CONTEXT_FILE" >&2
[ -n "$PLAN_FILES" ] && printf '  %s\n' $PLAN_FILES >&2
[ -n "$SUMMARY_FILES" ] && printf '  %s\n' $SUMMARY_FILES >&2
```

`{NN}-PATTERNS.md`, `{NN}-VERIFICATION.md`, `{NN}-RESEARCH.md`, `{NN}-DISCUSSION-LOG.md`, `{NN}-REVIEW*.md`는 자동 수집 대상 아님 (D-05). 사용자가 명시 요청 시에만 추가 Read.

D. Git 컨텍스트 수집 (D-06 BASE 결정 + D-07 적응형 cap):

```bash
BASE=$(git log -1 --format=%H -- .planning/phases/${PHASE_PAD}-*/ 2>/dev/null)
if [ -z "$BASE" ]; then
  RANGE="HEAD~10..HEAD"
else
  RANGE="${BASE}..HEAD"
fi

# git log: cap 없이 oneline 전체
GIT_LOG=$(git log ${RANGE} --oneline 2>/dev/null)

# git diff: 적응형 1000줄 cap
DIFF=$(git diff ${RANGE} 2>/dev/null)
LINES=$(printf '%s\n' "$DIFF" | wc -l | tr -d ' ')
if [ "$LINES" -gt 1000 ]; then
  GIT_DIFF=$(printf '[diff truncated — %s lines exceeded 1000-line cap; showing --stat summary]\n' "$LINES"; git diff --stat ${RANGE} 2>/dev/null)
else
  GIT_DIFF="$DIFF"
fi

echo "Git range: ${RANGE}" >&2
```

E. lessons 파일 경로 + append 안전 (D-17~D-21):

```bash
# UTC 기준 ISO 날짜 (D-17)
TODAY=$(date -u +%Y-%m-%d)
NOW_ISO=$(date -u +%Y-%m-%dT%H:%M:%SZ)
LESSONS_DIR=".planning/lessons"
LESSONS_FILE="${LESSONS_DIR}/${PHASE_PAD}-${TODAY}.md"

mkdir -p "$LESSONS_DIR"

# Lens English name 결정 (D-13 라벨과 일치)
case "$LENS_CODE" in
  ssc)  LENS_NAME="Start/Stop/Continue" ;;
  4ls)  LENS_NAME="4Ls" ;;
  dspm) LENS_NAME="Decisions/Surprises/Patterns/Mistakes" ;;
esac

# D-20: 같은 lens 중복 실행 disambiguation
# 파일이 있으면 같은 lens 헤더 개수를 세서 (run N) 접미사 결정
RUN_SUFFIX=""
if [ -f "$LESSONS_FILE" ]; then
  COUNT=$(grep -cE "^## Lens: ${LENS_NAME}( \\(run [0-9]+\\))?\$" "$LESSONS_FILE" 2>/dev/null || echo 0)
  if [ "$COUNT" -gt 0 ]; then
    RUN_SUFFIX=" (run $((COUNT + 1)))"
  fi
fi

LENS_HEADER="## Lens: ${LENS_NAME}${RUN_SUFFIX}"

# D-21: append 전 라인 카운트
if [ -f "$LESSONS_FILE" ]; then
  BEFORE=$(wc -l < "$LESSONS_FILE" | tr -d ' ')
else
  BEFORE=0
fi

# D-21: 새 파일이면 최상위 헤더 + 빈 줄 1개 먼저 작성
if [ ! -f "$LESSONS_FILE" ]; then
  {
    printf '# Lessons: Phase %s (%s)\n\n' "$PHASE_RAW" "$TODAY"
  } > "$LESSONS_FILE"
fi

# D-19/D-21: lens 섹션 전체를 한 번에 >> append (partial-write 회피)
{
  printf '%s\n' "$LENS_HEADER"
  printf '_Captured: %s_\n\n' "$NOW_ISO"
  # ... lens별 fixed subheading 본문 (D-09) — Claude가 facilitation 단계에서 생성한 마크다운을 여기에 삽입 ...
  printf '\n### Action Items\n'
  printf '| priority | item | next step |\n'
  printf '|----------|------|-----------|\n'
  # ... action items 행 ...
  printf '\n'
} >> "$LESSONS_FILE"

AFTER=$(wc -l < "$LESSONS_FILE" | tr -d ' ')
DELTA=$((AFTER - BEFORE))
echo "lessons file: ${LESSONS_FILE} +${DELTA} lines" >&2
```

F. plugin.json 1줄 추가 정확한 위치 (specifics §"plugin.json 수정 정확한 형태"):

현재 `.claude-plugin/plugin.json`은 (line 9-24)
```json
  "commands": [
    "./commands/sg-start.md",
    ...
    "./commands/sg-health.md"
  ],
  "homepage": "https://github.com/gyuha/super-gsd",
```

수정 후:
```json
  "commands": [
    "./commands/sg-start.md",
    ...
    "./commands/sg-health.md"
  ],
  "skills": "./skills/",
  "homepage": "https://github.com/gyuha/super-gsd",
```

`jq` 미사용 (키 순서 보존). Edit 도구로 `  ],\n  "homepage":` → `  ],\n  "skills": "./skills/",\n  "homepage":` 패턴 치환. 기존 다른 키/배열 요소/공백/들여쓰기는 글자 그대로 보존.

G. Lens별 fixed subheading 매핑 (D-09):
- `ssc` → `### Start` / `### Stop` / `### Continue`
- `4ls` → `### Liked` / `### Learned` / `### Lacked` / `### Longed For`
- `dspm` → `### Decisions` / `### Surprises` / `### Patterns` / `### Mistakes`

각 subheading 뒤에는 `- {item}` 형태의 bullet 리스트로 항목을 나열한다. Action Items는 별도 `### Action Items` subheading 아래 3컬럼 표.

H. Lens facilitation 흐름 (D-09 hybrid + D-10 artifact-grounded draft-then-confirm + D-11 transcript 금지 + D-16 본문 언어 auto-detect):

각 lens마다 SKILL.md 본문에 다음 facilitation prompt 블록을 둔다 (영문 outer prose, 본문 내 사용자 대화는 auto-detect). 흐름은 모든 lens 공통:

1. (영문 prose, Claude에게) "Read all three artifact types (CONTEXT, PLAN(s), SUMMARY(s)) and capture key signals from git log/diff (`${RANGE}`). Do NOT scan session transcript — Phase 10 ANALYZER scope (D-11)."
2. (영문 prose, Claude에게) "For each lens-specific subheading, propose 2–5 draft bullet items grounded in the artifacts above. Each bullet must cite the source (file path or commit hash). Present the full draft as a single markdown block."
3. (영문 prose, Claude에게) "Ask the user to confirm/edit/add/delete each subheading's items. Use user's input language (D-16). Single round-trip preferred — present all subheadings at once, not one-by-one."
4. (영문 prose, Claude에게) "After items are finalized, propose 2–4 Action Items rows (priority P1/P2/P3 | one-sentence item | concrete next step or `deferred to Phase N` label). No owner column (D-12). User confirms/edits the table."
5. (영문 prose, Claude에게) "Assemble the final lens section (header + `_Captured: {NOW_ISO}_` + subheadings + Action Items) and append to `${LESSONS_FILE}` using the bash block in §E. Emit `Lessons saved to ${LESSONS_FILE}.` to stdout. No other output."

DSPM lens 추가 가드 (D-11): facilitation prompt에 명시 — "DSPM in Phase 9 derives all four categories strictly from collected phase artifacts + git log/diff. Do NOT read or analyze session transcript, even if it appears relevant. Phase 10 ANALYZER will add transcript-based merging additively."

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: skills/sg-retro/SKILL.md 작성 + .claude-plugin/plugin.json에 skills 디렉터리 등록</name>
  <files>skills/sg-retro/SKILL.md, .claude-plugin/plugin.json</files>
  <read_first>
    - .planning/phases/09-sg-retro-skill-scaffold/09-CONTEXT.md (locked decisions D-01~D-21 + specifics 7개 부록)
    - commands/sg-status.md (lines 14-49 — STATE.md `^Phase:` 파싱 블록 인라인 복제 원본; lines 60-100 — case 블록 참고)
    - commands/sg-plan.md (lines 16-33 — `$ARGUMENTS` 비면 STATE.md fallback 패턴, D-01 참고)
    - commands/sg-execute.md (lines 16-24 — 동일 fallback 패턴 두 번째 인스턴스)
    - commands/sg-learn.md (전체 — 현재 hookify:hookify 호출 형태. Phase 9에서는 수정 금지, Phase 13 MIGRATION-01 책임이므로 절대 변경하지 않을 것 확인용)
    - .claude-plugin/plugin.json (전체 — 현재 키 순서/포맷, commands 배열, homepage 위치)
    - .planning/STATE.md (lines 28 — 실제 `Phase: 9 (sg-retro Skill scaffold) — Not started` 라인 포맷 확인)
    - .planning/milestones/v1.0-phases/05-lessons-feedback-loop/05-PATTERNS.md (`.planning/lessons/{NN}-{YYYY-MM-DD}.md` 네이밍 컨벤션 lock)
    - .planning/milestones/v1.1-phases/07-status-accuracy/07-CONTEXT.md (D-04~D-08 — STATE.md multi-line 파싱 lock + 자동화 테스트 자산 미도입 노선)
    - .planning/milestones/v1.1-phases/08-session-restore/08-CONTEXT.md (D-12, D-16 — AskUserQuestion 영문 brevity convention)
    - .planning/milestones/v1.1-phases/08-session-restore/08-01-PLAN.md (인라인 복제 블록 구조 참고 — Phase 8이 sg-status.md 인라인 복제한 동일 패턴)
    - $HOME/.claude/plugins/marketplaces/anthropic-agent-skills/template/SKILL.md (frontmatter 표준 — `name:` + `description:`. 없으면 hookify:hookify 또는 pm-execution `retro` SKILL.md를 reference로 사용)
  </read_first>
  <action>
    **목표**: 정확히 두 파일을 변경한다.
    1. **신규**: `skills/sg-retro/SKILL.md` — 다음 6개 섹션을 가진 단일 markdown 파일.
    2. **수정**: `.claude-plugin/plugin.json` — `commands` 배열과 `homepage` 사이에 `"skills": "./skills/",` 한 줄 추가.

    다른 파일(`commands/sg-learn.md`, `commands/sg-status.md`, `hooks/stop_hook.py`, 기타)은 절대 변경하지 않는다. 새 디렉터리 `skills/sg-retro/`는 신규 생성한다(부모 `skills/`도 함께).

    ---

    **A. `skills/sg-retro/SKILL.md` 구조 (전체 단일 파일):**

    **A-1. Frontmatter (영문 고정 — D-16):**
    ```yaml
    ---
    name: sg-retro
    description: Run a structured retrospective on a GSD phase with one of three lenses (SSC, 4Ls, DSPM) and append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md.
    ---
    ```
    `name`은 정확히 `sg-retro` (super-gsd 프로젝트의 sg- prefix 정책 준수). `description`은 한 문장, lens 코드 3개를 본문에 노출(학습 가능성 ↑).

    **A-2. `<objective>` 섹션 (영문 1-2 문단):**
    "Run a structured retrospective on a GSD phase. Auto-collect phase artifacts ({NN}-CONTEXT.md, {NN}-*-PLAN.md, {NN}-*-SUMMARY.md) and git log/diff since the phase directory's last commit. Let the user pick one of three lenses (Start/Stop/Continue, 4Ls, Decisions/Surprises/Patterns/Mistakes) via AskUserQuestion (or directly via the second argument). Facilitate an artifact-grounded draft-then-confirm flow, then append the result to .planning/lessons/{NN}-{YYYY-MM-DD}.md (append-only; same-lens repeats use `(run N)` suffix)."

    **A-3. `<execution_context>` 섹션 (영문 1 문단):**
    "Self-contained. Reads `.planning/STATE.md`, `.planning/phases/{NN}-*/{NN}-CONTEXT.md`, all `{NN}-*-PLAN.md`, all `{NN}-*-SUMMARY.md`, and `git log`/`git diff`. Writes only `.planning/lessons/{NN}-{YYYY-MM-DD}.md` (and creates `.planning/lessons/` if missing). Does NOT modify `hooks/stop_hook.py`, does NOT call `save_hookify_lessons()`, does NOT read or write `.planning/HANDOFF.md`."

    **A-4. `<process>` 섹션 (영문 prose + 인라인 bash 블록 — 본 plan의 `<interfaces>` 블록 A~G를 글자 그대로 인라인 복제):**

    `<process>` 본문에 다음 6 step을 순서대로 둔다. 각 step의 bash 블록은 본 plan `<interfaces>`의 동명 블록(A/B/C/D/E)을 글자 그대로 복사한다. step 사이의 영문 prose는 D-09/D-10/D-11 결정을 명시한다.

    - **Step 1 — Argument parsing + phase resolve.** `<interfaces>` 블록 A 복제. 영문 prose 한 줄: "Parse `$ARGUMENTS` into `PHASE_RAW` and `LENS_RAW`. If `PHASE_RAW` is empty, fall back to `.planning/STATE.md` `^Phase:` line using the multi-line pattern below (Phase 7 D-04~D-06 lock; **do not use single-token capture like `\\S+`**)."
    - **Step 2 — Lens code mapping or AskUserQuestion fallback.** `<interfaces>` 블록 B 복제. 영문 prose 한 줄: "Map `LENS_RAW` to one of `ssc`/`4ls`/`dspm` (case-insensitive). If empty or unmapped, invoke AskUserQuestion with header `Lens` and the three options below. No default option is preselected (D-15)."
    - **Step 3 — Collect target artifacts.** `<interfaces>` 블록 C 복제. 영문 prose 한 줄: "Collect exactly three artifact types (`{NN}-CONTEXT.md` + all `{NN}-*-PLAN.md` + all `{NN}-*-SUMMARY.md`). Do NOT auto-include `{NN}-PATTERNS.md`, `{NN}-VERIFICATION.md`, `{NN}-RESEARCH.md`, `{NN}-DISCUSSION-LOG.md` (D-05). Use the Read tool inline on each path in the lens facilitation step."
    - **Step 4 — Collect git context.** `<interfaces>` 블록 D 복제. 영문 prose 한 줄: "Compute `BASE` as the last commit touching `.planning/phases/${PHASE_PAD}-*/`. If empty, fallback to `HEAD~10..HEAD` (D-06). Apply adaptive 1000-line cap to diff (D-07); log is uncapped."
    - **Step 5 — Lens facilitation (artifact-grounded draft-then-confirm).** D-09/D-10/D-11 정책을 영문 prose 5문단으로 작성 — 본 plan `<interfaces>` 블록 H의 5 step을 글자 그대로 SKILL.md에 옮긴다. 각 lens(`ssc`/`4ls`/`dspm`)별로 별도 sub-block을 둔다:
        - **Sub-block `ssc`** — fixed subheadings: `### Start` / `### Stop` / `### Continue`. Facilitation: "For Start, propose practices/tools/conventions the team should begin adopting based on PLAN.md gaps and SUMMARY follow-ups. For Stop, propose anti-patterns visible in git diff or CONTEXT decisions reversed mid-phase. For Continue, propose practices visible in SUMMARY that delivered observed value." 모든 사용자 대화는 user 입력 언어 auto-detect (D-16).
        - **Sub-block `4ls`** — fixed subheadings: `### Liked` / `### Learned` / `### Lacked` / `### Longed For`. Facilitation: "Liked = observed strengths in CONTEXT/PLAN/SUMMARY artifacts. Learned = unexpected discoveries from git diff or SUMMARY lessons. Lacked = missing capabilities/info revealed by phase decisions. Longed For = wishes inferred from SUMMARY deferrals or CONTEXT 'Deferred Ideas' section."
        - **Sub-block `dspm`** — fixed subheadings: `### Decisions` / `### Surprises` / `### Patterns` / `### Mistakes`. Facilitation: "Decisions = D-XX locks from CONTEXT and SUMMARY lock-in. Surprises = git log commit messages that pivoted phase direction. Patterns = recurring techniques across multiple PLAN tasks. Mistakes = verification failures + known risks not mitigated (CONTEXT 'Known Risk Sites' that surfaced)." **명시적 가드** — facilitation prompt에 "DSPM in Phase 9 derives all four categories strictly from collected phase artifacts + git log/diff. Do NOT read or analyze session transcript, even if it appears relevant. Phase 10 ANALYZER will add transcript-based merging additively (D-11)."
    - **Step 6 — Append to lessons file.** `<interfaces>` 블록 E 복제. 영문 prose 한 줄: "Write the assembled lens section (header `## Lens: {name}{run-suffix}` + `_Captured: ${NOW_ISO}_` italic line + lens-specific subheadings + Action Items table) to `.planning/lessons/${PHASE_PAD}-${TODAY}.md` using a single `>>` redirect block (D-21). Create the directory if missing. Emit `lessons file: {path} +N lines` to stderr for verification."

    **A-5. `<lens_templates>` 섹션 (영문 마크다운 골격 3개, 결정적 마커):**

    SKILL.md 본문에 다음 세 마크다운 골격을 영문 그대로 둔다 (이는 facilitation 단계에서 Claude가 출력 형식 reference로 사용). CONTEXT.md `<specifics>` §"Lens별 출력 마크다운 골격"의 정확한 형태를 그대로 복제:

    ```markdown
    ## Lens: Start/Stop/Continue
    _Captured: {ISO-8601 UTC}_

    ### Start
    - [user-language item]
    ### Stop
    - [user-language item]
    ### Continue
    - [user-language item]

    ### Action Items
    | priority | item | next step |
    |----------|------|-----------|
    | P1 | [user-language summary] | [user-language concrete step] |
    ```
    동일 패턴으로 `4Ls` (Liked/Learned/Lacked/Longed For) 및 `Decisions/Surprises/Patterns/Mistakes` 골격도 두 개 더 추가한다.

    **A-6. `<success_criteria>` 섹션 (영문 5개 bullet):**
    1. `Skill(skill="sg-retro", args="...")` 호출 시 phase argument는 zero-pad되어 `.planning/phases/{NN}-*/` 디렉터리로 정확히 1개 매칭된다. 디렉터리가 없으면 stderr 메시지 + exit 1 (D-04).
    2. 두 번째 토큰이 `ssc`/`4ls`/`dspm` 중 하나(case-insensitive)면 AskUserQuestion을 건너뛴다. 아니면 AskUserQuestion header `Lens` + 3옵션(`(ssc)`/`(4ls)`/`(dspm)` 코드 병기)으로 lens를 선택받는다 (D-03, D-13).
    3. 각 lens 출력은 정확히 `## Lens: {name}` 헤더 + `_Captured: {ISO}_` italic + lens별 fixed subheadings + `### Action Items` 3컬럼 표(`priority | item | next step`) 형식을 따른다 (D-09, D-12).
    4. lessons 파일은 `.planning/lessons/{NN}-{YYYY-MM-DD}.md` 경로에 작성된다. 파일이 없으면 최상위 헤더 + 첫 lens 섹션, 있으면 새 lens 섹션만 끝에 append (D-17, D-18, D-19, D-21). 같은 날 같은 lens 두 번째는 `(run 2)` 접미사 (D-20).
    5. DSPM lens는 phase 아티팩트 + git log/diff만 근거로 한다 — session transcript 스캔 절대 없음 (D-11).

    ---

    **B. `.claude-plugin/plugin.json` 1줄 추가:**

    현재 line 23-25:
    ```json
        "./commands/sg-health.md"
      ],
      "homepage": "https://github.com/gyuha/super-gsd",
    ```

    수정 후:
    ```json
        "./commands/sg-health.md"
      ],
      "skills": "./skills/",
      "homepage": "https://github.com/gyuha/super-gsd",
    ```

    Edit 도구로 정확히 다음 치환을 수행한다 (양쪽 anchor 라인은 변경 금지):
    - old_string: `    "./commands/sg-health.md"\n  ],\n  "homepage": "https://github.com/gyuha/super-gsd",`
    - new_string: `    "./commands/sg-health.md"\n  ],\n  "skills": "./skills/",\n  "homepage": "https://github.com/gyuha/super-gsd",`

    `jq` 미사용 (키 순서/공백/들여쓰기 보존). 다른 키(`name`, `version`, `description`, `author`, `commands` 배열의 다른 요소, `repository`, `license`, `keywords`)는 절대 수정하지 않는다.

    ---

    **하지 말아야 할 것 (lock 위반):**
    - `commands/sg-learn.md` 수정 — Phase 13 MIGRATION-01 책임. 현재 `hookify:hookify` 호출은 그대로 둔다 (D-canonical_refs lock).
    - `commands/sg-status.md` 의 STATE.md 파싱 블록 수정 — Phase 7 D-07 lock. drift 시에만 양쪽 동시 수정, 본 phase에서는 sg-retro 쪽에서만 인라인 복제.
    - `hooks/stop_hook.py` 의 `_read_current_phase()` 또는 `save_hookify_lessons()` 수정 — D-08 lock. sg-retro Skill은 helper를 우회하고 lessons 파일을 직접 작성.
    - `\\S+` 단일 토큰 정규식으로 Phase 라인 파싱 — Phase 7 D-06 회귀 위반.
    - `yq` / `jq` 등 외부 도구 의존 — Phase 6 D-04 lock 위반.
    - 자동화 테스트 자산 추가 (`tests/sg-retro/`, fixture 디렉터리, 등) — Phase 7 D-08 lock 위반.
    - Python/셸 helper 신규 파일 — Phase 6 D-04 + Phase 7 D-08 lock 위반.
    - DSPM lens facilitation 안에서 session transcript 읽기/분석 — D-11 위반 (Phase 10 ANALYZER 영역).
    - AskUserQuestion에 default 옵션 지정 — D-15 위반.
    - 옵션 라벨에 lens 코드 괄호 누락 (예: `"Start/Stop/Continue"` 만 사용) — D-13 위반.
    - lessons 파일 덮어쓰기 (`>` redirect) — D-18 위반. 항상 `>>` append.
    - 같은 lens 중복 실행을 refuse하거나 거부 — D-20 위반. 반드시 `(run N)` 접미사로 누적 보존.
    - lens 본문 항목 영어 고정 — D-16 위반. 본문 항목은 사용자 언어 auto-detect.
    - 마크다운 구조 마커(`## Lens:`, `### Liked` 등)를 한글화 — D-16 위반 (Phase 12 파서 키).
    - HANDOFF.md 읽기/쓰기 — D-canonical_refs lock + Phase 8 D-16 정신.
  </action>
  <verify>
    <automated>
    cd /Users/gyuha/workspace/super-gsd \
    && test -f skills/sg-retro/SKILL.md \
    && grep -F 'name: sg-retro' skills/sg-retro/SKILL.md \
    && grep -F 'description:' skills/sg-retro/SKILL.md \
    && grep -F 'BEGIN STATE.md Phase parsing block (D-08' skills/sg-retro/SKILL.md \
    && grep -F "sed -E 's/^Phase:[[:space:]]*//'" skills/sg-retro/SKILL.md \
    && grep -F "sed -E 's/[[:space:]]+$//'" skills/sg-retro/SKILL.md \
    && grep -F 'PHASE_PAD=$(printf "%02d"' skills/sg-retro/SKILL.md \
    && grep -F 'ls -d .planning/phases/${PHASE_PAD}-*/' skills/sg-retro/SKILL.md \
    && grep -F 'Phase token must be a number' skills/sg-retro/SKILL.md \
    && grep -F 'not found. Available phases:' skills/sg-retro/SKILL.md \
    && grep -F '${PHASE_PAD}-CONTEXT.md' skills/sg-retro/SKILL.md \
    && grep -F '${PHASE_PAD}-*-PLAN.md' skills/sg-retro/SKILL.md \
    && grep -F '${PHASE_PAD}-*-SUMMARY.md' skills/sg-retro/SKILL.md \
    && grep -F 'git log -1 --format=%H -- .planning/phases/${PHASE_PAD}-*/' skills/sg-retro/SKILL.md \
    && grep -F 'HEAD~10..HEAD' skills/sg-retro/SKILL.md \
    && grep -F '1000-line cap' skills/sg-retro/SKILL.md \
    && grep -F 'git diff --stat' skills/sg-retro/SKILL.md \
    && grep -F 'Start/Stop/Continue (ssc)' skills/sg-retro/SKILL.md \
    && grep -F '4Ls (4ls)' skills/sg-retro/SKILL.md \
    && grep -F 'Decisions/Surprises/Patterns/Mistakes (dspm)' skills/sg-retro/SKILL.md \
    && grep -F 'header' skills/sg-retro/SKILL.md \
    && grep -F 'Lens' skills/sg-retro/SKILL.md \
    && grep -F '## Lens: Start/Stop/Continue' skills/sg-retro/SKILL.md \
    && grep -F '## Lens: 4Ls' skills/sg-retro/SKILL.md \
    && grep -F '## Lens: Decisions/Surprises/Patterns/Mistakes' skills/sg-retro/SKILL.md \
    && grep -F '### Start' skills/sg-retro/SKILL.md \
    && grep -F '### Stop' skills/sg-retro/SKILL.md \
    && grep -F '### Continue' skills/sg-retro/SKILL.md \
    && grep -F '### Liked' skills/sg-retro/SKILL.md \
    && grep -F '### Learned' skills/sg-retro/SKILL.md \
    && grep -F '### Lacked' skills/sg-retro/SKILL.md \
    && grep -F '### Longed For' skills/sg-retro/SKILL.md \
    && grep -F '### Decisions' skills/sg-retro/SKILL.md \
    && grep -F '### Surprises' skills/sg-retro/SKILL.md \
    && grep -F '### Patterns' skills/sg-retro/SKILL.md \
    && grep -F '### Mistakes' skills/sg-retro/SKILL.md \
    && grep -F '### Action Items' skills/sg-retro/SKILL.md \
    && grep -F '| priority | item | next step |' skills/sg-retro/SKILL.md \
    && grep -F '_Captured:' skills/sg-retro/SKILL.md \
    && grep -F '.planning/lessons/${PHASE_PAD}-' skills/sg-retro/SKILL.md \
    && grep -F 'mkdir -p' skills/sg-retro/SKILL.md \
    && grep -F '(run' skills/sg-retro/SKILL.md \
    && grep -F '>>' skills/sg-retro/SKILL.md \
    && grep -F 'wc -l' skills/sg-retro/SKILL.md \
    && grep -F 'Do NOT' skills/sg-retro/SKILL.md \
    && grep -F 'transcript' skills/sg-retro/SKILL.md \
    && ! grep -F '\\S+' skills/sg-retro/SKILL.md \
    && ! grep -F "Skill(skill=\"hookify:hookify\"" skills/sg-retro/SKILL.md \
    && ! grep -F 'save_hookify_lessons' skills/sg-retro/SKILL.md \
    && ! grep -F 'HANDOFF.md' skills/sg-retro/SKILL.md \
    && grep -F '"skills": "./skills/"' .claude-plugin/plugin.json \
    && grep -F '"./commands/sg-health.md"' .claude-plugin/plugin.json \
    && grep -F '"homepage": "https://github.com/gyuha/super-gsd"' .claude-plugin/plugin.json \
    && python3 -c "import json; d=json.load(open('.claude-plugin/plugin.json')); assert d['skills']=='./skills/', d.get('skills'); assert 'sg-retro' not in str(d.get('commands', [])), 'sg-retro should not be in commands array'; print('plugin.json OK')" \
    && ! git diff --name-only HEAD 2>/dev/null | grep -E '^(commands/sg-learn\\.md|commands/sg-status\\.md|hooks/stop_hook\\.py)$' \
    && ! ls tests/sg-retro* skills/sg-retro/*.py skills/sg-retro/*.sh 2>/dev/null \
    && echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    1. `skills/sg-retro/SKILL.md` 파일이 존재하며 frontmatter `name: sg-retro` + `description:` (한 문장, lens 코드 SSC/4Ls/DSPM 명시) 를 포함한다 (RETRO-01).
    2. SKILL.md 본문에 STATE.md `^Phase:` 파싱 블록이 `# --- BEGIN STATE.md Phase parsing block (D-08: ...) ---` BEGIN/END 주석 demarcate 형식으로 인라인 복제되어 있고, sed 패턴 `sed -E 's/^Phase:[[:space:]]*//'` + `sed -E 's/[[:space:]]+$//'` 두 줄 모두 등장한다 (D-01, D-08). 단일 토큰 정규식 `\\S+`는 SKILL.md 본문 어디에도 등장하지 않는다 (Phase 7 D-06 회귀 방지).
    3. SKILL.md 본문에 `PHASE_PAD=$(printf "%02d" "$PHASE_RAW")` zero-pad 변환과 `ls -d .planning/phases/${PHASE_PAD}-*/` glob, 그리고 디렉터리 미존재 시 `Phase {N} not found. Available phases:` stderr 메시지 + `exit 1` 분기가 명시되어 있다 (D-02, D-04).
    4. SKILL.md 본문에 LENS_RAW 케이스 분기(`ssc|4ls|dspm`) + AskUserQuestion fallback이 명시되어 있고, 3개 옵션 라벨 정확히 `"Start/Stop/Continue (ssc)"`, `"4Ls (4ls)"`, `"Decisions/Surprises/Patterns/Mistakes (dspm)"` 가 모두 등장한다 (D-03, D-13). header `Lens` 도 명시 (D-14).
    5. SKILL.md 본문에 컨텍스트 수집 대상이 정확히 `${PHASE_PAD}-CONTEXT.md` + `${PHASE_PAD}-*-PLAN.md` + `${PHASE_PAD}-*-SUMMARY.md` 세 종류로 한정되어 있고, `PATTERNS.md`/`VERIFICATION.md`/`RESEARCH.md` 등은 자동 수집 대상이 아니라는 점이 명시되어 있다 (D-05).
    6. SKILL.md 본문에 `git log -1 --format=%H -- .planning/phases/${PHASE_PAD}-*/` 로 `BASE` 결정 + `HEAD~10..HEAD` fallback + 1000줄 cap + `git diff --stat` fallback + `[diff truncated ... 1000-line cap; showing --stat summary]` 메시지가 모두 등장한다 (D-06, D-07).
    7. SKILL.md 본문에 세 lens 마크다운 골격(`## Lens: Start/Stop/Continue` / `## Lens: 4Ls` / `## Lens: Decisions/Surprises/Patterns/Mistakes`)이 모두 존재하며, 각각 lens별 fixed subheading (`### Start`/`### Stop`/`### Continue`, `### Liked`/`### Learned`/`### Lacked`/`### Longed For`, `### Decisions`/`### Surprises`/`### Patterns`/`### Mistakes`) 12개 subheading 라인이 모두 등장한다 (D-09).
    8. SKILL.md 본문에 `### Action Items` subheading + `| priority | item | next step |` 마크다운 표 헤더 라인이 정확히 등장한다 (D-12). owner 컬럼 없음.
    9. SKILL.md 본문에 `_Captured: {ISO}_` italic 라인 패턴이 등장하고 (`_Captured:` 문자열로 검증), lessons 파일 경로 `.planning/lessons/${PHASE_PAD}-` 가 명시되어 있다 (D-17, D-19).
    10. SKILL.md 본문에 `mkdir -p` (lessons 디렉터리 자동 생성), `(run` 문자열 (같은 lens 중복 시 `(run N)` 접미사), `>>` redirect 연산자, `wc -l` (라인 카운트 로그) 가 모두 등장한다 (D-18, D-20, D-21).
    11. SKILL.md 본문에 DSPM transcript 금지 가드가 명시되어 있다 — `Do NOT` + `transcript` 키워드가 SKILL.md 본문에 등장하고, 두 문자열이 같은 단락 안에 함께 나타난다 (D-11).
    12. SKILL.md 본문 어디에도 `Skill(skill="hookify:hookify"` 호출이 없고, `save_hookify_lessons` 함수 참조가 없고, `HANDOFF.md` 문자열이 등장하지 않는다 (D-08; canonical_refs lock).
    13. `.claude-plugin/plugin.json` 에 정확히 `"skills": "./skills/"` 키가 `"commands"` 배열 직후, `"homepage"` 직전 위치에 추가되어 있다 — `python3 -c "import json; json.load(open('.claude-plugin/plugin.json'))['skills']"` 가 `./skills/` 를 반환한다. 다른 키(`name`, `version`, `description`, `author`, `homepage`, `repository`, `license`, `keywords`)와 commands 배열의 14개 요소는 글자 그대로 보존되어 있다.
    14. `commands/sg-learn.md`, `commands/sg-status.md`, `hooks/stop_hook.py`, 기타 파일은 본 task로 인해 변경되지 않았다 — `git diff --name-only HEAD` 결과에 `skills/sg-retro/SKILL.md`, `.claude-plugin/plugin.json` 외 다른 파일이 등장하지 않는다 (PLAN/SUMMARY 산출물 제외).
    15. `tests/sg-retro*`, `skills/sg-retro/*.py`, `skills/sg-retro/*.sh` 같은 helper/test fixture 파일이 존재하지 않는다 (Phase 6 D-04 + Phase 7 D-08 lock).
  </acceptance_criteria>
  <done>
    `skills/sg-retro/SKILL.md` 신규 생성 + `.claude-plugin/plugin.json` 1줄 추가, 정확히 두 파일만 변경. 15개 acceptance criteria 모두 통과. Phase 7 D-04~D-06의 STATE.md multi-line 파싱 블록이 SKILL.md에 BEGIN/END demarcate된 형태로 인라인 복제되어 있고 단일 토큰 정규식은 없다. 세 lens 마크다운 골격 + Action Items 3컬럼 표 + lessons 파일 path/append 로직 + DSPM transcript 금지 가드 모두 본문에 반영. hookify, save_hookify_lessons, HANDOFF.md 참조 없음.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 3 lens × Phase 8 fixture 수동 회귀 검증 + lessons append 안전성 + 에러 경로 검증</name>
  <files>.planning/lessons/ (검증 동안 임시로 작성/삭제; 작업 종료 시 정리 상태 확인), 사전 백업: .planning/STATE.md (시나리오 5 단독 수정 시), .planning/phases/09-sg-retro-skill-scaffold/ (시나리오 6에서 임시 디렉터리 변경 시 — 본 phase 디렉터리는 건드리지 않고 가짜 phase 디렉터리로 fixture 구성)</files>
  <read_first>
    - skills/sg-retro/SKILL.md (Task 1 산출물 — 전체)
    - .claude-plugin/plugin.json (skills 등록 확인)
    - .planning/phases/09-sg-retro-skill-scaffold/09-CONTEXT.md (specifics §"수동 검증 시나리오 체크리스트" 11개 행)
    - .planning/milestones/v1.1-phases/08-session-restore/08-CONTEXT.md (Phase 8 fixture로 사용할 phase 디렉터리 위치 — 실제 검증은 09 가 아닌 phase 디렉터리에서 수행해야 함. CONTEXT specifics scenarios는 phase=9 기준이지만, Phase 9 자체는 SUMMARY가 없으므로 fixture로 archived 08 디렉터리를 사용)
    - .planning/milestones/v1.1-phases/08-session-restore/08-01-SUMMARY.md (실제 lens facilitation의 SUMMARY 입력으로 사용)
  </read_first>
  <what-built>
    Task 1에서 `skills/sg-retro/SKILL.md` 한 파일에 6 step(argument parsing + lens 매핑/AskUserQuestion + 타겟 아티팩트 수집 + git 컨텍스트 + lens facilitation 3종 + lessons append)을 모두 구현했고, `.claude-plugin/plugin.json`에 `"skills": "./skills/"` 한 줄을 추가했다. 자동화 테스트 자산은 추가하지 않았으므로(Phase 6 D-04 + Phase 7 D-08 lock 계승), 사람이 직접 3개 lens × 핵심 회귀 시나리오를 실행해 동작 정합성을 확인한다. 검증은 SUMMARY가 이미 있는 Phase 8 디렉터리(아카이브 위치 `.planning/milestones/v1.1-phases/08-session-restore/`)를 fixture로 사용하되, **fixture path가 현재 Skill의 glob `.planning/phases/{NN}-*/` 와 일치하지 않으므로 검증을 위한 임시 fixture 구성 단계를 포함한다**.
  </what-built>
  <action>
    사람이 다음 8개 시나리오를 순서대로 실행하고, 각 시나리오마다 (a) stdout/stderr 출력 (b) `.planning/lessons/` 변화 (c) 다른 파일 비-수정 을 확인한다. 자동화 자산 추가 없음.

    시나리오 fixture 전략: Phase 9 자체는 아직 SUMMARY가 없으므로 일반 회귀는 archived Phase 8 디렉터리를 임시로 `.planning/phases/08-session-restore/` 위치에 symlink로 노출시켜 fixture로 사용한다. 시나리오 종료 시 symlink 제거 + 임시 lessons 파일 정리 + (있으면) STATE.md/HANDOFF.md 원복.
  </what-built>
  <how-to-verify>
    1. **사전 준비 (백업 + fixture 구성).** 절대 잊지 말 것 — 이 phase는 시나리오마다 `.planning/lessons/`에 파일을 만든다:
       ```
       cd /Users/gyuha/workspace/super-gsd
       # lessons 디렉터리 baseline (Task 1 직후 시점)
       LESSONS_BASELINE=$(ls .planning/lessons/ 2>/dev/null | wc -l | tr -d ' ')
       echo "lessons baseline file count: $LESSONS_BASELINE"

       # STATE.md / HANDOFF.md 백업 (시나리오 5, 7에서 STATE.md 변경)
       cp .planning/STATE.md .planning/STATE.md.bak
       [ -f .planning/HANDOFF.md ] && cp .planning/HANDOFF.md .planning/HANDOFF.md.bak

       # Phase 8 fixture: archived 디렉터리를 현재 phases/ 위치에 symlink로 노출
       # (Phase 9의 Skill glob `.planning/phases/{NN}-*/` 가 매칭하도록)
       ln -s ../milestones/v1.1-phases/08-session-restore .planning/phases/08-session-restore
       ls -ld .planning/phases/08-session-restore  # symlink 확인
       ```

    2. **시나리오별 실행 (모두 Phase 8 fixture 기준):**

       **시나리오 1 — `args="8 ssc"` (SSC lens 직행, AskUserQuestion 생략):**
       - 실행: `Skill(skill="sg-retro", args="8 ssc")`
       - 기대 동작:
         - AskUserQuestion 호출 없음.
         - stderr에 `Collected artifacts:` + 3개 경로 (`08-CONTEXT.md`, `08-01-PLAN.md`, `08-01-SUMMARY.md`).
         - stderr에 `Git range: {SHA}..HEAD` (또는 `HEAD~10..HEAD` fallback).
         - SSC lens facilitation 진행 → 사용자에게 draft items 제시 → user 확정 → Action Items 표 확정 → lessons append.
         - 최종 파일: `.planning/lessons/08-{TODAY}.md` 신규 생성.
         - 파일 내용 구조: 최상위 `# Lessons: Phase 8 ({TODAY})` 헤더 + `## Lens: Start/Stop/Continue` + `_Captured: {ISO}_` + `### Start` / `### Stop` / `### Continue` 본문 + `### Action Items` 표 (`| priority | item | next step |`).
         - stderr 마지막 줄에 `lessons file: .planning/lessons/08-{TODAY}.md +N lines` 로그.

       **시나리오 2 — `args="8 4LS"` (case-insensitive 매핑, 같은 파일에 append):**
       - 실행: `Skill(skill="sg-retro", args="8 4LS")`
       - 기대 동작:
         - LENS_RAW=`4LS` → tr lowercase → `4ls` 매핑 성공, AskUserQuestion 생략.
         - 4Ls lens facilitation.
         - lessons 파일은 같은 `.planning/lessons/08-{TODAY}.md` (D-18 append).
         - 두 번째 `## Lens: 4Ls` 섹션이 첫 번째 `## Lens: Start/Stop/Continue` 섹션 뒤에 추가됨.
         - 최상위 헤더(`# Lessons: Phase 8 ...`)는 재작성되지 않음 (D-21).
         - `### Liked` / `### Learned` / `### Lacked` / `### Longed For` 4 subheading 모두 등장.

       **시나리오 3 — `args="8 dspm"` (DSPM lens, transcript 금지 가드 확인):**
       - 실행: `Skill(skill="sg-retro", args="8 dspm")`
       - 기대 동작:
         - DSPM lens facilitation 진행.
         - **확인 핵심**: Claude가 sessions transcript를 읽지 않고 phase 아티팩트 + git 컨텍스트만 사용. SKILL.md 본문의 `Do NOT ... transcript` 가드가 facilitation prompt에 반영되어 있어야 함 (D-11).
         - `### Decisions` / `### Surprises` / `### Patterns` / `### Mistakes` 4 subheading 모두 등장.
         - lessons 파일에 세 번째 lens 섹션 append.

       **시나리오 4 — `args="8 ssc"` 다시 (같은 lens 중복 — `(run 2)` 접미사):**
       - 실행: `Skill(skill="sg-retro", args="8 ssc")` (시나리오 1과 동일 lens, 같은 날)
       - 기대 동작:
         - SSC lens facilitation 진행.
         - lessons 파일에 네 번째 lens 섹션 append, 헤더는 `## Lens: Start/Stop/Continue (run 2)` 형태 (D-20).
         - 시나리오 1의 `## Lens: Start/Stop/Continue` (run 1) 섹션은 그대로 보존.
         - `grep -cE '^## Lens: Start/Stop/Continue' .planning/lessons/08-{TODAY}.md` 결과 = 2.

       **시나리오 5 — `args=""` (STATE.md fallback):**
       - 사전 작업: STATE.md의 `Phase:` 라인을 `Phase: 8 (session-restore) — testing` 로 수정 (또는 실제 STATE.md가 이미 Phase 9 기준이면 그대로 두고 Phase 9의 phase 디렉터리는 fixture가 없으므로 별도 단계로 검증).
         실용적 방법: `sed -i.tmp 's/^Phase: .*/Phase: 8 (session-restore) — testing/' .planning/STATE.md` (백업본은 이미 1단계에서 확보됨).
       - 실행: `Skill(skill="sg-retro", args="")`
       - 기대 동작:
         - STATE.md fallback으로 `PHASE_RAW=8` 추출.
         - LENS_RAW 비어 있음 → AskUserQuestion(header=`Lens`, 3 옵션 + 코드 병기) 호출.
         - 사용자가 `"4Ls (4ls)"` 선택 → 4Ls lens facilitation.
         - lessons 파일에 다섯 번째 lens 섹션 (`## Lens: 4Ls (run 2)`) append (D-20).
       - 사후 작업: `mv .planning/STATE.md.tmp .planning/STATE.md` 또는 1단계 백업 복원.

       **시나리오 6 — `args="99"` (존재하지 않는 phase, 에러 + 사용 가능 phase 목록):**
       - 실행: `Skill(skill="sg-retro", args="99")`
       - 기대 동작:
         - PHASE_RAW=99, PHASE_PAD=99, `.planning/phases/99-*/` glob 결과 없음.
         - stderr 출력 `Phase 99 not found. Available phases:` + `ls .planning/phases/` 결과 (현재 시점에서는 `08-session-restore`, `09-sg-retro-skill-scaffold` 등).
         - exit code 1 (또는 Skill이 0-아닌 종료 — Skill 환경에서는 stdout/stderr 출력 + 즉시 종료로 검증).
         - lessons 파일에 변경 없음.

       **시나리오 7 — `args="abc"` (숫자 아님):**
       - 실행: `Skill(skill="sg-retro", args="abc")`
       - 기대 동작:
         - 숫자 검증 실패 → stderr `Phase token must be a number. Got: 'abc'.` + `Available phases:` 목록.
         - exit 1, lessons 파일 변경 없음.

       **시나리오 8 — `args="8 sailboat"` (매핑되지 않는 lens 코드 → AskUserQuestion fallback):**
       - 실행: `Skill(skill="sg-retro", args="8 sailboat")`
       - 기대 동작:
         - LENS_RAW=`sailboat` → tr lowercase → `sailboat` → case 매핑 실패 → LENS_CODE 빈 문자열 → AskUserQuestion fallback (D-03).
         - AskUserQuestion 3 옵션 표시. 사용자가 `"Decisions/Surprises/Patterns/Mistakes (dspm)"` 선택 시 DSPM lens 진행.
         - lessons 파일에 여섯 번째 lens 섹션 (`## Lens: Decisions/Surprises/Patterns/Mistakes (run 2)`) append.

    3. **lessons 파일 종합 검증** — 8개 시나리오 종료 후 (정리 전):
       ```
       cat .planning/lessons/08-${TODAY}.md
       # 기대:
       # - 최상위 헤더 1줄: `# Lessons: Phase 8 (YYYY-MM-DD)` (중복 없이 정확히 1회)
       # - `## Lens: Start/Stop/Continue` 1회 + `## Lens: Start/Stop/Continue (run 2)` 1회
       # - `## Lens: 4Ls` 1회 + `## Lens: 4Ls (run 2)` 1회
       # - `## Lens: Decisions/Surprises/Patterns/Mistakes` 1회 + `## Lens: Decisions/Surprises/Patterns/Mistakes (run 2)` 1회
       # - 각 lens 섹션마다 `_Captured: {ISO}_` italic 라인 1개 + lens별 fixed subheadings + `### Action Items` 표
       grep -c '^# Lessons: Phase 8' .planning/lessons/08-${TODAY}.md  # 1
       grep -c '^## Lens: Start/Stop/Continue' .planning/lessons/08-${TODAY}.md  # 2
       grep -c '^## Lens: 4Ls' .planning/lessons/08-${TODAY}.md  # 2
       grep -c '^## Lens: Decisions/Surprises/Patterns/Mistakes' .planning/lessons/08-${TODAY}.md  # 2
       grep -c '_Captured:' .planning/lessons/08-${TODAY}.md  # 6
       grep -c '| priority | item | next step |' .planning/lessons/08-${TODAY}.md  # 6
       ```

    4. **HANDOFF.md / hooks/stop_hook.py 비-수정 검증** (D-08):
       ```
       diff .planning/HANDOFF.md .planning/HANDOFF.md.bak  # 변화 없음
       git diff hooks/stop_hook.py  # 빈 출력
       git diff commands/sg-learn.md commands/sg-status.md  # 빈 출력
       ```

    5. **정리 (필수):**
       ```
       # symlink 제거
       rm .planning/phases/08-session-restore  # symlink만 제거되며 archived 원본은 보존됨
       # 테스트로 생성한 lessons 파일 제거 (검증 끝났으므로 — 실제 phase 9 retro는 별도)
       rm -f .planning/lessons/08-${TODAY}.md
       # STATE.md / HANDOFF.md 백업 복원
       mv .planning/STATE.md.bak .planning/STATE.md
       [ -f .planning/HANDOFF.md.bak ] && mv .planning/HANDOFF.md.bak .planning/HANDOFF.md
       # 검증
       ls .planning/phases/  # 09-sg-retro-skill-scaffold 만 보여야 함
       ls .planning/lessons/ 2>/dev/null | wc -l  # baseline 과 동일 (대개 0)
       git diff --stat .planning/STATE.md .planning/HANDOFF.md .planning/phases/  # 원복 완료 확인 (빈 출력)
       ```

    6. **실 환경 smoke test** — 백업 복원 후 한 번 더 `Skill(skill="sg-retro", args="9")` 를 실 환경에서 실행해 다음을 확인:
       - Phase 9 디렉터리는 존재(`.planning/phases/09-sg-retro-skill-scaffold/`)하지만 SUMMARY가 아직 없으므로 `SUMMARY_FILES` 가 빈 문자열.
       - CONTEXT.md만 수집되고 PLAN-01 (본 파일)도 수집된다 (이 plan이 commit되었다면).
       - AskUserQuestion 호출 → 사용자가 lens 선택 가능.
       - 사용자가 검증용으로 `"Start/Stop/Continue (ssc)"` 선택 → SSC lens 진행 → lessons 파일 `.planning/lessons/09-{TODAY}.md` 생성 가능 확인.
       - (선택) 이 lessons 파일은 그대로 유지하거나 검증용이면 삭제.
  </how-to-verify>
  <verify>
    <human-check>
    8개 시나리오 모두 기대 동작과 정확히 일치하고, 시나리오 종료 후 정리 단계에서:
    (a) `git diff --stat` 결과에 본 plan의 의도된 변경(`skills/sg-retro/SKILL.md`, `.claude-plugin/plugin.json`, `.planning/phases/09-sg-retro-skill-scaffold/09-01-SUMMARY.md`) 외 다른 파일이 없다.
    (b) `.planning/HANDOFF.md`, `hooks/stop_hook.py`, `commands/sg-learn.md`, `commands/sg-status.md` 가 본 phase로 인해 변경되지 않았다.
    (c) `.planning/lessons/` 디렉터리가 baseline 상태 (Task 1 직후 시점)와 일치한다 (테스트로 생성한 `08-{TODAY}.md` 제거됨).
    (d) `.planning/phases/08-session-restore` symlink가 제거되어 있다.
    </human-check>
  </verify>
  <acceptance_criteria>
    1. 시나리오 1 (`args="8 ssc"`): AskUserQuestion 호출 없이 SSC lens 진행. `.planning/lessons/08-{TODAY}.md` 신규 생성, 최상위 헤더 + `## Lens: Start/Stop/Continue` + `_Captured:` + 3 subheading + Action Items 표.
    2. 시나리오 2 (`args="8 4LS"`): case-insensitive 매핑 성공, 4Ls lens 진행, 같은 파일에 append. 최상위 헤더 중복 없음 (D-21).
    3. 시나리오 3 (`args="8 dspm"`): DSPM lens 진행, transcript 읽기 없음 (Claude의 도구 호출 로그로 확인 — Read tool이 phase 아티팩트와 git 출력만 대상으로 함). 4 subheading 모두 등장.
    4. 시나리오 4 (`args="8 ssc"` 재실행): `## Lens: Start/Stop/Continue (run 2)` 접미사 적용, run 1 섹션 보존.
    5. 시나리오 5 (`args=""` STATE.md fallback): STATE.md `Phase: 8 (...)` 라인에서 `8` 추출 (multi-line 패턴이 정상 작동 — `Phase: 8` 의 `8` 만 추출, 뒤의 `(session-restore) — testing` 잘림 없이 정상 처리). AskUserQuestion 호출 → 사용자가 옵션 라벨에서 코드 `(4ls)` 추출 가능 (D-13). 4Ls (run 2) 섹션 추가.
    6. 시나리오 6 (`args="99"`): stderr `Phase 99 not found. Available phases:` + `ls .planning/phases/` 결과, exit 1. lessons 파일 무변경.
    7. 시나리오 7 (`args="abc"`): stderr `Phase token must be a number. Got: 'abc'.` + available phases, exit 1.
    8. 시나리오 8 (`args="8 sailboat"`): LENS_RAW 매핑 실패 → AskUserQuestion fallback, 사용자 선택 후 lens 진행.
    9. lessons 파일 최종 grep 결과 일치:
       - `^# Lessons: Phase 8` 정확히 1회
       - `^## Lens: Start/Stop/Continue` 2회 (run 1 + run 2)
       - `^## Lens: 4Ls` 2회
       - `^## Lens: Decisions/Surprises/Patterns/Mistakes` 2회
       - `_Captured:` 6회
       - `| priority | item | next step |` 6회
    10. SESS-04 정신 계승: 시나리오 종료 후 `.planning/HANDOFF.md` 가 baseline 과 동일 (`diff` 빈 출력).
    11. D-08 lock 유지: `hooks/stop_hook.py`, `commands/sg-learn.md`, `commands/sg-status.md` 가 본 phase 작업으로 변경되지 않았다 (`git diff` 빈 출력).
    12. 정리 후 `.planning/phases/08-session-restore` symlink 제거됨, `.planning/lessons/08-{TODAY}.md` 테스트 파일 제거됨, STATE.md/HANDOFF.md 원복됨.
    13. 실 환경 smoke test (시나리오 후속): `Skill(skill="sg-retro", args="9")` 실행 시 AskUserQuestion 정상 표시 + `.planning/lessons/09-{TODAY}.md` 생성 가능 확인 (이 파일은 실제 Phase 9 retro 진행 여부에 따라 유지/삭제).
  </acceptance_criteria>
  <done>
    8개 시나리오 모두 기대 동작과 일치, lessons 파일 grep 검증 9개 모두 일치, HANDOFF.md/hooks/sg-learn/sg-status 비-수정 확인, 정리 단계 완료, 실 환경 smoke test 통과.
  </done>
  <resume-signal>
    8개 시나리오 + lessons 파일 grep 검증 + 정리 + smoke test 모두 통과하면 "approved" 입력. 시나리오별 출력 불일치, lessons 파일 grep 카운트 불일치, 또는 정리 후 의도하지 않은 파일 변경이 발견되면 시나리오 번호와 실제 출력/diff를 보고 — Task 1로 돌아가 SKILL.md 수정.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (no untrusted boundary crossed) | `sg-retro` Skill은 로컬 `.planning/STATE.md`, `.planning/phases/{NN}-*/{NN}-CONTEXT.md` + `{NN}-*-PLAN.md` + `{NN}-*-SUMMARY.md`, `git log`/`git diff` 결과만 읽고, AskUserQuestion으로 사용자에게 선택을 받은 뒤 `.planning/lessons/{NN}-{YYYY-MM-DD}.md` 한 파일에만 append한다. 네트워크 없음. 외부 명령 실행은 `git`, `grep`, `sed`, `awk`, `ls`, `printf`, `wc`, `mkdir`, `date`, `tr` POSIX 도구뿐. `$ARGUMENTS` 는 phase 토큰 숫자 검증 + lens 토큰 case 매핑으로 화이트리스트 처리. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `.planning/STATE.md` `Phase:` 라인 (악성 콘텐츠) | accept | 본 Skill은 phase 번호만 추출하며 `awk '{print $1}'` + `grep -qE '^[0-9]+$'` 숫자 화이트리스트 검증으로 화이트리스트 통과한 값만 사용. 셸 인젝션 surface는 bash 변수 인용(`"$PHASE_RAW"`, `"$PHASE_PAD"`)으로 차단. `eval` 미사용. Repo가 이미 침해된 상태에서만 발생 가능 — Phase 7 T-07-01 / Phase 8 T-08-01과 동일 disposition. |
| T-09-02 | Tampering | `$ARGUMENTS` LENS_RAW (악성 lens 토큰) | mitigate | case 매핑 화이트리스트(`ssc`/`4ls`/`dspm`)로 정확히 3개 값만 허용. 매핑 실패 시 AskUserQuestion fallback — 사용자가 화이트리스트에서 선택. `LENS_CODE` 변수만 이후 단계에서 사용되며 이 변수는 case 분기로만 설정됨 (직접 사용자 입력 미반영). |
| T-09-03 | Information Disclosure | `git diff ${BASE}..HEAD` 출력이 lessons 파일 본문에 노출 | accept | `git diff`는 로컬 repo 콘텐츠로, lessons 파일은 같은 로컬 `.planning/lessons/` 디렉터리에 작성됨 — 외부 공개 surface 아님 (git commit 전 사용자가 검토 가능). 비밀이 git에 이미 노출되어 있으면 그것이 1차 위험이며 본 Skill은 그 상태를 악화시키지 않음. |
| T-09-04 | Denial of Service | `git diff` 크기 폭발 (대형 phase) | mitigate | D-07의 적응형 1000줄 cap + `git diff --stat` fallback이 SKILL.md에 인라인 복제됨. cap 초과 시 stat summary만 lens facilitation 입력으로 사용 — context window 부담 제한. |
| T-09-05 | Tampering | lessons 파일 partial write (시그널/인터럽트 중 append) | mitigate | D-21의 single `>>` redirect 블록으로 lens 섹션 전체를 한 번에 작성. `wc -l` before/after 비교 라인을 stderr에 로그하여 부분 작성 감지 가능 (사용자가 시각적으로 확인). atomicity 추가 보장은 OS append 시멘틱에 의존 — POSIX `O_APPEND` 보장 범위 내에서 single-block write는 atomic. |
| T-09-06 | Elevation of Privilege | Skill이 `hooks/stop_hook.py` 의 `save_hookify_lessons()` 우회로 권한 확장 (D-08 lock 위반) | mitigate | Task 1 action에 명시적 금지 + acceptance criteria 12번에서 `grep -F save_hookify_lessons` 빈 결과로 검증. Skill은 자체적으로 lessons 파일을 작성하지만 같은 `.planning/lessons/` 디렉터리이며, `hooks/stop_hook.py` 의 helper도 같은 경로를 쓰므로 권한 격차 없음 — 단순히 helper 코드 경로를 우회하는 것이지 권한이 다르지 않다. |
| T-09-SC | Tampering | npm/pip/cargo installs | n/a | Phase 9는 패키지 설치 작업 없음 — RESEARCH.md Package Legitimacy Audit 불필요 (canonical_refs에 RESEARCH.md 없음). |

요약: 읽기 + 단일 파일 append 작업. 사용자 입력은 두 화이트리스트(phase 숫자, lens 코드 3개) + AskUserQuestion 라벨 응답으로 한정. 네트워크 없음. bash 변수 인용 위생 + 화이트리스트 case 매핑 + 적응형 diff cap + single-block append + wc -l 로그 + Phase 7 D-04~D-06 multi-line 인라인 복제(`\\S+` 토큰 정규식 금지)로 mitigation 완료.
</threat_model>

<verification>
1. Task 1 자동 verify (`<automated>` 블록의 모든 `grep -F` / `! grep -F` / python3 json 회귀 체크)가 `PASS`로 종료한다.
2. Task 2 수동 검증 8개 시나리오 모두 기대 동작과 정확히 일치 — 특히:
   - 시나리오 1: SSC lens 직행, lessons 파일 신규 생성, 최상위 헤더 + 3 subheading + Action Items 표.
   - 시나리오 2: 4LS case-insensitive 매핑, 같은 파일 append, 최상위 헤더 중복 없음.
   - 시나리오 3: DSPM lens transcript 금지 가드 작동 (Claude가 transcript 도구 호출 안 함).
   - 시나리오 4: 같은 lens 두 번째 실행 → `(run 2)` 접미사.
   - 시나리오 5: STATE.md fallback, AskUserQuestion 라벨에서 코드 추출.
   - 시나리오 6: 존재하지 않는 phase → stderr + 사용 가능 목록 + exit 1.
   - 시나리오 7: 숫자 아닌 입력 → stderr + exit 1.
   - 시나리오 8: 매핑 안 되는 lens 토큰 → AskUserQuestion fallback.
3. lessons 파일 grep 검증 (시나리오 종료 후): 헤더 1회, 각 lens 헤더 2회 (run 1 + run 2), `_Captured:` 6회, Action Items 표 6회.
4. SESS-04 정신 계승: 시나리오 종료 후 `.planning/HANDOFF.md` baseline 일치, `hooks/stop_hook.py` / `commands/sg-learn.md` / `commands/sg-status.md` 무변경.
5. `git diff --stat` 결과: `skills/sg-retro/SKILL.md` 신규, `.claude-plugin/plugin.json` 1줄 변경, `.planning/phases/09-sg-retro-skill-scaffold/09-01-PLAN.md` (본 파일) + `09-01-SUMMARY.md` 외 다른 파일 없음.
6. `tests/sg-retro*`, `skills/sg-retro/*.py`, `skills/sg-retro/*.sh` 같은 새 helper/test 파일이 존재하지 않는다 (Phase 6 D-04 + Phase 7 D-08 lock).
</verification>

<success_criteria>
- RETRO-01 충족: `skills/sg-retro/SKILL.md` 존재 + `.claude-plugin/plugin.json` 의 `skills: "./skills/"` 등록으로 Claude Code Skill 시스템에 로드 가능.
- RETRO-02 충족: phase argument로 `{NN}-CONTEXT.md` + `{NN}-*-PLAN.md` + `{NN}-*-SUMMARY.md` 자동 수집 + git log/diff (BASE..HEAD or HEAD~10..HEAD fallback) 자동 수집.
- RETRO-03 (부분) 충족: 3개 lens (Start/Stop/Continue, 4Ls, Decisions/Surprises/Patterns/Mistakes) 구현 + AskUserQuestion 인터랙티브 선택 + 두 번째 토큰으로 비대화형 선택 가능. Sailboat/Five Whys/다중 lens 선택은 Phase 10 책임 (canonical_refs에 명시).
- RETRO-04 충족: 각 lens 출력은 일관된 마크다운 구조 (`## Lens: {name}` + `_Captured: {ISO}_` + lens별 fixed subheading + `### Action Items` 3컬럼 표) 가지며, `.planning/lessons/{NN}-{YYYY-MM-DD}.md` 에 lens별 섹션으로 저장.
- Phase 7 D-07 정신 계승 (D-08 인라인 복제 계약): STATE.md `^Phase:` 파싱 블록을 BEGIN/END 주석 demarcate된 형태로 sg-status.md 와 글자 그대로 일치하게 인라인 복제. 단일 토큰 정규식 `\\S+` 미도입.
- Phase 6 D-04 + Phase 7 D-08 lock 유지: helper 스크립트, 자동화 테스트 자산, fixture 디렉터리 추가 없음. 정확히 두 파일 (`skills/sg-retro/SKILL.md` + `.claude-plugin/plugin.json`) 만 변경.
- D-08 + canonical_refs lock 유지: `hooks/stop_hook.py`, `commands/sg-learn.md`, `commands/sg-status.md` 본 phase 작업으로 미변경. `save_hookify_lessons()` / `_read_current_phase()` 우회로 lessons 파일 직접 작성.
- D-11 transcript 금지 가드: DSPM lens facilitation prompt에 명시적 "Do NOT read session transcript" 가드 + Phase 10 ANALYZER 영역 명시.
- D-16 언어 정책 lock: frontmatter/AskUserQuestion/상태·에러 메시지 영문 고정, lens facilitation prompt 내 사용자 대화 및 lens 본문 항목 사용자 언어 auto-detect, 마크다운 구조 마커 영문 결정적.
- D-17/D-18/D-19/D-20/D-21 append-only 정신 계승 + LESSONS-01 직접 충족: 파일 없으면 최상위 헤더 + 첫 lens 섹션, 있으면 새 lens 섹션만 append. 같은 lens 중복 시 `(run N)` 접미사. 덮어쓰기 금지.
</success_criteria>

<output>
Create `.planning/phases/09-sg-retro-skill-scaffold/09-01-SUMMARY.md` when done.
</output>
