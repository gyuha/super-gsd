# Milestone v2.2: sg-next Auto-Advance

**Status:** ✅ SHIPPED 2026-05-24
**Phases:** 26
**Total Plans:** 1
**Version:** 0.0.33

## Overview

sg-next 명령 하나로 현재 워크플로우 단계를 자동 감지하고 다음 sg-* 명령을 확인 없이 즉시 invoke한다.
HANDOFF.md + STATE.md를 읽어 현재 stage를 파악하고, sg-status와 동일한 라우팅 테이블로 다음 단계를 결정한다.
모호한 상태(complete/init)에서만 AskUserQuestion으로 선택지를 제시한다.

## Phases

### Phase 26: sg-next 스킬 구현

**Goal**: sg-next 명령 하나로 현재 워크플로우 단계를 감지하고 다음 sg-* 명령을 확인 없이 자동 invoke한다
**Depends on**: Phase 25 (v2.2 신규 시작)
**Plans**: 1 plan

Plans:

- [x] 26-01-PLAN.md — skills/sg-next/SKILL.md 신규 생성 + HANDOFF append (NEXT-01~05)

**Key files:**
- `skills/sg-next/SKILL.md` — 신규 생성 (188줄 → sg-review 수정 후 201줄)

---

## Milestone Summary

**Key Accomplishments:**

1. `skills/sg-next/SKILL.md` 신규 생성 — HANDOFF.md + STATE.md 기반 자동 단계 진행 (NEXT-01~05 모두 충족)
2. sg-status와 동일한 11개 분기 라우팅 테이블을 D-07 inline-replication으로 복제
3. sg-next → sg-next 재진입 루프 처리: whitelist에 `sg-next` 추가 + FROM 컬럼 transparent-pass
4. conditional HANDOFF append: complete/init 상태에서 cancel 시 감사 로그 오염 방지
5. sg-review에서 Important 이슈 2개 발견·수정: HANDOFF init 체크 2조건화, FROM_STAGE 동적 읽기

**Key Decisions:**

- **D-01**: sg-next는 중복 방지 없음 — 상태 라우터이므로 idempotency 불필요
- **D-04**: HANDOFF append는 Skill() invoke 전 기록 — 감사 로그 완결성 우선
- **D-05**: sg-status 코드 D-07 inline-replication으로 복제 — 파싱 후처리 방식 불채택
- **D-07 inline-replication**: 3개 블록(STATE.md 파싱, HANDOFF 파싱+enum, routing) — drift 시 양쪽 동시 수정

**Issues Resolved:**

- sg-next → sg-next 자기참조 루프 (sg-review Critical 발견 → 즉각 수정)
- HANDOFF init 체크 두 번째 조건 누락 (sg-review Important)
- complete/init 분기 FROM_STAGE 하드코딩 (sg-review Important)

**Issues Deferred:**

- Quick tasks 13개 — 이전 세션 잔여물, v2.3 정리 예정
- HANDOFF stage enum에 `sg-next` 추가 미문서화 (CLAUDE.md Architecture 섹션)

**Technical Debt Incurred:**

- `.local.md` 룰 파일은 .gitignore로 제외 — 각 로컬 환경에 수동 설치 필요
- HANDOFF "sg-retro" FROM_STAGE 컬럼 파싱 버그 (sg-retro SKILL.md $4/$5 컬럼 혼용) — v2.3 수정 검토

---

*For current project status, see .planning/ROADMAP.md*
