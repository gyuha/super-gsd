---
quick_id: 260525-tr1
type: execute
autonomous: true
files_modified:
  - hooks/rule_runner.cjs
  - hooks/stop_hook.cjs
  - hooks/transcript_matcher.cjs
  - CLAUDE.md
  - README.md
  - README.ko.md
  - CHANGELOG.md
  - skills/sg-next/SKILL.md
  - skills/sg-status/SKILL.md
  - skills/sg-start/SKILL.md
  - skills/sg-health/SKILL.md
---

<objective>
Remove all hookify integration and references from super-gsd. Category A: strip the `_hookifyInstalled()` early-exit, the hookify rule glob, the `'hookify-complete'` signal branch, and now-orphaned helpers (`HOOKIFY_SIGNALS`, `_extractHookifyOutput`, `_joinLastNLinesWithTerminators`, `saveHookifyLessons`) from the three `.cjs` hooks. Category B: rewrite or delete hookify mentions in `CLAUDE.md`, `README*.md`, and the four `skills/sg-*/SKILL.md` files, then append a new "removed hookify integration" entry to `CHANGELOG.md` (without rewriting prior version history). The `hooks/*.py` files stay untouched — Phase 31 CLEAN-01 will delete them wholesale per the Phase 28/29 D-03 lock.
</objective>

<context>
@CLAUDE.md
@hooks/rule_runner.cjs
@hooks/stop_hook.cjs
@hooks/transcript_matcher.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Category A runtime integration from .cjs hooks</name>
  <files>hooks/rule_runner.cjs, hooks/stop_hook.cjs, hooks/transcript_matcher.cjs</files>
  <read_first>
    hooks/rule_runner.cjs (lines 1-15 header, 87-95 _hookifyInstalled, 270-280 loadGlob calls, 365-380 main entry)
    hooks/stop_hook.cjs (lines 108-152 orphan helpers, 200-215 hookify-complete branch)
    hooks/transcript_matcher.cjs (lines 21-25 HOOKIFY_SIGNALS, 44-50 detection chain)
  </read_first>
  <action>
    Make surgical edits across three .cjs files. Preserve byte-exact non-hookify code.

    In hooks/rule_runner.cjs:
    - Rewrite the header comment block (lines 2-10) to remove every mention of hookify. Keep the file's purpose statement (PreToolUse hook evaluates .claude/sg-rule.*.local.md rules), the supported-events line, and the prompt-event-unsupported note. Do NOT introduce new sentences beyond what is needed to keep the comment coherent.
    - Delete the entire `_hookifyInstalled()` function (lines 87-95) including the blank line that follows.
    - Delete the line `loadGlob(_globLocalMd(path.join(PLUGIN_ROOT, '.claude'), 'hookify'), 1);` near line 273. Leave the sg-rule loadGlob call immediately after it untouched.
    - In `main()` near line 370, delete the four-line `if (_hookifyInstalled()) { console.log(_pyJsonDumps({})); process.exit(0); }` block. Leave the config-reading code that follows untouched.
    - After the edits, scan for any remaining identifiers `hookify`, `_hookifyInstalled` (none should remain). Do not remove any other helper, import, or constant — `loadGlob`, `_globLocalMd`, `PLUGIN_ROOT`, `path`, `os`, `fs` all have non-hookify users.

    In hooks/stop_hook.cjs:
    - Delete the entire `_extractHookifyOutput()` function (lines 108-116) and the blank line after it.
    - Delete the entire `_joinLastNLinesWithTerminators()` function (lines 119-130) and the blank line after it. Verified: it has no callers outside `_extractHookifyOutput`.
    - Delete the entire `saveHookifyLessons()` function (lines 133-152) and the blank line after it.
    - In `main()` (around lines 203-210), delete the entire `else if (signal === 'hookify-complete') { ... }` branch — both the lessonFile path and the fallback path. Keep the surrounding `if/else if` chain (gsd-plan-complete, superpowers-implementation-complete, superpowers-review-complete branches and the final `else { response = {}; }`) byte-exact.
    - In the `_joinLastNLinesWithTerminators` markers list (line 112), the literal `'hookify complete'` is deleted along with the whole function — no separate edit needed.
    - After edits, the file should reference no `hookify`-named symbol. The `cmdLearn` variable assigned to `/super-gsd:sg-learn` (line 187) is consumed only by the deleted branch — confirm it has no other reader; if confirmed orphaned, delete the two `cmdLearn` assignments (platform === 'claude-code' branch and the else branch). Likewise verify `_todayYmd` — if its only caller was `saveHookifyLessons`, delete it too.
    - Run grep to confirm orphan status before deleting `cmdLearn` / `_todayYmd`. If grep shows another caller (e.g., a sg-learn branch added later), leave them in place.

    In hooks/transcript_matcher.cjs:
    - Delete the `HOOKIFY_SIGNALS` array declaration (lines 21-25) including the blank line after it.
    - Delete the line `if (HOOKIFY_SIGNALS.some(sig => recent.includes(sig))) return 'hookify-complete';` (line 49).
    - The three remaining signal-detection branches (gsd-plan, implementation, review) and the trailing `return '';` stay byte-exact.

    Do NOT touch `hooks/rule_runner.py`, `hooks/stop_hook.py`, `hooks/transcript_matcher.py`, or any other file in this task. Phase 31 deletes the .py files wholesale.

    Do NOT add any "hookify removed" comments in the source — the git log is the audit trail.
  </action>
  <verify>
    <automated>node --check hooks/rule_runner.cjs && node --check hooks/stop_hook.cjs && node --check hooks/transcript_matcher.cjs && echo '{}' | node hooks/stop_hook.cjs >/dev/null && echo '{"tool_name":"Bash","tool_input":{"command":"ls"}}' | node hooks/rule_runner.cjs >/dev/null && [ "$(grep -E -c hookify hooks/rule_runner.cjs hooks/stop_hook.cjs hooks/transcript_matcher.cjs | awk -F: '{s+=$2} END{print s}')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` passes on all three .cjs files.
    - Both smoke-test stdins (empty-object to stop_hook, simple Bash ls to rule_runner) exit 0 with no stderr.
    - `grep -E hookify` returns zero matches across the three .cjs files.
    - No orphaned constants, functions, or imports remain in the three .cjs files.
  </acceptance_criteria>
  <done>The three .cjs files no longer contain the string `hookify` in any case, all syntax checks pass, and both smoke recipes exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Category B doc references, add CHANGELOG entry, atomic commit</name>
  <files>CLAUDE.md, README.md, README.ko.md, CHANGELOG.md, skills/sg-next/SKILL.md, skills/sg-status/SKILL.md, skills/sg-start/SKILL.md, skills/sg-health/SKILL.md</files>
  <read_first>
    Run `rg -n hookify CLAUDE.md README.md README.ko.md skills/sg-next/SKILL.md skills/sg-status/SKILL.md skills/sg-start/SKILL.md skills/sg-health/SKILL.md` and read each matched line plus 3 lines of surrounding context to understand what each mention does (rule priority claim, onboarding instruction, status display, diagnostic check, architecture description).
    Read CHANGELOG.md top 20 lines to learn the existing version-entry format (heading style, date format, section labels).
  </read_first>
  <action>
    Walk each file and remove or rewrite hookify references. The rule: if a sentence/bullet exists only to explain hookify or to claim coexistence with hookify, delete the whole sentence/bullet. If a sentence has independent value and merely mentions hookify in passing, rewrite it to remove the hookify reference while preserving the surrounding claim. Match existing prose style (Korean where files are Korean, English where English).

    CLAUDE.md (4 matches):
    - Technology Stack > Rules bullet (around line 29): currently says ".claude/hookify.*.local.md, .claude/sg-rule.*.local.md". Rewrite to mention only `.claude/sg-rule.*.local.md` and the `rule_runner.cjs` evaluator. (Note: that bullet currently says `rule_runner.py` — leave that pre-existing inaccuracy alone per the surgical-changes rule; this task is about hookify only.)
    - Architecture > Hooks bullet for `rule_runner.py` (around the rule_runner description): currently says "hookify 플러그인이 설치된 환경에서는 자동으로 skip한다." Delete that sentence.
    - Architecture > 데이터 흐름 block: if it mentions hookify, remove that line; keep the surrounding flow lines untouched.
    - Architecture > 규칙 파일 우선순위 section: currently says "sg-rule.*.local.md (priority 2) > hookify.*.local.md (priority 1). 이름이 충돌할 경우 우선순위가 높은 파일이 채택된다." Replace the whole section body with a single sentence stating that only `sg-rule.*.local.md` files are evaluated, OR delete the entire "규칙 파일 우선순위" subsection if the priority concept no longer carries weight with only one source. (Choose deletion if the remaining sentence would be trivially "only sg-rule files exist." Choose rewrite if other text references the priority concept — search the file first.)

    README.md (1 match) and README.ko.md (1 match): one historical mention each. Read the surrounding paragraph; if the sentence is hookify-only, delete it; if it's a list item among other features, remove just that item. Match each language.

    skills/sg-next/SKILL.md (5 matches): workflow steps mention hookify integration. For each match, decide: if the step is hookify-only (e.g., "if hookify signal detected, do X"), remove the step; if it's a step that does something else and incidentally mentions hookify, rewrite to drop the hookify reference. The post-edit workflow must still read as a coherent next-step decision tree.

    skills/sg-status/SKILL.md (7 matches): status display lines that show hookify state or check for the hookify cache. Remove those lines entirely — the status display can simply not report hookify state once hookify isn't supported.

    skills/sg-start/SKILL.md (5 matches): onboarding sentences about installing or checking for hookify. Remove. If a numbered list breaks, renumber.

    skills/sg-health/SKILL.md (1 match): diagnostic that checks for hookify presence. Remove the check entirely (no replacement diagnostic).

    CHANGELOG.md: do NOT modify the 18 existing historical hookify entries — they document past behavior for posterity. ADD a new top-most version entry above the current latest version block. Use today's date (2026-05-25). Use the existing CHANGELOG version-entry format (read top 20 lines first to mirror heading levels and section labels). The entry should have a single "Removed" section bullet: `hookify 플러그인 통합 및 모든 코드/문서 참조 제거 (`.cjs` hooks와 skills/, CLAUDE.md, README*.md). `.py` 파일은 Phase 31에서 일괄 삭제 예정.` Use the existing version-numbering convention: read `.claude-plugin/plugin.json` for the current version and bump the patch (e.g., `0.0.9` -> `0.0.10`) to match the project's deploy convention. ALSO update `.claude-plugin/plugin.json` to the new version — this is required by the project's "버전 관리" convention which forbids CHANGELOG updates without a plugin.json bump.

    After all file edits, stage everything from Task 1 and Task 2 and create a single atomic commit:

    Subject: `chore: remove hookify integration and references`

    Body: brief summary mentioning Category A (.cjs runtime) and Category B (docs) scope. Note that `.py` files are out of scope (Phase 31). Note plugin.json version bump and CHANGELOG entry. Trailer: `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`.

    Stage these exact files: `hooks/rule_runner.cjs hooks/stop_hook.cjs hooks/transcript_matcher.cjs CLAUDE.md README.md README.ko.md CHANGELOG.md .claude-plugin/plugin.json skills/sg-next/SKILL.md skills/sg-status/SKILL.md skills/sg-start/SKILL.md skills/sg-health/SKILL.md`. Do not use `git add -A` or `git add .` — list files explicitly to keep the PLAN.md (quick directory) out; sg-quick commits it separately.

    Do NOT push.
  </action>
  <verify>
    <automated>[ "$(grep -E -l hookify hooks/rule_runner.cjs hooks/stop_hook.cjs hooks/transcript_matcher.cjs CLAUDE.md README.md README.ko.md skills/sg-next/SKILL.md skills/sg-status/SKILL.md skills/sg-start/SKILL.md skills/sg-health/SKILL.md 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && git log -1 --pretty=%s | grep -q '^chore: remove hookify' && git diff HEAD~1 --name-only | grep -q CHANGELOG.md && git diff HEAD~1 --name-only | grep -q '.claude-plugin/plugin.json'</automated>
  </verify>
  <acceptance_criteria>
    - Across `hooks/*.cjs`, `CLAUDE.md`, `README.md`, `README.ko.md`, and `skills/sg-{next,status,start,health}/SKILL.md`: zero `hookify` matches (case-insensitive via `grep -Ei` if needed for spot-check, but `grep -E` is the gate).
    - CHANGELOG.md still contains the 18 historical hookify mentions (preserved) AND one new top-most version entry documenting the removal.
    - `.claude-plugin/plugin.json` `version` field bumped one patch.
    - One git commit exists with subject `chore: remove hookify integration and references` and the Co-Authored-By trailer.
    - Commit includes all 12 files listed above and no others.
    - Working tree is clean (apart from the PLAN.md and STATE.md that sg-quick will commit afterwards).
  </acceptance_criteria>
  <done>The atomic removal commit exists, the project no longer references hookify in any active source/skill/doc file, CHANGELOG history is preserved, and a new CHANGELOG entry + plugin.json version bump record the removal.</done>
</task>

</tasks>

<success_criteria>
  - `grep -E -l hookify hooks/*.cjs CLAUDE.md README.md README.ko.md skills/sg-*/SKILL.md` returns no file names (zero matches). On systems without `grep -E`, `grep -rl hookify` over the same paths must likewise return empty.
  - `CHANGELOG.md` is the only file in the repo (outside `hooks/*.py` and historical `.planning/` artifacts) that still contains the string `hookify`. Its 18 historical entries are byte-identical to pre-task state. One new top entry has been added.
  - `node --check` passes for all three modified `.cjs` files; their two smoke-test invocations exit 0.
  - `hooks/*.py` files are byte-identical to pre-task state (verify with `git diff HEAD -- hooks/*.py` returning empty).
  - Exactly one new git commit exists with subject `chore: remove hookify integration and references`.
  - `.claude-plugin/plugin.json` version is bumped one patch level.
</success_criteria>
