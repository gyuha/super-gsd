---
phase: 34-sg-setup-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/sg-setup/SKILL.md
  - skills/sg-setup/SKILL.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02

must_haves:
  truths:
    - ".agents/skills/sg-setup/SKILL.md 파일이 존재하고 $sg-setup 명령으로 호출 가능하다 (SKILL-02)"
    - "skills/sg-setup/SKILL.md 파일이 존재하고 /super-gsd:sg-setup 명령으로 호출 가능하다 (SKILL-02, D-17)"
    - "$sg-setup 실행 시 require.resolve('@gyuha/super-gsd/package.json')으로 PKG_ROOT를 결정하고 hooks/, .agents/, .codex/hooks.json을 대상 프로젝트에 복사한다 (SKILL-01, D-01~D-04)"
    - "--gemini 인수 또는 GEMINI_PROJECT_DIR 환경변수 감지 시 .gemini/settings.json도 추가 복사된다 (SKILL-01, D-05~D-08)"
    - "대상 경로에 파일이 이미 존재하면 스킵+경고, --force 플래그 시 덮어쓰기한다 (SKILL-01, D-09~D-12)"
    - "복사 완료 후 복사된 파일 목록과 Copied/Skipped summary가 출력된다 (SKILL-01, D-13~D-16)"
  artifacts:
    - path: ".agents/skills/sg-setup/SKILL.md"
      provides: "Codex/Gemini 플랫폼용 sg-setup 스킬 — AI Read/Write 도구로 파일 복사"
      contains: "name: sg-setup"
    - path: "skills/sg-setup/SKILL.md"
      provides: "Claude Code 플랫폼용 sg-setup 스킬 — Bash Write 도구로 파일 복사"
      contains: "name: sg-setup"
  key_links:
    - from: ".agents/skills/sg-setup/SKILL.md"
      to: "@gyuha/super-gsd"
      via: "require.resolve('@gyuha/super-gsd/package.json')"
      pattern: "require\\.resolve.*@gyuha/super-gsd"
    - from: "skills/sg-setup/SKILL.md"
      to: "hooks/, .agents/, .codex/"
      via: "Bash Write tool 파일 복사"
      pattern: "PKG_ROOT"
---

<objective>
Phase 34 목표: `$sg-setup` 스킬 하나로 Codex/Gemini 세션 내부에서 프로젝트에 필요한 파일(`hooks/`, `.agents/`, 플랫폼별 설정 파일)을 자동으로 복사한다. (SKILL-01, SKILL-02)

Purpose: 현재 Codex/Gemini 사용자가 `npx @gyuha/super-gsd install`을 실행한 뒤 수동으로 파일을 확인해야 하는 과정을, 세션 내에서 `$sg-setup` 단일 명령으로 완결시킨다.

Output:
- `.agents/skills/sg-setup/SKILL.md` — Codex/Gemini 플랫폼 전용. AI Read/Write 도구를 사용해 npm 패키지 경로에서 대상 프로젝트로 파일을 직접 복사. (D-13~D-16)
- `skills/sg-setup/SKILL.md` — Claude Code 플랫폼 전용. 동일 로직을 Bash Write 도구로 구현. (D-17, skills/+.agents/ 쌍 커버 컨벤션)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-sg-setup-skill/34-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: .agents/skills/sg-setup/SKILL.md 생성 — Codex/Gemini 플랫폼 스킬 (per D-01~D-16, SKILL-01, SKILL-02)</name>
  <files>.agents/skills/sg-setup/SKILL.md</files>

  <read_first>
    - `.agents/skills/sg-execute/SKILL.md` — Platform Constraints 블록 패턴, constraints 섹션 구조
    - `.agents/skills/sg-review/SKILL.md` — AI Read/Write 도구 사용 패턴 (직접 파일 읽기/쓰기)
    - `bin/setup.js` — PKG_ROOT 결정 로직, 충돌 처리(스킵/--force), ANSI 출력, summary 패턴 (D-13~D-16 구현의 참조 기준)
    - `package.json` — `@gyuha/super-gsd` 패키지명 확인 (require.resolve 인수로 사용)
  </read_first>

  <action>
`.agents/skills/sg-setup/SKILL.md`를 아래 내용으로 생성한다 (per D-01~D-16, SKILL-01, SKILL-02).

AI Read/Write 도구를 사용하는 Codex/Gemini 전용 구현이다. Node.js `require.resolve`로 PKG_ROOT를 결정하고, AI Read 도구로 소스 파일을 읽은 뒤 AI Write 도구로 대상 경로에 쓴다 (per D-13~D-16).

파일 내용:

```
---
name: sg-setup
description: super-gsd 훅·스킬 파일을 현재 프로젝트에 복사 — Codex/Gemini 인세션 인스톨러
argument-hint: "[--gemini] [--force] - --gemini: .gemini/settings.json 추가 복사, --force: 기존 파일 덮어쓰기"
---

<objective>
npm 패키지 경로에서 super-gsd 운영에 필요한 파일(hooks/, .agents/skills/, .codex/hooks.json)을 현재 프로젝트 루트에 복사한다. Codex/Gemini 세션 내부에서 AI Read/Write 도구로 직접 파일을 복사하여 외부 CLI 없이 설치를 완료한다.
</objective>

<constraints>
## Platform Constraints (Codex / Gemini CLI)
- Node.js CLI 없음: npx/node 직접 실행 불가. AI Read/Write 도구로 파일을 복사한다.
- SubagentStop 미지원: 완료 후 $sg-execute를 수동 실행하세요.
- AskUserQuestion 미지원: 선택지를 텍스트로 출력하고 자유 입력을 받음.
</constraints>

<execution_context>
Self-contained. require.resolve로 @gyuha/super-gsd 패키지 루트를 결정한 뒤 AI Read/Write 도구로 파일을 현재 프로젝트에 복사한다.
</execution_context>

<process>
0. **인수 파싱.**

   ARGUMENTS를 공백으로 분리하여 플래그를 추출한다:
   - `--force` 포함 여부 → FORCE=true (기본 false)
   - `--gemini` 포함 여부 → GEMINI=true (기본 false)

1. **PKG_ROOT 결정 (per D-01~D-04).**

   아래 Node.js 코드를 Bash로 실행하여 PKG_ROOT를 결정한다 (D-02: require.resolve 실패 시 npm root -g fallback):
   ```bash
   PKG_ROOT=$(node -e "
     try {
       const pkg = require.resolve('@gyuha/super-gsd/package.json');
       const path = require('path');
       process.stdout.write(path.dirname(pkg));
     } catch(e) { process.exit(1); }
   " 2>/dev/null)
   if [ -z "$PKG_ROOT" ]; then
     PKG_ROOT=$(npm root -g 2>/dev/null)/@gyuha/super-gsd
     [ ! -d "$PKG_ROOT" ] && PKG_ROOT=""
   fi
   if [ -z "$PKG_ROOT" ]; then
     echo "[sg-setup] Cannot locate @gyuha/super-gsd package. Install it first:"
     echo "  npx @gyuha/super-gsd install"
     exit 1
   fi
   echo "[sg-setup] PKG_ROOT: $PKG_ROOT"
   ```

2. **플랫폼 감지 (per D-05~D-08).**

   --gemini 인수가 없을 때 환경변수 + 디렉토리로 자동 감지한다 (D-06):
   ```bash
   if [ "$GEMINI" != "true" ]; then
     if [ -n "$GEMINI_PROJECT_DIR" ] || [ -n "$GEMINI_API_KEY" ]; then
       GEMINI=true
       echo "[sg-setup] Gemini 환경 감지됨. .gemini/settings.json 도 복사합니다."
     elif [ -n "$CODEX_SHELL" ] || [ -n "$CODEX" ] || [ -d ".codex" ]; then
       echo "[sg-setup] Platform detected: codex (use --gemini to include Gemini settings)"
     else
       echo "[sg-setup] 플랫폼 감지 불가 — 기본 파일 세트(hooks, .agents, .codex)만 복사합니다."
     fi
   fi
   ```

3. **복사 대상 목록 구성.**

   항상 복사하는 파일 (Codex 기본 세트):
   - `hooks/stop_hook.cjs`
   - `hooks/rule_runner.cjs`
   - `hooks/transcript_matcher.cjs`
   - `hooks/lessons_ranker.cjs`
   - `hooks/hooks.json`
   - `.agents/skills/sg-execute/SKILL.md`
   - `.agents/skills/sg-plan/SKILL.md`
   - `.agents/skills/sg-retro/SKILL.md`
   - `.agents/skills/sg-review/SKILL.md`
   - `.agents/skills/sg-ship/SKILL.md`
   - `.agents/skills/sg-start/SKILL.md`
   - `.agents/skills/sg-status/SKILL.md`
   - `.codex/hooks.json`

   GEMINI=true 일 때 추가:
   - `.gemini/settings.json`

4. **파일 복사 실행 (per D-09~D-16, AI Read/Write 도구 사용).**

   각 복사 대상에 대해:

   a. 소스 경로: `$PKG_ROOT/<relative-path>`
   b. 대상 경로: `<cwd>/<relative-path>`

   **충돌 처리 (per D-09~D-12):**
   - 대상 파일이 이미 존재하고 FORCE=false → 스킵 + 경고 출력, skipped++ (per D-09~D-10)
   - 대상 파일이 이미 존재하고 FORCE=true → 덮어쓰기 실행, copied++ (per D-11)
   - 대상 파일 미존재 → 복사 실행, copied++ (per D-09)

   **AI 도구로 파일 복사 (per D-13~D-16):**
   - Read 도구로 `$PKG_ROOT/<relative-path>` 읽기
   - Write 도구로 `<cwd>/<relative-path>` 쓰기
   - 부모 디렉토리가 없으면 Write 도구가 자동으로 생성하므로 별도 mkdir 불필요

   각 파일 처리 후 결과 출력:
   - 복사 성공: `✓ <relative-path>`
   - 스킵: `⚠ <relative-path> already exists — skipping (use --force to overwrite)`
   - 오류: `✗ <relative-path>: <error message>`

5. **Summary 출력 (per D-13~D-16).**

   모든 파일 처리 완료 후:
   ```
   Installation complete.
     Copied:  <copied> files
     Skipped: <skipped> files (already exist)
   ```
   errors > 0이면 추가로:
   ```
     Errors:  <errors> (see above)
   ```

   완료 메시지:
   ```
   [sg-setup] 설치 완료. 다음 단계: $sg-execute 를 실행하여 현재 phase를 진행하세요.
   ```
</process>

<success_criteria>
1. @gyuha/super-gsd 패키지가 없으면 명확한 오류 메시지와 설치 방법을 안내하고 종료한다.
2. --gemini 플래그 또는 환경변수(GEMINI_PROJECT_DIR, GEMINI_API_KEY) 감지 시 .gemini/settings.json도 복사된다.
3. 모든 기본 파일(hooks/ 4개 .cjs + hooks.json, .agents/skills/ 7개 SKILL.md, .codex/hooks.json)이 AI Read/Write 도구로 복사된다.
4. 대상 파일 존재 + --force 없음 → 스킵 + 경고. --force 있음 → 덮어쓰기.
5. 완료 후 Copied/Skipped summary와 다음 단계 안내가 출력된다.
6. 파일 복사 단계에서 bash cp 또는 node fs.copyFileSync를 사용하지 않고 AI Read/Write 도구만 사용한다 (PKG_ROOT 결정 단계의 node -e 사용은 허용됨).
</success_criteria>
```
  </action>

  <verify>
    <automated>
      test -f /Users/gyuha/workspace/super-gsd/.agents/skills/sg-setup/SKILL.md && echo "FILE EXISTS OK" || echo "FILE MISSING"
    </automated>
    <automated>
      grep -c "name: sg-setup" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-setup/SKILL.md && echo "name OK"
    </automated>
    <automated>
      grep -c "require.resolve" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-setup/SKILL.md && echo "PKG_ROOT logic OK"
    </automated>
    <automated>
      grep -c "Platform Constraints" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-setup/SKILL.md && echo "constraints block OK"
    </automated>
    <automated>
      grep -c "GEMINI_PROJECT_DIR" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-setup/SKILL.md && echo "gemini detection OK"
    </automated>
    <automated>
      grep -c "already exists" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-setup/SKILL.md && echo "skip logic OK"
    </automated>
    <automated>
      grep -c "success_criteria" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-setup/SKILL.md && echo "success_criteria OK"
    </automated>
  </verify>

  <acceptance_criteria>
    - `.agents/skills/sg-setup/SKILL.md`가 존재한다
    - frontmatter에 `name: sg-setup`이 있다
    - `<constraints>` 블록에 `Platform Constraints (Codex / Gemini CLI)` 섹션이 있다
    - `require.resolve('@gyuha/super-gsd/package.json')`으로 PKG_ROOT를 결정하는 로직이 있다 (per D-01~D-04)
    - GEMINI_PROJECT_DIR 환경변수 자동 감지 로직이 있다 (per D-05~D-08)
    - 대상 파일 존재 시 스킵+경고, --force 시 덮어쓰기 로직이 있다 (per D-09~D-12)
    - AI Read/Write 도구로 파일 복사하는 지침이 있다 (per D-13~D-16)
    - 복사 완료 후 Copied/Skipped summary 출력 지침이 있다
    - `<success_criteria>` 블록이 있다
  </acceptance_criteria>

  <done>.agents/skills/sg-setup/SKILL.md가 Codex/Gemini 플랫폼에서 $sg-setup 명령으로 호출 가능하고, npm 패키지 경로에서 AI 도구로 모든 필수 파일을 복사하는 완전한 스킬이다</done>
</task>

<task type="auto">
  <name>Task 2: skills/sg-setup/SKILL.md 생성 — Claude Code 플랫폼 스킬 (per D-17, D-18, SKILL-01, SKILL-02)</name>
  <files>skills/sg-setup/SKILL.md</files>

  <read_first>
    - `skills/sg-plan/SKILL.md` — skills/ SKILL.md 구조 패턴 (objective, execution_context, process, success_criteria 블록)
    - `bin/setup.js` — PKG_ROOT 결정 로직, 충돌 처리, summary 출력 패턴 (Claude Code는 Bash 도구로 node 직접 실행 가능)
    - `.claude-plugin/plugin.json` — `"skills": "./skills/"` 디렉토리 스캔 방식 확인 (D-18: plugin.json 수정 불필요)
  </read_first>

  <action>
`skills/sg-setup/SKILL.md`를 아래 내용으로 생성한다 (per D-17, D-18, SKILL-01, SKILL-02).

Claude Code 플랫폼 전용 구현이다. Bash 도구로 `node`를 직접 실행할 수 있으므로 `bin/setup.js`의 로직을 인라인으로 재현한다. plugin.json은 `"skills": "./skills/"` 디렉토리 스캔 방식이므로 이 파일을 생성하면 `/super-gsd:sg-setup` 명령이 자동 등록된다 (per D-18).

파일 내용:

```
---
name: sg-setup
description: super-gsd 훅·스킬 파일을 현재 프로젝트에 복사 — Claude Code 인세션 인스톨러
argument-hint: "[--gemini] [--force] - --gemini: .gemini/settings.json 추가 복사, --force: 기존 파일 덮어쓰기"
---

<objective>
npm 패키지 경로에서 super-gsd 운영에 필요한 파일(hooks/, .agents/skills/, .codex/hooks.json)을 현재 프로젝트 루트에 복사한다. Claude Code 세션 내부에서 Bash 도구로 node를 직접 실행하여 설치를 완료한다.
</objective>

<execution_context>
Self-contained. require.resolve로 @gyuha/super-gsd 패키지 루트를 결정한 뒤 Bash 도구로 파일을 현재 프로젝트에 복사한다.
</execution_context>

<process>
0. **인수 파싱.**

   ARGUMENTS를 공백으로 분리하여 플래그를 추출한다:
   - `--force` 포함 여부 → FORCE=true (기본 false)
   - `--gemini` 포함 여부 → GEMINI=true (기본 false)

1. **PKG_ROOT 결정 (per D-01~D-04).**

   Bash 도구로 아래 명령을 실행한다 (D-02: require.resolve 실패 시 npm root -g fallback):
   ```bash
   PKG_ROOT=$(node -e "
     try {
       const pkg = require.resolve('@gyuha/super-gsd/package.json');
       const path = require('path');
       process.stdout.write(path.dirname(pkg));
     } catch(e) { process.exit(1); }
   " 2>/dev/null)
   if [ -z "$PKG_ROOT" ]; then
     PKG_ROOT=$(npm root -g 2>/dev/null)/@gyuha/super-gsd
     [ ! -d "$PKG_ROOT" ] && PKG_ROOT=""
   fi
   if [ -z "$PKG_ROOT" ]; then
     echo "[sg-setup] Cannot locate @gyuha/super-gsd package. Install it first:"
     echo "  npx @gyuha/super-gsd install"
     exit 1
   fi
   echo "[sg-setup] PKG_ROOT: $PKG_ROOT"
   ```

2. **플랫폼 감지 (per D-05~D-08).**

   --gemini 인수가 없을 때 환경변수 + 디렉토리로 자동 감지한다 (D-06):
   ```bash
   if [ "$GEMINI" != "true" ]; then
     if [ -n "$GEMINI_PROJECT_DIR" ] || [ -n "$GEMINI_API_KEY" ]; then
       GEMINI=true
       echo "[sg-setup] Gemini 환경 감지됨. .gemini/settings.json 도 복사합니다."
     elif [ -n "$CODEX_SHELL" ] || [ -n "$CODEX" ] || [ -d ".codex" ]; then
       echo "[sg-setup] Platform detected: codex (use --gemini to include Gemini settings)"
     else
       echo "[sg-setup] 플랫폼 감지 불가 — 기본 파일 세트(hooks, .agents, .codex)만 복사합니다."
     fi
   fi
   ```

3. **복사 대상 목록 구성.**

   항상 복사하는 파일:
   - `hooks/stop_hook.cjs`
   - `hooks/rule_runner.cjs`
   - `hooks/transcript_matcher.cjs`
   - `hooks/lessons_ranker.cjs`
   - `hooks/hooks.json`
   - `.agents/skills/sg-execute/SKILL.md`
   - `.agents/skills/sg-plan/SKILL.md`
   - `.agents/skills/sg-retro/SKILL.md`
   - `.agents/skills/sg-review/SKILL.md`
   - `.agents/skills/sg-ship/SKILL.md`
   - `.agents/skills/sg-start/SKILL.md`
   - `.agents/skills/sg-status/SKILL.md`
   - `.codex/hooks.json`

   GEMINI=true 일 때 추가:
   - `.gemini/settings.json`

4. **파일 복사 실행 (per D-09~D-12, Bash 도구 사용).**

   Bash 도구로 아래 스크립트를 실행한다. FORCE, GEMINI, PKG_ROOT 변수는 앞 단계에서 결정된 값으로 대체하여 실행한다:

   ```bash
   DEST_ROOT=$(pwd)
   COPIED=0
   SKIPPED=0
   ERRORS=0

   copy_file() {
     local REL_PATH="$1"
     local SRC="$PKG_ROOT/$REL_PATH"
     local DEST="$DEST_ROOT/$REL_PATH"

     if [ ! -f "$SRC" ]; then
       echo "✗ $REL_PATH: source not found in $PKG_ROOT"
       ERRORS=$((ERRORS + 1))
       return
     fi

     if [ -f "$DEST" ] && [ "$FORCE" != "true" ]; then
       echo "⚠ $REL_PATH already exists — skipping (use --force to overwrite)"
       SKIPPED=$((SKIPPED + 1))
       return
     fi

     mkdir -p "$(dirname "$DEST")"
     if cp "$SRC" "$DEST"; then
       if [ -f "$DEST" ] && [ "$FORCE" = "true" ]; then
         echo "✓ $REL_PATH (overwritten)"
       else
         echo "✓ $REL_PATH"
       fi
       COPIED=$((COPIED + 1))
     else
       echo "✗ $REL_PATH: copy failed"
       ERRORS=$((ERRORS + 1))
     fi
   }

   copy_file "hooks/stop_hook.cjs"
   copy_file "hooks/rule_runner.cjs"
   copy_file "hooks/transcript_matcher.cjs"
   copy_file "hooks/lessons_ranker.cjs"
   copy_file "hooks/hooks.json"
   copy_file ".agents/skills/sg-execute/SKILL.md"
   copy_file ".agents/skills/sg-plan/SKILL.md"
   copy_file ".agents/skills/sg-retro/SKILL.md"
   copy_file ".agents/skills/sg-review/SKILL.md"
   copy_file ".agents/skills/sg-ship/SKILL.md"
   copy_file ".agents/skills/sg-start/SKILL.md"
   copy_file ".agents/skills/sg-status/SKILL.md"
   copy_file ".codex/hooks.json"

   if [ "$GEMINI" = "true" ]; then
     copy_file ".gemini/settings.json"
   fi

   echo ""
   echo "Installation complete."
   echo "  Copied:  $COPIED files"
   echo "  Skipped: $SKIPPED files (already exist)"
   if [ "$ERRORS" -gt 0 ]; then
     echo "  Errors:  $ERRORS (see above)"
   fi
   ```

5. **완료 안내.**

   ```
   [sg-setup] 설치 완료. 다음 단계: /super-gsd:sg-execute 를 실행하여 현재 phase를 진행하세요.
   ```
</process>

<success_criteria>
1. @gyuha/super-gsd 패키지가 없으면 명확한 오류 메시지와 설치 방법을 안내하고 종료한다.
2. --gemini 플래그 또는 환경변수(GEMINI_PROJECT_DIR, GEMINI_API_KEY) 감지 시 .gemini/settings.json도 복사된다.
3. 모든 기본 파일(hooks/ 5개, .agents/skills/ 7개 SKILL.md, .codex/hooks.json)이 Bash 도구로 복사된다.
4. 대상 파일 존재 + --force 없음 → 스킵 + 경고. --force 있음 → 덮어쓰기.
5. 완료 후 Copied/Skipped summary와 다음 단계 안내가 출력된다.
6. plugin.json 수정 없이 skills/ 디렉토리 스캔으로 /super-gsd:sg-setup 명령이 자동 등록된다 (per D-18).
</success_criteria>
```
  </action>

  <verify>
    <automated>
      test -f /Users/gyuha/workspace/super-gsd/skills/sg-setup/SKILL.md && echo "FILE EXISTS OK" || echo "FILE MISSING"
    </automated>
    <automated>
      grep -c "name: sg-setup" /Users/gyuha/workspace/super-gsd/skills/sg-setup/SKILL.md && echo "name OK"
    </automated>
    <automated>
      grep -c "require.resolve" /Users/gyuha/workspace/super-gsd/skills/sg-setup/SKILL.md && echo "PKG_ROOT logic OK"
    </automated>
    <automated>
      grep -c "copy_file" /Users/gyuha/workspace/super-gsd/skills/sg-setup/SKILL.md && echo "copy function OK"
    </automated>
    <automated>
      grep -c "GEMINI_PROJECT_DIR" /Users/gyuha/workspace/super-gsd/skills/sg-setup/SKILL.md && echo "gemini detection OK"
    </automated>
    <automated>
      grep -c "already exists" /Users/gyuha/workspace/super-gsd/skills/sg-setup/SKILL.md && echo "skip logic OK"
    </automated>
    <automated>
      grep -c "success_criteria" /Users/gyuha/workspace/super-gsd/skills/sg-setup/SKILL.md && echo "success_criteria OK"
    </automated>
    <automated>
      grep '"skills"' /Users/gyuha/workspace/super-gsd/.claude-plugin/plugin.json | grep -q './skills/' && echo "D-18 OK: plugin.json uses ./skills/ dir scan — no explicit entry needed" || echo "D-18 WARN: check plugin.json skills field"
    </automated>
    <automated>
      node -e "
        const fs = require('fs');
        const agents = fs.readFileSync('/Users/gyuha/workspace/super-gsd/.agents/skills/sg-setup/SKILL.md','utf8');
        const skills = fs.readFileSync('/Users/gyuha/workspace/super-gsd/skills/sg-setup/SKILL.md','utf8');
        const assert = require('assert');
        assert(agents.includes('name: sg-setup'), 'agents name');
        assert(skills.includes('name: sg-setup'), 'skills name');
        assert(agents.includes('require.resolve'), 'agents PKG_ROOT');
        assert(skills.includes('require.resolve'), 'skills PKG_ROOT');
        assert(agents.includes('Platform Constraints'), 'agents constraints');
        assert(agents.includes('GEMINI_PROJECT_DIR'), 'agents gemini');
        assert(skills.includes('GEMINI_PROJECT_DIR'), 'skills gemini');
        assert(agents.includes('already exists'), 'agents skip');
        assert(skills.includes('already exists'), 'skills skip');
        console.log('Both SKILL.md files validated OK');
      "
    </automated>
  </verify>

  <acceptance_criteria>
    - `skills/sg-setup/SKILL.md`가 존재한다
    - frontmatter에 `name: sg-setup`이 있다
    - `require.resolve('@gyuha/super-gsd/package.json')`으로 PKG_ROOT를 결정하는 Bash 블록이 있다 (per D-01~D-04)
    - GEMINI_PROJECT_DIR 환경변수 자동 감지 로직이 있다 (per D-05~D-08)
    - `copy_file` 함수 또는 동등한 파일별 복사 로직과 충돌 처리(스킵/덮어쓰기)가 있다 (per D-09~D-12)
    - 복사 완료 후 Copied/Skipped summary 출력이 있다 (per D-13~D-16)
    - `<success_criteria>` 블록이 있다
    - `.agents/skills/sg-setup/SKILL.md`와 `skills/sg-setup/SKILL.md`가 모두 존재하여 쌍 커버 컨벤션을 충족한다 (D-17, CLAUDE.md 컨벤션)
  </acceptance_criteria>

  <done>skills/sg-setup/SKILL.md가 Claude Code 플랫폼에서 /super-gsd:sg-setup 명령으로 자동 등록되고, .agents/와 skills/ 쌍이 모두 존재하여 CLAUDE.md 쌍 커버 컨벤션을 충족한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → AI 에이전트 세션 | require.resolve로 @gyuha/super-gsd 패키지 위치를 결정 |
| PKG_ROOT (npm 캐시) → 대상 프로젝트 CWD | AI Read/Write 또는 cp로 파일 복사 수행 |
| $ARGUMENTS (사용자 입력) → 플래그 파싱 | --force, --gemini boolean 플래그만 허용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | 파일 복사 대상 경로 결정 | mitigate | 복사 대상 경로가 모두 코드에 하드코딩된 상대 경로. 사용자 입력으로 경로를 구성하지 않음. --gemini/--force는 boolean이며 경로에 영향을 주지 않음 |
| T-34-02 | Tampering | 기존 파일 덮어쓰기 | mitigate | 기본 동작은 스킵+경고 (per D-09~D-10). 덮어쓰기는 명시적 --force 플래그 시에만 발생 (per D-11) |
| T-34-03 | Elevation of Privilege | PKG_ROOT 결정 — require.resolve 결과 신뢰 | accept | npm 레지스트리 패키지 서명 검증은 npm 클라이언트 책임. @gyuha/ scoped 패키지로 typosquatting 위험 감소. PKG_ROOT는 읽기 전용 소스로만 사용 |
| T-34-04 | Information Disclosure | 환경변수 감지 출력 | accept | GEMINI_PROJECT_DIR 감지 결과를 stdout에 출력하나 변수 값 자체는 출력하지 않음. 경로 정보 유출 없음 |
| T-34-SC | Tampering | npm/node 실행 (SKILL.md는 npm install 없음) | accept | SKILL.md는 외부 npm 패키지를 설치하지 않음. require.resolve는 이미 설치된 패키지만 조회. slopcheck 불필요 |
</threat_model>

<verification>
## Phase 34 전체 검증

1. `test -f .agents/skills/sg-setup/SKILL.md && echo OK` → OK 출력
2. `test -f skills/sg-setup/SKILL.md && echo OK` → OK 출력
3. `.agents/skills/sg-setup/SKILL.md` frontmatter에 `name: sg-setup` 확인
4. `skills/sg-setup/SKILL.md` frontmatter에 `name: sg-setup` 확인
5. 두 파일 모두 `require.resolve('@gyuha/super-gsd/package.json')` 포함 확인
6. 두 파일 모두 `GEMINI_PROJECT_DIR` 환경변수 감지 로직 포함 확인
7. 두 파일 모두 `already exists` 충돌 처리 로직 포함 확인
8. `.agents/` 파일에 `Platform Constraints (Codex / Gemini CLI)` 블록 존재 확인
</verification>

<success_criteria>
- `.agents/skills/sg-setup/SKILL.md`가 존재하고 Codex/Gemini 세션에서 `$sg-setup` 명령으로 호출 가능하다 (SKILL-02)
- `skills/sg-setup/SKILL.md`가 존재하고 Claude Code에서 `/super-gsd:sg-setup` 명령으로 자동 등록된다 (SKILL-02, D-17, D-18)
- `$sg-setup` 실행 시 `require.resolve('@gyuha/super-gsd/package.json')`으로 PKG_ROOT를 결정하고 hooks/ 5개 파일, .agents/skills/ 7개 SKILL.md, .codex/hooks.json을 복사한다 (SKILL-01, D-01~D-04)
- --gemini 플래그 또는 GEMINI_PROJECT_DIR 환경변수 감지 시 .gemini/settings.json도 추가 복사된다 (SKILL-01, D-05~D-08)
- 기존 파일 존재 시 기본 동작은 스킵+경고, --force 시 덮어쓰기 (SKILL-01, D-09~D-12)
- 완료 후 복사된 파일 목록과 Copied/Skipped summary가 출력된다 (SKILL-01, D-13~D-16)
- skills/+.agents/ 쌍 커버 컨벤션을 충족한다 (CLAUDE.md 컨벤션, D-17)
</success_criteria>

<output>
Create `.planning/phases/34-sg-setup-skill/34-01-SUMMARY.md` when done
</output>
