# Lessons: Phase 46 (2026-05-31)

## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: 2026-05-31T15:35:31Z_
_Intent: capture technical decisions, unexpected outcomes, recurring techniques, and verification failures from this phase._

### Decisions
- D-01~D-09 locked: presence-only 마커 판정, on/off/무인자 의미론, idempotent, mkdir -p 가드, HANDOFF 행 미추가, pairwise 미러, AskUserQuestion 불필요 (46-CONTEXT.md)
- 마커 내용은 사람-가독 메타데이터(설명+ISO 타임스탬프)로 한정, 기계 감지는 존재-여부만 — 소비는 Phase 47 (D-04, 46-CONTEXT.md)
### Surprises
- 플랜 <verification>이 grep -L 'HANDOFF' / grep -rL 'grep -P'로 "리터럴 미포함"을 요구해, 의도를 설명하는 산문이 자기 검증을 실패시킴 (46-01-SUMMARY.md)
- 같은 플랜 안에서 action("constraints에 AskUserQuestion 명시")과 acceptance_criteria("must NOT contain AskUserQuestion")가 정면 충돌 (46-01-PLAN.md)
### Patterns
- sg-health 골격 + sg-status 미러 패턴 재사용으로 신규 토글 스킬을 일관되게 생성 (skills/sg-health, .agents/skills/sg-status)
- pairwise 미러를 같은 커밋에 묶는 규율 (D-08, commit bb2592f)
### Mistakes
- 초안에 리터럴 HANDOFF·grep -P 문자열 포함 → 파일 자체 검증 실패, 재구성 필요 (46-01-SUMMARY.md)
- 포터빌리티 노트가 존재하지 않는 case 블록을 기술 → 코드 리뷰 Minor로 적발, dbfeb25로 수정

### Action Items
| priority | item | next step |
|----------|------|-----------|
| 🔴 P1 | 리터럴 grep -L/grep -rL "미포함" 수용 기준이 설명 산문과 충돌 | sg-plan/plan-checker에 의미-기반 검증 권고 가드 추가 검토 |
| P2 | 플랜 내부 action↔acceptance_criteria 모순 | plan-lint 도입 시 모순 탐지 규칙 포함 (v2.11 이월 후보) |

## Lens: Start/Stop/Continue
_Captured: 2026-05-31T15:35:31Z_
_Intent: surface behavior changes — what to start, stop, or continue doing next phase._

### Start
- SKILL.md acceptance criteria를 리터럴 grep -L 'TOKEN'이 아닌 의미 기반 체크로 작성 (설명 산문에 토큰이 정당하게 등장하는 경우 충돌)
- 플랜 작성 시 action vs acceptance_criteria 모순 점검(plan-lint) — v2.11 이월 후보와 연결
### Stop
- 정당한 산문에 등장할 수 있는 토큰에 대해 "must NOT contain TOKEN" 리터럴 grep 수용 기준을 쓰는 것
### Continue
- bash 분기를 임시 디렉토리에서 실증 스모크 테스트한 뒤 done 선언 (SC1~SC4 검증)
- sg-health/sg-status 패턴 재사용 + pairwise 같은-커밋 규율
- 코드 리뷰 Minor도 즉시 수정 후 pairwise 양쪽 반영 (dbfeb25)

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P3 | 미러 분량/구조 비대칭 점검 | 향후 sg-* 미러 생성 시 pairwise diff 체크 |

