---
phase: "06-sg-health"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/transcript_matcher.py
autonomous: true
requirements:
  - HEALTH-06

must_haves:
  truths:
    - "D-06: hooks/transcript_matcher.py HOOKIFY_SIGNALS 리스트에서 bare 'hookify' 문자열만 제거하고 나머지 3개 패턴은 유지한다"
    - "HOOKIFY_SIGNALS에 'Retrospective complete', 'hooks generated', 'patterns extracted' 3개 패턴이 유지된다"
    - "detect_signal 함수가 여전히 hookify-complete를 정상 감지한다"
  artifacts:
    - path: "hooks/transcript_matcher.py"
      provides: "패치된 HOOKIFY_SIGNALS 리스트"
      contains: "Retrospective complete"
  key_links:
    - from: "hooks/transcript_matcher.py"
      to: "HOOKIFY_SIGNALS list"
      via: "direct list item removal"
      pattern: "HOOKIFY_SIGNALS = \\["
---

<objective>
`hooks/transcript_matcher.py`의 `HOOKIFY_SIGNALS` 리스트에서 bare `'hookify'` 항목만 제거한다. 나머지 3개 패턴(`'Retrospective complete'`, `'hooks generated'`, `'patterns extracted'`)은 그대로 유지한다.

Purpose: sg-health 실행 중 출력 텍스트에 `hookify`가 포함되어 Stop hook이 오발동하는 문제를 방지한다 (HEALTH-06).
Output: hooks/transcript_matcher.py (1줄 수정)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-sg-health/06-CONTEXT.md

<interfaces>
<!-- hooks/transcript_matcher.py 현재 HOOKIFY_SIGNALS 상태 -->
HOOKIFY_SIGNALS = [
    'hookify',              ← 이 줄만 제거 (D-06)
    'Retrospective complete',
    'hooks generated',
    'patterns extracted',
]

<!-- 패치 후 목표 상태 -->
HOOKIFY_SIGNALS = [
    'Retrospective complete',
    'hooks generated',
    'patterns extracted',
]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HOOKIFY_SIGNALS에서 'hookify' 항목 제거</name>
  <files>hooks/transcript_matcher.py</files>
  <read_first>
    - hooks/transcript_matcher.py — 현재 HOOKIFY_SIGNALS 리스트 전체와 detect_signal 함수 로직 확인
  </read_first>
  <action>
    hooks/transcript_matcher.py에서 HOOKIFY_SIGNALS 리스트의 `'hookify',` 줄 하나만 삭제한다 (per D-06).

    변경 전:
    HOOKIFY_SIGNALS = [
        'hookify',
        'Retrospective complete',
        'hooks generated',
        'patterns extracted',
    ]

    변경 후:
    HOOKIFY_SIGNALS = [
        'Retrospective complete',
        'hooks generated',
        'patterns extracted',
    ]

    이 외의 코드, 주석, 함수 시그니처, 변수명, 빈 줄은 일절 수정하지 않는다.
    GSD_PLAN_SIGNALS, REVIEW_SIGNALS 리스트는 건드리지 않는다.
  </action>
  <verify>
    <automated>python3 -c "
import sys
sys.path.insert(0, '/Users/gyuha/workspace/super-gsd/hooks')
from transcript_matcher import detect_signal, HOOKIFY_SIGNALS
assert 'hookify' not in HOOKIFY_SIGNALS, 'bare hookify still in list'
assert 'Retrospective complete' in HOOKIFY_SIGNALS, 'Retrospective complete missing'
assert 'hooks generated' in HOOKIFY_SIGNALS, 'hooks generated missing'
assert 'patterns extracted' in HOOKIFY_SIGNALS, 'patterns extracted missing'
print('OK: HOOKIFY_SIGNALS =', HOOKIFY_SIGNALS)
"</automated>
  </verify>
  <acceptance_criteria>
    - `'hookify'`가 HOOKIFY_SIGNALS 리스트에 없다 (`grep -c "'hookify'" hooks/transcript_matcher.py` → 0, 주석 제외 시)
    - `'Retrospective complete'`가 HOOKIFY_SIGNALS에 있다
    - `'hooks generated'`가 HOOKIFY_SIGNALS에 있다
    - `'patterns extracted'`가 HOOKIFY_SIGNALS에 있다
    - HOOKIFY_SIGNALS 길이가 3이다
    - detect_signal 함수 시그니처, GSD_PLAN_SIGNALS, REVIEW_SIGNALS는 변경 없다
    - python3 import 시 SyntaxError가 없다
  </acceptance_criteria>
  <done>HOOKIFY_SIGNALS에 'hookify' 항목이 없고 나머지 3개 패턴이 유지되며 파일이 python3로 정상 import된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript_matcher.py → 훅 시스템 | detect_signal 반환값이 stop_hook.py에서 다음 단계 안내 여부를 결정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | HOOKIFY_SIGNALS 리스트 | mitigate | 패치 후 python3 import 검증으로 목록 내용 및 길이 확인 |
| T-06-04 | Denial of Service | hookify-complete 신호 미감지 | mitigate | 나머지 3개 패턴 유지 확인 — 'Retrospective complete'가 실제 hookify 완료 신호 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜에 패키지 설치 없음 |
</threat_model>

<verification>
1. `grep -v "^#" hooks/transcript_matcher.py | grep -c "'hookify'"` → 0
2. `grep -c "'Retrospective complete'" hooks/transcript_matcher.py` → 1
3. `python3 -c "from hooks.transcript_matcher import HOOKIFY_SIGNALS; assert len(HOOKIFY_SIGNALS) == 3"` → 성공
</verification>

<success_criteria>
- HOOKIFY_SIGNALS에서 `'hookify'` 항목이 제거되었다
- 나머지 3개 패턴이 모두 유지된다
- transcript_matcher.py가 SyntaxError 없이 import된다
- sg-health 출력 중 `hookify` 단어가 포함되어도 Stop hook이 오발동하지 않는다
</success_criteria>

<output>
완료 시 `.planning/phases/06-sg-health/06-02-SUMMARY.md`를 생성한다
</output>
