# Lessons: Phase 44 (2026-05-30)

## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: 2026-05-31T01:38:00Z_

### Decisions

- **DOC-01 범위 확장 (D-02): README sg-retro 행도 함께 갱신** (`.planning/phases/44-documentation-sync/44-CONTEXT.md:38`). SC#1은 sg-learn 행만 명시했지만, 현재 sg-retro 행 description("6 lenses ... Sailboat, Five Whys, and more")이 Phase 42 D-04에 의해 dropped된 lens를 가리키므로 milestone-level stale 정합 필요. scope creep이 아닌 milestone closure 결정.
- **3-plan 분할 (D-03/D-04/D-05): wave:1 parallel + non-overlapping files**. ① README 쌍, ② SKILL.md 쌍 frontmatter, ③ TEAM.md 신규 섹션 — 3 작업의 인지·검증 모델이 다르므로 단일 plan 내에서 섞으면 Phase 43에서 발견된 "보존 vs 변경 acceptance 충돌" class drift 가능성 증가. 3-way Task() dispatch는 sg-parallel-execute 상한 3에 정확히 부합.
- **D-12 human override 적용**: skills/sg-retro/SKILL.md + .agents/skills/sg-retro/SKILL.md frontmatter description에 --pick 명시 추가. Phase 43에서 --pick 기능을 ship했음에도 frontmatter description에는 미반영 상태였음. user가 직접 "추가" 선택.
- **D-17 옵션 (b) 채택**: .agents/<constraints> + argument-hint + description 모두 새 동작 반영하도록 적극 갱신. Phase 42 retro에서 도출한 "preserve된 prose 블록이 묘사하는 구조가 변경되면 stale 누적"의 직접 적용.
- **Phase 43 retro P1 #2 closure → quick task `3d373d8`**: sg-parallel-execute → sg-review BASE==HEAD 자동화 도입. skills/sg-review/SKILL.md Step 1.5 + .agents/skills/sg-review/SKILL.md 대칭 변경. milestone 종료 전에 P1 deferral chain 끊음.

### Surprises

- **Plan-spec 자체에 산술 모순이 인코딩됨 (NEW sub-class, code review에서 발견)**: 44-01-PLAN.md의 token spec이 README.md:37에 들어갈 문구로 `"smart default runs three lenses (ssc, dspm) without prompting"`을 하드코딩 — "three lenses"라고 주장하면서 (ssc, dspm) 두 개만 열거. executor는 plan을 충실히 transcribe했고, plan acceptance도 literal substring 존재만 확인하지 의미적 정합은 검증 안 함. **한국어 번역자 path만 의미 정합을 가졌고 자동으로 "세 가지 렌즈 중 두 개"로 수정**. 영문 path는 의미 검증 없이 stale을 그대로 ship. 우연한 행운으로 Korean half가 documentation을 구함.
- **Phase 42 → 43 → 44의 prose-drift class 진화 패턴**: Phase 42 "preserve list vs grep acceptance 충돌"(3건) → Phase 43 "preserve 블록이 묘사하는 구조 자체가 변경"(3건, severity ↓) → Phase 44 "plan-spec 토큰 자체의 내부 산술 모순"(1건, Critical) — class 자체는 한 단계씩 더 정밀해지지만 매 phase 새 sub-class 발견. Phase template enhancement가 lesson을 한 phase씩 추격 중이지만 따라잡지 못함.
- **Plan-spec 신규 deviation 카테고리 surface**: 3개 plan agents 모두 "out-of-scope unchanged" acceptance가 wave:1 parallel 환경에서 false-fail한다고 동일하게 보고. plan acceptance가 단일 plan 실행을 전제하지만 sg-parallel-execute는 3-way concurrent dispatch. 본 phase에서 처음 명시적으로 captured.
- **Plugin cache vs working tree drift**: quick task `3d373d8`로 sg-review/SKILL.md에 auto-commit gate를 추가했으나 plugin cache v0.0.42는 reload 전이라 sg-review skill 호출 시 구버전 로드 → 또 manual commit 단계 거침. quick task가 working tree에는 적용됐지만 release(version bump + npm publish)까지 가야 실효. 같은 session 내 self-modification의 한계.

### Patterns

- **Korean translator path가 의미 검증 layer 역할**: 영문 source는 plan literal에 충실, Korean mirror는 의미 정합 우선. EN→KO 비대칭이 의도되지 않았지만 결과적으로 1-Critical Korean side correct + English side wrong. Diff stat 동일(4 lines each)이라 surface된 후에만 발견 가능.
- **3-phase 연속 parallel dispatch 안정성 확인**: Phase 42(2-way) → Phase 43(2-way) → Phase 44(3-way). 모두 wave:1 + non-overlapping files. ~5-10분/agent, conflict 0건. sg-parallel-execute 상한 3에서도 dispatch overhead 미미.
- **SUMMARY § "Plan-spec Deviations" 답습**: Phase 42에서 정립 → Phase 43 → Phase 44 모두 동일 형식. 3개 agent가 같은 deviation을 같은 표현으로 보고하는 일관성 확인. Reviewer가 deviation 의도성·심각도를 즉시 판단 가능.
- **Plan-internal contradiction이 다음 phase에서 항상 surface**: Phase 42 #6 preserve vs multiSelect grep / Phase 43 PICK_MODE grep 충돌 / Phase 44 plan-spec 자체 산술 모순. 매 phase 새 형태로 등장 → plan-lint 도입 시급도 시간 경과에 따라 누적.

### Mistakes

- **Plan 작성 시 token spec의 의미적 산술 정합 미검증** (44-01-PLAN.md:85/208/215). "three lenses (ssc, dspm)"이 plan에 들어간 시점에 plan-time validation이 count word + 따옴표 안 list 길이 일치 검사를 안 함. plan-checker agent의 새 검증 항목 후보.
- **Plugin reload 시점 misalign**: quick task `3d373d8`가 working tree에 적용된 후 같은 session에서 sg-review 호출 시 구버전 로드. 이를 인지하고 manual commit 처리는 했지만, 다음 session에서 plugin이 reload되어야 새 gate 작동. v2.9 ship으로 cache update 필요.
- **STATE.md `Phase:` 필드 sync는 milestone 4회 연속 미적용**: Phase 42·43·44 retro 모두에서 deferred 상태로 P1 또는 P2 list. milestone closure 직전인데도 해소 안 됨. Phase 44 retro에서도 placeholder 유지.

### Action Items

| priority | item | next step |
|----------|------|-----------|
| 🔴 P1 | Plan-lint: token spec에 count word(`two`/`three`/`N`/숫자)가 list `(a, b, c)`를 동반할 때 `count(list) == count_word` 자동 검증. Phase 44에서 발견된 NEW sub-class. | gsd-plan-checker agent에 count-word arithmetic 검사 추가, 또는 `hooks/plan_lint.cjs` 신규 작성. Phase 44 fix(44) 회귀 케이스(`'three lenses (ssc, dspm)'`)를 test fixture로 |
| 🔴 P1 | Plan template에 "out-of-scope unchanged" acceptance를 parallel-aware로 작성: 자기 plan agent의 수정 범위 밖만 검증하도록 명시 | gsd-plan-phase template / sg-plan SKILL.md 보강. Phase 44 3 plans 모두 surface한 동일 deviation을 회귀 케이스로 |
| 🔴 P1 | milestone v2.9 ship (배포) — version bump 0.0.42 → 0.0.43, CHANGELOG, push to main. Plugin cache reload되어 새 sg-retro(smart default) + sg-review(auto-commit gate) + sg-learn 동작이 다음 session부터 적용 | CLAUDE.md "배포" trigger 실행 후 sg-complete |
| 🔴 P1 | STATE.md `Phase:` 자동 sync (4 phase 연속 deferred 누적) | `skills/sg-plan/SKILL.md` 종료 시점에 ROADMAP active phase로 sync, 또는 hooks/state_sync.cjs 신설. v2.10 milestone 도입 |

## Lens: Start/Stop/Continue
_Captured: 2026-05-31T01:38:30Z_

### Start

- **Plan-lint count-word arithmetic check 도입** (NEW): plan token spec이 count word + list를 동반할 때 list 길이가 count word와 일치하는지 plan-time validation. Phase 44 Critical bug의 회귀 차단. 다른 sub-class들(Phase 42/43)과 함께 plan-lint를 progressively 강화.
- **Plan acceptance를 parallel-aware로 작성**: "out-of-scope unchanged" 같은 cross-plan 검증을 자기 plan agent 범위로 제한. 또는 plan별 "이 plan이 수정한 파일" enumerate를 frontmatter `files_modified`에서 동적으로 가져와 검증.
- **Plugin cache reload 시점을 ship workflow에 명시**: skills/ 변경 → 같은 session sg-* 호출 시 구버전 로드를 잠재적 deviation으로 surface. session reload 또는 version bump가 필요한 변경 type을 ship checklist에 포함.

### Stop

- **Plan token spec을 의미 검증 없이 literal로 transcribe하기**: Phase 44 Critical은 plan-spec 자체에 인코딩됨. executor가 plan을 신뢰하면 산술 모순도 그대로 ship. plan-time semantic validation 강제 필요.
- **STATE.md `Phase:` deferred 누적시키기**: 4 milestone phase 연속 deferred → 매번 same recurring issue. action item closure 자체가 stuck. P1을 다음 milestone 진입 전 closure 의무로 격상.
- **Single Korean translator path에 의미 검증 의존하기**: 의도되지 않은 영문/한글 비대칭이 Phase 44 Critical 발견을 가능케 했지만, future phase는 양쪽 동일 source(예: english-only)로 작성될 수도 있어 이 안전망 사라질 위험.

### Continue

- **wave:1 + non-overlapping files + parallel Task() dispatch**: Phase 42·43·44 모두 conflict 0건, ~5-10분/agent. 3-way까지 검증됨. 다른 milestone에서도 default pattern 유지.
- **SUMMARY § "Plan-spec Deviations (Surfaced)"**: 3개 phase 연속 honest reporting. Reviewer가 즉시 의도성 판단 가능. burying 없음. format 일관.
- **Code review를 last-line-of-defense로**: Phase 42 → 43 → 44 모두 review-time에 Critical/Important 발견 (각 1·3·1건). plan-time validation 추격 못한 부분이 review에서 잡힘.
- **Quick task로 deferred P1 closure**: Phase 43 retro P1 #2(sg-review auto-commit)가 Phase 44 진입 전 quick task `3d373d8`로 closure. milestone 종료 직전 deferred chain 끊음.
- **HANDOFF.md append-only audit log**: milestone 전체 4 phase 추적 가능. sg-next routing의 scan-back 패턴 안정.

### Action Items

| priority | item | next step |
|----------|------|-----------|
| 🔴 P1 | 다음 milestone에 plan-lint 도입 (count-word arithmetic + parallel-aware acceptance + preserve-vs-acceptance conflict 3-tier) | v2.10 또는 v3.0 milestone Phase 1 |
| 🔴 P1 | v2.9 ship: version bump 0.0.42 → 0.0.43, CHANGELOG, push | "배포" trigger 실행 후 sg-complete |
| P2 | 다음 milestone roadmap에 "retro action item closure 추적" 섹션 추가 — 이전 milestone P1 deferred 항목을 다음 milestone에서 explicit phase 또는 quick task로 배치 | gsd-new-milestone template 보강 |
| P2 | Plan template에 "Korean/English 의미 정합" 항목 추가 — pairwise 변경 plan에서 두 언어가 동일 source인지, 다른 source인지 명시 | gsd-plan-phase template |
