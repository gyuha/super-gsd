---
phase: 47-tdd-inject-fail-loop
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - skills/sg-review/SKILL.md
  - .agents/skills/sg-review/SKILL.md
autonomous: false
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, REVIEW-04]

must_haves:
  truths:
    - "When .planning/USE-TDD exists, sg-review injects a test-pass/fail verification + explicit PASS/FAIL surfacing directive into the requesting-code-review args blob"
    - "sg-review's notation that requesting-code-review is 'terminal' / 'no steps execute after this point' is corrected — control returns with the review result so a post-review loop step can run"
    - "On FAIL, sg-review reads the retry count from .planning/USE-TDD-RETRY; if count < 2 it asks the user (AskUserQuestion) whether to re-run sg-execute, and on approval writes count+1 plus the FAIL feedback to the file then re-invokes Skill(sg-execute)"
    - "On the second retry (count == 2) sg-review reports limit-exceeded and STOPS without asking; on PASS and on limit-exceeded it deletes .planning/USE-TDD-RETRY"
    - "The .agents/skills/sg-review mirror reflects the same verification + FAIL detection but uses a prose halt ('tests FAILED — re-run $sg-execute to fix') instead of AskUserQuestion auto-recall"
  artifacts:
    - path: "skills/sg-review/SKILL.md"
      provides: "TDD verification injection + terminal-notation correction + post-review failure loop (Step 5) with AskUserQuestion + counter reset"
      contains: "USE-TDD-RETRY"
    - path: ".agents/skills/sg-review/SKILL.md"
      provides: "Mirror: TDD verification + revision-required FAIL detection + prose-halt fallback"
      contains: "USE-TDD"
  key_links:
    - from: "skills/sg-review/SKILL.md post-review loop"
      to: "Skill(skill=\"sg-execute\")"
      via: "AskUserQuestion approval → write count+1+feedback to USE-TDD-RETRY → re-invoke sg-execute"
      pattern: "Skill\\(skill=\"sg-execute\"\\)"
    - from: "skills/sg-review/SKILL.md"
      to: ".planning/USE-TDD-RETRY"
      via: "read count on FAIL, write count+feedback on retry, rm -f on PASS/limit-exceeded"
      pattern: "USE-TDD-RETRY"
---

<objective>
Make sg-review the consumer/controller half of the TDD handshake. When `.planning/USE-TDD` is ON: inject a test PASS/FAIL verification directive into the `superpowers:requesting-code-review` context (judgment delegated to the review subagent — no test-runner detection, D-04). Correct the false "terminal" notation (D-05: requesting-code-review dispatches a Task subagent and RETURNS control). Add a bounded (max 2) post-review failure loop: on FAIL, ask the user to re-run sg-execute; on approval write count+feedback to `.planning/USE-TDD-RETRY` and re-invoke `Skill(sg-execute)` (which Plan 47-01 made retry-aware); at count == 2 report limit-exceeded and stop; delete the retry file on PASS and on limit-exceeded (D-06).

Purpose: REVIEW-01 (verification directive), REVIEW-02 (FAIL → ask → re-invoke), REVIEW-03 (max 2 + stop), REVIEW-04 (.agents prose fallback).
Output: Modified `skills/sg-review/SKILL.md` and its `.agents/skills/sg-review/SKILL.md` mirror, changed together (pairwise sync, D-08).

Note: `autonomous: false` reflects that the SHIPPED skill is interactive (AskUserQuestion in the failure loop is inherent runtime behavior). The editing of the SKILL.md files in this plan is itself fully autonomous.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-tdd-inject-fail-loop/47-CONTEXT.md
@.planning/phases/47-tdd-inject-fail-loop/47-PATTERNS.md
@.planning/phases/47-tdd-inject-fail-loop/47-01-PLAN.md

<interfaces>
<!-- The retry-file contract. Plan 47-02 OWNS writing this file; Plan 47-01 reads it. -->

`.planning/USE-TDD-RETRY` — plain-text, human-readable (NOT JSON), portable-shell-parseable (no grep -P). Carries TWO pieces of state: retry count + last review FAIL feedback. Recommended serialization (implementer discretion, D-01/Discretion): line 1 = integer count, lines 2+ = the FAIL feedback body. Plan 47-01 (sg-execute, shipped in wave 1) reads (a) presence to bypass its Step 7 idempotency short-circuit, and (b) the feedback body to inject a "## Previous Test Failures — Fix First" section. Whatever serialization is chosen here MUST match what Plan 47-01 reads — verify 47-01-SUMMARY.md for the exact convention it implemented, and keep them identical.

`.planning/USE-TDD` — presence-only, EXISTS = TDD mode ON (Phase 46). Read with `[ -f .planning/USE-TDD ]`.

Re-invocation target: `Skill(skill="sg-execute")` — Plan 47-01 made sg-execute detect `.planning/USE-TDD-RETRY` and (a) skip idempotency, (b) inject prior feedback. The handshake: sg-review writes USE-TDD-RETRY → re-invokes sg-execute → sg-execute reads it.

Idempotent delete pattern (skills/sg-use-tdd/SKILL.md:50): `rm -f .planning/USE-TDD-RETRY`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct terminal notation + inject TDD verification in skills/sg-review/SKILL.md</name>
  <files>skills/sg-review/SKILL.md</files>
  <read_first>
    - skills/sg-review/SKILL.md:158 (Step 3.9 comment — falsely calls requesting-code-review a "terminal Skill"; correction site per D-05)
    - skills/sg-review/SKILL.md:176-189 (Step 4 invocation; :178 says "no steps execute after this point" — correction site; :180-189 is the args blob = TDD verification injection target)
    - skills/sg-review/SKILL.md:76-119 (Step 1.5 AskUserQuestion auto-commit gate — form/tone analog for the failure-loop dialog in Task 2)
    - skills/sg-use-tdd/SKILL.md:36 (USE-TDD presence-check pattern), :50 (rm -f idempotent delete)
    - .planning/phases/47-tdd-inject-fail-loop/47-PATTERNS.md (`skills/sg-review/SKILL.md` section — correction + invocation sites)
  </read_first>
  <action>
    Two edits to skills/sg-review/SKILL.md, both prerequisites for the Task 2 loop:

    (1) Terminal-notation correction (D-05): Rewrite the :158 comment ("...is a terminal Skill so the moment immediately before invocation is the last possible point to record...") and the :178 line ("Session control transfers to the skill; no steps execute after this point:") to accurately state that `superpowers:requesting-code-review` dispatches a Task subagent and RETURNS control to sg-review with the review result, so a post-review step runs after it. Keep the HANDOFF.md recording behavior; only fix the wrong "terminal / last point / no steps after" claims.

    (2) TDD verification injection (D-04): When `.planning/USE-TDD` is present, augment the Step 4 args blob (:180-189) with a directive instructing the code-review subagent to verify whether tests pass or fail and to surface an explicit, unambiguous PASS/FAIL signal in its verdict. Judgment is delegated to the subagent — do NOT add any test-runner auto-detection (REVIEW-F1 is Future). When USE-TDD is absent, the args blob stays unchanged (non-invasive). Exact wording is implementer discretion.

    User-facing prose in the user's input language; machine tokens (`USE-TDD`, `superpowers:requesting-code-review`, paths) verbatim English.
  </action>
  <verify>
    <automated>! grep -q "terminal Skill" skills/sg-review/SKILL.md && ! grep -q "no steps execute after this point" skills/sg-review/SKILL.md && grep -qi "pass/fail\|pass or fail" skills/sg-review/SKILL.md && grep -q "USE-TDD" skills/sg-review/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "terminal Skill" skills/sg-review/SKILL.md` returns NOTHING (the false notation at :158 is removed/corrected).
    - `grep -q "no steps execute after this point" skills/sg-review/SKILL.md` returns NOTHING (the :178 notation is corrected).
    - The corrected text acknowledges control returns (grep matches "returns control" or "result" near the former :158/:178 sites).
    - `grep -qi "pass/fail\|pass or fail" skills/sg-review/SKILL.md` succeeds (verification directive present).
    - A `[ -f .planning/USE-TDD ]` presence test gates the verification injection.
    - The existing `Skill(skill="superpowers:requesting-code-review"` invocation line is still present.
    - No test-runner auto-detection introduced (no `jest`/`pytest`/`go test` literals added).
  </acceptance_criteria>
  <done>The false terminal/no-steps-after notation is corrected to reflect control return; USE-TDD-present review carries a PASS/FAIL verification directive delegated to the subagent; marker-absent args blob unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add bounded post-review failure loop (Step 5) to skills/sg-review/SKILL.md</name>
  <files>skills/sg-review/SKILL.md</files>
  <read_first>
    - skills/sg-review/SKILL.md:176-189 (Step 4 — the loop attaches AFTER this; depends on Task 1's correction)
    - skills/sg-review/SKILL.md:76-119 (Step 1.5 AskUserQuestion gate — copy form/tone/branch-on-selection structure)
    - skills/sg-use-tdd/SKILL.md:50 (rm -f idempotent delete)
    - .planning/phases/47-tdd-inject-fail-loop/47-01-PLAN.md + 47-01-SUMMARY.md (the exact USE-TDD-RETRY serialization sg-execute reads — MUST match)
    - .planning/phases/47-tdd-inject-fail-loop/47-PATTERNS.md (post-review loop analog; counter reset)
  </read_first>
  <action>
    Add a new Step 5 (post-review failure loop) that runs AFTER control returns from `superpowers:requesting-code-review`, gated on `.planning/USE-TDD` being present (D-05/D-06). Control flow:
    - On review PASS → `rm -f .planning/USE-TDD-RETRY` (counter reset, D-06) → done.
    - On review FAIL → read the current retry count from `.planning/USE-TDD-RETRY` (default 0 if file absent; line-1-count convention matching Plan 47-01).
      - If count < 2 → AskUserQuestion asking whether to re-run sg-execute to fix the failing tests (copy the form/tone/option-label structure of the Step 1.5 gate at :76-119). On approval → write count+1 on line 1 and the review FAIL feedback as the body to `.planning/USE-TDD-RETRY`, then re-invoke `Skill(skill="sg-execute")`. On decline → halt.
      - If count == 2 → report "retry limit exceeded" (user-language prose), `rm -f .planning/USE-TDD-RETRY` (D-06), and STOP without asking.

    Use portable shell only (no grep -P; for the count use `head -1`/`cut`, never BSD-awk `|` table parsing). Serialization MUST match what Plan 47-01's sg-execute reads — confirm against 47-01-SUMMARY.md. Machine tokens (`USE-TDD-RETRY`, `sg-execute`, `$sg-execute`, paths) verbatim English; question/option prose in user's input language.
  </action>
  <verify>
    <automated>grep -q "USE-TDD-RETRY" skills/sg-review/SKILL.md && grep -qE 'Skill\(skill="sg-execute"' skills/sg-review/SKILL.md && grep -q "AskUserQuestion" skills/sg-review/SKILL.md && grep -q "rm -f .planning/USE-TDD-RETRY" skills/sg-review/SKILL.md && ! grep -q "grep -P" skills/sg-review/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "USE-TDD-RETRY" skills/sg-review/SKILL.md` succeeds (loop reads/writes the retry file).
    - `grep -qE 'Skill\(skill="sg-execute"' skills/sg-review/SKILL.md` succeeds (re-invocation on approval).
    - `grep -q "AskUserQuestion" skills/sg-review/SKILL.md` succeeds (FAIL gate uses the Step 1.5 form).
    - `grep -c "rm -f .planning/USE-TDD-RETRY" skills/sg-review/SKILL.md` >= 1 (counter reset appears; both PASS and limit-exceeded paths must delete).
    - The loop is gated on `[ -f .planning/USE-TDD ]` (TDD-mode-only — non-TDD review unchanged).
    - Text shows a max-2 bound (grep matches a count comparison, e.g. "count < 2" / "count == 2" or equivalent prose).
    - No `grep -P`; no BSD-awk `|` table parse introduced.
  </acceptance_criteria>
  <done>A TDD-mode-gated Step 5 detects FAIL, reads the retry count, asks the user (count<2) then writes count+feedback and re-invokes sg-execute, or reports limit-exceeded and stops (count==2); USE-TDD-RETRY is deleted on PASS and limit-exceeded.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror verification + prose-halt failure fallback into .agents/skills/sg-review/SKILL.md</name>
  <files>.agents/skills/sg-review/SKILL.md</files>
  <read_first>
    - .agents/skills/sg-review/SKILL.md:18-23 (Platform Constraints — AskUserQuestion not supported)
    - .agents/skills/sg-review/SKILL.md:52-97 (existing interactive-gate → suggested-command-and-exit prose fallback analog; comment at :53-55)
    - .agents/skills/sg-review/SKILL.md:146-195 (prose-review Step 4; `## Verdict` block at :185-186 = `approved | approved-with-comments | revision-required`; FAIL signal = `revision-required`; output block :191-195)
    - skills/sg-review/SKILL.md (the primary, as edited in Tasks 1-2 — mirror MUST reflect the same verification + FAIL detection)
    - .planning/phases/47-tdd-inject-fail-loop/47-PATTERNS.md (`.agents/skills/sg-review/SKILL.md` section)
  </read_first>
  <action>
    Reflect Tasks 1-2 into the mirror, adapted to its no-Skill, no-AskUserQuestion model (D-07, REVIEW-04):

    (1) TDD verification (D-04 adapted): when `.planning/USE-TDD` is present, instruct the inline prose review (Step 4, :146-195) to verify test pass/fail and surface an explicit PASS/FAIL signal — here the FAIL signal maps to the existing `revision-required` verdict (:185-186). No test-runner detection.

    (2) Prose-halt failure fallback (D-07): when the verdict is FAIL/`revision-required` AND `.planning/USE-TDD` is present, instead of AskUserQuestion + auto-recall, print a prose halt instructing the user to re-run `$sg-execute` to fix the failing tests (e.g. "tests FAILED — re-run $sg-execute to fix"). Attach this after the verdict output block (:191-195). No automatic re-invocation; consistent with the Platform Constraints block (:18-23) and the existing prose-fallback analog (:52-97). `$sg-execute` verbatim; surrounding prose in user's input language.

    The mirror does NOT manage `.planning/USE-TDD-RETRY` counting (no auto-recall loop exists here) — it only detects FAIL and surfaces the manual re-run guidance.
  </action>
  <verify>
    <automated>grep -q "USE-TDD" .agents/skills/sg-review/SKILL.md && grep -q '\$sg-execute' .agents/skills/sg-review/SKILL.md && grep -qi "pass/fail\|pass or fail\|FAILED" .agents/skills/sg-review/SKILL.md && ! grep -q "AskUserQuestion(" .agents/skills/sg-review/SKILL.md && ! grep -q "grep -P" .agents/skills/sg-review/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "USE-TDD" .agents/skills/sg-review/SKILL.md` succeeds with a `[ -f .planning/USE-TDD ]` presence gate.
    - A PASS/FAIL verification directive is present, mapping FAIL to the existing `revision-required` verdict.
    - `grep -q '\$sg-execute' .agents/skills/sg-review/SKILL.md` succeeds (prose-halt names the re-run command verbatim).
    - A prose halt message (e.g. "tests FAILED" / "re-run $sg-execute") is present after the verdict output region.
    - `grep -q "AskUserQuestion(" .agents/skills/sg-review/SKILL.md` returns NOTHING (no interactive gate added — consistent with Platform Constraints :18-23).
    - No automatic `Skill(skill="sg-execute")` re-invocation introduced in the mirror.
    - No `grep -P` introduced.
  </acceptance_criteria>
  <done>The mirror's TDD-mode review surfaces a PASS/FAIL signal (FAIL = revision-required) and, on FAIL, emits a prose halt directing the user to re-run $sg-execute — no AskUserQuestion, no auto-recall, consistent with platform constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → skill logic | sg-review reads/writes `.planning/USE-TDD` and `.planning/USE-TDD-RETRY` (local files, no network/auth, no user-input-as-code) |
| review subagent result → loop control | the PASS/FAIL verdict returned by the code-review subagent drives the retry loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04 | Denial of service | unbounded retry loop (sg-review ⇄ sg-execute) | mitigate | Hard cap at 2 retries (count == 2 → report + `rm -f .planning/USE-TDD-RETRY` + STOP, no further AskUserQuestion). Counter persisted in the retry file; reset on PASS and on limit-exceeded so a clean restart is possible. |
| T-47-05 | Tampering | `.planning/USE-TDD-RETRY` count field manually edited to evade the cap | accept | Local single-developer file under `.planning/` (gitignored). A developer editing their own counter to re-run is not a threat — it is their own machine and their own choice. No privilege boundary crossed. |
| T-47-06 | Spoofing | falsified PASS verdict skipping the loop | accept | PASS/FAIL judgment is delegated to the trusted code-review subagent within the same session (D-04). No external/untrusted actor supplies the verdict. |
| T-47-07 | Information disclosure / Injection | marker file paths | mitigate | Paths are hardcoded constants (`.planning/USE-TDD`, `.planning/USE-TDD-RETRY`) — never derived from `$ARGUMENTS`. No path-injection surface. |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — only Markdown SKILL.md edits. No supply-chain surface. |
</threat_model>

<verification>
- `! grep -q "terminal Skill" skills/sg-review/SKILL.md` and `! grep -q "no steps execute after this point" skills/sg-review/SKILL.md` (D-05 correction).
- `grep -qE 'Skill\(skill="sg-execute"' skills/sg-review/SKILL.md` (re-invocation present).
- `grep -c "rm -f .planning/USE-TDD-RETRY" skills/sg-review/SKILL.md` >= 1 (D-06 reset).
- Both files contain `USE-TDD`; mirror has no `AskUserQuestion(`.
- `! grep -q "grep -P"` in both files (portability).
- Both files changed in a single commit (pairwise sync, D-08).
</verification>

<success_criteria>
- REVIEW-01: USE-TDD present → review carries PASS/FAIL verification directive; signal surfaced.
- REVIEW-02: FAIL + count<2 → AskUserQuestion → approve → write count+feedback → Skill(sg-execute) re-invoke.
- REVIEW-03: count==2 → report limit-exceeded + delete retry file + STOP (no ask); PASS → delete retry file.
- REVIEW-04: .agents mirror = same verification + FAIL detection, prose halt ("re-run $sg-execute"), no AskUserQuestion, no auto-recall.
- D-05: false terminal/no-steps notation corrected. Pairwise sync: both files modified together.
</success_criteria>

<output>
Create `.planning/phases/47-tdd-inject-fail-loop/47-02-SUMMARY.md` when done.
</output>
