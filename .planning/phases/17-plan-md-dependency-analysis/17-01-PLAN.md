---
phase: 17-plan-md-dependency-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-execute.md
autonomous: true
requirements:
  - TE-01a
  - TE-01b
  - TE-01c
must_haves:
  truths:
    - "wave 필드가 있는 PLAN.md에서 독립 그룹(PARALLEL_GROUPS)이 wave 번호 기준으로 올바르게 계산된다"
    - "files_modified 교집합이 있는 plan들은 같은 그룹으로 강제 병합된다"
    - "독립 그룹이 2개 미만이면 '병렬 그룹 감지 안됨 — 기존 순차 실행' 로그를 출력하고 기존 경로로 폴백한다"
    - "wave 필드가 없는 PLAN.md는 Step 8.5를 완전히 건너뛰고 기존 동작을 그대로 유지한다"
    - "parallel_groups.json이 .planning/phases/NN-*/에 생성되어 디버깅 및 Phase 18 스킬이 읽을 수 있다"
  artifacts:
    - path: "commands/sg-execute.md"
      provides: "Step 8.5 (의존성 분석) + Step 9 라우팅 분기"
      contains: "PARALLEL_GROUPS"
    - path: ".planning/phases/17-plan-md-dependency-analysis/parallel_groups.json"
      provides: "병렬 그룹 데이터 (실행 시 생성)"
      note: "실행 시 sg-execute가 생성하는 런타임 아티팩트"
  key_links:
    - from: "commands/sg-execute.md Step 8.5"
      to: ".planning/phases/NN-*/parallel_groups.json"
      via: "bash heredoc JSON 쓰기"
      pattern: "parallel_groups\\.json"
    - from: "commands/sg-execute.md Step 9"
      to: "PARALLEL_GROUPS 변수"
      via: "parallel_groups.json 읽기 후 분기"
      pattern: "PARALLEL_GROUPS"
---

<objective>
sg-execute.md에 Step 8.5를 추가하여 PLAN.md frontmatter의 wave/depends_on/files_modified를 파싱하고, 독립 병렬 그룹(PARALLEL_GROUPS)을 계산하여 JSON 파일로 저장한다. Step 9에는 그룹 수에 따른 라우팅 분기를 추가한다.

Purpose: Phase 18(sg-parallel-execute 스킬)이 PARALLEL_GROUPS를 소비하여 Task()로 병렬 실행할 수 있도록 기반을 마련한다.
Output: commands/sg-execute.md (Step 8.5 + Step 9 수정)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-plan-md-dependency-analysis/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: sg-execute.md에 Step 8.5 (PLAN.md 의존성 분석) 추가</name>
  <files>commands/sg-execute.md</files>

  <read_first>
    - commands/sg-execute.md 전체 — 현재 Step 8과 Step 9 사이의 정확한 위치 파악 필수.
      특히 Step 8 ("Append HANDOFF.md row.") 블록 끝과 Step 9 ("Build prompt and invoke Skill.") 블록 시작 사이에 Step 8.5를 삽입한다.
  </read_first>

  <action>
    commands/sg-execute.md의 Step 8 블록 직후, Step 9 블록 직전에 다음 Step 8.5 블록을 삽입한다.

    삽입 대상 위치: `8. **Append HANDOFF.md row.**` 블록이 끝나는 줄 바로 다음, `9. **Build prompt and invoke Skill.**` 줄 바로 앞.

    Step 8.5의 내용 (아래 내용 그대로 삽입):

    ---

    8.5. **PLAN.md 의존성 분석.** 모든 PLAN.md에서 wave/depends_on/files_modified를 파싱하여 독립 병렬 그룹(PARALLEL_GROUPS)을 계산하고 JSON 파일로 저장한다.

       ```bash
       PARALLEL_GROUPS=""
       GROUPS_JSON_FILE="$PHASE_DIR/parallel_groups.json"

       # wave 필드 존재 여부 확인 (하나라도 있으면 분석 진행)
       HAS_WAVE=$(grep -rl '^wave:' "$PHASE_DIR"/*-PLAN.md 2>/dev/null | head -1)

       if [ -z "$HAS_WAVE" ]; then
         echo "wave 필드 없음 — 기존 순차 실행 경로 유지"
       else
         # 각 PLAN.md에서 wave 번호와 files_modified 추출
         # 결과: "wave|planfile|file1,file2,..."
         PLAN_WAVE_FILES=""
         for PLAN_FILE in "$PHASE_DIR"/*-PLAN.md; do
           PLAN_BASE=$(basename "$PLAN_FILE")
           WAVE_NUM=$(grep -E '^wave:' "$PLAN_FILE" | head -1 | sed 's/wave:[[:space:]]*//')
           if [ -z "$WAVE_NUM" ]; then
             WAVE_NUM="99"
           fi
           # files_modified 블록: "  - path/to/file" 형식 파싱 (YAML 목록)
           FILES_IN_PLAN=$(awk '/^files_modified:/{found=1; next} found && /^[[:space:]]*-[[:space:]]/{gsub(/^[[:space:]]*-[[:space:]]*/,""); printf "%s,", $0} found && /^[^[:space:]-]/{exit}' "$PLAN_FILE" | sed 's/,$//')
           PLAN_WAVE_FILES="$PLAN_WAVE_FILES
    ${WAVE_NUM}|${PLAN_BASE}|${FILES_IN_PLAN}"
         done
         PLAN_WAVE_FILES=$(echo "$PLAN_WAVE_FILES" | grep -v '^$')

         # wave 값 목록 (중복 제거, 정렬)
         WAVE_NUMS=$(echo "$PLAN_WAVE_FILES" | awk -F'|' '{print $1}' | sort -n | uniq)

         # 각 wave별로 plan 목록과 files 수집
         # files_modified 교집합이 있는 plan은 같은 그룹으로 강제 병합
         GROUP_COUNT=0
         GROUPS_JSON="["
         FIRST_GROUP=1

         for W in $WAVE_NUMS; do
           # 이 wave의 plan들
           WAVE_PLANS=$(echo "$PLAN_WAVE_FILES" | awk -F'|' -v w="$W" '$1==w {print $2}')
           WAVE_FILES=$(echo "$PLAN_WAVE_FILES" | awk -F'|' -v w="$W" '$1==w {print $3}')

           # files_modified 교집합 기반 병합: union-find 단순 구현
           # plan별로 파일 집합을 저장하고 교집합 있는 plan끼리 같은 그룹으로 묶음
           # 셸 한계상: 교집합 있으면 하나의 병합 그룹, 없으면 각자 독립 그룹
           PLAN_LIST=$(echo "$WAVE_PLANS" | tr '\n' ' ' | sed 's/ $//')
           PLAN_COUNT=$(echo "$WAVE_PLANS" | grep -c '.')

           # 각 plan의 files 배열 구성 (plan_idx:files 형식)
           INDEXED=""
           IDX=0
           while IFS= read -r pline; do
             FILES_OF=$(echo "$PLAN_WAVE_FILES" | awk -F'|' -v p="$pline" '$2==p {print $3}')
             INDEXED="$INDEXED
    ${IDX}:${pline}:${FILES_OF}"
             IDX=$((IDX + 1))
           done <<PLANEOL
    $(echo "$WAVE_PLANS")
    PLANEOL
           INDEXED=$(echo "$INDEXED" | grep -v '^$')

           # 교집합 검사: 파일이 2개 이상의 plan에 나타나면 해당 plan들을 병합
           # 단순 구현: 모든 파일을 모아서 중복 있으면 병합, 없으면 독립
           ALL_FILES_RAW=$(echo "$WAVE_FILES" | tr ',' '\n' | grep -v '^$')
           DUP_FILES=$(echo "$ALL_FILES_RAW" | sort | uniq -d)

           if [ -n "$DUP_FILES" ] && [ "$PLAN_COUNT" -gt 1 ]; then
             # 중복 파일 있음 → 해당 plan들을 하나의 그룹으로 병합
             # 병합 그룹에 속하는 plan 판별 (중복 파일을 포함하는 plan)
             MERGED_PLANS=""
             SOLO_PLANS=""
             while IFS= read -r pline; do
               FILES_OF=$(echo "$PLAN_WAVE_FILES" | awk -F'|' -v p="$pline" '$2==p {print $3}' | tr ',' '\n')
               HAS_DUP=""
               while IFS= read -r df; do
                 if echo "$FILES_OF" | grep -qxF "$df"; then
                   HAS_DUP=1
                   break
                 fi
               done <<DUPEOF
    $(echo "$DUP_FILES")
    DUPEOF
               if [ -n "$HAS_DUP" ]; then
                 MERGED_PLANS="$MERGED_PLANS $pline"
               else
                 SOLO_PLANS="$SOLO_PLANS $pline"
               fi
             done <<WAVEEOF
    $(echo "$WAVE_PLANS")
    WAVEEOF

             # 병합 그룹 추가
             if [ -n "$MERGED_PLANS" ]; then
               PLANS_JSON=$(echo "$MERGED_PLANS" | tr ' ' '\n' | grep -v '^$' | awk '{printf "\"%s\",", $0}' | sed 's/,$//')
               [ "$FIRST_GROUP" -eq 0 ] && GROUPS_JSON="$GROUPS_JSON,"
               GROUPS_JSON="$GROUPS_JSON{\"wave\":${W},\"plans\":[${PLANS_JSON}],\"merged\":true}"
               GROUP_COUNT=$((GROUP_COUNT + 1))
               FIRST_GROUP=0
             fi
             # 나머지 solo plan들은 각자 독립 그룹
             for SP in $SOLO_PLANS; do
               [ -z "$SP" ] && continue
               [ "$FIRST_GROUP" -eq 0 ] && GROUPS_JSON="$GROUPS_JSON,"
               GROUPS_JSON="$GROUPS_JSON{\"wave\":${W},\"plans\":[\"${SP}\"],\"merged\":false}"
               GROUP_COUNT=$((GROUP_COUNT + 1))
               FIRST_GROUP=0
             done
           else
             # 중복 없음 → plan별 독립 그룹
             while IFS= read -r pline; do
               [ -z "$pline" ] && continue
               [ "$FIRST_GROUP" -eq 0 ] && GROUPS_JSON="$GROUPS_JSON,"
               GROUPS_JSON="$GROUPS_JSON{\"wave\":${W},\"plans\":[\"${pline}\"],\"merged\":false}"
               GROUP_COUNT=$((GROUP_COUNT + 1))
               FIRST_GROUP=0
             done <<WAVEEOF
    $(echo "$WAVE_PLANS")
    WAVEEOF
           fi
         done

         GROUPS_JSON="$GROUPS_JSON]"

         if [ "$GROUP_COUNT" -lt 2 ]; then
           echo "병렬 그룹 감지 안됨 — 기존 순차 실행"
           rm -f "$GROUPS_JSON_FILE"
         else
           PARALLEL_GROUPS="$GROUPS_JSON"
           printf '%s\n' "$GROUPS_JSON" > "$GROUPS_JSON_FILE"
           echo "병렬 그룹 ${GROUP_COUNT}개 감지 — parallel_groups.json 저장 완료"
         fi
       fi
       ```

    ---

    **구현 주의사항:**

    - `grep -P` 사용 금지 — macOS grep은 `-P` 미지원. `-E` (ERE) 또는 `-F` 사용.
    - `awk -F'|'` 파이프 구분 파싱 시 파이프 문자를 이스케이프 없이 사용 (BSD awk 호환).
    - `shasum`/`sha256sum` 패턴은 Step 6에 이미 있으므로 여기서는 사용 불필요.
    - heredoc 변수명(`PLANEOL`, `DUPEOF`, `WAVEEOF`)은 서로 겹치지 않게 고유하게 유지.
    - YAML `files_modified:` 블록은 들여쓰기 `  - ` 형식이며, 다음 최상위 키가 나오면 awk가 종료.
  </action>

  <verify>
    <automated>grep -n '8\.5\.' commands/sg-execute.md | head -5</automated>
  </verify>

  <acceptance_criteria>
    - commands/sg-execute.md에 "8.5. **PLAN.md 의존성 분석.**" 헤더가 존재한다
    - Step 8.5 블록에 `PARALLEL_GROUPS`, `parallel_groups.json`, `GROUP_COUNT` 변수가 모두 포함된다
    - `grep -P` 패턴이 Step 8.5 블록에 없다 (macOS 이식성)
    - Step 8.5는 Step 8 끝 이후, Step 9 시작 이전에 위치한다
    - wave 필드 없을 때 분기(`-z "$HAS_WAVE"`)와 있을 때 분기가 모두 존재한다
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: sg-execute.md Step 9에 병렬/순차 라우팅 분기 추가</name>
  <files>commands/sg-execute.md</files>

  <read_first>
    - commands/sg-execute.md Step 9 블록 전체 — 현재 "Build prompt and invoke Skill." 구조 파악.
      Step 8.5 삽입 후의 Step 9를 수정하므로 Task 1 완료 후 파일 상태를 기준으로 편집한다.
  </read_first>

  <action>
    commands/sg-execute.md의 Step 9 ("Build prompt and invoke Skill.") 블록을 수정하여,
    PARALLEL_GROUPS 유무에 따른 분기 라우팅을 앞에 추가한다.

    Step 9 블록의 **시작 부분**에 다음 내용을 삽입한다 (기존 "Assemble a single markdown blob..." 문장 바로 앞):

    ---

    **라우팅 분기.** Step 8.5에서 계산된 `PARALLEL_GROUPS` 변수를 확인한다:
    - `PARALLEL_GROUPS`가 비어 있지 않으면 (독립 그룹 2개 이상): `sg-parallel-execute` 스킬로 라우팅한다 (Phase 18에서 구현).
    - `PARALLEL_GROUPS`가 비어 있으면: 기존 순차 실행 경로(`superpowers:executing-plans`)를 그대로 사용한다.

    ```bash
    if [ -n "$PARALLEL_GROUPS" ]; then
      echo "=== 병렬 실행 경로 선택 ==="
      echo "PARALLEL_GROUPS: $PARALLEL_GROUPS"
      echo "sg-parallel-execute 스킬 라우팅 (Phase 18에서 구현 예정)"
      # TODO Phase 18: Skill(skill="sg-parallel-execute", args="$PARALLEL_GROUPS_JSON_FILE")
      echo "현재 Phase 17에서는 병렬 그룹이 감지되었으나 sg-parallel-execute 스킬이 미구현 상태입니다."
      echo "parallel_groups.json이 저장되었습니다: $GROUPS_JSON_FILE"
      echo "Phase 18 완료 후 이 경로가 활성화됩니다."
      echo ""
      echo "임시로 기존 순차 실행 경로로 폴백합니다."
    fi
    ```

    ---

    **구현 주의사항:**

    - 기존 Step 9의 "Assemble a single markdown blob..." 이후 내용(프롬프트 조립 + Skill 호출)은 변경하지 않는다.
    - 위 bash 블록은 Step 9 도입부에만 추가하며, 기존 순차 실행 로직은 그 아래에 그대로 유지된다.
    - `GROUPS_JSON_FILE` 변수는 Step 8.5에서 설정된 값을 그대로 참조한다.
    - Phase 18에서 이 TODO 주석 위치에 실제 스킬 호출이 들어간다.
  </action>

  <verify>
    <automated>grep -n 'PARALLEL_GROUPS' commands/sg-execute.md | head -10</automated>
  </verify>

  <acceptance_criteria>
    - commands/sg-execute.md Step 9 블록 안에 `if [ -n "$PARALLEL_GROUPS" ]` 분기가 존재한다
    - `sg-parallel-execute` 문자열이 Step 9 블록에 존재한다 (Phase 18 라우팅 플레이스홀더)
    - Step 9의 기존 `superpowers:executing-plans` Skill 호출 코드가 삭제되지 않고 그대로 유지된다
    - `Skill(skill="superpowers:executing-plans"` 패턴이 여전히 파일에 존재한다
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLAN.md frontmatter → bash 파서 | 사용자가 작성한 YAML이 신뢰되지 않은 셸 변수로 처리됨 |
| parallel_groups.json → Phase 18 스킬 | JSON 파일 내용이 스킬 입력으로 전달됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | parallel_groups.json 파일 | accept | 로컬 .planning/ 디렉토리에 저장, 동일 세션 내 소비. 외부 노출 없음 |
| T-17-02 | Information Disclosure | files_modified 경로 노출 | accept | 프로젝트 내부 경로만 포함, 비밀 정보 없음 |
| T-17-03 | Tampering | PLAN.md wave 필드 조작 | accept | wave 값은 그룹화 로직에만 사용, 임의 코드 실행 경로 없음 |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | 이 Phase는 패키지 설치 없음 — 순수 bash/sed/awk 구현 |
</threat_model>

<verification>
Phase 17 완료 확인:

```bash
# 1. Step 8.5 존재 확인
grep -c '8\.5\.' commands/sg-execute.md

# 2. PARALLEL_GROUPS 변수 참조 확인
grep -c 'PARALLEL_GROUPS' commands/sg-execute.md

# 3. parallel_groups.json 참조 확인
grep -c 'parallel_groups\.json' commands/sg-execute.md

# 4. wave 없는 폴백 분기 확인
grep -c 'HAS_WAVE' commands/sg-execute.md

# 5. 기존 superpowers:executing-plans 호출 보존 확인
grep -c 'superpowers:executing-plans' commands/sg-execute.md

# 6. grep -P 미사용 확인 (macOS 이식성)
grep -v '^#' commands/sg-execute.md | grep -c 'grep -P' || echo "OK: grep -P 없음"
```
</verification>

<success_criteria>
Phase 17 성공 기준 (ROADMAP.md Success Criteria 기반):

1. wave/depends_on/files_modified 필드가 있는 PLAN.md에서 PARALLEL_GROUPS가 올바르게 계산된다 (TE-01a)
2. files_modified 교집합이 있는 plan들은 merged:true 그룹으로 병합되어 파일 충돌 경로가 차단된다 (TE-01b)
3. 독립 그룹이 2개 미만이면 "병렬 그룹 감지 안됨 — 기존 순차 실행" 로그 후 기존 경로 실행 (TE-01c)
4. wave 필드 없는 PLAN.md는 Step 8.5를 건너뛰고 기존 동작 완전 보존
5. Step 9에 병렬/순차 라우팅 분기가 추가되어 Phase 18 스킬 연동 준비 완료
</success_criteria>

<output>
완료 시 `.planning/phases/17-plan-md-dependency-analysis/17-01-SUMMARY.md` 생성
</output>
