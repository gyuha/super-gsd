# Lessons: Phase 40 (2026-05-28)

## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: 2026-05-28T15:15:59Z_

### Decisions
- Step 1.5 삽입 위치: phase resolve(Step 1) 완료 직후, Step 2 이전 — PHASE_NUM 확정 후 브랜치 감지 (40-CONTEXT.md:A)
- feature 브랜치 판정 = main/master 이외 전부 제안 없음 (40-CONTEXT.md:B)
- skills/ = AskUserQuestion, .agents/ = 메시지 출력 후 자동 계속 — 플랫폼 제약 반영 (40-CONTEXT.md:C)
- sg-phase Step 4i: 텍스트 출력만, PR 자동 실행 없음, 단순성 우선 (40-CONTEXT.md:D, E)

### Surprises
- sg-phase Step 4i가 main 브랜치에서도 실행되는 문제를 code review에서 발견 — 플래너와 체커 모두 놓침. 코드 리뷰가 없었으면 main→main PR 명령 출력하는 버그가 배포됐을 것 (commit d7a2052)
- success_criteria 업데이트 누락도 code review에서만 포착 — 2개 Important 이슈 모두 코드 리뷰 단계에서 발견

### Patterns
- skills/ + .agents/ pairwise 수정 패턴 — sg-execute 두 파일 동시 수정, .agents/sg-phase 존재하지 않으므로 pairwise 불필요 확인 (40-01-SUMMARY.md)
- Read 도구 기반 파일 파싱 컨벤션: ROADMAP.md slug 추출 시 bash 파이프라인 금지, Claude가 직접 정규화 (v2.5 컨벤션 준수)
- parallel_groups.json 기반 wave barrier 실행: wave 1 완료 후 wave 2 실행 — 의존성 올바르게 처리됨

### Mistakes
- Step 4i 브랜치 가드 누락 — "조건 분기 없이 항상 출력" 결정이 main 브랜치 side effect를 간과함. code review에서 발견하여 수정 (d7a2052)
- success_criteria 업데이트 누락 — 새 기능 추가 시 success_criteria 갱신은 플래너 책임이지만 Phase 40에서 누락됨. code review에서 발견하여 수정 (d7a2052)

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | 플래너 프롬프트에 success_criteria 업데이트 명시 — 새 step/기능 추가 시 반드시 포함 | sg-plan SKILL.md 또는 CLAUDE.md에 체크리스트 항목 추가 |
| P1 | "unconditional output" 설계 시 브랜치/환경 컨텍스트 가드 검토 필수화 | 플래너 체크리스트에 "always-output 설계는 side effect 검토 필요" 추가 |
| P2 | code review --fix 패턴 지속 — Important 이슈 2개 모두 리뷰에서 포착됨 | sg-review 후 자동으로 --fix 실행하는 패턴 유지 |

## Lens: Start/Stop/Continue
_Captured: 2026-05-28T15:16:10Z_

### Start
- 플래너가 새 step/기능을 추가할 때 success_criteria 업데이트를 task로 명시 — Phase 40에서 누락되어 코드 리뷰에서 포착됨

### Stop
- 브랜치/환경 컨텍스트 없이 "조건 분기 없이 항상 출력" 설계를 채택하는 것 — 의도치 않은 환경(main 브랜치)에서 잘못된 명령이 출력될 수 있음

### Continue
- code review → fix → commit 사이클 유지 — Important 이슈를 배포 전에 포착
- pairwise convention (.agents/ 미러 동시 수정) 준수 — 누락 없이 적용됨
- wave barrier 기반 순차 실행 — 의존성 있는 plan들이 올바른 순서로 실행됨

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | "always-output" 패턴 사용 시 환경 가드 체크리스트 추가 | CLAUDE.md 또는 플래너 프롬프트에 명시 |
| P2 | sg-review + sg-review --fix를 기본 워크플로우로 정착 | sg-review 결과에 Important 이슈 있으면 자동 수정 후 진행 |

