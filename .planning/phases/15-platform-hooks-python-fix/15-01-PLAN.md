---
phase: 15
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .codex/hooks.json
  - .gemini/settings.json
  - hooks/stop_hook.py
  - hooks/rule_runner.py
  - .planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md
autonomous: true
requirements:
  - CODEX-04
  - MULTI-01

must_haves:
  truths:
    - ".codex/hooks.json 이 존재하고 Stop + PreToolUse 훅을 포함하며 SubagentStop은 포함하지 않는다"
    - ".gemini/settings.json 이 존재하고 SessionEnd + BeforeTool 훅을 포함한다"
    - "hooks/stop_hook.py 가 CLAUDE_PLUGIN_ROOT 없이 실행된다 (PLUGIN_ROOT 폴백 삽입)"
    - "hooks/rule_runner.py 가 CLAUDE_PLUGIN_ROOT 없이 실행되고 BeforeTool 이벤트를 인식한다"
  artifacts:
    - path: ".codex/hooks.json"
      provides: "Codex 전용 Stop + PreToolUse 훅 설정"
      contains: "PreToolUse"
    - path: ".gemini/settings.json"
      provides: "Gemini/Antigravity CLI 전용 SessionEnd + BeforeTool 훅 설정"
      contains: "SessionEnd"
    - path: "hooks/stop_hook.py"
      provides: "플랫폼 무관 PLUGIN_ROOT 계산 + 상대경로 교체"
      contains: "PLUGIN_ROOT"
    - path: "hooks/rule_runner.py"
      provides: "PLUGIN_ROOT + glob 경로 교체 + hookEventName 정규화"
      contains: "PLUGIN_ROOT"
    - path: ".planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md"
      provides: "Gemini/Antigravity 스키마 불확실 항목 human-verify 체크리스트"
  key_links:
    - from: ".codex/hooks.json"
      to: "hooks/stop_hook.py"
      via: "python3 hooks/stop_hook.py (상대경로)"
    - from: ".gemini/settings.json"
      to: "hooks/stop_hook.py"
      via: "python3 $GEMINI_PROJECT_DIR/hooks/stop_hook.py"
    - from: "hooks/stop_hook.py"
      to: ".planning/config.json"
      via: "os.path.join(PLUGIN_ROOT, '.planning/config.json')"
    - from: "hooks/rule_runner.py"
      to: ".claude/hookify.*.local.md"
      via: "os.path.join(PLUGIN_ROOT, '.claude', 'hookify.*.local.md')"
---

<objective>
Codex와 Gemini/Antigravity CLI 환경에서 super-gsd 훅이 동작하도록 플랫폼별 훅 설정 파일을 신규 생성하고,
Python 훅 두 개(stop_hook.py, rule_runner.py)가 CLAUDE_PLUGIN_ROOT 환경변수 없이도 실행되도록 수정한다.

Purpose: Claude Code 전용으로 작성된 훅 설정과 Python 상대경로를 Codex/Gemini 환경까지 확장해
multi-platform 학습 루프를 완성한다 (CODEX-04, MULTI-01).

Output:
- .codex/hooks.json — Codex Stop + PreToolUse 훅 (신규)
- .gemini/settings.json — Gemini SessionEnd + BeforeTool 훅 (신규)
- hooks/stop_hook.py — PLUGIN_ROOT 폴백 + 상대경로 4곳 교체 (수정)
- hooks/rule_runner.py — PLUGIN_ROOT 폴백 + glob 경로 2곳 교체 + hookEventName 정규화 (수정)
- 15-01-VERIFICATION.md — Gemini 스키마 불확실 항목 human-verify 체크리스트 (신규)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-platform-hooks-python-fix/15-CONTEXT.md

<!-- Existing hook reference (do NOT modify) -->
@hooks/hooks.json
@hooks/stop_hook.py
@hooks/rule_runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: .codex/hooks.json 신규 생성</name>
  <files>.codex/hooks.json</files>
  <action>
    `.codex/` 디렉토리를 생성하고 아래 조건을 만족하는 `.codex/hooks.json`을 작성한다.

    - `hooks` 최상위 키 아래 `PreToolUse`와 `Stop` 두 이벤트만 포함한다. `SubagentStop`은 포함하지 않는다 (Codex 미지원, per CODEX-04).
    - 각 이벤트의 command는 상대경로 사용: `python3 hooks/rule_runner.py`, `python3 hooks/stop_hook.py`.
      Codex는 PWD=프로젝트 루트를 보장하므로 상대경로가 안전하다. 절대경로(${CLAUDE_PLUGIN_ROOT}/...)는 사용하지 않는다.
    - timeout 단위는 seconds (Claude Code와 동일): PreToolUse=5, Stop=10.
    - 구조는 hooks/hooks.json 구조와 동일하게 중첩 `hooks` 배열 형태를 따른다.
    - `description` 키는 선택적으로 추가해도 되지만 없어도 무방하다.

    최종 파일 내용:
    {
      "hooks": {
        "PreToolUse": [{"hooks": [{"type": "command", "command": "python3 hooks/rule_runner.py", "timeout": 5}]}],
        "Stop": [{"hooks": [{"type": "command", "command": "python3 hooks/stop_hook.py", "timeout": 10}]}]
      }
    }
  </action>
  <verify>
    <automated>
      jq -e '.hooks | has("PreToolUse") and has("Stop") and (has("SubagentStop") | not)' /Users/gyuha/workspace/super-gsd/.codex/hooks.json
    </automated>
  </verify>
  <done>.codex/hooks.json 이 존재하고, PreToolUse + Stop 훅을 포함하며, SubagentStop 키가 없다. jq 검증 exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: .gemini/settings.json 신규 생성</name>
  <files>.gemini/settings.json</files>
  <action>
    `.gemini/` 디렉토리를 생성하고 `.gemini/settings.json`을 작성한다.

    조건:
    - `_schema_note` 키를 최상위에 추가해 스키마 불확실성을 명시한다:
      "Based on Gemini CLI docs. Antigravity CLI compatibility not confirmed — see .planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md"
    - `SessionEnd` 이벤트: stop_hook.py 실행. command에 `$GEMINI_PROJECT_DIR` 환경변수 사용.
      name 필드: "stop-hook", type: "command", timeout: 10000 (ms 단위, Gemini CLI 스펙).
    - `BeforeTool` 이벤트: rule_runner.py 실행. command에 `$GEMINI_PROJECT_DIR` 환경변수 사용.
      name 필드: "rule-runner", type: "command", timeout: 5000 (ms 단위).
    - `SubagentStop`에 대응하는 이벤트는 포함하지 않는다 (AfterAgent는 의미가 다르므로 제외, per CONTEXT.md Deferred).
    - command 경로: `python3 $GEMINI_PROJECT_DIR/hooks/stop_hook.py` 형태.

    최종 파일 내용:
    {
      "_schema_note": "Based on Gemini CLI docs. Antigravity CLI compatibility not confirmed — see .planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md",
      "hooks": {
        "SessionEnd": [{"hooks": [{"name": "stop-hook", "type": "command", "command": "python3 $GEMINI_PROJECT_DIR/hooks/stop_hook.py", "timeout": 10000}]}],
        "BeforeTool": [{"hooks": [{"name": "rule-runner", "type": "command", "command": "python3 $GEMINI_PROJECT_DIR/hooks/rule_runner.py", "timeout": 5000}]}]
      }
    }
  </action>
  <verify>
    <automated>
      jq -e '.hooks | has("SessionEnd") and has("BeforeTool")' /Users/gyuha/workspace/super-gsd/.gemini/settings.json &amp;&amp;
      jq -e '.hooks.SessionEnd[0].hooks[0].timeout == 10000' /Users/gyuha/workspace/super-gsd/.gemini/settings.json &amp;&amp;
      jq -e '.hooks.BeforeTool[0].hooks[0].command | contains("GEMINI_PROJECT_DIR")' /Users/gyuha/workspace/super-gsd/.gemini/settings.json
    </automated>
  </verify>
  <done>.gemini/settings.json 이 존재하고 SessionEnd + BeforeTool 훅을 포함한다. timeout이 ms 단위이고 $GEMINI_PROJECT_DIR 경로를 사용한다. jq 검증 exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: hooks/stop_hook.py — PLUGIN_ROOT 폴백 + 상대경로 교체</name>
  <files>hooks/stop_hook.py</files>
  <action>
    `hooks/stop_hook.py`를 수정한다. 변경 범위는 정확히 5줄 내외다.

    1. import 블록 직후, 첫 번째 함수 정의(`def load_config():`) 바로 앞에 PLUGIN_ROOT 계산 상수를 삽입한다:

       PLUGIN_ROOT = (
           os.environ.get("CLAUDE_PLUGIN_ROOT")
           or os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
       )

       `hooks/` 디렉토리 기준으로 dirname 두 번 = 플러그인 루트.

    2. `load_config()` 내 상대경로를 교체한다:
       - 변경 전: open('.planning/config.json', 'r')
       - 변경 후: open(os.path.join(PLUGIN_ROOT, '.planning', 'config.json'), 'r')

    3. `_read_current_phase()` 내 상대경로를 교체한다:
       - 변경 전: open('.planning/STATE.md', 'r')
       - 변경 후: open(os.path.join(PLUGIN_ROOT, '.planning', 'STATE.md'), 'r')

    4. `save_hookify_lessons()` 내 상대경로를 교체한다:
       - 변경 전: filepath = os.path.join('.planning', 'lessons', filename)
       - 변경 후: filepath = os.path.join(PLUGIN_ROOT, '.planning', 'lessons', filename)

    주의: `sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))` 줄은 이미 존재하며 수정하지 않는다.
    HANDOFF.md는 이 파일에서 직접 열지 않으므로 별도 교체 불필요 (stop_hook.py 현재 코드 기준).
    주석, 로직, 나머지 코드는 건드리지 않는다.
  </action>
  <verify>
    <automated>
      grep -q "PLUGIN_ROOT" /Users/gyuha/workspace/super-gsd/hooks/stop_hook.py &amp;&amp;
      grep -q "os.path.join(PLUGIN_ROOT" /Users/gyuha/workspace/super-gsd/hooks/stop_hook.py &amp;&amp;
      python3 -c "import ast, sys; ast.parse(open('/Users/gyuha/workspace/super-gsd/hooks/stop_hook.py').read()); sys.exit(0)"
    </automated>
  </verify>
  <done>stop_hook.py 에 PLUGIN_ROOT 상수가 있고 .planning/ 경로가 모두 os.path.join(PLUGIN_ROOT, ...) 형태로 교체됐다. Python 문법 오류 없음.</done>
</task>

<task type="auto">
  <name>Task 4: hooks/rule_runner.py — PLUGIN_ROOT + glob 경로 + hookEventName 정규화 + VERIFICATION.md 생성</name>
  <files>hooks/rule_runner.py, .planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md</files>
  <action>
    ## A. hooks/rule_runner.py 수정 (6줄 내외)

    1. import 블록 직후, `_hookify_installed()` 함수 정의 바로 앞에 PLUGIN_ROOT 상수를 삽입한다
       (stop_hook.py와 동일한 패턴):

       PLUGIN_ROOT = (
           os.environ.get("CLAUDE_PLUGIN_ROOT")
           or os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
       )

    2. `_load_rules()` 함수 내 glob 경로 두 곳을 교체한다:
       - 변경 전: _load_glob(os.path.join(".claude", "hookify.*.local.md"), priority=1)
       - 변경 후: _load_glob(os.path.join(PLUGIN_ROOT, ".claude", "hookify.*.local.md"), priority=1)
       - 변경 전: _load_glob(os.path.join(".claude", "sg-rule.*.local.md"), priority=2)
       - 변경 후: _load_glob(os.path.join(PLUGIN_ROOT, ".claude", "sg-rule.*.local.md"), priority=2)

    3. `main()` 함수 내 `input_data = json.load(sys.stdin)` 직후에 이벤트 이름 정규화 코드를 삽입한다:

       event_name = (
           input_data.get("hook_event_name")
           or input_data.get("hookEventName")
           or "PreToolUse"
       )

    4. `_evaluate()` 함수가 `event_name`을 받아 block 응답에 사용하도록 변경한다:
       - `_evaluate()` 시그니처를 `def _evaluate(rules: list, input_data: dict, event_name: str = "PreToolUse") -> dict:` 로 변경한다.
       - 함수 내 block 응답의 `"hookEventName": "PreToolUse"` 하드코딩을 `"hookEventName": event_name` 으로 교체한다.
       - `main()` 내 `_evaluate(rules, input_data)` 호출을 `_evaluate(rules, input_data, event_name)` 으로 변경한다.

    주석, 나머지 로직은 건드리지 않는다. `.planning/config.json` 경로는 main()에서 하드코딩되어 있으므로
    os.path.join(PLUGIN_ROOT, '.planning', 'config.json') 으로 교체한다 (main() 내 open(".planning/config.json") 1곳).

    ## B. 15-01-VERIFICATION.md 신규 생성

    아래 내용으로 `.planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md`를 생성한다.
    이 파일은 Gemini/Antigravity CLI 스키마 불확실 항목의 human-verify 체크리스트다.

    # Phase 15 — Verification Checklist

    ## Human-Verify 항목

    다음 항목들은 자동 검증이 불가능하며 Antigravity/Gemini CLI 실제 실행 환경에서 확인이 필요하다.

    | # | 항목 | 불확실 이유 | 검증 방법 | 상태 |
    |---|------|-----------|---------|------|
    | 1 | `.gemini/settings.json` 경로가 Antigravity에서도 동일한지 | antigravity.google/docs 미확인 | `agy` CLI 실행 후 훅 동작 여부 확인 | [ ] |
    | 2 | `hookEventName: "BeforeTool"` 필드가 응답에서 검증되는지 | 문서에서 validation 여부 미명시 | block 훅 등록 후 응답 JSON 관찰 | [ ] |
    | 3 | timeout 단위가 ms인지 (10000 = 10초) | Gemini CLI는 ms 명시, Antigravity 전용 문서 없음 | 타임아웃 로그 관찰 | [ ] |
    | 4 | `$GEMINI_PROJECT_DIR` 환경변수가 Antigravity에서도 주입되는지 | 마이그레이션 가이드에 묵시적 언급만 있음 | SessionStart 훅으로 `printenv GEMINI_PROJECT_DIR` 실행 | [ ] |
    | 5 | `_schema_note` 키를 Antigravity CLI가 무시하는지 (거부하지 않는지) | CLI가 알 수 없는 키 처리 방식 문서 없음 | 훅 설정 로드 후 오류 없음 확인 | [ ] |

    ## 검증 완료 시

    각 항목 상태를 `[x]`로 표시하고 실제 확인한 값(예: timeout 단위, 경로)을 메모한다.
    Antigravity 경로가 다를 경우 `.gemini/settings.json` 대신 해당 경로에 파일을 복사한다.
  </action>
  <verify>
    <automated>
      grep -q "PLUGIN_ROOT" /Users/gyuha/workspace/super-gsd/hooks/rule_runner.py &amp;&amp;
      grep -q "os.path.join(PLUGIN_ROOT" /Users/gyuha/workspace/super-gsd/hooks/rule_runner.py &amp;&amp;
      grep -q "event_name" /Users/gyuha/workspace/super-gsd/hooks/rule_runner.py &amp;&amp;
      grep -qv '"hookEventName": "PreToolUse"' /Users/gyuha/workspace/super-gsd/hooks/rule_runner.py &amp;&amp;
      python3 -c "import ast, sys; ast.parse(open('/Users/gyuha/workspace/super-gsd/hooks/rule_runner.py').read()); sys.exit(0)" &amp;&amp;
      test -f /Users/gyuha/workspace/super-gsd/.planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md
    </automated>
  </verify>
  <done>
    rule_runner.py에 PLUGIN_ROOT 상수가 있고 glob 경로 2곳이 교체됐다.
    hookEventName 하드코딩이 event_name 변수로 교체됐다.
    Python 문법 오류 없음.
    15-01-VERIFICATION.md가 존재하고 human-verify 체크리스트 5개 항목을 포함한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini CLI stdin → rule_runner.py | 외부 CLI가 주입하는 JSON. tool_name, hook_event_name 필드 신뢰 불가 |
| $GEMINI_PROJECT_DIR 환경변수 | 외부 환경에서 주입. 경로 조작 가능성 존재 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | hook_event_name 필드 | accept | 이벤트 이름은 라우팅에만 사용. 파일/명령 실행에 직접 사용하지 않으므로 low risk. |
| T-15-02 | Elevation of Privilege | $GEMINI_PROJECT_DIR 경로 | accept | hooks/stop_hook.py, rule_runner.py만 실행. 경로 탈출 불가 (고정 서브경로 사용). |
| T-15-03 | Information Disclosure | .planning/config.json via PLUGIN_ROOT | mitigate | 기존 try/except 패턴 유지. 파일 읽기 실패 시 빈 dict 반환으로 정보 노출 없음. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | 이 phase에서 새 패키지 설치 없음. 기존 stdlib만 사용. |
</threat_model>

<verification>
Phase 15 전체 검증:

1. .codex/hooks.json 구조 검증:
   jq '.hooks | keys' /Users/gyuha/workspace/super-gsd/.codex/hooks.json
   # 출력: ["PreToolUse", "Stop"] — SubagentStop 없음

2. .gemini/settings.json 구조 검증:
   jq '.hooks | keys' /Users/gyuha/workspace/super-gsd/.gemini/settings.json
   # 출력: ["BeforeTool", "SessionEnd"]

3. stop_hook.py Python 문법 + PLUGIN_ROOT 존재:
   python3 -c "import ast, sys; ast.parse(open('hooks/stop_hook.py').read()); sys.exit(0)"
   grep -c "PLUGIN_ROOT" hooks/stop_hook.py
   # 3 이상 (정의 1 + 사용 2~3)

4. rule_runner.py Python 문법 + PLUGIN_ROOT + event_name:
   python3 -c "import ast, sys; ast.parse(open('hooks/rule_runner.py').read()); sys.exit(0)"
   grep -c "PLUGIN_ROOT" hooks/rule_runner.py
   # 3 이상 (정의 1 + 사용 2)
   grep -c "event_name" hooks/rule_runner.py
   # 3 이상 (정의 + 전달 + 사용)

5. VERIFICATION.md 존재:
   test -f .planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md &amp;&amp; echo OK
</verification>

<success_criteria>
1. `.codex/hooks.json` 이 존재하고 Stop + PreToolUse 훅을 포함하며 SubagentStop은 포함하지 않는다
2. `.gemini/settings.json` 이 존재하고 SessionEnd + BeforeTool 훅을 포함하며 $GEMINI_PROJECT_DIR 경로와 ms 단위 timeout을 사용한다
3. `hooks/stop_hook.py` 가 PLUGIN_ROOT 상수를 정의하고 .planning/ 상대경로 전체를 os.path.join(PLUGIN_ROOT, ...) 으로 교체했다
4. `hooks/rule_runner.py` 가 PLUGIN_ROOT 상수를 정의하고, glob 경로 2곳을 교체하고, hookEventName을 수신 이벤트 이름으로 동적 반환한다
5. 두 Python 파일 모두 ast.parse() 문법 검증을 통과한다
6. `15-01-VERIFICATION.md` 가 존재하고 Gemini/Antigravity 스키마 불확실 항목 5개를 human-verify 체크리스트로 포함한다
</success_criteria>

<output>
완료 후 `.planning/phases/15-platform-hooks-python-fix/15-01-SUMMARY.md` 를 생성한다.
SUMMARY에는 실제 수정한 줄 수, PLUGIN_ROOT 정의 위치, glob 경로 교체 내용, hookEventName 변경 내용을 기록한다.
</output>
