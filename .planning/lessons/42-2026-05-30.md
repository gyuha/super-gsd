# Lessons: Phase 42 (2026-05-30)

## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: 2026-05-30T13:15:00Z_

### Decisions

- **Smart default = `dspm ssc` 고정 조합, dspm 먼저** (D-01, D-02 — `.planning/phases/42-smart-default-lens/42-CONTEXT.md:26-27`). 적응형 default(phase 종류·복잡도 기반 분기)는 v3.x 이후로 deferred. dspm을 앞에 둔 이유는 기술 회고→행동 권고 흐름.
- **Dropped lens(`4ls`/`sail`/`5why`) 완전 삭제, deprecated/주석 처리 금지** (D-05). silent fallback 대신 명시적 stderr 에러 + `exit 1` (D-06). 멀티 인자 중 첫 dropped 발견 즉시 거부 — no partial execution (D-07).
- **AskUserQuestion 6-option 블록 + 직후 parsing 블록 통째 삭제** (D-08). 도달 가능한 진입 경로 부재. 명시적 lens 선택 UI는 Phase 43 `--pick` flag로 재도입 예정.
- **pairwise 동일 commit staging** (D-10, commit `895fd34`). `skills/sg-retro/SKILL.md` + `.agents/skills/sg-retro/SKILL.md` 두 파일이 한 commit에 함께 staged — D-10 lock-in + Phase 32 Medium-1 convention.

### Surprises

- **Plan 자체에 byte-identical 보존 vs grep acceptance 직접 충돌이 두 곳에 있었음** (`42-01-SUMMARY.md:181-188`). #6 항목과 Step 6 BODY_PRINTF 주석에서 plan이 "보존" 지시와 "삭제된 토큰 부재" acceptance를 동시에 요구. Plan-time에 deletion의 cascade가 모두 포착되지 않음.
- **Code review가 plan-lint로 잡았어야 할 stale prose를 3건 잡음** (commit `a6bf7bc`). `skills/sg-retro/SKILL.md:14` `<objective>` "six lenses ... AskUserQuestion multiSelect", `.agents/skills/sg-retro/SKILL.md:20` `<constraints>` "numbered list", `.agents/skills/sg-retro/SKILL.md:200` "Common flow for ssc/4ls/dspm/sail lenses". 모두 plan의 "손대지 마" 영역이라 surgical 원칙에 따라 implementer가 건드리지 않은 결과 — plan-bug.
- **sg-parallel-execute가 commit을 안 하므로 sg-review 진입 시 `BASE_SHA == HEAD_SHA` 에러로 막힘**. workflow 가정(execute가 commit한다)이 parallel-execute에서 깨짐. 사용자 확인을 받고 수동으로 phase-level commit 후 진입.

### Patterns

- **pairwise mirror plan(skills/ + .agents/)에서 prose block sync는 매번 누락된다**. case-statement·LENS_NAME 매핑·`<lens_templates>` 같은 코드 블록은 grep acceptance로 잡히지만, `<objective>`/`<constraints>`/section header 같은 산문은 lens-token enumerate 검증이 plan-time에 빠지면 review-time에서만 잡힌다. Phase 32 Medium-1 → Phase 42에서 5건 추가 발견 — 반복 패턴.
- **STATE.md `Phase:` 필드 stale 재발**. `Phase: Not started (defining requirements)` 상태로 sg-execute·sg-retro 양쪽에서 PHASE_NUM 파싱 실패. HANDOFF.md 마지막 phase slug에서 수동으로 추출. 같은 milestone에서 두 번 표면화.
- **2-plan parallel execute가 깔끔하게 작동**. `wave:1` + `depends_on:[]` + non-overlapping `files_modified` + 한 응답에서 2 Task() agent 동시 dispatch → ~5분/agent, merge conflict 0건. v1.4에서 도입한 패턴이 v2.9에서도 유효함을 재확인 (`.planning/phases/42-smart-default-lens/parallel_groups.json`).

### Mistakes

- **Plan 작성 시 `<objective>` line 14에 `grep -c 'six lenses'` 0 acceptance와 "objective 손대지 마" 지시를 동시에 넣음** (`.planning/phases/42-smart-default-lens/42-01-PLAN.md:266` vs `:357`). 두 지시가 같은 라인을 가리킴 → implementer가 "preserve"를 따르고 acceptance를 SUMMARY에서 좁게 우회. Plan-internal contradiction.
- **`.agents/<constraints>` 블록 "byte-identical preserve" 지시가 numbered-list 삭제 premise를 무효화함을 plan-time에 인지 못 함** (`.planning/phases/42-smart-default-lens/42-02-PLAN.md:186-187`). plan 자체가 numbered list + map_num_to_code 삭제를 명령하면서 그 삭제를 documenting하는 `<constraints>` 줄을 보존하라고 지시. 정합성 미검증.
- **sg-retro 자기 자신을 수정한 phase의 retro를 plugin cache v0.0.42(pre-Phase-42)로 실행**. 새 동작(`smart default`)은 working tree에만 있고 로드된 skill은 6-lens 구버전. dogfooding 시점이 release 전. 직접 dspm+ssc로 진행하긴 했으나 정상 흐름은 아님.

### Action Items

| priority | item | next step |
|----------|------|-----------|
| P1 | Plan lint script: "preserve/byte-identical/손대지 마" 영역과 grep acceptance 패턴 cross-check, 같은 라인 가리키면 fail | 다음 plan 작성 전 (`hooks/plan_lint.cjs` 신설 또는 sg-plan 내장). Phase 42의 #6 contradiction + `<objective>` line 14 contradiction을 회귀 테스트 케이스로 |
| P1 | sg-parallel-execute 완료 → sg-review 진입 사이에 phase-level commit 자동화 또는 sg-review 진입 시 BASE_SHA==HEAD_SHA 감지하여 commit 안내 | `skills/sg-parallel-execute/SKILL.md` 종료 단계 또는 `skills/sg-review/SKILL.md` Step 1 보강 |
| P2 | pairwise mirror plan template에 prose block(`<objective>`/`<constraints>`/section headers) lens-token enumerate 검증 추가 | gsd-plan-phase template 또는 sg-plan SKILL.md 보강. 명시 token set이 plan time에 enumerate 안 되면 fail |
| P2 | STATE.md `Phase:` 필드 자동 갱신 — sg-plan 또는 sg-execute 진입 시 HANDOFF.md/ROADMAP에서 active phase 추출하여 sync | `skills/sg-plan/SKILL.md` 종료 시 또는 별도 `hooks/state_sync.cjs` |

## Lens: Start/Stop/Continue
_Captured: 2026-05-30T13:15:30Z_

### Start

- **Plan에 acceptance grep과 preserve 영역의 라인 단위 충돌 체크 도입**. Phase 42의 두 deviation은 plan-time에 잡혔어야 함 (`42-01-PLAN.md:266` vs `:357`, `42-02-PLAN.md:186-187` vs numbered-list 삭제 지시). plan lint 또는 plan-checker agent의 새 lens.
- **pairwise 변경 시 prose token enumerate를 plan 단계에서 작성**. 예: "smart default 변경 시 영향받는 산문 토큰 = {`six lenses`, `AskUserQuestion multiSelect`, `numbered list`, `map_num_to_code`, `ssc/4ls/dspm/sail`}". 이 목록이 있으면 implementer가 "preserve 영역도 토큰 부재 확인"이라는 surgical 판단을 내릴 수 있음.
- **sg-parallel-execute 종료 시 phase-level commit prompt**. 또는 sg-review 진입 시 working tree dirty 감지하여 자동 commit 안내. workflow 가정 깨짐 방지.

### Stop

- **`<objective>`/`<constraints>` 같은 named block을 acceptance grep 패턴과 별개로 "손대지 마"라고 plan에 적기**. 두 지시가 같은 라인을 가리키면 plan-internal contradiction. 적어도 두 지시 사이의 의존관계를 plan에 명시.
- **STATE.md `Phase:` 필드를 manual로 사용하기**. sg-plan/sg-execute 진입 시 HANDOFF.md/ROADMAP에서 derive하는 것이 정확하다. STATE.md는 milestone-level 메타데이터로 좁히고 phase pointer는 다른 출처 사용.
- **sg-retro 자기 자신을 수정한 phase의 retro를 plugin cache 구버전으로 무비판적으로 실행하기**. 새 동작이 working tree에만 있으면 release 전이고, 직접 보고하지 않으면 사용자가 dogfood 시점을 오인할 수 있음.

### Continue

- **wave:1 + non-overlapping files_modified + 2 Task() agent 동시 dispatch 패턴**. Phase 42에서 ~5분/agent, conflict 0건으로 깔끔히 작동. v1.4에서 도입 → v2.9에서 재검증. 향후 pairwise 변경 default 패턴으로 유지.
- **SUMMARY § "Plan-spec Deviations" 섹션으로 plan-spec 충돌을 명시적으로 surface**. `42-01-SUMMARY.md:181-188`가 모범. burying 안 하고 reviewer가 즉시 의도성 판단 가능.
- **Code review를 D-10 pairwise 동등성 last-line-of-defense로 활용**. plan-time에 못 잡은 prose drift 3건 모두 review에서 잡힘 — Critical 1 + Important 2 (`a6bf7bc`). review가 없었으면 prose drift가 main에 쌓였을 것.
- **HANDOFF.md append-only audit log + sg-next routing의 scan-back 패턴**. sg-next가 To 컬럼에 들어가도 직전 real stage를 복구하여 routing 정확도 유지. v2.7+ 도입한 패턴이 v2.9에서도 안정.

### Action Items

| priority | item | next step |
|----------|------|-----------|
| P1 | Plan template "Affected prose tokens" 섹션 추가 — pairwise 변경 plan은 영향받는 산문 토큰 셋을 명시 | gsd-plan-phase / sg-plan SKILL.md template 갱신 |
| P1 | sg-parallel-execute 또는 sg-review에서 working tree dirty 자동 감지 → phase-level commit 안내 | sg-review SKILL.md Step 1 또는 sg-parallel-execute 종료 단계 보강 |
| P2 | sg-plan 완료 시 STATE.md `Phase:` 필드 자동 sync | sg-plan SKILL.md Step N 추가 (또는 hooks/state_sync.cjs 신설) |
| P3 | sg-retro self-modification 감지 — 현재 phase가 sg-retro/SKILL.md를 수정했다면 retro 진입 시 plugin cache vs working tree drift 경고 | sg-retro SKILL.md Step 0 또는 별도 self-check |
