---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-retro/SKILL.md
autonomous: false
requirements:
  - ANALYZER-01
  - ANALYZER-02
  - ANALYZER-03
  - RETRO-03
  - RETRO-05

must_haves:
  truths:
    - "sg-retro args='10' 호출 시 6개 옵션(ssc/4ls/dspm/sail/5why/analyze)이 multiSelect:true AskUserQuestion으로 표시된다"
    - "args='10 sail' 호출 시 AskUserQuestion 없이 Sailboat lens가 바로 실행된다"
    - "args='10 5why' 호출 시 AskUserQuestion 없이 Five Whys 대화형 흐름이 시작된다"
    - "args='10 analyze' 호출 시 AskUserQuestion 없이 Conversation Analyzer가 실행된다"
    - "args='10 4ls dspm' 호출 시 4Ls 먼저, DSPM 다음으로 같은 lessons 파일에 순차 append된다"
    - "TRANSCRIPT_FILE이 없을 때 analyze lens는 'No transcript found' 메시지 후 graceful exit한다"
    - "analyze lens 실행 후 또는 multi-lens 마지막 종료 후 hookify rule draft가 1회 auto-suggest된다"
    - "Sailboat lens 출력이 ## Lens: Sailboat + Wind/Anchor/Sun/Rock 4 subheadings + Action Items 구조를 따른다"
    - "Five Whys lens 출력이 ## Lens: Five Whys + Problem Statement/Why 1-5/Root Cause + Action Items 구조를 따른다"
    - "Conversation Analyzer 출력이 ## Lens: Conversation Analyzer + Analysis Findings 표(4컬럼) + Draft Hookify Rules + Action Items 구조를 따른다"
  artifacts:
    - path: "skills/sg-retro/SKILL.md"
      provides: "sg-retro Skill — Phase 10 extensions fully applied"
      contains: "LENS_CODES_ARRAY"
    - path: "skills/sg-retro/SKILL.md"
      provides: "transcript collection bash block"
      contains: "TRANSCRIPT_FILE"
    - path: "skills/sg-retro/SKILL.md"
      provides: "Sailboat lens template"
      contains: "Sailboat (sail)"
    - path: "skills/sg-retro/SKILL.md"
      provides: "Five Whys lens template"
      contains: "Five Whys (5why)"
    - path: "skills/sg-retro/SKILL.md"
      provides: "Conversation Analyzer lens template"
      contains: "Conversation Analyzer (analyze)"
    - path: "skills/sg-retro/SKILL.md"
      provides: "multiSelect:true AskUserQuestion"
      contains: "multiSelect: true"
  key_links:
    - from: "Step 1 (argument parsing)"
      to: "LENS_CODES_ARRAY"
      via: "set -- $ARGUMENTS; EXTRA tokens ($3..$n) appended to array"
      pattern: "LENS_CODES_ARRAY"
    - from: "Step 2 (AskUserQuestion)"
      to: "LENS_CODES_ARRAY"
      via: "multiSelect response parsed into array"
      pattern: "multiSelect: true"
    - from: "Step 3 (artifact collection)"
      to: "Step 3b (transcript collection)"
      via: "bash block appended after artifact collection"
      pattern: "TRANSCRIPT_FILE"
    - from: "Multi-lens loop"
      to: "Step 5 (lens facilitation)"
      via: "iterate LENS_CODES_ARRAY, call sub-block per LENS_CODE"
      pattern: "LENS_CODES_ARRAY"
    - from: "analyze lens sub-block"
      to: "TRANSCRIPT_FILE"
      via: "Claude reads TRANSCRIPT_FILE using Read tool (not bash grep)"
      pattern: "analyze"
---

<objective>
`skills/sg-retro/SKILL.md`를 Phase 9 구현체에서 Phase 10 스펙으로 가산적으로 확장한다.

Purpose: ANALYZER-01/02/03(내장 conversation analyzer), RETRO-03(5 lens 완성), RETRO-05(multi-lens 다중 선택) 요건을 단일 파일 수정으로 충족한다.

Output: 수정된 `skills/sg-retro/SKILL.md` — 6개 lens 옵션(ssc/4ls/dspm/sail/5why/analyze), multiSelect:true, transcript 수집 bash 블록, Sailboat/Five Whys/Analyzer sub-block, multi-lens 순회 loop, auto-suggest hookify rule draft.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-conversation-analyzer-lens/10-CONTEXT.md
@.planning/phases/09-sg-retro-skill-scaffold/09-CONTEXT.md
@skills/sg-retro/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: skills/sg-retro/SKILL.md Phase 10 확장</name>
  <read_first>
    - skills/sg-retro/SKILL.md (전체 — 현재 구현체를 완전히 파악한 뒤 수정 시작)
    - .planning/phases/10-conversation-analyzer-lens/10-CONTEXT.md (D-01~D-22 전체)
  </read_first>
  <files>skills/sg-retro/SKILL.md</files>
  <action>
현재 `skills/sg-retro/SKILL.md`를 Read 도구로 전체 읽은 뒤, 아래 8개 변경을 순서대로 적용한다. 파일 전체를 Write 도구로 재작성한다.

**변경 1 — frontmatter `description` 업데이트**

현재:
  `Run a structured retrospective on a GSD phase with one of three lenses (SSC, 4Ls, DSPM) and append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md.`

교체:
  `Run a structured retrospective on a GSD phase with one of six lenses (SSC, 4Ls, DSPM, Sailboat, Five Whys, Conversation Analyzer) — select multiple lenses in one call — and append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md.`

**변경 2 — `<objective>` 블록 업데이트**

기존 `<objective>` 블록의 세 가지 lens 목록을 여섯 가지로 확장하고, "out of scope" 노트를 제거한다. 새 텍스트:

  Run a structured retrospective on a GSD phase. Auto-collect phase artifacts and git context. Let the user pick one or more of six lenses — Start/Stop/Continue (ssc), 4Ls (4ls), Decisions/Surprises/Patterns/Mistakes (dspm), Sailboat (sail), Five Whys (5why), Conversation Analyzer (analyze) — via AskUserQuestion multiSelect or directly via arguments. Facilitate each lens (artifact-grounded for ssc/4ls/dspm/sail; user-driven for 5why; transcript-native for analyze), then append all results sequentially to `.planning/lessons/{NN}-{YYYY-MM-DD}.md`. After all lenses complete, auto-suggest hookify rule drafts once.

**변경 3 — Step 1: EXTRA_LENS_CODES 파싱 추가 (per D-19, D-21)**

Step 1의 기존 bash 블록에서 `PHASE_RAW`/`LENS_RAW` 파싱 이후, 다음 블록을 추가한다:

```
# D-21: 3번째 이후 토큰을 추가 lens 코드로 파싱
EXTRA_LENS_CODES=""
if [ -n "${3:-}" ]; then
  shift 2
  EXTRA_LENS_CODES="$@"
fi
```

`LENS_CODES_ARRAY` 변수 선언(배열)은 Step 2에서 최종 확정되므로 Step 1에서는 선언만 한다:

```
LENS_CODES_ARRAY=""
```

**변경 4 — Step 2: multiSelect:true + 6개 옵션 + LENS_CODES_ARRAY 구성 (per D-18, D-19, D-20, D-21, D-22)**

Step 2의 기존 `LENS_CODE` 매핑 bash 블록을 아래와 같이 교체한다:

```bash
LENS_CODE=""
if [ -n "$LENS_RAW" ]; then
  LENS_LC=$(printf '%s' "$LENS_RAW" | tr '[:upper:]' '[:lower:]')
  case "$LENS_LC" in
    ssc)     LENS_CODE="ssc"     ;;
    4ls)     LENS_CODE="4ls"     ;;
    dspm)    LENS_CODE="dspm"    ;;
    sail)    LENS_CODE="sail"    ;;
    5why)    LENS_CODE="5why"    ;;
    analyze) LENS_CODE="analyze" ;;
    *)       LENS_CODE=""        ;;
  esac
fi
```

AskUserQuestion 호출을 다음으로 교체한다 (multiSelect:true, 6개 옵션, no default — D-22):

```
AskUserQuestion(
  questions: [{
    question: "Which retrospective lens(es) do you want to run?",
    header: "Lens",
    multiSelect: true,
    options: [
      { label: "Start/Stop/Continue (ssc)", description: "Behavior change — what to begin, stop, and continue based on phase artifacts." },
      { label: "4Ls (4ls)", description: "Liked / Learned / Lacked / Longed For — emotion + knowledge balance." },
      { label: "Decisions/Surprises/Patterns/Mistakes (dspm)", description: "Technical retrospective — locked decisions, pivots, recurring techniques, and verification failures." },
      { label: "Sailboat (sail)", description: "Wind / Anchor / Sun / Rock — metaphorical retrospective on propulsion, drag, energy, and risks." },
      { label: "Five Whys (5why)", description: "User-driven root cause analysis — state a problem, answer 5 iterative Why questions, derive root cause." },
      { label: "Conversation Analyzer (analyze)", description: "Extract frustration signals, correction patterns, repeated issues, and validated successes from the session transcript." }
    ]
  }]
)
```

multiSelect 응답에서 LENS_CODES_ARRAY 구성 bash 블록:

```bash
# multiSelect 응답에서 각 코드 추출 (괄호 안 코드 파싱)
LENS_CODES_ARRAY=$(printf '%s' "$ASKUSERQUESTION_RESPONSE" \
  | grep -oE '\((ssc|4ls|dspm|sail|5why|analyze)\)' \
  | tr -d '()')
```

single-lens argument 경로 (D-19):

```bash
# D-19: 인수 경로 — LENS_CODE가 있으면 배열 구성, EXTRA_LENS_CODES가 있으면 추가
if [ -n "$LENS_CODE" ]; then
  if [ -n "$EXTRA_LENS_CODES" ]; then
    # D-21: 3번째 이후 토큰도 배열에 포함
    VALID_EXTRAS=""
    for EC in $EXTRA_LENS_CODES; do
      EC_LC=$(printf '%s' "$EC" | tr '[:upper:]' '[:lower:]')
      case "$EC_LC" in
        ssc|4ls|dspm|sail|5why|analyze) VALID_EXTRAS="${VALID_EXTRAS} ${EC_LC}" ;;
      esac
    done
    LENS_CODES_ARRAY="${LENS_CODE}${VALID_EXTRAS}"
  else
    LENS_CODES_ARRAY="$LENS_CODE"
  fi
fi
```

**변경 5 — Step 3b: transcript 수집 bash 블록 추가 (per D-06)**

Step 3(artifact 수집 bash 블록) 직후에 다음 bash 블록을 삽입한다:

```bash
# D-06: 세션 transcript 수집 (analyze lens용)
PROJECT_SLUG=$(pwd | tr '/' '-' | sed 's/^-//')
TRANSCRIPT_DIR="$HOME/.claude/projects/${PROJECT_SLUG}"
TRANSCRIPT_FILE=$(ls -t "${TRANSCRIPT_DIR}"/*.jsonl 2>/dev/null | head -1)
if [ -z "$TRANSCRIPT_FILE" ]; then
  echo "[Conversation Analyzer] No transcript found at ${TRANSCRIPT_DIR}." >&2
  TRANSCRIPT_FILE=""
fi
```

**변경 6 — Step 5: Sailboat, Five Whys, Conversation Analyzer sub-block 추가 + multi-lens loop (per D-09~D-17, D-20)**

Step 5의 기존 "Common flow for all three lenses" 서술과 3개 sub-block을 유지하면서 아래 세 sub-block을 추가한다.

**Sub-block `sail` (Sailboat):**
- Fixed subheadings: `### Wind` / `### Anchor` / `### Sun` / `### Rock`
- Facilitation: D-11 artifact-grounded draft-then-confirm 방식. Wind/Sun은 SUMMARY의 긍정 신호(완료 항목, 예상보다 빠른 성과)에서, Anchor/Rock은 CONTEXT "Known Risk Sites" + 지연된 태스크 + git diff의 revertion 패턴에서 초안 도출. 각 항목은 출처 파일 경로 또는 commit hash를 인용. 전체 초안을 한 번에 제시한 뒤 사용자 수정/확정. Action Items 2-4개 확정 후 append.

**Sub-block `5why` (Five Whys):**
- Fixed subheadings: `### Problem Statement` / `### Why 1` ~ `### Why 5` / `### Root Cause`
- Facilitation (D-14, D-16 — 사용자 주도 대화형):
  1. AskUserQuestion header `"Five Whys"`, question `"What problem do you want to analyze? (Leave blank to let me suggest from phase artifacts)"` 로 problem statement 수집. 사용자가 공백 입력 시 Claude가 phase artifacts에서 Mistakes 또는 알려진 위험 항목을 후보로 제시하되 사용자 확인 필요.
  2. Why 1 질문을 plain text로 출력: `"Why did [problem statement] happen?"`. 사용자 답변 대기.
  3. Why 2~5까지 답변을 입력받으며 순차 진행. 각 단계에서 이전 답변을 why 질문의 대상으로 사용.
  4. 5번 완료 후 Root Cause 도출 요약 + Action Items 확정 후 append.
  - git artifacts는 문맥 보강용으로만 참조 (D-16).

**Sub-block `analyze` (Conversation Analyzer):**
- Fixed subheadings: `### Analysis Findings` (표) / `### Draft Hookify Rules` / `### Action Items`
- Facilitation (D-05, D-07, D-08):
  1. TRANSCRIPT_FILE이 비어 있으면 즉시 종료: `echo "No transcript found — skipping Conversation Analyzer." >&2` 후 해당 lens 결과 없이 다음 lens로 진행 또는 종료.
  2. TRANSCRIPT_FILE이 있으면 Claude가 Read 도구로 TRANSCRIPT_FILE을 직접 읽는다 (bash grep 금지 — D-05).
  3. 기본 스캔 범위: 최근 20-30 메시지 (D-07). 사용자가 "deep" 또는 `analyze deep` 토큰을 명시하면 전체 transcript 또는 최근 100 메시지로 확장.
  4. D-08 신호를 탐색하여 4 카테고리 분류:
     - `frustration`: "왜", "안 돼", "다시", "틀렸", "I didn't ask", "That's wrong" 등 사용자 불만 표현
     - `correction`: 사용자가 assistant의 이전 행동을 되돌리거나 재지시하는 패턴
     - `repeated`: 같은 종류의 실수 또는 지시가 2회 이상 반복
     - `validated-success`: 사용자가 명시적으로 수용·확인한 non-obvious assistant 선택
  5. 결과를 D-03 스키마 표로 출력:
     `| category | tool/event | pattern | context | severity |`
     severity: high(즉각 수정 필요) / medium(주의) / low(참고용)
  6. high/medium severity 항목 기반 Draft Hookify Rules 섹션 생성. 각 rule: `warn-{slug}` 또는 `block-{slug}` — Event: {tool}, Pattern: `{regex}`, Severity: {level} 형식.
  7. Action Items 확정 후 append.
  8. D-02 auto-suggest: `analyze` lens를 명시 선택한 경우, 이 sub-block 내부에서 rule draft를 포함하므로 Step 6의 별도 auto-suggest와 중복되지 않도록 `ANALYZE_LENS_RAN=true` 플래그를 설정.

**Multi-lens 실행 loop (D-20 구현):**

Step 5 전체를 아래 루프 구조로 감싼다:

```
for LENS_CODE in $LENS_CODES_ARRAY; do
  [각 LENS_CODE에 대해 해당 sub-block 실행]
  [해당 lens 결과를 lessons 파일에 append — Step 6 bash 블록 호출]
done
```

lens 사이에는 별도 "다음 lens로 진행?" 확인 없이 자동 진행 (D-20 "순차 실행"). 단, Five Whys(`5why`)는 대화형 흐름이 완전히 완료될 때까지 루프가 진행되지 않는다.

Five Whys가 multi-lens 배열에 포함된 경우 배열 첫 번째로 이동시키지 않는다 — 사용자 선택 순서 그대로 실행 (CONTEXT.md Known Risk Sites 처리 방식).

**변경 7 — Step 6 업데이트: multi-lens 종료 후 auto-suggest (per D-02)**

Step 6의 lessons append bash 블록 뒤에 다음을 추가한다:

```
# D-02: 모든 lens 완료 후 hookify rule draft auto-suggest (1회만)
# analyze lens가 이미 rule draft를 생성했으면 별도 출력 없이 reminder만.
if [ "${ANALYZE_LENS_RAN:-false}" = "true" ]; then
  echo "Hookify rule drafts were included in the Conversation Analyzer output above." >&2
else
  # Claude: high/medium severity 분석 결과 기반 또는 Action Items 기반 rule draft 제안
  echo "[Auto-suggest] Review the Action Items above and consider creating hookify rules for repeated patterns." >&2
fi
```

Step 6의 `LENS_NAME` case 블록에 새 lens 추가:

```bash
case "$LENS_CODE" in
  ssc)     LENS_NAME="Start/Stop/Continue" ;;
  4ls)     LENS_NAME="4Ls" ;;
  dspm)    LENS_NAME="Decisions/Surprises/Patterns/Mistakes" ;;
  sail)    LENS_NAME="Sailboat" ;;
  5why)    LENS_NAME="Five Whys" ;;
  analyze) LENS_NAME="Conversation Analyzer" ;;
esac
```

analyze lens가 TRANSCRIPT_FILE 없어 skip된 경우, lessons append 없이 다음 루프 iteration으로 continue한다.

**변경 8 — `<lens_templates>` 섹션에 3개 template 추가**

기존 3개 template 뒤에 추가:

Sailboat (sail):
```markdown
## Lens: Sailboat
_Captured: {ISO-8601 UTC}_

### Wind
- [user-language item — what propelled progress]

### Anchor
- [user-language item — what slowed down]

### Sun
- [user-language item — bright spot / energy source]

### Rock
- [user-language item — risk or obstacle encountered or ahead]

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | [user-language summary] | [user-language concrete step] |
```

Five Whys (5why):
```markdown
## Lens: Five Whys
_Captured: {ISO-8601 UTC}_

### Problem Statement
- [user-provided problem]

### Why 1
- [user answer]

### Why 2
- [user answer]

### Why 3
- [user answer]

### Why 4
- [user answer]

### Why 5
- [user answer]

### Root Cause
- [derived root cause statement]

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | [user-language summary] | [user-language concrete step] |
```

Conversation Analyzer (analyze):
```markdown
## Lens: Conversation Analyzer
_Captured: {ISO-8601 UTC}_

### Analysis Findings

| category | tool/event | pattern | context | severity |
|----------|------------|---------|---------|----------|
| frustration | Bash | `rm -rf` | User said "왜 삭제해" after cleanup | high |
| correction | Edit | `.env` write | User reverted env file change | high |
| repeated | Bash | `cat` instead of Read | Same correction 3 times | medium |
| validated-success | Edit | Surgical Edit over Write | User accepted minimal-change approach | low |

### Draft Hookify Rules

- `warn-dangerous-rm` — Event: bash, Pattern: `rm\s+-rf`, Severity: high
- `block-env-write` — Event: file, Pattern: `\.env$`, Severity: high

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | [user-language summary] | [user-language concrete step] |
```

**변경 9 — `<success_criteria>` 섹션 업데이트**

기존 5개 criteria를 유지하면서 Phase 10 criteria를 추가:

6. When `multiSelect: true` AskUserQuestion returns multiple lens labels, LENS_CODES_ARRAY contains all selected codes and each lens is executed sequentially with its result appended to the same lessons file (D-18, D-20, RETRO-05).
7. args="10 sail" / "10 5why" / "10 analyze" trigger direct execution of the respective lens without AskUserQuestion (D-19 extension to sail/5why/analyze).
8. args="10 4ls dspm" builds LENS_CODES_ARRAY=["4ls", "dspm"] and appends both lens sections to the same file in order (D-21).
9. When TRANSCRIPT_FILE is empty, the analyze lens emits "No transcript found" to stderr and exits the lens gracefully without appending to lessons (D-06).
10. The Conversation Analyzer reads the JSONL file using Claude's Read tool (not bash grep) and outputs a 4-column findings table (category/tool-event/pattern/context/severity) + Draft Hookify Rules section (ANALYZER-01, ANALYZER-02).
11. hookify rule draft auto-suggest occurs exactly once per sg-retro invocation — after all lenses complete (D-02). If analyze lens ran, its Draft Hookify Rules section serves as the auto-suggest output; no duplicate suggestion is made.
  </action>
  <verify>
    <automated>grep -qF 'multiSelect: true' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'Sailboat (sail)' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'Five Whys (5why)' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'Conversation Analyzer (analyze)' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'LENS_CODES_ARRAY' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'TRANSCRIPT_FILE' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'EXTRA_LENS_CODES' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'ANALYZE_LENS_RAN' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF '### Wind' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF '### Anchor' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF '### Problem Statement' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF '### Root Cause' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF '### Analysis Findings' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF '### Draft Hookify Rules' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'sail)    LENS_NAME="Sailboat"' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF '5why)    LENS_NAME="Five Whys"' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'analyze) LENS_NAME="Conversation Analyzer"' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'No transcript found' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'PROJECT_SLUG' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'frustration' skills/sg-retro/SKILL.md</automated>
    <automated>grep -qF 'validated-success' skills/sg-retro/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -qF 'multiSelect: true' skills/sg-retro/SKILL.md
    - grep -qF '"Sailboat (sail)"' skills/sg-retro/SKILL.md
    - grep -qF '"Five Whys (5why)"' skills/sg-retro/SKILL.md
    - grep -qF '"Conversation Analyzer (analyze)"' skills/sg-retro/SKILL.md
    - grep -qF 'LENS_CODES_ARRAY' skills/sg-retro/SKILL.md
    - grep -qF 'TRANSCRIPT_FILE' skills/sg-retro/SKILL.md
    - grep -qF 'EXTRA_LENS_CODES' skills/sg-retro/SKILL.md
    - grep -qF '### Wind' skills/sg-retro/SKILL.md
    - grep -qF '### Anchor' skills/sg-retro/SKILL.md
    - grep -qF '### Sun' skills/sg-retro/SKILL.md
    - grep -qF '### Rock' skills/sg-retro/SKILL.md
    - grep -qF '### Problem Statement' skills/sg-retro/SKILL.md
    - grep -qF '### Why 1' skills/sg-retro/SKILL.md
    - grep -qF '### Root Cause' skills/sg-retro/SKILL.md
    - grep -qF '### Analysis Findings' skills/sg-retro/SKILL.md
    - grep -qF '### Draft Hookify Rules' skills/sg-retro/SKILL.md
    - grep -qF 'No transcript found' skills/sg-retro/SKILL.md
    - grep -qF 'ANALYZE_LENS_RAN' skills/sg-retro/SKILL.md
    - grep -qF 'frustration' skills/sg-retro/SKILL.md
    - grep -qF 'validated-success' skills/sg-retro/SKILL.md
    - grep -qF 'correction' skills/sg-retro/SKILL.md
    - grep -qF 'repeated' skills/sg-retro/SKILL.md
    - grep -v '^#' skills/sg-retro/SKILL.md | grep -cF 'multiSelect' | grep -qE '^[1-9]'
  </acceptance_criteria>
  <done>
    - skills/sg-retro/SKILL.md에 6개 lens 옵션이 모두 존재하며 multiSelect:true로 설정됨
    - LENS_CODES_ARRAY가 Step 1(EXTRA), Step 2(AskUserQuestion 또는 인수 경로) 양쪽에서 구성됨
    - transcript 수집 bash 블록(PROJECT_SLUG, TRANSCRIPT_DIR, TRANSCRIPT_FILE)이 Step 3 뒤에 존재함
    - Sailboat(Wind/Anchor/Sun/Rock), Five Whys(Problem/Why1-5/Root Cause), Analyzer(Analysis Findings/Draft Rules) 3개 sub-block이 Step 5에 존재함
    - Step 6 LENS_NAME case 블록에 sail/5why/analyze 항목이 추가됨
    - lens_templates 섹션에 3개 template이 추가됨
    - success_criteria 섹션에 Phase 10 criteria(6-11번)가 추가됨
    - 모든 acceptance_criteria grep 체크 통과
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 수동 end-to-end 시나리오 검증 (Phase 10 확장 6개)</name>
  <read_first>
    - skills/sg-retro/SKILL.md (Task 1 완료 후 최신 상태)
    - .planning/phases/10-conversation-analyzer-lens/10-CONTEXT.md §"Multi-lens 수동 시나리오" (시나리오 12-18)
  </read_first>
  <files></files>
  <what-built>
    Task 1에서 sg-retro SKILL.md를 Phase 10 스펙으로 확장함 — 6개 lens(ssc/4ls/dspm/sail/5why/analyze), multiSelect:true AskUserQuestion, transcript 수집 bash 블록, 3개 신규 lens sub-block, multi-lens 순회 loop.
  </what-built>
  <how-to-verify>
    다음 7개 시나리오를 순서대로 실행하고 각 기대 동작을 확인한다.

    **시나리오 12: multi-select UI**
    `Skill(skill="sg-retro", args="10")`
    기대: AskUserQuestion이 6개 옵션 + multiSelect:true로 표시됨. 여러 개 선택 가능.

    **시나리오 13: Sailboat 직접 호출**
    `Skill(skill="sg-retro", args="10 sail")`
    기대: AskUserQuestion 없이 Sailboat lens 바로 실행. 초안에 Wind/Anchor/Sun/Rock 4개 섹션 포함. lessons 파일에 `## Lens: Sailboat` 헤더로 append됨.

    **시나리오 14: Five Whys 직접 호출**
    `Skill(skill="sg-retro", args="10 5why")`
    기대: AskUserQuestion 없이 Five Whys 대화형 흐름 시작. problem statement 질문 후 5번 Why 순차 진행. lessons 파일에 `## Lens: Five Whys` + Why 1-5 + Root Cause 구조로 append됨.

    **시나리오 15: Analyzer 직접 호출**
    `Skill(skill="sg-retro", args="10 analyze")`
    기대: AskUserQuestion 없이 transcript 수집 → Claude Read 도구로 JSONL 읽기 → 4-카테고리 표 + Draft Hookify Rules 출력. lessons 파일에 `## Lens: Conversation Analyzer` 헤더로 append됨.

    **시나리오 16: multi-lens 인수 경로**
    `Skill(skill="sg-retro", args="10 4ls dspm")`
    기대: LENS_CODES_ARRAY=["4ls", "dspm"]. 4Ls 먼저 실행·append, DSPM 다음 실행·append. 같은 lessons 파일에 두 lens 섹션이 순서대로 존재함.

    **시나리오 17: transcript 미존재 graceful exit**
    TRANSCRIPT_FILE이 없는 환경(또는 임시로 $HOME/.claude/projects/ 경로를 존재하지 않는 slug로 강제)에서 `Skill(skill="sg-retro", args="10 analyze")` 실행.
    기대: stderr에 "No transcript found" 메시지 출력 후 graceful exit. lessons 파일에 analyze 섹션 미추가. 에러 없이 정상 종료.

    **시나리오 18: multi-lens + analyze 포함 시 auto-suggest 1회**
    multiSelect AskUserQuestion에서 4ls + analyze 두 개 선택.
    기대: 4Ls 실행 → Analyzer 실행. Analyzer의 Draft Hookify Rules 섹션이 auto-suggest 역할을 함. 별도 duplicate rule 제안 없음. lessons 파일에 `## Lens: 4Ls` → `## Lens: Conversation Analyzer` 순서로 두 섹션이 append됨.

    **Phase 9 regression: 기존 3 lens 정상 동작 확인**
    `Skill(skill="sg-retro", args="10 ssc")` 또는 `args="10 4ls"` 실행하여 Phase 9 기능이 그대로 동작하는지 확인.
    기대: 기존 sub-block 동작 이상 없음. AskUserQuestion fallback 시 6개 옵션 표시.
  </how-to-verify>
  <resume-signal>모든 시나리오(12-18) 통과 여부를 "approved" 또는 실패한 시나리오 번호와 증상을 기술하여 응답.</resume-signal>
  <acceptance_criteria>
    - 시나리오 12: multiSelect:true AskUserQuestion에 6개 옵션이 표시됨
    - 시나리오 13: Sailboat lens 실행 후 lessons에 Wind/Anchor/Sun/Rock 섹션 존재
    - 시나리오 14: Five Whys 대화형 흐름 완료 후 lessons에 Why 1-5 + Root Cause 존재
    - 시나리오 15: analyze lens가 JSONL 읽기 후 4-카테고리 표 + Draft Rules 생성
    - 시나리오 16: 같은 lessons 파일에 4Ls → DSPM 두 섹션 순서대로 존재
    - 시나리오 17: "No transcript found" 메시지 후 에러 없이 종료
    - 시나리오 18: analyze 포함 multi-lens에서 rule draft 중복 없음
  </acceptance_criteria>
  <done>
    7개 시나리오 전체 통과 + Phase 9 regression 이상 없음.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md bash → filesystem | TRANSCRIPT_FILE 경로는 현재 사용자의 $HOME 하위로만 구성됨 (PROJECT_SLUG는 pwd 기반) |
| Claude Read → JSONL | Claude가 JSONL을 직접 읽으므로 bash injection 없음. 단, JSONL 내 악성 패턴 텍스트가 Claude LLM 해석에 영향 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | TRANSCRIPT_FILE path | accept | $HOME 하위 경로만 사용; 다른 사용자 transcript 접근 불가 |
| T-10-02 | Tampering | lessons append | accept | `>>` redirect 단일 블록; 기존 Phase 9 미티게이션 유지 |
| T-10-03 | Denial of Service | JSONL 파일 매우 큰 경우 | mitigate | 기본 스캔 범위 최근 20-30 메시지 제한(D-07); Read 도구가 자동으로 대형 파일 청크 처리 |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | 이 phase는 패키지 설치 없음 |
</threat_model>

<verification>
모든 acceptance_criteria grep 체크:

```bash
grep -qF 'multiSelect: true' skills/sg-retro/SKILL.md && echo "PASS: multiSelect" || echo "FAIL: multiSelect"
grep -qF '"Sailboat (sail)"' skills/sg-retro/SKILL.md && echo "PASS: Sailboat option" || echo "FAIL: Sailboat option"
grep -qF '"Five Whys (5why)"' skills/sg-retro/SKILL.md && echo "PASS: Five Whys option" || echo "FAIL: Five Whys option"
grep -qF '"Conversation Analyzer (analyze)"' skills/sg-retro/SKILL.md && echo "PASS: Analyzer option" || echo "FAIL: Analyzer option"
grep -qF 'LENS_CODES_ARRAY' skills/sg-retro/SKILL.md && echo "PASS: LENS_CODES_ARRAY" || echo "FAIL: LENS_CODES_ARRAY"
grep -qF 'TRANSCRIPT_FILE' skills/sg-retro/SKILL.md && echo "PASS: TRANSCRIPT_FILE" || echo "FAIL: TRANSCRIPT_FILE"
grep -qF 'EXTRA_LENS_CODES' skills/sg-retro/SKILL.md && echo "PASS: EXTRA_LENS_CODES" || echo "FAIL: EXTRA_LENS_CODES"
grep -qF '### Wind' skills/sg-retro/SKILL.md && echo "PASS: Wind" || echo "FAIL: Wind"
grep -qF '### Problem Statement' skills/sg-retro/SKILL.md && echo "PASS: Problem Statement" || echo "FAIL: Problem Statement"
grep -qF '### Analysis Findings' skills/sg-retro/SKILL.md && echo "PASS: Analysis Findings" || echo "FAIL: Analysis Findings"
grep -qF '### Draft Hookify Rules' skills/sg-retro/SKILL.md && echo "PASS: Draft Hookify Rules" || echo "FAIL: Draft Hookify Rules"
grep -qF 'No transcript found' skills/sg-retro/SKILL.md && echo "PASS: graceful fallback" || echo "FAIL: graceful fallback"
grep -qF 'ANALYZE_LENS_RAN' skills/sg-retro/SKILL.md && echo "PASS: auto-suggest guard" || echo "FAIL: auto-suggest guard"
grep -qF 'frustration' skills/sg-retro/SKILL.md && echo "PASS: frustration signal" || echo "FAIL: frustration signal"
grep -qF 'validated-success' skills/sg-retro/SKILL.md && echo "PASS: validated-success" || echo "FAIL: validated-success"
```
</verification>

<success_criteria>
Phase 10 완료 기준:

1. `skills/sg-retro/SKILL.md`에 6개 lens 옵션(ssc/4ls/dspm/sail/5why/analyze)이 모두 포함됨
2. multiSelect:true AskUserQuestion이 적용되어 여러 lens 동시 선택 가능
3. LENS_CODES_ARRAY 기반 multi-lens 순회 loop가 구현되어 같은 lessons 파일에 순차 append됨
4. transcript 수집 bash 블록(PROJECT_SLUG/TRANSCRIPT_DIR/TRANSCRIPT_FILE)이 Step 3 뒤에 존재하며 미존재 시 graceful fallback
5. Claude-native(Read 도구) 방식으로 JSONL 분석 (bash grep 미사용)
6. Sailboat(Wind/Anchor/Sun/Rock), Five Whys(Problem/Why1-5/Root Cause), Analyzer(Findings/Rules) 3개 신규 sub-block 완성
7. auto-suggest hookify rule draft가 모든 lens 완료 후 1회만 실행 (analyze lens와 중복 없음)
8. Phase 9 기존 3 lens(ssc/4ls/dspm) 동작에 regression 없음
9. 수동 시나리오 12-18 전체 통과
10. 수정 대상 파일이 skills/sg-retro/SKILL.md 단 하나 (hooks/stop_hook.py, commands/sg-learn.md 등 미수정)
</success_criteria>

<output>
Create `.planning/phases/10-conversation-analyzer-lens/10-01-SUMMARY.md` when done.
</output>
