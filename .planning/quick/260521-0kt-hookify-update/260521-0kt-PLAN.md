---
phase: quick-260521-0kt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-retro/SKILL.md
  - commands/sg-health.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "sg-retro SKILL.md의 어떤 줄에도 'hookify rule'이라는 문자열이 없다"
    - "sg-health.md의 Hookify 체크가 [OPTIONAL]로 표시되거나 제거된다"
  artifacts:
    - path: skills/sg-retro/SKILL.md
      provides: hookify 종속성 제거된 retro 스킬
    - path: commands/sg-health.md
      provides: hookify를 선택적 항목으로 표시한 health 커맨드
  key_links: []
---

<objective>
sg-retro SKILL.md와 sg-health.md에서 hookify 필수 종속성 참조를 제거한다.

Purpose: hookify는 더 이상 필수 의존성이 아니므로, 문서와 스크립트가 이를 필수로 가정하지 않아야 한다.
Output: "hookify rule drafts" → "sg-rule drafts", sg-health Hookify 체크 → [OPTIONAL] 처리
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@skills/sg-retro/SKILL.md
@commands/sg-health.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: sg-retro SKILL.md — hookify 참조를 sg-rule로 교체</name>
  <files>skills/sg-retro/SKILL.md</files>
  <action>
다음 네 곳을 정확히 교체한다 (다른 줄은 건드리지 않는다):

1. Line 7 (objective 블록): "auto-suggest hookify rule drafts once" → "auto-suggest sg-rule drafts once"

2. Line 363 (process 블록): "auto-suggest hookify rule drafts once:" → "auto-suggest sg-rule drafts once:"

3. Line 366 (bash 주석): "hookify rule draft auto-suggest" → "sg-rule draft auto-suggest"

4. Line 372 (echo 메시지): "hookify rules" → "sg-rules"
   전체 문자열: "consider creating hookify rules for repeated patterns." → "consider creating sg-rules for repeated patterns."

5. Line 547 (success_criteria): "hookify rule draft auto-suggest" → "sg-rule draft auto-suggest"
   그리고 같은 줄의 "Draft Hookify Rules section" → "Draft sg-rules section"

각 교체는 Edit 도구로 개별 수행한다. 줄 번호는 참고용이며 실제 위치로 확인 후 교체한다.
  </action>
  <verify>
    <automated>grep -n "hookify rule\|hookify rules\|hookify rule draft\|Draft Hookify" /Users/gyuha/workspace/super-gsd/skills/sg-retro/SKILL.md | grep -v "^Binary" | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL</automated>
  </verify>
  <done>SKILL.md에 "hookify rule" / "hookify rules" / "Draft Hookify Rules" 문자열이 0건이다. "sg-rule" 또는 "sg-rules"로 대체된 것이 grep으로 확인된다.</done>
</task>

<task type="auto">
  <name>Task 2: sg-health.md — Hookify 체크를 [OPTIONAL]로 변경</name>
  <files>commands/sg-health.md</files>
  <action>
sg-health.md의 "3. **Hookify 설치**" 섹션을 수정한다.

변경 전 (lines 35-42 근방):
```
3. **Hookify 설치**

   ```bash
   test -d "$HOME/.claude/plugins/data/hookify-claude-plugins-official" && echo OK || echo FAIL
   ```

   - OK → `Hookify .......... [OK]`
   - FAIL → `Hookify .......... [FAIL] 디렉토리 없음`, FAIL++
```

변경 후:
```
3. **Hookify 설치** *(선택)*

   ```bash
   test -d "$HOME/.claude/plugins/data/hookify-claude-plugins-official" && echo OK || echo OPTIONAL
   ```

   - OK → `Hookify .......... [OK]`
   - OPTIONAL → `Hookify .......... [OPTIONAL] 미설치 (선택적 의존성)` — FAIL 카운트에 포함하지 않는다
```

FAIL++ 줄 및 FAIL 관련 텍스트는 제거한다. 섹션 번호(3.)와 이후 섹션 번호는 유지한다.
  </action>
  <verify>
    <automated>grep -n "FAIL" /Users/gyuha/workspace/super-gsd/commands/sg-health.md | grep -i "hookify" | wc -l | xargs -I{} test {} -eq 0 && echo PASS || echo FAIL</automated>
  </verify>
  <done>sg-health.md의 Hookify 섹션에서 FAIL 카운트 증가가 사라지고 OPTIONAL 출력으로 대체된다. grep "OPTIONAL" commands/sg-health.md 가 1건 이상 반환된다.</done>
</task>

</tasks>

<verification>
```bash
# hookify rule 참조 완전 제거 확인
grep -rn "hookify rule\|hookify rules\|Draft Hookify" \
  /Users/gyuha/workspace/super-gsd/skills/sg-retro/SKILL.md \
  /Users/gyuha/workspace/super-gsd/commands/sg-health.md

# sg-rule 대체 확인
grep -c "sg-rule" /Users/gyuha/workspace/super-gsd/skills/sg-retro/SKILL.md

# health OPTIONAL 확인
grep "OPTIONAL" /Users/gyuha/workspace/super-gsd/commands/sg-health.md
```
</verification>

<success_criteria>
- skills/sg-retro/SKILL.md: "hookify rule" / "hookify rules" / "Draft Hookify" 0건
- skills/sg-retro/SKILL.md: "sg-rule" 5건 이상 (교체된 위치들)
- commands/sg-health.md: Hookify 섹션이 FAIL 카운트 없이 OPTIONAL로 출력
- 두 파일 외 다른 파일은 변경 없음
</success_criteria>

<output>
완료 후 `.planning/quick/260521-0kt-hookify-update/260521-0kt-SUMMARY.md` 생성
</output>
