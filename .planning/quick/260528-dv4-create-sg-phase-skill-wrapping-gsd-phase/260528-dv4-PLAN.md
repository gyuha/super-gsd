---
phase: quick-260528-dv4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-phase/SKILL.md
  - README.md
  - README.ko.md
autonomous: true
requirements: [QUICK-260528-dv4]

must_haves:
  truths:
    - "/super-gsd:sg-phase edit <N> delegates to gsd-phase --edit"
    - "/super-gsd:sg-phase remove <N> delegates to gsd-phase --remove"
    - "/super-gsd:sg-phase complete <N> reconciles ROADMAP Progress row + Phases checkbox + STATE.md for a finished phase"
    - "Unknown/empty subcommand prints a usage line and exits without delegating"
    - "README.md and README.ko.md describe sg-phase wrapping edit/remove plus the new complete operation"
  artifacts:
    - path: "skills/sg-phase/SKILL.md"
      provides: "sg-phase slash command: edit/remove delegation + complete reconcile logic"
      contains: "name: sg-phase"
    - path: "README.md"
      provides: "Updated phase-management narrative referencing sg-phase"
    - path: "README.ko.md"
      provides: "Updated Korean phase-management narrative referencing sg-phase"
  key_links:
    - from: "skills/sg-phase/SKILL.md"
      to: "gsd-phase"
      via: "Skill(skill=\"gsd-phase\", ...) delegation for edit/remove"
      pattern: "Skill\\(skill=\"gsd-phase\""
    - from: "skills/sg-phase/SKILL.md"
      to: ".planning/ROADMAP.md"
      via: "complete: Read + Edit Progress table row and Phases checkbox"
      pattern: "ROADMAP\\.md"
---

<objective>
Create a new super-gsd skill `skills/sg-phase/SKILL.md` exposing `/super-gsd:sg-phase`, and update README.md + README.ko.md to reflect it. The skill routes by leading subcommand into three operations on a phase:

- **edit** — delegates to GSD's `gsd-phase` skill in `--edit` mode.
- **remove** — delegates to `gsd-phase` in `--remove` mode.
- **complete** — NEW inline reconcile logic (gsd-phase has no complete mode). It marks a phase as done by recomputing Plans-complete, updating the ROADMAP `## Progress` table row (Plans-complete / Status=Complete / Completed=today), flipping the `## Phases` checkbox `- [ ]` → `- [x]`, updating STATE.md, and optionally appending a HANDOFF.md row.

Purpose: Automate the phase-completion reconciliation that was done by hand for phases 36/37/38 this session. GSD has no native repair for the Progress-table status (gsd-health detects drift but does not fix it) — sg-phase complete fills that gap, while reusing gsd-phase for the edit/remove CRUD that already exists.

Output: One new SKILL.md and two synchronized README narrative edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Wrapper SKILL.md structure patterns (frontmatter + <language> + objective/execution_context/process/success_criteria)
@skills/sg-cleanup/SKILL.md
@skills/sg-ship/SKILL.md

# Exact gsd-phase flags to delegate to (--edit, --remove, --insert)
@$HOME/.claude/skills/gsd-phase/SKILL.md

# README narrative to rewrite (English ~line 50, Korean ~line 50)
@README.md
@README.ko.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write skills/sg-phase/SKILL.md</name>
  <files>skills/sg-phase/SKILL.md</files>
  <action>
Create the new skill following the wrapper structure used by skills/sg-ship/SKILL.md and skills/sg-cleanup/SKILL.md.

Frontmatter (English source prose per v2.7 i18n):
- name: sg-phase
- description: One sentence covering the three operations — edit/remove delegate to gsd-phase, complete reconciles phase-completion metadata that gsd-phase cannot.
- argument-hint: "edit <N> [changes] | remove <N> | complete [N]" (complete's N is optional; defaults to STATE.md current phase, matching sg-ship's resolution pattern).

REQUIRED top-of-body `<language>` block (copy the exact auto-detect directive verbatim from skills/sg-ship/SKILL.md lines 7-12): detect input language, respond in it; this governs surfaced prose. Machine tokens — command names (`/super-gsd:sg-*`, `gsd-phase`), enum values (edit/remove/complete, stage names, Status values like `Complete`/`Not started`), phase slugs, file paths, timestamps, version IDs (`vX.Y`) — stay verbatim, never translated.

`<objective>`: State that sg-phase performs three phase operations routed by leading subcommand: edit and remove delegate to gsd-phase; complete reconciles completion metadata (the gap gsd-phase lacks).

`<execution_context>`: Self-contained. Reads .planning/STATE.md, .planning/ROADMAP.md, and the phase directory. For edit/remove it delegates to the gsd-phase Skill (terminal handoff — control transfers). For complete it mutates planning files inline via Read + Edit and returns a summary.

`<process>` — Step 1 parses the leading subcommand token from $ARGUMENTS:
- If empty OR not one of edit/remove/complete → print a one-line usage string: `Usage: /super-gsd:sg-phase <edit|remove|complete> <N> [changes]` and exit (do NOT delegate, do NOT mutate). Surface the usage prose in the user's language; keep the command token verbatim.

Step 2 — `edit` route: strip the leading `edit` token, take the remainder (`<N> [changes]`), and delegate. Before invoking, replace placeholders with literal resolved values. Invocation (terminal — no steps run after):
`Skill(skill="gsd-phase", args="--edit <remainder>")`

Step 3 — `remove` route: strip the leading `remove` token, take the phase number N, and delegate (terminal):
`Skill(skill="gsd-phase", args="--remove <N>")`

Step 4 — `complete` route (inline reconcile; gsd-phase has NO complete mode — do NOT delegate). Sub-steps:
  4a. Resolve phase N. If a number follows `complete`, use it. Otherwise Read .planning/STATE.md and extract the Phase: value from the YAML frontmatter (mirror sg-ship Step 1 resolution). If still unresolved, print: `Could not resolve phase. Pass it explicitly: /super-gsd:sg-phase complete <N>` and exit.
  4b. Locate the phase directory with zero-padded two-digit support:
```bash
PHASE_PAD=$(printf "%02d" "$PHASE_NUM" 2>/dev/null || echo "$PHASE_NUM")
PHASE_DIR=$(ls -d .planning/phases/${PHASE_PAD}-* 2>/dev/null | head -1)
PHASE_SLUG=$(printf '%s' "$PHASE_DIR" | xargs basename 2>/dev/null || echo "${PHASE_PAD}")
```
  4c. Recompute Plans-complete. If $PHASE_DIR exists, count SUMMARY vs PLAN files; else emit the ad-hoc sentinel:
```bash
if [ -n "$PHASE_DIR" ] && [ -d "$PHASE_DIR" ]; then
  SUMS=$(ls "$PHASE_DIR"/*-SUMMARY.md 2>/dev/null | wc -l | tr -d ' ')
  PLANS=$(ls "$PHASE_DIR"/*-PLAN.md 2>/dev/null | wc -l | tr -d ' ')
  PLANS_COMPLETE="${SUMS}/${PLANS}"
else
  PLANS_COMPLETE="— (ad-hoc)"
fi
TODAY=$(date +%Y-%m-%d)
echo "phase=$PHASE_NUM dir=${PHASE_DIR:-none} plans_complete=$PLANS_COMPLETE date=$TODAY"
```
  4d. Update .planning/ROADMAP.md `## Progress` pipe table (columns `| Phase | Milestone | Plans Complete | Status | Completed |`). Use the Read tool to load the table, find the row whose Phase column begins with `<N>.` (table rows are NOT zero-padded, e.g. `| 36. ...`). Use the Edit tool to set Plans-complete = $PLANS_COMPLETE, Status = `Complete`, Completed = $TODAY. Do NOT parse the table with awk pipe-splitting or grep -P (CLAUDE.md macOS portability) — Read + Claude interpretation + Edit only.
  4e. Update .planning/ROADMAP.md `## Phases` section: find the line for the target phase and flip its checkbox `- [ ]` → `- [x]` via the Edit tool. Phase lines look like `- [ ] **Phase 36: ...**` or `- [ ] Phase 36: ...`; match on the phase number.
  4f. Update .planning/STATE.md Current Position / progress as appropriate via Edit (e.g. note the phase as complete; if the frontmatter `progress.completed_phases` is present and the row flip increased the count, reflect it). Keep edits surgical — only touch lines describing position/progress.
  4g. Optionally append a HANDOFF.md row (append-only — never modify existing rows). Reuse the sg-ship Step 1.5 header-guard + FROM_STAGE extraction pattern, then:
```bash
TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)
echo "| $TS | $PHASE_SLUG | $FROM_STAGE | complete | - |" >> .planning/HANDOFF.md
```
  4h. Print a short confirmation summarizing what changed (phase, plans-complete, status, date), surfaced in the user's language; machine tokens verbatim.

Do NOT bump plugin.json/package.json/CHANGELOG (version bumps only on the 배포 deploy trigger). Do NOT create a `.agents/skills/sg-phase` pair (out of scope).
  </action>
  <verify>
    <automated>test -f skills/sg-phase/SKILL.md && grep -q '^name: sg-phase' skills/sg-phase/SKILL.md && grep -q '<language>' skills/sg-phase/SKILL.md && grep -q 'Skill(skill="gsd-phase", args="--edit' skills/sg-phase/SKILL.md && grep -q 'Skill(skill="gsd-phase", args="--remove' skills/sg-phase/SKILL.md && grep -Eq 'complete' skills/sg-phase/SKILL.md && echo OK</automated>
  </verify>
  <done>skills/sg-phase/SKILL.md exists with sg-phase frontmatter + argument-hint, a `<language>` auto-detect block, edit/remove gsd-phase delegations, inline complete reconcile logic (phase resolution, dir lookup with zero-padding, Plans-complete count + ad-hoc fallback, ROADMAP Progress-row + Phases-checkbox edits, STATE.md update, optional HANDOFF append), and a usage-line guard for empty/unknown subcommands. No awk pipe-splitting / grep -P; no version bump; no .agents pair.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md and README.ko.md phase-management narrative</name>
  <files>README.md, README.ko.md</files>
  <action>
Rewrite the phase-management narrative in BOTH READMEs so they agree (the user just synced the command tables — keep parity).

README.md `## Phase management (add / insert / remove / edit)` section (~line 50-52). The current intro sentence claims super-gsd does NOT wrap gsd-phase and uses `/gsd:phase` directly. Replace that narrative: super-gsd now provides `/super-gsd:sg-phase` which wraps the edit and remove operations (delegating to GSD's `gsd-phase`) and adds a `complete` operation that gsd-phase lacks — reconciling a finished phase's ROADMAP Progress row, Phases checkbox, and STATE.md. Note that add/insert (and edit/remove directly) remain available via gsd-phase. Update the flag/usage table or add a short sg-phase usage line covering `sg-phase edit <N> [changes]`, `sg-phase remove <N>`, `sg-phase complete [N]`. Keep machine tokens verbatim.

README.ko.md `## Phase 관리 (추가 / 삽입 / 제거 / 편집)` section (~line 49-51): make the equivalent change in Korean. The current sentence is `super-gsd는 GSD의 phase CRUD를 wrapping하지 않는다...` — rewrite so it states sg-phase wraps edit/remove (delegating to gsd-phase) and adds the complete operation gsd-phase에는 없는 기능으로 제공한다. Mirror README.md's structure and the same usage entries; keep command tokens (`/super-gsd:sg-phase`, `gsd-phase`, edit/remove/complete) verbatim.

Surgical edits only — do not restructure unrelated sections, the command tables above, or the `--insert` example block. Every changed line must trace to the sg-phase introduction.
  </action>
  <verify>
    <automated>grep -q 'sg-phase' README.md && grep -q 'sg-phase' README.ko.md && ! grep -q 'super-gsd does not wrap' README.md && ! grep -q 'wrapping하지 않는다' README.ko.md && echo OK</automated>
  </verify>
  <done>Both READMEs reference `/super-gsd:sg-phase`, describe edit/remove delegation to gsd-phase plus the new complete operation, and the stale "does not wrap"/"wrapping하지 않는다" claims are gone. English and Korean narratives are in parity; no unrelated sections touched.</done>
</task>

</tasks>

<verification>
- `skills/sg-phase/SKILL.md` exists with all three routes and the usage guard.
- gsd-phase delegation strings present for edit and remove; complete logic is inline (no gsd-phase delegation in the complete path).
- ROADMAP/STATE mutations use Read + Edit (no awk pipe-splitting, no grep -P).
- Both READMEs updated and mutually consistent; stale "does not wrap" narrative removed.
- No version bump; no `.agents/skills/sg-phase` created.
</verification>

<success_criteria>
1. `/super-gsd:sg-phase edit <N> [changes]` delegates to `Skill(skill="gsd-phase", args="--edit ...")`.
2. `/super-gsd:sg-phase remove <N>` delegates to `Skill(skill="gsd-phase", args="--remove <N>")`.
3. `/super-gsd:sg-phase complete [N]` resolves the phase (arg or STATE.md), locates the zero-padded phase dir, recomputes Plans-complete (or `— (ad-hoc)`), updates the ROADMAP Progress row (Plans-complete / Status=Complete / Completed=today), flips the Phases checkbox to `- [x]`, updates STATE.md, and optionally appends a HANDOFF.md row.
4. Empty or unknown subcommand prints a single usage line and exits without delegating or mutating.
5. A `<language>` auto-detect directive is present; surfaced prose follows user language while machine tokens stay verbatim.
6. README.md and README.ko.md both describe sg-phase (edit/remove wrap + complete) and stay in sync; the prior "does not wrap" narrative is gone.
7. No version bump and no `.agents/skills/sg-phase` pair created.
</success_criteria>

<output>
Create `.planning/quick/260528-dv4-create-sg-phase-skill-wrapping-gsd-phase/260528-dv4-SUMMARY.md` when done.
</output>
