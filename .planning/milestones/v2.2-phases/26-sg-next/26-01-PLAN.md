---
phase: 26-sg-next
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-next/SKILL.md
autonomous: true
requirements:
  - NEXT-01
  - NEXT-02
  - NEXT-03
  - NEXT-04
  - NEXT-05

must_haves:
  truths:
    - "skills/sg-next/SKILL.md가 존재하고 유효한 YAML frontmatter를 갖는다"
    - "HANDOFF.md + STATE.md를 읽어 현재 stage를 감지한다"
    - "stage → next-command 라우팅은 sg-status와 동일한 11개 분기를 사용한다"
    - "라우팅 후 1줄(→ /super-gsd:sg-[cmd]) 출력 후 확인 없이 즉시 Skill() invoke한다"
    - "complete/init 상태에서는 AskUserQuestion으로 선택지를 제시한다"
    - "Skill() invoke 전에 HANDOFF.md에 To: sg-next 행이 append된다"
  artifacts:
    - path: "skills/sg-next/SKILL.md"
      provides: "sg-next 스킬 전체 정의"
      contains: "name: sg-next"
    - path: "skills/sg-next/SKILL.md"
      provides: "D-07 inline-replication 블록 3개"
      contains: "BEGIN STATE.md Phase parsing block"
    - path: "skills/sg-next/SKILL.md"
      provides: "HANDOFF append 블록"
      contains: "sg-next | -"
  key_links:
    - from: "skills/sg-next/SKILL.md (STAGE_RAW 파싱)"
      to: ".planning/HANDOFF.md"
      via: "grep -E '^\\| [0-9]{4}-' | tail -1 | awk -F'|'"
      pattern: "LAST_ROW.*awk.*\\|.*5"
    - from: "skills/sg-next/SKILL.md (NEXT_CMD 결정)"
      to: "Skill() invoke"
      via: "case \"$STAGE_RAW\" in ... esac → Skill()"
      pattern: "Skill.*super-gsd:sg-"
---

<objective>
`skills/sg-next/SKILL.md`를 신규 생성한다. 이 스킬은 HANDOFF.md의 마지막 행과 STATE.md의 Phase 라인을 읽어 현재 stage를 감지하고, sg-status와 동일한 라우팅 테이블로 다음 sg-* 명령을 결정한 뒤, 1줄 출력 후 확인 없이 즉시 Skill()로 invoke한다. complete/init 상태에서만 AskUserQuestion으로 사용자에게 선택지를 제시하며, invoke 전에 HANDOFF.md에 감사 로그 행을 append한다.

Purpose: sg-next 한 명령으로 워크플로우의 다음 단계를 자동으로 전진시켜, 사용자가 다음 명령을 기억하거나 입력할 필요 없이 루프를 유지한다.
Output: skills/sg-next/SKILL.md (신규 파일 1개)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-sg-next/26-CONTEXT.md
@.planning/phases/26-sg-next/26-PATTERNS.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 복제 기준 파일 — 아래 블록을 sg-next에 그대로 가져온다 (D-05) -->

skills/sg-status/SKILL.md (lines 17-21) — STATE.md Phase 파싱 블록:
```bash
# --- BEGIN STATE.md Phase parsing block (D-07: Phase 8 sg-start이 동일 블록을 복제) ---
PHASE_LINE=$(grep -E '^Phase:' .planning/STATE.md 2>/dev/null | head -1 | sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//')
[ -z "$PHASE_LINE" ] && PHASE_LINE="(none)"
PHASE_NUM=$(echo "$PHASE_LINE" | grep -oE '^[0-9]+' || echo "")
# --- END STATE.md Phase parsing block ---
```

skills/sg-status/SKILL.md (lines 26-52) — HANDOFF 파싱 + enum 매핑 블록:
```bash
LAST_ROW=$(grep -E '^\| [0-9]{4}-' .planning/HANDOFF.md 2>/dev/null | tail -1)
if [ -z "$LAST_ROW" ]; then
  STAGE_RAW="init"
  TS=""
else
  STAGE_RAW=$(echo "$LAST_ROW" | awk -F'|' '{gsub(/ /,"",$5); print $5}')
  TS=$(echo "$LAST_ROW" | awk -F'|' '{gsub(/ /,"",$2); print $2}')
  case "$STAGE_RAW" in
    gsd-plan|ui-plan|superpowers|parallel|execute|review|sg-retro|hookify|ship|complete) ;;
    *) echo "Unknown stage '${STAGE_RAW}' in .planning/HANDOFF.md last row. Schema may be corrupted." >&2; exit 1 ;;
  esac
fi
case "$STAGE_RAW" in
  init)         STAGE_DISPLAY="init" ;;
  gsd-plan)     STAGE_DISPLAY="gsd" ;;
  ui-plan)      STAGE_DISPLAY="gsd" ;;
  superpowers)  STAGE_DISPLAY="superpowers" ;;
  parallel)     STAGE_DISPLAY="superpowers" ;;
  execute)      STAGE_DISPLAY="superpowers" ;;
  review)       STAGE_DISPLAY="superpowers" ;;
  sg-retro)     STAGE_DISPLAY="hookify" ;;
  hookify)      STAGE_DISPLAY="hookify" ;;
  ship)         STAGE_DISPLAY="ship" ;;
  complete)     STAGE_DISPLAY="complete" ;;
esac
```

skills/sg-status/SKILL.md (lines 66-107) — NEXT_PHASE 계산 + stage→next-command case 블록:
```bash
if [ "$STAGE_RAW" = "hookify" ] || [ "$STAGE_RAW" = "ship" ]; then
  if echo "$PHASE_NUM" | grep -qE '^[0-9]+$'; then
    NEXT_PHASE=$((PHASE_NUM + 1))
    if grep -qE "^### Phase ${NEXT_PHASE}:" .planning/ROADMAP.md 2>/dev/null; then
      NEXT_PHASE_EXISTS=1
    else
      NEXT_PHASE_EXISTS=0
    fi
  else
    NEXT_PHASE_EXISTS=0
  fi
fi
case "$STAGE_RAW" in
  init)
    if [ -n "$PHASE_NUM" ]; then
      NEXT_CMD="/super-gsd:sg-plan $PHASE_NUM"
    else
      NEXT_CMD="/super-gsd:sg-plan"
    fi
    ;;
  gsd-plan)    NEXT_CMD="/super-gsd:sg-execute" ;;
  ui-plan)     NEXT_CMD="/super-gsd:sg-execute" ;;
  superpowers) NEXT_CMD="/super-gsd:sg-review" ;;
  parallel)    NEXT_CMD="/super-gsd:sg-review" ;;
  execute)     NEXT_CMD="/super-gsd:sg-review" ;;
  review)      NEXT_CMD="/super-gsd:sg-learn" ;;
  sg-retro)    NEXT_CMD="/super-gsd:sg-ship" ;;
  hookify)     NEXT_CMD="/super-gsd:sg-ship" ;;
  ship)
    if [ "$NEXT_PHASE_EXISTS" = "1" ]; then
      NEXT_CMD="/super-gsd:sg-plan $NEXT_PHASE"
    else
      NEXT_CMD="/super-gsd:sg-complete"
    fi
    ;;
  complete) NEXT_CMD="/super-gsd:sg-new" ;;
  *) NEXT_CMD="(unknown stage: $STAGE_RAW)" ;;
esac
```

HANDOFF.md 스키마 (5컬럼):
```
| Timestamp | Phase | From | To | Plan Hash |
| --------- | ----- | ---- | -- | --------- |
| 2026-05-23T00:00:00Z | 26-sg-next | [STAGE_RAW] | sg-next | - |
```

AskUserQuestion 패턴 (skills/sg-start/SKILL.md lines 165-179 참조):
- header: "sg-next" (12자 이하)
- multiSelect: false
- "취소" 선택 시: "Cancelled. No changes made." emit 후 종료
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: skills/sg-next/SKILL.md 신규 생성 (HANDOFF 파싱 + 라우팅 + invoke)</name>
  <files>skills/sg-next/SKILL.md</files>

  <read_first>
    - skills/sg-status/SKILL.md — D-05: 복제 기준 파일. STATE.md 파싱 블록(lines 17-21), HANDOFF 파싱+enum 블록(lines 26-52), NEXT_PHASE+routing case 블록(lines 66-107) 전체를 글자 그대로 복제한다.
    - skills/sg-start/SKILL.md — D-07 inline-replication 적용 실례. AskUserQuestion 패턴(lines 165-179) 참조.
    - skills/sg-execute/SKILL.md — HANDOFF append 패턴(lines 123-130, 258) 참조. invoke 전 append 순서 확인.
    - .planning/phases/26-sg-next/26-CONTEXT.md — D-01~D-06 결정사항 전체.
    - .planning/phases/26-sg-next/26-PATTERNS.md — YAML frontmatter 패턴, HANDOFF append 패턴, AskUserQuestion 예시.
  </read_first>

  <action>
`skills/sg-next/` 디렉토리를 생성하고 `skills/sg-next/SKILL.md`를 아래 구조로 작성한다.

**YAML frontmatter** (D-05, 25-CONTEXT.md D-03/D-04 패턴):
- `name: sg-next`
- `description: "Use this when you want to advance to the next workflow stage automatically — reads HANDOFF.md and STATE.md to determine the current stage and immediately invokes the next sg-* command."`
- `argument-hint` 생략 (sg-next는 인자 없음)

**`<objective>` 블록**: 1-2문장. HANDOFF.md + STATE.md로 현재 stage를 감지하고 다음 sg-* 명령을 확인 없이 즉시 invoke하며, complete/init에서만 AskUserQuestion을 제시한다.

**`<execution_context>` 블록**: "Self-contained — reads .planning/HANDOFF.md, .planning/STATE.md, .planning/ROADMAP.md. Appends .planning/HANDOFF.md."

**`<process>` 블록** — 6개 step으로 구성:

Step 1 — STATE.md Phase 파싱 (D-07 inline-replication):
복제 블록 상단 주석: `# --- BEGIN STATE.md Phase parsing block (D-07: skills/sg-status/SKILL.md 복제 — drift 시 양쪽 동시 수정) ---`
sg-status/SKILL.md lines 17-21 코드를 글자 그대로 복제. 복제 블록 하단 주석: `# --- END STATE.md Phase parsing block ---`

Step 2 — HANDOFF.md stage 감지 + enum 매핑 (D-07 inline-replication):
복제 블록 상단 주석: `# --- BEGIN HANDOFF.md stage detection block (D-07: skills/sg-status/SKILL.md 복제 — drift 시 양쪽 동시 수정) ---`
sg-status/SKILL.md lines 26-52 코드를 글자 그대로 복제. 복제 블록 하단 주석: `# --- END HANDOFF.md stage detection block ---`

Step 3 — NEXT_PHASE 계산 + stage→next-command 라우팅 (D-07 inline-replication):
복제 블록 상단 주석: `# --- BEGIN next-command routing block (D-07: skills/sg-status/SKILL.md 복제 — drift 시 양쪽 동시 수정) ---`
sg-status/SKILL.md lines 66-107 코드를 글자 그대로 복제. 복제 블록 하단 주석: `# --- END next-command routing block ---`

Step 4 — HANDOFF.md append (D-04: invoke 전 기록):
아래 Bash 블록을 그대로 포함한다 (26-PATTERNS.md "HANDOFF.md append 패턴" 기준):
```bash
HANDOFF_FILE=".planning/HANDOFF.md"
if [ ! -f "$HANDOFF_FILE" ] || ! grep -q "Timestamp.*Phase.*From.*To.*Plan Hash" "$HANDOFF_FILE" 2>/dev/null; then
  mkdir -p "$(dirname "$HANDOFF_FILE")"
  printf '| Timestamp | Phase | From | To | Plan Hash |\n| --- | --- | --- | --- | --- |\n' > "$HANDOFF_FILE"
fi
TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)
PHASE_PAD=$(printf "%02d" "${PHASE_NUM:-0}" 2>/dev/null || echo "${PHASE_NUM:-0}")
PHASE_SLUG=$(ls -d .planning/phases/${PHASE_PAD}-* 2>/dev/null | head -1 | xargs basename 2>/dev/null)
[ -z "$PHASE_SLUG" ] && PHASE_SLUG="${PHASE_NUM:-unknown}"
FROM_STAGE=$(grep -E '^\| [0-9]{4}-' "$HANDOFF_FILE" | tail -1 | awk -F'|' '{gsub(/ /,"",$5); print $5}')
[ -z "$FROM_STAGE" ] && FROM_STAGE="init"
echo "| $TS | $PHASE_SLUG | $FROM_STAGE | sg-next | - |" >> "$HANDOFF_FILE"
```

Step 5 — complete/init 분기 (D-02, D-03):
`STAGE_RAW`가 `complete` 또는 `init`일 때 AskUserQuestion을 호출한다.

`complete` 상태:
```
AskUserQuestion(
  questions: [{
    question: "현재 Phase가 완료 상태입니다. 다음 단계를 선택하세요.",
    header: "sg-next",
    multiSelect: false,
    options: [
      { label: "sg-new 실행 (새 마일스톤 시작)", description: "Skill(skill='super-gsd:sg-new', args='')를 호출합니다." },
      { label: "취소", description: "변경 없이 종료합니다." }
    ]
  }]
)
```
- "sg-new 실행" 선택 시: `Skill(skill="super-gsd:sg-new", args="")`
- "취소" 선택 시: `Cancelled. No changes made.` emit 후 종료

`init` 상태:
```
AskUserQuestion(
  questions: [{
    question: "현재 단계를 감지할 수 없습니다 (init). 다음 단계를 선택하세요.",
    header: "sg-next",
    multiSelect: false,
    options: [
      { label: "[N이 있으면 'sg-plan N 실행', 없으면 'sg-plan 실행']", description: "/super-gsd:sg-plan $PHASE_NUM 을 호출합니다." },
      { label: "취소", description: "변경 없이 종료합니다." }
    ]
  }]
)
```
- PHASE_NUM이 있으면 label을 `"sg-plan $PHASE_NUM 실행"`, description을 `"/super-gsd:sg-plan $PHASE_NUM 을 호출합니다."` 로 구성한다.
- PHASE_NUM이 없으면 label을 `"sg-plan 실행"`, description을 `"/super-gsd:sg-plan 을 호출합니다."` 로 구성한다.
- "sg-plan" 선택 시: PHASE_NUM이 있으면 `Skill(skill="super-gsd:sg-plan", args="$PHASE_NUM")`, 없으면 `Skill(skill="super-gsd:sg-plan", args="")`
- "취소" 선택 시: `Cancelled. No changes made.` emit 후 종료

Step 6 — 1줄 출력 후 즉시 invoke (NEXT-03):
complete/init 이외의 모든 stage에서 실행된다:
```bash
echo "→ $NEXT_CMD"
# Before calling Skill, substitute the actual resolved command name.
# Session control transfers to the skill; no steps execute after this point.
```
이어서 `NEXT_CMD`에서 `/super-gsd:` 프리픽스를 제거한 skill name으로 Skill()을 호출한다. 예시:
- `NEXT_CMD="/super-gsd:sg-execute"` → `Skill(skill="super-gsd:sg-execute", args="")`
- `NEXT_CMD="/super-gsd:sg-plan 26"` → `Skill(skill="super-gsd:sg-plan", args="26")`
- `NEXT_CMD="/super-gsd:sg-review"` → `Skill(skill="super-gsd:sg-review", args="")`

**`<success_criteria>` 블록** — 5개 항목:
1. HANDOFF.md가 없거나 데이터 행이 0개이면 STAGE_RAW=init으로 처리한다 (NEXT-01).
2. stage→next-command 매핑이 sg-status와 동일한 11개 분기를 사용한다 (NEXT-02).
3. complete/init 이외 모든 stage에서 `→ /super-gsd:sg-[cmd]` 1줄을 출력한 뒤 즉시 Skill()로 invoke한다 — 확인 프롬프트 없음 (NEXT-03).
4. STAGE_RAW가 complete 또는 init이면 AskUserQuestion을 호출하고, 취소 선택 시 `Cancelled. No changes made.`를 emit 후 종료한다 (NEXT-04).
5. Skill() invoke 전에 HANDOFF.md에 `| TS | PHASE_SLUG | FROM_STAGE | sg-next | - |` 행이 append된다 (NEXT-05, D-04).
  </action>

  <verify>
    <automated>
      test -f skills/sg-next/SKILL.md || (echo "FAIL: 파일 없음" &amp;&amp; exit 1)
      grep -c 'name: sg-next' skills/sg-next/SKILL.md | grep -q '^1$' || (echo "FAIL: frontmatter name" &amp;&amp; exit 1)
      grep -c 'Use this when' skills/sg-next/SKILL.md | grep -qE '^[1-9]' || (echo "FAIL: description 패턴 없음" &amp;&amp; exit 1)
      grep -c 'BEGIN STATE.md Phase parsing block' skills/sg-next/SKILL.md | grep -q '^1$' || (echo "FAIL: STATE.md 파싱 블록 없음" &amp;&amp; exit 1)
      grep -c 'BEGIN HANDOFF.md stage detection block' skills/sg-next/SKILL.md | grep -q '^1$' || (echo "FAIL: HANDOFF 파싱 블록 없음" &amp;&amp; exit 1)
      grep -c 'BEGIN next-command routing block' skills/sg-next/SKILL.md | grep -q '^1$' || (echo "FAIL: 라우팅 블록 없음" &amp;&amp; exit 1)
      grep -c 'sg-next | -' skills/sg-next/SKILL.md | grep -qE '^[1-9]' || (echo "FAIL: HANDOFF append sg-next 행 없음" &amp;&amp; exit 1)
      grep -c 'AskUserQuestion' skills/sg-next/SKILL.md | grep -qE '^[1-9]' || (echo "FAIL: AskUserQuestion 없음" &amp;&amp; exit 1)
      grep -c 'Cancelled. No changes made.' skills/sg-next/SKILL.md | grep -qE '^[1-9]' || (echo "FAIL: Cancel 분기 없음" &amp;&amp; exit 1)
      grep -c 'drift 시 양쪽 동시 수정' skills/sg-next/SKILL.md | grep -qE '^3$' || (echo "FAIL: D-07 복제 주석 3개 있어야 함" &amp;&amp; exit 1)
      echo "ALL CHECKS PASSED"
    </automated>
  </verify>

  <acceptance_criteria>
    - skills/sg-next/SKILL.md 파일이 존재한다
    - YAML frontmatter에 `name: sg-next`와 `description: "Use this when ..."` 형식의 단일 줄 description이 있다
    - `<objective>`, `<execution_context>`, `<process>`, `<success_criteria>` 블록 4개가 모두 존재한다
    - D-07 BEGIN/END 주석 쌍이 3개 존재한다 (STATE.md 파싱, HANDOFF 파싱+enum, routing case)
    - 각 복제 블록에 "skills/sg-status/SKILL.md 복제 — drift 시 양쪽 동시 수정" 주석이 포함된다
    - HANDOFF append 블록에 `sg-next | -` 행 형식이 포함된다 (Plan Hash = `-`)
    - AskUserQuestion이 complete/init 분기 모두를 처리한다
    - "취소" 선택 시 `Cancelled. No changes made.` emit 코드가 있다
    - Skill() invoke 이전에 HANDOFF append 단계가 배치된다 (D-04)
    - `grep -P`를 사용하지 않는다 (macOS 이식성 — CLAUDE.md)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → HANDOFF.md | Skill이 파일에 행을 append — 경로 조작 불가 (경로 하드코딩) |
| SKILL.md → Skill() invoke | stage 라우팅 결과로 다른 Skill을 호출 — NEXT_CMD는 내부 case 블록에서만 생성됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | HANDOFF.md append | mitigate | 경로를 `.planning/HANDOFF.md` 리터럴로 하드코딩 — 환경변수/인자로 override 불가 |
| T-26-02 | Spoofing | STAGE_RAW 파싱 | mitigate | case 블록에서 허용 목록 외 값은 exit 1로 거부 (sg-status 복제 패턴 유지) |
| T-26-03 | Information Disclosure | STATE.md 읽기 실패 | accept | 파일 없으면 PHASE_LINE="(none)" fallback — 민감 정보 노출 없음 |
| T-26-04 | Denial of Service | AskUserQuestion 루프 | accept | 취소 선택 시 즉시 종료 — 재귀 invoke 없음 |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | 이 Plan에 패키지 설치 없음 — 순수 SKILL.md 생성 |
</threat_model>

<verification>
파일 검증:

```bash
# 파일 존재
test -f skills/sg-next/SKILL.md

# frontmatter 필수 필드
grep -c 'name: sg-next' skills/sg-next/SKILL.md

# D-07 복제 주석 3쌍 (STATE.md, HANDOFF, routing)
grep -c 'drift 시 양쪽 동시 수정' skills/sg-next/SKILL.md
# 출력 = 3

# HANDOFF append To: sg-next 포함
grep 'sg-next | -' skills/sg-next/SKILL.md

# macOS 이식성 — grep -P 미사용 확인
grep -n 'grep -P' skills/sg-next/SKILL.md | wc -l
# 출력 = 0

# AskUserQuestion 존재
grep -c 'AskUserQuestion' skills/sg-next/SKILL.md
```
</verification>

<success_criteria>
1. `skills/sg-next/SKILL.md`가 존재하고 YAML frontmatter(`name`, `description`)와 4개 블록(`<objective>`, `<execution_context>`, `<process>`, `<success_criteria>`)을 갖는다 (NEXT-01).
2. `<process>` 안에 sg-status에서 복제된 D-07 inline-replication 블록 3개(STATE.md 파싱, HANDOFF 파싱+enum, next-command routing)가 정확히 포함된다 (NEXT-01, NEXT-02).
3. HANDOFF append 단계가 Skill() invoke 단계 앞에 위치하며, `| ... | sg-next | - |` 행 형식을 사용한다 (NEXT-05, D-04).
4. complete/init 상태에서 AskUserQuestion이 호출되고, 취소 선택 시 `Cancelled. No changes made.`로 종료한다 (NEXT-04, D-02, D-03).
5. 그 외 모든 stage에서 `→ /super-gsd:sg-[cmd]` 1줄 출력 후 Skill()로 즉시 invoke한다 (NEXT-03).
</success_criteria>

<output>
완료 후 `.planning/phases/26-sg-next/26-01-SUMMARY.md`를 생성한다.
</output>
