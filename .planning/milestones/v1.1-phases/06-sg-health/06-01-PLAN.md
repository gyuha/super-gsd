---
phase: "06-sg-health"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-health.md
autonomous: true
requirements:
  - HEALTH-01
  - HEALTH-02
  - HEALTH-03
  - HEALTH-04
  - HEALTH-05

must_haves:
  truths:
    - "D-01: GSD 설치 여부를 ~/.claude/get-shit-done/ 디렉토리 존재로 판정하여 [OK] 또는 [FAIL]로 출력한다"
    - "D-02: Superpowers 설치 여부를 ~/.claude/plugins/data/superpowers-claude-plugins-official 디렉토리 존재로 판정하여 [OK] 또는 [FAIL]로 출력한다"
    - "D-03: Hookify 설치 여부를 ~/.claude/plugins/data/hookify-claude-plugins-official 디렉토리 존재로 판정하여 [OK] 또는 [FAIL]로 출력한다"
    - "D-04: commands/sg-health.md 파일만 생성한다 (별도 Python 스크립트 없음)"
    - "D-05: 출력 형식은 라인별 [OK]/[WARN]/[FAIL] + 마지막 요약 줄"
    - "sg-health를 실행하면 Stop hook과 SubagentStop hook 등록 여부가 각각 [OK] 또는 [FAIL]로 출력된다"
    - "sg-health를 실행하면 .planning/HANDOFF.md 스키마 상태가 [OK]/[WARN]/[FAIL]로 출력된다"
    - "sg-health를 실행하면 .planning/STATE.md frontmatter 파싱 가능 여부가 [OK]/[WARN]/[FAIL]로 출력된다"
    - "sg-health 실행 후 .planning/ 디렉토리 안의 어떤 파일도 생성되거나 수정되지 않는다"
  artifacts:
    - path: "commands/sg-health.md"
      provides: "sg-health 진단 명령 파일"
      contains: "name: sg-health"
  key_links:
    - from: "commands/sg-health.md"
      to: "hooks/hooks.json"
      via: "bash cat + grep for Stop/SubagentStop keys"
      pattern: "grep.*SubagentStop.*hooks.json"
    - from: "commands/sg-health.md"
      to: ".planning/HANDOFF.md"
      via: "bash grep for TSV data rows"
      pattern: "grep.*HANDOFF.md"
---

<objective>
`commands/sg-health.md` 명령 파일을 생성하여 GSD/Superpowers/Hookify 설치 상태, hooks.json 훅 등록, HANDOFF.md 스키마, STATE.md frontmatter를 라인별 [OK]/[WARN]/[FAIL]로 진단하고 요약 줄로 끝내는 읽기 전용 명령을 구현한다.

Purpose: 사용자가 단일 명령으로 super-gsd 워크플로우 전제 조건을 전부 점검할 수 있다.
Output: commands/sg-health.md (신규 생성)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sg-health/06-CONTEXT.md

<interfaces>
<!-- commands/sg-status.md 구조 — sg-health도 동일 패턴 적용 -->
<!-- 파일 구조: YAML frontmatter + <objective> + <execution_context> + <process> + <success_criteria> -->
<!-- process 섹션에 bash 블록으로 읽기 로직 기술 -->
<!-- 파일 쓰기 없음 — cat/grep으로만 읽음 -->

hooks/hooks.json 현재 상태:
  - "Stop" 키 존재 (hooks 배열에 command 항목 있음)
  - "SubagentStop" 키 없음
  → sg-health 실행 시 Stop hook: [OK], SubagentStop hook: [FAIL] 예상

.planning/HANDOFF.md 스키마:
  - 5컬럼 TSV: Timestamp | Phase | From | To | Plan Hash
  - 데이터 행 없으면 [WARN] (아직 인계 없음 — 정상)
  - 헤더/구분자만 있어도 [WARN] 허용
  - 데이터 행이 있는데 컬럼 수가 5가 아니면 [FAIL]

판정 기준 (Claude's Discretion from CONTEXT.md):
  - 설치 디렉토리 없음 → [FAIL]
  - hooks.json에 Stop/SubagentStop 중 하나라도 없음 → [FAIL]
  - HANDOFF.md 없음 → [WARN] (아직 사용 전 정상 상태)
  - HANDOFF.md 있는데 데이터 행 컬럼 수 != 5 → [FAIL]
  - STATE.md 없음 → [WARN]
  - STATE.md 있는데 frontmatter (---...---) 파싱 실패 → [FAIL]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: commands/sg-health.md 생성</name>
  <files>commands/sg-health.md</files>
  <read_first>
    - commands/sg-status.md — 명령 파일 구조 패턴 (frontmatter + objective + process + success_criteria)
    - hooks/hooks.json — Stop/SubagentStop 키 구조 확인 (검증 로직 기준)
    - .planning/HANDOFF.md — 5컬럼 TSV 스키마 확인 (헤더 행 형식)
    - .planning/phases/06-sg-health/06-CONTEXT.md — D-01~D-05 결정 사항, 출력 형식 예시
  </read_first>
  <action>
    commands/sg-health.md를 신규 생성한다. sg-status.md와 동일한 파일 구조(YAML frontmatter → objective → execution_context → process → success_criteria)를 사용한다.

    frontmatter:
    - name: sg-health
    - description: Diagnose super-gsd installation status — GSD, Superpowers, Hookify, hooks, HANDOFF.md, STATE.md

    process 섹션에서 아래 7가지 항목을 순서대로 점검하고 각각 한 줄씩 출력한다. FAIL/WARN 카운터를 누적하여 마지막에 요약 줄을 출력한다. 파일을 생성하거나 수정하는 명령(>, >>, tee, sed -i, rm 등)은 일절 사용하지 않는다.

    점검 항목 및 판정 기준 (per D-01~D-05):

    1. GSD 설치 (per D-01):
       - `~/.claude/get-shit-done/` 디렉토리 존재 여부를 `test -d "$HOME/.claude/get-shit-done"` 로 확인
       - 존재 → `GSD .............. [OK]`
       - 없음 → `GSD .............. [FAIL] ~/.claude/get-shit-done/ 디렉토리 없음`, FAIL++

    2. Superpowers 설치 (per D-02):
       - `test -d "$HOME/.claude/plugins/data/superpowers-claude-plugins-official"` 로 확인
       - 존재 → `Superpowers ...... [OK]`
       - 없음 → `Superpowers ...... [FAIL] 디렉토리 없음`, FAIL++

    3. Hookify 설치 (per D-03):
       - `test -d "$HOME/.claude/plugins/data/hookify-claude-plugins-official"` 로 확인
       - 존재 → `Hookify .......... [OK]`
       - 없음 → `Hookify .......... [FAIL] 디렉토리 없음`, FAIL++

    4. Stop hook 등록 (per HEALTH-02):
       - hooks/hooks.json을 cat으로 읽어 `"Stop"` 키가 최상위 hooks 객체에 있는지 `grep -q '"Stop"'` 로 확인
       - 있음 → `Stop hook ........ [OK]`
       - 없음 → `Stop hook ........ [FAIL] hooks.json에 Stop 훅 없음`, FAIL++

    5. SubagentStop hook 등록 (per HEALTH-02):
       - 같은 파일에서 `grep -q '"SubagentStop"'` 로 확인
       - 있음 → `SubagentStop hook  [OK]`
       - 없음 → `SubagentStop hook  [FAIL] hooks.json에 SubagentStop 훅 없음`, FAIL++

    6. HANDOFF.md 스키마 (per HEALTH-03):
       - 파일 없음: `test -f .planning/HANDOFF.md` 실패 → `HANDOFF.md ....... [WARN] 파일 없음 (아직 인계 없음)`, WARN++
       - 파일 있음: 데이터 행(`grep -E '^\| [0-9]{4}-'`)을 추출한 후:
         - 데이터 행 없음 → `HANDOFF.md ....... [WARN] 데이터 행 없음 (아직 인계 없음)`, WARN++
         - 데이터 행 있고 첫 번째 데이터 행의 파이프 구분 컬럼 수가 7이 아니면(| c1 | c2 | c3 | c4 | c5 | 구조에서 awk -F'|' '{print NF}'가 7임) → `HANDOFF.md ....... [FAIL] 스키마 손상 (5컬럼 TSV 아님)`, FAIL++
         - 컬럼 수 정상 → `HANDOFF.md ....... [OK]`

    7. STATE.md frontmatter (per HEALTH-04):
       - 파일 없음: `test -f .planning/STATE.md` 실패 → `STATE.md ......... [WARN] 파일 없음`, WARN++
       - 파일 있음: `grep -c '^---$' .planning/STATE.md` 결과가 2 이상이면 frontmatter 존재로 판정 → `STATE.md ......... [OK]`
       - 파일 있는데 `---` 구분자가 2개 미만 → `STATE.md ......... [FAIL] frontmatter 파싱 불가 (--- 구분자 없음)`, FAIL++

    마지막 출력:
    - 빈 줄 한 개
    - FAIL == 0 && WARN == 0: `모든 항목 정상입니다.`
    - 그 외: `[FAIL] ${FAIL}개, [WARN] ${WARN}개 — 위 항목을 확인하세요.`

    출력 형식은 D-05 예시와 정확히 일치해야 한다 (점 패딩으로 항목명 우측 정렬).
  </action>
  <verify>
    <automated>grep -c 'name: sg-health' /Users/gyuha/workspace/super-gsd/commands/sg-health.md && grep -c 'SubagentStop' /Users/gyuha/workspace/super-gsd/commands/sg-health.md && grep -c 'HANDOFF.md' /Users/gyuha/workspace/super-gsd/commands/sg-health.md && grep -c 'STATE.md' /Users/gyuha/workspace/super-gsd/commands/sg-health.md</automated>
  </verify>
  <acceptance_criteria>
    - commands/sg-health.md 파일이 존재한다
    - frontmatter에 `name: sg-health`가 정확히 있다
    - 7개 점검 항목(GSD, Superpowers, Hookify, Stop hook, SubagentStop hook, HANDOFF.md, STATE.md)이 모두 process 섹션에 있다
    - 각 항목의 판정 로직에 `test -d`, `grep -q`, `test -f` 같은 읽기 전용 명령만 사용한다 (>, >>가 없다)
    - 요약 줄 출력 로직(FAIL/WARN 카운터 집계)이 포함되어 있다
    - `grep -c '^>' commands/sg-health.md` 결과가 0이다 (행 시작의 '>' 리다이렉트 연산자 없음, HEALTH-05 보장)
  </acceptance_criteria>
  <done>commands/sg-health.md가 존재하고, 7개 진단 항목 + 요약 줄을 포함하며, 파일 쓰기 연산자를 포함하지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 명령 파일 → 파일시스템 | sg-health가 읽는 경로(~/.claude/*, hooks/hooks.json, .planning/*)는 신뢰할 수 있는 로컬 파일시스템 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | sg-health process section | mitigate | 파일 쓰기 연산자(>, >>, tee) 완전 배제 — acceptance_criteria에서 grep으로 검증 |
| T-06-02 | Information Disclosure | hooks/hooks.json 경로 노출 | accept | 로컬 플러그인 설정 파일, 민감 정보 없음 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜에 패키지 설치 없음 |
</threat_model>

<verification>
1. `grep -c 'name: sg-health' commands/sg-health.md` → 1
2. `grep -c 'SubagentStop' commands/sg-health.md` → 1 이상
3. `grep -c '^>' commands/sg-health.md` → 0 (파일 쓰기 없음)
4. `grep -c 'WARN' commands/sg-health.md` → 1 이상
5. `grep -c 'FAIL' commands/sg-health.md` → 1 이상
</verification>

<success_criteria>
- commands/sg-health.md가 존재하고 sg-status.md와 동일한 명령 파일 구조를 따른다
- 7개 진단 항목이 모두 포함된다 (GSD, Superpowers, Hookify, Stop hook, SubagentStop hook, HANDOFF.md, STATE.md)
- 각 항목의 판정 결과가 D-05 형식([OK]/[WARN]/[FAIL] + 설명)으로 출력된다
- 요약 줄이 마지막에 출력된다
- 파일 생성/수정 연산자가 전혀 없다 (HEALTH-05)
</success_criteria>

<output>
완료 시 `.planning/phases/06-sg-health/06-01-SUMMARY.md`를 생성한다
</output>
