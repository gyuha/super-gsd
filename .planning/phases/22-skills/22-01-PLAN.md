---
phase: 22
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - skills/sg-plan/SKILL.md
  - skills/sg-execute/SKILL.md
autonomous: true
requirements:
  - SC-01
  - SC-05
  - SC-06

must_haves:
  truths:
    - "skills/sg-plan/SKILL.md 가 존재하고 name, description, argument-hint frontmatter를 포함한다"
    - "skills/sg-execute/SKILL.md 가 존재하고 name, description, argument-hint frontmatter를 포함한다"
    - "두 파일 모두 <objective>, <process>, <success_criteria> 블록을 포함한다"
    - "sg-execute SKILL.md 에 HANDOFF 로직이 보존된다"
  artifacts:
    - path: "skills/sg-plan/SKILL.md"
      provides: "sg-plan 슬래시 명령 정의"
      contains: "name: sg-plan"
    - path: "skills/sg-execute/SKILL.md"
      provides: "sg-execute 슬래시 명령 정의 (HANDOFF 포함)"
      contains: "name: sg-execute"
---

<objective>
SC-01 구현: `skills/sg-plan/SKILL.md` 와 `skills/sg-execute/SKILL.md` 를 생성한다.
각 파일은 `commands/sg-plan.md`, `commands/sg-execute.md` 의 내용을 그대로 복사하되, YAML frontmatter만 skills 형식으로 유지한다.

Output artifacts:
- `skills/sg-plan/SKILL.md`
- `skills/sg-execute/SKILL.md`
</objective>

<tasks>
<task>
<name>skills/sg-plan/ 디렉토리 및 SKILL.md 생성</name>
<files>skills/sg-plan/SKILL.md</files>
<action>
1. `mkdir -p skills/sg-plan` 실행
2. `commands/sg-plan.md` 전체 내용을 읽는다
3. `skills/sg-plan/SKILL.md` 를 동일 내용으로 생성한다 (frontmatter 포함 전체 복사)
</action>
<verify>
- `ls skills/sg-plan/SKILL.md` 가 존재
- `grep -c 'name: sg-plan' skills/sg-plan/SKILL.md` 가 1 이상 (SC-05: name 키)
- `grep -c 'description:' skills/sg-plan/SKILL.md` 가 1 이상 (SC-05: description 키)
- `grep -c 'argument-hint' skills/sg-plan/SKILL.md` 가 1 이상 (SC-05: argument-hint 키)
- `grep -c '<objective>' skills/sg-plan/SKILL.md` 가 1 이상 (SC-06: objective 블록)
- `grep -c '<process>' skills/sg-plan/SKILL.md` 가 1 이상 (SC-06: process 블록)
- `grep -c '<success_criteria>' skills/sg-plan/SKILL.md` 가 1 이상 (SC-06: success_criteria 블록)
</verify>
<done>true</done>
</task>

<task>
<name>skills/sg-execute/ 디렉토리 및 SKILL.md 생성</name>
<files>skills/sg-execute/SKILL.md</files>
<action>
1. `mkdir -p skills/sg-execute` 실행
2. `commands/sg-execute.md` 전체 내용을 읽는다
3. `skills/sg-execute/SKILL.md` 를 동일 내용으로 생성한다 (frontmatter 포함 전체 복사)
</action>
<verify>
- `ls skills/sg-execute/SKILL.md` 가 존재
- `grep -c 'name: sg-execute' skills/sg-execute/SKILL.md` 가 1 이상 (SC-05: name 키)
- `grep -c 'description:' skills/sg-execute/SKILL.md` 가 1 이상 (SC-05: description 키)
- `grep -c 'argument-hint' skills/sg-execute/SKILL.md` 가 1 이상 (SC-05: argument-hint 키)
- `grep -c '<objective>' skills/sg-execute/SKILL.md` 가 1 이상 (SC-06: objective 블록)
- `grep -c '<process>' skills/sg-execute/SKILL.md` 가 1 이상 (SC-06: process 블록)
- `grep -c '<success_criteria>' skills/sg-execute/SKILL.md` 가 1 이상 (SC-06: success_criteria 블록)
- HANDOFF 로직이 보존됨: `grep -c 'HANDOFF' skills/sg-execute/SKILL.md` 가 1 이상
</verify>
<done>true</done>
</task>
</tasks>

<success_criteria>
1. `skills/sg-plan/SKILL.md` 가 존재하고 `name: sg-plan`, `description:`, `argument-hint:` frontmatter 키를 포함한다 (SC-05)
2. `skills/sg-execute/SKILL.md` 가 존재하고 `name: sg-execute`, `description:`, `argument-hint:` frontmatter 키를 포함한다 (SC-05)
3. 두 파일 모두 `<objective>`, `<process>`, `<success_criteria>` 블록을 포함한다 (SC-06)
4. sg-execute SKILL.md 에 HANDOFF 로직이 보존된다
</success_criteria>
</output>
