# Lessons: Phase 36 (2026-05-27)

## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: 2026-05-27T06:51:06Z

### Decisions
- TEXT-ONLY rule 고정 — bash 코드 블록 내 명령·플래그·변수명은 절대 수정 안 함. 순수 한글 prose인 bash `# comment`만 번역 허용 (36-01-SUMMARY, 36-02-SUMMARY)
- D-XX 레이블 보존 — `D-01`, `RETRO-04` 등 구조 식별자는 번역 대상 제외 (36-01-SUMMARY decisions)
- `<language>` 삽입-전용 — 기존 콘텐츠 삭제·재정렬 없음, frontmatter `---` 직후 삽입 (36-03-SUMMARY)
- Idempotency 적용 — 19개 파일 모두 삽입 전 `<language>` 존재 여부 확인 (36-03-SUMMARY)
- `.agents/sg-plan` `화면` 유지 — bash grep 정규식 내 Korean 문자열은 TEXT-ONLY 규칙으로 보존 (code review)

### Surprises
- `.agents/skills/` 전체 누락 — 3개 PLAN.md `files_modified` 모두에서 `.agents/` 파일 부재; code review Critical blocker로 발견 (commits 8946990, 4afe1bc)
- sg-execute awk 컨텍스트 한글 주석 2개 누락 — Task 1 초기 실행에서 놓침; post-task verification으로 자체 발견 후 fix commit (36-02-SUMMARY, commit 90b23c6)
- Wave 1 병렬 실행 충돌 없음 — 36-01과 36-02가 완전히 다른 파일 세트를 수정했으므로 git 충돌 0건

### Patterns
- Wave 1 병렬 + Wave 2 순차 — `wave:` 필드 + `depends_on:` 필드로 실행 그룹 분리; 파일 겹침 없는 wave는 동시 실행, 의존성 있는 wave는 완료 후 순차 처리
- Draft-then-verify 루프 — 각 task 완료 후 `grep -c '[가-힣]'` 자동 검증; 0이 아니면 fix commit 발행. 3개 plan 모두 동일 패턴 적용
- `files_modified` 필드가 code review coverage 기준 — 누락 시 CLAUDE.md `skills/ + .agents/ pair cover` 조항 위반으로 blocker 플래그

### Mistakes
- `.agents/skills/` 전체를 3개 PLAN.md에서 모두 누락 — CLAUDE.md에 명시된 paired coverage 규칙을 planning 단계에서 반영 안 함; 사후 수정으로 커밋 2개 추가 (8946990, 4afe1bc)
- sg-execute awk 블록 안 한글 주석 미번역 — multi-line awk 컨텍스트에서 일반 패턴으로 발견 안 됨; fix commit 필요했음 (36-02-SUMMARY)

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | `skills/` SKILL.md 수정 시 `.agents/skills/` 파일도 PLAN.md `files_modified`에 명시 | CLAUDE.md planning 체크리스트 또는 sg-plan 템플릿에 `.agents/` 항목 강제화 |
| P1 | i18n 번역 task의 `<verify>` 단계에 전체 파일 스캔(`grep -rn '[가-힣]'`) 포함 | 번역 task 템플릿 업데이트 (코드블록 포함 전체 스캔) |
| P2 | `depends_on` 필드 있는 PLAN은 병렬 배치에서 자동 분리 | sg-parallel-execute 그룹 빌더에 dependency 체크 로직 추가 |

## Lens: Start/Stop/Continue
_Captured: 2026-05-27T06:54:00Z_

### Start
- PLAN.md `files_modified`에 `.agents/skills/` 파일 명시적 포함 — `skills/sg-*/SKILL.md`를 수정하는 plan은 반드시 대응하는 `.agents/skills/*/SKILL.md`도 열거. planning 단계에서 CLAUDE.md `skills/ + .agents/ pair cover` 조항을 체크리스트 항목으로 포함 (36-02-SUMMARY deviations)
- 번역 task `<verify>`에 전체 파일 스캔 포함 — `grep -c '[가-힣]' <file>` 단일 파일 확인 외에, `grep -rn '[가-힣]' <dir>` 디렉토리 수준 스캔을 verify 단계에 추가. awk 컨텍스트 같은 비표준 위치 누락 방지 (36-02-SUMMARY Auto-fixed Issues)
- wave + depends_on 기반 병렬 그룹 분리 — PLAN.md 작성 시 `depends_on` 필드를 명시하면 sg-parallel-execute가 해당 PLAN을 앞 wave와 별도 배치로 자동 분리하도록 (PLAN.md 36-03)

### Stop
- 단일 task pass로 i18n 완료 가정 — awk 멀티라인 블록, YAML 인접 주석 등 비표준 위치는 첫 번째 pass에서 놓칠 수 있음. fix commit을 예상하고 verify를 2단계(파일별 + 디렉토리 전체)로 설계해야 함 (36-02-SUMMARY commit 90b23c6)
- `.agents/` 파일을 암묵적으로 처리될 것으로 가정 — `skills/`와 `.agents/skills/`는 같은 내용처럼 보여도 별개 파일임. 명시적 listing 없이 자동 포함되지 않음 (commits 8946990, 4afe1bc)

### Continue
- Wave 기반 병렬 실행 구조 — `wave:` 필드로 독립 그룹을 선언하고 sg-parallel-execute로 동시 실행하는 패턴은 이번 phase에서 검증됨. 파일 충돌 없이 병렬 단축 효과 확인 (36-01/02-SUMMARY metrics)
- Draft-then-verify 루프 — task 완료 즉시 자동화 verify 명령 실행, 실패 시 즉각 fix commit 발행하는 패턴. 3개 plan 모두 일관 적용, 모든 task가 0 Korean chars로 마감됨
- Code review를 blocker 발견 게이트로 사용 — `.agents/` 누락처럼 planning에서 빠진 항목을 code review가 잡아냄. `superpowers:requesting-code-review`를 phase 완료 전 필수 단계로 유지

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | `skills/sg-*/` 수정 plan에 `.agents/skills/*/` 항목 체크 강제화 | sg-plan이 생성하는 PLAN.md 템플릿에 `.agents/` pair cover 체크 항목 추가 |
| P2 | i18n verify 단계를 2-level(파일별 + 디렉토리 전체)로 표준화 | 번역 관련 task `<verify>` 블록 패턴을 표준 템플릿으로 문서화 |
| P3 | Wave 병렬 실행 패턴을 다른 phase 유형(리팩터링, 멀티 파일 수정)에도 적용 | sg-plan 단계에서 독립 파일 그룹 감지 시 wave 필드 자동 제안 |


## Lens: 4Ls
_Captured: 2026-05-27T14:27:32Z_

### Liked
- Wave 병렬 실행으로 36-01·36-02가 파일 충돌 0건으로 동시 진행 (36-01/02-SUMMARY metrics)
- task별 `grep -c '[가-힣]'` 자동 검증으로 모든 파일이 0 Korean chars로 객관적 완료 확인 (36-01-SUMMARY Verification)
- code review가 `.agents/` 누락을 Critical blocker로 잡아내 phase 완료 전 교정됨 (commits 8946990, 4afe1bc)

### Learned
- 한글 prose가 awk 멀티라인 블록 안에 숨어 있으면 prose-only 스캔으로 놓친다 (36-02-SUMMARY, commit 90b23c6)
- `skills/`와 `.agents/skills/`는 동일해 보여도 별개 파일 — 명시적 listing 없이는 자동 포함 안 됨 (CLAUDE.md pair-cover 조항)

### Lacked
- 36-CONTEXT.md 부재 — phase 위험 지도(Known Risk Sites)가 없어 awk-주석·`.agents/` 함정을 사전에 못 짚음
- 디렉토리 수준 i18n lint 게이트 부재 — 검증이 파일별 수동 grep에 의존

### Longed For
- `skills/`와 `.agents/skills/`를 한 번에 커버하는 단일 plan — `.agents/`가 사후 커밋으로 분리되지 않도록
- 번역 완료를 자동 차단하는 Korean-residue hook/CI 게이트

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | i18n verify에 디렉토리 전체 스캔(`grep -rn '[가-힣]'`) 추가 | 번역 task 템플릿 표준화 |
| P2 | transform phase에 최소 CONTEXT.md(위험 지도) 작성 | 다음 phase planning |

## Lens: Sailboat
_Captured: 2026-05-27T14:27:32Z_

### Wind
- 명확한 TEXT-ONLY 규칙 + 파일별 grep 검증으로 번역이 빠르고 확신 있게 진행 (36-01, 36-02)
- 36-01·36-03 plan이 무편차로 실행돼 재작업 드래그 없음 (36-01/03-SUMMARY)

### Anchor
- awk 컨텍스트에 묻힌 한글 주석이 36-02에 fix 사이클 유발 (commit 90b23c6)
- 36-CONTEXT.md 부재로 awk·`.agents/` 함정을 사전 회피할 위험 지도가 없었음

### Sun
- 강한 검증 습관(grep 카운트, post-commit 삭제 체크)으로 신뢰할 수 있는 PASS self-check 산출 (36-03)
- code review가 누락 blocker를 실제로 잡아내는 안전망으로 작동

### Rock
- `skills/` vs `.agents/skills/` 분기 — CLAUDE.md에 명시됐는데도 재발(code review Critical blocker)
- 자동 언어 lint 게이트 부재 → 향후 i18n 회귀가 조용히 유입될 수 있음

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | Korean-residue lint 게이트 도입(조용한 회귀 차단) | sg-rule 또는 hook 작성 |
| P2 | transform phase 위험 지도(CONTEXT.md) 선작성 | 다음 phase planning |
