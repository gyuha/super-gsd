---
phase: 05-lessons-feedback-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - commands/sg-lessons.md
  - commands/sg-plan.md
autonomous: true
requirements:
  - LESS-02

must_haves:
  truths:
    - "사용자가 /super-gsd:sg-lessons를 실행하면 .planning/lessons/ 의 내용이 출력된다"
    - ".planning/lessons/ 가 비어 있거나 없으면 안내 메시지가 출력되고 종료된다"
    - "/super-gsd:sg-plan 실행 시 .planning/lessons/ 파일이 있으면 Step 0에서 자동으로 내용이 출력된다"
    - "lessons 파일이 없으면 sg-plan의 Step 0이 조용히 건너뛰어진다"
  artifacts:
    - path: "commands/sg-lessons.md"
      provides: "lessons 파일 목록 읽기 + 출력 + phase 필터 명령어"
      contains: "sg-lessons"
    - path: "commands/sg-plan.md"
      provides: "Step 0 prior lessons 주입 + 기존 Step 1/2/3 체인 유지"
      contains: "Prior lessons"
  key_links:
    - from: "commands/sg-plan.md Step 0"
      to: ".planning/lessons/*.md"
      via: "bash glob + cat"
      pattern: ".planning/lessons"
    - from: "commands/sg-lessons.md"
      to: ".planning/lessons/*.md"
      via: "ls + cat"
      pattern: ".planning/lessons"
---

<objective>
sg-lessons 명령을 신규 생성하고, sg-plan의 Step 0에 prior lessons 자동 주입 로직을 추가한다. sg-lessons는 .planning/lessons/ 파일을 읽어 출력하는 자기 완결형 명령이다. sg-plan의 Step 0은 lessons 파일이 있을 때만 내용을 먼저 출력하고 없으면 조용히 넘어간다.

Purpose: LESS-02 — 다음 GSD phase 계획 시 이전 lessons가 자동으로 컨텍스트에 포함된다.
Output: commands/sg-lessons.md (신규), commands/sg-plan.md (Step 0 추가)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@commands/sg-plan.md
@commands/sg-learn.md
@commands/sg-status.md
@.planning/phases/05-lessons-feedback-loop/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: commands/sg-lessons.md 신규 생성</name>
  <files>commands/sg-lessons.md</files>
  <action>
    sg-status.md의 frontmatter 패턴을 따라 새 명령어 파일을 작성한다.

    frontmatter:
    - name: sg-lessons
    - description: List prior Hookify lessons from .planning/lessons/ and inject them as context for the next GSD phase.
    - argument-hint: "[phase] - optional. If provided, show only lessons for that phase number."

    objective 섹션: .planning/lessons/ 디렉토리의 모든 Markdown 파일을 읽어 순서대로 출력한다. 사용자가 /super-gsd:sg-plan을 실행하기 전에 이전 lessons를 확인하거나 컨텍스트로 활용할 수 있도록 한다.

    execution_context 섹션: Self-contained. Reads .planning/lessons/ directory. Writes nothing.

    process 섹션 — 4개 단계:
    1. Glob으로 파일 목록 수집:
       ```bash
       FILES=$(ls .planning/lessons/*.md 2>/dev/null | sort)
       ```
       비어 있으면 `No lessons recorded yet. Run /super-gsd:sg-learn after a review to capture lessons.` 출력 후 종료.

    2. ARGUMENTS로 phase 필터 적용. $ARGUMENTS가 비어 있지 않으면 해당 phase 번호로 시작하는 파일만 남긴다:
       ```bash
       if [ -n "$ARGUMENTS" ]; then
         PADDED=$(printf "%02d" "$ARGUMENTS" 2>/dev/null || echo "$ARGUMENTS")
         FILES=$(echo "$FILES" | grep "/${PADDED}-" 2>/dev/null)
       fi
       ```
       필터 후 빈 목록이면 `No lessons found for phase $ARGUMENTS.` 출력 후 종료.

    3. 각 파일 내용 출력. 파일명을 헤더로 표시하고 내용을 출력한다:
       ```bash
       for FILE in $FILES; do
         echo "--- $FILE ---"
         cat "$FILE"
         echo ""
       done
       ```

    4. 안내 메시지 출력:
       `Lessons loaded. Run /super-gsd:sg-plan to start the next phase — prior lessons are auto-injected.`

    success_criteria 섹션:
    1. .planning/lessons/ 에 파일이 있으면 내용이 출력된다.
    2. 파일이 없으면 안내 메시지만 출력되고 오류 없이 종료된다.
    3. phase 필터가 주어지면 해당 phase 파일만 출력된다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -c 'sg-lessons' commands/sg-lessons.md && grep -c '.planning/lessons' commands/sg-lessons.md && echo "sg-lessons.md: OK"</automated>
  </verify>
  <done>commands/sg-lessons.md 파일이 존재하고, sg-lessons 이름과 .planning/lessons/ 읽기 로직을 포함한다.</done>
</task>

<task type="auto">
  <name>Task 2: commands/sg-plan.md에 Step 0 prior lessons 주입 추가</name>
  <files>commands/sg-plan.md</files>
  <action>
    기존 sg-plan.md의 process 섹션을 수정해 Step 0을 맨 앞에 추가한다. 기존 Step 1/2/3은 번호를 포함해 내용을 그대로 유지한다.

    추가할 Step 0 (기존 "1. **Resolve phase.**" 앞에 삽입):
    ```
    0. **Prior lessons 주입.** .planning/lessons/ 아래 Markdown 파일이 있으면 내용을 먼저 출력한다:
       ```bash
       if ls .planning/lessons/*.md 2>/dev/null | head -1 | grep -q .; then
         echo "=== Prior Lessons (auto-injected) ==="
         cat .planning/lessons/*.md
         echo "=== End of Prior Lessons ==="
       fi
       ```
       파일이 없으면 이 단계를 조용히 건너뛴다.
    ```

    success_criteria 섹션에도 항목을 추가한다:
    "0. .planning/lessons/ 에 파일이 있으면 Step 1 전에 내용이 출력된다. 파일이 없으면 Step 0이 조용히 건너뛰어진다."

    기존 세 success_criteria 항목의 번호(1, 2, 3)는 변경하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -c 'Prior lessons' commands/sg-plan.md && grep -c '.planning/lessons' commands/sg-plan.md && echo "sg-plan.md Step 0: OK"</automated>
  </verify>
  <done>commands/sg-plan.md의 process 섹션에 Step 0이 추가되어 있고, 기존 Step 1/2/3/4가 그대로 유지된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/lessons/ → sg-plan | 로컬 파일 내용이 GSD 계획 컨텍스트로 주입됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | sg-lessons 출력 | accept | 프로젝트 로컬 lessons 파일 — .planning/ 컨벤션과 동일 보안 레벨, 로컬 파일시스템 접근 권한 필요 |
| T-05-05 | Tampering | sg-plan Step 0 cat | accept | cat은 읽기 전용 — 쓰기 없음. 경로는 하드코딩된 .planning/lessons/*.md |
| T-05-SC | Tampering | npm/pip installs | accept | 이 플랜은 신규 패키지 설치 없음 |
</threat_model>

<verification>
1. `ls .planning/phases/05-lessons-feedback-loop/commands/sg-lessons.md 2>/dev/null || ls commands/sg-lessons.md` — 파일 존재
2. `grep -c 'arg.*phase' commands/sg-lessons.md` — argument-hint에 phase 언급 존재
3. `grep 'Prior lessons' commands/sg-plan.md` — Step 0 텍스트 존재
4. `grep -c 'Step 1/2' commands/sg-plan.md` — 기존 Step 1/2 진행 메시지가 그대로 유지됨 (결과 = 1)
5. 전체 학습 루프 smoke test:
   ```bash
   mkdir -p .planning/lessons
   echo "# Lessons: Phase 04 (2026-05-16)\n\n- 작은 태스크로 분리하면 context 소비가 줄어든다." > .planning/lessons/04-2026-05-16.md
   grep 'Prior lessons' commands/sg-plan.md && echo "PASS: sg-plan has Step 0"
   grep '.planning/lessons' commands/sg-lessons.md && echo "PASS: sg-lessons reads lessons dir"
   rm .planning/lessons/04-2026-05-16.md
   ```
</verification>

<success_criteria>
1. /super-gsd:sg-lessons 명령이 .planning/lessons/ 파일 내용을 출력한다. 파일 없으면 안내 메시지를 출력한다.
2. /super-gsd:sg-plan 실행 시 .planning/lessons/ 에 파일이 있으면 Step 0에서 자동으로 내용을 출력하고, 없으면 조용히 건너뛴다.
3. GSD → Superpowers → Hookify → next GSD 사이클에서 lessons 파일이 자동 생성(05-01)되고 sg-plan에서 자동 주입(05-02)되어 수동 컨텍스트 전달이 불필요하다.
</success_criteria>

<output>
완료 후 `.planning/phases/05-lessons-feedback-loop/05-02-SUMMARY.md` 를 생성한다.
</output>
