---
phase: 30-skill-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-plan/SKILL.md
  - skills/sg-execute/SKILL.md
  - skills/sg-complete/SKILL.md
  - skills/sg-quick/SKILL.md
  - skills/sg-ui-plan/SKILL.md
  - .agents/skills/sg-plan/SKILL.md
  - .agents/skills/sg-execute/SKILL.md
  - .agents/skills/sg-ship/SKILL.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - AGENT-01

must_haves:
  truths:
    - "`grep -rn 'python3' skills/ .agents/skills/` 결과 0건 — 8개 SKILL.md 파일 모두에서 python3 토큰 완전 소거"
    - "skills/sg-plan, sg-execute, sg-complete의 lessons_ranker 호출이 node hooks/lessons_ranker.cjs를 사용한다 (D-05 two-token swap)"
    - "skills/sg-quick(2건)과 sg-ui-plan(1건)의 인라인 python3 -c 파싱이 node -e 패턴으로 교체되어 macOS/Linux 양쪽에서 동일한 JSON 파싱 결과를 반환한다 (D-07, D-08)"
    - ".agents/skills/sg-plan, sg-execute, sg-ship의 python3 호출이 동일한 규칙으로 교체되어 Codex CLI에서 Python 없이 실행 가능하다 (AGENT-01, D-10)"
    - "8개 SKILL.md 파일은 교체된 토큰 외 byte-exact 보존 — 들여쓰기, pipe 구조, redirection, 바깥 셸 변수 캡처 동일 (D-05)"
  artifacts:
    - path: "skills/sg-plan/SKILL.md"
      provides: "SKILL-01: lessons_ranker.cjs 호출 + Pattern C (with source) node -e 포매팅"
      contains: "node hooks/lessons_ranker.cjs"
    - path: "skills/sg-execute/SKILL.md"
      provides: "SKILL-01: lessons_ranker.cjs 호출 + Pattern C (source 없음 변형) node -e 포매팅"
      contains: "node hooks/lessons_ranker.cjs"
    - path: "skills/sg-complete/SKILL.md"
      provides: "SKILL-01: lessons_ranker.cjs --archive 호출"
      contains: "node hooks/lessons_ranker.cjs --archive"
    - path: "skills/sg-quick/SKILL.md"
      provides: "SKILL-02: Pattern A — quick_id, task_dir JSON 추출"
      contains: "node -e 'let s=\"\""
    - path: "skills/sg-ui-plan/SKILL.md"
      provides: "SKILL-02: Pattern B — JSON string unescape"
      contains: "node -e 'let s=\"\""
    - path: ".agents/skills/sg-plan/SKILL.md"
      provides: "AGENT-01: lessons_ranker.cjs + Pattern C + Pattern B (L27, L28, L63)"
      contains: "node hooks/lessons_ranker.cjs"
    - path: ".agents/skills/sg-execute/SKILL.md"
      provides: "AGENT-01: lessons_ranker.cjs + Pattern C source 없음 변형 (L27, L28)"
      contains: "node hooks/lessons_ranker.cjs"
    - path: ".agents/skills/sg-ship/SKILL.md"
      provides: "AGENT-01 D-10: python3 -m pytest → pytest 직접 호출"
      contains: "pytest 2>&1 || TEST_FAILED=1"
  key_links:
    - from: "skills/sg-plan/SKILL.md (Step 0)"
      to: "hooks/lessons_ranker.cjs"
      via: "node hooks/lessons_ranker.cjs --top 5 .planning/lessons/*.md"
      pattern: "node hooks/lessons_ranker\\.cjs"
    - from: ".agents/skills/sg-plan/SKILL.md (Step 1.5)"
      to: "gsd-sdk roadmap.get-phase stdout"
      via: "node -e Pattern B JSON unescape"
      pattern: "node -e 'let s"
---

<objective>
`skills/sg-*/SKILL.md` 5개 + `.agents/skills/sg-*/SKILL.md` 3개 안의 14개 `python3` 호출을 Node.js 기반 등가물로 surgical edit한다.

Purpose: Phase 28의 `.cjs` 산출물(hooks/lessons_ranker.cjs 등)과 Node 18+ 보장을 전제로, 사용자가 Python 없이도 모든 `sg-*` 명령을 실행할 수 있게 한다. D-05 원칙: `python3` → `node`, `*.py` → `*.cjs` 두 토큰만 교체, 나머지는 byte-exact 보존.

Output:
- 8개 SKILL.md 파일 수정 (14개 occurrence 교체)
- 단일 atomic commit: `feat(30): swap python3 → node in skill callers`

Requirements covered: SKILL-01 (D-05, per D-01), SKILL-02 (D-07, D-08 Pattern A/B), AGENT-01 (D-05, D-10, D-12)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-skill-agent/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — 4개 .cjs 파일 존재 확인</name>
  <files>읽기 전용 — 파일 수정 없음</files>
  <read_first>
    hooks/ 디렉토리 목록만 확인한다. 편집 전 전제 조건 검증이므로 소스 파일을 읽을 필요 없다.
  </read_first>
  <action>
    다음 4개 파일이 모두 존재하는지 확인한다 (D-15 pre-flight guard, per D-03 .py는 Phase 31까지 공존):

    ```bash
    test -f hooks/lessons_ranker.cjs && \
    test -f hooks/stop_hook.cjs && \
    test -f hooks/rule_runner.cjs && \
    test -f hooks/transcript_matcher.cjs && \
    echo "PRE-FLIGHT OK" || echo "PRE-FLIGHT FAILED — Phase 28 재확인 필요"
    ```

    결과가 `PRE-FLIGHT OK`이면 Task 2로 진행한다. 하나라도 missing이면 즉시 중단하고 Phase 28 결과물을 재확인한다.
  </action>
  <verify>
    <automated>test -f hooks/lessons_ranker.cjs && test -f hooks/stop_hook.cjs && test -f hooks/rule_runner.cjs && test -f hooks/transcript_matcher.cjs && echo OK</automated>
  </verify>
  <done>4개 .cjs 파일이 모두 존재하고 `OK`가 출력된다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SKILL-01 편집 — sg-plan, sg-execute, sg-complete (lessons_ranker.py → .cjs)</name>
  <files>
    skills/sg-plan/SKILL.md
    skills/sg-execute/SKILL.md
    skills/sg-complete/SKILL.md
  </files>
  <read_first>
    편집 전에 반드시 세 파일의 해당 행을 읽어 현재 상태를 확인한다:
    - skills/sg-plan/SKILL.md L15~L35 (Step 0 전체 bash 블록)
    - skills/sg-execute/SKILL.md L15~L35 (Step 0 전체 bash 블록)
    - skills/sg-complete/SKILL.md L28~L40 (Step 1.3 전체 bash 블록)
  </read_first>
  <action>
    D-05 two-token swap 원칙을 적용한다. **명시된 diff 외 모든 토큰은 byte-exact 보존**.

    **[A] skills/sg-plan/SKILL.md — 2건 교체:**

    교체 1 (L20 — lessons_ranker.py → .cjs, two-token swap):
    ```
    Before: python3 hooks/lessons_ranker.py --top 5 .planning/lessons/*.md 2>/dev/null \
    After:  node hooks/lessons_ranker.cjs --top 5 .planning/lessons/*.md 2>/dev/null \
    ```

    교체 2 (L21~L30 — Pattern C with source: python3 -c 멀티라인 블록 → node -e):
    ```
    Before (L21~L30):
          | python3 -c "
      import sys, json
      lines = [l for l in sys.stdin if l.strip()]
      for i, line in enumerate(lines, 1):
          try:
              d = json.loads(line)
              print(f\"{i}. [score {d['score']:.2f}] {d['pattern']} ({d['source']})\")
          except Exception:
              pass
      " || echo "(weighted ranking unavailable)"

    After (L21~L27, 10행 → 7행으로 줄어듦):
          | node -e '
      let buf="";process.stdin.on("data",d=>buf+=d).on("end",()=>{
        const lines=buf.split("\n").filter(l=>l.trim());
        lines.forEach((line,i)=>{
          try{const d=JSON.parse(line);console.log(`${i+1}. [score ${d.score.toFixed(2)}] ${d.pattern} (${d.source})`)}catch(e){}
        });
      })' || echo "(weighted ranking unavailable)"
    ```
    주의: single-quote 내부의 `${i+1}`, `${d.score...}`, `${d.pattern}`, `${d.source}` 는 JS 템플릿 리터럴이므로 셸에 의해 확장되지 않는다. 들여쓰기 6칸(공백 6개) 보존.

    **[B] skills/sg-execute/SKILL.md — 2건 교체:**

    교체 1 (L20 — 동일 two-token swap):
    ```
    Before: python3 hooks/lessons_ranker.py --top 5 .planning/lessons/*.md 2>/dev/null \
    After:  node hooks/lessons_ranker.cjs --top 5 .planning/lessons/*.md 2>/dev/null \
    ```

    교체 2 (L21~L29 — Pattern C source 없음 변형: `|| true` 종결, source 필드 미출력):
    ```
    Before (L21~L29):
          | python3 -c "
      import sys, json
      for i, line in enumerate((l for l in sys.stdin if l.strip()), 1):
          try:
              d = json.loads(line)
              print(f\"{i}. [score {d['score']:.2f}] {d['pattern']}\")
          except Exception:
              pass
      " || true

    After (L21~L27, 9행 → 7행):
          | node -e '
      let buf="";process.stdin.on("data",d=>buf+=d).on("end",()=>{
        const lines=buf.split("\n").filter(l=>l.trim());
        lines.forEach((line,i)=>{
          try{const d=JSON.parse(line);console.log(`${i+1}. [score ${d.score.toFixed(2)}] ${d.pattern}`)}catch(e){}
        });
      })' || true
    ```
    source 출력이 없음(d.pattern만). `|| true` 종결 보존.

    **[C] skills/sg-complete/SKILL.md — 1건 교체 (L36, two-token swap):**
    ```
    Before: python3 hooks/lessons_ranker.py --archive --milestone "$MILESTONE_VER" .planning/lessons/*.md 2>&1 || \
    After:  node hooks/lessons_ranker.cjs --archive --milestone "$MILESTONE_VER" .planning/lessons/*.md 2>&1 || \
    ```
    `2>&1 || \` 이후 행은 수정하지 않는다.
  </action>
  <verify>
    <automated>
      ! grep -n 'python3' skills/sg-plan/SKILL.md skills/sg-execute/SKILL.md skills/sg-complete/SKILL.md &&
      grep -c 'node hooks/lessons_ranker\.cjs' skills/sg-plan/SKILL.md | grep -q '^1$' &&
      grep -c 'node hooks/lessons_ranker\.cjs' skills/sg-execute/SKILL.md | grep -q '^1$' &&
      grep -c 'node hooks/lessons_ranker\.cjs' skills/sg-complete/SKILL.md | grep -q '^1$' &&
      grep -q "node -e '" skills/sg-plan/SKILL.md &&
      grep -q "node -e '" skills/sg-execute/SKILL.md &&
      echo "SKILL-01 OK"
    </automated>
  </verify>
  <done>
    - skills/sg-plan/SKILL.md: `python3` 0건, `node hooks/lessons_ranker.cjs` 1건, `node -e '` 1건
    - skills/sg-execute/SKILL.md: `python3` 0건, `node hooks/lessons_ranker.cjs` 1건, `node -e '` 1건
    - skills/sg-complete/SKILL.md: `python3` 0건, `node hooks/lessons_ranker.cjs` 1건 (`--archive`)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: SKILL-02 편집 — sg-quick (Pattern A), sg-ui-plan (Pattern B)</name>
  <files>
    skills/sg-quick/SKILL.md
    skills/sg-ui-plan/SKILL.md
  </files>
  <read_first>
    편집 전에 두 파일의 해당 행을 읽는다:
    - skills/sg-quick/SKILL.md L59~L67 (Step 2 전체 — INIT_JSON 파싱 2행)
    - skills/sg-ui-plan/SKILL.md L29~L37 (Step 2 전체 — PHASE_SECTION 추출)
  </read_first>
  <action>
    **[A] skills/sg-quick/SKILL.md — 2건 교체 (L62~L63, Pattern A — per D-07, D-08):**

    QUICK_ID 행 (L62):
    ```
    Before:
      QUICK_ID=$(echo "$INIT_JSON" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('quick_id') or d.get('id',''))")
    After:
      QUICK_ID=$(echo "$INIT_JSON" | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{const j=JSON.parse(s);process.stdout.write(j.quick_id||j.id||"")}catch(e){}})')
    ```

    TASK_DIR 행 (L63):
    ```
    Before:
      TASK_DIR=$(echo "$INIT_JSON" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('task_dir') or d.get('dir',''))")
    After:
      TASK_DIR=$(echo "$INIT_JSON" | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{const j=JSON.parse(s);process.stdout.write(j.task_dir||j.dir||"")}catch(e){}})')
    ```

    Python의 `d.get('quick_id') or d.get('id','')` falsy short-circuit은 JS의 `j.quick_id||j.id||""` 로 동등하게 대응된다 (빈 문자열도 falsy — 동등성 OK).
    `process.stdout.write` 사용으로 Python `print` (개행 포함)와 달리 개행 없이 출력한다 — 셸 변수 캡처 시 동등 동작.

    **[B] skills/sg-ui-plan/SKILL.md — 1건 교체 (L32, Pattern B — per D-08):**
    ```
    Before:
      PHASE_SECTION=$(echo "$PHASE_SECTION_RAW" | python3 -c 'import json,sys; v=sys.stdin.read().strip(); print(json.loads(v))' 2>/dev/null || echo "$PHASE_SECTION_RAW")
    After:
      PHASE_SECTION=$(echo "$PHASE_SECTION_RAW" | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{process.stdout.write(JSON.parse(s.trim()))}catch(e){}})' 2>/dev/null || echo "$PHASE_SECTION_RAW")
    ```
    `2>/dev/null || echo "$PHASE_SECTION_RAW"` fallback 구조 byte-exact 보존.
  </action>
  <verify>
    <automated>
      ! grep -n 'python3' skills/sg-quick/SKILL.md skills/sg-ui-plan/SKILL.md &&
      grep -c "node -e 'let s" skills/sg-quick/SKILL.md | grep -q '^2$' &&
      grep -c "node -e 'let s" skills/sg-ui-plan/SKILL.md | grep -q '^1$' &&
      echo "SKILL-02 OK"
    </automated>
  </verify>
  <done>
    - skills/sg-quick/SKILL.md: `python3` 0건, `node -e 'let s` 2건 (QUICK_ID, TASK_DIR)
    - skills/sg-ui-plan/SKILL.md: `python3` 0건, `node -e 'let s` 1건 (PHASE_SECTION)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: AGENT-01 편집 — .agents/skills/sg-plan, sg-execute, sg-ship</name>
  <files>
    .agents/skills/sg-plan/SKILL.md
    .agents/skills/sg-execute/SKILL.md
    .agents/skills/sg-ship/SKILL.md
  </files>
  <read_first>
    편집 전에 세 파일의 해당 행을 읽는다:
    - .agents/skills/sg-plan/SKILL.md L22~L42 (Step 0 bash 블록), L60~L70 (Step 1.5 PHASE_SECTION 추출)
    - .agents/skills/sg-execute/SKILL.md L22~L40 (Step 0 bash 블록)
    - .agents/skills/sg-ship/SKILL.md L99~L112 (테스트 러너 분기 전체)
  </read_first>
  <action>
    **[A] .agents/skills/sg-plan/SKILL.md — 3건 교체:**

    교체 1 (L27 — two-token swap, lessons_ranker):
    ```
    Before: python3 hooks/lessons_ranker.py --top 5 .planning/lessons/*.md 2>/dev/null \
    After:  node hooks/lessons_ranker.cjs --top 5 .planning/lessons/*.md 2>/dev/null \
    ```

    교체 2 (L28~L37 — Pattern C source 없음 변형. 이 파일의 Step 0는 source 없는 변형이다):
    현재 .agents/skills/sg-plan/SKILL.md의 Step 0 python3 -c 블록은 `{d["score"]}`, `{d["pattern"]}` 만 출력하며 source를 출력하지 않는다.
    교체:
    ```
    Before (L28~L37):
          | python3 -c "
      import sys, json
      for i, line in enumerate((l for l in sys.stdin if l.strip()), 1):
          try:
              d = json.loads(line)
              print(f'{i}. [score {d[\"score\"]:.2f}] {d[\"pattern\"]}')
          except Exception:
              pass
      " || true

    After (L28~L34):
          | node -e '
      let buf="";process.stdin.on("data",d=>buf+=d).on("end",()=>{
        const lines=buf.split("\n").filter(l=>l.trim());
        lines.forEach((line,i)=>{
          try{const d=JSON.parse(line);console.log(`${i+1}. [score ${d.score.toFixed(2)}] ${d.pattern}`)}catch(e){}
        });
      })' || true
    ```
    `|| true` 종결 보존.

    교체 3 (L63 — Pattern B, PHASE_SECTION JSON unescape):
    ```
    Before:
      PHASE_SECTION=$(echo "$PHASE_SECTION_RAW" | python3 -c 'import json,sys; v=sys.stdin.read().strip(); print(json.loads(v))' 2>/dev/null || echo "$PHASE_SECTION_RAW")
    After:
      PHASE_SECTION=$(echo "$PHASE_SECTION_RAW" | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{process.stdout.write(JSON.parse(s.trim()))}catch(e){}})' 2>/dev/null || echo "$PHASE_SECTION_RAW")
    ```
    skills/sg-ui-plan/SKILL.md L32와 동일 변환 (D-08 Pattern B).

    **[B] .agents/skills/sg-execute/SKILL.md — 2건 교체:**

    교체 1 (L27 — two-token swap):
    ```
    Before: python3 hooks/lessons_ranker.py --top 5 .planning/lessons/*.md 2>/dev/null \
    After:  node hooks/lessons_ranker.cjs --top 5 .planning/lessons/*.md 2>/dev/null \
    ```

    교체 2 (L28~L36 — Pattern C source 없음 변형, skills/sg-execute와 동일):
    ```
    Before (L28~L36):
          | python3 -c "
      import sys, json
      for i, line in enumerate((l for l in sys.stdin if l.strip()), 1):
          try:
              d = json.loads(line)
              print(f'{i}. [score {d[\"score\"]:.2f}] {d[\"pattern\"]}')
          except Exception:
              pass
      " || true

    After (L28~L34):
          | node -e '
      let buf="";process.stdin.on("data",d=>buf+=d).on("end",()=>{
        const lines=buf.split("\n").filter(l=>l.trim());
        lines.forEach((line,i)=>{
          try{const d=JSON.parse(line);console.log(`${i+1}. [score ${d.score.toFixed(2)}] ${d.pattern}`)}catch(e){}
        });
      })' || true
    ```

    **[C] .agents/skills/sg-ship/SKILL.md — 1건 교체 (L106, D-10):**
    ```
    Before:   python3 -m pytest 2>&1 || TEST_FAILED=1
    After:    pytest 2>&1 || TEST_FAILED=1
    ```
    `elif [ -f "pyproject.toml" ] || [ -f "setup.py" ]; then` 가드 행은 수정하지 않는다 (D-11).
    fallback 추가 없음 (D-12).
  </action>
  <verify>
    <automated>
      ! grep -n 'python3' .agents/skills/sg-plan/SKILL.md .agents/skills/sg-execute/SKILL.md .agents/skills/sg-ship/SKILL.md &&
      grep -c 'node hooks/lessons_ranker\.cjs' .agents/skills/sg-plan/SKILL.md | grep -q '^1$' &&
      grep -c 'node hooks/lessons_ranker\.cjs' .agents/skills/sg-execute/SKILL.md | grep -q '^1$' &&
      grep -q "pytest 2>&1 || TEST_FAILED=1" .agents/skills/sg-ship/SKILL.md &&
      ! grep -q "python3 -m pytest" .agents/skills/sg-ship/SKILL.md &&
      echo "AGENT-01 OK"
    </automated>
  </verify>
  <done>
    - .agents/skills/sg-plan/SKILL.md: `python3` 0건, `node hooks/lessons_ranker.cjs` 1건, `node -e '` 2건 (Pattern C + Pattern B)
    - .agents/skills/sg-execute/SKILL.md: `python3` 0건, `node hooks/lessons_ranker.cjs` 1건, `node -e '` 1건
    - .agents/skills/sg-ship/SKILL.md: `python3` 0건, `pytest 2>&1 || TEST_FAILED=1` 존재, `python3 -m pytest` 없음
  </done>
</task>

<task type="auto">
  <name>Task 5: 4-tier 검증 — static grep, file existence, node -e dry-run, manual 안내</name>
  <files>읽기 전용 — 파일 수정 없음</files>
  <read_first>
    검증 명령 실행 전 파일 읽기 불필요. 이전 Task들의 편집 결과를 직접 grep으로 검증한다.
  </read_first>
  <action>
    D-14 4-tier 검증을 순서대로 실행한다.

    **Tier 1 — Static grep (자동, must be 0 matches):**
    ```bash
    ! grep -rn 'python3' skills/ .agents/skills/
    ```
    결과가 0건이면 성공. 1건이라도 있으면 해당 파일/행을 찾아 Task 2~4로 돌아가 수정한다.

    Phase 29 lesson 적용: negated grep을 &&-chain의 첫 번째 요소로 배치한다.

    **Tier 2 — File existence (자동):**
    ```bash
    ls skills/sg-plan/SKILL.md \
       skills/sg-execute/SKILL.md \
       skills/sg-complete/SKILL.md \
       skills/sg-quick/SKILL.md \
       skills/sg-ui-plan/SKILL.md \
       .agents/skills/sg-plan/SKILL.md \
       .agents/skills/sg-execute/SKILL.md \
       .agents/skills/sg-ship/SKILL.md
    ```
    8개 파일 모두 출력되면 성공.

    **Tier 3 — node -e dry-run (자동, 3 Pattern + lessons_ranker):**

    3a. Pattern A dry-run:
    ```bash
    echo '{"quick_id":"abc","id":"fallback"}' | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{const j=JSON.parse(s);process.stdout.write(j.quick_id||j.id||"")}catch(e){}})'
    # expected output: abc (개행 없음)
    ```

    3b. Pattern B dry-run:
    ```bash
    printf '"hello world"' | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{process.stdout.write(JSON.parse(s.trim()))}catch(e){}})'
    # expected output: hello world (개행 없음)
    ```

    3c. Pattern C dry-run (with source):
    ```bash
    printf '{"score":0.42,"pattern":"P1","source":"f.md"}\n{"score":0.31,"pattern":"P2","source":"g.md"}\n' | node -e 'let buf="";process.stdin.on("data",d=>buf+=d).on("end",()=>{const lines=buf.split("\n").filter(l=>l.trim());lines.forEach((line,i)=>{try{const d=JSON.parse(line);console.log(`${i+1}. [score ${d.score.toFixed(2)}] ${d.pattern} (${d.source})`)}catch(e){}});})'
    # expected:
    # 1. [score 0.42] P1 (f.md)
    # 2. [score 0.31] P2 (g.md)
    ```

    3d. lessons_ranker.cjs direct call:
    ```bash
    node hooks/lessons_ranker.cjs --top 5 .planning/lessons/*.md | head -3
    # expected: 최대 3행의 JSON-lines 출력 (lessons 파일이 없으면 빈 출력도 OK)
    ```

    **Tier 4 — Manual (사용자 환경, 자동화 불가):**
    다음 명령을 차례로 실행하여 Step 0(lessons 주입) 또는 stdin 파싱이 깨지지 않는지 확인한다:
    - `/super-gsd:sg-plan` — Step 0 lessons 출력 확인 (또는 "files not found" skip 메시지)
    - `/super-gsd:sg-execute` — Step 0 reminder 확인
    - `/super-gsd:sg-complete` — Step 1.3 lessons archive 시도 확인
    - `/super-gsd:sg-quick` — INIT_JSON 파싱으로 QUICK_ID/TASK_DIR 정상 추출 확인
    - `/super-gsd:sg-ui-plan` — PHASE_SECTION 추출 정상 동작 확인

    Codex CLI `.agents/skills/` 사본(sg-plan, sg-execute, sg-ship)은 사용자 Codex 환경에서 수동 확인 (D-14 Tier 4).
  </action>
  <verify>
    <automated>
      ! grep -rn 'python3' skills/ .agents/skills/ &&
      echo '{"quick_id":"test_id"}' | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{const j=JSON.parse(s);process.stdout.write(j.quick_id||j.id||"")}catch(e){}})' | grep -q 'test_id' &&
      printf '"unwrapped"' | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{process.stdout.write(JSON.parse(s.trim()))}catch(e){}})' | grep -q 'unwrapped' &&
      echo "TIER 1+3 OK"
    </automated>
    <human-check>
      Tier 4 (선택): `/super-gsd:sg-plan` 실행 후 "=== Weighted Top-N Patterns ===" 섹션이 출력되거나 lessons 파일 부재 시 조용히 skip되는지 확인한다.
    </human-check>
  </verify>
  <done>
    - Tier 1: `grep -rn 'python3' skills/ .agents/skills/` 결과 0건
    - Tier 2: 8개 SKILL.md 파일 존재 확인
    - Tier 3: Pattern A/B/C dry-run 및 lessons_ranker.cjs 모두 expected output 반환
    - Tier 4: 메모 — 사용자 환경 manual 확인 필요
  </done>
</task>

<task type="auto">
  <name>Task 6: Single atomic commit</name>
  <files>git staging 전용 — 파일 내용 수정 없음</files>
  <read_first>
    커밋 전 추가 파일 읽기 불필요.
  </read_first>
  <action>
    D-16 single atomic commit 규칙에 따라 8개 SKILL.md 파일을 동시 staging한 뒤 단일 커밋을 생성한다.

    ```bash
    git add \
      skills/sg-plan/SKILL.md \
      skills/sg-execute/SKILL.md \
      skills/sg-complete/SKILL.md \
      skills/sg-quick/SKILL.md \
      skills/sg-ui-plan/SKILL.md \
      .agents/skills/sg-plan/SKILL.md \
      .agents/skills/sg-execute/SKILL.md \
      .agents/skills/sg-ship/SKILL.md

    git commit -m "feat(30): swap python3 → node in skill callers"
    ```

    커밋 후 즉시 `COMMIT_SHA=$(git rev-parse HEAD)` 를 캡처하여 verify gate에 사용한다 (Phase 29 lesson: git log HEAD assumption 회피).

    커밋 메시지에 다른 파일을 포함하거나 `--amend`를 사용하지 않는다.
  </action>
  <verify>
    <automated>
      COMMIT_SHA=$(git rev-parse HEAD) &&
      git log $COMMIT_SHA -1 --pretty=%s | grep -Fq 'feat(30): swap python3' &&
      git show --stat $COMMIT_SHA | grep -c 'SKILL.md' | grep -q '^8$' &&
      echo "COMMIT OK"
    </automated>
  </verify>
  <done>
    - `feat(30): swap python3 → node in skill callers` 커밋이 HEAD에 존재
    - 커밋의 변경 파일 수가 정확히 8개
    - `git show $COMMIT_SHA -- skills/sg-plan/SKILL.md` 에서 `python3` 삭제 diff가 보임
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md bash 블록 → shell | SKILL.md의 bash 스니펫은 Claude Code/Codex가 직접 실행한다. node -e 코드는 single-quote 내부에 위치하므로 셸 변수 확장으로부터 격리됨 |
| gsd-sdk stdout → node -e stdin | gsd-sdk JSON 출력이 node -e의 stdin으로 흐름. 신뢰된 내부 도구이지만 파싱 실패 시 빈 문자열 반환으로 fallback |
| lessons_ranker.cjs stdout → node -e stdin (Pattern C) | lessons_ranker.cjs 출력이 파이프로 node -e에 흐름. 두 단계 모두 내부 도구이며 JSON-lines 형식이 고정됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | SKILL.md bash 블록 내 node -e single-quote 경계 | mitigate | single-quote 내부 JS 코드에 셸 메타문자(`$`, `` ` ``, `\`)가 없음을 편집 후 확인. `${i+1}` 등은 JS template literal이므로 single-quote 안에서 셸 확장되지 않음 |
| T-30-02 | Information Disclosure | lessons_ranker.cjs → stdout (JSON-lines) | accept | lessons 데이터는 프로젝트 내부 회고 기록이며 비밀 정보 아님. 외부 전송 없음 |
| T-30-03 | Denial of Service | node -e stdin 미종결 (EOF 미수신) | mitigate | `echo` / `printf` 기반 입력은 명확히 EOF를 전달. `process.stdin.on('end')` 패턴은 pipe EOF에서 안정적으로 발화됨. Tier 3 dry-run으로 검증 |
| T-30-04 | Elevation of Privilege | pytest 직접 호출 (D-10) | accept | `.agents/skills/sg-ship/SKILL.md`의 pytest 호출은 `pyproject.toml`/`setup.py` 존재 여부 파일 가드(D-11)로 진입 조건이 제한됨. super-gsd 자체는 Python 프로젝트가 아니므로 이 분기를 실행하지 않음 |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | 이 Phase는 package manager install 없음 — 기존 hooks/*.cjs 파일을 호출하는 텍스트 편집만 수행 |
</threat_model>

<verification>
Phase 30 전체 완료 조건 (ROADMAP Success Criteria #4 매핑):

```bash
# 최종 통합 검증 명령
! grep -rn 'python3' skills/ .agents/skills/
```

결과가 0건이면 Phase 30 완료.

추가 확인:
- `git log --oneline -1` → `feat(30): swap python3 → node in skill callers`
- 8개 SKILL.md 파일 모두 존재 및 수정됨
</verification>

<success_criteria>
1. `grep -rn 'python3' skills/ .agents/skills/` 결과 0건 (ROADMAP Success Criterion #4 부분 충족 — hook 설정 파일은 Phase 29에서 완료)
2. 8개 SKILL.md 파일이 모두 커밋에 포함되고 각 파일에서 `python3` 토큰이 완전 소거됨
3. `node hooks/lessons_ranker.cjs` 호출이 sg-plan(1), sg-execute(1), sg-complete(1), .agents/sg-plan(1), .agents/sg-execute(1) 총 5건 존재
4. `node -e` Pattern A가 sg-quick에 2건, Pattern B가 sg-ui-plan과 .agents/sg-plan에 각 1건 존재
5. `.agents/skills/sg-ship/SKILL.md`에서 `python3 -m pytest` 없음 + `pytest 2>&1 || TEST_FAILED=1` 존재 (D-10, D-11 보존)
6. single atomic commit `feat(30): swap python3 → node in skill callers` 이 정확히 8개 파일 변경을 포함
</success_criteria>

<output>
완료 후 `.planning/phases/30-skill-agent/30-01-SUMMARY.md` 생성.

SUMMARY.md에 포함:
- 교체된 14개 occurrence 목록 (파일, 행, 패턴)
- 4-tier 검증 결과 (각 Tier pass/fail)
- commit SHA
- Tier 4 manual 확인 여부 (실행 시 결과, 미실행 시 "pending")
</output>
