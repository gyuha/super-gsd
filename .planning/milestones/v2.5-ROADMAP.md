# Milestone v2.5: Superpowers-Native File Parsing

**Status:** ✅ SHIPPED 2026-05-26
**Phases:** 32
**Total Plans:** 2

## Overview

super-gsd skills의 bash 파이프라인(grep/sed/awk) 파일 파싱을 Superpowers 방식(Read 도구 + Claude 해석)으로 전환하고, CLAUDE.md 컨벤션을 업데이트했다. Phase 32 회고(Conversation Analyzer lens)에서 3개 sg-rule 신규 생성, sg-plan discuss-phase 디렉토리 오작동 방지 패치, CLAUDE.md `.agents/` 쌍 커버 규칙 추가가 이루어졌다.

## Phases

### Phase 32: 파일 파싱 방식 전환

**Goal**: super-gsd skills의 파일 파싱 방식을 bash 파이프라인(grep|sed|awk)에서 Superpowers 방식(Read 도구 + Claude 해석)으로 전환하고, CLAUDE.md 컨벤션을 업데이트한다
**Depends on**: Phase 31 (v2.5 신규 시작)
**Requirements**: PARSE-01, PARSE-02, PARSE-03
**Plans**: 2 plans

Plans:

- [x] 32-01-PLAN.md — skills/sg-{complete,execute,plan,review,ship,ui-plan,lessons,quick}/SKILL.md bash 파싱 → Read 도구 전환 (PARSE-01)
- [x] 32-02-PLAN.md — .agents/skills/ 4개 + sg-retro lock 주석 + CLAUDE.md 컨벤션 업데이트 (PARSE-02, PARSE-03)

**Success Criteria** (all met):
1. ✓ `skills/sg-*/SKILL.md` 전체에서 STATE.md/HANDOFF.md/ROADMAP.md bash 파싱 제거 → Read 도구 교체
2. ✓ `CLAUDE.md` macOS 셸 이식성 섹션의 Phase lock 언급 제거, Superpowers Read 방식 권장
3. ✓ sg-next, sg-status, sg-start 등 파일 읽기 의존 skills 정상 동작 수동 검증 (code review 통과)

---

## Milestone Summary

**Key Decisions:**

- Read 도구 지시문은 bash 코드 블록 안에 주석으로 삽입하여 `else` 분기 구조를 유지 (32-02)
- sg-retro bash 파이프라인은 macOS 호환성 이유로 유지하고 주석만 교체 (32-02)
- HANDOFF.md init 블록(printf table headers)은 쓰기 작업이므로 bash 유지 (32-01)
- Step 8.5 wave/files_modified awk 블록(sg-execute)은 복잡 로직이므로 파싱 전환 대상 제외 (32-01)

**Issues Resolved:**

- `# Read .planning/...` bash 주석 패턴 — `.agents/` SKILL.md에서 Read 지시문이 bash 주석으로 오해되어 변수 미설정 (Phase 32 retro High severity → sg-rule 생성)
- `process.env.ARGUMENTS` 사용 — node 자식 프로세스에 셸 변수 자동 export 안됨 (Phase 32 retro High severity → sg-rule 생성)
- bash 펜스 안 Read 지시문 혼재 (Phase 32 retro High severity → sg-rule 생성)
- discuss-phase가 신규 phase 디렉토리 대신 기존 phase 디렉토리에 CONTEXT.md 작성하는 버그 → sg-plan pre-create + auto-move 패치 (Phase 32 retro Medium severity)
- `skills/` 패턴 변환 시 `.agents/` 변형 누락 → CLAUDE.md 쌍 커버 규칙 추가 (Phase 32 retro Medium severity)

**Issues Deferred:**

- v1.3 Multi-Platform Support (Phases 14-16) — 별도 마일스톤에서 처리
- HANDOFF "hookify" stage 값 보존 tech debt — v1.3에서 rename 검토 예정

**Technical Debt Incurred:**

- None

---

_For current project status, see .planning/ROADMAP.md_
