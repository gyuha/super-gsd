# Lessons: Phase 39 (2026-05-28)

## Lens: Start/Stop/Continue
_Captured: 2026-05-28T13:45:48Z_

### Start
- PLAN.md 파일 목록 작성 시 `grep -rln '>> .*HANDOFF'` 또는 실제 codebase 탐색으로 HANDOFF-appending 파일 전체를 확인 (sg-phase 누락 방지) — 39-01-SUMMARY
- 스키마 변경(열 추가/삭제) 시 sg-health NF 체크를 항목 체크리스트에 포함 — NF==7→8 업데이트를 코드 리뷰에서야 발견 (7c2d8c1)

### Stop
- PLAN.md에 수정 대상 파일 목록을 수동으로 나열하는 방식 — grep 탐색 없이 작성하면 sg-phase처럼 누락이 발생
- sg-execute success_criteria에 스키마 열 수를 하드코딩하는 패턴 — 변경할 때마다 stale해짐 (7c2d8c1)

### Continue
- 코드 리뷰 → --fix 즉시 수정 사이클 — 실질적 버그 4개(sg-phase, sg-health, HANDOFF.md 헤더, success_criteria)를 잡아냄
- CONTEXT.md에 결정사항(A~D) 사전 문서화 → 구현 중 불확실성 최소화 (39-CONTEXT.md)
- pairwise convention (skills/ + .agents/ 동시 수정) — v2.7 이후 누락 없이 자연스럽게 정착

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | PLAN.md 작성 시 HANDOFF-appending 파일 전체를 grep으로 탐색 | 다음 스키마 변경 Phase에서 `grep -rln '>> .*HANDOFF'` 실행 후 목록 검증 |
| P1 | 스키마 변경 시 sg-health NF 값 동시 업데이트 | 성공 기준에 "스키마 열 수 변경 시 NF 값 함께 업데이트" 명시 |
| P2 | success_criteria 스키마 열 수 하드코딩 제거 검토 | "6-column" 대신 "current HANDOFF schema" 형식으로 추상화 |

## Lens: 4Ls
_Captured: 2026-05-28T13:46:10Z_

### Liked
- 병렬 실행(Wave 1 + Wave 2 동시)으로 13+2 파일을 단일 세션에 처리 — sg-parallel-execute 실제 가치 확인 (39-01-SUMMARY)
- 코드 리뷰가 sg-phase·sg-health 두 가지 실질 버그를 잡아냄 — 리뷰 없이는 놓쳤을 것 (7c2d8c1)

### Learned
- HANDOFF append가 있는 파일은 grep으로 탐색해야 완전한 목록이 나옴 — 수동 나열은 불완전 (39-01-SUMMARY, sg-phase 누락)
- awk -F'|' 기준 NF 계산: 6열 행은 NF=8 (앞뒤 빈 필드 2개 포함) — 직관적이지 않음 (7c2d8c1 sg-health 수정)
- 스키마 변경은 데이터 파일뿐 아니라 검증기(sg-health)와 계약 문서(success_criteria)도 함께 업데이트해야 함

### Lacked
- PLAN.md 파일 목록 자동화 도구 — "HANDOFF append가 있는 파일 전체"를 미리 알 수 있었다면 sg-phase 누락 없었음
- sg-health 스키마 체크 검증 시나리오 — NF 숫자가 틀려도 실행 전에 감지할 방법이 없었음

### Longed For
- 스키마 버전 관리 또는 관련 검증 코드 자동 탐색 — 열 추가 시 영향받는 파서/검증기 목록 자동 생성
- CONTEXT.md 검증 기준에 sg-health 체크 항목도 포함했더라면 (이번 CONTEXT에서는 누락됨)

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | HANDOFF-appending 파일 자동 탐색 컨벤션 수립 | CLAUDE.md에 "HANDOFF 스키마 변경 시 grep -rln 탐색 필수" 추가 |
| P2 | sg-health NF 체크 설명에 awk NF 계산 공식 주석 추가 | sg-health SKILL.md에 "NF = 열수 + 2 (앞뒤 빈 필드)" 주석 |
| P3 | CONTEXT.md 검증 기준에 sg-health 체크 항목 포함 | 다음 스키마 변경 CONTEXT에서 "sg-health NF 값 확인" 항목 추가 |

## Lens: Decisions/Surprises/Patterns/Mistakes
_Captured: 2026-05-28T13:46:34Z_

### Decisions
- D-A: 선택적 6번째 컬럼 추가, 스키마 버전 필드 없음 — awk 빈 문자열 처리로 하위 호환 확보 (39-CONTEXT.md 결정 A)
- D-B: primary = HANDOFF User 컬럼($7), secondary = git log 작성자 — phase/* 브랜치 분석 제외 (39-CONTEXT.md 결정 B)
- D-D: 혼합 스키마(5열+6열 공존) 허용, 기존 행 마이그레이션 없음 (39-CONTEXT.md 결정 D)

### Surprises
- sg-phase가 PLAN 파일 목록에서 누락됨 — 코드 리뷰에서 발견, 7c2d8c1에서 수정 (39-01-PLAN.md 미기재)
- sg-health NF 체크가 구 5열 스키마 기준(NF==7)으로 잘못 설정되어 있었음 — 리뷰 전까지 미인지 (7c2d8c1)
- .agents/sg-complete, .agents/sg-ui-plan 디렉토리 없음 — 예상했지만 실제 ls 확인이 필요했음

### Patterns
- GIT_USER 추출 3줄 패턴(`git config user.name`, guard, echo)이 13개 파일에 동일하게 반복 → 공통 함수화 후보
- pairwise convention (skills/ + .agents/ 동시 수정) — v2.7부터 반복 확인, 이번 Phase에서도 자연스럽게 준수

### Mistakes
- sg-phase HANDOFF append에 GIT_USER 추가 누락 → 코드 리뷰 발견, 즉시 수정 (7c2d8c1)
- sg-health NF == 7 → 8 미업데이트 → 코드 리뷰 발견, 즉시 수정 (7c2d8c1)
- sg-execute success_criteria "5-column schema" 텍스트 stale → 코드 리뷰 발견, 즉시 수정 (7c2d8c1)

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | PLAN.md 파일 목록을 grep 기반으로 검증하는 컨벤션 도입 | 다음 PLAN 작성 시 `grep -rln 'HANDOFF_FILE'` 실행으로 누락 방지 |
| P2 | GIT_USER 3줄 패턴 공통화 검토 | Phase 40+ 에서 공통 스니펫 또는 헬퍼 함수 도입 타당성 검토 |
| P3 | 스키마 변경 체크리스트 작성 | sg-health NF, success_criteria 텍스트를 변경 체크리스트에 포함 |

## Lens: Sailboat
_Captured: 2026-05-28T13:46:52Z_

### Wind
- Phase 39 전체가 하루 내 완료 — Wave 1(13파일) + Wave 2(2파일) 병렬 실행 성공 (39-01-SUMMARY)
- 코드 리뷰 --fix 모드가 4개 문제를 즉시 발견·수정 — 품질 보증 사이클이 실제로 작동 (7c2d8c1)

### Anchor
- PLAN.md 파일 목록 수동 나열 → sg-phase 누락 — grep 탐색 없이 작성한 결과 (39-01-PLAN.md)

### Sun
- CONTEXT.md 사전 결정 문서화(A~D) → 구현 중 판단 비용 최소화 (39-CONTEXT.md)
- pairwise convention 정착 → 별도 체크 없이도 .agents/ 파일 누락 없음

### Rock
- NF 체크같은 검증 코드가 스키마 변경 시 조용히 틀려질 수 있음 — 향후 스키마 변경마다 sg-health 필수 업데이트 (7c2d8c1)

### Action Items
| priority | item | next step |
|----------|------|-----------|
| P1 | 스키마 변경 시 sg-health NF 체크 필수 업데이트 | sg-health를 "스키마 변경 영향 파일" 목록에 항상 포함 |
| P2 | PLAN.md 파일 목록 grep 검증 단계 추가 | PLAN 작성 후 `grep -rln '>> .*HANDOFF'` 실행으로 누락 검증 |
| P3 | 병렬 실행(sg-parallel-execute) 사용 지속 | Wave 구조 있는 Phase에서 병렬 실행 패턴 유지 |

