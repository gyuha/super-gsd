---
phase: 19-result-integration-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-execute.md
autonomous: true
requirements:
  - TE-04a
  - TE-04b
  - TE-05a
  - TE-05b

must_haves:
  truths:
    - "HANDOFF.md echo 라인이 Step 8에서 Step 8.5 이후로 이동된다"
    - "PARALLEL_GROUPS 유무에 따라 HANDOFF_TO가 'parallel' 또는 'superpowers'로 분기된다"
    - "Step 8.5의 'wave 필드 없음' 로그에 '[TE-05a]' 태그가 추가된다"
    - "Step 8 TS/FROM_STAGE/PHASE_SLUG 계산 로직은 변경되지 않는다"
    - "Step 7 idempotency 검사, Step 7.5 HANDOFF 초기화, Step 0 lessons 주입은 변경되지 않는다"
    - "Step 9 라우팅 분기(PARALLEL_GROUPS → sg-parallel-execute, 순차 → superpowers)는 변경되지 않는다"
  artifacts:
    - path: "commands/sg-execute.md"
      provides: "Step 8 HANDOFF_TO 분기 + Step 8.5 [TE-05a] 태그"
      contains: "HANDOFF_TO"
---

<objective>
`commands/sg-execute.md`를 수정하여:
1. Step 8의 HANDOFF.md echo 라인을 Step 8.5 이후로 이동하고, `$HANDOFF_TO` 변수를 사용하도록 변경한다.
2. Step 8.5 종료 후 `PARALLEL_GROUPS` 유무에 따라 `HANDOFF_TO`를 결정하고 HANDOFF.md에 기록한다.
3. Step 8.5의 "wave 필드 없음" echo에 `[TE-05a]` 태그를 추가한다.

Purpose: TE-04a, TE-04b, TE-05a, TE-05b 충족 — 병렬 경로는 To: parallel, 순차 경로는 To: superpowers로 정확히 기록한다.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: sg-execute.md Step 8 HANDOFF_TO 분기 + Step 8.5 [TE-05a] 태그</name>
  <files>commands/sg-execute.md</files>

  <read_first>
    - commands/sg-execute.md — Step 8 (lines ~117-126), Step 8.5 (lines ~128-237), Step 9 (lines ~239+) 전체 현재 상태 확인
    - .planning/phases/19-result-integration-regression/19-CONTEXT.md — D-03 결정사항 (HANDOFF_TO 변수 방식), D-04 ([TE-05a] 태그)
  </read_first>

  <action>
  3가지 변경을 순서대로 적용한다. 각 변경 전에 해당 라인을 Read tool로 확인한다.

  **변경 1: Step 8 — echo 라인 제거 (변수 계산만 남김)**

  Step 8 코드 블록에서 마지막 echo 라인만 제거한다:
  ```bash
  # 제거 대상 (Step 8 마지막 라인):
  echo "| $TS | $PHASE_SLUG | $FROM_STAGE | superpowers | $PLAN_HASH |" >> .planning/HANDOFF.md
  ```

  제거 후 Step 8 블록:
  ```bash
  TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)
  FROM_STAGE=$(grep -E '^\| [0-9]{4}-' .planning/HANDOFF.md | tail -1 | awk -F'|' '{gsub(/ /,"",$5); print $5}')
  if [ -z "$FROM_STAGE" ]; then
    FROM_STAGE="init"
  fi
  PHASE_SLUG=$(basename "$PHASE_DIR")
  ```

  Step 8 헤더도 "(변수만 계산 — HANDOFF.md 기록은 Step 8.5 완료 후 수행)" 으로 부제를 추가한다:
  ```
  8. **Append HANDOFF.md row (변수 계산).** HANDOFF_TO는 Step 8.5 완료 후 결정되므로, 이 단계에서는 메타 변수만 계산한다:
  ```

  **변경 2: Step 8.5 — wave 없음 로그에 [TE-05a] 태그 추가**

  현재:
  ```bash
  echo "wave 필드 없음 — 기존 순차 실행 경로 유지"
  ```

  변경 후:
  ```bash
  echo "[TE-05a] wave 필드 없음 — 기존 순차 실행 경로 유지. v1.3 이전 동작 보존."
  ```

  **변경 3: Step 8.5 종료 직후 (fi 다음) — HANDOFF_TO 분기 + echo 삽입**

  Step 8.5 코드 블록의 닫는 `fi` 바로 다음, Step 9 헤더(`9.`) 직전에 아래 블록을 삽입한다:

  ```bash
  # HANDOFF_TO 결정 및 HANDOFF.md 기록
  if [ -n "$PARALLEL_GROUPS" ]; then
    HANDOFF_TO="parallel"
  else
    HANDOFF_TO="superpowers"
  fi
  echo "| $TS | $PHASE_SLUG | $FROM_STAGE | $HANDOFF_TO | $PLAN_HASH |" >> .planning/HANDOFF.md
  ```

  이 블록은 Step 8.5 코드 블록의 닫는 ` ``` ` 안에 포함되거나, 별도 코드 블록으로 Step 9 앞에 추가된다. Step 9 헤더 바로 앞에 위치해야 한다.

  금지 사항:
  - Step 7 (idempotency 검사) 수정 금지
  - Step 7.5 (HANDOFF 초기화) 수정 금지
  - Step 0 (lessons reminder) 수정 금지
  - Step 9 라우팅 블록 수정 금지
  - Step 8.5 파싱 로직 수정 금지 (wave/files_modified 파싱, GROUP_COUNT 계산 등)
  </action>

  <verify>
  <automated>
    # 변경 1: superpowers 하드코딩이 제거됐는지
    HARD_COUNT=$(grep -c '"| \$TS | \$PHASE_SLUG | \$FROM_STAGE | superpowers | \$PLAN_HASH |"' commands/sg-execute.md 2>/dev/null || echo "0")
    [ "$HARD_COUNT" -eq 0 ] && echo "PASS: hardcoded superpowers echo removed" || echo "FAIL: hardcoded echo still present"

    # 변경 2: [TE-05a] 태그 존재
    TE05A=$(grep -c '\[TE-05a\]' commands/sg-execute.md || echo "0")
    [ "$TE05A" -ge 1 ] && echo "PASS: [TE-05a] tag present" || echo "FAIL: [TE-05a] tag missing"

    # 변경 3: HANDOFF_TO 변수 사용
    HTO_COUNT=$(grep -c 'HANDOFF_TO' commands/sg-execute.md || echo "0")
    [ "$HTO_COUNT" -ge 2 ] && echo "PASS: HANDOFF_TO variable present (>=2 occurrences)" || echo "FAIL: HANDOFF_TO missing or only 1 occurrence"

    # 순차 경로 보존
    SEQ=$(grep -c 'superpowers:executing-plans' commands/sg-execute.md || echo "0")
    [ "$SEQ" -ge 1 ] && echo "PASS: sequential path preserved" || echo "FAIL: sequential path removed"

    # Step 7 idempotency 보존
    IDEM=$(grep -c 'Idempotency check' commands/sg-execute.md || echo "0")
    [ "$IDEM" -ge 1 ] && echo "PASS: idempotency check preserved" || echo "FAIL: idempotency check missing"

    # Step 9 sg-parallel-execute 라우팅 보존
    PARA=$(grep -c 'sg-parallel-execute' commands/sg-execute.md || echo "0")
    [ "$PARA" -ge 1 ] && echo "PASS: sg-parallel-execute routing preserved" || echo "FAIL: sg-parallel-execute routing missing"
  </automated>
  </verify>

  <acceptance_criteria>
  - Step 8.5 종료 후 HANDOFF_TO="parallel" 또는 HANDOFF_TO="superpowers"로 분기되는 코드가 존재한다
  - HANDOFF.md echo 라인이 $HANDOFF_TO를 사용한다
  - Step 8.5 "wave 필드 없음" 로그에 "[TE-05a]" 태그가 포함된다
  - Step 7 idempotency 검사 코드가 변경되지 않았다
  - 순차 경로(superpowers:executing-plans)가 Step 9에 그대로 존재한다
  - sg-parallel-execute Skill() 호출이 Step 9에 그대로 존재한다
  </acceptance_criteria>

  <done>commands/sg-execute.md에 3가지 변경이 적용되어 있고, acceptance_criteria를 모두 충족한다.</done>
</task>

</tasks>

<success_criteria>
1. commands/sg-execute.md Step 8.5 종료 후 PARALLEL_GROUPS 유무로 HANDOFF_TO가 분기된다.
2. HANDOFF.md에 $HANDOFF_TO가 To 셀로 기록된다 (병렬: parallel, 순차: superpowers).
3. Step 8.5의 "wave 필드 없음" 로그에 [TE-05a] 태그가 포함된다.
4. Step 7, Step 7.5, Step 0, Step 9 라우팅 — 변경되지 않는다.
</success_criteria>
