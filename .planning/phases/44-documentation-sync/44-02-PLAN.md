---
phase: 44-documentation-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-retro/SKILL.md
  - .agents/skills/sg-retro/SKILL.md
autonomous: true
requirements: [DOC-01]
tags: [sg-retro, skill, frontmatter, pairwise, pick]

must_haves:
  truths:
    - "`skills/sg-retro/SKILL.md` line 3 frontmatter `description`에 `--pick` 토큰이 명시적으로 포함된다 (D-12 human override)"
    - "`.agents/skills/sg-retro/SKILL.md` line 3 frontmatter `description`에도 `--pick` 토큰이 의미 동등하게 포함된다 (D-16 pairwise mirror)"
    - "두 파일의 frontmatter `description`은 기존 필수 토큰(`three lenses`, `(ssc, dspm, analyze)`, smart default 관련 토큰)을 모두 보존한다 (Phase 42·43 cumulative)"
    - "`.agents/`의 `argument-hint`(line 4)와 `<language>` 블록 등 frontmatter 외 영역은 byte-identical 보존된다"
    - "두 파일 모두 본 plan 변경 후에도 YAML frontmatter 구조(`---` 구분자 2개, `name`/`description` 필드)는 손상되지 않는다"
  artifacts:
    - path: "skills/sg-retro/SKILL.md"
      provides: "line 3 description에 --pick 토큰 추가 (in-place 한 줄 갱신)"
      contains: "--pick"
    - path: ".agents/skills/sg-retro/SKILL.md"
      provides: "line 3 description에 --pick 토큰 추가 (pairwise mirror)"
      contains: "--pick"
  key_links:
    - from: "skills/sg-retro/SKILL.md line 3 (description)"
      to: ".agents/skills/sg-retro/SKILL.md line 3 (description)"
      via: "D-16 pairwise convention (Phase 32 Medium-1 + Phase 42 D-10 carry-forward)"
      pattern: "--pick"
---

<objective>
`skills/sg-retro/SKILL.md`와 `.agents/skills/sg-retro/SKILL.md`의 **frontmatter `description` 필드(line 3)** 한 줄만 surgical edit하여 Phase 43에서 추가된 `--pick` 동작을 광고에 반영한다 (CONTEXT.md D-12 human override + D-16 pairwise mirror).

변경 내용:

1. **`skills/sg-retro/SKILL.md` line 3** — 기존 `description`은 Phase 42에서 `three lenses (ssc, dspm, analyze)` + `Smart default (dspm+ssc)` 토큰을 이미 포함하나 `--pick` 토큰은 부재. 한 절 추가하여 `--pick` 동작을 명시.
2. **`.agents/skills/sg-retro/SKILL.md` line 3** — 기존 description도 `three lenses (ssc, dspm, analyze)` + `smart default (dspm+ssc)` 토큰을 이미 포함하나 `--pick` 환경 한정 안내는 `argument-hint`에만 존재. description에도 한 절 추가하되 환경 한정 문구(`AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI`)는 보존 (D-16).

Human override 적용 (CONTEXT D-12):
- 원본 D-12는 `--pick` 추가를 "권장 (옵셔널)"로 표기, D-13은 `.agents/` description은 "변경 권장 없음".
- 사용자 명시적 결정: **양쪽 모두 `--pick` 토큰을 description에 명시적으로 추가**. read-only verification이 아니라 real edit.

Out of scope (본 plan):
- frontmatter `name` 필드 — byte-identical
- `.agents/`의 `argument-hint` 필드(line 4) — 이미 Phase 43에서 `--pick is not supported in this environment ... use positional args instead` graceful-exit 안내 포함. byte-identical 보존.
- `<language>` 블록 및 본문 모든 step — byte-identical
- README.md/README.ko.md — 44-01-PLAN 담당
- `.planning/TEAM.md` — 44-03-PLAN 담당

Output: 두 파일 모두 line 3 description 한 줄만 변경된 pairwise mirror — 다른 모든 줄은 byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-documentation-sync/44-CONTEXT.md
@.planning/phases/43-pick-display-polish/43-CONTEXT.md
@.planning/phases/42-smart-default-lens/42-CONTEXT.md
@.planning/lessons/42-2026-05-30.md
@.planning/lessons/43-2026-05-30.md
@CLAUDE.md
@skills/sg-retro/SKILL.md
@.agents/skills/sg-retro/SKILL.md
</context>

<interfaces>
<!-- Phase 44 plan 44-02에서 변경되는 식별자와 정확한 토큰. -->
<!-- Phase 43 retro P1 #1 적용: 변경되는 산문 토큰 + 변경되는 구조를 묘사하는 보존 블록 두 카테고리를 모두 명시한다. -->

**대상 파일 (write):** `skills/sg-retro/SKILL.md`, `.agents/skills/sg-retro/SKILL.md`. **각 파일 정확히 1줄(line 3)의 description 필드만 변경.** 다른 모든 줄(frontmatter 구분자, `name` 필드, `argument-hint` 필드(.agents만), 모든 본문)은 byte-identical.

**A. `skills/sg-retro/SKILL.md` line 3 — 현재 (Phase 42 적용 상태):**

```
description: Use this when a phase is complete and a structured retrospective is needed — collects phase artifacts and git context, then facilitates one or more of three lenses (ssc, dspm, analyze) and appends results to .planning/lessons/. Smart default (dspm+ssc) runs when no lens argument is provided.
```

**A. `skills/sg-retro/SKILL.md` line 3 — 변경 후 (D-12 human override):**

```
description: Use this when a phase is complete and a structured retrospective is needed — collects phase artifacts and git context, then facilitates one or more of three lenses (ssc, dspm, analyze) and appends results to .planning/lessons/. Smart default (dspm+ssc) runs when no lens argument is provided; pass --pick for interactive lens selection.
```

변경 내용:
- 기존 마지막 문장 `Smart default (dspm+ssc) runs when no lens argument is provided.`의 마침표를 세미콜론(`;`)으로 교체
- 그 뒤에 ` pass --pick for interactive lens selection.` 한 절 추가
- 다른 모든 토큰(`Use this when ... is needed`, `collects phase artifacts and git context`, `three lenses (ssc, dspm, analyze)`, `appends results to .planning/lessons/`, `Smart default (dspm+ssc)`)은 byte-identical 보존

필수 포함 토큰 (변경 후):
- `Use this when` (Phase 25 description 패턴 보존)
- `three lenses (ssc, dspm, analyze)` (Phase 42 carry-forward)
- `Smart default (dspm+ssc)` (Phase 42 carry-forward, 대문자 S 유지)
- `--pick` (D-12 human override 신규 토큰)
- `interactive lens selection` (--pick 의미 설명)
- `.planning/lessons/` (Phase 42 carry-forward)

**B. `.agents/skills/sg-retro/SKILL.md` line 3 — 현재 (Phase 42·43 적용 상태):**

```
description: Run a structured retrospective on a GSD phase with one of three lenses (ssc, dspm, analyze) — select multiple lenses in one call or omit lens argument for smart default (dspm+ssc) — and append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md. AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.
```

**B. `.agents/skills/sg-retro/SKILL.md` line 3 — 변경 후 (D-12 + D-16 pairwise mirror):**

```
description: Run a structured retrospective on a GSD phase with one of three lenses (ssc, dspm, analyze) — select multiple lenses in one call or omit lens argument for smart default (dspm+ssc) — and append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md. `--pick` flag is rejected with a graceful exit explaining positional alternative (no AskUserQuestion in this environment). AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.
```

변경 내용:
- 기존 두 문장 사이(`...append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md.` 와 `AskUserQuestion-free version ...`)에 한 문장 삽입: ``--pick` flag is rejected with a graceful exit explaining positional alternative (no AskUserQuestion in this environment).``
- 기존 `AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.` 마지막 문장은 보존 (환경 한정 안내 — D-16)
- 다른 모든 토큰은 byte-identical 보존

필수 포함 토큰 (변경 후):
- `Run a structured retrospective on a GSD phase` (Phase 42 carry-forward)
- `three lenses (ssc, dspm, analyze)` (Phase 42 carry-forward)
- `smart default (dspm+ssc)` (Phase 42 carry-forward, 소문자 s — `.agents/`는 소문자 사용 중)
- `.planning/lessons/{NN}-{YYYY-MM-DD}.md` (Phase 42 carry-forward)
- ``--pick` flag is rejected with a graceful exit` (D-12 신규 토큰)
- `no AskUserQuestion in this environment` (`.agents/` 환경 제약 명시)
- `AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI` (Phase 14 carry-forward — 환경 한정 안내)

**`.agents/` 변경 정당화 (CONTEXT D-13 + 사용자 override):**
- 원본 D-13은 `.agents/`는 변경 권장 없음 — `argument-hint`(line 4)가 이미 `--pick` graceful-exit를 안내한다는 이유.
- 그러나 description은 SKILL 발동 트리거(`description` 기반 매칭)이므로, `--pick` 환경 한정 동작이 description에 명시되어야 사용자가 `.agents/`에서 `--pick`을 시도했을 때 즉시 의도를 이해할 수 있음.
- D-16 pairwise convention 유지를 위해 `skills/`와 동일하게 description 갱신 — 양쪽 description이 의미 분기(silent 거부 vs 명시 거부)되지 않도록.
- 단, `.agents/`는 `skills/`와 다른 톤(positive vs explicit-rejection) — `skills/`는 `pass --pick for interactive lens selection`(능동), `.agents/`는 `--pick flag is rejected with a graceful exit`(수동 reject 안내).

---

**변경되는 산문 토큰 (Phase 43 retro P1 #1 카테고리 1 — 명시적 enumerate):**

- `skills/sg-retro/SKILL.md` line 3의 마지막 문장 끝(`...is provided.`) — 마침표를 세미콜론으로 바꾸고 ` pass --pick for interactive lens selection.` 추가
- `.agents/skills/sg-retro/SKILL.md` line 3의 중간(`...md. AskUserQuestion-free` 사이) — 한 문장 삽입 ``--pick` flag is rejected with a graceful exit explaining positional alternative (no AskUserQuestion in this environment). ``

**변경되는 구조를 묘사하는 보존 블록 (Phase 43 retro P1 #1 카테고리 2 — 신규 sub-class, 본 plan 작성 시 명시):**

- **YAML frontmatter delimiters (`---` line 1 + line 4 [skills/] 또는 line 5 [.agents/])** — 구분자 행은 절대 변경 금지. line 3에 한 줄만 변경하므로 line 4·5의 `---` 위치가 보존되어야 함. acceptance에서 frontmatter close marker가 올바른 line에 있는지 검증.
- **`name: sg-retro` 필드 (line 2)** — byte-identical.
- **`.agents/`의 `argument-hint` 필드 (line 4)** — Phase 43에서 이미 `--pick is not supported in this environment (AskUserQuestion required) — use positional args instead.` graceful-exit 안내 추가됨. byte-identical 보존 (D-13 + Phase 43 carry-forward).
- **`<language>` 블록 (line 5-11 [skills/] 또는 line 6-12 [.agents/])** — byte-identical.
- **모든 본문 step(`<objective>`, Step 1-6, `<success_criteria>`, HANDOFF append 블록 등)** — byte-identical.

**보존되는 산문 토큰 (절대 변경 금지):**

`skills/sg-retro/SKILL.md`:
- line 1: `---`
- line 2: `name: sg-retro`
- line 3 description의 시작 절부터 `Smart default (dspm+ssc) runs when no lens argument is provided` 까지의 모든 토큰 — byte-identical
- line 4: `---`
- line 5~ 모든 본문 — byte-identical

`.agents/skills/sg-retro/SKILL.md`:
- line 1: `---`
- line 2: `name: sg-retro`
- line 3 description의 시작 절부터 `append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md.` 까지의 모든 토큰 — byte-identical
- line 3의 마지막 문장 `AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.` — byte-identical (단, 위치는 새 문장 뒤로 이동)
- line 4: `argument-hint: "[phase] [lens...|--pick] - e.g. '14 ssc' or '14 --pick'. lens: ssc|dspm|analyze. Omit lens for smart default (dspm+ssc). --pick is not supported in this environment (AskUserQuestion required) — use positional args instead."` — byte-identical
- line 5: `---`
- line 6~ 모든 본문 — byte-identical

**보존 블록과 변경 acceptance의 충돌 회피 (Phase 42 mistake #1 + Phase 43 P1 #1 직접 적용):**

- `--pick` 토큰은 `skills/sg-retro/SKILL.md`의 description 외 영역(`<objective>`, Step 1-6, success_criteria 등)에서 Phase 43이 이미 광범위하게 사용. 본 plan 변경 후 `skills/sg-retro/SKILL.md`의 `grep -c -- '--pick'` count는 Phase 43 기존 count + 1(description에 추가). 정확한 count보다 `≥ 기존+1` 범위 검증 권장 (Phase 43 retro P1 #1).
- `.agents/skills/sg-retro/SKILL.md`의 `--pick` 토큰은 기존에 `argument-hint`(line 4)에 2회, 본문에 N회 존재. 본 plan 추가 후 description에 1회 추가 → 정확한 count는 baseline에 따라 다름. acceptance는 `≥ 기존+1` 범위 검증.
- `three lenses (ssc, dspm, analyze)` 토큰은 description에 정확히 1회, 본문에 여러 회. 본 plan은 description의 이 토큰을 byte-identical 보존하므로 count 변경 없음 — acceptance는 description line에 한정한 line-aware grep 권장.
- `Smart default (dspm+ssc)` (`skills/` 대문자 S) vs `smart default (dspm+ssc)` (`.agents/` 소문자 s) — 두 파일이 다른 case 사용 중. 본 plan은 양쪽 모두 기존 case 보존 (drift 방지). acceptance는 각 파일별로 정확한 case 검증.

**`.agents/` frontmatter 검증 시 line 번호 주의:**

- `.agents/skills/sg-retro/SKILL.md`는 `argument-hint`가 있어 frontmatter가 5줄 구조 (line 1 `---`, line 2 `name`, line 3 `description`, line 4 `argument-hint`, line 5 `---`).
- `skills/sg-retro/SKILL.md`는 `argument-hint` 없는 4줄 구조 (line 1 `---`, line 2 `name`, line 3 `description`, line 4 `---`).
- 두 파일 frontmatter 구조가 다르므로 line-aware acceptance는 각 파일별 분리.

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: skills/sg-retro/SKILL.md line 3 description에 --pick 토큰 추가</name>
  <files>skills/sg-retro/SKILL.md</files>
  <read_first>
    - skills/sg-retro/SKILL.md (전체 — frontmatter 4줄 구조 확인 + 본문 변경 없음 확인)
    - .planning/phases/44-documentation-sync/44-CONTEXT.md (D-12, D-16, SC#3)
    - .planning/phases/42-smart-default-lens/42-CONTEXT.md (D-10 pairwise + Phase 42 description 갱신 사실 확인)
    - .planning/phases/43-pick-display-polish/43-CONTEXT.md (D-17 — Phase 43은 description 갱신 안 했음 확인)
    - CLAUDE.md §"사용자 언어 메시지" (description은 영문 source — 머신 토큰 영문 보존)
  </read_first>
  <action>
**A. `skills/sg-retro/SKILL.md` line 3 description 갱신:**

현재 line 3 (Phase 42 적용 상태 — verbatim 검증 후 시작):
```
description: Use this when a phase is complete and a structured retrospective is needed — collects phase artifacts and git context, then facilitates one or more of three lenses (ssc, dspm, analyze) and appends results to .planning/lessons/. Smart default (dspm+ssc) runs when no lens argument is provided.
```

변경 후 line 3:
```
description: Use this when a phase is complete and a structured retrospective is needed — collects phase artifacts and git context, then facilitates one or more of three lenses (ssc, dspm, analyze) and appends results to .planning/lessons/. Smart default (dspm+ssc) runs when no lens argument is provided; pass --pick for interactive lens selection.
```

변경 차이 (정확한 minimal diff):
- 기존: `... no lens argument is provided.`
- 변경: `... no lens argument is provided; pass --pick for interactive lens selection.`

minimal edit: 마지막 마침표(`.`)를 `;`로 교체 + ` pass --pick for interactive lens selection.` 19 글자 추가.

**B. 보존 검증:**
- line 1 `---`, line 2 `name: sg-retro`, line 4 `---`는 byte-identical 보존.
- line 5 이후 본문(`<language>` 블록부터 끝까지) 전체 byte-identical 보존.
- line 3의 시작부터 `Smart default (dspm+ssc) runs when no lens argument is provided` 까지의 모든 토큰은 byte-identical 보존.

**C. 산문 규칙 (CLAUDE.md):**
- em-dash `—` (U+2014) 보존. ASCII `--`로 교체 금지.
- 머신 토큰(`--pick`) 영문 그대로.
- description은 frontmatter 한 줄이므로 줄바꿈 금지. YAML scalar 한 줄로 작성.
  </action>
  <verify>
    <automated>
# A) 변경된 description 전체 substring 매칭
grep -F 'description: Use this when a phase is complete and a structured retrospective is needed — collects phase artifacts and git context, then facilitates one or more of three lenses (ssc, dspm, analyze) and appends results to .planning/lessons/. Smart default (dspm+ssc) runs when no lens argument is provided; pass --pick for interactive lens selection.' skills/sg-retro/SKILL.md && \

# B) 신규 토큰 존재
grep -F 'pass --pick for interactive lens selection' skills/sg-retro/SKILL.md && \

# C) 기존 토큰 보존
grep -F 'three lenses (ssc, dspm, analyze)' skills/sg-retro/SKILL.md && \
grep -F 'Smart default (dspm+ssc) runs when no lens argument is provided' skills/sg-retro/SKILL.md && \
grep -F 'collects phase artifacts and git context' skills/sg-retro/SKILL.md && \

# D) 이전 마침표 패턴 부재 (`is provided.` 단독은 부재 — 이제 세미콜론으로 이어짐)
! grep -F 'no lens argument is provided.

' skills/sg-retro/SKILL.md && \

# E) Frontmatter 구조 보존 (line 1 + line 4)
awk 'NR==1' skills/sg-retro/SKILL.md | grep -qx '\-\-\-' && \
awk 'NR==2' skills/sg-retro/SKILL.md | grep -qx 'name: sg-retro' && \
awk 'NR==4' skills/sg-retro/SKILL.md | grep -qx '\-\-\-' && \

# F) Line 3가 description 한 줄인지 확인
awk 'NR==3' skills/sg-retro/SKILL.md | grep -q '^description: Use this when a phase is complete' && \

# G) <language> 블록 보존 (line 5 시작)
awk 'NR==5' skills/sg-retro/SKILL.md | grep -qx '<language>' && \

# H) 본문 핵심 키워드 보존 (Phase 42·43 carry-forward)
grep -q 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md && \
grep -q 'PICK_MODE=false' skills/sg-retro/SKILL.md && \
grep -q '🔴 P1' skills/sg-retro/SKILL.md && \

# I) 본문 변경 사이즈 (description 한 줄만 변경)
test "$(git diff skills/sg-retro/SKILL.md | grep -c '^[+-][^+-]')" -le "4" && \

echo "Task 1 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -F 'pass --pick for interactive lens selection' skills/sg-retro/SKILL.md` matches (신규 토큰)
    - Source: `grep -F 'three lenses (ssc, dspm, analyze)' skills/sg-retro/SKILL.md` matches (Phase 42 carry-forward)
    - Source: `grep -F 'Smart default (dspm+ssc) runs when no lens argument is provided' skills/sg-retro/SKILL.md` matches (Phase 42 carry-forward)
    - Source: `grep -F 'Use this when a phase is complete and a structured retrospective is needed' skills/sg-retro/SKILL.md` matches (Phase 25 patterns 보존)
    - CLI: `awk 'NR==1' skills/sg-retro/SKILL.md` returns `---`
    - CLI: `awk 'NR==2' skills/sg-retro/SKILL.md` returns `name: sg-retro`
    - CLI: `awk 'NR==3' skills/sg-retro/SKILL.md` starts with `description: Use this when` and ends with `pass --pick for interactive lens selection.`
    - CLI: `awk 'NR==4' skills/sg-retro/SKILL.md` returns `---`
    - CLI: `awk 'NR==5' skills/sg-retro/SKILL.md` returns `<language>` (본문 시작 위치 보존)
    - CLI: `git diff skills/sg-retro/SKILL.md`의 `+/-` 라인 수 ≤ 4 (description 한 줄만 변경)
    - Source: `grep -c 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md` returns ≥ 1 (Phase 42 smart default 분기 보존)
    - Source: `grep -c 'PICK_MODE=false' skills/sg-retro/SKILL.md` returns ≥ 1 (Phase 43 --pick 탐지 보존)
  </acceptance_criteria>
  <done>
`skills/sg-retro/SKILL.md` line 3 description 한 줄만 surgical 갱신되어 `--pick` 동작이 광고에 반영되었다. Phase 42에서 추가한 `three lenses (ssc, dspm, analyze)` + `Smart default (dspm+ssc)` 토큰은 byte-identical 보존, 그 뒤에 한 절(`; pass --pick for interactive lens selection`)이 추가되었다. frontmatter 구조(line 1/2/4의 `---`/`name:`/`---`), `<language>` 블록(line 5~), 본문 모든 step(Phase 42·43 carry-forward 포함 — `PICK_MODE`, smart default 분기, `🔴 P1` prefix 등)은 byte-identical 보존되었다.
  </done>
</task>

<task type="auto">
  <name>Task 2: .agents/skills/sg-retro/SKILL.md line 3 description에 --pick graceful-exit 안내 추가 (pairwise mirror)</name>
  <files>.agents/skills/sg-retro/SKILL.md</files>
  <read_first>
    - .agents/skills/sg-retro/SKILL.md (전체 — frontmatter 5줄 구조 확인 + argument-hint 보존 확인)
    - .planning/phases/44-documentation-sync/44-CONTEXT.md (D-13, D-16 — human override)
    - .planning/phases/43-pick-display-polish/43-CONTEXT.md (D-17 — `.agents/` argument-hint graceful-exit 안내)
    - skills/sg-retro/SKILL.md (Task 1 적용 후 — pairwise mirror 비교)
    - CLAUDE.md §"skills/ + .agents/ 쌍 커버" (Phase 32 Medium-1 convention)
  </read_first>
  <action>
**A. `.agents/skills/sg-retro/SKILL.md` line 3 description 갱신:**

현재 line 3 (Phase 42·43 적용 상태 — verbatim 검증 후 시작):
```
description: Run a structured retrospective on a GSD phase with one of three lenses (ssc, dspm, analyze) — select multiple lenses in one call or omit lens argument for smart default (dspm+ssc) — and append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md. AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.
```

변경 후 line 3:
```
description: Run a structured retrospective on a GSD phase with one of three lenses (ssc, dspm, analyze) — select multiple lenses in one call or omit lens argument for smart default (dspm+ssc) — and append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md. `--pick` flag is rejected with a graceful exit explaining positional alternative (no AskUserQuestion in this environment). AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.
```

변경 차이 (정확한 minimal diff):
- 기존: `...append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md. AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.`
- 변경: `...append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md. ` + ``--pick` flag is rejected with a graceful exit explaining positional alternative (no AskUserQuestion in this environment). ` + `AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.`

minimal edit: 두 문장 사이에 한 문장 삽입 — `` `--pick` flag is rejected with a graceful exit explaining positional alternative (no AskUserQuestion in this environment). `` (118 글자 + trailing space 1 = 119 글자 추가).

**B. argument-hint(line 4) 보존 검증:**

현재 line 4 (Phase 43 carry-forward):
```
argument-hint: "[phase] [lens...|--pick] - e.g. '14 ssc' or '14 --pick'. lens: ssc|dspm|analyze. Omit lens for smart default (dspm+ssc). --pick is not supported in this environment (AskUserQuestion required) — use positional args instead."
```

본 plan은 line 4를 **byte-identical 보존**. `argument-hint`의 graceful-exit 안내(`--pick is not supported in this environment ... use positional args instead.`)는 description 신규 문장과 의미 일관 (silent override 거부 + 명시적 reject + alternative 안내).

**C. 보존 검증:**
- line 1 `---`, line 2 `name: sg-retro`, line 5 `---`는 byte-identical 보존.
- line 6 이후 본문(`<language>` 블록부터 끝까지) 전체 byte-identical 보존.
- line 3의 시작부터 `append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md.` 까지의 모든 토큰은 byte-identical 보존.
- line 3의 마지막 문장 `AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.`는 byte-identical 보존 (위치는 새 문장 뒤로 이동).

**D. 산문 규칙 (CLAUDE.md):**
- em-dash `—` (U+2014) 보존.
- 머신 토큰 `--pick`은 백틱 감싸기(`\`--pick\``) 사용 — `skills/`(백틱 없음)와 다른 case. `.agents/`는 백틱 사용이 일관된 스타일 (line 4 argument-hint에서도 `--pick`은 백틱 없음 — 단, description 산문 안에서는 백틱이 명확성 제공). 본 plan 변경 후 description의 `\`--pick\``는 frontmatter YAML scalar 안에서 백틱 1쌍이 첫 등장. YAML parser는 `\``를 특수문자로 해석하지 않으므로 안전.
- description은 YAML scalar 한 줄로 작성 — 줄바꿈 금지.

**E. Pairwise mirror 일관성 (D-16):**
- `skills/`는 `; pass --pick for interactive lens selection.` (positive, 한 동작 안내)
- `.agents/`는 `\`--pick\` flag is rejected with a graceful exit explaining positional alternative (no AskUserQuestion in this environment).` (rejection + alternative)
- 두 description의 톤은 다르지만 의미는 일관 — 양쪽 모두 `--pick` 동작을 description에 명시.
- D-16 pairwise convention 충족: 같은 milestone(v2.9) 내 같은 commit 또는 인접 commit에 두 변경이 같이 staged.
  </action>
  <verify>
    <automated>
# A) 변경된 description 전체 substring 매칭
grep -F 'description: Run a structured retrospective on a GSD phase with one of three lenses (ssc, dspm, analyze) — select multiple lenses in one call or omit lens argument for smart default (dspm+ssc) — and append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md. `--pick` flag is rejected with a graceful exit explaining positional alternative (no AskUserQuestion in this environment). AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.' .agents/skills/sg-retro/SKILL.md && \

# B) 신규 토큰 존재
grep -F '`--pick` flag is rejected with a graceful exit explaining positional alternative' .agents/skills/sg-retro/SKILL.md && \
grep -F 'no AskUserQuestion in this environment' .agents/skills/sg-retro/SKILL.md && \

# C) 기존 토큰 보존 (Phase 14·42 carry-forward)
grep -F 'Run a structured retrospective on a GSD phase with one of three lenses (ssc, dspm, analyze)' .agents/skills/sg-retro/SKILL.md && \
grep -F 'smart default (dspm+ssc)' .agents/skills/sg-retro/SKILL.md && \
grep -F 'append results to .planning/lessons/{NN}-{YYYY-MM-DD}.md' .agents/skills/sg-retro/SKILL.md && \
grep -F 'AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI' .agents/skills/sg-retro/SKILL.md && \

# D) argument-hint(line 4) 보존 (Phase 43 carry-forward)
grep -F 'argument-hint: "[phase] [lens...|--pick] - e.g. ' .agents/skills/sg-retro/SKILL.md && \
grep -F '--pick is not supported in this environment (AskUserQuestion required) — use positional args instead' .agents/skills/sg-retro/SKILL.md && \

# E) Frontmatter 구조 보존 (line 1 + line 5)
awk 'NR==1' .agents/skills/sg-retro/SKILL.md | grep -qx '\-\-\-' && \
awk 'NR==2' .agents/skills/sg-retro/SKILL.md | grep -qx 'name: sg-retro' && \
awk 'NR==5' .agents/skills/sg-retro/SKILL.md | grep -qx '\-\-\-' && \

# F) Line 3가 description 한 줄, Line 4가 argument-hint 한 줄
awk 'NR==3' .agents/skills/sg-retro/SKILL.md | grep -q '^description: Run a structured retrospective' && \
awk 'NR==4' .agents/skills/sg-retro/SKILL.md | grep -q '^argument-hint:' && \

# G) <language> 블록 보존 (line 6 시작 — argument-hint가 있으므로 1줄 밀림)
awk 'NR==6' .agents/skills/sg-retro/SKILL.md | grep -qx '<language>' && \

# H) 본문 변경 사이즈 (description 한 줄만 변경)
test "$(git diff .agents/skills/sg-retro/SKILL.md | grep -c '^[+-][^+-]')" -le "4" && \

# I) Pairwise mirror — 양쪽 description에 --pick 존재
grep -F '\-\-pick' skills/sg-retro/SKILL.md > /dev/null && \
grep -F '\-\-pick' .agents/skills/sg-retro/SKILL.md > /dev/null && \

echo "Task 2 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -F '\`--pick\` flag is rejected with a graceful exit explaining positional alternative' .agents/skills/sg-retro/SKILL.md` matches (신규 토큰)
    - Source: `grep -F 'no AskUserQuestion in this environment' .agents/skills/sg-retro/SKILL.md` matches (환경 명시)
    - Source: `grep -F 'Run a structured retrospective on a GSD phase with one of three lenses (ssc, dspm, analyze)' .agents/skills/sg-retro/SKILL.md` matches (Phase 42 carry-forward)
    - Source: `grep -F 'smart default (dspm+ssc)' .agents/skills/sg-retro/SKILL.md` matches (Phase 42 carry-forward, 소문자 s)
    - Source: `grep -F 'AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI' .agents/skills/sg-retro/SKILL.md` matches (Phase 14 carry-forward — line 3 끝부분 보존)
    - Source: `grep -F '--pick is not supported in this environment (AskUserQuestion required) — use positional args instead' .agents/skills/sg-retro/SKILL.md` matches (Phase 43 argument-hint 보존)
    - CLI: `awk 'NR==1' .agents/skills/sg-retro/SKILL.md` returns `---`
    - CLI: `awk 'NR==2' .agents/skills/sg-retro/SKILL.md` returns `name: sg-retro`
    - CLI: `awk 'NR==4' .agents/skills/sg-retro/SKILL.md` starts with `argument-hint:`
    - CLI: `awk 'NR==5' .agents/skills/sg-retro/SKILL.md` returns `---`
    - CLI: `awk 'NR==6' .agents/skills/sg-retro/SKILL.md` returns `<language>` (본문 시작 위치 보존)
    - CLI: `git diff .agents/skills/sg-retro/SKILL.md`의 `+/-` 라인 수 ≤ 4 (description 한 줄만 변경)
    - Pairwise mirror 충족: 양쪽 SKILL.md의 description에 `--pick` 토큰 존재 (D-16 충족)
  </acceptance_criteria>
  <done>
`.agents/skills/sg-retro/SKILL.md` line 3 description 한 줄만 surgical 갱신되어 `--pick` graceful-exit 동작이 광고에 반영되었다. 한 문장(`` `--pick` flag is rejected with a graceful exit explaining positional alternative (no AskUserQuestion in this environment).``)이 두 기존 문장 사이에 삽입되었고, `AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI.` 환경 한정 안내(Phase 14 carry-forward)는 끝부분에 byte-identical 보존되었다. line 4 `argument-hint`(Phase 43 carry-forward), frontmatter 구분자 행(line 1/5), `<language>` 블록(line 6~), 본문 모든 step은 byte-identical 보존되었다. `skills/` ↔ `.agents/` pairwise mirror가 D-16 convention에 따라 같은 wave에 적용되었다 — 양쪽 description에 `--pick` 토큰이 등장하되 톤은 환경에 맞게 분기(skills/ positive, .agents/ rejection + alternative).
  </done>
</task>

<task type="auto">
  <name>Task 3: SC#3 verification + pairwise diff 검증 + plan-spec deviation 사전 점검</name>
  <files></files>
  <read_first>
    - skills/sg-retro/SKILL.md (Task 1 적용 후)
    - .agents/skills/sg-retro/SKILL.md (Task 2 적용 후)
    - .planning/phases/44-documentation-sync/44-CONTEXT.md (SC#3, D-12, D-13, D-16)
    - .planning/lessons/43-2026-05-30.md (pairwise + Plan-spec Deviations 패턴)
  </read_first>
  <action>
Task 1+2 변경이 SC#3("sg-retro SKILL.md description frontmatter도 3 lens 반영")을 D-12 human override 해석으로 충족하는지 정적 검증하고, pairwise mirror 일관성과 plan-spec deviation 가능성을 사전 점검한다.

**SC#3 충족 시나리오 (D-12 human override 적용):**

원본 SC#3 문구는 "3 lens 반영"만 명시. Phase 42 적용 시점에 이미 `three lenses (ssc, dspm, analyze)` 토큰이 양쪽 description에 존재하므로 SC#3은 grep 검증만으로 충족 가능했음. 그러나 D-12 human override로 `--pick` 토큰도 양쪽 description에 명시적으로 추가됨 → SC#3을 의미적으로 확장 충족.

**정적 검증 명령:**

```bash
# 1. 양쪽 description에 3 lens 토큰 (Phase 42 carry-forward — 본 plan 보존)
grep -F 'three lenses (ssc, dspm, analyze)' skills/sg-retro/SKILL.md
grep -F 'three lenses (ssc, dspm, analyze)' .agents/skills/sg-retro/SKILL.md

# 2. 양쪽 description에 smart default 토큰 (Phase 42 carry-forward)
grep -F 'Smart default (dspm+ssc)' skills/sg-retro/SKILL.md       # 대문자 S
grep -F 'smart default (dspm+ssc)' .agents/skills/sg-retro/SKILL.md  # 소문자 s

# 3. 양쪽 description에 --pick 토큰 (본 plan 신규)
grep -F 'pass --pick for interactive lens selection' skills/sg-retro/SKILL.md
grep -F '`--pick` flag is rejected with a graceful exit' .agents/skills/sg-retro/SKILL.md

# 4. argument-hint 보존 (.agents/만 — Phase 43 carry-forward)
grep -F '--pick is not supported in this environment' .agents/skills/sg-retro/SKILL.md

# 5. Frontmatter 구조 보존
awk 'NR==1' skills/sg-retro/SKILL.md | grep -qx '\-\-\-'
awk 'NR==4' skills/sg-retro/SKILL.md | grep -qx '\-\-\-'
awk 'NR==1' .agents/skills/sg-retro/SKILL.md | grep -qx '\-\-\-'
awk 'NR==5' .agents/skills/sg-retro/SKILL.md | grep -qx '\-\-\-'

# 6. 본문 보존 (Phase 42·43 carry-forward 핵심 토큰)
grep -F 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md
grep -F 'PICK_MODE=false' skills/sg-retro/SKILL.md
grep -F '🔴 P1' skills/sg-retro/SKILL.md
```

**Pairwise diff 검증 (D-16):**

```bash
# 두 파일 모두 정확히 line 3만 변경 (≤ 4 +/- 라인)
git diff skills/sg-retro/SKILL.md | grep -c '^[+-][^+-]'        # expected: ≤ 4
git diff .agents/skills/sg-retro/SKILL.md | grep -c '^[+-][^+-]'  # expected: ≤ 4

# 양쪽 모두 같은 wave에 staged
# (sg-parallel-execute가 자동 처리)
```

**Plan-spec deviation 사전 점검 (Phase 42·43 retro 패턴):**

본 plan의 `<interfaces>` 블록 token spec과 acceptance 사이 충돌 가능성 점검:

**주의 deviation 시나리오:**

- `.agents/` description에서 `` `--pick` `` (백틱 감싼) 토큰이 등장. YAML scalar 안에서 backtick 사용이 안전한가? YAML 1.2 spec 기준 backtick은 unquoted scalar에서 안전하나, parser에 따라 다를 수 있음. 본 plan은 `argument-hint`처럼 double-quoted scalar 사용이 아닌 unquoted scalar에 backtick 포함. Phase 42·43 시점에 `.agents/`의 description은 unquoted scalar로 backtick 없이 운영됨. **본 plan에서 backtick 도입은 첫 사례** — YAML 호환성 risk가 낮지만 0은 아님. Mitigation: backtick 대신 plain text(`'--pick' flag` 또는 `the --pick flag`) 사용 옵션 — 그러나 `skills/` description은 plain text(`pass --pick`)를 이미 사용하므로 비대칭. 본 plan은 `.agents/`의 backtick을 유지하되 deviation으로 surface 가능성.
  - **사후 확인 가이드:** YAML parser가 frontmatter를 정상 파싱하는지 확인 — `node -e "const yaml = require('js-yaml'); const fs = require('fs'); const c = fs.readFileSync('.agents/skills/sg-retro/SKILL.md', 'utf8'); const m = c.match(/^---\n([\s\S]+?)\n---/); console.log(yaml.load(m[1]));"` 또는 단순히 description grep 매칭.
  - **대안 옵션:** backtick 제거하고 `the --pick flag is rejected with a graceful exit` 사용 — implementer가 YAML 파싱 risk 발견 시 결정.

- `skills/`와 `.agents/`의 description 톤이 다름 (positive vs rejection) — pairwise mirror가 의미적으로 일관하지만 form이 다름. 향후 reviewer가 "왜 두 description이 다르게 쓰여있나?" 질문할 가능성 → SUMMARY § "Plan-spec Deviations (Surfaced)"에 정직하게 기록 권장 (의도된 분기).

- 본 plan은 description 한 줄만 변경이므로 line 4 `---` 위치(`skills/`) 또는 line 5 `---` 위치(`.agents/`)가 보존되어야 함 — line 3에 줄바꿈을 실수로 넣으면 frontmatter 구조가 깨짐. Mitigation: acceptance에서 `awk 'NR==4'` / `awk 'NR==5'` 검증.

**스코프 일관성 점검:**

```bash
# README는 본 plan에서 손대지 않음
test "$(git diff --stat HEAD -- README.md 2>/dev/null | wc -l)" = "0"
test "$(git diff --stat HEAD -- README.ko.md 2>/dev/null | wc -l)" = "0"

# TEAM.md는 본 plan에서 손대지 않음
test "$(git diff --stat HEAD -- .planning/TEAM.md 2>/dev/null | wc -l)" = "0"

# 본문은 본 plan에서 손대지 않음 (description 한 줄만)
test "$(git diff skills/sg-retro/SKILL.md | grep -c '^[+-][^+-]')" -le "4"
test "$(git diff .agents/skills/sg-retro/SKILL.md | grep -c '^[+-][^+-]')" -le "4"
```

**최종 SUMMARY 작성 가이드 (D-16 staging 적용):**

- 본 plan은 양쪽 SKILL.md를 단일 commit으로 staging (pairwise convention, D-16).
- 44-01-PLAN(README 쌍)과 44-03-PLAN(TEAM.md)이 모두 wave:1이므로 sg-parallel-execute가 3-way Task() dispatch.
- 본 plan은 D-12 human override 적용 사례 — read-only verification에서 real edit로 결정한 이유를 SUMMARY에 명시 (사용자 결정의 trace).
  </action>
  <verify>
    <automated>
# 정적 SC#3 검증
grep -F 'three lenses (ssc, dspm, analyze)' skills/sg-retro/SKILL.md && \
grep -F 'three lenses (ssc, dspm, analyze)' .agents/skills/sg-retro/SKILL.md && \
grep -F 'Smart default (dspm+ssc)' skills/sg-retro/SKILL.md && \
grep -F 'smart default (dspm+ssc)' .agents/skills/sg-retro/SKILL.md && \

# --pick 토큰 양쪽 description 존재
grep -F 'pass --pick for interactive lens selection' skills/sg-retro/SKILL.md && \
grep -F '`--pick` flag is rejected with a graceful exit' .agents/skills/sg-retro/SKILL.md && \

# argument-hint 보존
grep -F '--pick is not supported in this environment' .agents/skills/sg-retro/SKILL.md && \

# Frontmatter 구조
awk 'NR==1' skills/sg-retro/SKILL.md | grep -qx '\-\-\-' && \
awk 'NR==4' skills/sg-retro/SKILL.md | grep -qx '\-\-\-' && \
awk 'NR==1' .agents/skills/sg-retro/SKILL.md | grep -qx '\-\-\-' && \
awk 'NR==5' .agents/skills/sg-retro/SKILL.md | grep -qx '\-\-\-' && \

# 본문 보존
grep -F 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md && \
grep -F 'PICK_MODE=false' skills/sg-retro/SKILL.md && \

# Out of scope: 다른 plan 영역 미변경
test "$(git diff --stat HEAD -- README.md 2>/dev/null | wc -l)" = "0" && \
test "$(git diff --stat HEAD -- README.ko.md 2>/dev/null | wc -l)" = "0" && \
test "$(git diff --stat HEAD -- .planning/TEAM.md 2>/dev/null | wc -l)" = "0" && \

# Diff size 합리적
test "$(git diff skills/sg-retro/SKILL.md | grep -c '^[+-][^+-]')" -le "4" && \
test "$(git diff .agents/skills/sg-retro/SKILL.md | grep -c '^[+-][^+-]')" -le "4" && \

echo "Task 3 PASS — SC#3 verified (D-12 human override applied) + pairwise mirror confirmed"
    </automated>
  </verify>
  <acceptance_criteria>
    - 양쪽 SKILL.md description에 `three lenses (ssc, dspm, analyze)` 토큰 (Phase 42 carry-forward)
    - 양쪽 SKILL.md description에 smart default + dspm+ssc 토큰 (case 차이 허용 — `skills/`는 `Smart default`, `.agents/`는 `smart default`)
    - 양쪽 SKILL.md description에 `--pick` 토큰 (D-12 human override 신규)
    - `.agents/`의 `argument-hint`(line 4) 보존 (Phase 43 carry-forward)
    - 두 파일 frontmatter 구조 보존 (`---` 구분자, `name:` 필드)
    - 두 파일 본문 byte-identical (Phase 42·43 핵심 토큰 — `LENS_CODES_ARRAY="dspm ssc"`, `PICK_MODE=false` 등)
    - Out-of-scope 영역(README 쌍, TEAM.md) 미변경
    - 각 SKILL.md 파일 git diff `+/-` 라인 수 ≤ 4 (description 한 줄만 변경)
    - Pairwise mirror 충족 — 양쪽 description이 `--pick` 동작을 명시 (톤은 환경에 맞게 분기)
  </acceptance_criteria>
  <done>
`skills/sg-retro/SKILL.md`와 `.agents/skills/sg-retro/SKILL.md` 양쪽 frontmatter `description`(line 3)에 D-12 human override에 따라 `--pick` 토큰이 명시적으로 추가되었다. SC#3("3 lens 반영")이 D-12 확장 해석으로 충족됨 — Phase 42 carry-forward 토큰(`three lenses (ssc, dspm, analyze)`, smart default + dspm+ssc) 보존 + 신규 `--pick` 토큰 추가. D-16 pairwise convention 준수: 양쪽 변경이 같은 wave에 staged, sg-parallel-execute가 3-way dispatch (44-01 README + 44-02 SKILL.md 쌍 + 44-03 TEAM.md). 본문 모든 step + `.agents/`의 `argument-hint`(Phase 43 graceful-exit 안내) byte-identical 보존. Out-of-scope 영역(README 쌍, TEAM.md) 미변경.
  </done>
</task>

</tasks>

<threat_model>

**T-44-01 — Spoofing (description-driven discoverability):** `description` 필드는 SKILL trigger의 1차 매칭 surface. 본 plan의 `--pick` 토큰 추가는 `--pick` 시도를 description 매칭으로 더 잘 routing하게 만든다. risk: description에 부정확한 토큰을 넣으면 사용자가 잘못된 기대로 SKILL을 invoke. Mitigation: `<interfaces>` 토큰 spec이 Phase 42·43 실제 동작과 일치 (`skills/`는 interactive lens selection, `.agents/`는 graceful exit 안내).

**T-44-02 — Tampering (YAML frontmatter 깨짐):** description은 YAML scalar 한 줄. 줄바꿈을 실수로 삽입하면 frontmatter 파싱 실패 → SKILL 로딩 실패. 본 plan의 `.agents/` description에 backtick(`` ` ``) 사용은 YAML 호환성 risk 낮지만 0 아님. Mitigation: Task 3 verify에서 `awk 'NR==4|5'`로 `---` 구분자 위치 검증 + 가능 시 YAML parser 테스트 사후 확인. plan-spec deviation 발생 시 SUMMARY에 surface.

**T-44-03 — Repudiation:** N/A. git commit으로 audit trail 보존. D-12 human override 적용 trace는 SUMMARY § "Decisions Applied"에 기록.

**T-44-04 — Information Disclosure:** N/A. SKILL.md description은 public 광고 surface.

**T-44-05 — Denial of Service:** N/A. text-only edit.

**T-44-06 — Elevation of Privilege:** N/A.

</threat_model>

<verification_steps>

**시나리오 1 — `skills/` description `--pick` 광고 (SC#3 확장):**
사용자가 `skills/sg-retro/SKILL.md` line 3을 보면 `three lenses (ssc, dspm, analyze)`, `Smart default (dspm+ssc)`, `pass --pick for interactive lens selection` 토큰이 모두 보인다. Phase 42·43 카탈로그가 description 하나로 완결.

**시나리오 2 — `.agents/` description graceful-exit 광고 (D-12 human override):**
Codex/Gemini/Antigravity 사용자가 `.agents/skills/sg-retro/SKILL.md` line 3을 보면 `--pick` 시도가 graceful exit + positional alternative로 처리됨을 description에서 즉시 이해. argument-hint(line 4)와 일관.

**시나리오 3 — Pairwise mirror 충족 (D-16):**
양쪽 SKILL.md에 `--pick` 토큰 등장. milestone v2.9 내 D-10/D-16 convention 충족. `.agents/`와 `skills/`가 description에서 의미 분기(silent vs explicit)되지 않음.

**시나리오 4 — Frontmatter 구조 보존:**
- `skills/sg-retro/SKILL.md`: line 1 `---`, line 2 `name: sg-retro`, line 3 `description: ...`, line 4 `---`, line 5 `<language>`.
- `.agents/skills/sg-retro/SKILL.md`: line 1 `---`, line 2 `name: sg-retro`, line 3 `description: ...`, line 4 `argument-hint: "..."`, line 5 `---`, line 6 `<language>`.

**시나리오 5 — argument-hint(.agents/) byte-identical 보존:**
Phase 43에서 추가된 `--pick is not supported in this environment (AskUserQuestion required) — use positional args instead.` 안내가 line 4에 그대로 존재. description의 새 graceful-exit 문장과 의미 일관.

**시나리오 6 — 본문 미변경:**
- `LENS_CODES_ARRAY="dspm ssc"` (Phase 42 smart default 분기) byte-identical
- `PICK_MODE=false` 탐지 블록 (Phase 43) byte-identical
- `🔴 P1` prefix 가이드 (Phase 43 DISPLAY-01) byte-identical
- INTENT_LINE case 매핑 (Phase 43 DISPLAY-02) byte-identical

**시나리오 7 — Plan-spec deviation surface (Phase 42·43 패턴):**
`.agents/` description의 backtick(`` `--pick` ``) 사용이 YAML 파싱 호환성 risk를 가질 수 있음. SUMMARY § "Plan-spec Deviations (Surfaced)"에 정직하게 기록. parser 사후 테스트 권장.

**시나리오 8 — Out-of-scope 미변경:**
`git diff --stat HEAD -- README.md` = 0. `git diff --stat HEAD -- README.ko.md` = 0. `git diff --stat HEAD -- .planning/TEAM.md` = 0.

**시나리오 9 — Surgical diff size:**
양쪽 SKILL.md 모두 `git diff`의 `+/-` 라인 수 ≤ 4 (정확히 1줄 변경 + 컨텍스트 ±2). 의도하지 않은 추가 변경 없음.

**시나리오 10 — D-12 human override 적용 trace:**
SUMMARY § "Decisions Applied"에 "D-12: 원본 옵셔널 → 사용자 명시 결정으로 real edit. D-13: 원본 변경 권장 없음 → D-16 pairwise convention + 사용자 결정으로 .agents/도 real edit" 기록.

</verification_steps>

<success_criteria>

1. `skills/sg-retro/SKILL.md` line 3 description에 D-12 human override 신규 토큰(`pass --pick for interactive lens selection`)이 정확히 추가되고, Phase 42 carry-forward 토큰(`three lenses (ssc, dspm, analyze)`, `Smart default (dspm+ssc)`, `.planning/lessons/`)이 byte-identical 보존된다.
2. `.agents/skills/sg-retro/SKILL.md` line 3 description에 D-12 + D-16 신규 토큰(`` `--pick` flag is rejected with a graceful exit explaining positional alternative (no AskUserQuestion in this environment)``)이 정확히 추가되고, Phase 42 carry-forward 토큰(`three lenses (ssc, dspm, analyze)`, `smart default (dspm+ssc)`, `.planning/lessons/{NN}-{YYYY-MM-DD}.md`) + Phase 14 carry-forward 토큰(`AskUserQuestion-free version for Codex/Gemini CLI/Antigravity CLI`)이 byte-identical 보존된다.
3. `.agents/skills/sg-retro/SKILL.md` line 4 `argument-hint`(Phase 43 carry-forward — `--pick is not supported in this environment ... use positional args instead`)가 byte-identical 보존된다.
4. 양쪽 SKILL.md의 frontmatter 구조(line 1/2/4의 `---`/`name:`/`---` for `skills/`, line 1/2/5의 `---`/`name:`/`---` for `.agents/`)가 보존되고, `<language>` 블록 및 본문 모든 step이 byte-identical 보존된다.
5. D-16 pairwise convention 준수 — 양쪽 SKILL.md의 description에 `--pick` 토큰이 등장 (톤 분기 허용: `skills/` positive vs `.agents/` rejection + alternative). 같은 wave(wave:1)에 staged.
6. 본 plan 외 영역(README.md, README.ko.md, .planning/TEAM.md) 미변경. 각 SKILL.md 파일 `git diff`의 `+/-` 라인 수 ≤ 4 (description 한 줄만 변경).
7. Phase 42·43 본문 핵심 토큰(`LENS_CODES_ARRAY="dspm ssc"`, `PICK_MODE=false`, `🔴 P1`, INTENT_LINE case 매핑 등) byte-identical 보존.
8. Plan-spec deviation 발생 시 SUMMARY § "Plan-spec Deviations (Surfaced)"에 정직하게 surface (특히 `.agents/` description의 backtick 사용 YAML 호환성 trace).
9. D-12 human override 적용 trace가 SUMMARY § "Decisions Applied"에 명시.

</success_criteria>

<output>

**SUMMARY 작성 가이드:**

본 plan 종료 시 `.planning/phases/44-documentation-sync/44-02-SUMMARY.md` 생성. 형식은 Phase 42-02-SUMMARY.md / Phase 43-02-SUMMARY.md 답습.

필수 섹션:
- `# Phase 44 — Plan 02 SUMMARY`
- `## Goal` — 1-2 문단 (양쪽 SKILL.md description에 `--pick` 토큰 추가, D-12 human override 적용)
- `## Decisions Applied` — D-12 human override(원본 옵셔널 → real edit) + D-13 override(원본 변경 권장 없음 → D-16 pairwise convention 적용) trace
- `## Changes Made` — 변경된 토큰 enumerate (양쪽 파일 line 3, 정확한 diff before/after)
- `## Verification` — Task 3 정적 검증 명령 결과
- `## Plan-spec Deviations (Surfaced)` — `.agents/` backtick YAML 호환성 trace + tone divergence(positive vs rejection) 의도성 explain
- `## D-10/D-16 Staging Note` — pairwise SKILL.md를 단일 commit으로 staging. 44-01 README + 44-03 TEAM.md는 별도 commit. 3 plan 모두 wave:1로 sg-parallel-execute가 3-way Task() dispatch.
- `## Next Steps` — 사용자가 sg-review 시 재실행할 수 있는 정적 검증 명령 1-2개

본 plan은 D-12/D-13 원본 결정을 사용자가 override한 사례 — 향후 phase에서 "discuss-phase 결정이 plan/execute 시점에 사용자 override로 바뀐 trace"를 SUMMARY에 명시하는 패턴이 안정화될 수 있는지 검증.

</output>
