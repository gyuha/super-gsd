---
phase: 01-plugin-scaffold
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - README.md
autonomous: true
requirements:
  - PLUGIN-02
tags:
  - claude-code-plugin
  - documentation
  - readme
must_haves:
  truths:
    - "A new user reading README understands what super-gsd is, what it requires (GSD/Superpowers/Hookify), and the 3-stage workflow"
    - "A new user can copy two install commands from README and install the plugin"
    - "A new user can run the 4-item Verify install checklist and confirm GSD/Superpowers/Hookify still work (non-invasive verified)"
    - "A Korean-speaking user finds a 한국어 요약 section preserving the core message"
  artifacts:
    - path: "README.md"
      provides: "9-section English-primary plugin landing page with ASCII workflow diagram, Korean summary, install commands, and verify checklist"
      contains: "/plugin marketplace add gyuha/super-gsd"
  key_links:
    - from: "README.md Installation section"
      to: ".claude-plugin/marketplace.json"
      via: "command `/plugin marketplace add gyuha/super-gsd`"
      pattern: "/plugin marketplace add gyuha/super-gsd"
    - from: "README.md Installation section"
      to: ".claude-plugin/plugin.json"
      via: "command `/plugin install super-gsd@super-gsd`"
      pattern: "/plugin install super-gsd@super-gsd"
    - from: "README.md Verify install section"
      to: "GSD / Superpowers / Hookify availability"
      via: "4-item checklist (/plugin list, /gsd-progress, superpowers:* skill, /hookify:help)"
      pattern: "/hookify:help"
---

## Phase Goal

**As a** Claude Code user who lands on the super-gsd GitHub page for the first time, **I want to** read a single README that explains the plugin, lists prerequisites, gives me copy-paste install commands, and tells me exactly how to verify my existing GSD/Superpowers/Hookify still work, **so that** I can install and trust the plugin without reading any other file.

<objective>
Write the single user-facing entry point — `README.md` — covering Phase 1 success criteria 2, 3, and the human side of 4. The README is English-primary with one Korean summary section, includes an ASCII workflow diagram (per D-11), and contains the exact install command pair and verify checklist mandated by D-07 and D-13.

Purpose: This is the only Phase 1 artifact a human reads directly. Manifests (Plan 01-01) make the plugin installable; README makes installation discoverable, understood, and verifiable.

Output: One file (`README.md`) satisfying PLUGIN-02 (README explains install method, dependencies, and workflow diagram).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-plugin-scaffold/01-CONTEXT.md
@.planning/phases/01-plugin-scaffold/01-SKELETON.md
@.planning/phases/01-plugin-scaffold/01-01-SUMMARY.md
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json

<interfaces>
The executor MUST read `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` (created by Plan 01-01) before writing README to mirror identity strings exactly. Specifically:

- `plugin.json.name` (= `"super-gsd"`) → must appear in README install commands AND `/plugin list` reference.
- `plugin.json.version` (= `"0.0.1"`) → optional to mention but if mentioned, must match.
- `plugin.json.description` → README's "What this is" opening line should be consistent with this description (rephrasing allowed; core meaning must match).
- `marketplace.json.name` (= `"super-gsd"`) → confirms the `/plugin install super-gsd@super-gsd` form (plugin@marketplace).
- `marketplace.json.owner` (= `"gyuha"`) → confirms the `gyuha/super-gsd` form in `/plugin marketplace add`.

The install command pair is therefore NOT improvised; it is derived from the two manifest files plus D-07.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with all 9 sections in CONTEXT.md D-12 order</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/01-plugin-scaffold/01-CONTEXT.md (D-07 install command shape, D-10 language policy, D-11 ASCII diagram, D-12 nine-section order, D-13 four-item verify checklist)
    - .planning/PROJECT.md (Core Value, "What This Is" — source for English description and Korean summary)
    - .planning/ROADMAP.md (Phase 2/3/4 entries — source for Roadmap section)
    - .claude-plugin/plugin.json (just created — must mirror name/description)
    - .claude-plugin/marketplace.json (just created — confirms owner/marketplace name)
    - .planning/phases/01-plugin-scaffold/01-01-SUMMARY.md (final identity strings as written to disk)
  </read_first>
  <action>
    Overwrite the existing stub `README.md` at repo root with the full Phase 1 README. The current file is a 12-byte stub — replace it entirely (do not append, do not preserve).

    Structure MUST be exactly these 9 sections, in this order (per D-12). Use level-1 `#` for the title and level-2 `##` for the other 8 sections. The Korean summary section heading must include the literal Korean characters `한국어 요약` so it can be found unambiguously.

    Section 1 — Title (`# super-gsd`):
    - Heading `# super-gsd` followed by a single tagline line summarizing the plugin in one English sentence. The tagline must mention all three of GSD, Superpowers, Hookify (chain semantic must be visible). May reuse / rephrase the description from `plugin.json` written in Plan 01-01.

    Section 2 — ASCII diagram (heading `## Workflow`):
    - A fenced code block (use ``` with no language tag or `text` tag) containing an ASCII art diagram (per D-11). Required: it must show at least three nodes labeled with the words `GSD`, `Superpowers`, `Hookify` and at least one arrow connecting them. The loop-back from Hookify to GSD should be visible (the "learning loop" is central per Core Value). Keep under 20 lines. Do not use Mermaid syntax; do not embed an image. ASCII only.

    Section 3 — What this is (heading `## What this is`):
    - 2–4 short English paragraphs explaining: (a) the role separation insight (planning vs. implementation vs. retrospection), (b) the problem super-gsd solves (manual handoff is fragile), (c) what Phase 1 ships specifically (an installable plugin shell; commands and hooks arrive in later phases — point readers at the Roadmap section). Be honest that Phase 1 has no commands yet — DO NOT oversell.

    Section 4 — Prerequisites (heading `## Prerequisites`):
    - A bulleted list of three required external plugins, each with the canonical name and a one-line description:
      - GSD (e.g., `get-shit-done-cc` or equivalent installation)
      - Superpowers (`claude-plugins-official/superpowers`)
      - Hookify (`claude-plugins-official/hookify`)
    - State explicitly that super-gsd is non-invasive and does not modify any of these (per D-14).

    Section 5 — Installation (heading `## Installation`):
    - Lead with one short paragraph: "After the prerequisites above are installed, run these two commands in your Claude Code session:"
    - Followed by a fenced code block containing EXACTLY these two lines, in this order, EXACTLY as written (per D-07 — these strings are LOCKED):
      ```
      /plugin marketplace add gyuha/super-gsd
      /plugin install super-gsd@super-gsd
      ```
    - Then one short closing paragraph telling the user to move to "Verify install" next.

    Section 6 — Verify install (heading `## Verify install`):
    - Lead with one sentence: "After installation, confirm super-gsd loaded cleanly and your existing tools still work."
    - A numbered list with EXACTLY 4 items, in this order, expressing the 4 checks from D-13 verbatim semantically:
      1. Run `/plugin list` and confirm `super-gsd` appears in the listing.
      2. Run `/gsd-progress` (or any GSD command) and confirm GSD responds normally — proves GSD remains intact.
      3. Access a Superpowers skill via the `Skill` tree (e.g., `superpowers:*`) and confirm it is still discoverable — proves Superpowers remains intact.
      4. Run `/hookify:help` and confirm Hookify responds — proves Hookify remains intact.
    - The list MUST have exactly 4 numbered items (acceptance criterion counts them).

    Section 7 — Roadmap (heading `## Roadmap`):
    - Brief paragraph: "super-gsd ships in MVP vertical slices."
    - A bulleted list of Phase 2, Phase 3, Phase 4 entries (Phase 1 is the current shipped phase; you may include it marked as shipped). Each entry: phase name + one-line description sourced from ROADMAP.md. Per D-09 must list future commands/hooks:
      - Phase 2 — Manual Handoff & Status: introduces `/super-gsd:to-superpowers` and `/super-gsd:status`
      - Phase 3 — Auto-Advance Hooks: introduces Stop / SubagentStop hooks
      - Phase 4 — Lessons Feedback Loop: introduces `.planning/lessons/` persistence

    Section 8 — 한국어 요약 (heading `## 한국어 요약`):
    - Heading text MUST contain the literal characters `한국어 요약`.
    - 1–3 short Korean paragraphs (per D-10). Distill PROJECT.md's "What This Is" + "Core Value" — the learning-loop theme and role-separation insight in Korean. Do not translate the entire English README; keep it a compact summary.

    Section 9 — License (heading `## License`):
    - One line: "Released under the MIT License. See [LICENSE](./LICENSE) for the full text."

    Formatting notes:
    - Trailing newline at end of file.
    - No HTML, no badges, no images in Phase 1.
    - Headings are the only place D-12 ordering is checked — keep them as the section anchors.
    - Do NOT add extra sections like "Contributing", "FAQ", "Acknowledgements" — those are explicitly deferred (CONTEXT.md Deferred Ideas).
  </action>
  <verify>
    <automated>bash -c 'set -e; test -s README.md; grep -Fq "/plugin marketplace add gyuha/super-gsd" README.md; grep -Fq "/plugin install super-gsd@super-gsd" README.md; grep -Fq "한국어 요약" README.md; grep -Fq "GSD" README.md && grep -Fq "Superpowers" README.md && grep -Fq "Hookify" README.md; HEADINGS=$(grep -E "^## " README.md | wc -l | tr -d " "); test "$HEADINGS" = "8" || { echo "expected 8 level-2 headings, got $HEADINGS"; exit 1; }; ORDER=$(grep -E "^## " README.md | tr "\n" "|"); echo "$ORDER" | grep -q "^## Workflow|## What this is|## Prerequisites|## Installation|## Verify install|## Roadmap|## 한국어 요약|## License|$" || { echo "section order wrong: $ORDER"; exit 1; }; VERIFY_ITEMS=$(awk "/^## Verify install/,/^## Roadmap/" README.md | grep -cE "^[0-9]+\\."); test "$VERIFY_ITEMS" = "4" || { echo "expected 4 numbered verify items, got $VERIFY_ITEMS"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists and is non-empty (file size > 1KB).
    - File contains the exact literal string `/plugin marketplace add gyuha/super-gsd` (single match minimum).
    - File contains the exact literal string `/plugin install super-gsd@super-gsd` (single match minimum).
    - File contains the literal Korean characters `한국어 요약` as part of a section heading.
    - File mentions all three external tools by name: `GSD`, `Superpowers`, `Hookify`.
    - File has exactly 8 level-2 (`## `) headings, in this order: `Workflow`, `What this is`, `Prerequisites`, `Installation`, `Verify install`, `Roadmap`, `한국어 요약`, `License`. (The Title is `# super-gsd` at level 1 and counts as section 1 but not as a `## ` heading; that's the D-12 9-section structure with 1 H1 + 8 H2.)
    - The "Verify install" section contains exactly 4 numbered list items (lines matching `^[0-9]+\.`).
    - The ASCII workflow diagram is inside a fenced code block (no Mermaid syntax — file does NOT contain the substring "```mermaid").
    - The Roadmap section mentions Phase 2, Phase 3, and Phase 4 (per D-09).
    - File does NOT contain any image syntax `![` (no images in Phase 1).
    - File ends with a trailing newline.
  </acceptance_criteria>
  <done>
    A reader who has never seen this project before can, by reading only README.md, (1) understand what super-gsd is, (2) confirm they have the three prerequisites, (3) copy two commands to install, (4) execute four checks to verify nothing broke, and (5) know what's coming in Phase 2/3/4. All four Phase 1 success criteria from ROADMAP.md are addressed by the combination of this plan + Plan 01-01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub README → end-user trust | The README is the marketing surface. What it claims about install method, prerequisites, and non-invasive behavior must match what the manifests actually do. |
| README command strings → user shell | Users will literally copy-paste `/plugin marketplace add ...` and `/plugin install ...`. Typos or drift between README and manifest would cause failed installs. |

## STRIDE Threat Register

Phase 1 ships only Markdown — no executable code in the README. The realistic threats are documentation-accuracy issues, not code execution issues.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | README install commands drift from marketplace.json owner/name | mitigate | Acceptance criterion enforces the exact literal strings `/plugin marketplace add gyuha/super-gsd` and `/plugin install super-gsd@super-gsd`. These strings must agree with `marketplace.json.owner` + `marketplace.json.name` + `plugin.json.name` produced by Plan 01-01; the `read_first` step requires reading both manifests before writing README. |
| T-02-02 | Spoofing | README claims compatibility with GSD/Superpowers/Hookify without users being able to verify | mitigate | Section 6 "Verify install" provides 4 concrete checklist items (D-13) the user can execute themselves — claim and verification ship together. |
| T-02-03 | Repudiation | README overpromises Phase 1 capability (e.g., claims commands exist) | mitigate | Section 3 "What this is" explicitly states Phase 1 ships no commands yet; Section 7 "Roadmap" places commands in Phase 2. The action instruction says "Be honest that Phase 1 has no commands yet — DO NOT oversell." |
| T-02-04 | Information Disclosure | Author/owner email leaks into README | mitigate | D-10 / D-06 forbid email; README does not include an author/owner field. No acceptance criterion requires email; positive criteria avoid the topic. |
| T-02-05 | Tampering | README provides Mermaid or image diagram that doesn't render in some viewers | mitigate | Acceptance criterion forbids `` ```mermaid `` substring and `![` image syntax. Per D-11, only ASCII. |
| T-02-SC | Supply chain (package installs) | npm/pip/cargo install steps | n/a | Phase 1 ships no executable code and recommends no package installs from README. The recommended commands are `/plugin marketplace add` and `/plugin install`, which are Claude Code native commands acting on this repo itself. |
| T-02-06 | Elevation of Privilege | README executes code | n/a | Markdown is not executable. Vector structurally absent. |
</threat_model>

<verification>
After the task completes, run end-to-end sanity:

```
# 1. README parses as Markdown with required anchor sections in correct order
grep -E "^# |^## " README.md

# 2. The two install command strings appear and are byte-identical to what marketplace.json + plugin.json imply
grep -F "/plugin marketplace add gyuha/super-gsd" README.md
grep -F "/plugin install super-gsd@super-gsd" README.md

# 3. Cross-file identity sanity: plugin name in manifests matches the install command in README
PNAME=$(node -p 'require("./.claude-plugin/plugin.json").name')
MOWNER=$(node -p 'require("./.claude-plugin/marketplace.json").owner')
grep -F "/plugin install ${PNAME}@${PNAME}" README.md
grep -F "/plugin marketplace add ${MOWNER}/${PNAME}" README.md

# 4. ASCII-only diagram — no Mermaid, no images
! grep -q '```mermaid' README.md && ! grep -q '!\[' README.md && echo "ascii-only: PASS"

# 5. Verify install has 4 numbered items, nothing more, nothing less
awk '/^## Verify install/,/^## Roadmap/' README.md | grep -cE '^[0-9]+\.'
```
</verification>

<success_criteria>
- README.md exists at repo root and is non-empty.
- D-12 nine-section order present (1 H1 + 8 H2 in the specified sequence).
- D-07 install command pair present byte-for-byte.
- D-11 workflow diagram is ASCII inside a fenced code block (no Mermaid, no image).
- D-10 English primary with one 한국어 요약 section.
- D-13 verify checklist has exactly 4 numbered items mapping to /plugin list, GSD, Superpowers, Hookify.
- D-09 Roadmap section lists Phase 2, 3, 4 future commands/hooks.
- Cross-file identity: install commands in README agree with `name` and `owner` fields in the manifests created by Plan 01-01.
</success_criteria>

<output>
Create `.planning/phases/01-plugin-scaffold/01-02-SUMMARY.md` summarizing:
- README.md final byte count and section count
- Confirmation of the two install command strings (copy-paste them verbatim into the SUMMARY for the record)
- Confirmation that ASCII diagram is present and Mermaid/image are absent
- Confirmation that 한국어 요약 section was included
- Any deviations from D-07..D-13 (should be none)
- Pointer that the combined output of Plan 01-01 + Plan 01-02 satisfies PLUGIN-01, PLUGIN-02, PLUGIN-03 and the 4 ROADMAP Phase 1 success criteria
</output>
