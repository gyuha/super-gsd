# Milestone Archive: v2.4 Hooks Node Migration

**Status:** ✅ Shipped 2026-05-26
**Phases:** 28–31 (4 phases, 7 plans)
**Commits:** ~45 commits (578016c → b6dbd33)
**Files changed:** 97 files, +9418 / -935 lines

## What Was Delivered

Python 의존성을 완전히 제거하고 Node.js 18+ 단독으로 실행되는 플러그인으로 전환.
`hooks/*.py` 4개 삭제, `hooks/*.cjs` 4개 신규 작성, 3개 hook config 교체, 8개 SKILL.md 교체, 문서 전면 갱신.

## Key Accomplishments

1. `hooks/{stop_hook,transcript_matcher,rule_runner,lessons_ranker}.cjs` 4개 신규 작성 — 외부 의존성 0, Python 버전과 동일한 stdin/stdout/exit code
2. `hooks/hooks.json`, `.codex/hooks.json`, `.gemini/settings.json` — `python3 ... .py` → `node ... .cjs` 전환 완료
3. 8개 SKILL.md (`skills/` + `.agents/skills/`) 내 모든 `python3` 호출 → `node`/`node -e`/`node hooks/*.cjs` 교체 (14 occurrences)
4. `hooks/*.py` 4개 일괄 삭제 (commit 877a666) — invocation-orphan 해소
5. CLAUDE.md/README.md/README.ko.md/CHANGELOG.md 문서 전면 갱신 (Node 18+ 기반)
6. v2.4 마일스톤 종합 검증: `grep -rn 'python3' hooks/ skills/ .agents/skills/ .codex/ .gemini/ CLAUDE.md README.md README.ko.md` → 0건

## Phase Details

### Phase 28: Core hook scripts Node 포팅

**Goal**: `hooks/*.py` 4개 파일이 외부 의존성 0인 순수 JS CommonJS(`.cjs`)로 재작성
**Requirements**: NODE-01, NODE-02, NODE-03, NODE-04
**Plans**: 28-00 (fixtures), 28-01 (transcript_matcher), 28-02 (rule_runner), 28-03 (lessons_ranker), 28-04 (stop_hook)
**Completed**: 2026-05-25

### Phase 29: Hook 설정 명령 교체

**Goal**: Claude Code/Codex/Gemini 3개 플랫폼 hook config의 `python3 → node` 교체
**Requirements**: CFG-01, CFG-02, CFG-03
**Plans**: 29-01 (3개 config 파일 일괄 교체)
**Completed**: 2026-05-25

### Phase 30: Skill/Agent 내부 호출 교체

**Goal**: `skills/sg-*/SKILL.md`와 `.agents/skills/sg-*/SKILL.md` 내 모든 `python3` 호출 교체
**Requirements**: SKILL-01, SKILL-02, AGENT-01
**Plans**: 30-01 (8개 SKILL.md 일괄 교체)
**Completed**: 2026-05-25

### Phase 31: 정리 + 문서

**Goal**: `.py` 4개 삭제 완료 확인 + CLAUDE.md/README/CHANGELOG Node 기반 갱신
**Requirements**: CLEAN-01, DOC-01, DOC-02, DOC-03
**Plans**: 31-01 (문서 3종 + plugin.json 0.0.38 bump)
**Completed**: 2026-05-26

## Milestone Summary

**Tech Decisions:**
- `.cjs` (CommonJS) 선택 — Node 18+ 내장 모듈만 사용, zero external deps, `require()` 호환성
- CLEAN-01은 반드시 마지막 phase — 모든 `.cjs` 검증 완료 후 원자적 삭제
- sg-health `.py` 존재 체크 버그 발견 및 리뷰 피드백으로 즉각 수정 (review → fix cycle 효과)

**Issues Resolved:**
- Code review (sg-review Phase 31): sg-health SKILL.md의 `.py` 존재 체크가 항상 WARN — Critical 즉시 수정
- sg-retro/sg-complete의 산문 `.py` 참조 — Important 즉시 수정

**Technical Debt:**
- Phase 15 역사 항목(README Roadmap)의 `hooks/*.py` 언급 — 보존 (역사 기록)
