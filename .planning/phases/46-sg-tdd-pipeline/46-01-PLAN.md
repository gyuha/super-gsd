---
phase: 46-sg-tdd-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-tdd/SKILL.md
  - .agents/skills/sg-tdd/SKILL.md
autonomous: true
requirements:
  - TDD-01
  - TDD-02
  - TDD-03
  - MIRROR-01

must_haves:
  truths:
    - "/super-gsd:sg-tdd를 호출하면 tdd_mode 플래그를 확인하고, 활성화 상태면 Superpowers test-driven-development 스킬을 오케스트레이션한다"
    - "Skill() 호출 전에 HANDOFF.md에 tdd stage 행이 append된다"
    - "tdd_mode=off 상태에서 직접 호출하면 경고 메시지와 활성화 방법을 안내한다"
    - "TDD 검증 실패 시 AskUserQuestion으로 소프트 경고를 표시하고, proceed/retry 선택지를 제공한다"
    - ".agents/skills/sg-tdd/SKILL.md가 존재하여 Codex/Gemini에서 $sg-tdd로 호출 가능하다"
  artifacts:
    - path: "skills/sg-tdd/SKILL.md"
      provides: "sg-tdd 슬래시 명령 정의"
      contains: "superpowers:test-driven-development"
    - path: ".agents/skills/sg-tdd/SKILL.md"
      provides: "Codex/Gemini sg-tdd 미러"
      contains: "test-driven-development"
  key_links:
    - from: "skills/sg-tdd/SKILL.md"
      to: "Superpowers test-driven-development"
      via: "Skill() invocation"
      pattern: "superpowers:test-driven-development"
    - from: "skills/sg-tdd/SKILL.md"
      to: ".planning/HANDOFF.md"
      via: "echo append"
      pattern: "execute.*tdd"
---

<objective>
skills/sg-tdd/SKILL.md와 .agents/skills/sg-tdd/SKILL.md 두 파일을 동시에 생성한다 (MIRROR-01 / D-08).

Purpose: sg-execute 완료 후 tdd_mode가 활성화된 경우에 호출되는 TDD 검증 게이트를 구현한다. Non-invasive 제약(D-07)에 따라 Superpowers 내부 파일을 수정하지 않고 Skill() 단일 호출로만 구현한다.
Output: skills/sg-tdd/SKILL.md, .agents/skills/sg-tdd/SKILL.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-sg-tdd-pipeline/46-CONTEXT.md
@.planning/phases/46-sg-tdd-pipeline/46-PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
<!-- 패턴 참조 — 아래 시그니처를 복제/적용한다. 탐색 불필요. -->

From skills/sg-execute/SKILL.md (analog role-match):
- YAML frontmatter: name, description, argument-hint 세 필드
- language 블록: 4행 언어 감지 지침
- objective, execution_context, process, success_criteria 블록 구조
- HANDOFF.md append 패턴 (6컬럼): | TS | PHASE_SLUG | FROM | TO | PLAN_HASH | GIT_USER |
- HANDOFF.md 자동 초기화 패턴 (header 없으면 생성)
- Skill() 호출은 마지막 문장 — 이후 코드 실행 없음

From .agents/skills/sg-execute/SKILL.md (mirror analog):
- constraints 블록: Platform Constraints (Codex / Gemini CLI / Antigravity CLI)
- AskUserQuestion 대신 plain text 경고 + read reply
- 명령 prefix: $sg-* (슬래시 없음)
- SubagentStop 미지원 명시

From hooks/stop_hook.cjs (config.json 읽기 패턴 bash 등가):
- TDD_MODE=$(node -e "try{const c=require('./.planning/config.json');console.log(c.super_gsd&&c.super_gsd.tdd_mode?'true':'false')}catch(e){console.log('false')}" 2>/dev/null || echo "false")

From skills/sg-next/SKILL.md (AskUserQuestion 패턴):
- AskUserQuestion(questions:[{question, header, multiSelect:false, options:[{label, description}]}])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: skills/sg-tdd/SKILL.md 생성</name>
  <files>skills/sg-tdd/SKILL.md</files>
  <read_first>
    - skills/sg-execute/SKILL.md — YAML frontmatter, language 블록, HANDOFF append 패턴, Skill() 호출 위치
    - .planning/phases/46-sg-tdd-pipeline/46-PATTERNS.md — sg-tdd 전체 패턴 명세 (frontmatter, tdd_mode guard, HANDOFF append, AskUserQuestion failure pattern, Terminal Skill invocation)
    - .planning/phases/46-sg-tdd-pipeline/46-CONTEXT.md — D-01~D-08 결정 사항 확인
  </read_first>
  <action>
    skills/sg-tdd/SKILL.md를 신규 생성한다. 파일 구조는 skills/sg-execute/SKILL.md를 역할 유사체로 사용한다.

    YAML frontmatter (per D-07 Non-invasive, D-08 MIRROR-01):
    - name: sg-tdd
    - description: Use this when execute is complete and TDD verification is required — invokes superpowers:test-driven-development and appends a tdd stage row to HANDOFF.md.
    - argument-hint: "[phase] - optional. Defaults to STATE.md current phase"

    language 블록: sg-execute의 4행 언어 감지 블록을 그대로 복사한다.

    objective 블록: "tdd_mode가 활성화된 경우 현재 phase의 구현 결과를 Superpowers test-driven-development 스킬로 TDD 준수 여부를 검증하고, HANDOFF.md에 tdd stage 행을 기록한다. Non-invasive 제약(D-07)에 따라 Superpowers 또는 GSD 내부 파일을 수정하지 않는다."

    process 블록은 다음 순서로 구성한다:

    Step 1 — tdd_mode 플래그 확인 (D-02, per D-03):
    config.json에서 tdd_mode를 읽는다. bash 등가 패턴(macOS 호환, jq/python3 미사용):
    TDD_MODE=$(node -e "try{const c=require('./.planning/config.json');console.log(c.super_gsd&&c.super_gsd.tdd_mode?'true':'false')}catch(e){console.log('false')}" 2>/dev/null || echo "false")
    tdd_mode가 "true"가 아니면 경고를 출력하고 계속 진행한다 (블록 아님, per D-02):
    메시지: "tdd_mode is not enabled. To activate: set super_gsd.tdd_mode: true in .planning/config.json. Recommended: re-run sg-execute first."

    Step 2 — Phase resolve:
    ARGUMENTS가 비어있지 않으면 PHASE_NUM=$ARGUMENTS, 아니면 Read .planning/STATE.md에서 YAML frontmatter의 Phase: 값을 추출한다. PHASE_NUM이 비어있으면 "Could not resolve current phase. Pass phase number explicitly: /super-gsd:sg-tdd <phase>" 출력 후 exit.

    Step 3 — Phase directory 확인:
    PHASE_PAD=$(printf "%02d" "$PHASE_NUM" 2>/dev/null || echo "$PHASE_NUM")
    PHASE_DIR=$(ls -d .planning/phases/${PHASE_PAD}-* 2>/dev/null | head -1)
    없으면 "No phase directory matches..." 출력 후 exit.

    Step 4 — Phase meta 읽기:
    Read .planning/ROADMAP.md에서 ### Phase PHASE_NUM: 섹션의 PHASE_NAME, GOAL, SC_TEXT를 추출한다.

    Step 5 — HANDOFF.md 자동 초기화:
    HANDOFF_FILE=".planning/HANDOFF.md"
    파일 없거나 header 없으면 생성한다 (sg-execute Step 7.5 패턴과 동일).

    Step 6 — HANDOFF.md에 tdd 행 append (Skill() 호출 전):
    From 컬럼은 항상 "execute"를 사용한다 (sg-tdd는 항상 execute 뒤에 온다, per PATTERNS.md):
    TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)
    PHASE_SLUG=$(basename "$PHASE_DIR")
    GIT_USER=$(git config user.name 2>/dev/null || echo "-")
    [ -z "$GIT_USER" ] && GIT_USER="-"
    echo "| $TS | $PHASE_SLUG | execute | tdd | - | $GIT_USER |" >> .planning/HANDOFF.md

    Step 7 — 프롬프트 빌드 + TDD 검증 완료 신호 출력 + Skill() 호출 (D-06, D-07):
    Superpowers에 전달할 컨텍스트 blob을 아래 형식으로 조립한다:
    # TDD Verification — Phase N (PHASE_NAME)
    ## Goal
    GOAL
    ## Success Criteria
    SC_TEXT
    ## Instruction
    Verify TDD compliance for the implementation above using superpowers:test-driven-development.
    Check that: (1) tests were written before or alongside implementation, (2) all tests pass, (3) no production code exists without a corresponding test.
    If TDD verification finds issues, surface them and ask the user: proceed to sg-review or retry.

    컨텍스트 blob 출력 후 반드시 아래 정확한 문자열을 출력한다 (D-06 transcript 신호):
    echo "TDD verification complete"

    그 다음 Skill() 호출 — 이후 어떤 코드도 실행하지 않는다 (Terminal Skill pattern):
    Skill(skill="superpowers:test-driven-development", args="<the context blob above>")

    TDD 검증 실패 처리 (D-01 — Superpowers 스킬이 실패를 보고할 때 sg-tdd 스킬 내부에서 처리):
    Superpowers 스킬 완료 후 실패 신호가 감지되면 AskUserQuestion으로 소프트 경고를 제공한다:
    AskUserQuestion(questions:[{question:"TDD verification found issues. How do you want to proceed?", header:"sg-tdd", multiSelect:false, options:[{label:"Proceed to sg-review", description:"Continues to /super-gsd:sg-review."},{label:"Retry TDD verification", description:"Re-runs superpowers:test-driven-development."}]}])

    success_criteria 블록:
    1. tdd_mode: false 또는 미설정 상태에서 호출하면 경고 메시지를 출력하고 계속 진행한다 (블록 없음).
    2. tdd_mode: true 상태에서 호출하면 Superpowers test-driven-development 스킬을 정확히 한 번 호출한다.
    3. Skill() 호출 전에 HANDOFF.md에 tdd stage 행이 기록된다 (From=execute, To=tdd, Plan Hash=-).
    4. Skill() 호출 직전에 "TDD verification complete" 문자열이 출력된다.
    5. TDD 검증 실패 시 AskUserQuestion으로 소프트 경고 + proceed/retry 선택지를 제공한다.
  </action>
  <verify>
    <automated>grep -c "superpowers:test-driven-development" skills/sg-tdd/SKILL.md && grep -c "TDD verification complete" skills/sg-tdd/SKILL.md && grep -c "tdd_mode" skills/sg-tdd/SKILL.md && grep -c "execute | tdd" skills/sg-tdd/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/sg-tdd/SKILL.md 파일이 존재한다
    - YAML frontmatter에 name: sg-tdd, description: "Use this when execute is complete...", argument-hint 필드가 있다
    - language 블록이 존재한다 (Korean/English 감지)
    - process 블록에 "tdd_mode" 읽기 + 경고 로직이 있다
    - "execute | tdd | - |" 패턴이 HANDOFF append 코드에 있다
    - "TDD verification complete" 문자열이 정확히 출력되는 코드가 있다
    - "superpowers:test-driven-development" Skill() 호출이 마지막 단계에 있다
    - AskUserQuestion failure soft-warn 패턴이 있다 (proceed/retry 선택지)
    - grep -c "superpowers:test-driven-development" skills/sg-tdd/SKILL.md 결과가 1 이상
  </acceptance_criteria>
  <done>skills/sg-tdd/SKILL.md가 생성되고 위 acceptance_criteria를 모두 충족한다</done>
</task>

<task type="auto">
  <name>Task 2: .agents/skills/sg-tdd/SKILL.md 생성 (MIRROR-01 pairwise sync)</name>
  <files>.agents/skills/sg-tdd/SKILL.md</files>
  <read_first>
    - skills/sg-tdd/SKILL.md — Task 1에서 방금 생성한 파일. 이 파일을 기준으로 미러를 만든다.
    - .agents/skills/sg-execute/SKILL.md — constraints 블록 패턴, $sg-* 명령 prefix, AskUserQuestion 대체 패턴
    - .planning/phases/46-sg-tdd-pipeline/46-PATTERNS.md — .agents/ 미러 차이점 명세 (constraints 블록, $sg-* prefix, AskUserQuestion → plain text)
  </read_first>
  <action>
    .agents/skills/sg-tdd/SKILL.md를 신규 생성한다. 기반은 Task 1에서 생성한 skills/sg-tdd/SKILL.md이다.
    MIRROR-01 / D-08 규칙에 따라 내용이 동일해야 하며 다음 세 가지만 변경한다:

    변경 1 — constraints 블록 추가 (objective 블록 뒤, execution_context 블록 앞에 삽입):
    <constraints>
    ## Platform Constraints (Codex / Gemini CLI / Antigravity CLI)
    - Superpowers integration unavailable: superpowers:test-driven-development skill cannot be used. Runs in direct TDD verification mode.
    - SubagentStop not supported: no automatic trigger on stage completion. Run $sg-review manually after completion.
    - AskUserQuestion not supported: failure path is presented as a plain text numbered list. Read user reply and proceed accordingly.
    </constraints>

    변경 2 — 모든 /super-gsd:sg-* 명령 참조를 $sg-* 형식으로 교체한다:
    - /super-gsd:sg-tdd → $sg-tdd
    - /super-gsd:sg-review → $sg-review
    - /super-gsd:sg-execute → $sg-execute

    변경 3 — AskUserQuestion 실패 경로를 plain text로 교체한다:
    AskUserQuestion(...) 블록 대신 다음 형식으로:
    TDD verification found issues. How do you want to proceed?
    1. Proceed to $sg-review
    2. Retry TDD verification
    (Read user reply and proceed: "1" → continue to sg-review, "2" → re-run verification)

    이 세 가지 변경 외 나머지 내용(Step 1~7 로직, HANDOFF append 패턴, config 읽기 패턴, "TDD verification complete" 신호 문자열)은 skills/ 원본과 동일하게 유지한다.
  </action>
  <verify>
    <automated>grep -c "superpowers:test-driven-development" .agents/skills/sg-tdd/SKILL.md && grep -c "Platform Constraints" .agents/skills/sg-tdd/SKILL.md && grep -c "\$sg-review" .agents/skills/sg-tdd/SKILL.md && grep -c "TDD verification complete" .agents/skills/sg-tdd/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .agents/skills/sg-tdd/SKILL.md 파일이 존재한다
    - constraints 블록이 있고 "Platform Constraints (Codex / Gemini CLI / Antigravity CLI)" 헤더가 있다
    - AskUserQuestion 대신 plain text numbered list로 실패 경로가 기술되어 있다
    - "$sg-review", "$sg-tdd", "$sg-execute" 형식의 명령 참조가 있다 (슬래시 없음)
    - "/super-gsd:sg-" 패턴이 없다: grep -c "/super-gsd:sg-" .agents/skills/sg-tdd/SKILL.md 결과가 0
    - "TDD verification complete" 문자열이 있다
    - "superpowers:test-driven-development" Skill() 호출이 있다
    - grep -c "Platform Constraints" .agents/skills/sg-tdd/SKILL.md 결과가 1 이상
  </acceptance_criteria>
  <done>.agents/skills/sg-tdd/SKILL.md가 생성되고 MIRROR-01 pairwise sync acceptance_criteria를 모두 충족한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → Superpowers | sg-tdd가 Skill()로 전달하는 args blob은 ROADMAP/PLAN 내용에서 파생 — 사용자 제어 데이터 |
| config.json → tdd_mode | .planning/config.json은 사용자 프로젝트 파일 — 임의 값 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | .planning/HANDOFF.md append | accept | append-only 패턴 준수, SKILL.md는 헤더 행을 수정하지 않음 |
| T-46-02 | Information Disclosure | config.json 읽기 오류 | accept | try/catch fallback → false, 민감 정보 노출 없음 |
| T-46-03 | Tampering | args blob in Skill() | accept | Superpowers 내부 처리, 외부 공격 면적 없음 |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | 이 플랜에 패키지 설치 없음 |
</threat_model>

<verification>
Phase 46 Plan 01 완료 확인:
- ls skills/sg-tdd/SKILL.md .agents/skills/sg-tdd/SKILL.md → 두 파일 모두 존재
- grep "superpowers:test-driven-development" skills/sg-tdd/SKILL.md → 매칭 1건 이상
- grep "TDD verification complete" skills/sg-tdd/SKILL.md → 매칭 1건 이상
- grep "Platform Constraints" .agents/skills/sg-tdd/SKILL.md → 매칭 1건 이상
- grep "/super-gsd:sg-" .agents/skills/sg-tdd/SKILL.md → 매칭 0건 (미러에 슬래시 명령 없음)
</verification>

<success_criteria>
- skills/sg-tdd/SKILL.md와 .agents/skills/sg-tdd/SKILL.md가 동시에 생성된다 (MIRROR-01)
- skills/ 파일은 /super-gsd:sg-tdd 형식, .agents/ 파일은 $sg-tdd 형식을 사용한다
- 두 파일 모두 "TDD verification complete" 신호 문자열을 포함한다
- Non-invasive 제약 준수: Superpowers/GSD 내부 파일을 수정하지 않는다
- HANDOFF append From="execute", To="tdd", Plan Hash="-" 패턴이 구현된다
</success_criteria>

<output>
완료 시 .planning/phases/46-sg-tdd-pipeline/46-01-SUMMARY.md를 생성한다
</output>
