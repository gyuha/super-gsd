---
phase: 05-lessons-feedback-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/transcript_matcher.py
  - hooks/stop_hook.py
autonomous: true
requirements:
  - LESS-01

must_haves:
  truths:
    - "Hookify가 완료되면 stop_hook.py가 .planning/lessons/ 파일을 자동으로 생성한다"
    - "같은 날 같은 phase에서 두 번 실행해도 lessons 파일이 중복 생성되지 않는다"
    - "lessons 저장 실패가 Claude Code의 동작을 차단하지 않는다"
    - "auto_advance: false 설정 시 hookify-complete 분기도 비활성화된다"
  artifacts:
    - path: "hooks/transcript_matcher.py"
      provides: "HOOKIFY_SIGNALS 상수 + hookify-complete 반환값을 포함한 detect_signal()"
      exports: ["detect_signal"]
    - path: "hooks/stop_hook.py"
      provides: "save_hookify_lessons() 함수 + hookify-complete 분기 처리"
      contains: "hookify-complete"
  key_links:
    - from: "hooks/stop_hook.py"
      to: ".planning/lessons/{NN}-{YYYY-MM-DD}.md"
      via: "save_hookify_lessons() 호출"
      pattern: "save_hookify_lessons"
    - from: "hooks/transcript_matcher.py"
      to: "hooks/stop_hook.py"
      via: "detect_signal() 반환값 'hookify-complete'"
      pattern: "hookify-complete"
---

<objective>
transcript_matcher.py에 HOOKIFY_SIGNALS를 추가하고 detect_signal()이 'hookify-complete'를 반환하도록 확장한다. stop_hook.py에 save_hookify_lessons() 함수와 hookify-complete 분기를 추가해 Hookify 완료 시 .planning/lessons/{NN}-{YYYY-MM-DD}.md를 자동 생성한다.

Purpose: LESS-01 — Hookify 출력을 다음 GSD phase가 참조할 수 있도록 파일로 영속화한다.
Output: 수정된 transcript_matcher.py, 수정된 stop_hook.py, .planning/lessons/ 자동 생성 동작
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@hooks/transcript_matcher.py
@hooks/stop_hook.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: transcript_matcher.py에 HOOKIFY_SIGNALS + hookify-complete 반환 추가</name>
  <files>hooks/transcript_matcher.py</files>
  <action>
    기존 파일의 두 신호 분기 패턴을 유지하면서 세 번째 신호를 추가한다.

    1. docstring의 반환값 명세를 업데이트한다:
       `Returns: 'gsd-plan-complete' | 'superpowers-review-complete' | 'hookify-complete' | ''`

    2. REVIEW_SIGNALS 아래에 HOOKIFY_SIGNALS 상수를 추가한다:
       ```
       HOOKIFY_SIGNALS = [
           'hookify',
           'Retrospective complete',
           'hooks generated',
           'patterns extracted',
       ]
       ```

    3. `return ''` 직전에 hookify-complete 감지 분기를 추가한다:
       ```
       if any(sig in recent for sig in HOOKIFY_SIGNALS):
           return 'hookify-complete'
       ```

    우선순위 순서는 반드시 GSD_PLAN → REVIEW → HOOKIFY 순이어야 한다. 기존 두 분기의 순서와 로직은 변경하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/super-gsd && python3 -c "
from hooks.transcript_matcher import detect_signal
import tempfile, os

# hookify-complete 신호 감지 테스트
with tempfile.NamedTemporaryFile(mode='w', suffix='.txt', delete=False) as f:
    f.write('hookify\nsome output\npatterns extracted\n')
    path = f.name
try:
    result = detect_signal(path)
    assert result == 'hookify-complete', f'Expected hookify-complete, got {result!r}'
    print('PASS: hookify-complete detected')
finally:
    os.unlink(path)

# 기존 신호 회귀 테스트
with tempfile.NamedTemporaryFile(mode='w', suffix='.txt', delete=False) as f:
    f.write('gsd-plan-phase\nPLAN.md created\n')
    path = f.name
try:
    result = detect_signal(path)
    assert result == 'gsd-plan-complete', f'Expected gsd-plan-complete, got {result!r}'
    print('PASS: gsd-plan-complete still works')
finally:
    os.unlink(path)

print('All tests passed')
"
</automated>
  </verify>
  <done>detect_signal()이 hookify 관련 텍스트를 포함한 transcript에서 'hookify-complete'를 반환하고, 기존 두 신호도 동일하게 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: stop_hook.py에 save_hookify_lessons() + hookify-complete 분기 추가</name>
  <files>hooks/stop_hook.py</files>
  <action>
    기존 stop_hook.py의 구조를 유지하면서 두 가지를 추가한다.

    1. import 블록에 `import datetime`, `import re` 를 추가한다 (json, os, sys 뒤에 배치).

    2. load_config() 함수 아래, main() 함수 위에 세 개의 헬퍼 함수를 추가한다.

       `_read_current_phase()`:
       - `.planning/STATE.md`를 열어 `^Phase:\s*(\S+)` 패턴의 첫 번째 매치를 반환한다.
       - 파일 없거나 매치 없으면 `'unknown'`을 반환한다.
       - 모든 예외를 catch해 `'unknown'` 반환.

       `_extract_hookify_output(transcript_path)`:
       - transcript_path 파일을 열어 마지막 200줄을 읽는다.
       - 마지막 200줄 텍스트에서 다음 마커를 순서대로 rfind로 검색한다:
         `'## Lessons'`, `'## Patterns'`, `'## Hooks Generated'`, `'hookify complete'`
       - 마커를 찾으면 그 위치 이후 텍스트를 반환한다.
       - 마커 없으면 최근 200줄 전체 텍스트를 반환한다.
       - 모든 예외를 catch해 `''` 반환.

       `save_hookify_lessons(transcript_path)`:
       - `_read_current_phase()`로 phase 번호를 얻는다.
       - `_extract_hookify_output(transcript_path)`로 내용을 얻는다. 내용이 비어 있으면 `''` 반환.
       - 파일명을 결정한다: `today = datetime.date.today().strftime('%Y-%m-%d')`, phase 번호가 숫자이면 `f"{int(phase):02d}"` 형식으로 zero-padding, 아니면 그대로 사용. 최종 파일명: `f"{padded}-{today}.md"`.
       - filepath = `os.path.join('.planning', 'lessons', filename)`.
       - `os.makedirs(os.path.dirname(filepath), exist_ok=True)` 로 디렉토리를 보장한다.
       - 파일이 이미 존재하면 그 경로를 그대로 반환한다 (idempotency — 같은 날 같은 phase 중복 방지).
       - 새 파일을 쓴다. 헤더: `f"# Lessons: Phase {phase} ({today})\n\n"`, 이어서 추출된 내용.
       - 성공 시 filepath 반환. 모든 예외를 catch해 `''` 반환.

    3. main() 함수의 `else:` 분기를 `hookify-complete` 분기로 교체한다:
       ```
       elif signal == 'hookify-complete':
           lesson_file = save_hookify_lessons(transcript_path)
           if lesson_file:
               msg = (
                   f"Hookify complete. Lessons saved to {lesson_file}. "
                   "Run /super-gsd:sg-plan to start the next phase — prior lessons will be included as context."
               )
           else:
               msg = "Hookify complete. Run /super-gsd:sg-plan to start the next phase."
           print(json.dumps({"systemMessage": msg}), file=sys.stdout)
       else:
           print(json.dumps({}), file=sys.stdout)
       ```

    기존 finally: sys.exit(0) 패턴을 유지한다. 어떤 경로에서도 exit 0이 보장되어야 한다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/super-gsd && python3 -c "
import sys, os, json, tempfile, datetime

# stop_hook을 직접 import해서 헬퍼 함수들 테스트
sys.path.insert(0, 'hooks')
import importlib.util
import hooks.stop_hook as sh

# _read_current_phase 테스트
phase = sh._read_current_phase()
print(f'_read_current_phase: {phase!r}')
assert phase != None, 'should return a string'

# save_hookify_lessons 테스트 — hookify 내용이 있는 임시 transcript
with tempfile.NamedTemporaryFile(mode='w', suffix='.jsonl', delete=False) as f:
    f.write('## Lessons\n- Use smaller tasks\n- Test before committing\n')
    tpath = f.name
try:
    result = sh.save_hookify_lessons(tpath)
    print(f'save_hookify_lessons returned: {result!r}')
    if result:
        assert os.path.exists(result), f'File not found: {result}'
        content = open(result).read()
        assert '# Lessons: Phase' in content, 'Missing header'
        print(f'PASS: lessons file created at {result}')
        os.remove(result)
    else:
        print('INFO: No lessons file (phase may be unknown, acceptable)')
finally:
    os.unlink(tpath)

print('stop_hook.py helpers: OK')
"
</automated>
  </verify>
  <done>save_hookify_lessons()가 transcript에서 내용을 추출해 .planning/lessons/{NN}-{YYYY-MM-DD}.md를 생성한다. hookify-complete signal이 들어오면 파일 저장 + 안내 메시지가 systemMessage로 출력된다. 기존 신호 분기는 변경 없이 동작한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript file → Python | 외부 파일 읽기 — 경로 traversal 및 인코딩 오류 가능 |
| lessons file write → disk | .planning/lessons/ 경로 밖에 쓰지 않아야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | _extract_hookify_output() | mitigate | transcript_path는 Claude Code가 주입하는 경로만 수락; open() 실패 시 '' 반환으로 처리 |
| T-05-02 | Information Disclosure | .planning/lessons/*.md | accept | 프로젝트 내부 파일, GSD .planning/ 컨벤션과 동일 보안 레벨 |
| T-05-03 | Denial of Service | stop_hook.py crash | mitigate | finally: sys.exit(0) — 모든 경로에서 exit 0 보장, 훅 실패가 Claude Code 차단 불가 |
| T-05-SC | Tampering | npm/pip installs | accept | 이 플랜은 신규 패키지 설치 없음 — stdlib(datetime, re, os)만 사용 |
</threat_model>

<verification>
1. `python3 -c "from hooks.transcript_matcher import detect_signal; print(detect_signal.__doc__)"` — 'hookify-complete' 반환값이 docstring에 포함되어 있어야 한다.
2. `grep -c 'hookify-complete' hooks/transcript_matcher.py` — 결과 >= 1
3. `grep -c 'hookify-complete' hooks/stop_hook.py` — 결과 >= 1
4. `grep -c 'save_hookify_lessons' hooks/stop_hook.py` — 결과 >= 2 (정의 + 호출)
5. `python3 hooks/stop_hook.py` 에 빈 JSON stdin 전달 시 exit 0으로 종료되어야 한다:
   `echo '{"transcript_path":""}' | python3 hooks/stop_hook.py && echo "exit: $?"`
</verification>

<success_criteria>
1. Hookify 완료 transcript가 주어지면 stop_hook.py가 .planning/lessons/{NN}-{YYYY-MM-DD}.md를 생성한다.
2. 같은 날 같은 phase에서 두 번 실행해도 파일이 한 개만 존재한다.
3. lessons 저장 실패(I/O 오류 등)가 Claude Code 동작을 차단하지 않는다 — exit 0 항상 보장.
4. auto_advance: false 설정 시 hookify-complete 분기도 실행되지 않는다.
</success_criteria>

<output>
완료 후 `.planning/phases/05-lessons-feedback-loop/05-01-SUMMARY.md` 를 생성한다.
</output>
