---
phase: 02-manual-handoff-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/HANDOFF.md
  - .claude-plugin/plugin.json
  - CHANGELOG.md
autonomous: true
requirements:
  - STATE-02

must_haves:
  decisions:
    - D-15  # HANDOFF.md를 이번 phase에서 최초 생성
    - D-22  # markdown table, append-only
    - D-23  # 열 5개: Timestamp | Phase | From | To | Plan Hash
    - D-25  # Stage enum: init | gsd-plan | superpowers | review | hookify
    - D-26  # ISO 8601 UTC timestamp, 초기 파일은 헤더만 (init row 사전 작성 안 함)
    - D-02  # version 0.0.1 -> 0.0.2 patch bump, CHANGELOG [0.0.2] entry
  truths:
    - "사용자가 `.planning/HANDOFF.md` 파일을 열면 5열 markdown table (Timestamp | Phase | From | To | Plan Hash) 헤더와 구분 행만 보이고 데이터 행은 비어 있다"
    - "사용자가 `/plugin list`를 실행했을 때 super-gsd 버전이 `0.0.2`로 표시된다 (Wave 2 명령들이 설치된 이후)"
    - "사용자가 CHANGELOG.md를 열면 `[0.0.2]` 헤더와 이번 phase 변경 사항이 명시되어 있다"
  artifacts:
    - path: ".planning/HANDOFF.md"
      provides: "Phase 2 핸드오프 상태 추적 테이블 (append-only)"
      contains: "| Timestamp | Phase | From | To | Plan Hash |"
    - path: ".claude-plugin/plugin.json"
      provides: "플러그인 매니페스트, patch-bumped version"
      contains: "\"version\": \"0.0.2\""
    - path: "CHANGELOG.md"
      provides: "버전별 변경 로그"
      contains: "## [0.0.2]"
  key_links:
    - from: "commands/to-superpowers.md"
      to: ".planning/HANDOFF.md"
      via: "Wave 2 명령들이 이 plan에서 록인한 5열 스키마에 맞춰 append"
      pattern: "Timestamp.*Phase.*From.*To.*Plan Hash"
    - from: "commands/status.md"
      to: ".planning/HANDOFF.md"
      via: "Wave 2 명령이 마지막 데이터 행의 To 컬럼을 읽어 stage 판정"
      pattern: "tail.*\\|.*\\|"
---

<objective>
Phase 2의 두 슬래시 명령이 의존할 상태 파일과 플러그인 정체성 변경을 먼저 록인한다. 구체적으로:
1. `.planning/HANDOFF.md`를 5열 markdown table 스키마로 초기 생성한다 (헤더 + 구분 행만, 데이터 행 없음).
2. `.claude-plugin/plugin.json`의 `version` 필드를 `0.0.1` → `0.0.2`로 patch bump한다.
3. `CHANGELOG.md`에 `[0.0.2]` entry를 추가하여 Phase 2 변경 사항을 기록한다.

목적: Wave 2에서 작성될 `commands/to-superpowers.md`와 `commands/status.md`가 동일한 mental model로 같은 스키마를 읽고 쓸 수 있도록 단일 진실 원본을 먼저 확립한다. 멱등성 키 `(Phase, To)` (D-24)와 stage 판정 알고리즘 (D-27)이 모두 이 스키마에 의존하므로 schema-first 순서가 필수이다.
산출물: 디스크에 존재하는 빈 HANDOFF.md, 패치된 plugin.json, 업데이트된 CHANGELOG.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-manual-handoff-status/02-CONTEXT.md

<interfaces>
<!-- 이 plan이 만드는 스키마/식별자. Wave 2 명령들은 codebase 탐색 없이 이 인터페이스에 직접 의존한다. -->

HANDOFF.md 헤더 행 (정확한 문자열):
```
| Timestamp | Phase | From | To | Plan Hash |
| --------- | ----- | ---- | -- | --------- |
```

Stage enum (From/To 컬럼 허용 값, D-25):
- `init`
- `gsd-plan`
- `superpowers`
- `review`
- `hookify`

Timestamp 형식 (D-26): ISO 8601 UTC, 예: `2026-05-15T11:23:45Z`

Plan Hash 형식 (D-23): 해당 phase 디렉토리의 모든 `*-PLAN.md` 파일 본문을 cat 으로 합친 sha256 의 앞 7자리. 빈 phase 디렉토리면 sentinel `nodata`. (Wave 2 명령들이 계산함; 이 plan은 형식만 명시.)

plugin.json 정체성 변경 (D-02):
- 이전: `"version": "0.0.1"`
- 이후: `"version": "0.0.2"`
- 다른 필드(name/description/author/repository/license/keywords)는 변경하지 않음.

CHANGELOG.md entry 형식 (Keep a Changelog, 기존 [0.0.1] 패턴과 일치):
```
## [0.0.2] - <YYYY-MM-DD>

### Added

- <항목들>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .planning/HANDOFF.md 초기 스키마 작성</name>
  <files>.planning/HANDOFF.md</files>
  <read_first>
    - .planning/phases/02-manual-handoff-status/02-CONTEXT.md (D-15, D-22, D-23, D-25, D-26 확인)
    - .planning/STATE.md (현재 phase / last_updated timestamp 스타일 참조용, ISO 8601 UTC가 이미 쓰이는지 확인)
  </read_first>
  <action>
    `.planning/HANDOFF.md`를 신규 작성한다. 파일은 정확히 다음 순서의 내용으로 구성된다.

    1. 1행: 파일 정체성을 알리는 짧은 마크다운 HTML 주석. 정확히 다음 문자열을 사용한다 (Claude's Discretion에서 채택, D-22/D-26 출처를 가리키도록):
       `<!-- Append-only handoff log. Schema locked by .planning/phases/02-manual-handoff-status/02-CONTEXT.md (D-22..D-26). -->`

    2. 빈 줄 1개.

    3. 한국어 문서 제목 (CLAUDE.md GSD 문서 작성 지침은 `.planning/` 내부 산출물에 적용; D-30은 `commands/*.md`만 영문 예외이며 이 파일은 `.planning/` 내부이므로 한글 본문):
       `# 핸드오프 로그`

    4. 빈 줄 1개.

    5. 1문단 한글 설명. 이 파일이 (a) `/super-gsd:to-superpowers`가 append하는 append-only 로그이고, (b) `/super-gsd:status`가 마지막 행 To 컬럼을 읽어 stage를 판정하며, (c) 사람이 직접 수정하지 말아야 한다는 사실을 명시한다. 정확한 어휘 결정은 executor 재량.

    6. 빈 줄 1개.

    7. 한글 섹션 제목: `## 스키마`

    8. 빈 줄 1개.

    9. 스키마 설명 짧은 목록 (5열 의미 + Stage enum 5종 나열). 정확한 어휘는 executor 재량이되 다음 식별자(영문)는 그대로 노출:
       - 열 이름 5개: `Timestamp`, `Phase`, `From`, `To`, `Plan Hash`
       - Stage enum 5개: `init`, `gsd-plan`, `superpowers`, `review`, `hookify`
       - Timestamp 형식 식별 문자열: `ISO 8601 UTC` 와 예시 `2026-05-15T11:23:45Z`

    10. 빈 줄 1개.

    11. 한글 섹션 제목: `## 로그`

    12. 빈 줄 1개.

    13. **핵심 — 정확한 문자열로 작성해야 하는 5열 마크다운 테이블 헤더와 구분 행** (이 두 행 뒤에 어떤 데이터 행도 추가하지 않는다):
        ```
        | Timestamp | Phase | From | To | Plan Hash |
        | --------- | ----- | ---- | -- | --------- |
        ```

    14. 파일 끝에 trailing newline 1개.

    데이터 행을 사전 작성하지 않는다 (D-26: "초기 HANDOFF.md = 헤더 + 빈 table, init row 없음"). 첫 번째 데이터 행은 Wave 2 `/super-gsd:to-superpowers` 첫 실행 시 append된다.
  </action>
  <verify>
    <automated>
      test -f .planning/HANDOFF.md && \
      grep -Fxq "| Timestamp | Phase | From | To | Plan Hash |" .planning/HANDOFF.md && \
      grep -Fxq "| --------- | ----- | ---- | -- | --------- |" .planning/HANDOFF.md && \
      grep -Fq "Append-only handoff log" .planning/HANDOFF.md && \
      grep -Fq "init" .planning/HANDOFF.md && grep -Fq "gsd-plan" .planning/HANDOFF.md && \
      grep -Fq "superpowers" .planning/HANDOFF.md && grep -Fq "review" .planning/HANDOFF.md && \
      grep -Fq "hookify" .planning/HANDOFF.md && \
      grep -Fq "ISO 8601 UTC" .planning/HANDOFF.md && \
      [ "$(grep -cE '^\| [0-9]{4}-[0-9]{2}-[0-9]{2}T' .planning/HANDOFF.md)" = "0" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/HANDOFF.md` 파일이 존재한다.
    - 헤더 행이 정확히 `| Timestamp | Phase | From | To | Plan Hash |` 이다 (공백·세퍼레이터 일치).
    - 구분 행이 정확히 `| --------- | ----- | ---- | -- | --------- |` 이다.
    - 5종 Stage enum 문자열(`init`, `gsd-plan`, `superpowers`, `review`, `hookify`) 모두 본문에 노출된다.
    - `ISO 8601 UTC` 문자열이 본문에 노출된다.
    - 데이터 행(ISO timestamp 로 시작하는 `| YYYY-MM-DDTHH:MM:SSZ |` 패턴) 개수 = 0.
    - HTML 주석 `Append-only handoff log` 가 파일 어딘가에 포함된다.
  </acceptance_criteria>
  <done>
    Wave 2 명령들이 (a) 헤더 행 정확 매칭으로 스키마 검증을 통과하고, (b) 데이터 행 0개 상태에서 stage = `init` 으로 정상 판정할 수 있다. 사람이 파일을 열어 스키마와 사용 목적을 한 번에 이해할 수 있다.
  </done>
</task>

<task type="auto">
  <name>Task 2: plugin.json patch bump + CHANGELOG [0.0.2] entry</name>
  <files>.claude-plugin/plugin.json, CHANGELOG.md</files>
  <read_first>
    - .claude-plugin/plugin.json (현재 version 0.0.1 + 다른 필드 정확값 확인 — 다른 필드는 손대지 않음)
    - CHANGELOG.md (기존 [0.0.1] entry 포맷 그대로 모방)
    - .planning/phases/02-manual-handoff-status/02-CONTEXT.md (D-02 carry-forward 확인)
  </read_first>
  <action>
    두 파일을 다음과 같이 정확히 수정한다.

    (1) `.claude-plugin/plugin.json`:
    - 오직 `"version"` 키의 값만 `"0.0.1"` → `"0.0.2"` 로 교체한다.
    - 다른 필드(`name`, `description`, `author`, `repository`, `license`, `keywords`)는 한 글자도 변경하지 않는다.
    - JSON 형식(들여쓰기, trailing newline) 보존.

    (2) `CHANGELOG.md`:
    - 기존 `## [0.0.1] - 2026-05-15` 블록 **위에** 새 `## [0.0.2] - <YYYY-MM-DD>` 블록을 추가한다. `<YYYY-MM-DD>` 는 실행 시점 UTC 날짜 (`date -u +%F`).
    - 새 블록은 Keep a Changelog 컨벤션에 따라 `### Added` 서브섹션을 가진다.
    - `### Added` 아래 정확히 다음 3개 불릿 항목을 작성한다 (영문, 기존 [0.0.1] entry 톤과 일관, OSS surface):
      - `Manual handoff command: /super-gsd:to-superpowers (packages a GSD phase into a Superpowers-ready prompt and auto-invokes superpowers:executing-plans)`
      - `Workflow status command: /super-gsd:status (shows current stage, last handoff timestamp, and next recommended command)`
      - `Append-only handoff log scaffold: .planning/HANDOFF.md (5-column markdown table — Timestamp | Phase | From | To | Plan Hash)`
    - 기존 [0.0.1] 섹션 본문은 변경하지 않는다.
    - 파일 헤더(`# Changelog` 와 그 아래 1줄 설명)는 변경하지 않는다.
  </action>
  <verify>
    <automated>
      test "$(jq -r '.version' .claude-plugin/plugin.json)" = "0.0.2" && \
      test "$(jq -r '.name' .claude-plugin/plugin.json)" = "super-gsd" && \
      test "$(jq -r '.author' .claude-plugin/plugin.json)" = "gyuha" && \
      grep -Eq '^## \[0\.0\.2\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md && \
      grep -q '^## \[0\.0\.1\] - 2026-05-15$' CHANGELOG.md && \
      grep -Fq '/super-gsd:to-superpowers' CHANGELOG.md && \
      grep -Fq '/super-gsd:status' CHANGELOG.md && \
      grep -Fq '.planning/HANDOFF.md' CHANGELOG.md && \
      awk '/^## \[0\.0\.2\]/{p=NR} /^## \[0\.0\.1\]/{q=NR} END{exit !(p && q && p < q)}' CHANGELOG.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `jq -r '.version' .claude-plugin/plugin.json` = `0.0.2`
    - plugin.json 다른 필드(name=super-gsd, author=gyuha, license=MIT 등) 변경 없음.
    - `CHANGELOG.md` 에 `## [0.0.2] - <date>` 헤더 추가 (date는 ISO 형식 YYYY-MM-DD).
    - [0.0.2] 블록이 [0.0.1] 블록보다 파일 내 위쪽에 위치 (Keep a Changelog 컨벤션).
    - `### Added` 아래 3개 항목 모두 등장 (`/super-gsd:to-superpowers`, `/super-gsd:status`, `.planning/HANDOFF.md`).
    - 기존 [0.0.1] 섹션 본문 보존.
  </acceptance_criteria>
  <done>
    `/plugin list` 가 `super-gsd 0.0.2` 를 표시할 준비가 되었고, 사용자는 CHANGELOG.md 의 [0.0.2] 항목으로 이번 phase 가 무엇을 추가했는지 단번에 파악할 수 있다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/HANDOFF.md` ↔ Wave 2 명령 | 이 plan 이 정의한 스키마를 Wave 2 명령이 신뢰. 헤더 행 오타 시 명령이 schema-validation 실패. |
| plugin.json ↔ 사용자 환경 | `/plugin list` 와 marketplace 가 이 매니페스트를 신뢰. 다른 필드 손상 시 설치/검증 흐름이 깨짐. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `.planning/HANDOFF.md` (Wave 2 명령들이 직접 append) | mitigate | 헤더 행을 grep -Fxq 로 정확 매칭 검증 (acceptance_criteria); Wave 2 명령에는 schema-mismatch 시 abort 로직을 요구 (Plan 02 측에서 다룸) |
| T-02-02 | Tampering | `.claude-plugin/plugin.json` 다른 필드 우발적 변경 | mitigate | jq 로 `version` 외에도 `name`/`author` 검증; Edit tool 사용으로 부분 변경 — 전체 재작성 금지 |
| T-02-03 | Information Disclosure | CHANGELOG entry 가 향후 명령 동작을 사용자 surface 에 노출 (영문) | accept | OSS public 정보이며 비밀 없음 (D-30 OSS surface 정책에 따라 영문 의도적) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 이 plan 은 패키지 설치를 수반하지 않음 (마크다운/JSON 편집만). Legitimacy gate 비해당. |
</threat_model>

<verification>
## Phase-level Checks (이 plan 단독)

1. `.planning/HANDOFF.md` 헤더·구분 행이 정확히 5열 스키마와 일치한다.
2. plugin.json `version` 만 변경되고 다른 필드는 그대로다.
3. CHANGELOG.md 의 [0.0.2] entry 가 [0.0.1] 위쪽에 있고 3개 변경 사항을 정확히 나열한다.

## Wave 2 와의 호환성 사전 검증

- Wave 2 의 `/super-gsd:to-superpowers` 명령이 헤더 행을 식별하기 위해 사용할 grep 패턴(예: `grep -Fxq "| Timestamp | Phase | From | To | Plan Hash |"`)이 이 plan 산출 파일에 대해 즉시 통과해야 한다.
- Wave 2 의 `/super-gsd:status` 가 데이터 행 0개 상태에서 stage = `init` 으로 판정해야 한다 — 이 plan 이 의도적으로 init row 를 작성하지 않은 이유이다.
</verification>

<success_criteria>
- Wave 2 명령들이 별도 mental model 협상 없이 이 plan 의 스키마를 그대로 사용할 수 있다.
- `/plugin list` 후속 호출 시 super-gsd 가 `0.0.2` 로 표시된다 (Wave 2 가 합쳐진 후).
- 사람 사용자가 `.planning/HANDOFF.md` 를 열면 (a) 무엇인지, (b) 어떤 스키마인지, (c) 누가 쓰는지 한 화면에 보인다.
</success_criteria>

<output>
완료 시 `.planning/phases/02-manual-handoff-status/02-01-SUMMARY.md` 를 생성한다 (execute-plan 컨벤션).
</output>
