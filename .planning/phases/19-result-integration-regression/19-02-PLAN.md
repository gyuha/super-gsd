---
phase: 19-result-integration-regression
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified: []
autonomous: true
requirements:
  - TE-04b
  - TE-05a
  - TE-05b
---

<objective>
wave 필드 없는 픽스처 PLAN.md를 사용해 sg-execute Step 8.5 로직을 추적 실행하여 세 가지를 검증한다:
1. `[TE-05a]` 로그 태그가 출력된다
2. `PARALLEL_GROUPS`가 비어 있다 (순차 경로 유지)
3. `HANDOFF_TO="superpowers"`로 결정된다

Purpose: TE-04b, TE-05a, TE-05b — wave 없는 기존 경로가 완전히 보존됨을 확인.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 픽스처 생성 + Step 8.5 로직 추적 검증</name>
  <files>(임시 파일만 — 삭제 후 흔적 없음)</files>

  <read_first>
    - commands/sg-execute.md — Step 8.5 블록 전체 (wave 없음 분기 확인용)
  </read_first>

  <action>
  다음 3단계를 순서대로 실행한다.

  **Step A: 픽스처 PLAN.md 생성**

  wave 필드가 없는 최소 PLAN.md를 생성한다. 이 파일은 테스트 목적으로만 사용되며 이후 삭제된다:

  ```bash
  FIXTURE_FILE=".planning/phases/19-result-integration-regression/test-fixture-PLAN.md"
  cat > "$FIXTURE_FILE" << 'EOF'
  ---
  phase: 19-result-integration-regression
  plan: 99
  type: execute
  autonomous: true
  ---

  <objective>
  픽스처 — wave 필드 없음. Step 8.5 smoke test용.
  </objective>
  EOF
  echo "픽스처 생성: $FIXTURE_FILE"
  ```

  **Step B: Step 8.5 로직 추적 실행**

  sg-execute Step 8.5와 동일한 로직을 픽스처 디렉토리에 적용하여 출력을 캡처한다:

  ```bash
  FIXTURE_DIR=".planning/phases/19-result-integration-regression"
  PARALLEL_GROUPS=""
  GROUP_COUNT=0

  # sg-execute Step 8.5: wave 필드 존재 여부 확인
  HAS_WAVE=$(grep -rl '^wave:' "$FIXTURE_DIR"/test-fixture-PLAN.md 2>/dev/null | head -1)

  if [ -z "$HAS_WAVE" ]; then
    LOG_OUTPUT=$(echo "[TE-05a] wave 필드 없음 — 기존 순차 실행 경로 유지. v1.3 이전 동작 보존.")
    echo "$LOG_OUTPUT"
  fi

  # HANDOFF_TO 분기 (Step 8.5 이후 삽입 코드)
  if [ -n "$PARALLEL_GROUPS" ]; then
    HANDOFF_TO="parallel"
  else
    HANDOFF_TO="superpowers"
  fi
  echo "PARALLEL_GROUPS: '${PARALLEL_GROUPS}'"
  echo "HANDOFF_TO: '${HANDOFF_TO}'"
  ```

  **Step C: 검증**

  출력 결과를 확인한다:

  ```bash
  # 검증 1: [TE-05a] 태그 출력
  echo "$LOG_OUTPUT" | grep -q '\[TE-05a\]' && echo "PASS: [TE-05a] 태그 출력됨" || echo "FAIL: [TE-05a] 태그 없음"

  # 검증 2: PARALLEL_GROUPS 비어 있음
  [ -z "$PARALLEL_GROUPS" ] && echo "PASS: PARALLEL_GROUPS 비어 있음 (순차 경로)" || echo "FAIL: PARALLEL_GROUPS 비어 있지 않음"

  # 검증 3: HANDOFF_TO = superpowers
  [ "$HANDOFF_TO" = "superpowers" ] && echo "PASS: HANDOFF_TO=superpowers (순차 경로)" || echo "FAIL: HANDOFF_TO=$HANDOFF_TO"

  # 픽스처 삭제
  rm -f "$FIXTURE_FILE"
  echo "픽스처 삭제 완료"
  ```
  </action>

  <verify>
  <automated>
    # 픽스처가 삭제됐는지 확인
    [ ! -f ".planning/phases/19-result-integration-regression/test-fixture-PLAN.md" ] && echo "PASS: fixture cleaned up" || echo "FAIL: fixture not deleted"

    # sg-execute.md에 [TE-05a] 태그가 있는지 재확인 (Task 1 의존)
    grep -c '\[TE-05a\]' commands/sg-execute.md && echo "PASS: [TE-05a] in sg-execute.md" || echo "FAIL: [TE-05a] missing from sg-execute.md"
  </automated>
  </verify>

  <acceptance_criteria>
  - Step B 실행 시 "[TE-05a] wave 필드 없음" 로그가 출력된다
  - Step B 실행 후 PARALLEL_GROUPS가 비어 있다
  - Step B 실행 후 HANDOFF_TO가 "superpowers"다
  - 픽스처 파일이 Step C 이후 삭제된다
  </acceptance_criteria>

  <done>3가지 검증이 모두 PASS이고 픽스처 파일이 삭제되어 있다.</done>
</task>

</tasks>

<success_criteria>
1. wave 없는 픽스처로 Step 8.5 로직을 추적 실행했을 때 [TE-05a] 로그가 출력된다.
2. PARALLEL_GROUPS가 비어 있고 HANDOFF_TO가 "superpowers"다.
3. 픽스처 파일이 삭제된다.
</success_criteria>
