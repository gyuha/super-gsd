---
phase: 44-documentation-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.ko.md
autonomous: true
requirements: [DOC-01]
tags: [documentation, readme, commands-table, sg-learn, sg-retro]

must_haves:
  truths:
    - "README.md Commands 테이블의 sg-learn 행(line 37)에 smart default + three lenses + ssc + dspm + --pick 토큰이 모두 포함된다"
    - "README.md Commands 테이블의 sg-retro 행(line 50)에서 stale 토큰(`6 lenses`, `Sailboat`, `Five Whys`, `and more`)이 제거되고 새 동작(`three lenses` + `ssc, dspm, analyze` + smart default + `--pick`) 토큰으로 교체된다"
    - "README.ko.md Commands 테이블의 sg-learn 행과 sg-retro 행이 영문 mirror와 의미적으로 동일한 한글 description으로 갱신된다 (머신 토큰 `--pick`/`ssc`/`dspm`/`analyze`는 영문 그대로)"
    - "두 README의 Commands 테이블 헤더 행(`| Command | What it does | When to use |` / `| 명령어 | 하는 일 | 사용 시점 |`)과 sg-learn·sg-retro 외 19개 행은 byte-identical 보존"
    - "README.md/README.ko.md의 sg-learn 행을 제외한 모든 sg-learn 본문 언급(workflow diagram, end-to-end 예시, Roadmap Phase 13 항목 등)은 byte-identical 보존 (surgical change)"
  artifacts:
    - path: "README.md"
      provides: "Commands 테이블 sg-learn 행 + sg-retro 행 갱신 (영문 source)"
      contains: "smart default"
    - path: "README.ko.md"
      provides: "Commands 테이블 sg-learn 행 + sg-retro 행 갱신 (한글 mirror)"
      contains: "스마트 기본값"
  key_links:
    - from: "README.md line 37 (sg-learn 행)"
      to: "README.ko.md line 37 (sg-learn 행)"
      via: "1:1 row-index mirror lock-step (Phase 41 D-05 carry-forward)"
      pattern: "smart default ↔ 스마트 기본값"
    - from: "README.md line 50 (sg-retro 행)"
      to: "README.ko.md line 50 (sg-retro 행)"
      via: "1:1 row-index mirror lock-step"
      pattern: "three lenses ↔ 세 가지 렌즈"
---

<objective>
`README.md`와 `README.ko.md`의 Commands 테이블에서 **sg-learn 행과 sg-retro 행 두 줄만** surgical edit하여 Phase 42 (smart default + 3 lens consolidation) + Phase 43 (`--pick` + DISPLAY-01/02) 변경을 사용자가 발견·이해할 수 있도록 광고한다.

변경 내용 (CONTEXT D-06, D-07 token spec 적용):

1. **sg-learn 행 description** — 현재 `Run a retrospective via \`sg-retro\` to extract patterns and generate hooks` → 새 동작(smart default + three lenses + --pick) 반영.
2. **sg-retro 행 description (D-02 — milestone goal 정합 필수)** — 현재 `Run a standalone retrospective with 6 lenses (Sailboat, Five Whys, and more) and save findings to \`.planning/lessons/\`` → stale 토큰 4개(`6 lenses`/`Sailboat`/`Five Whys`/`and more`) 제거 + 새 토큰(`three lenses` + 3 lens 이름 + smart default + `--pick`) 추가.
3. **`README.ko.md` 동일 두 행** — 영문 source와 1:1 의미적 mirror (한국어 산문, 머신 토큰 영문 보존).

Out of scope (본 plan):
- Commands 테이블의 다른 19개 행 — byte-identical 보존
- README의 sg-learn/sg-retro 본문 언급 (workflow diagram, end-to-end 예시, Roadmap, Multi-Platform 섹션 등) — byte-identical 보존
- `skills/sg-retro/SKILL.md` / `.agents/skills/sg-retro/SKILL.md` frontmatter — 44-02-PLAN 담당
- `.planning/TEAM.md` 신규 섹션 — 44-03-PLAN 담당

Output: 두 파일 모두 line 37과 line 50의 description 셀만 변경된 1:1 mirror — 다른 모든 줄은 byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-documentation-sync/44-CONTEXT.md
@.planning/phases/43-pick-display-polish/43-CONTEXT.md
@.planning/phases/42-smart-default-lens/42-CONTEXT.md
@.planning/lessons/42-2026-05-30.md
@.planning/lessons/43-2026-05-30.md
@CLAUDE.md
@README.md
@README.ko.md
</context>

<interfaces>
<!-- Phase 44 plan 44-01에서 변경되는 식별자와 정확한 토큰. 실행자는 추가 탐색 없이 이 값들을 그대로 사용한다. -->
<!-- Phase 43 retro P1 #1 적용: 변경되는 산문 토큰 + 변경되는 구조를 묘사하는 보존 블록 두 카테고리를 모두 명시한다. -->

**대상 파일 (write):** `README.md`, `README.ko.md`. **각 파일 정확히 2줄(line 37, line 50)의 description 셀만 변경.** 헤더 행·다른 19개 행·다른 모든 섹션은 byte-identical.

**A. README.md line 37 — sg-learn 행 새 description (영문 source):**

```
| `/super-gsd:sg-learn` | Run a structured retrospective via `sg-retro` — smart default runs three lenses (ssc, dspm) without prompting; pass `--pick` to choose lenses interactively | After the review is done |
```

필수 포함 토큰 (D-06):
- `smart default` (lowercase, 정확히 1회)
- `three lenses` (lowercase, hyphen 없음, 정확히 1회)
- `(ssc, dspm)` (괄호 내부 정확한 lens 이름 — smart default가 적용하는 2개 lens)
- `\`--pick\`` (백틱 감싼 머신 토큰, 정확히 1회)
- `\`sg-retro\`` (백틱 감싼 머신 토큰 보존 — 기존에도 존재)

제거 대상 토큰 (현재 line 37 본문에서 사라져야 하는 토큰):
- `extract patterns and generate hooks` — Hookify-era 잔존 문구. 현재 동작은 hook 생성이 아니라 lessons append.

**B. README.md line 50 — sg-retro 행 새 description (영문 source — D-07):**

```
| `/super-gsd:sg-retro` | Run a standalone retrospective with three lenses (ssc, dspm, analyze) — smart default applies dspm+ssc when no lens argument is given; pass `--pick` for interactive selection. Results saved to `.planning/lessons/` | After any work session to capture lessons; also invoked automatically by `sg-learn` |
```

필수 포함 토큰 (D-07):
- `three lenses` (lowercase, 정확히 1회)
- `(ssc, dspm, analyze)` (괄호 내부 3개 lens 이름 — 정확히 1회)
- `smart default` (lowercase, 정확히 1회)
- `dspm+ssc` (lowercase, plus sign, 공백 없음, 정확히 1회)
- `\`--pick\`` (백틱 감싼 머신 토큰, 정확히 1회)
- `\`.planning/lessons/\`` (백틱 감싼 경로 — 기존에도 존재, 보존)

제거 대상 토큰 (현재 line 50 본문에서 완전히 사라져야 하는 stale 토큰):
- `6 lenses` (Phase 42에서 3 lens로 통합됨)
- `Sailboat` (Phase 42 D-04 dropped)
- `Five Whys` (Phase 42 D-04 dropped)
- `and more` (모호한 placeholder — 새 description은 정확한 3 lens 이름 명시)

**C. README.ko.md line 37 — sg-learn 행 새 description (한글 mirror):**

```
| `/super-gsd:sg-learn` | `sg-retro`를 통한 구조화된 회고 실행 — 스마트 기본값으로 세 가지 렌즈 중 두 개(ssc, dspm)를 질문 없이 실행; lens를 직접 고르려면 `--pick` 사용 | 리뷰 완료 후 |
```

필수 포함 토큰 (D-06 한글 mirror):
- `스마트 기본값` (한국어 산문 — D-06 권장)
- `세 가지 렌즈` (한국어 — `three lenses`의 자연스러운 번역)
- `(ssc, dspm)` (괄호 내부 lens 이름 — 영문 그대로, 머신 토큰)
- `\`--pick\`` (백틱 감싼 머신 토큰 — 영문 그대로)
- `\`sg-retro\`` (백틱 감싼 머신 토큰 — 영문 그대로, 기존 보존)

**D. README.ko.md line 50 — sg-retro 행 새 description (한글 mirror — D-07 mirror):**

```
| `/super-gsd:sg-retro` | 세 가지 렌즈(ssc, dspm, analyze)로 독립 회고 실행 — lens 인자를 생략하면 스마트 기본값으로 dspm+ssc 자동 적용; lens를 직접 고르려면 `--pick` 사용. 결과는 `.planning/lessons/`에 저장 | 작업 세션 후 교훈을 캡처할 때; `sg-learn`이 자동으로 호출하기도 함 |
```

필수 포함 토큰 (D-07 한글 mirror):
- `세 가지 렌즈` (한국어)
- `(ssc, dspm, analyze)` (괄호 내부 3개 lens 이름 — 영문 그대로)
- `스마트 기본값` (한국어)
- `dspm+ssc` (영문, plus sign, 공백 없음)
- `\`--pick\`` (영문 머신 토큰)
- `\`.planning/lessons/\`` (영문 경로 — 기존 보존)

제거 대상 토큰 (한글):
- `6가지 렌즈(Sailboat, Five Whys 등)` — Phase 42 통합 이후 완전 제거. 한글 stale 표현.

---

**변경되는 산문 토큰 (Phase 43 retro P1 #1 카테고리 1 — 명시적 enumerate):**

- README.md line 37 description 셀 본문 전체 (헤더 셀과 third 셀 `After the review is done`는 보존)
- README.md line 50 description 셀 본문 전체 (헤더 셀과 third 셀 `After any work session to capture lessons; also invoked automatically by \`sg-learn\``는 보존)
- README.ko.md line 37 description 셀 본문 전체 (헤더 셀과 third 셀 `리뷰 완료 후`는 보존)
- README.ko.md line 50 description 셀 본문 전체 (헤더 셀과 third 셀 `작업 세션 후 교훈을 캡처할 때; \`sg-learn\`이 자동으로 호출하기도 함`는 보존)

**변경되는 구조를 묘사하는 보존 블록 (Phase 43 retro P1 #1 카테고리 2 — 신규 sub-class, 본 plan 작성 시 명시):**

- Commands 테이블 헤더 행 (`| Command | What it does | When to use |` / `|---------|-------------|-------------|` 구분자 행) — 본 plan의 grep acceptance가 description 셀을 검사하지만 이 헤더 행은 byte-identical 보존되어야 함. 두 카테고리 충돌 가능성: 헤더 행에 `Run`/`Use`/`Save` 같은 단어가 있으면 grep count가 부정확해질 수 있음 → acceptance는 line-numbered grep 또는 sg-learn·sg-retro 행 limit으로 한정.
- Commands 테이블의 다른 19개 행 — 행 형식(`| /super-gsd:sg-xxx | ... | ... |`)은 변경되는 두 행과 동일 구조이므로 grep으로 임의 단어를 셀 때 한 번에 카운트되지 않도록 토큰을 row-내부에서 unique하게 선택해야 함 (예: `smart default`는 다른 19개 행에 존재하지 않음 — safe; `Run`은 여러 행에 존재 — unsafe).
- README.md ↔ README.ko.md mirror lock-step 구조 — 두 파일의 line 37/50이 같은 의미적 행을 담고 있어야 함. byte-identical은 아님(언어가 다름)이지만 row-index mirror는 보존되어야 함.
- 본 plan 외 모든 영역 — `# super-gsd` 헤더 ~ 라이선스 footer 사이의 다른 모든 산문/코드 블록/표는 byte-identical.

**보존되는 산문 토큰 (절대 변경 금지):**

- README.md/README.ko.md의 line 1-28 (헤더, intro, workflow diagram, Commands 섹션 헤더 + 테이블 헤더 행)
- README.md/README.ko.md의 line 29-36 (sg-start ~ sg-ui-plan 6개 행) — byte-identical
- README.md/README.ko.md의 line 38-49 (sg-execute ~ sg-parallel-execute 12개 행) + line 51 (sg-setup 또는 sg-phase) — byte-identical (정확한 row 수는 grep 시점 확인)
- README.md/README.ko.md의 line 52~ (나머지 모든 섹션: Phase management, Usage Examples, Team Workflow, Installation, Multi-Platform Support, Prerequisites, Verify install, Roadmap, License) — byte-identical
- README.md의 line 17의 workflow diagram에 등장하는 `sg-learn` 토큰은 변경 없음 (본 plan은 description 셀만 대상)
- README.md의 line 37·50 셀에서도 column 1(`| \`/super-gsd:sg-learn\` |` / `| \`/super-gsd:sg-retro\` |`)과 column 3(when-to-use 셀)은 변경 없음 — description 셀(column 2)만 변경

**보존 블록과 변경 acceptance의 충돌 회피 (Phase 42 mistake #1 + Phase 43 P1 #1 직접 적용):**

- "보존되는 산문 토큰"에 명시된 영역은 acceptance에서 grep으로 검사하지 않거나, 검사 시 line-range 또는 unique-row 토큰을 사용한다.
- `--pick` 토큰은 본 plan 변경 후 README.md에 정확히 2회 등장한다(line 37 + line 50). README.md 다른 영역에는 `--pick` 토큰 부재 — `grep -c '\-\-pick' README.md` returns `2` 검증 가능.
- `smart default` 토큰도 본 plan 변경 후 README.md에 정확히 2회(line 37 + line 50). 다른 영역에는 부재.
- `three lenses` 토큰도 정확히 2회. 단, README.md line 300 Phase 9 Roadmap 항목(`introduces the built-in \`sg-retro\` skill with 3 retrospection lenses`)은 `3 retrospection lenses`로 다른 표현 — grep 충돌 없음.
- 한글: `스마트 기본값`은 README.ko.md에 본 plan 후 정확히 2회. `세 가지 렌즈`도 정확히 2회. 단, line 300 Phase 9 한글 항목(`3가지 회고 렌즈를 지원하는 내장 \`sg-retro\` 스킬`)은 `3가지 회고 렌즈`로 다른 표현 — 충돌 없음.
- 제거 대상 토큰(`6 lenses`, `Sailboat`, `Five Whys`, `and more`): 본 plan 변경 후 README.md에서 `6 lenses` 0회, `Sailboat` 0회, `Five Whys` 0회 검증. (`and more`는 너무 일반적 — line 50에 한정된 검증 권장.) README.ko.md에서 `6가지 렌즈(Sailboat` 0회. (`Sailboat`/`Five Whys`는 한글 README에도 line 50에만 등장 → 제거 후 0회.)
- Roadmap Phase 10 항목(`Sailboat·Five Whys 포함 총 6개 렌즈로 확장`) — **이 항목은 본 plan scope 외**. Roadmap은 과거 사실의 시간순 기록이므로 stale 표현이 아니라 historical accuracy. 본 plan은 Commands 테이블 한정.
- 단, 일관성을 위해 Roadmap Phase 10 행도 갱신하고 싶을 수 있으나 — **CONTEXT D-01에 따라 본 phase write 대상은 3 파일(README.md/README.ko.md/TEAM.md)에 한정** + Roadmap 갱신은 phase 이력 변경으로 별도 판단 필요. 본 plan은 Commands 테이블 2개 행만 surgical 대상.

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: README.md Commands 테이블 sg-learn 행 + sg-retro 행 description 갱신</name>
  <files>README.md</files>
  <read_first>
    - README.md (line 25-53 — Commands 테이블 전체)
    - .planning/phases/44-documentation-sync/44-CONTEXT.md (D-01, D-02, D-06, D-07, D-14, D-15)
    - .planning/phases/42-smart-default-lens/42-CONTEXT.md (smart default = dspm+ssc 사실 확인)
    - .planning/phases/43-pick-display-polish/43-CONTEXT.md (--pick 동작 사실 확인)
    - CLAUDE.md §"사용자 언어 메시지" (머신 토큰 영문 보존 규칙)
  </read_first>
  <action>
**A. README.md line 37 — sg-learn 행 description 셀 교체:**

현재 (line 37 전체):
```
| `/super-gsd:sg-learn` | Run a retrospective via `sg-retro` to extract patterns and generate hooks | After the review is done |
```

변경 후 (line 37 전체):
```
| `/super-gsd:sg-learn` | Run a structured retrospective via `sg-retro` — smart default runs three lenses (ssc, dspm) without prompting; pass `--pick` to choose lenses interactively | After the review is done |
```

변경 내용:
- column 1(`| \`/super-gsd:sg-learn\` |`)과 column 3(`| After the review is done |`)은 변경 없음
- column 2(description)만 교체
- 제거 토큰: `Run a retrospective` (의미는 보존하되 phrasing 갱신), `to extract patterns and generate hooks` (Hookify-era 잔존 문구 — 현재 동작과 불일치)
- 추가 토큰: `Run a structured retrospective`, `smart default`, `three lenses (ssc, dspm)`, `without prompting`, `pass \`--pick\` to choose lenses interactively`

**B. README.md line 50 — sg-retro 행 description 셀 교체 (D-02):**

현재 (line 50 전체):
```
| `/super-gsd:sg-retro` | Run a standalone retrospective with 6 lenses (Sailboat, Five Whys, and more) and save findings to `.planning/lessons/` | After any work session to capture lessons; also invoked automatically by `sg-learn` |
```

변경 후 (line 50 전체):
```
| `/super-gsd:sg-retro` | Run a standalone retrospective with three lenses (ssc, dspm, analyze) — smart default applies dspm+ssc when no lens argument is given; pass `--pick` for interactive selection. Results saved to `.planning/lessons/` | After any work session to capture lessons; also invoked automatically by `sg-learn` |
```

변경 내용:
- column 1과 column 3은 변경 없음
- column 2(description)만 교체
- 제거 토큰: `6 lenses`, `Sailboat`, `Five Whys`, `and more` (모두 Phase 42 D-04로 dropped된 lens 이름) — column 2에서 완전히 사라짐
- 추가 토큰: `three lenses`, `(ssc, dspm, analyze)`, `smart default`, `dspm+ssc`, `pass \`--pick\` for interactive selection`
- 보존 토큰: `Run a standalone retrospective`, `\`.planning/lessons/\``, `save findings to` → `Results saved to` (의미 동등 phrasing 갱신)

**C. 보존 검증:**
- Commands 테이블의 다른 19개 행(line 31-36, line 38-49, line 51)은 손대지 않는다.
- 테이블 헤더 행(line 29-30)은 손대지 않는다.
- README.md의 line 1-28, line 52~ 모든 영역은 byte-identical 보존.
- 특히 workflow diagram(line 17)의 `sg-learn` 토큰, end-to-end 예시(line 115-116)의 `sg-retro 회고 실행`/`/super-gsd:sg-learn` 토큰, Roadmap Phase 9/10/13 항목의 sg-retro/sg-learn 언급은 모두 byte-identical 보존.

**D. macOS 셸 이식성 / 산문 규칙:**
- em-dash `—` (U+2014) 사용 — ASCII `--` 아님. CLAUDE.md 컨벤션 일관.
- 머신 토큰(`sg-retro`, `--pick`, `ssc`, `dspm`, `analyze`, `.planning/lessons/`, `sg-learn`)은 백틱 감싼 형태 유지 — CLAUDE.md "사용자 언어 메시지" 규칙.
- 영문 description은 자연스러운 영어 산문 — 필수 토큰 포함하되 phrasing은 위 spec과 일치.
  </action>
  <verify>
    <automated>
# A) sg-learn 행 새 토큰 존재 (line 37 또는 description 셀에서)
grep -F 'smart default runs three lenses (ssc, dspm) without prompting' README.md && \
grep -F 'pass `--pick` to choose lenses interactively' README.md && \
grep -F 'Run a structured retrospective via `sg-retro`' README.md && \

# B) sg-learn 행 stale 토큰 제거
! grep -F 'to extract patterns and generate hooks' README.md && \

# C) sg-retro 행 새 토큰 존재
grep -F 'three lenses (ssc, dspm, analyze)' README.md && \
grep -F 'smart default applies dspm+ssc when no lens argument is given' README.md && \
grep -F 'pass `--pick` for interactive selection' README.md && \
grep -F 'Results saved to `.planning/lessons/`' README.md && \

# D) sg-retro 행 stale 토큰 제거 (Commands 테이블 한정 — Roadmap Phase 10 line은 별도 표현이라 충돌 없음)
! grep -F '6 lenses (Sailboat, Five Whys, and more)' README.md && \

# E) 보존: sg-learn/sg-retro 행 column 1과 column 3 보존
grep -F '| `/super-gsd:sg-learn` |' README.md && \
grep -F '| After the review is done |' README.md && \
grep -F '| `/super-gsd:sg-retro` |' README.md && \
grep -F 'also invoked automatically by `sg-learn`' README.md && \

# F) 보존: 다른 19개 행 sample (전수는 H에서)
grep -F '| `/super-gsd:sg-start` |' README.md && \
grep -F '| `/super-gsd:sg-execute` |' README.md && \
grep -F '| `/super-gsd:sg-status` |' README.md && \

# G) 보존: 테이블 헤더 행
grep -F '| Command | What it does | When to use |' README.md && \
grep -F '|---------|-------------|-------------|' README.md && \

# H) Commands 테이블 행 총 개수 보존 (1 header + 21 data rows = 22 lines containing `| \`/super-gsd:`)
test "$(grep -c '| `/super-gsd:' README.md)" = "21" && \

# I) 보존: workflow diagram + Roadmap의 sg-learn/sg-retro 언급
grep -F 'sg-new/sg-start → sg-explore → sg-plan → sg-execute → sg-review → sg-learn → sg-ship → sg-complete' README.md && \
grep -F '# 6. Learn — runs sg-retro retrospective; findings are saved to .planning/lessons/' README.md && \
grep -F 'introduces the built-in `sg-retro` skill with 3 retrospection lenses' README.md && \

# J) 토큰 출현 횟수 합리적 (Phase 43 retro P1 #1: 정확한 count보다 합리적 range)
test "$(grep -c 'smart default' README.md)" -ge "2" && \
test "$(grep -c 'three lenses' README.md)" -ge "2" && \
test "$(grep -c -- '--pick' README.md)" -ge "2" && \

echo "Task 1 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -F 'smart default runs three lenses (ssc, dspm) without prompting' README.md` matches (sg-learn 행 새 description)
    - Source: `grep -F 'three lenses (ssc, dspm, analyze)' README.md` matches (sg-retro 행 새 description)
    - Source: `grep -F 'smart default applies dspm+ssc when no lens argument is given' README.md` matches (sg-retro 행 새 description)
    - Source: `grep -F 'pass \`--pick\` for interactive selection' README.md` matches (sg-retro 행 --pick 토큰)
    - Source: `grep -F 'to extract patterns and generate hooks' README.md` returns no match (Hookify-era stale 제거)
    - Source: `grep -F '6 lenses (Sailboat, Five Whys, and more)' README.md` returns no match (Phase 42 dropped lens 명 제거)
    - Source: `grep -c '| \`/super-gsd:' README.md` returns `21` (Commands 테이블 21개 행 보존)
    - Source: `grep -F '| Command | What it does | When to use |' README.md` matches (헤더 행 보존)
    - Source: `grep -F 'introduces the built-in \`sg-retro\` skill with 3 retrospection lenses' README.md` matches (Roadmap Phase 9 historical 항목 보존)
    - Source: `grep -c 'smart default' README.md` returns ≥ 2 (line 37 + line 50)
    - Source: `grep -c 'three lenses' README.md` returns ≥ 2 (line 37 + line 50)
    - Source: `grep -c -- '--pick' README.md` returns ≥ 2 (line 37 + line 50)
    - CLI: `git diff --stat HEAD -- README.md` shows exactly 2 lines changed (sg-learn 행 + sg-retro 행)
  </acceptance_criteria>
  <done>
README.md의 Commands 테이블에서 sg-learn 행(line 37)과 sg-retro 행(line 50)의 description 셀 두 곳만 surgical 교체되었다. sg-learn 행은 `smart default`/`three lenses (ssc, dspm)`/`--pick` 토큰을 포함하고, sg-retro 행은 `three lenses (ssc, dspm, analyze)`/`smart default`/`dspm+ssc`/`--pick` 토큰을 포함한다. Stale 토큰(`extract patterns and generate hooks`, `6 lenses`, `Sailboat`, `Five Whys`, `and more`)은 Commands 테이블에서 완전히 사라졌다. 테이블 헤더, 다른 19개 행, README의 모든 다른 섹션(workflow diagram, end-to-end 예시, Roadmap historical 항목)은 byte-identical 보존되었다.
  </done>
</task>

<task type="auto">
  <name>Task 2: README.ko.md Commands 테이블 sg-learn 행 + sg-retro 행 description 갱신 (mirror)</name>
  <files>README.ko.md</files>
  <read_first>
    - README.ko.md (line 25-53 — Commands 테이블 전체)
    - README.md (Task 1 적용 후 — 영문 source 확인)
    - .planning/phases/44-documentation-sync/44-CONTEXT.md (D-06, D-07 한글 mirror 가이드)
    - CLAUDE.md §"사용자 언어 메시지" (머신 토큰 영문 그대로, 산문은 한국어)
  </read_first>
  <action>
**A. README.ko.md line 37 — sg-learn 행 description 셀 교체 (한글 mirror):**

현재 (line 37 전체):
```
| `/super-gsd:sg-learn` | `sg-retro`를 통한 회고 실행, 패턴 추출 및 훅 생성 | 리뷰 완료 후 |
```

변경 후 (line 37 전체):
```
| `/super-gsd:sg-learn` | `sg-retro`를 통한 구조화된 회고 실행 — 스마트 기본값으로 세 가지 렌즈 중 두 개(ssc, dspm)를 질문 없이 실행; lens를 직접 고르려면 `--pick` 사용 | 리뷰 완료 후 |
```

변경 내용:
- column 1과 column 3(`| 리뷰 완료 후 |`)은 변경 없음
- column 2(description)만 교체
- 제거 토큰: `회고 실행, 패턴 추출 및 훅 생성` (Hookify-era 한글 표현 — 현재 동작과 불일치)
- 추가 토큰: `구조화된 회고 실행`, `스마트 기본값`, `세 가지 렌즈`, `(ssc, dspm)`, `질문 없이 실행`, `\`--pick\` 사용`
- 머신 토큰 보존: `\`sg-retro\``, `\`--pick\``, `ssc`, `dspm`은 모두 영문 그대로

**B. README.ko.md line 50 — sg-retro 행 description 셀 교체 (한글 mirror — D-07):**

현재 (line 50 전체):
```
| `/super-gsd:sg-retro` | 6가지 렌즈(Sailboat, Five Whys 등)로 독립 회고 실행 후 결과를 `.planning/lessons/`에 저장 | 작업 세션 후 교훈을 캡처할 때; `sg-learn`이 자동으로 호출하기도 함 |
```

변경 후 (line 50 전체):
```
| `/super-gsd:sg-retro` | 세 가지 렌즈(ssc, dspm, analyze)로 독립 회고 실행 — lens 인자를 생략하면 스마트 기본값으로 dspm+ssc 자동 적용; lens를 직접 고르려면 `--pick` 사용. 결과는 `.planning/lessons/`에 저장 | 작업 세션 후 교훈을 캡처할 때; `sg-learn`이 자동으로 호출하기도 함 |
```

변경 내용:
- column 1과 column 3은 변경 없음
- column 2(description)만 교체
- 제거 토큰: `6가지 렌즈(Sailboat, Five Whys 등)` (Phase 42 dropped lens 한글 표현 — 완전히 사라짐)
- 추가 토큰: `세 가지 렌즈`, `(ssc, dspm, analyze)`, `스마트 기본값`, `dspm+ssc`, `\`--pick\` 사용`, `결과는`
- 머신 토큰 보존: `\`sg-retro\``, `\`--pick\``, `ssc`, `dspm`, `analyze`, `\`.planning/lessons/\``, `\`sg-learn\``은 모두 영문 그대로

**C. 보존 검증:**
- Commands 테이블의 다른 19개 행(line 31-36, line 38-49, line 51)은 손대지 않는다.
- 테이블 헤더 행(line 29-30: `| 명령어 | 하는 일 | 사용 시점 |` / `|--------|---------|-----------|`)은 손대지 않는다.
- README.ko.md의 line 1-28, line 52~ 모든 영역은 byte-identical 보존.
- 특히 workflow diagram(line 17)의 `sg-learn` 토큰, end-to-end 예시(line 115-116)의 `sg-retro 회고 실행`/`/super-gsd:sg-learn` 토큰, Roadmap 한글 항목들(line 300 `3가지 회고 렌즈`, line 301 `Sailboat·Five Whys 포함 총 6개 렌즈로 확장`)은 모두 byte-identical 보존.

**D. 산문 규칙 (CLAUDE.md):**
- em-dash `—` (U+2014) 사용. ASCII `--` 아님.
- 머신 토큰은 영문 그대로 백틱 감싸기. lens 이름(ssc/dspm/analyze)도 영문 그대로.
- 한국어 산문 자연스러움 우선 — 위 spec과 정확히 일치하는 phrasing 권장.

**E. README.md와 README.ko.md row-index mirror 검증 (D-05 carry-forward):**
- README.md line 37의 description 셀과 README.ko.md line 37의 description 셀이 같은 의미적 내용을 다루는지 확인.
- README.md line 50의 description 셀과 README.ko.md line 50의 description 셀이 같은 의미적 내용을 다루는지 확인.
- 두 파일의 line 37/50이 row-index mirror로 정렬되어 있는지 확인.
  </action>
  <verify>
    <automated>
# A) sg-learn 행 새 한글 토큰 존재
grep -F '`sg-retro`를 통한 구조화된 회고 실행' README.ko.md && \
grep -F '스마트 기본값으로 세 가지 렌즈 중 두 개(ssc, dspm)를 질문 없이 실행' README.ko.md && \
grep -F 'lens를 직접 고르려면 `--pick` 사용' README.ko.md && \

# B) sg-learn 행 stale 한글 토큰 제거
! grep -F '`sg-retro`를 통한 회고 실행, 패턴 추출 및 훅 생성' README.ko.md && \

# C) sg-retro 행 새 한글 토큰 존재
grep -F '세 가지 렌즈(ssc, dspm, analyze)로 독립 회고 실행' README.ko.md && \
grep -F 'lens 인자를 생략하면 스마트 기본값으로 dspm+ssc 자동 적용' README.ko.md && \
grep -F '결과는 `.planning/lessons/`에 저장' README.ko.md && \

# D) sg-retro 행 stale 한글 토큰 제거 (Commands 테이블 한정)
! grep -F '6가지 렌즈(Sailboat, Five Whys 등)로 독립 회고 실행' README.ko.md && \

# E) 보존: column 1 + column 3
grep -F '| `/super-gsd:sg-learn` |' README.ko.md && \
grep -F '| 리뷰 완료 후 |' README.ko.md && \
grep -F '| `/super-gsd:sg-retro` |' README.ko.md && \
grep -F '작업 세션 후 교훈을 캡처할 때; `sg-learn`이 자동으로 호출하기도 함' README.ko.md && \

# F) 보존: 다른 19개 행 sample
grep -F '| `/super-gsd:sg-start` |' README.ko.md && \
grep -F '| `/super-gsd:sg-execute` |' README.ko.md && \
grep -F '| `/super-gsd:sg-status` |' README.ko.md && \

# G) 보존: 한글 테이블 헤더 행
grep -F '| 명령어 | 하는 일 | 사용 시점 |' README.ko.md && \

# H) Commands 테이블 행 총 개수 보존
test "$(grep -c '| `/super-gsd:' README.ko.md)" = "21" && \

# I) 보존: workflow diagram + Roadmap 한글 historical 항목
grep -F 'sg-new/sg-start → sg-explore → sg-plan → sg-execute → sg-review → sg-learn → sg-ship → sg-complete' README.ko.md && \
grep -F '# 6. 학습 — sg-retro 회고 실행; 교훈이 .planning/lessons/에 저장됨' README.ko.md && \
grep -F '3가지 회고 렌즈를 지원하는 내장 `sg-retro` 스킬 도입' README.ko.md && \

# J) 토큰 출현 횟수 합리적 (한글)
test "$(grep -c '스마트 기본값' README.ko.md)" -ge "2" && \
test "$(grep -c '세 가지 렌즈' README.ko.md)" -ge "2" && \
test "$(grep -c -- '--pick' README.ko.md)" -ge "2" && \

# K) Row-index mirror 검증: README.md와 README.ko.md의 line 37과 line 50이 sg-learn/sg-retro 행을 담는지
awk 'NR==37 && /sg-learn/' README.md > /dev/null && \
awk 'NR==37 && /sg-learn/' README.ko.md > /dev/null && \
awk 'NR==50 && /sg-retro/' README.md > /dev/null && \
awk 'NR==50 && /sg-retro/' README.ko.md > /dev/null && \

echo "Task 2 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -F '\`sg-retro\`를 통한 구조화된 회고 실행' README.ko.md` matches (sg-learn 행 새 description)
    - Source: `grep -F '세 가지 렌즈(ssc, dspm, analyze)로 독립 회고 실행' README.ko.md` matches (sg-retro 행 새 description)
    - Source: `grep -F 'lens를 직접 고르려면 \`--pick\` 사용' README.ko.md` matches ≥ 1 (두 행 모두 사용 가능 — 합리적 count)
    - Source: `grep -F '6가지 렌즈(Sailboat, Five Whys 등)' README.ko.md` returns no match
    - Source: `grep -F '회고 실행, 패턴 추출 및 훅 생성' README.ko.md` returns no match
    - Source: `grep -c '| \`/super-gsd:' README.ko.md` returns `21` (Commands 테이블 21개 행 보존)
    - Source: `grep -F '| 명령어 | 하는 일 | 사용 시점 |' README.ko.md` matches (한글 헤더 행 보존)
    - Source: `grep -F '3가지 회고 렌즈를 지원하는 내장 \`sg-retro\` 스킬 도입' README.ko.md` matches (Roadmap Phase 9 한글 historical 보존)
    - Source: `grep -c '스마트 기본값' README.ko.md` returns ≥ 2
    - Source: `grep -c '세 가지 렌즈' README.ko.md` returns ≥ 2
    - Source: `grep -c -- '--pick' README.ko.md` returns ≥ 2
    - CLI: `awk 'NR==37 && /sg-learn/' README.ko.md` returns the sg-learn 행 (row-index mirror 보존)
    - CLI: `awk 'NR==50 && /sg-retro/' README.ko.md` returns the sg-retro 행 (row-index mirror 보존)
    - CLI: `git diff --stat HEAD -- README.ko.md` shows exactly 2 lines changed
  </acceptance_criteria>
  <done>
README.ko.md의 Commands 테이블에서 sg-learn 행(line 37)과 sg-retro 행(line 50)의 한글 description 셀 두 곳만 surgical 교체되었다. 영문 source(README.md)와 의미적으로 1:1 mirror — 머신 토큰(`sg-retro`/`--pick`/`ssc`/`dspm`/`analyze`/`.planning/lessons/`/`sg-learn`)은 영문 그대로 백틱 감싼 형태 유지, 산문은 한국어로 자연스럽게 작성. Stale 한글 토큰(`회고 실행, 패턴 추출 및 훅 생성`, `6가지 렌즈(Sailboat, Five Whys 등)`)은 Commands 테이블에서 완전히 사라졌다. 테이블 헤더, 다른 19개 행, README.ko.md의 모든 다른 섹션(workflow diagram, end-to-end 예시, Roadmap 한글 historical 항목)은 byte-identical 보존되었다. README.md와 README.ko.md의 row-index mirror 정렬이 유지되었다.
  </done>
</task>

<task type="auto">
  <name>Task 3: SC#1 verification + plan-spec deviation 사전 점검</name>
  <files></files>
  <read_first>
    - README.md (Task 1 적용 후)
    - README.ko.md (Task 2 적용 후)
    - .planning/phases/44-documentation-sync/44-CONTEXT.md (SC#1)
    - .planning/lessons/43-2026-05-30.md (Plan-spec Deviations 패턴)
  </read_first>
  <action>
Task 1+2 변경이 SC#1("README.md/README.ko.md Commands 테이블의 sg-learn 행 description이 새 동작을 반영한다")를 충족하는지 정적 검증하고, plan-spec deviation 가능성을 사전 점검한다.

**SC#1 충족 시나리오 (D-02 확장 — sg-retro 행도 포함):**

1. README.md Commands 테이블 sg-learn 행 description에 D-06 필수 토큰 5개(`smart default`, `three lenses`, `ssc`, `dspm`, `--pick`) 모두 포함.
2. README.md Commands 테이블 sg-retro 행 description에 D-07 필수 토큰 6개(`three lenses`, `(ssc, dspm, analyze)`, `smart default`, `dspm+ssc`, `--pick`, `.planning/lessons/`) 모두 포함 + stale 토큰 4개(`6 lenses`, `Sailboat`, `Five Whys`, `and more`) 완전 제거.
3. README.ko.md 동일 두 행이 한글 mirror로 갱신 + 머신 토큰 영문 보존.

**정적 검증 명령 (sg-review 시 재실행 가능):**

```bash
# 1. 새 토큰 존재 (영문 + 한글)
grep -c 'smart default' README.md README.ko.md
# expected: README.md ≥ 2, README.ko.md (한글 표현이라 0 — `스마트 기본값`)
grep -c '스마트 기본값' README.ko.md
# expected: ≥ 2

# 2. Stale 토큰 부재
grep -F '6 lenses (Sailboat' README.md
# expected: no match
grep -F '6가지 렌즈(Sailboat' README.ko.md
# expected: no match
grep -F 'to extract patterns and generate hooks' README.md
# expected: no match
grep -F '회고 실행, 패턴 추출 및 훅 생성' README.ko.md
# expected: no match

# 3. 보존 영역 byte-identical (행 수)
grep -c '| `/super-gsd:' README.md README.ko.md
# expected: 21 (each file)

# 4. Row-index mirror
awk 'NR==37' README.md | grep -q 'sg-learn'
awk 'NR==50' README.md | grep -q 'sg-retro'
awk 'NR==37' README.ko.md | grep -q 'sg-learn'
awk 'NR==50' README.ko.md | grep -q 'sg-retro'
```

**Plan-spec deviation 사전 점검 (Phase 42·43 retro 패턴 답습):**

본 plan의 `<interfaces>` 블록에 명시된 token spec과 acceptance criteria 사이에 충돌 가능성이 있는지 점검한다. 충돌 발견 시 SUMMARY § "Plan-spec Deviations (Surfaced)"에 명시.

**주의 deviation 시나리오:**

- `grep -c 'smart default' README.md` returns `2`로 명시했으나, line 37의 표현이 `smart default runs three lenses`이고 line 50의 표현이 `smart default applies dspm+ssc`이므로 둘 다 `smart default` substring을 포함 — count `≥ 2` 검증 적정. 만약 정확히 `= 2`로 검증하면 향후 다른 행에 `smart default` 추가 시 false fail 발생 → `≥ 2` 권장 (Phase 43 retro 적용).
- `grep -c -- '--pick'` count도 `≥ 2`로 합리적 range. README.md의 다른 영역(예: Codex/Gemini install 섹션 line 233)에는 `--pick` 토큰이 없으므로 정확히 2회 등장 예상.
- `three lenses` 한글 mirror(`세 가지 렌즈`)도 동일 패턴. count `≥ 2`.

**스코프 일관성 점검:**

- 본 plan은 README.md/README.ko.md의 Commands 테이블 2개 행만 대상. 다른 모든 영역은 byte-identical 보존.
- SC#1 본문은 "sg-learn 행"만 명시하나 D-02에 따라 sg-retro 행도 함께 갱신 — milestone goal 정합 충족.
- `skills/sg-retro/SKILL.md` / `.agents/skills/sg-retro/SKILL.md` frontmatter는 본 plan 미대상 (44-02-PLAN 담당).
- `.planning/TEAM.md` 신규 섹션은 본 plan 미대상 (44-03-PLAN 담당).

**Out of scope confirmations:**

```bash
# sg-retro SKILL.md는 본 plan에서 손대지 않음
test "$(git diff --stat HEAD -- skills/sg-retro/SKILL.md 2>/dev/null | wc -l)" = "0"
test "$(git diff --stat HEAD -- .agents/skills/sg-retro/SKILL.md 2>/dev/null | wc -l)" = "0"

# TEAM.md는 본 plan에서 손대지 않음
test "$(git diff --stat HEAD -- .planning/TEAM.md 2>/dev/null | wc -l)" = "0"
```

**최종 SUMMARY 작성 가이드 (D-16 staging 적용):**

- 본 plan 단독 commit 가능. wave:1 + non-overlapping files_modified → 다른 두 plan과 conflict 없음.
- D-10/D-16 pairwise convention은 본 plan에 직접 적용 안 됨 (README.md/README.ko.md는 mirror지만 같은 plan 내 commit이므로 D-16 pairwise SKILL.md commit과 별개).
- 44-02-PLAN(SKILL.md 쌍)과 44-03-PLAN(TEAM.md)이 모두 wave:1이므로 sg-parallel-execute가 3-way dispatch.
  </action>
  <verify>
    <automated>
# 정적 SC#1 검증
grep -F 'smart default runs three lenses (ssc, dspm) without prompting' README.md && \
grep -F 'pass `--pick` to choose lenses interactively' README.md && \
grep -F 'three lenses (ssc, dspm, analyze)' README.md && \
grep -F 'smart default applies dspm+ssc when no lens argument is given' README.md && \
grep -F 'pass `--pick` for interactive selection' README.md && \
grep -F '`sg-retro`를 통한 구조화된 회고 실행' README.ko.md && \
grep -F '세 가지 렌즈(ssc, dspm, analyze)로 독립 회고 실행' README.ko.md && \

# Stale 토큰 부재
! grep -F '6 lenses (Sailboat, Five Whys, and more)' README.md && \
! grep -F '6가지 렌즈(Sailboat, Five Whys 등)로 독립 회고 실행' README.ko.md && \
! grep -F 'to extract patterns and generate hooks' README.md && \
! grep -F '회고 실행, 패턴 추출 및 훅 생성' README.ko.md && \

# 보존 영역
test "$(grep -c '| `/super-gsd:' README.md)" = "21" && \
test "$(grep -c '| `/super-gsd:' README.ko.md)" = "21" && \

# Row-index mirror
awk 'NR==37' README.md | grep -q 'sg-learn' && \
awk 'NR==50' README.md | grep -q 'sg-retro' && \
awk 'NR==37' README.ko.md | grep -q 'sg-learn' && \
awk 'NR==50' README.ko.md | grep -q 'sg-retro' && \

# Out of scope: 다른 plan 영역 미변경
test "$(git diff --stat HEAD -- skills/sg-retro/SKILL.md 2>/dev/null | wc -l)" = "0" && \
test "$(git diff --stat HEAD -- .agents/skills/sg-retro/SKILL.md 2>/dev/null | wc -l)" = "0" && \
test "$(git diff --stat HEAD -- .planning/TEAM.md 2>/dev/null | wc -l)" = "0" && \

# Diff size 합리적 (각 파일 정확히 2줄 변경)
test "$(git diff README.md | grep -c '^[+-][^+-]')" -le "5" && \
test "$(git diff README.ko.md | grep -c '^[+-][^+-]')" -le "5" && \

echo "Task 3 PASS — SC#1 verified"
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md sg-learn 행 description에 D-06 필수 토큰 5개(`smart default`, `three lenses`, `ssc`, `dspm`, `--pick`) 모두 grep 가능
    - README.md sg-retro 행 description에 D-07 필수 토큰 6개(`three lenses`, `(ssc, dspm, analyze)`, `smart default`, `dspm+ssc`, `--pick`, `.planning/lessons/`) 모두 grep 가능
    - README.md sg-retro 행 stale 토큰(`6 lenses`, `Sailboat`, `Five Whys`, `and more`)이 Commands 테이블에서 grep 0회
    - README.ko.md 동일 두 행이 한글 mirror로 갱신, 머신 토큰 영문 보존
    - 두 README 모두 Commands 테이블 행 수 21 보존
    - 두 README 모두 row-index 37/50에 sg-learn/sg-retro 행 위치 보존
    - 본 plan 외 영역(skills/.agents SKILL.md, TEAM.md) 미변경
    - 각 README 파일 git diff가 정확히 2줄 변경 수준
  </acceptance_criteria>
  <done>
README.md와 README.ko.md의 Commands 테이블 sg-learn 행(line 37) + sg-retro 행(line 50)이 새 동작(smart default + 3 lens + --pick)을 반영하도록 surgical 갱신되었다. 영문 source와 한글 mirror가 의미적으로 1:1 정렬. Stale 토큰(Hookify-era + 6 lens dropped)은 Commands 테이블에서 완전 제거. 본 plan 외 영역(SKILL.md frontmatter, TEAM.md)은 미변경. SC#1이 D-02 확장 해석(sg-retro 행 포함)으로 충족됨.
  </done>
</task>

</tasks>

<threat_model>
<!-- STRIDE 짧은 register. 본 plan은 text-only edits이므로 대부분 N/A. -->

**T-44-01 — Spoofing (impersonating commands):** N/A. 본 plan은 사용자에게 노출되는 광고 텍스트만 변경. 실제 명령 동작은 Phase 42·43에서 이미 구현 완료. 새 description이 거짓 광고가 되지 않도록 token spec(D-06/D-07)이 실제 SKILL.md 동작과 일치하는지가 risk — 본 plan은 token spec을 verbatim 적용하므로 mitigation 자체에 포함.

**T-44-02 — Tampering (prose drift):** Phase 42·43 retro lesson 핵심. README.md ↔ README.ko.md mirror가 분리되거나, 변경된 토큰이 보존 블록(`6 lenses` Roadmap historical 항목, Commands 테이블 헤더 등)을 의도치 않게 건드릴 risk. Mitigation: `<interfaces>` 블록의 "변경되는 산문 토큰" + "변경되는 구조를 묘사하는 보존 블록" 두 카테고리 enumerate + acceptance에서 보존 영역 grep 검증.

**T-44-03 — Repudiation:** N/A. 문서 변경은 git commit으로 audit trail 완전 보존.

**T-44-04 — Information Disclosure:** N/A. README는 public 문서. 새 토큰은 모두 Phase 42·43 SUMMARY에 이미 공개.

**T-44-05 — Denial of Service:** N/A. text-only edit.

**T-44-06 — Elevation of Privilege:** N/A.

</threat_model>

<verification_steps>
<!-- D-18-style 시나리오 점검 — sg-review 시점에 재실행 가능. -->

**시나리오 1 — sg-learn 행 새 동작 광고 (SC#1 핵심):**
사용자가 README.md를 열어 Commands 테이블 sg-learn 행을 보면 `smart default`, `three lenses (ssc, dspm)`, `--pick` 키워드가 모두 보인다. 한글 사용자가 README.ko.md를 열어도 동일 의미가 한글로 자연스럽게 표현된다.

**시나리오 2 — sg-retro 행 stale 표현 완전 제거 (D-02):**
README.md/README.ko.md의 Commands 테이블 sg-retro 행에서 `6 lenses`, `Sailboat`, `Five Whys`, `and more`, `6가지 렌즈(Sailboat, Five Whys 등)` 표현이 grep 0회. 사용자가 milestone v2.9 release를 보고 "왜 6 lens라고 적혀 있지?" 같은 confused report를 보내지 않는다.

**시나리오 3 — Roadmap historical accuracy 보존:**
README.md의 Roadmap Phase 10 항목(`Sailboat·Five Whys 포함 총 6개 렌즈로 확장`)은 byte-identical 보존. Phase 10은 v1.2 시점의 historical fact — 본 phase는 historical 항목을 retroactive하게 다시 쓰지 않는다.

**시나리오 4 — Commands 테이블 21개 행 보존:**
`grep -c '| \`/super-gsd:' README.md` returns `21`. `grep -c '| \`/super-gsd:' README.ko.md` returns `21`. sg-learn/sg-retro 외 19개 행(sg-start, sg-explore, sg-plan, sg-ui-plan, sg-execute, sg-review, sg-lessons, sg-ship, sg-complete, sg-new, sg-next, sg-status, sg-update, sg-quick, sg-health, sg-cleanup, sg-parallel-execute, sg-phase, sg-setup)이 모두 보존.

**시나리오 5 — 테이블 헤더 + 구분자 행 보존:**
`grep -F '| Command | What it does | When to use |' README.md` matches. `grep -F '| 명령어 | 하는 일 | 사용 시점 |' README.ko.md` matches.

**시나리오 6 — Mirror lock-step (D-05 carry-forward):**
README.md line 37과 README.ko.md line 37이 같은 의미적 행(sg-learn). README.md line 50과 README.ko.md line 50이 같은 의미적 행(sg-retro). 한 파일만 갱신되고 다른 파일이 stale로 남는 case 0건.

**시나리오 7 — Plan-spec deviation surface (Phase 42·43 패턴):**
Task 1+2 실행 중 `<interfaces>` token spec과 실제 적용 간 deviation 발견 시 SUMMARY § "Plan-spec Deviations (Surfaced)"에 정직하게 기록. Burying 금지.

**시나리오 8 — Out-of-scope 미변경:**
`git diff --stat HEAD -- skills/sg-retro/SKILL.md` = 0 lines. `git diff --stat HEAD -- .agents/skills/sg-retro/SKILL.md` = 0 lines. `git diff --stat HEAD -- .planning/TEAM.md` = 0 lines. (3개 모두 다른 plan 담당.)

**시나리오 9 — Surgical diff size:**
`git diff README.md`의 변경 줄 수 ≤ 5 (정확히 2줄 변경 + ± 컨텍스트). 같은 expectation을 README.ko.md에 적용. 의도하지 않은 추가 변경 없음.

</verification_steps>

<success_criteria>

1. README.md Commands 테이블 sg-learn 행(line 37)의 description 셀이 D-06 필수 토큰 5개(`smart default`, `three lenses`, `(ssc, dspm)`, `--pick`, `\`sg-retro\``)를 모두 포함하고, Hookify-era stale 토큰(`extract patterns and generate hooks`)이 제거된다.
2. README.md Commands 테이블 sg-retro 행(line 50)의 description 셀이 D-07 필수 토큰 6개(`three lenses`, `(ssc, dspm, analyze)`, `smart default`, `dspm+ssc`, `--pick`, `\`.planning/lessons/\``)를 모두 포함하고, Phase 42 dropped 토큰 4개(`6 lenses`, `Sailboat`, `Five Whys`, `and more`)가 Commands 테이블에서 완전히 제거된다.
3. README.ko.md Commands 테이블 sg-learn 행(line 37) + sg-retro 행(line 50)의 한글 description이 영문 source와 의미적으로 1:1 mirror — 머신 토큰(`sg-retro`/`--pick`/`ssc`/`dspm`/`analyze`/`.planning/lessons/`/`sg-learn`)은 영문 그대로 백틱 감싼 형태 유지, 산문은 한국어로 자연스럽게 표현. 한글 stale 표현(`6가지 렌즈(Sailboat, Five Whys 등)`, `회고 실행, 패턴 추출 및 훅 생성`)이 완전히 제거된다.
4. 두 README 파일 모두 Commands 테이블 행 총 개수(`grep -c '| \`/super-gsd:'`)가 21로 보존된다. 테이블 헤더 행(`| Command | What it does | When to use |` / `| 명령어 | 하는 일 | 사용 시점 |`)이 byte-identical 보존된다.
5. README.md ↔ README.ko.md row-index mirror가 보존된다 — line 37은 양쪽 파일에서 sg-learn 행, line 50은 양쪽 파일에서 sg-retro 행.
6. 본 plan 외 영역(`skills/sg-retro/SKILL.md`, `.agents/skills/sg-retro/SKILL.md`, `.planning/TEAM.md`, README의 workflow diagram + end-to-end 예시 + Roadmap historical 항목)은 byte-identical 보존된다.
7. 각 README 파일의 `git diff --stat HEAD`는 정확히 2줄 변경 수준(line 37 + line 50). 의도하지 않은 추가 변경 없음.
8. Plan-spec deviation 발생 시 SUMMARY § "Plan-spec Deviations (Surfaced)" 섹션에 정직하게 surface (Phase 42·43 패턴).

</success_criteria>

<output>
**SUMMARY 작성 가이드:**

본 plan 종료 시 `.planning/phases/44-documentation-sync/44-01-SUMMARY.md`를 생성한다. 형식은 Phase 42-01-SUMMARY.md / Phase 43-01-SUMMARY.md를 답습.

필수 섹션:
- `# Phase 44 — Plan 01 SUMMARY`
- `## Goal` — 1-2 문단 (README 두 파일 sg-learn/sg-retro 행 갱신 요약)
- `## Changes Made` — 변경된 토큰 enumerate (영문/한글 각각, line 번호 명시)
- `## Verification` — Task 3 정적 검증 명령 결과 (grep 결과 요약)
- `## Plan-spec Deviations (Surfaced)` — deviation 발견 시 honestly 기록 (Phase 42·43 패턴)
- `## D-10/D-16 Staging Note` — 본 plan은 README 쌍(영문+한글)을 단일 commit으로 staging. SKILL.md 쌍(44-02)과 TEAM.md(44-03)는 별도 commit. 3개 plan 모두 wave:1로 sg-parallel-execute가 3-way Task() dispatch.
- `## Next Steps` — 사용자가 sg-review 시 재실행할 수 있는 정적 검증 명령 1-2개

본 plan은 `<interfaces>`에 "변경되는 산문 토큰"과 "변경되는 구조를 묘사하는 보존 블록" 두 카테고리를 명시적으로 enumerate함으로써 Phase 43 retro P1 #1 lesson을 직접 closure한다. Phase 44 retro에서 효과 검증.
</output>
