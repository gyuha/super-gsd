---
phase: 44-documentation-sync
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/TEAM.md
autonomous: true
requirements: [DOC-01]
tags: [documentation, team, retrospective, workflow]

must_haves:
  truths:
    - "`.planning/TEAM.md`에 새 섹션 `## Retrospective workflow`가 `## File ownership` 섹션과 `## Merge order` 섹션 사이에 삽입된다 (D-09 위치)"
    - "신규 섹션이 영문 헤더 + 영문 산문으로 작성된다 (D-08 — TEAM.md 기존 영문 dominant 패턴 유지)"
    - "신규 섹션이 4개 sub-block을 포함한다: When to run sg-learn / What sg-learn does / When to use --pick / Where results live (D-10)"
    - "신규 섹션 길이가 ~30줄 이내로 유지된다 (D-11 — File ownership 섹션과 시각적 균형)"
    - "TEAM.md의 기존 모든 섹션(`# Team Workflow Guide`, `## Quick Start`, `## Branch strategy`, `## File ownership`, `## Merge order`)은 byte-identical 보존된다"
  artifacts:
    - path: ".planning/TEAM.md"
      provides: "신규 `## Retrospective workflow` 섹션 (영문, ~30줄, 4 sub-block)"
      contains: "## Retrospective workflow"
  key_links:
    - from: ".planning/TEAM.md `## File ownership` 섹션 끝"
      to: ".planning/TEAM.md `## Merge order` 섹션 시작"
      via: "신규 `## Retrospective workflow` 섹션 삽입 (line ~85-86 사이)"
      pattern: "## Retrospective workflow"
    - from: "Retrospective workflow 섹션 `When to run sg-learn` sub-block"
      to: "Retrospective workflow 섹션 `Where results live` sub-block"
      via: "4 sub-block 흐름 (실행 시점 → 동작 → --pick → 결과 위치)"
      pattern: "smart default"
---

<objective>
`.planning/TEAM.md`에 새 섹션 `## Retrospective workflow`를 추가하여 팀원이 Phase 42·43의 새 sg-learn/sg-retro 동작(smart default + 3 lens + `--pick`)을 워크플로우 가이드로 학습할 수 있도록 한다 (CONTEXT D-08~D-11).

변경 내용:

1. **새 섹션 헤더**: `## Retrospective workflow` (영문 — TEAM.md 기존 영문 dominant 패턴 유지, D-08).
2. **삽입 위치**: `## File ownership` 섹션 끝(line 85의 `---` 구분자)과 `## Merge order` 섹션 시작(line 87의 `## Merge order`) 사이 (D-09).
3. **섹션 내용** — 4 sub-block (D-10):
   - **When to run sg-learn**: `sg-review` 종료 직후, `sg-ship` 진입 전.
   - **What sg-learn does**: `sg-retro` 호출 → smart default `dspm+ssc` 2 lens 자동 실행 → 결과 append.
   - **When to use `--pick`**: smart default 외 특정 lens 조합이 필요할 때 (예: `analyze` 단독, `ssc` 단독).
   - **Where results live**: `.planning/lessons/{NN}-{YYYY-MM-DD}.md` + `HANDOFF.md` 1행.
4. **섹션 길이**: ~30줄 이내 (D-11 — File ownership 섹션과 시각적 균형, 약 23줄과 비슷한 규모).

Out of scope (본 plan):
- README.md/README.ko.md — 44-01-PLAN 담당
- `skills/sg-retro/SKILL.md` / `.agents/skills/sg-retro/SKILL.md` — 44-02-PLAN 담당
- TEAM.md 기존 섹션 본문 — byte-identical 보존
- TEAM.md 전체 한글화 — deferred (CONTEXT.md "Deferred Ideas")
- 별도 `docs/RETROSPECTIVE.md` 신설 — scope creep, 본 plan은 inline section만

Output: `.planning/TEAM.md`에 line 85-86 사이로 새 섹션(약 30줄) 삽입. 기존 모든 섹션은 byte-identical 보존.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-documentation-sync/44-CONTEXT.md
@.planning/phases/42-smart-default-lens/42-CONTEXT.md
@.planning/phases/43-pick-display-polish/43-CONTEXT.md
@.planning/lessons/42-2026-05-30.md
@.planning/lessons/43-2026-05-30.md
@CLAUDE.md
@.planning/TEAM.md
</context>

<interfaces>
<!-- Phase 44 plan 44-03에서 변경되는 식별자와 정확한 토큰. -->
<!-- Phase 43 retro P1 #1 적용: 변경되는 산문 토큰 + 변경되는 구조를 묘사하는 보존 블록 두 카테고리를 모두 명시한다. -->

**대상 파일 (write):** `.planning/TEAM.md`. **신규 섹션 1개 삽입.** 기존 모든 섹션 byte-identical.

**A. 새 섹션 정확한 본문 (영문 source — D-08, D-10 적용, ~28줄):**

`.planning/TEAM.md`의 line 85(File ownership 섹션 끝의 `---` 구분자)와 line 87(`## Merge order` 시작) 사이에 다음 블록을 정확히 삽입한다. (line 86은 빈 줄 — 보존 또는 신규 블록 시작 직전에 배치.)

```markdown
## Retrospective workflow

After implementation review, capture lessons before shipping so the next phase starts with current context.

**When to run sg-learn:**

Run `/super-gsd:sg-learn` immediately after `sg-review` completes and before `sg-ship`. Skipping this step is the most common cause of lessons drift between phases.

**What sg-learn does:**

`sg-learn` is a thin pass-through to the `sg-retro` skill. Without arguments, sg-retro runs a smart default of two lenses (`dspm` + `ssc`) — no prompt, no AskUserQuestion. Results are appended to `.planning/lessons/{NN}-{YYYY-MM-DD}.md`.

**When to use `--pick`:**

Pass `--pick` when you need a non-default lens combination — for example to run only `analyze` (conversation transcript scan), only `ssc` (behavior changes), or all three lenses together. `sg-learn --pick` or `sg-retro {phase} --pick` opens a single AskUserQuestion multiSelect with the three available lenses (`ssc`, `dspm`, `analyze`). Combining `--pick` with a positional lens argument is rejected with a graceful error — pick one form.

**Where results live:**

| Artifact | Path | Mode |
|----------|------|------|
| Lessons file | `.planning/lessons/{NN}-{YYYY-MM-DD}.md` | append-only |
| Handoff row | `.planning/HANDOFF.md` (one row per sg-learn invocation) | append-only |

The lessons file groups output by lens (`## Lens: ...` headers). Action Items rated P1 are emphasized with a `🔴 P1` prefix in the priority cell so the next phase's `sg-plan` surfaces them first.

---
```

**섹션 구조 세부 사항 (D-10, D-11):**

- 섹션 헤더: `## Retrospective workflow` (영문 — D-08)
- 인트로 산문 1줄 (`After implementation review, ...`)
- 4 sub-block, 각각 bold sub-header (`**When to run sg-learn:**` 등) + 1-2 문단 또는 mini-table
- 마지막 sub-block(`Where results live`)에 mini-table 1개 — 다른 섹션(`File ownership`)의 `| File | Owner | When modified |` 테이블 패턴 답습
- 섹션 끝 `---` 구분자 — 다른 섹션 사이 구분자와 동일 패턴
- 총 줄 수: 약 28줄 (D-11 ~30줄 상한 준수)

**머신 토큰 (영문 보존 — 산문 안에서도):**
- `/super-gsd:sg-learn`, `/super-gsd:sg-retro`, `sg-review`, `sg-ship`, `sg-plan`
- `sg-retro` (skill name)
- `dspm`, `ssc`, `analyze` (lens 이름)
- `--pick`, `--pick` (flag)
- `.planning/lessons/{NN}-{YYYY-MM-DD}.md`, `.planning/HANDOFF.md`
- `🔴 P1` (Phase 43 DISPLAY-01 emoji prefix)
- AskUserQuestion (Claude Code 기능 이름)

**필수 포함 토큰 (D-10 4 sub-block 충족):**
- `When to run sg-learn` (sub-block 1)
- `sg-review` + `sg-ship` (실행 시점 명시)
- `What sg-learn does` (sub-block 2)
- `smart default` + `dspm` + `ssc` (자동 실행 동작)
- `When to use \`--pick\`` (sub-block 3)
- `multiSelect` 또는 동등 표현 (AskUserQuestion 1회 호출 사실)
- `Where results live` (sub-block 4)
- `.planning/lessons/` + `HANDOFF.md` (결과 위치)

**선택 포함 토큰:**
- `🔴 P1` (Phase 43 DISPLAY-01 사실 — 사용자가 lessons 파일을 열었을 때 보이는 시각적 신호 설명)
- `Action Items` (lessons 파일 구조)

---

**변경되는 산문 토큰 (Phase 43 retro P1 #1 카테고리 1 — 명시적 enumerate):**

본 plan은 **신규 섹션 삽입**이므로 "변경되는 산문 토큰"은 전체 신규 블록. 기존 산문은 변경되지 않음. 그러나 신규 토큰이 기존 산문의 토큰과 unique한지 점검:

- `## Retrospective workflow` — TEAM.md 기존 섹션 헤더에 동일 문자열 없음 (unique). 다른 `##` 헤더: `## Quick Start`, `## Branch strategy`, `## File ownership`, `## Merge order`, `## 팀 현황` (한글, line 27 — Quick Start 섹션의 sample table 내부) — 충돌 없음.
- `## Retrospective workflow` 섹션 본문 내 토큰들이 TEAM.md 다른 영역에 기존 등장하는지 점검:
  - `sg-learn` — TEAM.md line 25 sample table 같은 영역에 부재 (TEAM.md 기존에 `sg-learn` 토큰 없음).
  - `sg-retro` — TEAM.md 기존에 부재.
  - `dspm`, `ssc`, `analyze`, `--pick`, `🔴 P1` — TEAM.md 기존에 모두 부재.
  - `smart default` — TEAM.md 기존에 부재.
  - `sg-review`, `sg-ship`, `sg-plan` — TEAM.md 기존에 부재.
  - `AskUserQuestion` — TEAM.md line 39, 51 (Branch strategy 섹션)에 등장. 본 plan 신규 섹션에도 1회 추가 → 총 출현 3회. acceptance에서 정확한 count 명시.
  - `HANDOFF.md` — TEAM.md line 9, 68, 83 (Quick Start, File ownership)에 등장. 본 plan 신규 섹션에도 1회 추가 → 총 출현 4회. acceptance에서 합리적 range로 검증.
  - `.planning/lessons/` — TEAM.md 기존에 부재.
  - `.planning/HANDOFF.md` — TEAM.md line 9, 68에 등장.

**변경되는 구조를 묘사하는 보존 블록 (Phase 43 retro P1 #1 카테고리 2 — 신규 sub-class, 본 plan 작성 시 명시):**

- **`## File ownership` 섹션 끝의 `---` 구분자 (line 85)** — 신규 섹션 직전에 위치. 본 plan은 이 `---`를 보존하고 그 뒤에 신규 섹션 시작. acceptance 검증.
- **`## Merge order` 섹션 시작 (line 87 `## Merge order`)** — 신규 섹션 끝에 추가하는 `---` 구분자가 이 line 직전에 위치하도록 — `---` 구분자 패턴은 다른 섹션 간격(`## File ownership`과 `## Merge order` 사이 line 85의 `---`처럼) 답습.
- **TEAM.md 전체 헤더 구조** — `# Team Workflow Guide` (line 1) + 5개 `##` 섹션 헤더. 본 plan은 6번째 섹션 추가. 다른 5개 헤더와 본문은 byte-identical 보존.
- **File ownership 섹션의 mini-table 구조 (line 64-68)** — `| File | Owner | When modified |` 헤더 + 3행. 본 plan의 신규 섹션 `Where results live` sub-block도 비슷한 3-column mini-table 사용 — 시각적 일관.

**보존되는 산문 토큰 (절대 변경 금지):**

- `# Team Workflow Guide` (line 1) — byte-identical
- intro paragraph (line 3) — byte-identical
- line 5 `---` — byte-identical
- `## Quick Start` 섹션 전체 (line 7-35) — byte-identical
- line 35 `---` — byte-identical
- `## Branch strategy` 섹션 전체 (line 37-57) — byte-identical
- line 58 `---` — byte-identical
- `## File ownership` 섹션 전체 (line 60-85) — byte-identical
- line 85 `---` — byte-identical (신규 섹션 직전 구분자)
- line 86 빈 줄 — byte-identical (또는 신규 섹션 본문이 line 86부터 시작하면 빈 줄을 신규 섹션이 흡수)
- `## Merge order` 섹션 전체 (line 87-104) — byte-identical (line 번호는 신규 섹션 삽입으로 밀림)
- 마지막 줄 (현재 line 104 `Always target main as the base branch...`) — byte-identical

**보존 블록과 변경 acceptance의 충돌 회피 (Phase 42 mistake #1 + Phase 43 P1 #1 직접 적용):**

- `## Retrospective workflow` 헤더는 신규 — `grep -c '^## Retrospective workflow'` returns `1`. TEAM.md에 다른 `Retrospective` 토큰 부재.
- 신규 섹션의 `🔴 P1` 토큰은 TEAM.md 기존에 부재 — `grep -c '🔴 P1'` returns `1` (Phase 43 DISPLAY-01 일관성 광고).
- 신규 섹션의 `smart default` 토큰은 TEAM.md 기존에 부재 — `grep -c 'smart default'` returns ≥ 1 (자연스러운 산문 안에서 한 번 또는 두 번 등장 가능).
- `AskUserQuestion` 토큰 count는 기존 2회 + 신규 1회 = 총 3회. acceptance에서 정확한 count 검증 가능 또는 `≥ 3` 합리적 range.
- 섹션 총 줄 수 — D-11 ~30줄 상한. 정확한 줄 수보다 `≤ 30` 범위 검증.

**길이 상한 검증 (D-11):**

- 신규 섹션 약 28줄 (헤더 1 + 빈 줄 + 인트로 1 + 빈 줄 + 4 sub-block × ~5줄/block + table 5줄 + 끝 `---` 1줄 + 여유)
- acceptance: `awk '/^## Retrospective workflow/,/^---$/' .planning/TEAM.md | wc -l` returns ≤ 30

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TEAM.md에 ## Retrospective workflow 섹션 삽입</name>
  <files>.planning/TEAM.md</files>
  <read_first>
    - .planning/TEAM.md (전체 — 105줄, 5 섹션 구조 + `---` 구분자 패턴 확인)
    - .planning/phases/44-documentation-sync/44-CONTEXT.md (D-08, D-09, D-10, D-11)
    - .planning/phases/42-smart-default-lens/42-CONTEXT.md (smart default = dspm+ssc 사실)
    - .planning/phases/43-pick-display-polish/43-CONTEXT.md (`--pick` 동작 + `🔴 P1` 사실)
    - CLAUDE.md §"사용자 언어 메시지" (머신 토큰 영문 보존)
    - CLAUDE.md §"GSD 문서 작성 지침" (TEAM.md는 영문 dominant 유지 — D-08 근거)
  </read_first>
  <action>
**A. TEAM.md 현재 구조 확인:**

기존 섹션 구조 (line 번호 기준):
- line 1: `# Team Workflow Guide`
- line 3: intro paragraph
- line 5: `---`
- line 7-35: `## Quick Start` 섹션
- line 35: `---`
- line 37-57: `## Branch strategy` 섹션
- line 58: `---`
- line 60-85: `## File ownership` 섹션
- line 85: `---`
- line 86: 빈 줄
- line 87: `## Merge order` 섹션 시작
- line 104: 마지막 줄 (`Always target main as the base branch...`)

**B. 신규 섹션 삽입 위치 결정 (D-09):**

`## File ownership` 섹션 끝의 `---` 구분자(line 85)와 `## Merge order` 섹션 시작(line 87) 사이. line 85의 `---`는 File ownership 섹션의 닫는 구분자로 보존하고, 신규 섹션은 line 86부터 시작하여 자체적으로 끝에 `---` 구분자를 가진다.

**삽입 위치 (Edit tool 사용 시):**

기존 `## File ownership` 섹션의 마지막 줄(line 83 `HANDOFF.md is append-only. Never edit or delete existing rows.`) 뒤의 `---` 구분자(line 85)와 `## Merge order` 헤더(line 87) 사이에 신규 섹션 본문 + 끝 `---` 구분자를 삽입.

**Edit tool 매칭 키 (unique substring):**

기존 텍스트(line 83-87):
```
HANDOFF.md is append-only. Never edit or delete existing rows.

---

## Merge order
```

위 5줄 블록 중 `---` 구분자 다음과 `## Merge order` 사이에 신규 섹션 본문을 삽입한다. 정확한 변경:

**Before (기존):**
```
HANDOFF.md is append-only. Never edit or delete existing rows.

---

## Merge order
```

**After (변경 후):**
```
HANDOFF.md is append-only. Never edit or delete existing rows.

---

## Retrospective workflow

After implementation review, capture lessons before shipping so the next phase starts with current context.

**When to run sg-learn:**

Run `/super-gsd:sg-learn` immediately after `sg-review` completes and before `sg-ship`. Skipping this step is the most common cause of lessons drift between phases.

**What sg-learn does:**

`sg-learn` is a thin pass-through to the `sg-retro` skill. Without arguments, sg-retro runs a smart default of two lenses (`dspm` + `ssc`) — no prompt, no AskUserQuestion. Results are appended to `.planning/lessons/{NN}-{YYYY-MM-DD}.md`.

**When to use `--pick`:**

Pass `--pick` when you need a non-default lens combination — for example to run only `analyze` (conversation transcript scan), only `ssc` (behavior changes), or all three lenses together. `sg-learn --pick` or `sg-retro {phase} --pick` opens a single AskUserQuestion multiSelect with the three available lenses (`ssc`, `dspm`, `analyze`). Combining `--pick` with a positional lens argument is rejected with a graceful error — pick one form.

**Where results live:**

| Artifact | Path | Mode |
|----------|------|------|
| Lessons file | `.planning/lessons/{NN}-{YYYY-MM-DD}.md` | append-only |
| Handoff row | `.planning/HANDOFF.md` (one row per sg-learn invocation) | append-only |

The lessons file groups output by lens (`## Lens: ...` headers). Action Items rated P1 are emphasized with a `🔴 P1` prefix in the priority cell so the next phase's `sg-plan` surfaces them first.

---

## Merge order
```

**C. 산문 규칙 (CLAUDE.md):**

- em-dash `—` (U+2014) 사용 — ASCII `--`로 교체 금지. 신규 섹션 산문에 `— no prompt, no AskUserQuestion` 등 em-dash 등장.
- 머신 토큰(영문 그대로): `/super-gsd:sg-learn`, `/super-gsd:sg-retro`, `sg-review`, `sg-ship`, `sg-plan`, `sg-retro`, `sg-learn`, `dspm`, `ssc`, `analyze`, `--pick`, `.planning/lessons/{NN}-{YYYY-MM-DD}.md`, `.planning/HANDOFF.md`, `🔴 P1`, `AskUserQuestion`.
- 머신 토큰은 백틱(`` ` ``) 또는 plain text — 본 plan은 백틱 사용으로 다른 섹션(Quick Start, Branch strategy)과 일관.
- 산문은 영문 (D-08 — TEAM.md 영문 dominant 유지).

**D. 보존 검증:**

- TEAM.md의 line 1 `# Team Workflow Guide`, intro, line 5 `---`, `## Quick Start` 섹션 전체, `## Branch strategy` 섹션 전체, `## File ownership` 섹션 전체 (line 60-83) + 끝 `---` (line 85), `## Merge order` 섹션 전체 (line 87-104)는 모두 byte-identical 보존.
- 신규 섹션은 line 85 `---`과 line 87 `## Merge order` 사이에 삽입됨 — 두 인접 마커 사이에 약 28-30줄 추가.
- TEAM.md의 한국어 sample table(`## 팀 현황 | 팀원 | 최근 Phase | 최근 Stage | 마지막 활동` line 27-33)은 Quick Start 섹션 내부에 있고 본 plan 변경 대상 아님 — byte-identical 보존.

**E. 섹션 길이 검증 (D-11):**

신규 섹션의 총 줄 수가 ~30줄 이내인지 확인. 위 본문은 약 28줄 (헤더 + 빈 줄 + 인트로 + 4 sub-block + table + 끝 `---`). 30줄 상한 준수.
  </action>
  <verify>
    <automated>
# A) 신규 섹션 헤더 존재
grep -F '## Retrospective workflow' .planning/TEAM.md && \

# B) 4 sub-block 헤더 존재
grep -F '**When to run sg-learn:**' .planning/TEAM.md && \
grep -F '**What sg-learn does:**' .planning/TEAM.md && \
grep -F '**When to use `--pick`:**' .planning/TEAM.md && \
grep -F '**Where results live:**' .planning/TEAM.md && \

# C) 핵심 정보 토큰 존재 (D-10 sub-block 내용)
grep -F 'immediately after `sg-review` completes and before `sg-ship`' .planning/TEAM.md && \
grep -F 'smart default of two lenses (`dspm` + `ssc`)' .planning/TEAM.md && \
grep -F 'no prompt, no AskUserQuestion' .planning/TEAM.md && \
grep -F 'single AskUserQuestion multiSelect' .planning/TEAM.md && \
grep -F 'Combining `--pick` with a positional lens argument is rejected' .planning/TEAM.md && \
grep -F '`.planning/lessons/{NN}-{YYYY-MM-DD}.md`' .planning/TEAM.md && \
grep -F '`🔴 P1` prefix in the priority cell' .planning/TEAM.md && \

# D) 머신 토큰 영문 보존 (lens 이름, flag)
grep -F '`dspm`' .planning/TEAM.md && \
grep -F '`ssc`' .planning/TEAM.md && \
grep -F '`analyze`' .planning/TEAM.md && \
grep -F '`--pick`' .planning/TEAM.md && \

# E) 위치 검증 — Retrospective workflow가 File ownership 다음, Merge order 직전
awk '/^## File ownership/{fo=NR} /^## Retrospective workflow/{rw=NR} /^## Merge order/{mo=NR} END {exit !(fo < rw && rw < mo)}' .planning/TEAM.md && \

# F) 기존 섹션 헤더 보존
grep -F '# Team Workflow Guide' .planning/TEAM.md && \
grep -F '## Quick Start' .planning/TEAM.md && \
grep -F '## Branch strategy' .planning/TEAM.md && \
grep -F '## File ownership' .planning/TEAM.md && \
grep -F '## Merge order' .planning/TEAM.md && \

# G) 섹션 헤더 총 개수 (1 H1 + 6 H2 = 신규 추가 후 7개; H2는 ## 시작)
test "$(grep -c '^## ' .planning/TEAM.md)" = "6" && \

# H) 길이 상한 (D-11 ~30줄 — Retrospective workflow 섹션 끝까지)
test "$(awk '/^## Retrospective workflow/,/^## Merge order/' .planning/TEAM.md | wc -l)" -le "32" && \

# I) 기존 섹션 본문 핵심 토큰 보존 (sample check)
grep -F '`git config user.name`' .planning/TEAM.md && \
grep -F 'phase/{PHASE_PAD}-{slug}' .planning/TEAM.md && \
grep -F '| Timestamp | Phase | From | To | Plan Hash | User |' .planning/TEAM.md && \
grep -F 'gh pr create --base main --title' .planning/TEAM.md && \
grep -F 'HANDOFF.md is append-only. Never edit or delete existing rows.' .planning/TEAM.md && \

# J) `---` 구분자 패턴 보존 (신규 섹션 끝에 1개 추가됨)
test "$(grep -c '^---$' .planning/TEAM.md)" = "5" && \

# K) 한글 sample table (Quick Start 내부) 보존
grep -F '## 팀 현황' .planning/TEAM.md && \
grep -F '| 팀원 | 최근 Phase | 최근 Stage | 마지막 활동 |' .planning/TEAM.md && \

# L) AskUserQuestion 토큰 출현 (기존 2회 + 신규 1회 = 총 3회 합리적 range)
test "$(grep -c 'AskUserQuestion' .planning/TEAM.md)" -ge "3" && \

echo "Task 1 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -F '## Retrospective workflow' .planning/TEAM.md` matches exactly 1 line (신규 섹션 헤더)
    - Source: `grep -F '**When to run sg-learn:**' .planning/TEAM.md` matches (sub-block 1)
    - Source: `grep -F '**What sg-learn does:**' .planning/TEAM.md` matches (sub-block 2)
    - Source: `grep -F '**When to use \`--pick\`:**' .planning/TEAM.md` matches (sub-block 3)
    - Source: `grep -F '**Where results live:**' .planning/TEAM.md` matches (sub-block 4)
    - Source: `grep -F 'smart default of two lenses (\`dspm\` + \`ssc\`)' .planning/TEAM.md` matches (D-10 sub-block 2 내용)
    - Source: `grep -F '\`🔴 P1\` prefix in the priority cell' .planning/TEAM.md` matches (Phase 43 DISPLAY-01 광고)
    - CLI: `awk '/^## File ownership/{fo=NR} /^## Retrospective workflow/{rw=NR} /^## Merge order/{mo=NR} END {exit !(fo < rw && rw < mo)}' .planning/TEAM.md` returns exit 0 (위치 검증)
    - CLI: `grep -c '^## ' .planning/TEAM.md` returns `6` (5 기존 + 1 신규)
    - CLI: `awk '/^## Retrospective workflow/,/^## Merge order/' .planning/TEAM.md | wc -l` returns ≤ 32 (D-11 상한)
    - Source: `grep -F 'HANDOFF.md is append-only. Never edit or delete existing rows.' .planning/TEAM.md` matches (File ownership 보존)
    - Source: `grep -F '## 팀 현황' .planning/TEAM.md` matches (Quick Start 한글 sample 보존)
    - Source: `grep -F 'gh pr create --base main --title' .planning/TEAM.md` matches (Merge order 보존)
    - CLI: `grep -c '^---$' .planning/TEAM.md` returns `5` (기존 4 + 신규 섹션 끝 1)
    - CLI: `git diff --stat HEAD -- .planning/TEAM.md` shows ~28-30 lines added (신규 섹션 길이)
  </acceptance_criteria>
  <done>
`.planning/TEAM.md`에 `## Retrospective workflow` 섹션이 `## File ownership` 섹션과 `## Merge order` 섹션 사이에 삽입되었다 (D-09 위치). 섹션은 영문 헤더 + 영문 산문 (D-08), 4 sub-block (When to run sg-learn / What sg-learn does / When to use `--pick` / Where results live — D-10), ~28줄 (D-11 ~30줄 상한 준수)로 구성되었다. Phase 42·43의 새 동작(smart default `dspm`+`ssc`, `--pick` graceful exit, `🔴 P1` DISPLAY-01)이 모두 광고되었다. 기존 섹션 5개(Team Workflow Guide / Quick Start / Branch strategy / File ownership / Merge order) + 한국어 sample table은 byte-identical 보존되었다.
  </done>
</task>

<task type="auto">
  <name>Task 2: SC#2 verification + plan-spec deviation 사전 점검</name>
  <files></files>
  <read_first>
    - .planning/TEAM.md (Task 1 적용 후)
    - .planning/phases/44-documentation-sync/44-CONTEXT.md (SC#2)
    - .planning/lessons/43-2026-05-30.md (Plan-spec Deviations 패턴)
  </read_first>
  <action>
Task 1 변경이 SC#2("`.planning/TEAM.md`에 회고 워크플로우 가이드(언제 sg-learn 실행, 결과 어디 저장, --pick 사용 시점)가 추가된다")를 충족하는지 정적 검증하고, plan-spec deviation 가능성을 사전 점검한다.

**SC#2 충족 시나리오 (D-10 4 sub-block 명시):**

SC#2 본문은 3가지 정보를 명시:
1. **언제 sg-learn 실행** → sub-block "When to run sg-learn" 충족
2. **결과 어디 저장** → sub-block "Where results live" 충족
3. **--pick 사용 시점** → sub-block "When to use `--pick`" 충족

D-10은 4번째 sub-block "What sg-learn does"를 추가 권장 — 본 plan이 이 권장도 적용. SC#2 spirit 확장 충족.

**정적 검증 명령:**

```bash
# 1. 4 sub-block 헤더 존재
grep -F '**When to run sg-learn:**' .planning/TEAM.md
grep -F '**What sg-learn does:**' .planning/TEAM.md
grep -F '**When to use `--pick`:**' .planning/TEAM.md
grep -F '**Where results live:**' .planning/TEAM.md

# 2. SC#2 본문의 3가지 정보 명시
grep -F 'immediately after `sg-review` completes and before `sg-ship`' .planning/TEAM.md  # 언제 실행
grep -F '`.planning/lessons/{NN}-{YYYY-MM-DD}.md`' .planning/TEAM.md  # 결과 저장 위치
grep -F 'non-default lens combination' .planning/TEAM.md  # --pick 사용 시점

# 3. 위치 검증
awk '/^## File ownership/{fo=NR} /^## Retrospective workflow/{rw=NR} /^## Merge order/{mo=NR} END {if (fo < rw && rw < mo) print "OK"; else print "FAIL"}' .planning/TEAM.md

# 4. 길이 상한 검증 (D-11)
awk '/^## Retrospective workflow/,/^## Merge order/' .planning/TEAM.md | wc -l   # expected ≤ 32
```

**Plan-spec deviation 사전 점검 (Phase 42·43 retro 패턴):**

본 plan의 `<interfaces>` 블록 token spec과 acceptance 사이 충돌 가능성:

**주의 deviation 시나리오:**

- `AskUserQuestion` 토큰 count — TEAM.md 기존에 2회 등장 (line 39, 51 Branch strategy 섹션). 본 plan은 신규 섹션에 1회 추가. acceptance는 `≥ 3` 합리적 range로 명시 (정확한 count 보다 안전).
- `HANDOFF.md` vs `.planning/HANDOFF.md` 표기 일관성 — TEAM.md 기존 line 9, 68, 83에 `HANDOFF.md`, `.planning/HANDOFF.md` 혼용. 본 plan 신규 섹션 mini-table은 `.planning/HANDOFF.md` 사용. 일관성 risk 낮음 (기존 패턴 모방).
- 머신 토큰 백틱 사용 — `--pick`, `sg-learn`, `sg-retro`, lens 이름 모두 백틱 감싸기. TEAM.md 기존 다른 섹션 패턴(`/super-gsd:sg-status --team`, `phase/{PHASE_PAD}-{slug}`)과 일관.
- 섹션 길이 — D-11 ~30줄 상한. 위 본문 약 28줄 (table 5줄 포함). acceptance에서 `≤ 32` 합리적 range로 명시 (em-dash 줄바꿈, 코드블록 등 약간의 차이 허용).
- 한국어 sample table(line 27-33)은 Quick Start 섹션 내부 — TEAM.md 영문 dominant 패턴에서 유일한 한국어 인용. 본 plan 신규 섹션은 영문만 사용 (D-08) — 추가 한국어 토큰 없음 → drift 없음.

**스코프 일관성 점검:**

```bash
# README 쌍과 SKILL.md 쌍은 본 plan에서 손대지 않음
test "$(git diff --stat HEAD -- README.md 2>/dev/null | wc -l)" = "0"
test "$(git diff --stat HEAD -- README.ko.md 2>/dev/null | wc -l)" = "0"
test "$(git diff --stat HEAD -- skills/sg-retro/SKILL.md 2>/dev/null | wc -l)" = "0"
test "$(git diff --stat HEAD -- .agents/skills/sg-retro/SKILL.md 2>/dev/null | wc -l)" = "0"

# TEAM.md만 변경
test "$(git diff --stat HEAD -- .planning/TEAM.md 2>/dev/null | wc -l)" = "1"
```

**TEAM.md 전체 한글화 옵션 — out of scope (CONTEXT.md "Deferred Ideas"):**

본 plan은 영문 dominant 패턴 유지 (D-08). 향후 milestone 단위 결정 후보로 backlog. 신규 섹션을 한국어로 작성하지 않은 이유를 SUMMARY § "Decisions Applied"에 명시.

**최종 SUMMARY 작성 가이드 (D-16 staging 적용):**

- 본 plan은 TEAM.md 단독 변경, 단일 commit.
- 44-01-PLAN(README 쌍) + 44-02-PLAN(SKILL.md 쌍) + 44-03-PLAN(TEAM.md) 모두 wave:1로 sg-parallel-execute가 3-way Task() dispatch.
- 본 plan은 D-10 4 sub-block 권장을 적용 (SC#2 본문은 3가지 정보만 명시했으나 "What sg-learn does"도 추가).
- D-08 영문 패턴 유지 결정(한글 작성하지 않음) trace SUMMARY에 명시.
  </action>
  <verify>
    <automated>
# 정적 SC#2 검증
grep -F '**When to run sg-learn:**' .planning/TEAM.md && \
grep -F '**What sg-learn does:**' .planning/TEAM.md && \
grep -F '**When to use `--pick`:**' .planning/TEAM.md && \
grep -F '**Where results live:**' .planning/TEAM.md && \

# SC#2 본문 3가지 정보
grep -F 'immediately after `sg-review` completes and before `sg-ship`' .planning/TEAM.md && \
grep -F '`.planning/lessons/{NN}-{YYYY-MM-DD}.md`' .planning/TEAM.md && \
grep -F 'non-default lens combination' .planning/TEAM.md && \

# 위치
awk '/^## File ownership/{fo=NR} /^## Retrospective workflow/{rw=NR} /^## Merge order/{mo=NR} END {exit !(fo < rw && rw < mo)}' .planning/TEAM.md && \

# 길이 상한
test "$(awk '/^## Retrospective workflow/,/^## Merge order/' .planning/TEAM.md | wc -l)" -le "32" && \

# 기존 섹션 보존 (sample)
grep -F '# Team Workflow Guide' .planning/TEAM.md && \
grep -F '## Quick Start' .planning/TEAM.md && \
grep -F '## Branch strategy' .planning/TEAM.md && \
grep -F '## File ownership' .planning/TEAM.md && \
grep -F '## Merge order' .planning/TEAM.md && \
grep -F '## 팀 현황' .planning/TEAM.md && \

# AskUserQuestion 합리적 range (기존 2 + 신규 1)
test "$(grep -c 'AskUserQuestion' .planning/TEAM.md)" -ge "3" && \

# Out of scope: 다른 plan 영역 미변경
test "$(git diff --stat HEAD -- README.md 2>/dev/null | wc -l)" = "0" && \
test "$(git diff --stat HEAD -- README.ko.md 2>/dev/null | wc -l)" = "0" && \
test "$(git diff --stat HEAD -- skills/sg-retro/SKILL.md 2>/dev/null | wc -l)" = "0" && \
test "$(git diff --stat HEAD -- .agents/skills/sg-retro/SKILL.md 2>/dev/null | wc -l)" = "0" && \

echo "Task 2 PASS — SC#2 verified (D-10 4 sub-block applied) + length within D-11 cap"
    </automated>
  </verify>
  <acceptance_criteria>
    - TEAM.md에 4 sub-block 헤더 모두 존재 (When to run sg-learn / What sg-learn does / When to use --pick / Where results live)
    - SC#2 본문 3가지 정보(언제 실행 / 결과 저장 / --pick 시점) 모두 명시
    - 신규 섹션이 `## File ownership` 다음, `## Merge order` 직전 위치
    - 신규 섹션 길이 ≤ 32줄 (D-11 ~30줄 상한 ± 여유)
    - 기존 5개 섹션 헤더 + 한국어 sample table 보존
    - AskUserQuestion 토큰 ≥ 3회 (기존 2 + 신규 1)
    - Out-of-scope 영역(README 쌍, SKILL.md 쌍) 미변경
    - TEAM.md만 본 plan에서 변경
  </acceptance_criteria>
  <done>
SC#2가 D-10 4 sub-block 권장을 모두 적용하여 충족됨 — SC#2 본문의 3가지 정보(언제 실행 / 결과 저장 / --pick 시점) + D-10 권장 4번째 정보(sg-learn이 무엇을 하는지)가 모두 명시. 신규 섹션 위치(D-09: File ownership과 Merge order 사이), 길이(D-11 ~30줄 상한 준수), 언어(D-08 영문 유지)도 모두 충족. 기존 5개 섹션과 한국어 sample table은 byte-identical 보존. 다른 plan 영역(README 쌍, SKILL.md 쌍) 미변경 — pairwise non-overlap files_modified 충족.
  </done>
</task>

</tasks>

<threat_model>

**T-44-01 — Spoofing (false workflow guidance):** TEAM.md는 팀원이 처음 읽는 workflow 안내서. 본 plan의 신규 섹션 내용이 실제 Phase 42·43 동작과 일치하지 않으면 팀원이 잘못된 절차를 따름. Mitigation: `<interfaces>` token spec이 Phase 42 (smart default = `dspm` + `ssc`) + Phase 43 (`--pick` graceful exit, `🔴 P1` prefix) 사실과 일치 검증.

**T-44-02 — Tampering (기존 섹션 손상):** 신규 섹션 삽입 시 기존 5개 섹션의 byte-identity가 깨질 risk (예: line 85 `---`을 삭제, Quick Start의 한국어 sample table에 임의 변경). Mitigation: acceptance에서 모든 기존 섹션 헤더 + sample table + Merge order의 핵심 토큰 보존 검증.

**T-44-03 — Repudiation:** N/A. git commit으로 audit trail.

**T-44-04 — Information Disclosure:** N/A. TEAM.md는 internal team 문서지만 .planning/은 .gitignore — 정보 노출 없음.

**T-44-05 — Denial of Service:** N/A.

**T-44-06 — Elevation of Privilege:** N/A.

</threat_model>

<verification_steps>

**시나리오 1 — 새 팀원이 sg-learn 실행 시점 학습 (SC#2 핵심):**
새 팀원이 `.planning/TEAM.md`를 열어 `## Retrospective workflow` 섹션을 보면 "Run `/super-gsd:sg-learn` immediately after `sg-review` completes and before `sg-ship`" 문장에서 즉시 실행 시점을 파악한다.

**시나리오 2 — 결과 저장 위치 학습:**
팀원이 mini-table에서 lessons 파일 경로(`.planning/lessons/{NN}-{YYYY-MM-DD}.md`)와 HANDOFF.md append 모드를 한눈에 확인.

**시나리오 3 — `--pick` 사용 시점 학습:**
팀원이 "Pass `--pick` when you need a non-default lens combination — for example to run only `analyze`, only `ssc`, or all three lenses together." 산문에서 `--pick`이 smart default(`dspm`+`ssc`)와 다른 조합을 원할 때 사용함을 학습.

**시나리오 4 — smart default 동작 학습:**
"sg-retro runs a smart default of two lenses (`dspm` + `ssc`) — no prompt, no AskUserQuestion." 산문에서 Phase 42 smart default 동작 정확히 이해.

**시나리오 5 — `🔴 P1` 시각적 신호 학습 (Phase 43 DISPLAY-01 광고):**
"Action Items rated P1 are emphasized with a `🔴 P1` prefix in the priority cell" 산문에서 lessons 파일 열었을 때 P1 시각적 신호의 의미 학습.

**시나리오 6 — 섹션 위치 검증 (D-09):**
`grep -n '^## ' .planning/TEAM.md` 출력에서 섹션 순서가 Quick Start → Branch strategy → File ownership → Retrospective workflow → Merge order인지 확인.

**시나리오 7 — 길이 상한 (D-11):**
`awk '/^## Retrospective workflow/,/^## Merge order/' .planning/TEAM.md | wc -l` returns ≤ 32. 섹션이 File ownership 섹션(약 23줄)과 시각적 균형.

**시나리오 8 — 기존 섹션 보존:**
`grep -c '^## ' .planning/TEAM.md` returns `6` (5 기존 + 1 신규 — `## 팀 현황`은 Quick Start 내부 sample table 헤더로 count되지 않음 — 확인 필요). `# Team Workflow Guide` (line 1), intro paragraph, `## Quick Start` ~ `## Merge order`의 모든 본문 토큰 보존.

**시나리오 9 — 한국어 sample table 보존 (drift 방지):**
TEAM.md Quick Start 섹션 line 27-33의 `## 팀 현황 | 팀원 | 최근 Phase | ...` 한국어 sample은 본 plan과 무관하게 byte-identical 보존.

**시나리오 10 — Out-of-scope 미변경:**
`git diff --stat HEAD -- README.md` = 0. `git diff --stat HEAD -- README.ko.md` = 0. `git diff --stat HEAD -- skills/sg-retro/SKILL.md` = 0. `git diff --stat HEAD -- .agents/skills/sg-retro/SKILL.md` = 0.

**시나리오 11 — Plan-spec deviation surface (Phase 42·43 패턴):**
Task 1 실행 중 token count 또는 phrasing이 spec과 다르게 적용되면 SUMMARY § "Plan-spec Deviations (Surfaced)"에 정직하게 기록.

</verification_steps>

<success_criteria>

1. `.planning/TEAM.md`에 `## Retrospective workflow` 섹션이 1개 추가되고, 위치가 `## File ownership` 섹션과 `## Merge order` 섹션 사이로 정확히 정렬된다 (D-09).
2. 신규 섹션이 영문 헤더 + 영문 산문으로 작성된다 (D-08 — TEAM.md 영문 dominant 유지).
3. 신규 섹션이 4개 sub-block을 포함한다: `**When to run sg-learn:**`, `**What sg-learn does:**`, `**When to use \`--pick\`:**`, `**Where results live:**` (D-10).
4. 4 sub-block의 내용이 SC#2 본문의 3가지 정보(언제 실행 / 결과 저장 / --pick 시점) + D-10 추가 권장(sg-learn 동작)을 모두 명시한다.
5. 신규 섹션 길이가 ≤ 32줄 (D-11 ~30줄 상한 ± 여유) 이내로 유지된다.
6. Phase 42 carry-forward 토큰(`smart default`, `dspm`, `ssc`, `analyze`, `lessons file`, `HANDOFF.md`) + Phase 43 carry-forward 토큰(`--pick`, `🔴 P1`, `AskUserQuestion multiSelect`, `graceful error`)이 모두 신규 섹션에 등장한다.
7. TEAM.md 기존 5개 섹션(`# Team Workflow Guide`, `## Quick Start`, `## Branch strategy`, `## File ownership`, `## Merge order`) + 한국어 sample table(line 27-33)이 byte-identical 보존된다.
8. 본 plan 외 영역(README.md, README.ko.md, skills/sg-retro/SKILL.md, .agents/skills/sg-retro/SKILL.md) 미변경.
9. 머신 토큰(`sg-learn`, `sg-retro`, `dspm`, `ssc`, `analyze`, `--pick`, `.planning/lessons/`, `HANDOFF.md`, `AskUserQuestion`, `🔴 P1`)이 영문 그대로 백틱 감싼 형태로 사용된다 (CLAUDE.md "사용자 언어 메시지" 규칙).
10. Plan-spec deviation 발생 시 SUMMARY § "Plan-spec Deviations (Surfaced)"에 정직하게 surface (Phase 42·43 패턴).

</success_criteria>

<output>

**SUMMARY 작성 가이드:**

본 plan 종료 시 `.planning/phases/44-documentation-sync/44-03-SUMMARY.md` 생성.

필수 섹션:
- `# Phase 44 — Plan 03 SUMMARY`
- `## Goal` — TEAM.md에 회고 워크플로우 섹션 추가 요약
- `## Decisions Applied` — D-08(영문 유지) + D-09(위치) + D-10(4 sub-block) + D-11(~30줄 상한) trace
- `## Changes Made` — 신규 섹션 본문 enumerate (sub-block별 정보 요약)
- `## Verification` — Task 2 정적 검증 명령 결과
- `## Plan-spec Deviations (Surfaced)` — deviation 발견 시 honestly 기록
- `## D-10/D-16 Staging Note` — 본 plan은 TEAM.md 단독 commit. 44-01 README 쌍과 44-02 SKILL.md 쌍은 별도 commit. 3 plan 모두 wave:1, sg-parallel-execute 3-way Task() dispatch.
- `## Out-of-Scope Trace` — TEAM.md 전체 한글화는 D-08에 따라 deferred. 본 plan은 영문 dominant 패턴 유지.
- `## Next Steps` — 사용자가 sg-review 시 재실행할 수 있는 정적 검증 명령

본 plan은 `<interfaces>`에 "변경되는 산문 토큰"과 "변경되는 구조를 묘사하는 보존 블록" 두 카테고리를 명시함으로써 Phase 43 retro P1 #1 lesson 직접 closure에 참여 (44-01, 44-02와 함께).

</output>
