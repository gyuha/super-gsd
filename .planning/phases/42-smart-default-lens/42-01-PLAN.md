---
phase: 42-smart-default-lens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-retro/SKILL.md
autonomous: true
requirements: [LENS-01, LENS-02]
tags: [sg-retro, lens-consolidation, smart-default, skills]

must_haves:
  truths:
    - "인자 없이 sg-retro를 호출하면 AskUserQuestion 없이 dspm → ssc 순서로 즉시 실행된다"
    - "ssc/dspm/analyze 인자는 그대로 매칭되어 단독 또는 멀티 실행된다"
    - "4ls/sail/5why 인자는 stderr 에러 + exit 1로 거부되고 lessons 파일이 생성되지 않는다"
    - "멀티 인자 중 첫 dropped lens 발견 즉시 거부되며 후속 lens는 실행되지 않는다"
    - "<process> 블록에서 4ls/sail/5why 관련 sub-block과 LENS_NAME 매핑이 완전히 사라진다"
  artifacts:
    - path: "skills/sg-retro/SKILL.md"
      provides: "Smart default lens 자동 진입 + 3-lens consolidation + dropped-lens reject"
      contains: 'LENS_CODES_ARRAY="dspm ssc"'
  key_links:
    - from: "skills/sg-retro/SKILL.md Step 2 case-statement"
      to: "Step 5 sub-block / LENS_NAME 매핑"
      via: "LENS_CODE 변수 흐름"
      pattern: 'case "\$LENS_LC" in'
    - from: "skills/sg-retro/SKILL.md Step 2 smart default 분기"
      to: "Step 4-6 LENS_CODES_ARRAY 순차 실행"
      via: "LENS_CODES_ARRAY 변수"
      pattern: "LENS_CODES_ARRAY"
---

<objective>
`skills/sg-retro/SKILL.md`를 surgical edit 하여 Phase 42의 두 가지 핵심 변경을 적용한다:

1. **Smart default 자동 진입 (D-01, D-02, D-08)** — `LENS_RAW`와 `EXTRA_LENS_CODES`가 모두 비어 있을 때 `LENS_CODES_ARRAY="dspm ssc"`로 즉시 세팅하고, Step 2의 AskUserQuestion 6-옵션 multiSelect 블록 및 그 직후 parsing 블록을 **통째로 삭제**한다.
2. **Lens 6→3 통합 (D-03, D-04, D-05, D-09)** — `ssc`/`dspm`/`analyze` 3개만 유지. `4ls`/`sail`/`5why` 관련 코드는 (a) Step 2 case 매핑, (b) Step 2 VALID_EXTRAS inner case, (c) Step 5 sub-block 본문, (d) Step 5 LENS_NAME 매핑, (e) `<success_criteria>` 항목 #2/#5/#7/#8, (f) frontmatter `description`에서 모두 제거한다.
3. **Dropped lens reject (D-06, D-07)** — Step 2의 case 매핑에서 `4ls`/`sail`/`5why`(또는 미지의 코드)가 들어오면 stderr에 영문 에러 메시지를 emit하고 `exit 1`. 멀티 인자도 첫 dropped 발견 즉시 거부.

Purpose: lens 선택 마찰을 제거하고(LENS-01) lens 종류를 핵심 3개로 통합하여(LENS-02) 회고 진입 비용을 0에 가깝게 낮춘다.

Output: 의미적으로 동등한 단일 파일 (`skills/sg-retro/SKILL.md`) — 라인 수는 감소하지만 Step 번호/구조는 보존.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-smart-default-lens/42-CONTEXT.md
@.planning/phases/42-smart-default-lens/42-PATTERNS.md
@CLAUDE.md
@skills/sg-retro/SKILL.md
</context>

<interfaces>
<!-- Phase 42에서 변경 대상이 되는 식별자와 정확한 토큰. 실행자는 추가 탐색 없이 이 값들을 그대로 사용한다. -->

**유지 lens 코드 (3개):** `ssc`, `dspm`, `analyze`
**제거 lens 코드 (3개):** `4ls`, `sail`, `5why`

**유지 LENS_NAME 매핑:**
- `ssc` → `"Start/Stop/Continue"`
- `dspm` → `"Decisions/Surprises/Patterns/Mistakes"`
- `analyze` → `"Conversation Analyzer"`

**Smart default 진입 조건:** `[ -z "$LENS_CODES_ARRAY" ] && [ -z "$LENS_RAW" ] && [ -z "$EXTRA_LENS_CODES" ]`
**Smart default 세팅:** `LENS_CODES_ARRAY="dspm ssc"` (dspm을 앞에 — 기술 회고 → 행동 권고 흐름)

**Dropped-lens 에러 메시지 (영문 fallback — Claude가 출력 시점에 사용자 언어로 렌더링):**
```
Lens '{code}' is no longer supported (removed in v2.9).
Available lenses: ssc, dspm, analyze.
Run without lens argument to use smart default (dspm+ssc).
```
이어서 `exit 1`. 메시지 본문에서 `{code}`는 실제 입력된 dropped 코드로 치환.

**현재 6-케이스 case-statement (Step 2, 80-94줄):**
```bash
case "$LENS_LC" in
  ssc)     LENS_CODE="ssc"     ;;
  4ls)     LENS_CODE="4ls"     ;;
  dspm)    LENS_CODE="dspm"    ;;
  sail)    LENS_CODE="sail"    ;;
  5why)    LENS_CODE="5why"    ;;
  analyze) LENS_CODE="analyze" ;;
  *)       LENS_CODE=""        ;;
esac
```

**현재 VALID_EXTRAS inner case (Step 2, 약 130줄):**
```bash
case "$EC_LC" in
  ssc|4ls|dspm|sail|5why|analyze) VALID_EXTRAS="${VALID_EXTRAS} ${EC_LC}" ;;
esac
```

**현재 LENS_NAME 매핑 (Step 5, 299-306줄):**
```bash
case "$LENS_CODE" in
  ssc)     LENS_NAME="Start/Stop/Continue" ;;
  4ls)     LENS_NAME="4Ls" ;;
  dspm)    LENS_NAME="Decisions/Surprises/Patterns/Mistakes" ;;
  sail)    LENS_NAME="Sailboat" ;;
  5why)    LENS_NAME="Five Whys" ;;
  analyze) LENS_NAME="Conversation Analyzer" ;;
esac
```

**보존 금지 영역 (절대 손대지 않는다):**
- Step 1 STATE.md parsing 블록 (macOS 호환 grep+sed+awk 파이프라인, 주석 마커 사이의 모든 줄)
- `<language>` 블록
- Step 3 artifact collection
- Step 5 ssc/dspm/analyze sub-block 본문 (DSPM explicit guard 포함)
- Step 5 analyze 끝의 `ANALYZE_LENS_RAN=true`
- Step 6 append 로직 (`>>` redirect 단일 블록)
- `<success_criteria>` 항목 #1, #3, #4, #6, #9, #10, #11

**현재 description (1-3줄):**
```
description: Use this when a phase is complete and a structured retrospective is needed — collects phase artifacts and git context, then facilitates one or more of six lenses (ssc, 4ls, dspm, sail, 5why, analyze) and appends results to .planning/lessons/.
```
→ 변경 후:
```
description: Use this when a phase is complete and a structured retrospective is needed — collects phase artifacts and git context, then facilitates one or more of three lenses (ssc, dspm, analyze) and appends results to .planning/lessons/. Smart default (dspm+ssc) runs when no lens argument is provided.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Smart default 진입 + Step 2 lens case 매핑 축소 + AskUserQuestion 블록 삭제</name>
  <files>skills/sg-retro/SKILL.md</files>
  <read_first>
    - skills/sg-retro/SKILL.md (전체 — 변경 영역 lines 80-150 + frontmatter 1-3 포함, 인접 블록 들여쓰기/정렬 보존을 위해 전체 컨텍스트 확보)
    - .planning/phases/42-smart-default-lens/42-CONTEXT.md (D-01, D-02, D-05, D-06, D-07, D-08, D-09 결정)
    - .planning/phases/42-smart-default-lens/42-PATTERNS.md §1.A, §1.C, §1.D, §1.E (변경 지시 본문)
    - CLAUDE.md §"사용자 언어 메시지" + §"macOS 셸 이식성"
  </read_first>
  <action>
**A. frontmatter description 교체 (D-01, D-04):**
Line 3의 `description` 필드 본문에서 `six lenses (ssc, 4ls, dspm, sail, 5why, analyze)` 부분을 `three lenses (ssc, dspm, analyze)`로 교체하고, 문장 끝(마침표 이전)에 ` Smart default (dspm+ssc) runs when no lens argument is provided.` 한 문장을 추가한다. `<language>` 블록(이어지는 줄)과 다른 frontmatter 필드(name)는 손대지 않는다.

**B. Step 2 — `LENS_CODE` case-statement 축소 + dropped reject 분기 (D-05, D-06, D-09):**
Step 2의 기존 6-케이스 case-statement (lines ~80-94)에서:
- 보존: `ssc)`, `dspm)`, `analyze)` 세 줄 (들여쓰기·공백 패딩 그대로).
- 삭제: `4ls)`, `sail)`, `5why)` 세 줄 완전 제거 (주석/deprecated 마커 금지 — D-05).
- `*)` fallback 분기를 dropped/unknown 에러 분기로 교체. 동작:
  1. 입력된 `$LENS_LC` 값이 `4ls`, `sail`, `5why` 중 하나면 `printf "Lens '%s' is no longer supported (removed in v2.9).\nAvailable lenses: ssc, dspm, analyze.\nRun without lens argument to use smart default (dspm+ssc).\n" "$LENS_LC" >&2` 실행 후 `exit 1`.
  2. 그 외 미지의 코드도 동일한 메시지로 처리(코드 값은 그대로 치환). 즉 4ls/sail/5why를 별도 case로 분리하든 단일 `*)` 안에서 처리하든 메시지·exit 동작은 일관되어야 한다. D-09 권장은 "별도 case로 분리"이므로 다음과 같이 구현한다:

```bash
case "$LENS_LC" in
  ssc)     LENS_CODE="ssc"     ;;
  dspm)    LENS_CODE="dspm"    ;;
  analyze) LENS_CODE="analyze" ;;
  4ls|sail|5why)
    printf "Lens '%s' is no longer supported (removed in v2.9).\nAvailable lenses: ssc, dspm, analyze.\nRun without lens argument to use smart default (dspm+ssc).\n" "$LENS_LC" >&2
    exit 1
    ;;
  *)
    printf "Lens '%s' is no longer supported (removed in v2.9).\nAvailable lenses: ssc, dspm, analyze.\nRun without lens argument to use smart default (dspm+ssc).\n" "$LENS_LC" >&2
    exit 1
    ;;
esac
```

(들여쓰기는 기존 case-statement와 동일하게 2-space. `;;` 위치는 일관 유지.)

**C. Step 2 — AskUserQuestion 호출 블록 + 직후 parsing 블록 삭제 (D-08):**
PATTERNS.md §1.D에 기술된 `AskUserQuestion(... questions: [{ ... 6 options ... }] ...)` 전체 블록(약 lines 96-127) **및** 그 직후의 multiSelect parsing bash 블록(`LENS_CODES_ARRAY=$(printf '%s' "$ASKUSERQUESTION_RESPONSE" \\ | grep -oE '\\((ssc|4ls|dspm|sail|5why|analyze)\\)' ...` 포함 약 5-7줄)을 통째로 삭제한다. 두 블록 사이의 안내 산문(있다면)도 함께 제거. 삭제 후 위/아래 보존 블록 사이에 빈 줄 1개만 남도록 한다.

**D. Step 2 — VALID_EXTRAS inner case 축소 (D-05):**
PATTERNS.md §1.E에 기술된 D-19 멀티-lens 인자 경로의 inner case 줄
```bash
ssc|4ls|dspm|sail|5why|analyze) VALID_EXTRAS="${VALID_EXTRAS} ${EC_LC}" ;;
```
의 패턴을 `ssc|dspm|analyze) VALID_EXTRAS="${VALID_EXTRAS} ${EC_LC}" ;;`로 축소. 추가로 dropped 토큰이 `EXTRA_LENS_CODES`에 포함된 경우 D-07에 따라 즉시 에러 + exit 1로 거부해야 하므로, 동일 case에 dropped 분기를 명시적으로 추가한다:

```bash
case "$EC_LC" in
  ssc|dspm|analyze) VALID_EXTRAS="${VALID_EXTRAS} ${EC_LC}" ;;
  4ls|sail|5why)
    printf "Lens '%s' is no longer supported (removed in v2.9).\nAvailable lenses: ssc, dspm, analyze.\nRun without lens argument to use smart default (dspm+ssc).\n" "$EC_LC" >&2
    exit 1
    ;;
  *)
    printf "Lens '%s' is no longer supported (removed in v2.9).\nAvailable lenses: ssc, dspm, analyze.\nRun without lens argument to use smart default (dspm+ssc).\n" "$EC_LC" >&2
    exit 1
    ;;
esac
```

이 변경으로 `sg-retro 42 dspm sail` 같은 멀티 인자에서 두 번째 토큰이 dropped일 때 첫 dropped 발견 즉시 거부된다 (D-07).

**E. Step 2 — Smart default 신규 진입 분기 (D-02):**
D-19 경로(LENS_CODES_ARRAY 구성 if-elif) 직후, Step 3로 넘어가기 전 위치에 다음 분기를 추가:

```bash
# Phase 42 (D-02): smart default — no args → dspm+ssc, dspm first (technical core → behavior recommendation)
if [ -z "$LENS_CODES_ARRAY" ] && [ -z "$LENS_RAW" ] && [ -z "$EXTRA_LENS_CODES" ]; then
  LENS_CODES_ARRAY="dspm ssc"
fi
```

stderr 안내 로그 출력은 CONTEXT line 77의 Claude 재량 영역 — 본 plan에서는 **출력하지 않는** 쪽으로 결정(잡음 최소화, 사용자 입장에서 smart default가 동작했음은 lessons 파일의 lens 헤더로 확인 가능). 추후 사용자 피드백 누적 시 재검토.

**F. macOS 셸 이식성 (CLAUDE.md):**
- `grep -P`(PCRE) 금지. 위 어떤 분기에서도 `printf` + `>&2` 패턴만 사용 (echo 다중 줄 회피).
- 변수 확장 시 항상 큰따옴표 사용(`"$LENS_LC"`).
- Step 1의 STATE.md parsing 블록(`# --- BEGIN ... ---` ~ `# --- END ... ---` 사이)은 **절대 손대지 않는다**.

**G. 의미적 보존 검증:**
- 변경 전/후 두 파일 모두에서 Step 1, Step 3, Step 4, Step 5 ssc/dspm/analyze sub-block 본문, Step 6 append 블록은 byte-identical 이어야 한다.
- success_criteria 블록은 본 Task에서 손대지 않는다 (Task 2가 담당).
  </action>
  <verify>
    <automated>
# A) frontmatter description 업데이트 검증
grep -q 'three lenses (ssc, dspm, analyze)' skills/sg-retro/SKILL.md && \
grep -q 'Smart default (dspm+ssc)' skills/sg-retro/SKILL.md && \
! grep -q 'six lenses (ssc, 4ls, dspm, sail, 5why, analyze)' skills/sg-retro/SKILL.md && \

# B) Step 2 case-statement — ssc/dspm/analyze 케이스 보존
grep -c '^\s*ssc)\s*LENS_CODE="ssc"' skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \
grep -c '^\s*dspm)\s*LENS_CODE="dspm"' skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \
grep -c '^\s*analyze)\s*LENS_CODE="analyze"' skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \

# C) dropped lens 케이스 줄 완전 삭제 (case-statement 영역)
! grep -E '^\s*4ls\)\s*LENS_CODE=' skills/sg-retro/SKILL.md && \
! grep -E '^\s*sail\)\s*LENS_CODE=' skills/sg-retro/SKILL.md && \
! grep -E '^\s*5why\)\s*LENS_CODE=' skills/sg-retro/SKILL.md && \

# D) dropped reject 메시지 패턴 존재
grep -q "no longer supported (removed in v2.9)" skills/sg-retro/SKILL.md && \
grep -q "Available lenses: ssc, dspm, analyze" skills/sg-retro/SKILL.md && \
grep -q "Run without lens argument to use smart default (dspm+ssc)" skills/sg-retro/SKILL.md && \

# E) AskUserQuestion 6-옵션 블록 완전 삭제
! grep -q 'multiSelect: true' skills/sg-retro/SKILL.md && \
! grep -q 'Which retrospective lens(es) do you want to run' skills/sg-retro/SKILL.md && \
! grep -qE 'grep -oE.*\(ssc\|4ls\|dspm\|sail\|5why\|analyze\)' skills/sg-retro/SKILL.md && \

# F) Smart default 분기 신규 삽입
grep -q 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md && \
grep -q 'D-02.*smart default' skills/sg-retro/SKILL.md && \

# G) VALID_EXTRAS inner case 축소
grep -qE 'ssc\|dspm\|analyze\) VALID_EXTRAS=' skills/sg-retro/SKILL.md && \
! grep -qE 'ssc\|4ls\|dspm\|sail\|5why\|analyze\) VALID_EXTRAS=' skills/sg-retro/SKILL.md && \

# H) Step 1 STATE.md parsing 블록 보존 (macOS 호환 주석)
grep -q 'BEGIN STATE.md Phase parsing block (macOS-compatible grep + sed + awk pipeline)' skills/sg-retro/SKILL.md && \

echo "Task 1 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'three lenses (ssc, dspm, analyze)' skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'six lenses' skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -E '^\s*4ls\)' skills/sg-retro/SKILL.md` returns no match (exit 1)
    - Source: `grep -E '^\s*sail\)' skills/sg-retro/SKILL.md` returns no match (exit 1)
    - Source: `grep -E '^\s*5why\)' skills/sg-retro/SKILL.md` returns no match (exit 1)
    - Source: `grep -c "no longer supported (removed in v2.9)" skills/sg-retro/SKILL.md` returns ≥ 1
    - Source: `grep -c 'LENS_CODES_ARRAY="dspm ssc"' skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'multiSelect: true' skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -c 'BEGIN STATE.md Phase parsing block (macOS-compatible grep + sed + awk pipeline)' skills/sg-retro/SKILL.md` returns `1`
    - Behavior assertion (manual verification via Task 3): `Skill(skill="sg-retro", args="42")` → no AskUserQuestion call observed, lessons file contains `## Lens:` header for dspm followed by ssc in that order
    - Behavior assertion (manual verification via Task 3): `Skill(skill="sg-retro", args="42 sail")` → stderr contains string `Lens 'sail' is no longer supported (removed in v2.9).`, process exits with code 1, no new lessons file is created in `.planning/lessons/`
  </acceptance_criteria>
  <done>
Frontmatter description이 3-lens + smart default 안내 문구로 교체되었고, Step 2의 case-statement는 ssc/dspm/analyze만 매칭하며 4ls/sail/5why와 미지 코드는 stderr 에러 + exit 1로 거부된다. AskUserQuestion 6-옵션 multiSelect 블록과 직후 parsing 블록이 완전히 삭제되었고, Smart default 분기(`LENS_CODES_ARRAY="dspm ssc"`)가 신규 삽입되었다. VALID_EXTRAS inner case도 3-lens + dropped reject로 축소되었다. Step 1 STATE.md parsing 블록은 byte-identical로 보존되었다.
  </done>
</task>

<task type="auto">
  <name>Task 2: Step 5 sub-block 삭제 + LENS_NAME 매핑 축소 + success_criteria 재작성</name>
  <files>skills/sg-retro/SKILL.md</files>
  <read_first>
    - skills/sg-retro/SKILL.md (Task 1 변경 적용 후 상태 — Step 5 234-306줄 + success_criteria 408-420줄)
    - .planning/phases/42-smart-default-lens/42-CONTEXT.md (D-05, D-06, D-07 + Out of scope 명시)
    - .planning/phases/42-smart-default-lens/42-PATTERNS.md §1.F, §1.G, §1.H (sub-block 삭제 + LENS_NAME 축소 + success_criteria 재작성 표)
    - CLAUDE.md §"사용자 언어 메시지"
  </read_first>
  <action>
**A. Step 5 — Sub-block 삭제 (D-05):**
Step 5의 lens facilitation sub-block에서 다음 세 블록을 **완전히 삭제**한다 (주석/deprecated 마커 금지):

1. **`4ls` sub-block** (현재 약 lines 238-240) — Liked/Learned/Lacked/Longed For 4개 subheading + facilitation 본문 전체.
2. **`sail` sub-block** (현재 약 lines 247-249) — Wind/Anchor/Sun/Rock + facilitation 본문 전체.
3. **`5why` sub-block** (현재 약 lines 251-261) — Problem Statement + Why 1~5 + Root Cause + 7-step AskUserQuestion 절차 전체.

**보존:**
- `ssc` sub-block (Step 5 시작부, lines ~234-237)
- `dspm` sub-block + **DSPM explicit guard** (`**Explicit guard:** DSPM derives all four categories strictly from collected phase artifacts + git log/git diff. Do NOT read or analyze session transcript...`) — byte-identical 유지.
- `analyze` sub-block + 끝의 `ANALYZE_LENS_RAN=true` flag-setter — byte-identical 유지.

삭제 후 인접 보존 블록 사이에 빈 줄 1개만 남도록 정리. sub-block들 사이의 헤더(`### Lens: ...`)는 보존 블록의 헤더만 남는다.

**B. Step 5 — LENS_NAME case-statement 축소 (D-05):**
PATTERNS.md §1.G에 기술된 LENS_NAME 매핑(현재 lines 299-306)에서 `4ls)`, `sail)`, `5why)` 세 줄을 삭제하여 다음 3-케이스만 남긴다:

```bash
case "$LENS_CODE" in
  ssc)     LENS_NAME="Start/Stop/Continue" ;;
  dspm)    LENS_NAME="Decisions/Surprises/Patterns/Mistakes" ;;
  analyze) LENS_NAME="Conversation Analyzer" ;;
esac
```

들여쓰기·공백 패딩은 기존과 동일(가장 긴 토큰 `analyze`(7자)에 맞춘 정렬 유지). 주석으로 제거된 매핑을 남기지 않는다.

**C. `<success_criteria>` 블록 재작성 (D-02, D-05, D-06, D-07 검증 요구):**
현재 11개 항목 중 dropped lens를 참조하는 항목들을 PATTERNS.md §1.H 표에 따라 다음과 같이 수정한다.

**수정 대상 항목:**

- **#2** (현재 본문 요지: "second argument is `ssc`/`4ls`/`dspm`/`sail`/`5why`/`analyze` (case-insensitive)... six options")
  → 재작성 후:
  ```
  Second argument (if provided) is one of `ssc`/`dspm`/`analyze` (case-insensitive). Removed codes (`4ls`/`sail`/`5why`) or any unknown code emit a stderr error message containing "no longer supported (removed in v2.9)" and exit 1 without creating a lessons file. When no second argument is provided AND no extra-lens arguments are provided, smart default applies: `LENS_CODES_ARRAY="dspm ssc"` is set automatically (dspm first, then ssc) without invoking AskUserQuestion.
  ```

- **#5** (현재 본문 요지: "DSPM lens derives strictly from phase artifacts... Session-transcript scanning is never performed for ssc/4ls/dspm/sail lenses (D-11)")
  → 재작성 후:
  ```
  DSPM lens derives strictly from collected phase artifacts + `git log`/`git diff`. Session-transcript scanning is never performed for ssc/dspm lenses (D-11). The analyze lens is the only path that reads the session transcript.
  ```
  (`/4ls/sail` 토큰 제거 — 3개 lens만 언급)

- **#7** (현재 본문 요지: "args=\"10 sail\" / \"10 5why\" / \"10 analyze\" trigger direct execution")
  → 재작성 후 (D-06 reject 검증으로 재작성):
  ```
  Invocation with a removed lens code, e.g. `args="10 sail"` / `args="10 4ls"` / `args="10 5why"`, exits with code 1, emits the dropped-lens error message to stderr, and does not create or append to any lessons file. `args="10 analyze"` continues to trigger direct analyze-only execution.
  ```

- **#8** (현재 본문 요지: "args=\"10 4ls dspm\" builds LENS_CODES_ARRAY=[\"4ls\", \"dspm\"]...")
  → 재작성 후 (D-07 multi-arg reject + 정상 multi 동시 검증):
  ```
  Multi-lens invocation `args="10 ssc dspm"` builds `LENS_CODES_ARRAY="ssc dspm"` and executes both lenses sequentially with a single combined run suffix. Mixed invocation where one of the extra codes is removed (e.g. `args="10 dspm sail"`) rejects on first dropped code encountered: stderr error, exit 1, dspm is NOT executed (D-07 — no partial execution).
  ```

**손대지 않는 항목 (보존):** #1 (phase 인자 파싱), #3 (lens 출력 형식), #4 (lessons 파일 경로 + run suffix), #6 (LENS_CODES_ARRAY 순차 실행), #9 (analyze TRANSCRIPT 없을 때), #10 (analyze 5-column table), #11 (sg-rule auto-suggest once).

**D. success_criteria 항목 번호 유지:**
4개 항목을 본문 재작성하지만 **항목 번호와 위치는 변경하지 않는다** (#2/#5/#7/#8의 자리에 그대로). 항목 추가/삭제 없음 — surgical content rewrite만.

**E. 손대지 말 것:**
- Step 1, 3, 4, 6 모든 줄 (byte-identical)
- Step 5의 ssc/dspm/analyze sub-block 본문 + DSPM explicit guard
- `<objective>` 블록
- `<language>` 블록
- frontmatter `name` 필드 + (Task 1에서 이미 수정된) `description`
  </action>
  <verify>
    <automated>
# A) Step 5 sub-block 삭제 검증 — 4ls/sail/5why 관련 본문 키워드 부재
# (case-statement는 Task 1에서 삭제됨. 여기서는 facilitation 본문의 고유 키워드 부재 확인.)
! grep -qE '^### Lens: 4Ls' skills/sg-retro/SKILL.md && \
! grep -qE '^### Lens: Sailboat' skills/sg-retro/SKILL.md && \
! grep -qE '^### Lens: Five Whys' skills/sg-retro/SKILL.md && \
! grep -qE '(Liked|Learned|Lacked|Longed For)' skills/sg-retro/SKILL.md && \
! grep -qE '(Wind|Anchor|Sun|Rock).*sail' skills/sg-retro/SKILL.md && \
! grep -qE 'Problem Statement.*Why 1' skills/sg-retro/SKILL.md && \

# B) Step 5 ssc/dspm/analyze sub-block 보존
grep -qE 'Start/Stop/Continue' skills/sg-retro/SKILL.md && \
grep -qE 'Decisions/Surprises/Patterns/Mistakes' skills/sg-retro/SKILL.md && \
grep -qE 'Conversation Analyzer' skills/sg-retro/SKILL.md && \
grep -q 'ANALYZE_LENS_RAN=true' skills/sg-retro/SKILL.md && \
grep -q 'Explicit guard' skills/sg-retro/SKILL.md && \

# C) LENS_NAME 매핑 — 3-케이스만 남음
grep -c '^\s*ssc)\s*LENS_NAME="Start/Stop/Continue"' skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \
grep -c '^\s*dspm)\s*LENS_NAME="Decisions/Surprises/Patterns/Mistakes"' skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \
grep -c '^\s*analyze)\s*LENS_NAME="Conversation Analyzer"' skills/sg-retro/SKILL.md | grep -qE '^[1-9]' && \
! grep -qE '^\s*4ls\)\s*LENS_NAME=' skills/sg-retro/SKILL.md && \
! grep -qE '^\s*sail\)\s*LENS_NAME=' skills/sg-retro/SKILL.md && \
! grep -qE '^\s*5why\)\s*LENS_NAME=' skills/sg-retro/SKILL.md && \

# D) success_criteria 재작성 — 신규 키워드 존재
grep -q 'no longer supported (removed in v2.9)' skills/sg-retro/SKILL.md && \
grep -q 'smart default applies' skills/sg-retro/SKILL.md && \
grep -q 'no partial execution' skills/sg-retro/SKILL.md && \

# E) success_criteria — 구식 multi-arg 예시 토큰 부재
! grep -qE 'args="10 4ls dspm"' skills/sg-retro/SKILL.md && \
! grep -qE 'ssc/4ls/dspm/sail/5why/analyze' skills/sg-retro/SKILL.md && \

# F) <success_criteria> 블록 자체는 보존 (블록 태그 1쌍 유지)
test "$(grep -c '<success_criteria>' skills/sg-retro/SKILL.md)" = "1" && \
test "$(grep -c '</success_criteria>' skills/sg-retro/SKILL.md)" = "1" && \

echo "Task 2 PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '^### Lens: 4Ls' skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -c '^### Lens: Sailboat' skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -c '^### Lens: Five Whys' skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -E '^\s*4ls\)\s*LENS_NAME=' skills/sg-retro/SKILL.md` returns no match
    - Source: `grep -E '^\s*sail\)\s*LENS_NAME=' skills/sg-retro/SKILL.md` returns no match
    - Source: `grep -E '^\s*5why\)\s*LENS_NAME=' skills/sg-retro/SKILL.md` returns no match
    - Source: `grep -c 'Start/Stop/Continue' skills/sg-retro/SKILL.md` returns ≥ 2 (sub-block 헤더 + LENS_NAME case)
    - Source: `grep -c 'Decisions/Surprises/Patterns/Mistakes' skills/sg-retro/SKILL.md` returns ≥ 2
    - Source: `grep -c 'Conversation Analyzer' skills/sg-retro/SKILL.md` returns ≥ 2
    - Source: `grep -c 'ANALYZE_LENS_RAN=true' skills/sg-retro/SKILL.md` returns `1`
    - Source: `grep -c 'Explicit guard' skills/sg-retro/SKILL.md` returns ≥ 1
    - Source: `grep -c 'args="10 4ls dspm"' skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -c 'ssc/4ls/dspm/sail/5why/analyze' skills/sg-retro/SKILL.md` returns `0`
    - Source: `grep -c '<success_criteria>' skills/sg-retro/SKILL.md` returns `1` AND `grep -c '</success_criteria>' skills/sg-retro/SKILL.md` returns `1`
    - Test command: `wc -l skills/sg-retro/SKILL.md` returns a count strictly less than 420 (변경 전 라인 수보다 감소 — sub-block 3개 삭제 효과)
  </acceptance_criteria>
  <done>
Step 5의 4ls/sail/5why sub-block 본문이 완전히 삭제되었고, LENS_NAME 매핑이 3-케이스로 축소되었다. `<success_criteria>` 항목 #2/#5/#7/#8이 D-02/D-05/D-06/D-07 동작을 반영하도록 재작성되었고, 보존 항목(#1/#3/#4/#6/#9/#10/#11)은 byte-identical로 유지되었다. Step 1 STATE.md parsing 블록 및 Step 5의 ssc/dspm/analyze sub-block은 손대지 않았다.
  </done>
</task>

<task type="auto">
  <name>Task 3: D-12 검증 시나리오 실행 + pairwise diff 정합 사전 점검</name>
  <files></files>
  <read_first>
    - skills/sg-retro/SKILL.md (Task 1+2 변경 적용 후 상태)
    - .planning/phases/42-smart-default-lens/42-CONTEXT.md (D-12 검증 시나리오 6개)
  </read_first>
  <action>
Task 1+2 변경이 D-12의 6개 시나리오를 모두 충족하는지 정적/동적 검증한다. 본 plan은 `skills/sg-retro/SKILL.md`만 수정하므로 시나리오 6(pairwise diff)은 사전 점검만 수행하고 최종 검증은 plan 42-02 완료 후 별도로 실행한다.

**시나리오 1 — Smart default 자동 진입 (D-02):**
`Skill(skill="sg-retro", args="42")` 호출을 모의 실행한다 (또는 SKILL.md의 bash 블록을 dry-run 모드로 sourcing).
- 기대: AskUserQuestion 호출 없음. `LENS_CODES_ARRAY="dspm ssc"`로 세팅되고 Step 3 이후로 진행.
- 검증: `.planning/lessons/42-{YYYY-MM-DD}.md` 파일이 생성되거나 append되며, 파일에 `## Lens: Decisions/Surprises/Patterns/Mistakes`가 `## Lens: Start/Stop/Continue`보다 먼저 등장한다.
- 동적 검증이 불가능한 환경(테스트 lessons 파일 생성 회피)에서는 grep 검증으로 대체:
  ```bash
  grep -A 2 'D-02.*smart default' skills/sg-retro/SKILL.md | grep -q 'LENS_CODES_ARRAY="dspm ssc"'
  ```

**시나리오 2 — 단일 lens 인자 (보존 동작):**
`Skill(skill="sg-retro", args="42 ssc")` → ssc 단독 실행.
- 검증 (grep): `case` 블록 내 `ssc) LENS_CODE="ssc"` 매핑이 유지되어 있고, Step 5의 ssc sub-block 헤더가 존재한다.

**시나리오 3 — Dropped lens 단독 인자 (D-06):**
`Skill(skill="sg-retro", args="42 sail")` → stderr 에러 + exit 1, lessons 파일 미생성.
- 검증 (grep + 동작): SKILL.md에 `4ls|sail|5why)` 분기 또는 `sail)` 분기가 있고 그 분기 내부에 `exit 1`이 포함된다.
  ```bash
  grep -A 3 -E '(4ls\|sail\|5why|sail\))' skills/sg-retro/SKILL.md | grep -q 'exit 1'
  ```
- 가능하면 실제로 dropped 인자로 한 번 실행해 stderr 출력과 exit code를 확인한다. 단 lessons 파일이 생성되지 않는지 사전·사후 `ls .planning/lessons/ | wc -l` 결과를 비교.

**시나리오 4 — Multi-lens 인자 부분 dropped (D-07):**
`Skill(skill="sg-retro", args="42 dspm sail")` → 첫 dropped(`sail`) 발견 즉시 거부, dspm도 실행되지 않음.
- 검증: VALID_EXTRAS inner case에 dropped 분기가 추가되어 `exit 1` 직전에 stderr emit한다. dspm 실행이 차단되도록 dropped 분기가 VALID_EXTRAS 구성 **이전** 또는 **즉시 종료** 위치에 있음을 정적 확인.
  ```bash
  grep -B 1 -A 4 'EC_LC' skills/sg-retro/SKILL.md | grep -E '(4ls|sail|5why)' | grep -q 'exit 1' || \
  grep -A 6 -E 'ssc\|dspm\|analyze\) VALID_EXTRAS' skills/sg-retro/SKILL.md | grep -q 'exit 1'
  ```

**시나리오 5 — sg-learn pass-through (D-11):**
`Skill(skill="sg-learn", args="42")` → sg-retro forward → smart default 적용.
- 검증: `skills/sg-learn/SKILL.md`가 변경되지 않았는지 `git diff --stat skills/sg-learn/SKILL.md` 확인 (변경 없음 = 0 lines).
  ```bash
  git diff --stat HEAD -- skills/sg-learn/SKILL.md | wc -l
  ```
  결과가 0이면 통과.

**시나리오 6 — Pairwise diff (D-10) — 사전 점검만:**
`.agents/skills/sg-retro/SKILL.md`는 plan 42-02의 책임이다. 본 plan에서는 의미적 변경 목록(smart default 분기 / 3-lens 매핑 / dropped reject / sub-block 삭제 / LENS_NAME 축소 / success_criteria 재작성)을 두 파일이 동일하게 적용했는지를 plan 42-02 완료 후 별도로 확인한다.
- 본 task에서는 `skills/sg-retro/SKILL.md` 측의 변경이 D-12 시나리오 1-5를 모두 충족하는지에 집중한다.

**Out of scope (이 plan에서 건드리지 않음):**
- `.agents/skills/sg-retro/SKILL.md` 수정 (plan 42-02 담당)
- `skills/sg-learn/SKILL.md` / `.agents/skills/sg-learn/SKILL.md` 수정 (D-11)
- README/TEAM.md (Phase 44 담당)
- DISPLAY-01/02 (Phase 43 담당)
- `--pick` flag (Phase 43 담당)
  </action>
  <verify>
    <automated>
# 시나리오 1: smart default 분기 정적 검증
grep -A 3 'D-02.*smart default' skills/sg-retro/SKILL.md | grep -q 'LENS_CODES_ARRAY="dspm ssc"' && \

# 시나리오 2: ssc 단독 매핑 보존
grep -qE '^\s*ssc\)\s*LENS_CODE="ssc"' skills/sg-retro/SKILL.md && \

# 시나리오 3: dropped 단독 reject — 분기 + exit 1
grep -A 4 -E '4ls\|sail\|5why\)' skills/sg-retro/SKILL.md | grep -q 'exit 1' && \

# 시나리오 4: VALID_EXTRAS multi-arg dropped reject
grep -A 6 -E 'ssc\|dspm\|analyze\) VALID_EXTRAS' skills/sg-retro/SKILL.md | grep -q 'exit 1' && \

# 시나리오 5: sg-learn 미변경
test "$(git diff --stat HEAD -- skills/sg-learn/SKILL.md 2>/dev/null | wc -l)" = "0" && \

# 추가: 변경 전 line 수 대비 감소 확인 (sub-block 3개 + AskUserQuestion 블록 삭제 효과)
test "$(wc -l < skills/sg-retro/SKILL.md)" -lt 420 && \

echo "Task 3 PASS — D-12 scenarios 1-5 verified statically"
    </automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -A 3 'D-02.*smart default' skills/sg-retro/SKILL.md` output contains `LENS_CODES_ARRAY="dspm ssc"`
    - Source: `grep -E '^\s*ssc\)\s*LENS_CODE="ssc"' skills/sg-retro/SKILL.md` returns exit code 0
    - Source: `grep -A 4 -E '4ls\|sail\|5why\)' skills/sg-retro/SKILL.md` output contains `exit 1`
    - Source: `grep -A 6 -E 'ssc\|dspm\|analyze\) VALID_EXTRAS' skills/sg-retro/SKILL.md` output contains `exit 1` (multi-arg dropped reject)
    - CLI output: `git diff --stat HEAD -- skills/sg-learn/SKILL.md | wc -l` returns `0`
    - CLI output: `wc -l < skills/sg-retro/SKILL.md` returns a value strictly less than `420`
    - Behavior assertion: D-12 시나리오 6 (pairwise diff) 사전 점검 — `skills/sg-retro/SKILL.md` 측에서 변경한 의미 단위 목록(smart default / 3-lens 매핑 / dropped reject / sub-block 삭제 / LENS_NAME 축소 / success_criteria 재작성)이 모두 적용되어 있음을 확인. 최종 정합은 plan 42-02 완료 시 검증.
  </acceptance_criteria>
  <done>
D-12 검증 시나리오 1-5가 정적·동적으로 통과한다. `skills/sg-learn/SKILL.md`는 변경되지 않았다(D-11 thin pass-through 보존). 파일 라인 수는 420보다 작게 감소했다. 시나리오 6의 최종 검증은 plan 42-02 완료 후 별도로 실행할 준비가 되어 있다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 인자 → Bash 변수 | `$LENS_RAW` / `$EXTRA_LENS_CODES`는 Skill 호출 시 인자에서 유래. 내부 신뢰 영역 (Claude Code 런타임이 호출). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | (N/A) | skills/sg-retro/SKILL.md text edits | accept | Threat model: N/A for SKILL.md text edits. No new external input surfaces introduced. Phase modifies only local skill markdown files consumed by Claude Code runtime. No auth/credential/PII surfaces. |
</threat_model>

<verification_steps>
## D-12 검증 시나리오 (Task 3에서 실행)

1. **Smart default 자동 진입** — `Skill(skill="sg-retro", args="42")`: AskUserQuestion 없음, lessons 파일에 dspm → ssc 순서로 lens 헤더 append.
2. **단일 lens 인자** — `Skill(skill="sg-retro", args="42 ssc")`: ssc 단독 실행, lessons 파일에 ssc 헤더만 append.
3. **Dropped lens 단독 인자** — `Skill(skill="sg-retro", args="42 sail")`: stderr에 `Lens 'sail' is no longer supported (removed in v2.9).` 메시지, exit 1, lessons 파일 미생성.
4. **Multi-lens 인자 부분 dropped** — `Skill(skill="sg-retro", args="42 dspm sail")`: 첫 dropped(`sail`) 발견 즉시 거부, dspm도 실행되지 않음, lessons 파일 미생성.
5. **sg-learn pass-through** — `Skill(skill="sg-learn", args="42")`: sg-retro로 forward되어 smart default 적용. `skills/sg-learn/SKILL.md` 자체는 본 plan에서 미변경.
6. **Pairwise diff (cross-plan)** — `skills/sg-retro/SKILL.md`와 `.agents/skills/sg-retro/SKILL.md`의 의미적 동등성 검증은 plan 42-02 완료 후 별도 수행 (헤더·`<constraints>`·`<lens_templates>`·success_criteria 항목 수 차이는 허용).
</verification_steps>

<success_criteria>
- `skills/sg-retro/SKILL.md`에서 lens 코드 `4ls`/`sail`/`5why`가 case-statement, LENS_NAME 매핑, sub-block, frontmatter description, success_criteria 본문에서 모두 제거되었다.
- Smart default 분기(`LENS_CODES_ARRAY="dspm ssc"`)가 D-02 조건(`LENS_RAW` empty AND `EXTRA_LENS_CODES` empty) 하에 동작하도록 신규 삽입되었다.
- AskUserQuestion 6-옵션 multiSelect 블록과 직후 parsing 블록이 완전히 삭제되었다 (D-08).
- Dropped lens 인자(단독 또는 멀티)에 대해 stderr 에러 메시지 + exit 1 동작이 case-statement와 VALID_EXTRAS inner case 양쪽에 일관되게 구현되었다 (D-06, D-07).
- Step 1 STATE.md parsing 블록(macOS 호환 grep+sed+awk)이 byte-identical로 보존되었다.
- Step 5의 ssc/dspm/analyze sub-block 본문(DSPM explicit guard + `ANALYZE_LENS_RAN=true` flag 포함)이 byte-identical로 보존되었다.
- `<success_criteria>` 블록의 항목 #1/#3/#4/#6/#9/#10/#11이 byte-identical로 보존되었고, #2/#5/#7/#8이 새 동작(smart default + 3-lens + dropped reject)을 정확히 반영하도록 재작성되었다.
- `skills/sg-learn/SKILL.md`는 변경되지 않았다 (D-11 thin pass-through 보존).
</success_criteria>

<output>
Create `.planning/phases/42-smart-default-lens/42-01-SUMMARY.md` when done.

**중요 (D-10 staging 제약):** 본 plan(42-01)과 plan 42-02는 동일 commit에 staged 되어야 한다. 단독 commit 금지. plan 42-02가 완료될 때까지 commit 보류하거나, 두 plan을 별도 커밋으로 만들지 않고 마지막에 합쳐 staging한다. 실행자는 자신의 task가 끝나도 `git commit`을 단독으로 호출하지 말고, phase 전체가 완료된 후 commit하도록 한다. (참고: GSD execute-plan은 phase 단위 commit을 사용하므로 자연스럽게 충족됨.)
</output>
