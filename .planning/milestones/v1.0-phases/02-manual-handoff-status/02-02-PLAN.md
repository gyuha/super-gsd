---
phase: 02-manual-handoff-status
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - commands/to-superpowers.md
  - commands/status.md
autonomous: true
requirements:
  - HAND-01
  - HAND-02
  - HAND-03
  - HAND-04
  - STATE-01

must_haves:
  decisions:
    - D-01   # commands/ 디렉토리 이번 phase에서 처음 생성, 다른 subdir 미생성
    - D-14   # 비침투적, GSD/Superpowers/Hookify 파일 미수정
    - D-16   # commands/{name}.md flat 배치, plugin.json name 이 namespace prefix
    - D-17   # frontmatter = name + description + argument-hint, allowed-tools 미명시
    - D-18   # 본문 = <objective>/<execution_context>/<process>/<success_criteria> XML
    - D-19   # 하이브리드: 구조화 프롬프트 표시 + Skill 자동 invoke
    - D-20   # Skill = superpowers:executing-plans 고정
    - D-21   # 프롬프트 표준 구성: phase meta + goal + SC + REQ-ID + PLAN 본문 인라인
    - D-24   # 멱등성 키 = (Phase, To), Plan Hash 변경 시 신규 row 허용
    - D-27   # status stage 판정 = HANDOFF.md 마지막 row To 컬럼, 빈 table → init
    - D-28   # stage → 다음 권장 명령 매핑 5종
    - D-29   # status 출력 = 3줄 + 빈 줄 + 1줄 next command
    - D-30   # commands/*.md 본문 사용자 노출 문자열은 영문 유지
  truths:
    - "사용자가 `/super-gsd:to-superpowers 2` 를 실행하면 (a) Phase 2 의 meta·goal·success criteria·REQ-ID·모든 PLAN.md 본문이 인라인된 단일 markdown 프롬프트가 출력되고, (b) `superpowers:executing-plans` Skill 이 그 프롬프트를 입력으로 자동 호출되며, (c) HANDOFF.md 에 신규 row 가 append 된다"
    - "사용자가 phase 인자 없이 `/super-gsd:to-superpowers` 를 실행하면 STATE.md 의 현재 phase 가 자동 추출되어 동일 동작이 일어난다"
    - "동일 phase 의 PLAN.md 가 변경되지 않은 상태에서 `/super-gsd:to-superpowers 2` 를 두 번 실행하면 두 번째 실행은 HANDOFF.md 에 row 를 추가하지 않고 `Already handed off ...` 메시지를 출력한다"
    - "동일 phase 의 PLAN.md 가 수정된 뒤 재실행하면 Plan Hash 가 달라져 신규 row 가 append 된다"
    - "사용자가 `/super-gsd:status` 를 실행하면 정확히 3줄(Phase / Stage / Last handoff) + 빈 줄 + 1줄(Next: ...) 이 출력되고, HANDOFF.md 가 비어 있을 때 Stage = `init` 으로 판정된다"
  artifacts:
    - path: "commands/to-superpowers.md"
      provides: "/super-gsd:to-superpowers 슬래시 명령 본문"
      exports: ["frontmatter: name/description/argument-hint", "XML sections: objective/execution_context/process/success_criteria"]
    - path: "commands/status.md"
      provides: "/super-gsd:status 슬래시 명령 본문"
      exports: ["frontmatter: name/description", "XML sections: objective/execution_context/process/success_criteria"]
  key_links:
    - from: "commands/to-superpowers.md"
      to: ".planning/HANDOFF.md"
      via: "append-only row write — 5열 스키마 (Plan 02-01 록인) 사용"
      pattern: "Timestamp.*Phase.*From.*To.*Plan Hash"
    - from: "commands/to-superpowers.md"
      to: "superpowers:executing-plans Skill"
      via: "Skill tool invoke — 구조화 프롬프트를 args 로 전달"
      pattern: "superpowers:executing-plans"
    - from: "commands/status.md"
      to: ".planning/HANDOFF.md"
      via: "마지막 데이터 행의 To 컬럼을 grep/awk 로 추출해 stage 판정"
      pattern: "tail.*HANDOFF.md"
    - from: "commands/{to-superpowers,status}.md"
      to: ".planning/STATE.md"
      via: "현재 phase 번호 자동 추출 (HAND-02 / status header)"
      pattern: "Phase: [0-9]"
---

<objective>
Phase 2 의 두 슬래시 명령을 디스크에 작성한다.

1. `commands/to-superpowers.md` — `/super-gsd:to-superpowers [phase]` 본문. (a) 현재 phase 를 해석하고, (b) 구조화 프롬프트를 빌드하며, (c) `(Phase, To=superpowers)` 멱등성 검사 후 HANDOFF.md 에 append 하고, (d) 사용자에게 프롬프트를 표시하면서 동시에 `superpowers:executing-plans` Skill 을 자동 호출한다.
2. `commands/status.md` — `/super-gsd:status` 본문. HANDOFF.md 마지막 row 의 To 컬럼을 stage 로 판정하고, STATE.md 에서 phase meta 를 가져와 정확히 4줄(3 + 빈 줄 + Next) 형식으로 출력한다.

목적: Phase 2 의 사용자 가치 (수동 핸드오프 + 상태 조회) 가 단일 명령 호출로 끝나도록 만든다. Plan 02-01 이 록인한 HANDOFF.md 스키마를 두 명령이 정확히 같은 방식으로 읽고 쓰도록 보장한다.

산출물: 두 markdown 파일. plugin.json `name: "super-gsd"` 가 자동으로 namespace prefix 가 되어 (D-16) Claude Code 가 이들을 `/super-gsd:to-superpowers` 와 `/super-gsd:status` 로 자동 노출한다 — 매니페스트 추가 등록 불필요.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-manual-handoff-status/02-CONTEXT.md
@.planning/phases/02-manual-handoff-status/02-01-SUMMARY.md
@.planning/HANDOFF.md
@.claude-plugin/plugin.json

<interfaces>
<!-- Plan 02-01 이 록인한 인터페이스. 이 plan 의 명령들은 이것에 직접 의존한다. -->

HANDOFF.md 5열 스키마 (Plan 02-01 산출):
- 헤더 행: `| Timestamp | Phase | From | To | Plan Hash |`
- 구분 행: `| --------- | ----- | ---- | -- | --------- |`
- Stage enum: `init` | `gsd-plan` | `superpowers` | `review` | `hookify`
- Timestamp: ISO 8601 UTC (`2026-05-15T11:23:45Z`)

데이터 행 append 형식 예 (이 plan 의 to-superpowers 명령이 작성):
`| 2026-05-15T11:23:45Z | 2 | gsd-plan | superpowers | a1b2c3d |`

Plan Hash 계산 (D-23):
- 입력: `.planning/phases/<phase-dir>/*-PLAN.md` 모두 cat 으로 연결
- 알고리즘: sha256 후 앞 7자
- 빈 phase 디렉토리: sentinel `nodata`
- Bash 권장: `cat .planning/phases/<phase-dir>/*-PLAN.md | shasum -a 256 | cut -c1-7` (macOS), `sha256sum` (linux). 양쪽 다 가능하도록 `shasum -a 256 2>/dev/null || sha256sum` fallback.

Stage → 다음 권장 명령 매핑 (D-28):
| Stage | Next command |
|-------|--------------|
| `init` | `/gsd:plan-phase {current_phase}` |
| `gsd-plan` | `/super-gsd:to-superpowers` |
| `superpowers` | `/hookify` |
| `review` | `/hookify` |
| `hookify` | `/gsd:discuss-phase {next_phase}` (다음 phase 가 ROADMAP 에 없으면 `/gsd:complete-milestone`) |

명령 frontmatter 표준 (D-17):
```yaml
---
name: <command-name>
description: <one-liner for /help>
argument-hint: <only if command accepts arguments>
---
```
`allowed-tools` 키 미명시 — 두 명령 모두 Read / Write / Bash / Skill 호출 필요.

명령 본문 XML 구조 (D-18):
```
<objective>...</objective>
<execution_context>...</execution_context>
<process>...</process>
<success_criteria>...</success_criteria>
```

Skill 호출 시그니처 (D-19, D-20):
- 도구 이름: `Skill`
- 인자: `skill="superpowers:executing-plans"`, `args="<프롬프트 markdown blob>"`
- 호출 위치: `<process>` 의 최종 단계, 프롬프트 표시 직후
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: commands/to-superpowers.md 슬래시 명령 작성</name>
  <files>commands/to-superpowers.md</files>
  <read_first>
    - .planning/phases/02-manual-handoff-status/02-CONTEXT.md (D-16..D-30 전체)
    - .planning/HANDOFF.md (Plan 02-01 산출 — 헤더 행 정확 매칭 후 schema validation 수행)
    - .claude-plugin/plugin.json (name=super-gsd 확인 — namespace prefix 가 자동으로 `/super-gsd:` 가 되는지 mental model 확립; 파일은 수정하지 않음)
    - .planning/STATE.md (HAND-02 자동 추출 데이터 원천)
    - .planning/ROADMAP.md (Phase goal·SC·다음 phase 계산용)
    - .planning/REQUIREMENTS.md (REQ-ID 1줄 설명 grep 원천)
  </read_first>
  <action>
    `commands/to-superpowers.md` 를 신규 작성한다. `commands/` 디렉토리는 이번 phase 에서 처음 생성된다 (D-01) — `mkdir -p commands` 가 선행되도록 명령 본문이 아니라 executor 가 처리.

    **본문의 사용자 노출 문자열은 전부 영문 (D-30, OSS surface).** 다만 본 plan 의 한국어 지시에 의한 "이런 의도로 영문 문장을 작성한다" 와 같은 메타 설명은 그대로 한글로 남는다.

    파일은 다음 두 부분으로 구성된다.

    ### Part A: YAML frontmatter (D-17)
    정확히 3개 키:
    - `name: to-superpowers`
    - `description: Hand off the current GSD phase to Superpowers — package PLAN/REQ/SC into a single prompt and auto-invoke superpowers:executing-plans.`
    - `argument-hint: "[phase] - optional. Defaults to STATE.md current phase"`
    `allowed-tools` 는 절대 추가하지 않는다 (D-17 명시).

    ### Part B: 본문 XML 4개 섹션 (D-18)

    #### `<objective>`
    영문 2~4문장. 명령의 목적과 결과물을 사용자가 이해할 수 있게 서술. 다음 사실을 모두 포함:
    - "Package the current phase's PLAN.md, REQUIREMENTS.md mapping, and ROADMAP success criteria into a single Superpowers-ready prompt."
    - "Auto-invoke `superpowers:executing-plans` with that prompt."
    - "Append a timestamped row to `.planning/HANDOFF.md` (skipped if `(phase, to=superpowers)` was already handed off and the plan hash matches)."

    #### `<execution_context>`
    빈 섹션 또는 한 줄 메모만. CONTEXT D-18 후반부 "외부 워크플로 파일 참조는 두지 않음" 지시에 따라 GSD `@$HOME/.claude/...` 같은 외부 참조 금지. 정확히 다음 한 줄만 작성 (영문):
    `This command is self-contained — no external workflow files imported. Reads .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/<phase>/*-PLAN.md, .planning/HANDOFF.md.`

    #### `<process>`
    명령이 수행하는 단계를 영문 번호 목록으로 작성. 정확히 다음 9단계 시퀀스를 사용 (각 단계의 영문 어휘는 executor 재량이되 시맨틱은 정확히 일치).

    **Step 1 — Resolve phase (HAND-02):**
    - `$ARGUMENTS` (Claude Code 슬래시 명령 인자 변수) 가 비어 있지 않으면 그대로 phase 식별자.
    - 비어 있으면 `.planning/STATE.md` 본문에서 `Phase: <N>` 패턴을 grep / awk 로 추출 (frontmatter `progress.current_phase` 가 없으므로 본문 `## Current Position` 라인 사용). 예: `grep -E '^Phase: [0-9]+' .planning/STATE.md | head -1 | awk '{print $2}'`.
    - 추출 실패 시 에러 메시지 (영문, OSS surface): `Could not resolve current phase. Pass phase number explicitly: /super-gsd:to-superpowers <phase>` 후 명령 종료.

    **Step 2 — Locate phase directory:**
    - `.planning/phases/` 아래에서 `<phase>-*` glob 매칭. 예: phase=`2` → `ls -d .planning/phases/02-*` (zero-pad 2자리). 정확한 매칭 알고리즘은 executor 재량이되 phase 번호가 1자리·2자리·소수(2.1) 모두 처리.
    - 매칭 0개면 에러 (영문): `No phase directory matches '<phase>' under .planning/phases/. Run /gsd:discuss-phase first.` 후 종료.

    **Step 3 — Extract phase meta (D-21):**
    - ROADMAP.md 에서 `### Phase <N>: <Name>` 헤더와 그 아래의 `**Goal**:` 1줄을 grep.
    - `**Success Criteria**` 블록 아래 번호 목록을 추출.
    - `**Requirements**:` 1줄에서 REQ-ID 리스트 추출.

    **Step 4 — Map REQ-IDs to 1-line definitions (HAND-03, D-21):**
    - 각 REQ-ID 에 대해 `.planning/REQUIREMENTS.md` 에서 `**<REQ-ID>**:` 시작 줄을 grep 으로 추출.

    **Step 5 — Collect PLAN.md bodies (D-21):**
    - 해당 phase 디렉토리의 `*-PLAN.md` 모두 (수치 정렬) 를 변수에 적재. 각 파일을 `## <plan filename>` 헤더 + fenced ` ```markdown` 블록으로 감싼다.

    **Step 6 — Compute Plan Hash (D-23):**
    - 모든 `*-PLAN.md` 본문을 cat 한 뒤 sha256 의 앞 7자 추출.
    - Bash 의사 코드: `PLAN_HASH=$(cat <phase-dir>/*-PLAN.md 2>/dev/null | { shasum -a 256 2>/dev/null || sha256sum; } | cut -c1-7)`. 빈 출력이면 `PLAN_HASH=nodata`.

    **Step 7 — Idempotency check (HAND-04, D-24):**
    - `.planning/HANDOFF.md` 에서 마지막 행 중 `Phase = <N>` AND `To = superpowers` 인 row 가 있는지 검사.
    - 의사 코드:
      ```
      EXISTING_HASH=$(grep -E "^\| [^|]+ \| <N> \| [^|]+ \| superpowers \|" .planning/HANDOFF.md | tail -1 | awk -F'|' '{gsub(/ /,"",$6); print $6}')
      if [ -n "$EXISTING_HASH" ] && [ "$EXISTING_HASH" = "$PLAN_HASH" ]; then
        echo "Already handed off Phase <N> to superpowers (plan hash matches: $PLAN_HASH). Skipping append. Use /super-gsd:status to inspect, or modify a PLAN.md to re-handoff."
        # Do NOT append; do NOT invoke Skill; exit.
        exit 0
      fi
      ```
    - PLAN.md 가 변경되어 `$PLAN_HASH` ≠ `$EXISTING_HASH` 이면 신규 row append 허용 (D-24 후단).

    **Step 8 — Append HANDOFF.md row (HAND-04, D-22):**
    - `TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)`.
    - From 컬럼: 이전 row 의 To 컬럼 (없으면 `init`; HANDOFF.md 가 비어 있으면 `init`). awk/tail 로 결정.
    - 새 row 정확 형식: `| <TS> | <N> | <FROM> | superpowers | <PLAN_HASH> |`
    - `>> .planning/HANDOFF.md` 로 append. 헤더/구분 행이 손상되지 않게 반드시 append 모드.
    - 동시 쓰기 race 완화: append 전 헤더 행이 `grep -Fxq "| Timestamp | Phase | From | To | Plan Hash |"` 으로 정확히 존재하는지 검증; 실패 시 에러 (영문): `.planning/HANDOFF.md schema mismatch — header row not found. Aborting append.` 후 종료.

    **Step 9 — Build prompt + invoke Skill (HAND-01, HAND-03, D-19, D-20, D-21):**
    - 단일 markdown blob 변수 `PROMPT` 를 다음 순서로 빌드 (영문 라벨):
      ```
      # Superpowers Execution Handoff — Phase <N> (<Phase Name>)

      ## Goal
      <Goal line from ROADMAP>

      ## Success Criteria
      1. ...
      2. ...

      ## Requirements
      - <REQ-ID>: <one-line definition>
      ...

      ## Plans

      ### <plan-filename-1>
      ```markdown
      <full PLAN.md body>
      ```

      ### <plan-filename-2>
      ```markdown
      <full PLAN.md body>
      ```

      ## Instruction to Superpowers
      Execute the plans above using the superpowers:executing-plans skill. Treat each PLAN.md as the authoritative source of tasks and acceptance criteria.
      ```
    - 사용자에게 `$PROMPT` 를 표시 (echo / 직접 출력).
    - 이어서 정확한 Skill 호출 시그니처로 자동 invoke:
      `Skill(skill="superpowers:executing-plans", args="<$PROMPT 내용>")`
    - 두 동작 사이에 사용자 confirmation 요구 금지 (D-19 "사용자 입력 없이 한 번에").

    **Step 10 (post-Skill) — Final user message (영문):**
    - `Handoff complete. HANDOFF.md updated; superpowers:executing-plans invoked. Use /super-gsd:status to inspect workflow state.`

    #### `<success_criteria>`
    명령 자체의 성공 기준을 영문으로 4개 불릿:
    1. The prompt blob displayed to the user contains Phase number, Goal, Success Criteria, all REQ-IDs with definitions, and full PLAN.md bodies.
    2. `superpowers:executing-plans` Skill is invoked exactly once per run (or zero times when idempotency check skips).
    3. `.planning/HANDOFF.md` gains at most one new row per run, matching the 5-column schema.
    4. Re-running with an unchanged plan hash produces the `Already handed off ...` message and does not append.

    ### 파일 끝
    Trailing newline 1개.
  </action>
  <verify>
    <automated>
      test -f commands/to-superpowers.md && \
      grep -Pzoq '(?s)^---\n.*name:\s*to-superpowers.*description:.*argument-hint:.*\n---\n' commands/to-superpowers.md && \
      ! grep -E '^allowed-tools:' commands/to-superpowers.md && \
      grep -Fq '<objective>' commands/to-superpowers.md && grep -Fq '</objective>' commands/to-superpowers.md && \
      grep -Fq '<execution_context>' commands/to-superpowers.md && grep -Fq '</execution_context>' commands/to-superpowers.md && \
      grep -Fq '<process>' commands/to-superpowers.md && grep -Fq '</process>' commands/to-superpowers.md && \
      grep -Fq '<success_criteria>' commands/to-superpowers.md && grep -Fq '</success_criteria>' commands/to-superpowers.md && \
      grep -Fq 'superpowers:executing-plans' commands/to-superpowers.md && \
      grep -Fq '.planning/HANDOFF.md' commands/to-superpowers.md && \
      grep -Fq '.planning/STATE.md' commands/to-superpowers.md && \
      grep -Fq 'Plan Hash' commands/to-superpowers.md && \
      grep -Fq 'Already handed off' commands/to-superpowers.md && \
      grep -Fq '$ARGUMENTS' commands/to-superpowers.md && \
      grep -Fq 'date -u' commands/to-superpowers.md && \
      python3 -c "import re, sys; sys.exit(1 if re.search(r'[가-힣]', open('commands/to-superpowers.md').read()) else 0)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `commands/to-superpowers.md` 파일 존재.
    - Frontmatter 에 `name: to-superpowers`, `description:`, `argument-hint:` 3개 키 모두 존재.
    - `allowed-tools:` 키 부재 (D-17).
    - 본문에 4개 XML 섹션 모두 open/close 태그 등장: `<objective>`, `<execution_context>`, `<process>`, `<success_criteria>`.
    - `superpowers:executing-plans` 문자열 등장 (D-20 Skill 고정).
    - `.planning/HANDOFF.md` 문자열 등장 (HAND-04).
    - `.planning/STATE.md` 문자열 등장 (HAND-02 자동 추출).
    - `Plan Hash` 문자열 등장 (D-23/D-24 멱등성 키 일부).
    - `Already handed off` 문자열 등장 (D-24 멱등성 메시지).
    - `$ARGUMENTS` 변수 사용 (Claude Code 슬래시 인자 컨벤션).
    - `date -u` 호출 등장 (D-26 ISO 8601 UTC).
  </acceptance_criteria>
  <done>
    `/super-gsd:to-superpowers 2` 가 실제 세션에서 (a) 4섹션 markdown 프롬프트를 출력하고, (b) Skill 을 자동 호출하며, (c) HANDOFF.md 에 5열 row 를 append 하고, (d) 재실행 시 plan hash 가 같으면 append 를 skip 한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: commands/status.md 슬래시 명령 작성</name>
  <files>commands/status.md</files>
  <read_first>
    - .planning/phases/02-manual-handoff-status/02-CONTEXT.md (D-25, D-27, D-28, D-29 중점)
    - .planning/HANDOFF.md (Plan 02-01 산출 — Stage enum / 마지막 행 추출 패턴 결정)
    - .planning/STATE.md (Phase 번호 + 이름)
    - .planning/ROADMAP.md (다음 phase 번호 계산 — hookify → /gsd:discuss-phase {next_phase})
  </read_first>
  <action>
    `commands/status.md` 를 신규 작성한다. Task 1 과 동일 컨벤션 (영문 사용자 surface, XML 4 섹션, frontmatter 키 제한).

    ### Part A: YAML frontmatter (D-17)
    정확히 2개 키 (인자 없는 명령이므로 `argument-hint` 생략):
    - `name: status`
    - `description: Show the current super-gsd workflow stage, last handoff timestamp, and the next recommended command.`
    `allowed-tools` 키 미사용.

    ### Part B: XML 4 섹션 (D-18)

    #### `<objective>`
    영문 1~2문장:
    `Inspect the current super-gsd workflow state. Read .planning/HANDOFF.md to determine the current stage, .planning/STATE.md for the current phase number and name, and .planning/ROADMAP.md for next-phase lookup. Output exactly three header lines, a blank line, and one "Next:" line.`

    #### `<execution_context>`
    한 줄 (영문):
    `Self-contained — reads .planning/HANDOFF.md, .planning/STATE.md, .planning/ROADMAP.md. Writes nothing.`

    #### `<process>`
    영문 번호 목록. 정확히 다음 6단계.

    **Step 1 — Read current phase from STATE.md:**
    - `grep -E '^Phase: [0-9]' .planning/STATE.md | head -1` → `PHASE_NUM`.
    - 같은 줄에서 phase 이름이 ROADMAP 에 있는지 lookup. ROADMAP 의 `### Phase <N>: <Name>` 라인을 grep 으로 phase 이름 추출. 예: `PHASE_NAME=$(grep -E "^### Phase $PHASE_NUM:" .planning/ROADMAP.md | head -1 | sed -E 's/^### Phase [^:]+: //' | sed -E 's/\s*$//')`.

    **Step 2 — Determine stage from HANDOFF.md (D-27):**
    - HANDOFF.md 가 없거나 데이터 행이 0개면 `STAGE=init`.
    - 그렇지 않으면 마지막 데이터 행의 To 컬럼을 추출:
      ```
      LAST_ROW=$(grep -E '^\| [0-9]{4}-' .planning/HANDOFF.md | tail -1)
      STAGE=$(echo "$LAST_ROW" | awk -F'|' '{gsub(/ /,"",$5); print $5}')
      TS=$(echo "$LAST_ROW" | awk -F'|' '{gsub(/ /,"",$2); print $2}')
      ```
    - STAGE 가 enum (`init|gsd-plan|superpowers|review|hookify`) 외 값이면 에러 (영문): `Unknown stage '<STAGE>' in .planning/HANDOFF.md last row. Schema may be corrupted.`

    **Step 3 — Compute last handoff timestamp:**
    - 빈 table → `LAST_TS="(none)"` (영문 OSS surface).
    - 데이터 행 존재 → Step 2 의 `TS`.

    **Step 4 — Compute next-phase number (D-28 hookify branch):**
    - 현재 STAGE = `hookify` 일 때만 필요.
    - `NEXT_PHASE=$((PHASE_NUM + 1))` (정수 phase 만 단순 +1; 소수 phase 는 ROADMAP grep 으로 fallback 처리, executor 재량).
    - ROADMAP.md 에 `### Phase $NEXT_PHASE:` 가 존재하지 않으면 NEXT_CMD = `/gsd:complete-milestone`, 존재하면 `/gsd:discuss-phase $NEXT_PHASE`.

    **Step 5 — Map stage → next command (D-28):**
    - Bash case 문 의사 코드:
      ```
      case "$STAGE" in
        init)        NEXT_CMD="/gsd:plan-phase $PHASE_NUM" ;;
        gsd-plan)    NEXT_CMD="/super-gsd:to-superpowers" ;;
        superpowers) NEXT_CMD="/hookify" ;;
        review)      NEXT_CMD="/hookify" ;;
        hookify)     # Step 4 의 결과 사용
                     ;;
      esac
      ```

    **Step 6 — Print output (D-29):**
    - 정확히 다음 4 라인 + trailing newline (모든 사용자 surface 영문):
      ```
      Phase: <PHASE_NUM> (<PHASE_NAME>)
      Stage: <STAGE>
      Last handoff: <LAST_TS>

      Next: <NEXT_CMD>
      ```
    - 세 헤더 라인 + 빈 줄 1개 + Next 라인. 추가 출력 금지 (D-29).

    #### `<success_criteria>`
    영문 불릿 3개:
    1. The output is exactly 4 non-empty lines separated by one blank line (3 header + blank + 1 next).
    2. When `.planning/HANDOFF.md` has only header rows (no data rows), Stage is `init` and Last handoff is `(none)`.
    3. The `Next:` command matches the D-28 mapping for the detected stage.

    ### 파일 끝
    Trailing newline 1개.
  </action>
  <verify>
    <automated>
      test -f commands/status.md && \
      grep -Pzoq '(?s)^---\n.*name:\s*status.*description:.*\n---\n' commands/status.md && \
      ! grep -E '^argument-hint:' commands/status.md && \
      ! grep -E '^allowed-tools:' commands/status.md && \
      grep -Fq '<objective>' commands/status.md && grep -Fq '</objective>' commands/status.md && \
      grep -Fq '<execution_context>' commands/status.md && grep -Fq '</execution_context>' commands/status.md && \
      grep -Fq '<process>' commands/status.md && grep -Fq '</process>' commands/status.md && \
      grep -Fq '<success_criteria>' commands/status.md && grep -Fq '</success_criteria>' commands/status.md && \
      grep -Fq '.planning/HANDOFF.md' commands/status.md && \
      grep -Fq '.planning/STATE.md' commands/status.md && \
      grep -Fq '.planning/ROADMAP.md' commands/status.md && \
      grep -Fq 'init' commands/status.md && grep -Fq 'gsd-plan' commands/status.md && \
      grep -Fq 'superpowers' commands/status.md && grep -Fq 'hookify' commands/status.md && \
      grep -Fq '/gsd:plan-phase' commands/status.md && \
      grep -Fq '/super-gsd:to-superpowers' commands/status.md && \
      grep -Fq '/hookify' commands/status.md && \
      grep -Fq '/gsd:discuss-phase' commands/status.md && \
      grep -Fq '/gsd:complete-milestone' commands/status.md && \
      grep -Fq 'Phase:' commands/status.md && grep -Fq 'Stage:' commands/status.md && \
      grep -Fq 'Last handoff:' commands/status.md && grep -Fq 'Next:' commands/status.md && \
      python3 -c "import re, sys; sys.exit(1 if re.search(r'[가-힣]', open('commands/status.md').read()) else 0)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `commands/status.md` 파일 존재.
    - Frontmatter `name: status` + `description:` 정확히 2개 키. `argument-hint` 없음 (인자 없는 명령). `allowed-tools` 없음.
    - 4개 XML 섹션 open/close 모두 등장.
    - 3개 입력 파일 경로 모두 명시: `.planning/HANDOFF.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`.
    - 5종 Stage enum 문자열 모두 등장 (`init`, `gsd-plan`, `superpowers`, `review`, `hookify`).
    - 5종 next-command 매핑 모두 등장: `/gsd:plan-phase`, `/super-gsd:to-superpowers`, `/hookify`, `/gsd:discuss-phase`, `/gsd:complete-milestone`.
    - 4개 출력 라벨 모두 등장: `Phase:`, `Stage:`, `Last handoff:`, `Next:`.
  </acceptance_criteria>
  <done>
    `/super-gsd:status` 가 실제 세션에서 정확히 4 라인 (헤더 3 + 빈 줄 + Next 1) 을 출력하고, HANDOFF.md 상태에 따라 D-28 매핑된 정확한 다음 명령을 표시한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 PLAN.md 본문 → 자동 invoke 되는 LLM 세션 (Skill) | `/super-gsd:to-superpowers` 가 PLAN.md 본문을 markdown blob 으로 인라인하여 `superpowers:executing-plans` Skill 에 args 로 전달. PLAN.md 가 직접 작성자만 수정 가능한 신뢰 영역이지만, 다른 phase / 외부 PR / 의도되지 않은 콘텐츠가 끼어든 경우 prompt injection 표면이 된다. |
| `/super-gsd:to-superpowers` 의 HANDOFF.md append → 다른 명령·hook 의 동시 쓰기 | Phase 3 이 동일 HANDOFF.md 를 Stop/SubagentStop hook 에서 자동 append 할 예정. 같은 session 내 race 는 드물지만 동일 phase 가 두 번 실행되는 경우 last-write-wins 으로 row 가 손실되거나 중복될 수 있다. |
| 슬래시 명령 출력 (영문) → OSS 사용자 surface | D-30 결정에 따라 commands/*.md 내 사용자 노출 문자열이 영문이다. 내부 `.planning/` 한글 본문이 누설되면 일관성 깨짐. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | PLAN.md 인라인 시 prompt injection (Skill args 안에 PLAN.md 본문이 들어감) | mitigate | PLAN.md 본문을 fenced ` ```markdown` 블록으로 감싸서 메타지시와 콘텐츠 경계를 명확화 (D-21 형식). 추가로 `## Instruction to Superpowers` 섹션을 PLAN 본문 뒤에 두어 후속 지시가 우선 적용되도록 순서 고정. PLAN.md 작성자는 신뢰된 사용자이므로 (저장소 권한 모델) 잔여 위험 acceptable. |
| T-02-05 | Tampering / DoS | HANDOFF.md 동시 append race — 동일 phase 두 번 invoke 시 row 중복 또는 손실 | mitigate | 멱등성 키 `(Phase, To, PLAN_HASH)` 검사를 append 전에 수행 (D-24). 헤더 행 정확 매칭으로 schema validation 추가 (Task 1 Step 8). 실제 동시 invoke 는 단일 Claude Code 세션 사용 시 매우 드물고, race window 가 ms 단위라 추가 file lock 미적용 (잔여 위험 accept). |
| T-02-06 | Information Disclosure | STATE.md 본문 phase 추출 시 의도치 않은 다른 데이터 라인 매칭 | mitigate | grep 패턴을 `^Phase: [0-9]+` 로 anchor + `head -1` 로 첫 매치만 사용. 다중 매치 시 첫 줄 우선 — STATE.md 컨벤션상 안전. |
| T-02-07 | Repudiation | HANDOFF.md row 의 timestamp 가 local clock 의존 | accept | `date -u` 사용으로 UTC 강제 (D-26). NTP 동기화는 OS 책임. 잔여 위험 OSS 컨텍스트에서 accept. |
| T-02-08 | Elevation of Privilege | Skill invoke 가 임의 코드 실행으로 이어짐 | accept | `superpowers:executing-plans` 는 user-installed 신뢰 plugin (D-14 비침투, prerequisite 명시). 호스트 Claude Code 권한 모델에 위임. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | 이 plan 은 markdown 파일 작성만 수행. 패키지 설치 없음. Legitimacy gate 비해당. |
</threat_model>

<verification>
## End-to-end 동작 검증 (수동 — `/gsd:execute-phase` 가 별도 wave 에서 수행 예정)

1. **HAND-01 / HAND-03:** Claude Code 세션에서 `/super-gsd:to-superpowers 2` 실행 → (a) 4섹션 markdown 프롬프트(Phase / Goal / Success Criteria / Requirements / Plans / Instruction) 출력, (b) `superpowers:executing-plans` Skill 자동 호출 흔적.
2. **HAND-02:** `/super-gsd:to-superpowers` (인자 없음) 실행 → STATE.md 의 현재 phase 가 자동으로 해석되고 위 (1) 과 동일 동작.
3. **HAND-04:** `.planning/HANDOFF.md` 를 직접 열어 1개 신규 row 가 정확히 5열 형식으로 append 되었는지 확인.
4. **Idempotency (HAND-04 + D-24):** PLAN.md 변경 없이 즉시 재실행 → `Already handed off ...` 메시지 출력, HANDOFF.md row 개수 변화 없음.
5. **STATE-01:** `/super-gsd:status` 실행 → 정확히 4 라인 (3 header + 빈 줄 + Next) 출력, Stage = `superpowers`, Next = `/hookify`.
6. **STATE-02:** `.planning/HANDOFF.md` 가 사람이 읽을 수 있는 markdown table 형식 유지.

## 자동 검증 (acceptance_criteria 의 grep 들)
- Task 1·2 의 모든 acceptance_criteria 가 통과한다.
- frontmatter 키 화이트리스트 검증 (frontmatter 안에 `allowed-tools` 가 등장하지 않음).
</verification>

<success_criteria>
- 사용자가 `/super-gsd:to-superpowers` 와 `/super-gsd:status` 를 호출했을 때 ROADMAP Phase 2 의 4가지 SC 모두 충족된다.
- 두 명령이 Plan 02-01 이 록인한 HANDOFF.md 5열 스키마를 read/write 양방향으로 정확히 사용한다.
- 모든 사용자 노출 문자열이 영문이고 `.planning/` 내부 한글 정책을 침범하지 않는다 (D-30).
- 비침투성 — GSD/Superpowers/Hookify 의 어떤 파일도 수정되지 않았다 (D-14).
- D-NN 결정 13개 (위 must_haves.decisions) 가 명령 본문에서 모두 시각적으로 추적 가능 (식별자·라벨·매핑이 본문에 등장).
</success_criteria>

<output>
완료 시 `.planning/phases/02-manual-handoff-status/02-02-SUMMARY.md` 를 생성한다 (execute-plan 컨벤션).
</output>
