---
phase: 32-skill-superpowers-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/sg-execute/SKILL.md
  - .agents/skills/sg-plan/SKILL.md
  - .agents/skills/sg-review/SKILL.md
  - .agents/skills/sg-ship/SKILL.md
  - skills/sg-retro/SKILL.md
  - CLAUDE.md
autonomous: true
requirements:
  - PARSE-02
  - PARSE-03
must_haves:
  truths:
    - ".agents/skills/sg-{execute,plan,review,ship}에서 STATE.md Phase 파싱이 Read 도구 + Claude 해석으로 교체된다"
    - ".agents/skills/sg-{execute,plan,review}에서 HANDOFF.md 마지막 행 파싱이 Read 도구 + Claude 해석으로 교체된다"
    - ".agents/skills/sg-review에서 PLAN.md objective 파싱이 Read 도구 + Claude 해석으로 교체된다"
    - "skills/sg-retro/SKILL.md의 D-04~D-06 lock 주석 두 곳이 macOS 호환성 주석으로 교체된다 (bash 코드 자체는 유지)"
    - "CLAUDE.md의 'Phase 7 D-04~D-06 lock' 참조가 Superpowers 방식 권장 설명으로 교체된다"
  artifacts:
    - path: ".agents/skills/sg-execute/SKILL.md"
      provides: "STATE.md + HANDOFF 파싱 → Read 도구"
    - path: ".agents/skills/sg-plan/SKILL.md"
      provides: "STATE.md + HANDOFF 파싱 → Read 도구"
    - path: ".agents/skills/sg-review/SKILL.md"
      provides: "STATE.md + PLAN.md objective + HANDOFF 파싱 → Read 도구"
    - path: ".agents/skills/sg-ship/SKILL.md"
      provides: "STATE.md 파싱 → Read 도구"
    - path: "skills/sg-retro/SKILL.md"
      provides: "D-04~D-06 lock 주석 제거 (코드 유지)"
    - path: "CLAUDE.md"
      provides: "Phase 7 D-04~D-06 lock 컨벤션 제거 + Superpowers 방식 권장"
  key_links:
    - from: ".agents/skills/sg-*/SKILL.md"
      to: ".planning/STATE.md"
      via: "Read 도구 직접 호출"
      pattern: "Read .planning/STATE.md"
    - from: "CLAUDE.md macOS 셸 이식성 섹션"
      to: "Superpowers 방식 권장 설명"
      via: "컨벤션 교체"
      pattern: "Read 도구로 .planning/STATE.md를 열어"
---

<objective>
`.agents/skills/sg-{execute,plan,review,ship}/SKILL.md` 4개 파일에서 bash 파이프라인 파일 파싱을 Read 도구로 전환하고(D-01 per PARSE-02), `skills/sg-retro/SKILL.md`의 D-04~D-06 lock 주석을 macOS 호환성 주석으로 교체하며(D-02 per PARSE-03), `CLAUDE.md`의 Phase 7 D-04~D-06 lock 컨벤션 항목을 Superpowers 방식 권장 설명으로 교체한다(D-03 per PARSE-03).

Purpose: skills/ 와 .agents/skills/ 간 drift를 방지하고(D-01), 락 주석을 제거해 더 이상 유효하지 않은 제약을 코드베이스에서 완전히 제거한다.
Output: .agents/skills/ 4개 파일이 skills/ 버전과 동일한 파싱 방식을 사용하고, CLAUDE.md가 현행 권장 방식을 정확히 반영한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/32-skill-superpowers-migration/32-CONTEXT.md
@.planning/phases/32-skill-superpowers-migration/32-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: .agents/skills/sg-execute + sg-plan + sg-ship — Pattern A+B 전환</name>
  <files>
    .agents/skills/sg-execute/SKILL.md
    .agents/skills/sg-plan/SKILL.md
    .agents/skills/sg-ship/SKILL.md
  </files>
  <read_first>
    - .agents/skills/sg-execute/SKILL.md — Step 1(Phase 파싱 multi-line sed), Step 9.5(HANDOFF FROM_STAGE awk) 위치 확인
    - .agents/skills/sg-plan/SKILL.md — Step 1(Phase 파싱 multi-line sed), Step 2b(PREV_STAGE awk) 위치 확인
    - .agents/skills/sg-ship/SKILL.md — Step 1(Phase 파싱 multi-line sed), Step 6(FROM_STAGE awk 확인) 위치 확인
    - .planning/phases/32-skill-superpowers-migration/32-PATTERNS.md — 패턴 A/B .agents 변형 Before/After 확인 (3줄 multi-line sed 패턴)
  </read_first>
  <steps>
    1. `.agents/skills/sg-execute/SKILL.md` Step 1: multi-line Phase 파싱 블록 교체:
       ```bash
       PHASE_NUM=$(grep -E '^Phase:' .planning/STATE.md 2>/dev/null | head -1 \
                   | sed -E 's/^Phase:[[:space:]]*//' \
                   | sed -E 's/[[:space:]]+$//' \
                   | awk '{print $1}')
       ```
       → `Read .planning/STATE.md, then extract the Phase: value from the YAML frontmatter. Set PHASE_NUM to the extracted value.`

    2. `.agents/skills/sg-execute/SKILL.md` Step 9.5 FROM_STAGE: HANDOFF awk 파싱 교체:
       `grep -E '^\| [0-9]{4}-' .planning/HANDOFF.md | tail -1 | awk -F'|' '{gsub(/ /,"",$5); print $5}'`
       → `Read .planning/HANDOFF.md, then extract the To column (5th pipe-delimited field) from the last row starting with "| " followed by a 4-digit year. Set FROM_STAGE (default "init" if empty).`
       주의: Step 7의 EXISTING_HASH grep 줄(`grep -E "^\| [^|]+ \|...execute..."`)은 변경하지 않는다.

    3. `.agents/skills/sg-plan/SKILL.md` Step 1: multi-line Phase 파싱 블록 교체:
       (3줄 multi-line sed 패턴 전체)
       → `Read .planning/STATE.md, then extract the Phase: value from the YAML frontmatter. Set PHASE_NUM to the extracted value.`

    4. `.agents/skills/sg-plan/SKILL.md` Step 2b PREV_STAGE: HANDOFF awk 파싱 교체:
       `grep -E '^\| [0-9]{4}-' "$HANDOFF_FILE" | tail -1 | awk -F'|' '{gsub(/ /,"",$5); print $5}'`
       → `Read .planning/HANDOFF.md, then extract the To column (5th pipe-delimited field) from the last row starting with "| " followed by a 4-digit year. Set PREV_STAGE to the extracted value (empty string if no data rows exist).`
       주의: HANDOFF 초기화 블록(`if [ ! -f "$HANDOFF_FILE" ]...`)과 idempotency grep 줄은 변경하지 않는다.

    5. `.agents/skills/sg-ship/SKILL.md` Step 1: multi-line Phase 파싱 블록 교체:
       (2~3줄 multi-line sed 패턴 전체)
       → `Read .planning/STATE.md, then extract the Phase: value from the YAML frontmatter. Set PHASE_NUM to the extracted value.`

    6. `.agents/skills/sg-ship/SKILL.md` Step 6 FROM_STAGE: HANDOFF awk 파싱 교체:
       `grep -E '^\| [0-9]{4}-' "$HANDOFF_FILE" | tail -1 | awk -F'|' '{gsub(/ /,"",$5); print $5}'`
       → `Read .planning/HANDOFF.md, then extract the To column (5th pipe-delimited field) from the last row starting with "| " followed by a 4-digit year. Set FROM_STAGE (default "review" if empty).`
  </steps>
  <verify>
    <automated>grep -rn "grep -E '\^Phase:'" .agents/skills/sg-execute/SKILL.md .agents/skills/sg-plan/SKILL.md .agents/skills/sg-ship/SKILL.md | wc -l</automated>
    <automated>grep -rn "awk -F'\|'.*gsub" .agents/skills/sg-execute/SKILL.md .agents/skills/sg-plan/SKILL.md .agents/skills/sg-ship/SKILL.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "grep -E '\^Phase:'" .agents/skills/sg-execute/SKILL.md .agents/skills/sg-plan/SKILL.md .agents/skills/sg-ship/SKILL.md` 결과 0건
    - `grep -rn "awk -F'\|'.*gsub.*print.*5" .agents/skills/sg-execute/SKILL.md .agents/skills/sg-plan/SKILL.md .agents/skills/sg-ship/SKILL.md` 결과 0건
    - `grep -c "Read .planning/STATE.md" .agents/skills/sg-execute/SKILL.md` 결과 1건 이상
    - sg-execute EXISTING_HASH grep 줄(`grep -E "^\| [^|]+ \|.*execute"`) 변경 없이 존재
    - sg-plan HANDOFF 초기화 블록(`if [ ! -f "$HANDOFF_FILE" ]`) 변경 없이 존재
  </acceptance_criteria>
  <done>.agents/skills/sg-execute, sg-plan, sg-ship 3개 파일에서 STATE.md Phase 파싱과 HANDOFF.md 마지막 행 파싱이 Read 도구 지시문으로 교체된다.</done>
</task>

<task type="auto">
  <name>Task 2: .agents/skills/sg-review — Pattern A+B+D 전환</name>
  <files>
    .agents/skills/sg-review/SKILL.md
  </files>
  <read_first>
    - .agents/skills/sg-review/SKILL.md — 전체 파일 읽기: Step 3 PHASE_NUM(multi-line 4줄 sed 패턴), PLAN_REQUIREMENTS(sed -n objective 파싱), Step 3.9 FROM_STAGE_R(awk HANDOFF 파싱) 위치 확인
    - .planning/phases/32-skill-superpowers-migration/32-PATTERNS.md — .agents 버전이 `grep -oE '^[0-9]+'` 추가 파이프 사용하는 패턴 B 변형 확인
  </read_first>
  <steps>
    1. `.agents/skills/sg-review/SKILL.md` Step 3 PHASE_NUM: 4줄 multi-line Phase 파싱 블록 전체 교체:
       ```bash
       PHASE_NUM=$(grep -E '^Phase:' .planning/STATE.md 2>/dev/null | head -1 \
                   | sed -E 's/^Phase:[[:space:]]*//' \
                   | sed -E 's/[[:space:]]+$//' \
                   | grep -oE '^[0-9]+')
       ```
       → `Read .planning/STATE.md, then extract the Phase: value from the YAML frontmatter. Set PHASE_NUM to the extracted value.`

    2. `.agents/skills/sg-review/SKILL.md` Step 3 PLAN_REQUIREMENTS: sed -n objective 파싱 교체:
       `sed -n '/<objective>/,/<\/objective>/p' "$PLAN_FILE" 2>/dev/null | grep -v 'objective>'`
       → `Read the PLAN_FILE path, then extract the text content between <objective> and </objective> tags. Set PLAN_REQUIREMENTS.`

    3. `.agents/skills/sg-review/SKILL.md` Step 3.9 FROM_STAGE_R: HANDOFF awk 파싱 교체:
       `grep -E '^\| [0-9]{4}-' "$HANDOFF_FILE" | tail -1 | awk -F'|' '{gsub(/ /,"",$5); print $5}'`
       → `Read .planning/HANDOFF.md, then extract the To column (5th pipe-delimited field) from the last row starting with "| " followed by a 4-digit year. Set FROM_STAGE_R (default "init" if empty).`
       주의: HANDOFF 초기화 블록과 HANDOFF.md append 줄은 변경하지 않는다.
  </steps>
  <verify>
    <automated>grep -n "grep -E '\^Phase:'\|sed -n '/<objective>" .agents/skills/sg-review/SKILL.md | wc -l</automated>
    <automated>grep -n "awk -F'\|'.*gsub" .agents/skills/sg-review/SKILL.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "grep -E '\^Phase:'" .agents/skills/sg-review/SKILL.md` 결과 0건
    - `grep -n "sed -n '/<objective>" .agents/skills/sg-review/SKILL.md` 결과 0건
    - `grep -n "awk -F'\|'.*gsub.*print.*5" .agents/skills/sg-review/SKILL.md` 결과 0건
    - `grep -c "Read .planning/STATE.md" .agents/skills/sg-review/SKILL.md` 결과 1건 이상
    - HANDOFF 초기화 블록과 append 줄(`echo "| $TS | $PHASE_SLUG_R | $FROM_STAGE_R | review | - |"`) 변경 없이 존재
  </acceptance_criteria>
  <done>.agents/skills/sg-review에서 STATE.md Phase 파싱, PLAN.md objective 파싱, HANDOFF.md 마지막 행 파싱이 모두 Read 도구 지시문으로 교체된다.</done>
</task>

<task type="auto">
  <name>Task 3: skills/sg-retro 주석 교체 + CLAUDE.md 컨벤션 업데이트 (D-02, D-03)</name>
  <files>
    skills/sg-retro/SKILL.md
    CLAUDE.md
  </files>
  <read_first>
    - skills/sg-retro/SKILL.md — line 18 (D-04~D-06 lock 설명 텍스트), line 26 (D-08 bash 주석) 위치 확인. 이외 bash 코드는 변경 없음 확인
    - .agents/skills/sg-retro/SKILL.md — 이미 lock 주석 없는 형식 참조 (목표 상태 예시)
    - CLAUDE.md — line 96 (Phase 7 D-04~D-06 lock) 위치 확인
    - .planning/phases/32-skill-superpowers-migration/32-PATTERNS.md — 패턴 H/I Before/After 정확한 텍스트 확인
  </read_first>
  <steps>
    1. `skills/sg-retro/SKILL.md` line 18 텍스트 교체 (per D-02):
       찾을 텍스트: `using the multi-line \`sed\` pattern below (Phase 7 D-04~D-06 lock). **Do not introduce a single-token regex shortcut** — preserve the full grep + sed + awk pipeline as-is.`
       교체 텍스트: `using the multi-line \`grep + sed + awk\` pattern below (macOS 호환 파이프라인). **Do not replace with a single-token regex** — preserve the full pipeline for macOS/BSD compatibility.`
       주의: 이 줄 이전/이후 텍스트는 변경하지 않는다. bash 코드 블록(line 26 이후) 자체는 일절 변경 없음.

    2. `skills/sg-retro/SKILL.md` line 26 bash 주석 교체 (per D-02):
       찾을 텍스트: `# --- BEGIN STATE.md Phase parsing block (D-08: Phase 7 D-04~D-06 multi-line 패턴 인라인 복제) ---`
       교체 텍스트: `# --- BEGIN STATE.md Phase parsing block (macOS 호환 grep + sed + awk 파이프라인) ---`
       주의: 주석 내용만 변경. 해당 블록 내 bash 코드 전체는 유지.

    3. `CLAUDE.md` 컨벤션 항목 교체 (per D-03):
       찾을 텍스트: `- **STATE.md Phase 파싱** — 단일 토큰 정규식 대신 \`grep -E '^Phase:' | sed -E 's/^Phase:[[:space:]]*//' | awk '{print $1}'\` 파이프라인을 사용한다 (Phase 7 D-04~D-06 lock).`
       교체 텍스트: `- **STATE.md Phase 파싱 (skills/SKILL.md)** — SKILL.md에서 \`$ARGUMENTS\`가 없을 때는 Read 도구로 \`.planning/STATE.md\`를 열어 \`Phase:\` 필드를 직접 읽는다 (Superpowers 방식). bash 파이프라인(\`grep | sed | awk\`)은 \`hooks/*.cjs\`와 \`skills/sg-retro/SKILL.md\`처럼 macOS 호환성 이유로 명시된 경우에만 유지한다.`
       주의: 해당 불릿 항목 한 줄만 교체. 주변 항목과 섹션 제목은 변경하지 않는다.
  </steps>
  <verify>
    <automated>grep -n "D-04~D-06 lock\|Phase 7 D-04~D-06\|D-08: Phase 7" skills/sg-retro/SKILL.md CLAUDE.md | wc -l</automated>
    <automated>grep -n "macOS 호환" skills/sg-retro/SKILL.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "D-04~D-06 lock" skills/sg-retro/SKILL.md` 결과 0건
    - `grep -n "D-08: Phase 7 D-04~D-06" skills/sg-retro/SKILL.md` 결과 0건
    - `grep -n "Phase 7 D-04~D-06 lock" CLAUDE.md` 결과 0건
    - `grep -n "macOS 호환 grep + sed + awk 파이프라인" skills/sg-retro/SKILL.md` 결과 1건 (line 26 주석)
    - `grep -n "Superpowers 방식" CLAUDE.md` 결과 1건 이상
    - `grep -n "PHASE_RAW=\$(grep -E '\^Phase:'" skills/sg-retro/SKILL.md` — bash 코드는 변경 없이 존재
    - `git diff HEAD -- skills/sg-status/SKILL.md skills/sg-next/SKILL.md skills/sg-start/SKILL.md` 빈 출력 (제외 대상 변경 없음)
  </acceptance_criteria>
  <done>sg-retro의 두 lock 주석이 macOS 호환성 주석으로 교체되고, CLAUDE.md가 Superpowers 방식을 권장 방식으로 명시한다. bash 코드는 변경되지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md 텍스트 편집 | 주석 교체 범위 초과 시 bash 코드 의도치 않게 변경 위험 |
| CLAUDE.md 컨벤션 편집 | 인접 항목 변경 시 기존 컨벤션 훼손 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering | sg-retro bash Phase 파싱 코드 | mitigate | acceptance_criteria에서 bash 코드(`PHASE_RAW=$(grep`) 보존 명시적 확인 |
| T-32-05 | Tampering | CLAUDE.md 인접 컨벤션 항목 | mitigate | 한 불릿 항목만 교체, 섹션 제목/주변 항목 변경 금지를 steps에 명시 |
| T-32-06 | Tampering | sg-status/sg-next/sg-start (D-07/D-04 lock) | mitigate | acceptance_criteria에서 git diff 결과 빈 출력으로 검증 |
</threat_model>

<verification>
```bash
# 전체 lock 주석 제거 확인
grep -rn "D-04~D-06 lock\|Phase 7 D-04~D-06\|D-08: Phase 7" \
  skills/sg-retro/SKILL.md \
  CLAUDE.md \
  .agents/skills/sg-execute/SKILL.md \
  .agents/skills/sg-plan/SKILL.md \
  .agents/skills/sg-review/SKILL.md \
  .agents/skills/sg-ship/SKILL.md

# .agents bash Phase 파싱 0건 확인
grep -rn "grep -E '\^Phase:'" \
  .agents/skills/sg-execute/SKILL.md \
  .agents/skills/sg-plan/SKILL.md \
  .agents/skills/sg-review/SKILL.md \
  .agents/skills/sg-ship/SKILL.md

# 제외 대상 변경 없음
git diff HEAD -- skills/sg-status/SKILL.md skills/sg-next/SKILL.md skills/sg-start/SKILL.md hooks/
```
</verification>

<success_criteria>
1. `grep -rn "grep -E '\^Phase:'" .agents/skills/sg-{execute,plan,review,ship}/SKILL.md` 결과 0건
2. `grep -rn "awk -F'\|'.*gsub.*print.*5" .agents/skills/sg-{execute,plan,review,ship}/SKILL.md` 결과 0건
3. `grep -n "sed -n '/<objective>" .agents/skills/sg-review/SKILL.md` 결과 0건
4. `grep -n "D-04~D-06 lock" skills/sg-retro/SKILL.md` 결과 0건
5. `grep -n "Phase 7 D-04~D-06 lock" CLAUDE.md` 결과 0건
6. `grep -n "Superpowers 방식" CLAUDE.md` 결과 1건 이상
7. `git diff HEAD -- skills/sg-status/SKILL.md skills/sg-next/SKILL.md skills/sg-start/SKILL.md` 빈 출력
</success_criteria>

<output>
Create `.planning/phases/32-skill-superpowers-migration/32-02-SUMMARY.md` when done.
</output>
