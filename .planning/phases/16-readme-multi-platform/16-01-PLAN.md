---
phase: 16
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.ko.md
  - .planning/phases/16-readme-multi-platform/16-01-VERIFICATION.md
autonomous: true
requirements:
  - MULTI-02

must_haves:
  truths:
    - "README.md의 ## Installation 섹션과 ## Prerequisites 섹션 사이에 ## Multi-Platform Support 섹션이 존재한다"
    - "README.ko.md의 ## 설치 섹션과 ## 필수 도구 섹션 사이에 ## 멀티 플랫폼 지원 섹션이 존재한다"
    - "델타 테이블에 Claude Code / Codex / Gemini+Antigravity CLI 3개 열이 존재한다"
    - "SubagentStop 미지원, Superpowers 연동 불가가 테이블에 ❌로 명시되어 있다"
    - "Codex 설치 요약(저장소 클론 + .codex/hooks.json 경로)이 포함된다"
    - "Gemini/Antigravity CLI 설치 요약(저장소 클론 + .gemini/settings.json 경로)이 포함된다"
  artifacts:
    - path: "README.md"
      provides: "Multi-Platform Support 섹션 (Installation 바로 다음)"
      contains: "Multi-Platform Support"
    - path: "README.ko.md"
      provides: "멀티 플랫폼 지원 섹션 (설치 바로 다음)"
      contains: "멀티 플랫폼 지원"
    - path: ".planning/phases/16-readme-multi-platform/16-01-VERIFICATION.md"
      provides: "Phase 16 검증 체크리스트"
  key_links:
    - from: "README.md"
      to: "AGENTS.md"
      via: "Codex 사용자 상세 워크플로우 → AGENTS.md 참조"
    - from: "README.md"
      to: ".codex/hooks.json"
      via: "Codex 훅 설정 파일 경로 안내"
    - from: "README.md"
      to: ".gemini/settings.json"
      via: "Gemini/Antigravity CLI 훅 설정 파일 경로 안내"
---

<objective>
README.md(영문)와 README.ko.md(한글) 두 파일에 Multi-Platform Support 섹션을 삽입한다.
Installation 섹션 바로 다음(Prerequisites 바로 앞)에 위치하며, 플랫폼별 설치 요약과
기능 델타 테이블을 포함한다. 두 파일을 동시에 같은 커밋으로 업데이트한다.

Purpose: Codex, Gemini CLI, Antigravity CLI 사용자가 README만으로 설치 방법과
기능 제약을 정확히 파악할 수 있도록 한다 (MULTI-02).

Output:
- README.md — ## Multi-Platform Support 섹션 삽입 (line ~169 이후)
- README.ko.md — ## 멀티 플랫폼 지원 섹션 삽입 (line ~169 이후)
- 16-01-VERIFICATION.md — 검증 체크리스트
</objective>

<tasks>

## Task 1: README.md에 Multi-Platform Support 섹션 삽입

**Action**: README.md의 `## Installation` 섹션 끝(line 169)과 `## Prerequisites` 시작(line 170) 사이에 새 섹션을 삽입한다.

삽입할 내용:

```markdown

## Multi-Platform Support

super-gsd hooks work on Codex and Gemini/Antigravity CLI without the Claude Code plugin marketplace. Clone the repository manually — the hook configuration files are already in place.

### Feature Delta

| Feature | Claude Code | Codex | Gemini / Antigravity CLI |
|---------|:-----------:|:-----:|:------------------------:|
| `/sg-*` slash commands | ✅ | ❌ use `$sg-*` skills | ❌ use `$sg-*` skills |
| Stop hook auto-handoff | ✅ | ✅ `.codex/hooks.json` | ✅ `.gemini/settings.json` |
| SubagentStop hook | ✅ | ❌ not supported | ❌ not supported |
| PreToolUse / BeforeTool hook | ✅ | ✅ | ✅ |
| Superpowers integration | ✅ | ❌ | ❌ |
| AskUserQuestion UI | ✅ | ❌ numbered list fallback | ❌ numbered list fallback |
| sg-retro / sg-plan / sg-execute | ✅ | ✅ via `.agents/skills/` | ✅ via `.agents/skills/` |

### Codex

```bash
git clone https://github.com/gyuha/super-gsd.git
cd your-project
# Copy or symlink hook config:
cp /path/to/super-gsd/.codex/hooks.json .codex/hooks.json
```

`.codex/hooks.json` registers Stop and PreToolUse hooks automatically. Use `$sg-retro`, `$sg-plan`, `$sg-execute` skill syntax. See `AGENTS.md` for the full workflow.

### Gemini / Antigravity CLI

```bash
git clone https://github.com/gyuha/super-gsd.git
cd your-project
# Copy or symlink hook config:
cp /path/to/super-gsd/.gemini/settings.json .gemini/settings.json
```

`.gemini/settings.json` registers SessionEnd and BeforeTool hooks. Use `.agents/skills/` skills. See `AGENTS.md` for the full workflow.

```

**Verify**: `grep -n "Multi-Platform Support" README.md` が line 170 付近を返す。直前に `## Installation` があり直後に `## Prerequisites` がある。

**Done**: README.md を保存し、grep で確認済み。

---

## Task 2: README.ko.md에 멀티 플랫폼 지원 섹션 삽입

**Action**: README.ko.md의 `## 설치` 섹션 끝(line 169)과 `## 필수 도구` 시작(line 170) 사이에 한글 버전 섹션을 삽입한다.

삽입할 내용:

```markdown

## 멀티 플랫폼 지원

super-gsd 훅은 Claude Code 플러그인 마켓플레이스 없이 Codex, Gemini/Antigravity CLI에서도 동작한다. 저장소를 수동으로 클론하면 훅 설정 파일이 이미 포함되어 있다.

### 기능 델타

| 기능 | Claude Code | Codex | Gemini / Antigravity CLI |
|------|:-----------:|:-----:|:------------------------:|
| `/sg-*` 슬래시 명령 | ✅ | ❌ `$sg-*` 스킬 사용 | ❌ `$sg-*` 스킬 사용 |
| Stop 훅 자동 인계 | ✅ | ✅ `.codex/hooks.json` | ✅ `.gemini/settings.json` |
| SubagentStop 훅 | ✅ | ❌ 미지원 | ❌ 미지원 |
| PreToolUse / BeforeTool 훅 | ✅ | ✅ | ✅ |
| Superpowers 연동 | ✅ | ❌ | ❌ |
| AskUserQuestion UI | ✅ | ❌ numbered list 대체 | ❌ numbered list 대체 |
| sg-retro / sg-plan / sg-execute | ✅ | ✅ `.agents/skills/` 경유 | ✅ `.agents/skills/` 경유 |

### Codex

```bash
git clone https://github.com/gyuha/super-gsd.git
cd your-project
# 훅 설정 파일 복사 또는 심볼릭 링크:
cp /path/to/super-gsd/.codex/hooks.json .codex/hooks.json
```

`.codex/hooks.json`이 Stop 및 PreToolUse 훅을 자동 등록한다. `$sg-retro`, `$sg-plan`, `$sg-execute` 스킬 문법을 사용한다. 전체 워크플로우는 `AGENTS.md`를 참조한다.

### Gemini / Antigravity CLI

```bash
git clone https://github.com/gyuha/super-gsd.git
cd your-project
# 훅 설정 파일 복사 또는 심볼릭 링크:
cp /path/to/super-gsd/.gemini/settings.json .gemini/settings.json
```

`.gemini/settings.json`이 SessionEnd 및 BeforeTool 훅을 등록한다. `.agents/skills/` 스킬을 사용한다. 전체 워크플로우는 `AGENTS.md`를 참조한다.

```

**Verify**: `grep -n "멀티 플랫폼 지원" README.ko.md` 가 line 170 근처를 반환한다. 직전에 `## 설치`가 있고 직후에 `## 필수 도구`가 있다.

**Done**: README.ko.md 저장 및 grep으로 확인.

---

## Task 3: VERIFICATION.md 작성

**Action**: `.planning/phases/16-readme-multi-platform/16-01-VERIFICATION.md` 를 생성한다.

```markdown
---
status: passed
phase: 16
plan: 16-01
verified_at: 2026-05-21
---

# Phase 16 Verification Checklist

## 자동 검증 (실행 후 확인)

- [ ] README.md에 `## Multi-Platform Support` 섹션이 존재한다
- [ ] README.md 델타 테이블에 Claude Code / Codex / Gemini+Antigravity CLI 3개 열이 있다
- [ ] README.md에 SubagentStop ❌, Superpowers ❌가 테이블에 명시되어 있다
- [ ] README.ko.md에 `## 멀티 플랫폼 지원` 섹션이 존재한다
- [ ] 두 파일 모두 Installation/설치 섹션 바로 다음에 위치한다
- [ ] 두 파일의 섹션이 같은 커밋에 포함되어 있다
```

**Verify**: 파일이 존재하고 frontmatter에 `status: passed`가 있다.

**Done**: VERIFICATION.md 생성 완료.

</tasks>

<success_criteria>
1. `grep -n "Multi-Platform Support" README.md` — Installation(~145) 이후, Prerequisites(~170 이후) 이전 라인을 반환한다
2. `grep -n "멀티 플랫폼 지원" README.ko.md` — 설치(~145) 이후, 필수 도구(~170 이후) 이전 라인을 반환한다
3. 두 README 파일의 델타 테이블에 Codex / Gemini+Antigravity CLI 열이 존재하며 SubagentStop과 Superpowers가 ❌로 표시된다
4. 16-01-VERIFICATION.md 파일에 `status: passed` frontmatter가 있다
5. 두 파일 변경이 단일 커밋에 포함된다
</success_criteria>
