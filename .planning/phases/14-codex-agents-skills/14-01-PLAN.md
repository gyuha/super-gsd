---
phase: 14-codex-agents-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - .agents/skills/sg-retro/SKILL.md
  - .agents/skills/sg-start/SKILL.md
  - .agents/skills/sg-plan/SKILL.md
  - .agents/skills/sg-execute/SKILL.md
  - .agents/skills/sg-review/SKILL.md
  - .agents/skills/sg-status/SKILL.md
autonomous: true
requirements:
  - CODEX-01
  - CODEX-02
  - CODEX-03

must_haves:
  truths:
    - "Codex 세션 시작 시 AGENTS.md가 자동 주입되고, $sg-* 달러 문법으로 워크플로우 단계가 안내된다"
    - "AGENTS.md에 SubagentStop/Superpowers/AskUserQuestion 미지원 사실이 명시된다"
    - "sg-retro SKILL.md가 AskUserQuestion 없이 numbered list fallback으로 실행된다"
    - "5개 래퍼 스킬이 각각 platform-agnostic 지침 + Superpowers 연동 불가를 명시한다"
  artifacts:
    - path: "AGENTS.md"
      provides: "Codex 진입점 안내 (8KiB 이하, 5-섹션 구조)"
    - path: ".agents/skills/sg-retro/SKILL.md"
      provides: "AskUserQuestion 제거 버전 sg-retro 스킬"
    - path: ".agents/skills/sg-start/SKILL.md"
      provides: "sg-start 래퍼 스킬 (GSD 위임 + prose 폴백)"
    - path: ".agents/skills/sg-plan/SKILL.md"
      provides: "sg-plan 래퍼 스킬"
    - path: ".agents/skills/sg-execute/SKILL.md"
      provides: "sg-execute 래퍼 스킬 (직접 구현 모드)"
    - path: ".agents/skills/sg-review/SKILL.md"
      provides: "sg-review 래퍼 스킬 (prose 리뷰 직접 수행)"
    - path: ".agents/skills/sg-status/SKILL.md"
      provides: "sg-status 래퍼 스킬 (HANDOFF.md + STATE.md 직접 파싱)"
  key_links:
    - from: "AGENTS.md"
      to: ".agents/skills/"
      via: "경로 안내 섹션"
      pattern: ".agents/skills/"
    - from: ".agents/skills/sg-retro/SKILL.md"
      to: ".planning/lessons/"
      via: "lessons 파일 append"
      pattern: "planning/lessons"
---

<objective>
Phase 14: Codex, Gemini CLI, Antigravity CLI 사용자가 super-gsd 워크플로우를 이해하고 핵심 스킬을 실행할 수 있도록, AGENTS.md를 완전 재작성하고 `.agents/skills/` 아래 6개 SKILL.md를 신규 생성한다.

Purpose:
- AGENTS.md는 Codex 세션 시작 시 자동 주입되므로 올바른 진입점 안내가 CODEX-01 달성의 핵심이다.
- 6개 SKILL.md는 Claude Code 전용 도구(AskUserQuestion, Superpowers, SubagentStop) 없이 동작하는 platform-agnostic 버전을 제공한다.

Output:
- AGENTS.md (교체): GSD 마커 제거, 5-섹션 Codex 어휘 버전, 8KiB 이하
- .agents/skills/sg-retro/SKILL.md: AskUserQuestion → numbered list fallback 교체
- .agents/skills/sg-{start,plan,execute,review,status}/SKILL.md: 5개 래퍼 스킬
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-codex-agents-skills/14-CONTEXT.md

참조 원본 파일 (구현 시 반드시 읽고 내용을 기반으로 작성할 것):
- AGENTS.md — 재작성 대상 현재 파일
- skills/sg-retro/SKILL.md — .agents/skills/sg-retro/SKILL.md 원본
- commands/sg-start.md — .agents/skills/sg-start/SKILL.md 원본
- commands/sg-plan.md — .agents/skills/sg-plan/SKILL.md 원본
- commands/sg-execute.md — .agents/skills/sg-execute/SKILL.md 원본
- commands/sg-review.md — .agents/skills/sg-review/SKILL.md 원본
- commands/sg-status.md — .agents/skills/sg-status/SKILL.md 원본
</context>

<interfaces>
<!-- 구현자가 참조할 기존 패턴 요약 -->

AGENTS.md 현재 구조 (교체 대상):
- GSD 마커 기반 섹션 (<!-- GSD:*-start/end --> 태그 포함)
- Codex 어휘가 아닌 Claude Code 어휘로 작성됨
- 현재 크기: 4.3KiB (8KiB 이하 달성 가능)

SKILL.md 표준 frontmatter:
```yaml
---
name: sg-{name}
description: {한 줄 설명}
argument-hint: "{argument 힌트}"
---
```

STATE.md Phase 파싱 블록 (절대 shortcut 금지 — 프로젝트 전반 lock 패턴):
```bash
# --- BEGIN STATE.md Phase parsing block ---
PHASE_LINE=$(grep -E '^Phase:' .planning/STATE.md 2>/dev/null | head -1 | sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//')
[ -z "$PHASE_LINE" ] && PHASE_LINE="(none)"
PHASE_NUM=$(echo "$PHASE_LINE" | grep -oE '^[0-9]+' || echo "")
# --- END STATE.md Phase parsing block ---
```

HANDOFF.md 마지막 행 파싱 패턴:
```bash
LAST_ROW=$(grep -E '^\| [0-9]{4}-' .planning/HANDOFF.md 2>/dev/null | tail -1)
STAGE_RAW=$(echo "$LAST_ROW" | awk -F'|' '{gsub(/ /,"",$5); print $5}')
TS=$(echo "$LAST_ROW" | awk -F'|' '{gsub(/ /,"",$2); print $2}')
```

platform constraints 블록 (모든 .agents/skills/ SKILL.md에 포함):
- Superpowers 연동 불가 (Claude Code 전용 도구)
- SubagentStop 미지원
- AskUserQuestion 미지원
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: AGENTS.md 완전 재작성 (D-A1/A2/A3)</name>
  <files>AGENTS.md</files>
  <action>
기존 AGENTS.md를 GSD 마커(<!-- GSD:*-start/end -->)와 Claude Code 전용 어휘를 모두 제거하고 아래 5-섹션 구조로 완전 재작성한다. 8KiB(8,192바이트) 이하를 반드시 유지한다. 재작성 후 `wc -c AGENTS.md`로 크기를 확인한다.

**작성 규칙 (D-A1~A3):**
- Codex 달러 문법: `/super-gsd:sg-start` 대신 `$sg-start` 형태로 표기
- 각 스킬 설명은 이름 + 한 줄 설명만 (상세 옵션은 각 SKILL.md에서 self-documenting)
- 전체 파일을 새로 작성하며 GSD 마커 섹션을 남기지 않는다

**5-섹션 구조:**

**섹션 1: Project**
super-gsd 프로젝트 설명. "GSD → sg-retro 2단계 AI 개발 워크플로우를 Codex/Gemini CLI/Antigravity CLI에서 실행하는 플러그인"으로 기술. Core Value는 CLAUDE.md의 설명을 Codex 어휘로 재서술.

**섹션 2: Quick Start**
3단계:
1. 사전 조건: GSD(`get-shit-done-cc`) 설치 여부 확인
2. `$sg-start` — 세션 시작
3. `$sg-plan N` → `$sg-execute` → `$sg-review` → `$sg-retro` 순서 안내

**섹션 3: Platform Limitations (Codex / Gemini CLI / Antigravity CLI)**
별도 경고 블록으로 강조. 다음 세 가지를 명확히 명시:
- **SubagentStop 미지원**: sg-retro, sg-learn을 종료 시 자동 트리거할 수 없다. 각 단계 완료 후 수동으로 `$sg-retro`를 호출해야 한다.
- **Superpowers 연동 불가**: sg-execute는 직접 구현 모드로 실행되며 superpowers:executing-plans를 사용하지 않는다. sg-review는 prose 리뷰 모드로 실행된다.
- **AskUserQuestion 미지원**: sg-retro는 numbered list fallback으로 렌즈를 선택한다.

**섹션 4: Skills**
`.agents/skills/` 경로 명시. 6개 스킬 목록:
- `sg-start` — 기존 세션 감지 또는 신규 프로젝트 시작
- `sg-plan N` — 컨텍스트 수집 후 GSD phase 계획 생성
- `sg-execute` — PLAN.md를 읽고 태스크를 순차 실행
- `sg-review` — 변경 파일 코드 리뷰 prose 수행
- `sg-status` — 현재 워크플로우 단계 확인
- `sg-retro` — phase 회고 (6개 렌즈, numbered list fallback)

스킬 호출 방법:
```
# Codex 달러 문법
$sg-plan 14

# 또는 Skill 도구 직접 사용
Skill(skill="super-gsd/sg-plan", args="14")
```

**섹션 5: Workflow Overview**
단계별 흐름 (텍스트 흐름도):
```
$sg-start → $sg-plan N → $sg-execute → $sg-review → $sg-retro → $sg-ship
```
각 단계 한 줄 설명 포함. sg-retro는 수동 호출 필수임을 재강조.
  </action>
  <verify>
    <automated>wc -c /Users/gyuha/workspace/super-gsd/AGENTS.md</automated>
  </verify>
  <done>
- AGENTS.md에 GSD 마커(<!-- GSD: -->)가 없다
- 5개 섹션(Project / Quick Start / Platform Limitations / Skills / Workflow Overview)이 존재한다
- `$sg-*` 달러 문법이 Quick Start와 Skills 섹션에 사용된다
- SubagentStop/Superpowers/AskUserQuestion 미지원 사실이 Platform Limitations 섹션에 명시된다
- 파일 크기가 8,192바이트 이하이다
  </done>
</task>

<task type="auto">
  <name>Task 2: .agents/skills/sg-retro/SKILL.md 신규 생성 (D-B1~B4)</name>
  <files>.agents/skills/sg-retro/SKILL.md</files>
  <action>
`skills/sg-retro/SKILL.md`를 기반으로 `.agents/skills/sg-retro/SKILL.md`를 신규 생성한다. 반드시 `skills/sg-retro/SKILL.md`를 먼저 읽어 Step 1~6 전체 구조, 파싱 로직, multi-lens loop, lens_templates, success_criteria를 파악한 뒤 아래 변경사항만 적용한다.

**변경사항 (D-B1~B4):**

Step 2에서 AskUserQuestion 호출 블록 전체를 아래 numbered list 출력 + 자유 입력 대기 패턴으로 교체한다:

원본 (Claude Code 버전):
```
AskUserQuestion(
  questions: [{
    question: "Which retrospective lens(es) do you want to run?",
    header: "Lens",
    multiSelect: true,
    options: [...]
  }]
)
```

교체 후 (Codex 버전):
```bash
# AskUserQuestion 미지원 — numbered list fallback (D-B2)
printf '\n렌즈를 선택하세요:\n'
printf '1) ssc     — Start/Stop/Continue\n'
printf '2) 4ls     — 4Ls (Like/Learned/Lacked/Longed for)\n'
printf '3) dspm    — Decisions/Surprises/Patterns/Mistakes\n'
printf '4) sail    — Sailboat\n'
printf '5) 5why    — Five Whys\n'
printf '6) analyze — Conversation Analyzer\n\n'
printf '번호 또는 코드로 입력하세요. 복수 선택: "1 3" 또는 "ssc dspm"\n'
# 사용자 입력을 직접 읽거나 다음 turn에서 응답을 파싱한다
```

이후 응답 파싱 로직 (D-B2/D-B3 multiSelect 지원):
- 숫자 입력("1", "1 3")을 해당 코드("ssc", "ssc dspm")로 매핑
- 코드 직접 입력("ssc dspm")도 처리
- LENS_CODES_ARRAY를 구성해 기존 multi-lens loop에 진입

**5why 서브블록 내 AskUserQuestion 처리:**
5why 서브블록에서 AskUserQuestion으로 Problem/Why1~5를 수집하는 부분은, numbered list 없이 Claude가 직접 질문을 텍스트로 출력하고 다음 turn 응답을 입력으로 처리하도록 변경한다:
```
[Five Whys — Problem] 분석할 문제를 설명하세요. (공백이면 phase artifacts에서 제안합니다)
```

**변경하지 않는 부분 (D-B4 외):**
- Step 1 Phase 파싱 블록 (grep-sed-awk 파이프라인 그대로 복제)
- Step 3 Artifact 수집 블록
- Step 3b Transcript 수집 블록
- Step 4 Git context 수집 블록
- Step 5 Multi-lens loop 전체 (5why/analyze 서브블록 포함, AskUserQuestion 부분 제외)
- Step 6 Append 블록
- lens_templates 섹션 전체
- success_criteria (AskUserQuestion 관련 항목을 numbered list fallback으로 표현 수정)

**파일 생성 전 디렉토리 생성:**
`mkdir -p .agents/skills/sg-retro`
  </action>
  <verify>
    <automated>! grep -q "AskUserQuestion" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-retro/SKILL.md && echo "OK: AskUserQuestion not present"</automated>
  </verify>
  <done>
- `.agents/skills/sg-retro/SKILL.md`가 존재한다
- 파일 내에 `AskUserQuestion` 문자열이 없다 (grep 결과 0)
- numbered list fallback 블록 (`1) ssc`, `2) 4ls` 등)이 Step 2에 있다
- `.planning/lessons/` 경로 참조가 존재한다
- Step 1 Phase 파싱 블록 (grep-sed-awk 파이프라인)이 보존되어 있다
  </done>
</task>

<task type="auto">
  <name>Task 3: .agents/skills/sg-start/SKILL.md 신규 생성 (D-C1/C2)</name>
  <files>.agents/skills/sg-start/SKILL.md</files>
  <action>
`commands/sg-start.md`를 기반으로 `.agents/skills/sg-start/SKILL.md`를 신규 생성한다. 먼저 `commands/sg-start.md`를 읽어 전체 동작(세션 감지 → 5개 라인 emit → 분기)을 파악한다.

**frontmatter:**
```yaml
---
name: sg-start
description: 기존 세션 감지 또는 신규 프로젝트 시작 — super-gsd 워크플로우 진입점
argument-hint: "[project-name] - 신규 프로젝트 시작 시에만 사용. 기존 세션이 없을 때 gsd-new-project에 전달."
---
```

**<objective>:**
commands/sg-start.md와 동일한 목적 기술 + 아래 platform 제약 추가:
- AskUserQuestion 미지원으로 Resume/Start new milestone/Cancel 3-옵션 대신 텍스트 목록으로 출력하고 응답 입력 대기

**<constraints> 블록 추가 (D-C1):**
```
## Platform Constraints (Codex / Gemini CLI / Antigravity CLI)
- Superpowers 연동 불가: Claude Code 전용 도구
- SubagentStop 미지원: 단계 종료 시 자동 트리거 없음
- AskUserQuestion 미지원: 선택지를 텍스트로 출력하고 자유 입력을 받음
```

**<process>:**

Step 1: STATE.md Phase 파싱 블록 (commands/sg-start.md에서 그대로 복제 — lock 패턴)

Step 2: STATE.md frontmatter 추가 파싱 + HANDOFF.md 마지막 행 파싱 (commands/sg-start.md Step 2 그대로 복제)

Step 3: Last activity 시각 결정 (commands/sg-start.md Step 3 그대로 복제)

Step 4: NEXT_PHASE 계산 + Next 명령 매핑 (commands/sg-start.md Step 4 그대로 복제). 단, NEXT_CMD의 명령 형태를 `/super-gsd:sg-*` 대신 `$sg-*` 달러 문법으로 변경:
- `/super-gsd:sg-plan N` → `$sg-plan N`
- `/super-gsd:sg-execute` → `$sg-execute`
- 등

Step 5: 5개 라인 emit + 선택 분기 (D-C1 — AskUserQuestion 대신 텍스트 출력):
```
세션 감지됨.

Milestone: <MILESTONE_DISPLAY>
Phase: <PHASE_LINE>
Stage: <STAGE_DISPLAY>
Last activity: <LAST_ACTIVITY_DISPLAY>
Next: <NEXT_CMD>

다음 중 선택하세요:
1) Resume — Next 명령을 직접 실행합니다
2) Start new milestone — gsd-new-milestone 스킬을 호출합니다
3) Cancel — 변경 없이 종료합니다

번호 또는 텍스트로 입력하세요.
```

응답 파싱:
- "1" 또는 "Resume" → emit-only 종료, 사용자가 Next 명령 직접 실행
- "2" 또는 "Start new milestone" → `Skill(skill="gsd-new-milestone", args="")`
- "3" 또는 "Cancel" → `Cancelled. No changes made.` emit 후 종료

Step 6: D-17 fallback (STATE.md 미감지 시 `Skill(skill="gsd-new-project", args="$ARGUMENTS")`)

**<success_criteria>:**
commands/sg-start.md success_criteria를 AskUserQuestion → 텍스트 선택으로 표현 수정.

**파일 생성 전 디렉토리 생성:**
`mkdir -p .agents/skills/sg-start`
  </action>
  <verify>
    <automated>grep -l "Platform Constraints" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-start/SKILL.md</automated>
  </verify>
  <done>
- `.agents/skills/sg-start/SKILL.md`가 존재한다
- `<constraints>` 또는 `Platform Constraints` 블록이 있고 Superpowers/SubagentStop/AskUserQuestion 미지원이 명시된다
- `$sg-*` 달러 문법이 NEXT_CMD 매핑에 사용된다
- 텍스트 선택 목록(1/2/3)이 Step 5에 있다
- AskUserQuestion 호출 코드가 없다
  </done>
</task>

<task type="auto">
  <name>Task 4: .agents/skills/sg-plan/SKILL.md 신규 생성 (D-C1/C2)</name>
  <files>.agents/skills/sg-plan/SKILL.md</files>
  <action>
`commands/sg-plan.md`를 기반으로 `.agents/skills/sg-plan/SKILL.md`를 신규 생성한다. 먼저 `commands/sg-plan.md`를 읽어 전체 동작(lessons 주입 → phase resolve → gsd-discuss-phase Agent → gsd-plan-phase Skill)을 파악한다.

**frontmatter:**
```yaml
---
name: sg-plan
description: 컨텍스트 수집 후 GSD phase 계획 생성 — gsd-discuss-phase → gsd-plan-phase 체인 실행
argument-hint: "[phase] - optional. STATE.md 현재 phase 사용."
---
```

**<constraints> 블록 (D-C1):**
```
## Platform Constraints (Codex / Gemini CLI / Antigravity CLI)
- Superpowers 연동 불가: Claude Code 전용 도구
- SubagentStop 미지원: 단계 종료 시 자동 트리거 없음
- AskUserQuestion 미지원: 필요한 입력은 arguments로 전달
```

**<process>:**

Step 0: Lessons 주입 (commands/sg-plan.md Step 0 그대로 복제 — `python3 hooks/lessons_ranker.py` 호출 포함)

Step 1: Phase resolve (commands/sg-plan.md Step 1 그대로 복제)

**GSD 위임 우선 경로 (D-C2):**
```bash
# GSD 설치 여부 확인
if command -v gsd-discuss-phase >/dev/null 2>&1 || ls ~/.claude/get-shit-done 2>/dev/null | grep -q .; then
  GSD_AVAILABLE=1
else
  GSD_AVAILABLE=0
fi
```

GSD 있는 경우 (주 경로):
- commands/sg-plan.md Step 2~3 그대로 실행 (Agent → Skill 체인)
- HANDOFF.md gsd-plan 행 기록 (commands/sg-plan.md Step 2.5 그대로 복제)

GSD 없는 경우 (prose 폴백):
```
[sg-plan] GSD not found. Running manual planning mode.

Phase N planning steps:
1. Read .planning/phases/NN-*/NN-CONTEXT.md if it exists
2. Read .planning/ROADMAP.md Phase N section
3. Read .planning/REQUIREMENTS.md for relevant requirement IDs
4. Create .planning/phases/NN-{slug}/NN-01-PLAN.md with tasks, verification, and success criteria
   (Use GSD PLAN.md format: YAML frontmatter + tasks with Action/Verify/Done when)
5. Update .planning/ROADMAP.md to mark plan as created
```

**<success_criteria>:**
- GSD 있으면 commands/sg-plan.md의 기존 success_criteria 그대로
- GSD 없으면 prose 폴백으로 PLAN.md가 생성됨

**파일 생성 전 디렉토리 생성:**
`mkdir -p .agents/skills/sg-plan`
  </action>
  <verify>
    <automated>grep -l "Platform Constraints" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-plan/SKILL.md</automated>
  </verify>
  <done>
- `.agents/skills/sg-plan/SKILL.md`가 존재한다
- `Platform Constraints` 블록이 있다
- GSD 설치 여부 확인 + 주 경로/폴백 분기가 있다
- prose 폴백 모드에서 PLAN.md 생성 절차가 명시된다
  </done>
</task>

<task type="auto">
  <name>Task 5: .agents/skills/sg-execute/SKILL.md 신규 생성 (D-C1/C3)</name>
  <files>.agents/skills/sg-execute/SKILL.md</files>
  <action>
`commands/sg-execute.md`를 기반으로 `.agents/skills/sg-execute/SKILL.md`를 신규 생성한다. 먼저 `commands/sg-execute.md`를 읽어 전체 동작(lessons 리마인더 → phase resolve → PLAN.md 수집 → superpowers:executing-plans 호출)을 파악한다.

**핵심 차이점 (D-C3):**
`superpowers:executing-plans` 호출 불가 → "직접 구현 모드"만 제공한다. PLAN.md를 읽고 task를 순차 직접 실행하는 prose 지침으로 대체.

**frontmatter:**
```yaml
---
name: sg-execute
description: PLAN.md를 읽고 phase 태스크를 순차 실행 — Superpowers 없이 직접 구현 모드
argument-hint: "[phase] - optional. STATE.md 현재 phase 사용."
---
```

**<constraints> 블록 (D-C1):**
```
## Platform Constraints (Codex / Gemini CLI / Antigravity CLI)
- Superpowers 연동 불가: superpowers:executing-plans 스킬을 사용할 수 없습니다. 직접 구현 모드로 실행됩니다.
- SubagentStop 미지원: 단계 종료 시 자동 트리거 없음. 완료 후 $sg-review를 수동 실행하세요.
- AskUserQuestion 미지원
```

**<process>:**

Step 0: Lessons 리마인더 (commands/sg-execute.md Step 0 그대로 복제)

Step 1: Phase resolve (commands/sg-execute.md Step 1 그대로 복제)

Step 2: Phase 디렉토리 locate (commands/sg-execute.md Step 2 그대로 복제)

Step 3: Phase meta 추출 — ROADMAP.md에서 Goal, Success Criteria, REQ-IDs 추출 (commands/sg-execute.md Step 3 그대로 복제)

Step 4: REQ-ID → 한 줄 정의 매핑 (commands/sg-execute.md Step 4 그대로 복제)

Step 5: PLAN.md bodies 수집 (commands/sg-execute.md Step 5 그대로 복제)

Step 6: Plan Hash 계산 (commands/sg-execute.md Step 6 그대로 복제)

Step 7: Idempotency 체크 (commands/sg-execute.md Step 7 그대로 복제). 단, `To` 컬럼 비교 대상을 `superpowers` 대신 `execute`로 변경한다.

Step 7.5: HANDOFF.md 자동 초기화 (commands/sg-execute.md Step 7.5 그대로 복제)

Step 8: HANDOFF.md 행 append (commands/sg-execute.md Step 8 기반). `To` 값을 `superpowers` 대신 `execute`로 변경:
```bash
echo "| $TS | $PHASE_SLUG | $FROM_STAGE | execute | $PLAN_HASH |" >> .planning/HANDOFF.md
```

Step 9: **직접 구현 모드** (superpowers:executing-plans 대체):
수집한 PLAN.md 내용을 표시한 뒤, 아래 지침에 따라 각 task를 순차적으로 직접 실행한다:

```
## 직접 구현 지침

Phase <N> (<PHASE_NAME>) 실행을 시작합니다.
Goal: <GOAL>

Success Criteria:
<SC_TEXT>

--- 실행 순서 ---
각 PLAN.md의 task를 wave 순서대로 순차 실행합니다:

1. 각 task의 <files>에 명시된 파일을 생성하거나 수정합니다
2. <action>의 지침을 따라 구현합니다
3. <verify>의 자동화 명령을 실행해 검증합니다
4. <done>의 완료 조건이 충족됐는지 확인합니다
5. 모든 task 완료 후 성공 기준을 재확인합니다

checkpoint:human-verify 태스크는 멈추고 사용자 확인을 요청합니다.
checkpoint:decision 태스크는 선택지를 텍스트로 출력하고 입력을 기다립니다.

완료 후 $sg-review를 실행하여 코드 리뷰를 진행하세요.
```

**파일 생성 전 디렉토리 생성:**
`mkdir -p .agents/skills/sg-execute`
  </action>
  <verify>
    <automated>! grep -q "superpowers:executing-plans" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-execute/SKILL.md && echo "OK: no superpowers:executing-plans"</automated>
  </verify>
  <done>
- `.agents/skills/sg-execute/SKILL.md`가 존재한다
- `superpowers:executing-plans` Skill 호출 코드가 없다 (grep 결과 0)
- `Platform Constraints` 블록이 있고 Superpowers 연동 불가가 명시된다
- 직접 구현 모드 지침(wave 순서 task 실행)이 있다
- HANDOFF.md에 `execute` 행이 기록되는 로직이 있다
  </done>
</task>

<task type="auto">
  <name>Task 6: .agents/skills/sg-review/SKILL.md 신규 생성 (D-C1/C4)</name>
  <files>.agents/skills/sg-review/SKILL.md</files>
  <action>
`commands/sg-review.md`를 기반으로 `.agents/skills/sg-review/SKILL.md`를 신규 생성한다. 먼저 `commands/sg-review.md`를 읽어 전체 동작(git range 도출 → description 결정 → superpowers:requesting-code-review 호출)을 파악한다.

**핵심 차이점 (D-C4):**
`superpowers:requesting-code-review` 호출 불가 → prose 코드 리뷰 직접 수행.

**frontmatter:**
```yaml
---
name: sg-review
description: 변경 파일 코드 리뷰를 직접 수행 — Superpowers 없이 prose 리뷰 모드
argument-hint: "[구현 내용 설명] - optional. 없으면 최근 커밋 메시지 사용."
---
```

**<constraints> 블록 (D-C1):**
```
## Platform Constraints (Codex / Gemini CLI / Antigravity CLI)
- Superpowers 연동 불가: superpowers:requesting-code-review 스킬을 사용할 수 없습니다. Prose 리뷰 모드로 실행됩니다.
- SubagentStop 미지원: 완료 후 $sg-retro를 수동 실행하세요.
- AskUserQuestion 미지원
```

**<process>:**

Step 1: Git range 도출 (commands/sg-review.md Step 1 그대로 복제)

Step 2: Description 결정 (commands/sg-review.md Step 2 그대로 복제)

Step 3: PLAN.md 읽기 (commands/sg-review.md Step 3 그대로 복제)

Step 3.9: HANDOFF.md 행 기록 (commands/sg-review.md Step 3.9 기반). `To` 값을 `review` 그대로 유지.

Step 4: **Prose 리뷰 직접 수행** (superpowers:requesting-code-review 대체):

```
## 코드 리뷰 실행

리뷰 범위: $BASE_SHA..$HEAD_SHA
구현 내용: $DESCRIPTION

--- 리뷰 절차 ---
1. `git diff $BASE_SHA $HEAD_SHA --name-only`로 변경 파일 목록 확인
2. 각 변경 파일을 Read 도구로 열어 내용 확인
3. 다음 기준으로 리뷰 수행:
   - 명시된 requirements/success criteria 충족 여부
   - 로직 오류, 엣지케이스 누락
   - 기존 코드와의 일관성 (명명 규칙, 스타일)
   - 보안 취약점 (있는 경우)
4. 리뷰 결과를 .planning/phases/NN-*/NN-NN-SUMMARY.md에 기록
   - ## Review Findings 섹션: 발견사항 목록
   - ## Verdict 섹션: approved / approved-with-comments / revision-required
5. 완료 후 $sg-retro를 실행해 회고를 진행하세요.
```

**파일 생성 전 디렉토리 생성:**
`mkdir -p .agents/skills/sg-review`
  </action>
  <verify>
    <automated>! grep -q "superpowers:requesting-code-review" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-review/SKILL.md && echo "OK: no superpowers:requesting-code-review"</automated>
  </verify>
  <done>
- `.agents/skills/sg-review/SKILL.md`가 존재한다
- `superpowers:requesting-code-review` Skill 호출 코드가 없다 (grep 결과 0)
- `Platform Constraints` 블록이 있다
- prose 리뷰 절차(변경 파일 목록 → 각 파일 리뷰 → SUMMARY.md 기록)가 있다
- HANDOFF.md `review` 행 기록 로직이 있다
  </done>
</task>

<task type="auto">
  <name>Task 7: .agents/skills/sg-status/SKILL.md 신규 생성 (D-C1/C5)</name>
  <files>.agents/skills/sg-status/SKILL.md</files>
  <action>
`commands/sg-status.md`를 기반으로 `.agents/skills/sg-status/SKILL.md`를 신규 생성한다. 먼저 `commands/sg-status.md`를 읽어 전체 동작(STATE.md Phase 파싱 → HANDOFF.md 마지막 행 파싱 → next command 매핑 → 5개 라인 출력)을 파악한다.

**핵심 특징 (D-C5):**
sg-status는 GSD 위임 없이 HANDOFF.md + STATE.md 직접 파싱으로 완전 독립 실행 가능하다. GSD 설치 여부 확인 불필요. Superpowers도 사용하지 않는다.

**frontmatter:**
```yaml
---
name: sg-status
description: 현재 워크플로우 단계, 마지막 handoff 시각, 다음 권장 명령 표시
---
```

**<constraints> 블록 (D-C1):**
```
## Platform Constraints (Codex / Gemini CLI / Antigravity CLI)
- Superpowers 연동 없음: 이 스킬은 완전 독립 실행 가능합니다
- SubagentStop 미지원: 워크플로우 상태만 표시하므로 영향 없음
- AskUserQuestion 미지원: 출력만 수행하므로 영향 없음
```

**<process>:**

Step 1: STATE.md Phase 파싱 블록 (commands/sg-status.md Step 1 그대로 복제 — lock 패턴):
```bash
# --- BEGIN STATE.md Phase parsing block ---
PHASE_LINE=$(grep -E '^Phase:' .planning/STATE.md 2>/dev/null | head -1 | sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//')
[ -z "$PHASE_LINE" ] && PHASE_LINE="(none)"
PHASE_NUM=$(echo "$PHASE_LINE" | grep -oE '^[0-9]+' || echo "")
# --- END STATE.md Phase parsing block ---
```

Step 2: HANDOFF.md Stage 결정 (commands/sg-status.md Step 2 그대로 복제)

Step 3: 마지막 handoff timestamp 계산 (commands/sg-status.md Step 3 그대로 복제)

Step 4: hookify/ship 분기 next-phase 계산 (commands/sg-status.md Step 4 그대로 복제)

Step 5: Stage → next command 매핑 (commands/sg-status.md Step 5 기반). `/super-gsd:sg-*` 명령을 `$sg-*` 달러 문법으로 변경:
```bash
case "$STAGE_RAW" in
  init)
    if [ -n "$PHASE_NUM" ]; then
      NEXT_CMD="$sg-plan $PHASE_NUM"
    else
      NEXT_CMD="$sg-plan"
    fi
    ;;
  gsd-plan)    NEXT_CMD="$sg-execute" ;;
  superpowers) NEXT_CMD="$sg-review" ;;
  execute)     NEXT_CMD="$sg-review" ;;
  review)      NEXT_CMD="$sg-retro" ;;
  hookify)     NEXT_CMD="$sg-ship" ;;
  ship)
    if [ "$NEXT_PHASE_EXISTS" = "1" ]; then
      NEXT_CMD="$sg-plan $NEXT_PHASE"
    else
      NEXT_CMD="$sg-complete"
    fi
    ;;
  complete) NEXT_CMD="$sg-start" ;;
  *) NEXT_CMD="(unknown stage: $STAGE_RAW)" ;;
esac
```

주의: `execute` stage (Codex 직접 실행 모드)를 `review`로 라우팅하는 항목 추가.

Step 6: 5개 라인 출력 (commands/sg-status.md Step 6 그대로 복제):
```
Phase: <PHASE_LINE>
Stage: <STAGE_DISPLAY>
Last handoff: <LAST_TS>

Next: <NEXT_CMD>
```

**<success_criteria>:**
commands/sg-status.md success_criteria 기반. `$sg-*` 달러 문법으로 표현 수정. `execute` stage → `$sg-review` 라우팅 항목 추가.

**파일 생성 전 디렉토리 생성:**
`mkdir -p .agents/skills/sg-status`
  </action>
  <verify>
    <automated>ls /Users/gyuha/workspace/super-gsd/.agents/skills/sg-status/SKILL.md</automated>
  </verify>
  <done>
- `.agents/skills/sg-status/SKILL.md`가 존재한다
- STATE.md Phase 파싱 블록 (grep-sed-awk 파이프라인)이 보존되어 있다
- `$sg-*` 달러 문법이 NEXT_CMD 매핑에 사용된다
- `execute` stage가 `$sg-review`로 라우팅된다
- `Platform Constraints` 블록에 "완전 독립 실행 가능"이 명시된다
- 출력이 정확히 5개 라인(Phase/Stage/Last handoff/빈줄/Next)이다
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex 세션 → AGENTS.md | 자동 주입되므로 내용 정확성이 사용자 경험의 전부 |
| SKILL.md → 파일시스템 | .agents/skills/ 신규 생성, AGENTS.md 덮어쓰기 |
| sg-retro → .planning/lessons/ | 회고 결과 append (기존 경로 재사용) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | AGENTS.md | mitigate | GSD 재실행 시 GSD 마커 기반 섹션만 덮어씀. 마커 없는 버전이므로 GSD가 기존 섹션을 overwrite하지 않음 — 실제 재생성 충돌 위험 낮음 |
| T-14-02 | Information Disclosure | .agents/skills/sg-retro/SKILL.md | accept | numbered list fallback이 AskUserQuestion과 동일 옵션 텍스트 사용 — 내용 노출 위험 없음 |
| T-14-03 | Denial of Service | sg-execute 직접 구현 모드 | accept | 사용자가 직접 실행하는 모드이므로 무한 루프 등의 위험은 사용자 책임 범위 |
| T-14-SC | Tampering | npm/pip/cargo | accept | 이 phase는 패키지 설치 없음 — Markdown 파일만 생성 |
</threat_model>

<verification>
모든 task 완료 후 아래 명령으로 최종 검증한다:

```bash
# 1. AGENTS.md GSD 마커 제거 확인
grep -c "GSD:" /Users/gyuha/workspace/super-gsd/AGENTS.md
# 결과: 0

# 2. AGENTS.md 크기 확인
wc -c /Users/gyuha/workspace/super-gsd/AGENTS.md
# 결과: 8192 이하

# 3. 6개 SKILL.md 존재 확인
ls /Users/gyuha/workspace/super-gsd/.agents/skills/*/SKILL.md
# 결과: 6개 파일

# 4. AskUserQuestion 미사용 확인 (sg-retro)
grep -c "AskUserQuestion" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-retro/SKILL.md
# 결과: 0

# 5. Superpowers 호출 없음 확인 (sg-execute, sg-review)
grep -c "superpowers:" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-execute/SKILL.md
grep -c "superpowers:" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-review/SKILL.md
# 결과: 0, 0

# 6. Platform Constraints 블록 존재 확인
grep -l "Platform Constraints" /Users/gyuha/workspace/super-gsd/.agents/skills/*/SKILL.md
# 결과: 6개 파일 모두
```
</verification>

<success_criteria>
1. Codex 세션 시작 시 AGENTS.md가 자동 주입되고, `$sg-*` 달러 문법으로 워크플로우 단계가 안내된다
2. AGENTS.md에 SubagentStop 미지원 사실이 별도 "Platform Limitations" 섹션에 명시되고, sg-retro 수동 호출 안내가 포함된다
3. `.agents/skills/sg-retro/SKILL.md`가 AskUserQuestion 없이 numbered list fallback으로 실행되고 회고 결과를 `.planning/lessons/`에 저장한다
4. `.agents/skills/sg-{start,plan,execute,review,status}/SKILL.md` 5개가 각각 `Platform Constraints` 블록에 Superpowers/SubagentStop/AskUserQuestion 미지원을 명시한다
5. AGENTS.md 파일 크기가 8KiB(8,192바이트) 이하이다
6. GSD 마커(`<!-- GSD: -->`)가 AGENTS.md에 남아 있지 않다
</success_criteria>

<output>
완료 시 `.planning/phases/14-codex-agents-skills/14-01-SUMMARY.md`를 생성한다.
</output>
