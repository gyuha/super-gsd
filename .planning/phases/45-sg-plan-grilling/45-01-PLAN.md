---
phase: "45"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-plan/SKILL.md
  - .agents/skills/sg-plan/SKILL.md
autonomous: true
requirements:
  - GRILL-01
  - GRILL-02
  - GRILL-03
  - GRILL-04
  - GRILL-05
  - GRILL-06

must_haves:
  truths:
    - "sg-plan을 실행하면 gsd-discuss-phase 호출 전에 Claude가 사용자에게 모호함 해소 질문을 한 번에 하나씩 제시한다"
    - "각 질문에는 Claude가 권장 답변을 함께 제시한다"
    - "코드베이스로 답할 수 있는 질문은 사용자에게 묻지 않고 Claude가 직접 탐색해 해소한다"
    - "이전 답변이 다음 질문의 분기를 결정하는 설계 트리 방식으로 진행된다"
    - "Claude가 모든 모호함이 해소됐다고 판단하면 합의 요약을 제시하고, 사용자가 확정/추가 질문으로 종료를 결정한다 — Claude가 단독 종료하지 않는다"
    - "grill 합의 요약이 gsd-discuss-phase Agent 프롬프트에 inline으로 주입되어 후속 계획에 반영된다"
    - "skills/ 와 .agents/skills/ 두 파일이 같은 커밋에서 함께 변경된다"
  artifacts:
    - path: "skills/sg-plan/SKILL.md"
      provides: "Claude Code용 sg-plan — grill 단계(Step 1과 Step 2 사이) + discuss Agent 프롬프트 주입 + 업데이트된 success_criteria"
      contains: "grill"
    - path: ".agents/skills/sg-plan/SKILL.md"
      provides: "Codex/Gemini용 sg-plan 미러 — grill 단계(Step 1과 Step 1.5 사이) + discuss Agent 프롬프트 주입 + 업데이트된 success_criteria"
      contains: "grill"
  key_links:
    - from: "grill 합의 요약"
      to: "gsd-discuss-phase Agent 프롬프트"
      via: "inline 문단 주입 (D-14)"
      pattern: "locked context.*do not re-ask"
    - from: "skills/sg-plan/SKILL.md"
      to: ".agents/skills/sg-plan/SKILL.md"
      via: "동일 커밋 (D-15 pairwise sync)"
      pattern: "pairwise"
---

<objective>
`sg-plan`의 두 SKILL.md 파일(`.claude` 버전과 `.agents` 미러 버전)에 grill-me 원칙 기반의 선행 모호함 해소 단계를 삽입한다.

Purpose: gsd-discuss-phase subagent를 호출하기 전, sg-plan 메인 컨텍스트에서 Claude가 사용자에게 한 번에 하나씩 질문하여 계획 입력의 불확실성을 제거한다 (GRILL-01~06).

Output:
- `skills/sg-plan/SKILL.md` — grill 단계가 Step 1.5로 삽입됨 (Step 1 직후 / Step 2 직전)
- `.agents/skills/sg-plan/SKILL.md` — grill 단계가 Step 1.6으로 삽입됨 (Step 1 직후 / Step 1.5 이전)
- 두 파일의 `<success_criteria>` 블록에 grill 관련 기준 추가
- 두 파일의 gsd-discuss-phase Agent 프롬프트에 합의 요약 inline 주입 지시 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-sg-plan-grilling/45-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key decisions from 45-CONTEXT.md:
- D-01: grill 삽입 위치 = Step 1(phase 해소) 완료 직후, gsd-discuss-phase Agent 호출 직전
- D-02: .agents 버전은 grill을 Step 1.5(Visual Companion) 이전에 삽입
- D-03: .claude 버전에는 Visual Companion이 없음 — 구조 divergence 유지, grill만 삽입
- D-04/D-05: 한 번에 하나씩 질문, 각 질문마다 권장 답변 동반
- D-06: .claude는 AskUserQuestion, .agents는 번호 선택 프로즈 폴백 + 개방형 입력
- D-07: 질문·권장안·요약 산문은 사용자 입력 언어로, 머신 토큰은 영문 유지
- D-08/D-09: 코드베이스 탐색 우선 → 탐색 불가 시에만 사용자 질문 (2단계 판정)
- D-10: 고정 목록 아님 — 설계 트리를 따라 각 답변 후 가장 불확실하고 후속 결정을 가장 많이 좌우하는 항목을 다음 질문으로 선택
- D-11: Claude 단독 종료 금지 — 합의 요약 제시 + 사용자 확정/추가질문 게이트
- D-12: 합의 요약 = 번호 매긴 결정·제약 목록 + "이게 전부인가요? 더 다룰 게 있으면 알려주세요" + 종료 확인
- D-13/D-14: grill 합의는 별도 파일 없이 gsd-discuss-phase Agent 프롬프트에 inline 주입. 주입 문구: "The user and I have already grilled the ambiguities. Treat the following as locked context — do NOT re-ask: {요약}"
- D-15: 두 파일 같은 커밋에서 변경
</context>

<interfaces>
<!-- skills/sg-plan/SKILL.md 현재 구조 (Claude Code 버전) -->
<!-- Step 0: Prior lessons injection (line 23-40) -->
<!-- Step 1: Resolve phase (line 42-54) — bash block -->
<!-- [grill 삽입 지점: Step 1 직후, Step 2 직전] -->
<!-- Step 2: Print progress + Phase dir pre-create (line 56-76) -->
<!-- Step 2.1 Agent block: gsd-discuss-phase (line 67-76) -->
<!--   prompt 문자열 (line 72): "Your task is to run the GSD discuss-phase workflow..." -->
<!--   [D-14 주입 지점: 이 prompt 문자열 끝에 합의 요약 주입 문단 지시 추가] -->
<!-- Step 2.2: Validate discuss-phase output location (line 78-102) -->
<!-- Step 2.5: HANDOFF.md recording (line 104-122) -->
<!-- Step 3: gsd-plan-phase Skill (line 124-129) -->

<!-- .agents/skills/sg-plan/SKILL.md 현재 구조 (Codex/Gemini 버전) -->
<!-- Step 0: Prior lessons injection (line 30-47) -->
<!-- Step 1: Resolve phase (line 49-60) -->
<!-- [grill 삽입 지점: Step 1 직후, Step 1.5 이전] -->
<!-- Step 1.5: Visual Companion check (line 62-95) — AskUserQuestion 미지원 프로즈 폴백 패턴 사용 -->
<!--   패턴: 번호 선택 프롬프트 "1. ... / 2. ... / Enter your choice (1 or 2):" -->
<!-- Step 2: GSD availability check + 분기 (line 97-163) -->
<!--   2a. gsd-discuss-phase Agent block (line 110-117) -->
<!--     prompt (line 114): "Your task is to run the GSD discuss-phase workflow..." -->
<!--     [D-14 주입 지점: 이 prompt 문자열에 합의 요약 주입 지시 추가] -->
<!--   2b. HANDOFF.md recording -->
<!--   2c. gsd-plan-phase Skill -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: skills/sg-plan/SKILL.md에 grill 단계 삽입 (.claude 버전)</name>
  <files>skills/sg-plan/SKILL.md</files>
  <read_first>
    - skills/sg-plan/SKILL.md (수정 대상 전체)
    - .planning/phases/45-sg-plan-grilling/45-CONTEXT.md (D-01~D-15 전체, 특히 D-06 .claude AskUserQuestion 지시, D-14 주입 문구 지점)
  </read_first>
  <action>
Step 1(phase 해소)과 Step 2(진행 메시지 출력) 사이에 새 Step 1.5로 grill 단계를 삽입한다. 기존 Step 2 이후의 번호(2, 2.1, 2.2, 2.5, 3)는 그대로 유지한다.

**Step 1.5 내용 (per D-01, D-04, D-05, D-06, D-08, D-09, D-10, D-11, D-12, D-14):**

Step 제목: `1.5. **Ambiguity grilling.** Before calling gsd-discuss-phase, resolve planning ambiguities one question at a time in the main context.`

지시문에 다음 5개 규칙을 명시한다:

1. **Codebase-first rule (per D-08, D-09):** 질문을 사용자에게 던지기 전, 먼저 Read/Bash로 코드베이스에서 답을 찾을 수 있는지 자문한다. 파일 존재 여부·현재 구현·함수 시그니처·설정 값처럼 코드베이스에서 답이 가능한 항목은 직접 탐색해 해소하고 사용자에게 묻지 않는다. 오직 설계 의도·우선순위·UX 선호처럼 코드에 없는 정보만 사용자에게 질문한다.

2. **One-at-a-time rule (per D-04, D-05):** 사용자에게 질문할 때는 반드시 한 번에 하나씩만 묻는다. 각 질문에 Claude의 권장 답변을 함께 제시한다. 질문 수에 상한 없음.

3. **Design-tree traversal (per D-10):** 고정 질문 목록을 쓰지 않는다. 각 답변이 확정되면, 다음으로 가장 불확실하고 후속 결정을 가장 많이 좌우하는 항목을 다음 질문으로 선택한다. 설계 트리 의존성을 순차적으로 해소한다.

4. **AskUserQuestion mechanism (per D-06):** `.claude` 버전은 AskUserQuestion 도구로 질문한다. options에 권장 답변을 하나의 선택지로 포함하거나, description에 "권장: {권장안}" 형태로 명시한다.

5. **Termination gate — user-confirmed only (per D-11, D-12):** Claude가 모든 모호함이 해소됐다고 판단하면 단독으로 종료하지 않는다. 대신 번호 매긴 합의 요약(결정·제약 목록)을 제시하고, "이게 전부인가요? 더 다룰 내용이 있으면 알려주세요"를 명시적으로 묻는다. AskUserQuestion으로 두 가지 선택지를 제시한다: "확정 — gsd-discuss-phase 진행" vs "추가 질문 — 계속 grilling". 사용자가 "확정"을 선택해야만 Step 2로 진행한다.

**합의 요약의 gsd-discuss-phase 프롬프트 주입 (per D-13, D-14):**

grill 확정 후 합의 요약(GRILL_SUMMARY 변수)을 Step 2.1의 Agent 호출 prompt 문자열에 inline으로 추가하도록 지시한다. SKILL.md Step 2.1의 Agent prompt 설명에 다음 지시를 추가한다: "When constructing the Agent prompt, append the following paragraph at the end of the prompt string (replacing {GRILL_SUMMARY} with the actual grilled consensus text): 'The user and I have already grilled the ambiguities. Treat the following as locked context — do NOT re-ask: {GRILL_SUMMARY}'"

**언어 규칙 (per D-07):** grill 단계 지시문은 영문으로 작성하되(SKILL.md 지시문은 영문 관례), Claude가 사용자에게 표면화하는 질문·권장안·합의 요약 산문은 사용자 입력 언어로 표면화하라고 명시한다. 명령명(`/super-gsd:sg-plan`), 파일 경로, enum, phase 슬러그는 번역하지 않는다.

**`<success_criteria>` 업데이트:** 파일 끝의 `<success_criteria>` 블록에 다음 항목들을 추가한다:
- Step 1.5 grill 단계가 Step 1 완료 직후 실행되고 gsd-discuss-phase Agent 호출 직전에 완료된다
- 코드베이스 탐색으로 답할 수 있는 항목은 사용자에게 묻지 않고 직접 해소된다
- 질문은 한 번에 하나씩, 각 질문마다 권장 답변이 제시된다
- Claude가 모호함 해소를 판단해도 단독 종료하지 않고 합의 요약 + 사용자 확정 게이트를 거친다
- grill 합의 요약이 gsd-discuss-phase Agent prompt에 inline으로 주입된다
  </action>
  <verify>
    <automated>grep -n "grill" /Users/gyuha/workspace/super-gsd/skills/sg-plan/SKILL.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - skills/sg-plan/SKILL.md에 Step 1.5 grill 단계가 존재하고, grep으로 "grill"이 검색된다
    - Step 1.5가 Step 1 완료 직후, Step 2(진행 메시지 출력) 직전에 위치한다
    - Step 1.5 지시문이 다음 5가지를 명시한다: ① 코드베이스 우선 탐색 규칙 ② 한 번에 하나씩 + 권장 답변 동반 ③ 설계 트리 순차 해소 ④ AskUserQuestion 메커니즘 ⑤ 사용자 확정 종료 게이트 (Claude 단독 종료 금지)
    - gsd-discuss-phase Agent prompt 지시 부분에 "locked context" 또는 "do NOT re-ask" 문구가 포함된 inline 주입 지시가 있다
    - `<success_criteria>` 블록에 grill 관련 기준이 추가되어 있다
    - bash 스니펫이 추가된 경우 grep -E 사용 (grep -P 없음), macOS 이식성 준수
  </acceptance_criteria>
  <done>skills/sg-plan/SKILL.md에 grill 단계(Step 1.5)가 삽입되고, gsd-discuss-phase Agent 프롬프트에 합의 주입 지시가 추가되며, success_criteria가 갱신된 상태</done>
</task>

<task type="auto">
  <name>Task 2: .agents/skills/sg-plan/SKILL.md에 grill 단계 삽입 (.agents 미러 버전) — 같은 커밋</name>
  <files>.agents/skills/sg-plan/SKILL.md</files>
  <read_first>
    - .agents/skills/sg-plan/SKILL.md (수정 대상 전체 — 특히 Step 1.5 Visual Companion 프로즈 폴백 패턴 line 62-95을 grill의 .agents 질문 메커니즘 참고 모델로 활용)
    - .planning/phases/45-sg-plan-grilling/45-CONTEXT.md (D-02, D-06 .agents 개방형 프롬프트, D-12 프로즈 번호 선택 종료 게이트)
  </read_first>
  <action>
Step 1(phase 해소)과 Step 1.5(Visual Companion) 사이에 새 Step 1.6으로 grill 단계를 삽입한다 (per D-02: grill이 Step 1.5 이전). 기존 Step 1.5, Step 2, 2a, 2b, 2c 번호는 그대로 유지한다.

**Step 1.6 내용 (per D-02, D-04, D-05, D-06, D-08, D-09, D-10, D-11, D-12, D-14):**

Step 제목: `1.6. **Ambiguity grilling.** Before Visual Companion check and gsd-discuss-phase, resolve planning ambiguities one question at a time in the main context.`

지시문에 다음 5개 규칙을 명시한다:

1. **Codebase-first rule (per D-08, D-09):** 질문을 사용자에게 던지기 전, 먼저 Read/Bash로 코드베이스에서 답을 찾을 수 있는지 자문한다. 코드베이스 탐색으로 확인 가능한 항목(파일 존재 여부·현재 구현·함수 시그니처)은 직접 해소하고 사용자에게 묻지 않는다. 오직 설계 의도·우선순위·UX 선호처럼 코드에 없는 정보만 사용자에게 질문한다.

2. **One-at-a-time rule (per D-04, D-05):** 사용자에게 질문할 때는 반드시 한 번에 하나씩만 묻는다. 각 질문에 Claude의 권장 답변을 함께 제시한다. 질문 수에 상한 없음.

3. **Design-tree traversal (per D-10):** 고정 질문 목록을 쓰지 않는다. 각 답변이 확정되면, 다음으로 가장 불확실하고 후속 결정을 가장 많이 좌우하는 항목을 다음 질문으로 선택한다.

4. **Prose fallback mechanism (per D-06):** AskUserQuestion 미지원 플랫폼이므로 Step 1.5(Visual Companion)의 번호 선택 패턴을 답습한다. 각 질문은 다음 형식으로 출력한다:

   ```
   [sg-plan] Grilling question: {질문 내용}

   Recommended answer: {Claude의 권장 답변}
   Or type your own answer:
   ```

   사용자가 텍스트로 답변을 입력하면 그것을 답으로 수락하고 다음 질문으로 진행한다.

5. **Termination gate — user-confirmed only (per D-11, D-12):** Claude가 모든 모호함이 해소됐다고 판단하면 단독으로 종료하지 않는다. 번호 매긴 합의 요약(결정·제약 목록)을 출력하고, "이게 전부인가요? 더 다룰 내용이 있으면 알려주세요"를 명시적으로 묻는다. 프로즈 번호 선택 형식으로 제시한다:

   ```
   [sg-plan] Grilling complete — consensus summary:
   1. {결정 1}
   2. {결정 2}
   ...

   Is this everything? Let me know if there's more to cover.

   1. Confirm — proceed to gsd-discuss-phase
   2. More questions — continue grilling

   Enter your choice (1 or 2):
   ```

   사용자가 "1" 또는 "Confirm"을 입력해야만 Step 1.5로 진행한다.

**합의 요약의 gsd-discuss-phase 프롬프트 주입 (per D-13, D-14):**

grill 확정 후 합의 요약(GRILL_SUMMARY)을 Step 2a의 Agent 호출 prompt 문자열에 inline으로 추가하도록 지시한다. Step 2a의 Agent prompt 설명에 다음 지시를 추가한다: "When constructing the Agent prompt, append the following paragraph at the end of the prompt string (replacing {GRILL_SUMMARY} with the actual grilled consensus text): 'The user and I have already grilled the ambiguities. Treat the following as locked context — do NOT re-ask: {GRILL_SUMMARY}'"

**언어 규칙 (per D-07):** Claude가 사용자에게 표면화하는 질문·권장안·합의 요약 산문은 사용자 입력 언어로 표면화하라고 명시한다. 명령명, 파일 경로, enum, phase 슬러그는 번역하지 않는다.

**`<success_criteria>` 업데이트:** 파일 끝의 `<success_criteria>` 블록에 다음 항목들을 추가한다:
- Step 1.6 grill 단계가 Step 1 완료 직후, Step 1.5(Visual Companion) 이전에 실행된다
- 코드베이스 탐색으로 답할 수 있는 항목은 사용자에게 묻지 않고 직접 해소된다
- 질문은 한 번에 하나씩, 각 질문마다 권장 답변이 제시된다
- Claude가 단독 종료하지 않고 합의 요약 + 사용자 확정 게이트(프로즈 번호 선택)를 거친다
- grill 합의 요약이 gsd-discuss-phase Agent prompt(Step 2a)에 inline으로 주입된다

**커밋 시 필수 (per D-15):** 이 Task 2의 변경과 Task 1의 변경(skills/sg-plan/SKILL.md)을 반드시 같은 커밋에서 함께 스테이징하고 커밋한다. 두 파일을 분리된 커밋으로 나누지 않는다.
  </action>
  <verify>
    <automated>grep -n "grill" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-plan/SKILL.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - .agents/skills/sg-plan/SKILL.md에 grill 단계(Step 1.6)가 존재하고, grep으로 "grill"이 검색된다
    - Step 1.6이 Step 1 완료 직후, Step 1.5(Visual Companion) 이전에 위치한다
    - Step 1.6 지시문이 다음 5가지를 명시한다: ① 코드베이스 우선 탐색 규칙 ② 한 번에 하나씩 + 권장 답변 동반 ③ 설계 트리 순차 해소 ④ 프로즈 번호 선택 + 개방형 입력 폴백 메커니즘 ⑤ 사용자 확정 종료 게이트 (Claude 단독 종료 금지, 프로즈 번호 선택 형식)
    - Step 2a의 gsd-discuss-phase Agent prompt 지시에 "locked context" 또는 "do NOT re-ask" 문구가 포함된 inline 주입 지시가 있다
    - `<success_criteria>` 블록에 grill 관련 기준이 추가되어 있다
    - skills/sg-plan/SKILL.md와 .agents/skills/sg-plan/SKILL.md가 동일한 git 커밋에 포함되어 있다 (git log --oneline -1 -- skills/sg-plan/SKILL.md 와 .agents/skills/sg-plan/SKILL.md의 커밋 해시가 일치)
  </acceptance_criteria>
  <done>.agents/skills/sg-plan/SKILL.md에 grill 단계(Step 1.6)가 삽입되고, Step 2a gsd-discuss-phase Agent 프롬프트에 합의 주입 지시가 추가되며, success_criteria가 갱신되고, 두 파일이 같은 커밋으로 기록된 상태</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → sg-plan grill step | 사용자 답변이 grill 합의 요약으로 흡수되어 gsd-discuss-phase 프롬프트에 주입됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | grill 합의 요약 → gsd-discuss-phase 프롬프트 주입 | accept | 신뢰 경계가 동일 세션 내부 (사용자·Claude 간). 외부 입력이 gsd-discuss-phase를 덮어쓸 위험 없음. Non-invasive 구조상 gsd-discuss-phase 자체는 수정되지 않음 |
| T-45-02 | Spoofing | 사용자가 grill 중 "확정"을 허위로 선택해 모호함이 해소되지 않은 채 진행 | accept | grill의 목적은 사용자와 Claude 간 합의 품질 향상이며, 사용자가 의도적으로 건너뛰는 것은 사용자 자율 결정임. 기술적 강제 불필요 |
| T-45-SC | Tampering | npm/pip/cargo 패키지 설치 없음 | accept | 본 phase는 순수 SKILL.md 산문 편집. 패키지 설치 없음 |
</threat_model>

<verification>
두 파일 모두 변경 완료 후:

```bash
# 두 파일에 grill 단계가 존재하는지 확인
grep -c "grill" /Users/gyuha/workspace/super-gsd/skills/sg-plan/SKILL.md
grep -c "grill" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-plan/SKILL.md

# 두 파일의 grill 삽입 위치 확인 (Step 번호 기준)
grep -n "^1\.[5-9]\." /Users/gyuha/workspace/super-gsd/skills/sg-plan/SKILL.md
grep -n "^1\.[5-9]\." /Users/gyuha/workspace/super-gsd/.agents/skills/sg-plan/SKILL.md

# "locked context" 또는 "do NOT re-ask" 주입 문구 확인
grep -n "locked context\|do NOT re-ask" /Users/gyuha/workspace/super-gsd/skills/sg-plan/SKILL.md
grep -n "locked context\|do NOT re-ask" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-plan/SKILL.md

# 두 파일이 같은 커밋에 포함되었는지 확인
git -C /Users/gyuha/workspace/super-gsd log --oneline -1 -- skills/sg-plan/SKILL.md
git -C /Users/gyuha/workspace/super-gsd log --oneline -1 -- .agents/skills/sg-plan/SKILL.md

# macOS 이식성: grep -P 없는지 확인
grep -n "grep -P" /Users/gyuha/workspace/super-gsd/skills/sg-plan/SKILL.md
grep -n "grep -P" /Users/gyuha/workspace/super-gsd/.agents/skills/sg-plan/SKILL.md
```
</verification>

<success_criteria>
- skills/sg-plan/SKILL.md에 Step 1.5 grill 단계가 삽입됨 (Step 1 직후 / Step 2 직전)
- .agents/skills/sg-plan/SKILL.md에 Step 1.6 grill 단계가 삽입됨 (Step 1 직후 / Step 1.5 이전)
- 두 파일 모두 GRILL-01~06 요구사항을 충족하는 지시문을 포함: ① 코드베이스 우선 탐색 ② 한 번에 하나씩 + 권장 답변 ③ 설계 트리 순차 해소 ④ 플랫폼 적합 질문 메커니즘 ⑤ 사용자 확정 종료 게이트 ⑥ 합의 inline 주입
- gsd-discuss-phase Agent prompt에 "locked context — do NOT re-ask" 패턴의 합의 주입 지시가 두 파일 모두에 있음
- 두 파일의 `<success_criteria>` 블록에 grill 관련 기준이 추가됨
- skills/sg-plan/SKILL.md와 .agents/skills/sg-plan/SKILL.md가 동일 git 커밋에 포함됨
- 추가된 bash 스니펫에 grep -P 없음 (macOS 이식성 준수)
</success_criteria>

<output>
완료 후 `.planning/phases/45-sg-plan-grilling/45-01-SUMMARY.md`를 생성한다
</output>
