---
phase: 36-skills-skills
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-setup/SKILL.md
  - skills/sg-execute/SKILL.md
  - skills/sg-ui-plan/SKILL.md
  - skills/sg-plan/SKILL.md
  - skills/sg-lessons/SKILL.md
  - skills/sg-new/SKILL.md
  - skills/sg-complete/SKILL.md
  - skills/sg-status/SKILL.md
  - skills/sg-review/SKILL.md
autonomous: true
requirements:
  - I18N-01

must_haves:
  truths:
    - "skills/sg-setup/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-execute/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-ui-plan/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-plan/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-lessons/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-new/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-complete/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-status/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "skills/sg-review/SKILL.md contains zero Korean characters outside of bash code blocks"
    - "bash code block content (commands, flags, variable names) is unchanged in all 9 files"
    - "YAML frontmatter field names and structure are unchanged in all 9 files"
  artifacts:
    - path: "skills/sg-setup/SKILL.md"
      provides: "Translated sg-setup skill"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-execute/SKILL.md"
      provides: "Translated sg-execute skill"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-ui-plan/SKILL.md"
      provides: "Translated sg-ui-plan skill"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-plan/SKILL.md"
      provides: "Translated sg-plan skill"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-lessons/SKILL.md"
      provides: "Translated sg-lessons skill"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-new/SKILL.md"
      provides: "Translated sg-new skill"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-complete/SKILL.md"
      provides: "Translated sg-complete skill"
      contains: "no Korean characters in prose sections"
    - path: "skills/sg-status/SKILL.md"
      provides: "Translated sg-status skill (1 Korean line)"
      contains: "no Korean characters"
    - path: "skills/sg-review/SKILL.md"
      provides: "Translated sg-review skill (1 Korean line)"
      contains: "no Korean characters"
---

<objective>
Translate Korean prose content in the remaining 9 Korean-containing SKILL.md files to English. Per I18N-01, only prose text is translated — bash code block commands, flags, and variable names are never modified (text-only rule). This plan runs in parallel with Plan 36-01 (no file overlap).

Purpose: Complete I18N-01 coverage for all 14 Korean-containing skills/ SKILL.md files.
Output: 9 modified SKILL.md files with all Korean prose replaced by equivalent English text, bash code untouched, YAML frontmatter structure unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate sg-setup, sg-execute, and sg-ui-plan SKILL.md files (I18N-01)</name>
  <files>skills/sg-setup/SKILL.md, skills/sg-execute/SKILL.md, skills/sg-ui-plan/SKILL.md</files>
  <read_first>
    skills/sg-setup/SKILL.md
    skills/sg-execute/SKILL.md
    skills/sg-ui-plan/SKILL.md
  </read_first>
  <action>
Translate all Korean prose text in these three files to English. Apply TEXT-ONLY RULE throughout: bash code block content (commands, flags, variable names, string literals) is NEVER modified. Only prose text outside bash fences and inline-prose bash comments are translated.

For skills/sg-setup/SKILL.md:
- YAML frontmatter `description` field: "super-gsd 훅·스킬 파일을 현재 프로젝트에 복사 — Claude Code 인세션 인스톨러" → "Copy super-gsd hook and skill files to the current project — Claude Code in-session installer"
- YAML `argument-hint` field content: "--gemini: .gemini/settings.json 추가 복사, --force: 기존 파일 덮어쓰기" → "--gemini: also copy .gemini/settings.json, --force: overwrite existing files"
- <objective> block: translate the full Korean prose to English
- <execution_context> block: translate Korean prose
- Step 0 header "인수 파싱." → "Argument parsing."
- Step 1 header "PKG_ROOT 결정 (per D-01~D-04)." prose
- Step 2 header "플랫폼 감지 (per D-05~D-08)." prose and inline bash comments: "Gemini 환경 감지됨. .gemini/settings.json 도 복사합니다." → "Gemini environment detected. Also copying .gemini/settings.json.", "플랫폼 감지 불가 — 기본 파일 세트..." → "Platform not detected — copying default file set..."
- Step 4 header "파일 복사 실행 (per D-09~D-12, Bash 도구 사용)." prose
- Step 5 "완료 안내." and the completion message: "[sg-setup] 설치 완료. 다음 단계: /super-gsd:sg-execute 를 실행하여 현재 phase를 진행하세요." → "[sg-setup] Installation complete. Next step: run /super-gsd:sg-execute to proceed with the current phase."
- <success_criteria> items: item 1, 2, 3, 4, 5, 6 — translate Korean portions

For skills/sg-execute/SKILL.md:
- Step 8.5 comment "[TE-05a] wave 필드 없음 — 기존 순차 실행 경로 유지. v1.3 이전 동작 보존." → "[TE-05a] No wave field — keeping existing sequential execution path. Preserving v1.3 behavior."
- Inline comment "# wave 필드 존재 여부 확인 (하나라도 있으면 분석 진행)" → "# Check if wave field exists (proceed if at least one is found)"
- Comment "# 각 PLAN.md에서 wave 번호와 files_modified 추출" → "# Extract wave number and files_modified from each PLAN.md"
- Comment "# 결과: ..." → "# Result: ..."
- Comment "# wave 값 목록 (중복 제거, 정렬)" → "# Wave value list (deduplicated, sorted)"
- Comment "# 각 wave별로 plan 목록과 files 수집" → "# Collect plan list and files per wave"
- Comment "# files_modified 교집합이 있는 plan은 같은 그룹으로 강제 병합" → "# Plans with overlapping files_modified are force-merged into the same group"
- echo string "병렬 그룹 감지 안됨 — 기존 순차 실행" → "No parallel groups detected — using existing sequential execution"
- echo string "병렬 그룹 ${GROUP_COUNT}개 감지 — parallel_groups.json 저장 완료" → "${GROUP_COUNT} parallel group(s) detected — parallel_groups.json saved"
- Step 9 comment "# HANDOFF_TO 결정 및 HANDOFF.md 기록" → "# Determine HANDOFF_TO and record HANDOFF.md"
- Step 9 "라우팅 분기." prose → "Routing branch."
- echo string "=== 병렬 실행 경로 선택: ${GROUP_COUNT}개 그룹 감지 ===" → "=== Parallel execution path selected: ${GROUP_COUNT} group(s) detected ==="
- Step 7.5 header "HANDOFF.md 자동 초기화." → "HANDOFF.md auto-initialization."
- Step 8 header "Append HANDOFF.md row (변수 계산)." → "Append HANDOFF.md row (variable computation)."

For skills/sg-ui-plan/SKILL.md:
- <objective> block: translate full Korean prose
- Step 1 header and bash comment "Phase resolve. $ARGUMENTS가 있으면..." → "Phase resolve. If $ARGUMENTS is provided..."
- Error message "[sg-ui-plan] Error: PHASE_NUM을 결정할 수 없습니다. 명시적으로 전달하세요:" → "[sg-ui-plan] Error: Cannot determine PHASE_NUM. Pass it explicitly:"
- Step 2 "PHASE_SECTION 추출." and warn message "[sg-ui-plan] WARN: ROADMAP에서 Phase $PHASE_NUM 섹션을 찾을 수 없습니다. 빈 컨텍스트로 brainstorming을 실행합니다." → "[sg-ui-plan] WARN: Phase $PHASE_NUM section not found in ROADMAP. Running brainstorming with empty context."
- Step 3 echo "[sg-ui-plan] Phase $PHASE_NUM UI 설계 brainstorming 시작..." → "[sg-ui-plan] Starting UI design brainstorming for Phase $PHASE_NUM..."
- Agent prompt Korean text: "Phase $PHASE_NUM UI 설계를 진행합니다..." → "Proceeding with Phase $PHASE_NUM UI design..." (translate the args string passed to brainstorming)
- Agent error message "[sg-ui-plan] brainstorming 실패." → "[sg-ui-plan] Brainstorming failed."
- Note "brainstorming이 UI 설계 전용 명령의 핵심이므로 실패 시 중단한다." → "Brainstorming is the core of this UI design command — abort on failure."
- Step 4 header and "[sg-ui-plan] HANDOFF.md에 ui-plan 기록 완료." → "[sg-ui-plan] ui-plan recorded in HANDOFF.md."
- Final message "[sg-ui-plan] UI 설계 brainstorming 완료. 다음 단계: /super-gsd:sg-execute" → "[sg-ui-plan] UI design brainstorming complete. Next step: /super-gsd:sg-execute"
- <success_criteria> items with Korean: items 1-5, translate Korean portions
  </action>
  <verify>
    <automated>grep -c '[가-힣]' /Users/gyuha/workspace/super-gsd/skills/sg-setup/SKILL.md && grep -c '[가-힣]' /Users/gyuha/workspace/super-gsd/skills/sg-execute/SKILL.md && grep -c '[가-힣]' /Users/gyuha/workspace/super-gsd/skills/sg-ui-plan/SKILL.md</automated>
  </verify>
  <done>All three files return 0 from grep -c '[가-힣]'. YAML frontmatter field names unchanged. Bash code content unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Translate sg-plan, sg-lessons, sg-new, sg-complete, sg-status, sg-review SKILL.md files (I18N-01)</name>
  <files>skills/sg-plan/SKILL.md, skills/sg-lessons/SKILL.md, skills/sg-new/SKILL.md, skills/sg-complete/SKILL.md, skills/sg-status/SKILL.md, skills/sg-review/SKILL.md</files>
  <read_first>
    skills/sg-plan/SKILL.md
    skills/sg-lessons/SKILL.md
    skills/sg-new/SKILL.md
    skills/sg-complete/SKILL.md
    skills/sg-status/SKILL.md
    skills/sg-review/SKILL.md
  </read_first>
  <action>
Translate all Korean prose text in these six files to English. Apply TEXT-ONLY RULE: bash code block content is NEVER modified.

For skills/sg-plan/SKILL.md:
- Step 0 header "Prior lessons 주입." → "Prior lessons injection."
- Step 0 prose about lessons display: "파일이 없으면 이 단계를 조용히 건너뛴다." → "If no files exist, skip this step silently."
- Step 2.1 header "Phase 디렉토리 pre-create." prose: "discuss-phase 실행 전, Phase 디렉토리가 없으면 임시 슬러그로 미리 생성한다. 이미 있으면 건너뛴다:" → "Before running discuss-phase, pre-create Phase directory with a temporary slug if it does not exist. Skip if already present:"
- bash echo "[sg-plan] Pre-created .planning/phases/${PHASE_PAD}-new-phase for discuss-phase output" — already English
- Step 2.2 header "discuss-phase 출력 위치 검증." bash comments: "-new-phase 플레이스홀더를 제외하고 실제 디렉토리에서만 확인" → "Exclude -new-phase placeholder, check only real directories", "-new-phase 플레이스홀더보다 실제 슬러그 디렉토리를 우선 선택" → "Prefer real slug directory over -new-phase placeholder", "-new-phase 플레이스홀더가 비어 있으면 삭제" → "Delete -new-phase placeholder if empty"
- Step 2.5 header "HANDOFF.md에 gsd-plan 행 idempotent 기록." prose → "Idempotent gsd-plan row recording in HANDOFF.md."
- Step 2.5 prose: "gsd-plan-phase가 terminal Skill이므로 제어가 반환되지 않는다. 호출 직전이 기록 가능한 최후 시점이다. 동일 phase의 gsd-plan 행이 이미 있으면 skip한다:" → "gsd-plan-phase is a terminal Skill so control does not return. The moment immediately before invocation is the last possible point to record. Skip if a gsd-plan row for the same phase already exists:"
- <success_criteria> items with Korean: items 0, 2, 3, 4, 5, 6 — translate Korean portions

For skills/sg-lessons/SKILL.md:
- <objective> block: ".planning/lessons/ 디렉토리의 모든 Markdown 파일을 읽어 순서대로 출력한다. 사용자가 /super-gsd:sg-plan을 실행하기 전에 이전 lessons를 확인하거나 컨텍스트로 활용할 수 있도록 한다." → "Read all Markdown files in the .planning/lessons/ directory and print them in order. Allows the user to review prior lessons or use them as context before running /super-gsd:sg-plan."
- Step 0 header "milestone 필터 확인." → "Milestone filter check."
- Step 0 bash echo "No milestone archive found" — already English; "Run /super-gsd:sg-complete to create the archive when closing a milestone." — already English; "Milestone lessons loaded:" — already English
- Step 2 header and prose: "$ARGUMENTS로 phase 필터 적용. $ARGUMENTS가 비어 있지 않으면 해당 phase 번호로 시작하는 파일만 남긴다. `phase-03`, `03`, `3` 등 다양한 형식을 숫자로 정규화한다:" → "Apply phase filter from $ARGUMENTS. If $ARGUMENTS is non-empty, keep only files starting with that phase number. Normalize various formats like `phase-03`, `03`, `3` to a number:"
- Step 3 header "각 파일 내용 출력. 파일명을 헤더로 표시하고 내용을 출력한다:" → "Print each file's content. Display filename as header then content:"
- Step 4 "안내 메시지 출력:" → "Print guidance message:"

For skills/sg-new/SKILL.md:
- The recommendation block display text contains Korean: "/super-gsd:sg-plan ${NEXT_PHASE}   ← lessons 주입 + discuss + plan 자동 체인 (권장)" → "/super-gsd:sg-plan ${NEXT_PHASE}   ← lessons injection + discuss + plan auto-chain (recommended)"
- "또는:" → "or:"
- "자동 진행 (HANDOFF/STATE 기반 라우팅)" → "auto-advance (HANDOFF/STATE-based routing)"
- "raw GSD discuss만" → "raw GSD discuss only"
- "raw GSD plan만" → "raw GSD plan only"
- Note: The display block in Step 2's bash block is a string printed by echo, so it's user-visible output — translate it. The surrounding bash structure (if, echo, NEXT_PHASE variable) is unchanged.

For skills/sg-complete/SKILL.md:
- Step 1.3 header "Lessons archive (milestone close)." bash comment and prose: "STATE.md에서 milestone 버전을 읽고 lessons_ranker.cjs --archive를 실행한다. 실패해도 sg-complete를 차단하지 않는다:" → "Read milestone version from STATE.md and run lessons_ranker.cjs --archive. Failure does not block sg-complete:"
- bash warn messages: "[warn] sg-complete: milestone version not found in STATE.md — skipping lessons archive" — already English; "[sg-complete] Archiving lessons to..." — already English; "[warn] lessons archive failed — continuing" — already English
- <success_criteria> item 5: "Step 1.3이 STATE.md에서 milestone 버전을 읽어 lessons archive를 실행한다. 버전 읽기 실패 시 warn만 출력하고 Step 2(Skill 호출)로 진행한다." → "Step 1.3 reads the milestone version from STATE.md and runs lessons archive. On version read failure, only a warn is emitted and execution proceeds to Step 2 (Skill invocation)."

For skills/sg-status/SKILL.md:
- Single Korean line in <process>: The note at line 104 "상대시각 환산 (예: `N 일 전`/`N hr 전` 형식) 절대 금지 — D-06 lock. 절대시각 그대로 사용." within sg-start is already handled in Plan 36-01. In sg-status, check for any Korean in process prose — specifically the step headers should be verified. The primary Korean content is just a bash comment about absolute timestamp prohibition which may not appear in sg-status. Read the file and translate any Korean prose found.

For skills/sg-review/SKILL.md:
- Step 3.9 header "HANDOFF.md에 review 행 기록." → "Record review row in HANDOFF.md."
- The prose "superpowers:requesting-code-review는 terminal Skill이므로 호출 직전이 기록 가능한 최후 시점이다:" → "superpowers:requesting-code-review is a terminal Skill so the moment immediately before invocation is the last possible point to record:"
  </action>
  <verify>
    <automated>for f in skills/sg-plan/SKILL.md skills/sg-lessons/SKILL.md skills/sg-new/SKILL.md skills/sg-complete/SKILL.md skills/sg-status/SKILL.md skills/sg-review/SKILL.md; do count=$(grep -c '[가-힣]' /Users/gyuha/workspace/super-gsd/$f 2>/dev/null || echo 0); echo "$f: $count"; done</automated>
  </verify>
  <done>All six files return 0 from grep -c '[가-힣]'. YAML frontmatter field names unchanged. Bash code content unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file edit → SKILL.md content | Translation must not alter functional instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Tampering | bash code blocks in SKILL.md files | mitigate | text-only rule: verify with grep after each file edit |
| T-36-04 | Tampering | YAML frontmatter description/argument-hint fields | mitigate | translate content only, never field names |
</threat_model>

<verification>
After both tasks complete:
```bash
for f in skills/sg-setup/SKILL.md skills/sg-execute/SKILL.md skills/sg-ui-plan/SKILL.md skills/sg-plan/SKILL.md skills/sg-lessons/SKILL.md skills/sg-new/SKILL.md skills/sg-complete/SKILL.md skills/sg-status/SKILL.md skills/sg-review/SKILL.md; do
  count=$(grep -c '[가-힣]' "$f" 2>/dev/null || echo 0)
  echo "$f: $count Korean chars"
done
```
All files must report 0.
</verification>

<success_criteria>
- All 9 SKILL.md files contain zero Korean characters (grep -c '[가-힣]' returns 0 for each)
- bash code block content (commands, flags, variable names) is unchanged
- YAML frontmatter field names and structure are unchanged
- No functional behavior changes — only language of prose text replaced
</success_criteria>

<output>
Create `.planning/phases/36-skills-skills/36-02-SUMMARY.md` when done.
</output>
