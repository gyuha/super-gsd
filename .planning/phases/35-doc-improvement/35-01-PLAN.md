---
phase: 35-doc-improvement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [README.md, AGENTS.md, README.ko.md]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]

must_haves:
  truths:
    - "README.md Codex 섹션에 npx @gyuha/super-gsd install 단일 명령이 있고 git clone 블록이 없다"
    - "README.md Gemini 섹션에 npx @gyuha/super-gsd install --gemini 단일 명령이 있고 git clone 블록이 없다"
    - "README.md Verify install에 Claude Code / Codex / Gemini 서브섹션이 존재한다"
    - "AGENTS.md Quick Start에 Step 0 설치 블록이 Step 1 앞에 있다"
    - "README.ko.md Codex/Gemini 섹션이 영문과 의미 동일한 한글로 교체되어 있다"
    - "README.ko.md 설치 확인에 Codex/Gemini 한글 서브섹션이 추가되어 있다"
  artifacts:
    - path: "README.md"
      provides: "Codex/Gemini npx 설치 섹션 + Verify 서브섹션"
      contains: "npx @gyuha/super-gsd install"
    - path: "AGENTS.md"
      provides: "Step 0 설치 블록"
      contains: "Step 0"
    - path: "README.ko.md"
      provides: "한글 npx 설치 섹션 + 설치 확인 서브섹션"
      contains: "npx @gyuha/super-gsd install"
  key_links:
    - from: "README.md Codex section"
      to: "AGENTS.md"
      via: "See AGENTS.md for the full workflow"
      pattern: "AGENTS\\.md"
    - from: "README.ko.md"
      to: "README.md"
      via: "한글 동기화 (의미 동일)"
      pattern: "npx @gyuha/super-gsd install"
---

<objective>
README.md, AGENTS.md, README.ko.md 세 문서의 Codex/Gemini 설치 섹션을 기존 4단계 `git clone + cp` 시퀀스에서 단일 `npx @gyuha/super-gsd install` 명령으로 교체하고, Verify install 섹션에 플랫폼별 검증 서브섹션을 추가한다. 코드 변경은 없으며 순수 문서 업데이트다.

Purpose: 신규 사용자가 설치 시 복잡한 git clone + 수동 복사 과정 없이 단일 npx 명령으로 시작할 수 있도록 온보딩 경험을 개선한다.
Output: 세 문서 파일 수정 — git clone 블록 제거, npx 명령 추가, Verify 서브섹션 추가, AGENTS.md Step 0 추가.
</objective>

<execution_context>
@/Users/gyuha/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gyuha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gyuha/workspace/super-gsd/.planning/PROJECT.md
@/Users/gyuha/workspace/super-gsd/.planning/ROADMAP.md
@/Users/gyuha/workspace/super-gsd/.planning/STATE.md
@/Users/gyuha/workspace/super-gsd/.planning/phases/35-doc-improvement/35-CONTEXT.md
@/Users/gyuha/workspace/super-gsd/README.md
@/Users/gyuha/workspace/super-gsd/AGENTS.md
@/Users/gyuha/workspace/super-gsd/README.ko.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md — Codex/Gemini 설치 섹션 교체 (DOC-01)</name>
  <files>README.md</files>
  <action>
README.md의 두 설치 섹션을 Edit 도구로 교체한다. 두 편집은 파일의 서로 다른 위치를 수정하므로 순서대로 실행한다.

**편집 1 — Codex 섹션 (lines 190-204):**

old_string:
```
### Codex

```bash
git clone https://github.com/gyuha/super-gsd.git ~/super-gsd
cd your-project
# Copy hook config, hook scripts, and agent skills (all three are required):
mkdir -p .codex
cp ~/super-gsd/.codex/hooks.json .codex/hooks.json
cp -r ~/super-gsd/hooks .
cp -r ~/super-gsd/.agents .
```

`.codex/hooks.json` registers Stop and PreToolUse hooks automatically. The `hooks/` directory contains the Node.js scripts (CommonJS .cjs) that the hooks invoke. The `.agents/` directory contains the `$sg-*` skills — omitting it means no skills are available. Use `$sg-retro`, `$sg-plan`, `$sg-execute`, `$sg-review`, `$sg-start`, `$sg-status` skill syntax. See `AGENTS.md` for the full workflow.

> **Note:** The Stop hook prints a `Run $sg-*` reminder message — it does not auto-invoke the next skill. You must run each `$sg-*` command manually after each stage.
```

new_string:
```
### Codex

```bash
npx @gyuha/super-gsd install
```

This installs `.codex/hooks.json` (Stop and PreToolUse hooks), `hooks/` (Node.js .cjs scripts), and `.agents/skills/` (`$sg-*` skills). Use `$sg-retro`, `$sg-plan`, `$sg-execute`, `$sg-review`, `$sg-start`, `$sg-status` skill syntax. See `AGENTS.md` for the full workflow.

> **Note:** The Stop hook prints a `Run $sg-*` reminder message — it does not auto-invoke the next skill. You must run each `$sg-*` command manually after each stage.

> **Tip:** Inside a Codex session, you can also run `$sg-setup` to install directly without leaving the session.
```

**편집 2 — Gemini 섹션 (lines 206-222):**

old_string:
```
### Gemini / Antigravity CLI

Gemini CLI is supported. Antigravity CLI compatibility has not been independently verified — see `.planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md`.

```bash
git clone https://github.com/gyuha/super-gsd.git ~/super-gsd
cd your-project
# Copy hook config, hook scripts, and agent skills (all three are required):
mkdir -p .gemini
cp ~/super-gsd/.gemini/settings.json .gemini/settings.json
cp -r ~/super-gsd/hooks .
cp -r ~/super-gsd/.agents .
```

`.gemini/settings.json` registers SessionEnd and BeforeTool hooks. The `hooks/` directory contains the Node.js scripts (CommonJS .cjs) that the hooks invoke. The `.agents/` directory contains the `$sg-*` skills — omitting it means no skills are available. Use `.agents/skills/` skills. See `AGENTS.md` for the full workflow.

> **Note:** The SessionEnd hook prints a `Run $sg-*` reminder message — it does not auto-invoke the next skill. You must run each `$sg-*` command manually after each stage.
```

new_string:
```
### Gemini / Antigravity CLI

Gemini CLI is supported. Antigravity CLI compatibility has not been independently verified — see `.planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md`.

```bash
npx @gyuha/super-gsd install --gemini
```

This installs `.gemini/settings.json` (SessionEnd and BeforeTool hooks), `hooks/` (Node.js .cjs scripts), and `.agents/skills/` (`$sg-*` skills). Use `.agents/skills/` skills. See `AGENTS.md` for the full workflow.

> **Note:** The SessionEnd hook prints a `Run $sg-*` reminder message — it does not auto-invoke the next skill. You must run each `$sg-*` command manually after each stage.

> **Tip:** Inside a Gemini session, you can also run `$sg-setup --gemini` to install directly without leaving the session.
```

두 편집 모두 D-01(설명 텍스트 1-2줄 요약 유지), D-03(명령어 정확), D-04(git clone 제거 + $sg-setup Note 추가) 결정을 구현한다.
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -c "git clone" README.md | grep -q "^0$" && echo "PASS: no git clone" || echo "FAIL: git clone still present"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -c "npx @gyuha/super-gsd install" README.md | grep -qE "^[2-9]" && echo "PASS: npx command present (2+)" || echo "FAIL: npx command missing"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "install --gemini" README.md && echo "PASS: --gemini flag present" || echo "FAIL: --gemini missing"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "sg-setup" README.md && echo "PASS: sg-setup tip present" || echo "FAIL: sg-setup tip missing"</automated>
  </verify>
  <done>README.md에서 git clone 블록이 사라지고, Codex 섹션에 `npx @gyuha/super-gsd install`, Gemini 섹션에 `npx @gyuha/super-gsd install --gemini` 명령이 각각 1회씩 존재하며, 각 섹션 하단에 `$sg-setup` Tip Note 블록이 있다.</done>
</task>

<task type="auto">
  <name>Task 2: README.md — Verify install 서브섹션 추가 (DOC-02)</name>
  <files>README.md</files>
  <action>
README.md의 `## Verify install` 섹션을 Edit 도구로 교체한다. 기존 단일 번호 목록을 Claude Code / Codex / Gemini 세 서브섹션으로 분리한다 (per D-05, D-06, D-07).

old_string:
```
## Verify install

After installation, confirm `super-gsd` loaded cleanly and your existing tools still work.

1. Run `/plugin list` and confirm that `super-gsd` appears in the listing with name, version, and description matching `.claude-plugin/plugin.json`.
2. Run `/gsd-progress` (or any other GSD command) and confirm GSD responds normally — this proves GSD remains intact and unmodified.
3. Open the `Skill` tree and confirm that `superpowers:*` skills are still discoverable and invokable — this proves Superpowers remains intact and unmodified.

If all three checks pass, `super-gsd` is installed correctly and non-invasively.
```

new_string:
```
## Verify install

After installation, confirm `super-gsd` loaded cleanly and your existing tools still work.

### Claude Code

1. Run `/plugin list` and confirm that `super-gsd` appears in the listing with name, version, and description matching `.claude-plugin/plugin.json`.
2. Run `/gsd-progress` (or any other GSD command) and confirm GSD responds normally — this proves GSD remains intact and unmodified.
3. Open the `Skill` tree and confirm that `superpowers:*` skills are still discoverable and invokable — this proves Superpowers remains intact and unmodified.

### Codex

1. `cat .codex/hooks.json` — confirm hooks.json exists
2. `ls hooks/*.cjs` — confirm hook scripts exist
3. `ls .agents/skills/` — confirm skills directory exists
4. Run `$sg-status` — confirm skill responds

### Gemini

1. `cat .gemini/settings.json` — confirm settings.json exists
2. `ls hooks/*.cjs` — confirm hook scripts exist
3. `ls .agents/skills/` — confirm skills directory exists
4. Run `$sg-status` — confirm skill responds

If checks pass for your platform, `super-gsd` is installed correctly.
```
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "### Claude Code" README.md && echo "PASS: Claude Code subsection" || echo "FAIL: Claude Code subsection missing"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "### Codex" README.md && echo "PASS: Codex subsection" || echo "FAIL: Codex subsection missing"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "### Gemini" README.md && echo "PASS: Gemini subsection" || echo "FAIL: Gemini subsection missing"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "cat .codex/hooks.json" README.md && echo "PASS: Codex verify step present" || echo "FAIL: Codex verify step missing"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "cat .gemini/settings.json" README.md && echo "PASS: Gemini verify step present" || echo "FAIL: Gemini verify step missing"</automated>
  </verify>
  <done>README.md Verify install 섹션이 Claude Code / Codex / Gemini 세 서브섹션(### 헤딩)으로 분리되어 있고, Codex 서브섹션은 4단계(cat hooks.json, ls hooks, ls .agents/skills/, $sg-status), Gemini 서브섹션은 4단계(cat settings.json, ls hooks, ls .agents/skills/, $sg-status)를 포함한다.</done>
</task>

<task type="auto">
  <name>Task 3: AGENTS.md Step 0 추가 (DOC-03)</name>
  <files>AGENTS.md</files>
  <action>
AGENTS.md의 Quick Start 섹션에 현재 `**Step 1`보다 앞에 `**Step 0`을 추가한다 (per D-08, D-09). Edit 도구 사용.

old_string:
```
## Quick Start

**Step 1: 사전 조건 확인**
```

new_string:
```
## Quick Start

**Step 0: 설치**

```bash
# 터미널에서 (세션 외부):
npx @gyuha/super-gsd install          # Codex 기본
npx @gyuha/super-gsd install --gemini # Gemini 추가
```

> **또는 세션 내부에서:** `$sg-setup` (Codex) / `$sg-setup --gemini` (Gemini)

**Step 1: 사전 조건 확인**
```

기존 Step 1~3과 Platform Limitations 섹션은 변경하지 않는다 (per D-10).
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "Step 0" AGENTS.md && echo "PASS: Step 0 present" || echo "FAIL: Step 0 missing"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "npx @gyuha/super-gsd install" AGENTS.md && echo "PASS: npx command in AGENTS.md" || echo "FAIL: npx command missing from AGENTS.md"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "Step 1: 사전 조건 확인" AGENTS.md && echo "PASS: Step 1 still present" || echo "FAIL: Step 1 was removed"</automated>
  </verify>
  <done>AGENTS.md Quick Start에 `**Step 0: 설치**` 블록이 `**Step 1`보다 앞에 위치하며, npx 두 명령(기본 + --gemini)과 세션 내 $sg-setup 대안 Note가 포함되어 있다. Step 1~3은 변경 없이 그대로다.</done>
</task>

<task type="auto">
  <name>Task 4: README.ko.md 설치 섹션 및 설치 확인 동기화 (DOC-04)</name>
  <files>README.ko.md</files>
  <action>
README.ko.md를 Edit 도구로 세 곳 수정한다. 기술 명령어는 영문 그대로 유지하고 설명 텍스트만 한글로 작성한다 (per D-11, D-12).

**편집 1 — Codex 섹션 (lines 189-203):**

old_string:
```
### Codex

```bash
git clone https://github.com/gyuha/super-gsd.git ~/super-gsd
cd your-project
# 훅 설정 파일, 훅 스크립트, 에이전트 스킬을 모두 복사 (셋 다 필수):
mkdir -p .codex
cp ~/super-gsd/.codex/hooks.json .codex/hooks.json
cp -r ~/super-gsd/hooks .
cp -r ~/super-gsd/.agents .
```

`.codex/hooks.json`이 Stop 및 PreToolUse 훅을 자동 등록한다. `hooks/` 디렉터리에는 훅이 실행하는 Node.js 스크립트(CommonJS .cjs)가 포함되어 있다. `.agents/` 디렉터리에는 `$sg-*` 스킬이 포함되어 있으며, 이를 생략하면 사용 가능한 스킬이 없다. `$sg-retro`, `$sg-plan`, `$sg-execute`, `$sg-review`, `$sg-start`, `$sg-status` 스킬 문법을 사용한다. 전체 워크플로우는 `AGENTS.md`를 참조한다.

> **참고:** Stop 훅은 `Run $sg-*` 안내 메시지를 출력할 뿐이며, 다음 스킬을 자동으로 invoke하지 않는다. 각 단계 후 `$sg-*` 명령을 직접 실행해야 한다.
```

new_string:
```
### Codex

```bash
npx @gyuha/super-gsd install
```

`.codex/hooks.json` (Stop 및 PreToolUse 훅), `hooks/` (Node.js .cjs 스크립트), `.agents/skills/` (`$sg-*` 스킬)을 설치한다. `$sg-retro`, `$sg-plan`, `$sg-execute`, `$sg-review`, `$sg-start`, `$sg-status` 스킬 문법을 사용한다. 전체 워크플로우는 `AGENTS.md`를 참조한다.

> **참고:** Stop 훅은 `Run $sg-*` 안내 메시지를 출력할 뿐이며, 다음 스킬을 자동으로 invoke하지 않는다. 각 단계 후 `$sg-*` 명령을 직접 실행해야 한다.

> **팁:** Codex 세션 내부에서 `$sg-setup`을 실행하면 세션을 종료하지 않고 설치할 수 있다.
```

**편집 2 — Gemini 섹션 (lines 205-221):**

old_string:
```
### Gemini / Antigravity CLI

Gemini CLI는 지원된다. Antigravity CLI 호환성은 독립적으로 검증되지 않았다 — `.planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md` 참조.

```bash
git clone https://github.com/gyuha/super-gsd.git ~/super-gsd
cd your-project
# 훅 설정 파일, 훅 스크립트, 에이전트 스킬을 모두 복사 (셋 다 필수):
mkdir -p .gemini
cp ~/super-gsd/.gemini/settings.json .gemini/settings.json
cp -r ~/super-gsd/hooks .
cp -r ~/super-gsd/.agents .
```

`.gemini/settings.json`이 SessionEnd 및 BeforeTool 훅을 등록한다. `hooks/` 디렉터리에는 훅이 실행하는 Node.js 스크립트(CommonJS .cjs)가 포함되어 있다. `.agents/` 디렉터리에는 `$sg-*` 스킬이 포함되어 있으며, 이를 생략하면 사용 가능한 스킬이 없다. `.agents/skills/` 스킬을 사용한다. 전체 워크플로우는 `AGENTS.md`를 참조한다.

> **참고:** SessionEnd 훅은 `Run $sg-*` 안내 메시지를 출력할 뿐이며, 다음 스킬을 자동으로 invoke하지 않는다. 각 단계 후 `$sg-*` 명령을 직접 실행해야 한다.
```

new_string:
```
### Gemini / Antigravity CLI

Gemini CLI는 지원된다. Antigravity CLI 호환성은 독립적으로 검증되지 않았다 — `.planning/phases/15-platform-hooks-python-fix/15-01-VERIFICATION.md` 참조.

```bash
npx @gyuha/super-gsd install --gemini
```

`.gemini/settings.json` (SessionEnd 및 BeforeTool 훅), `hooks/` (Node.js .cjs 스크립트), `.agents/skills/` (`$sg-*` 스킬)을 설치한다. `.agents/skills/` 스킬을 사용한다. 전체 워크플로우는 `AGENTS.md`를 참조한다.

> **참고:** SessionEnd 훅은 `Run $sg-*` 안내 메시지를 출력할 뿐이며, 다음 스킬을 자동으로 invoke하지 않는다. 각 단계 후 `$sg-*` 명령을 직접 실행해야 한다.

> **팁:** Gemini 세션 내부에서 `$sg-setup --gemini`를 실행하면 세션을 종료하지 않고 설치할 수 있다.
```

**편집 3 — 설치 확인 섹션 (lines 232-240):**

old_string:
```
## 설치 확인

설치 후 `super-gsd`가 올바르게 로드됐는지, 기존 도구들이 정상 작동하는지 확인한다.

1. `/plugin list`를 실행하여 `super-gsd`가 `.claude-plugin/plugin.json`의 이름, 버전, 설명과 일치하는지 확인한다.
2. `/gsd-progress`(또는 다른 GSD 명령)를 실행하여 GSD가 정상 응답하는지 확인한다 — GSD가 수정되지 않았음을 증명한다.
3. Skill 트리를 열어 `superpowers:*` 스킬이 발견 가능하고 호출 가능한지 확인한다 — Superpowers가 수정되지 않았음을 증명한다.

세 가지 확인이 모두 통과하면 `super-gsd`가 올바르게, 비침투적으로 설치된 것이다.
```

new_string:
```
## 설치 확인

설치 후 `super-gsd`가 올바르게 로드됐는지, 기존 도구들이 정상 작동하는지 확인한다.

### Claude Code

1. `/plugin list`를 실행하여 `super-gsd`가 `.claude-plugin/plugin.json`의 이름, 버전, 설명과 일치하는지 확인한다.
2. `/gsd-progress`(또는 다른 GSD 명령)를 실행하여 GSD가 정상 응답하는지 확인한다 — GSD가 수정되지 않았음을 증명한다.
3. Skill 트리를 열어 `superpowers:*` 스킬이 발견 가능하고 호출 가능한지 확인한다 — Superpowers가 수정되지 않았음을 증명한다.

### Codex

1. `cat .codex/hooks.json` — hooks.json 존재 확인
2. `ls hooks/*.cjs` — 훅 스크립트 존재 확인
3. `ls .agents/skills/` — 스킬 디렉터리 존재 확인
4. `$sg-status` 실행 — 스킬 응답 확인

### Gemini

1. `cat .gemini/settings.json` — settings.json 존재 확인
2. `ls hooks/*.cjs` — 훅 스크립트 존재 확인
3. `ls .agents/skills/` — 스킬 디렉터리 존재 확인
4. `$sg-status` 실행 — 스킬 응답 확인

플랫폼별 확인이 모두 통과하면 `super-gsd`가 올바르게 설치된 것이다.
```
  </action>
  <verify>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -c "git clone" README.ko.md | grep -q "^0$" && echo "PASS: no git clone in README.ko.md" || echo "FAIL: git clone still in README.ko.md"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -c "npx @gyuha/super-gsd install" README.ko.md | grep -qE "^[2-9]" && echo "PASS: npx commands in README.ko.md" || echo "FAIL: npx commands missing"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "팁" README.ko.md && echo "PASS: tip notes in README.ko.md" || echo "FAIL: tip notes missing"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "cat .codex/hooks.json" README.ko.md && echo "PASS: Codex verify steps in README.ko.md" || echo "FAIL: Codex verify steps missing"</automated>
    <automated>cd /Users/gyuha/workspace/super-gsd && grep -q "cat .gemini/settings.json" README.ko.md && echo "PASS: Gemini verify steps in README.ko.md" || echo "FAIL: Gemini verify steps missing"</automated>
  </verify>
  <done>README.ko.md에서 git clone 블록이 사라지고, Codex/Gemini 설치 섹션이 npx 단일 명령 + 한글 설명으로 교체되어 있으며, 설치 확인 섹션이 Claude Code / Codex / Gemini 세 한글 서브섹션으로 분리되어 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 | 순수 문서 업데이트 — 코드 실행 경로 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | README.md / AGENTS.md / README.ko.md | accept | 문서 파일만 수정; git diff로 변경 범위 검토 가능 |
</threat_model>

<verification>
모든 태스크 완료 후 다음을 확인한다:

```bash
# git clone이 세 문서 어디에도 남아 있지 않은지 확인
grep -rn "git clone" README.md AGENTS.md README.ko.md

# npx 명령이 영문/한글 README 모두에 있는지 확인
grep -n "npx @gyuha/super-gsd install" README.md README.ko.md AGENTS.md

# Verify 서브섹션이 세 플랫폼 모두 있는지 확인
grep -n "### Claude Code\|### Codex\|### Gemini" README.md README.ko.md

# AGENTS.md Step 0 확인
grep -n "Step 0\|Step 1\|Step 2\|Step 3" AGENTS.md
```
</verification>

<success_criteria>
1. README.md: `git clone` 블록 0개, `npx @gyuha/super-gsd install` 2개 (Codex + Gemini), `$sg-setup` Tip 2개
2. README.md Verify install: `### Claude Code`, `### Codex`, `### Gemini` 서브섹션 각 1개씩
3. AGENTS.md: `Step 0` 블록이 `Step 1` 앞에 위치, npx 두 명령 포함
4. README.ko.md: `git clone` 블록 0개, `npx @gyuha/super-gsd install` 2개, 설치 확인 3개 서브섹션 (한글)
</success_criteria>

<output>
완료 후 `.planning/phases/35-doc-improvement/35-01-SUMMARY.md`를 생성한다.
</output>
