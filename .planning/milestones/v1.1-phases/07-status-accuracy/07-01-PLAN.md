---
phase: 07-status-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/sg-status.md
autonomous: false
requirements:
  - STATUS-01
  - STATUS-02
  - STATUS-03

must_haves:
  truths:
    - "sg-status가 HANDOFF.md 마지막 데이터 행의 `To` 컬럼을 읽어 storage enum(`init|gsd-plan|superpowers|review|hookify`) → display enum(`init|gsd|superpowers|hookify`)로 변환해 `Stage:` 라인에 출력한다 (D-01, D-02; STATUS-01)"
    - "HANDOFF.md가 없거나 데이터 행이 0개일 때 sg-status가 오류 없이 `Stage: init`, `Last handoff: (none)`을 출력한다 (STATUS-02)"
    - "STATE.md `Phase:` 라인의 콜론 뒤 전체 문자열(trim된)을 그대로 출력한다. `Phase: Not started` 입력 시 `Not`만 잘려나오던 버그가 사라진다 (D-04, D-05, D-06; STATUS-03)"
    - "Next 명령 라우팅은 storage 5-state 기준 case 블록을 그대로 유지한다 — `review` stage는 표시는 `superpowers`이지만 Next는 `/super-gsd:sg-learn`이다 (D-03)"
    - "출력은 정확히 3 header lines + blank + Next 1줄 형식을 유지한다 (D-29 lock)"
    - "STATE.md `Phase:` 라인에서 phase 번호 정수를 추출할 수 없을 때(예: `Phase: Not started`), Next 명령은 `/super-gsd:sg-plan`(인자 생략)을 emit한다 (CONTEXT specifics scenario 6 fallback (b))"
    - "D-07: STATE.md Phase 라인 파싱 블록은 `# --- BEGIN/END STATE.md Phase parsing block (D-07: Phase 8 sg-start이 동일 블록을 복제) ---` 주석으로 명확히 demarcate되어, Phase 8 sg-start가 동일 블록을 인라인 복제할 수 있다."
    - "D-08: 자동화 테스트 자산을 추가하지 않는다 — `files_modified`는 `commands/sg-status.md` 단일 파일만 포함하며, `tests/`, `hooks/`, `bin/` 등 새 디렉터리/파일은 생성하지 않는다 (Phase 6 D-04 bash-only 노선 일관 유지)."
    - "D-09: 수동 회귀 검증 체크리스트(CONTEXT.md `<specifics>` 7개 시나리오)는 Task 2 (checkpoint:human-verify) 안에 명시되어 있고, 자동화 자산 없이 사람이 직접 실행해 회귀 없음을 확인한다."
  artifacts:
    - path: "commands/sg-status.md"
      provides: "sg-status 명령 정의 — 단일 수정 대상"
      contains: "case \"$STAGE_RAW\" in"
  key_links:
    - from: "commands/sg-status.md (Phase 파싱 블록)"
      to: ".planning/STATE.md `Phase:` 라인"
      via: "grep + sed로 콜론 뒤 전체 문자열 캡처"
      pattern: "sed -E 's/\\^Phase:\\[\\[:space:\\]\\]\\*//'"
    - from: "commands/sg-status.md (display 매핑)"
      to: "Stage 출력 라인"
      via: "$STAGE_RAW → $STAGE_DISPLAY 변환 case 블록"
      pattern: "gsd-plan).*gsd"
    - from: "commands/sg-status.md (Next 분기)"
      to: "$STAGE_RAW (5-state 그대로)"
      via: "case \"$STAGE_RAW\" in"
      pattern: "review.*sg-learn"
---

<objective>
`commands/sg-status.md` bash 블록을 D-01~D-06 결정에 맞춰 수정하여 STATUS-01/02/03 요건을 충족시킨다.

Purpose: 
- 사용자 멘탈모델(3-stage: gsd/superpowers/hookify)에 맞춘 표시 enum 도입 — storage 5-state는 그대로 유지(D-03)
- STATE.md `Phase:` 라인 파싱 회귀 버그 제거 — `Not started` 같은 비숫자 라인에서 `Not`만 잘려나오던 문제 (D-04~D-06; STATUS-03)
- HANDOFF.md 부재/빈 데이터 행 케이스에서 오류 없이 init 상태 출력 (STATUS-02)

Output: `commands/sg-status.md` 한 파일만 수정. 헬퍼 스크립트, 테스트 자산, 다른 명령 파일 변경 없음 (D-08, Phase 6 D-04 노선).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-status-accuracy/07-CONTEXT.md
@.planning/HANDOFF.md
@commands/sg-status.md

<interfaces>
<!-- 파싱 대상 파일들의 현재 포맷(이미 codebase에 존재). 실행자는 추가 탐색 없이 아래 형식을 직접 사용한다. -->

STATE.md `Phase:` 라인 포맷 (`.planning/STATE.md` line 28 기준 — 현실 예):
```
Phase: 6 (sg-health) — Not started
```
회귀 fixture 후보 라인 (수동 검증 시나리오 6):
```
Phase: Not started
```

HANDOFF.md 데이터 행 포맷 (5컬럼 `|` 구분 Markdown table, Phase 2 D-22 lock):
```
| 2026-05-16T13:01:11Z | 06-sg-health | init | superpowers | 415be74 |
```
컬럼 인덱스 (awk `-F'|'` 기준, 앞쪽 leading `|` 때문에 컬럼 1은 빈 문자열):
- $2 = Timestamp
- $3 = Phase
- $4 = From
- $5 = To  ← stage 판정 대상
- $6 = Plan Hash

Storage enum (HANDOFF.md `To` 컬럼 — Phase 2 D-22 lock, 변경 불가):
```
init | gsd-plan | superpowers | review | hookify
```

Display enum (사용자에게 보이는 `Stage:` 라벨 — D-01):
```
init | gsd | superpowers | hookify
```

Storage → Display 매핑 (D-02):
```
init        → init
gsd-plan    → gsd
superpowers → superpowers
review      → superpowers
hookify     → hookify
```

Next 명령 매핑 (storage 기준 — D-03, Phase 2 D-28 lock):
```
init        → /super-gsd:sg-plan <PHASE_NUM>     (PHASE_NUM 비정수면 /super-gsd:sg-plan)
gsd-plan    → /super-gsd:sg-execute
superpowers → /super-gsd:sg-learn
review      → /super-gsd:sg-learn
hookify     → /super-gsd:sg-plan <NEXT_PHASE>    (다음 phase 존재) | /super-gsd:sg-complete (없음)
```

출력 형식 (D-29 lock — 정확히 5줄, 마지막 줄 trailing newline 1개):
```
Phase: <STATE.md Phase: 라인 콜론 뒤 trim된 전체 문자열>
Stage: <display enum>
Last handoff: <ISO timestamp | (none)>

Next: <next-command>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sg-status bash 블록을 D-01~D-06 규칙에 맞춰 갱신</name>
  <files>commands/sg-status.md</files>
  <read_first>
    - commands/sg-status.md (전체 — 현재 process 블록 step 1~6, 특히 line 17 PHASE_NUM 추출 / lines 27-41 stage 파싱 / lines 68-84 case 블록)
    - .planning/phases/07-status-accuracy/07-CONTEXT.md (locked decisions D-01~D-09)
    - .planning/HANDOFF.md (5컬럼 포맷 확인용)
    - .planning/STATE.md (Phase 라인 실제 포맷 확인용)
  </read_first>
  <action>
    `commands/sg-status.md` 의 `<process>` 블록 안 bash 코드를 갱신한다. 다른 파일은 절대 건드리지 않는다. 변경 범위는 다음 세 영역으로 한정한다:

    영역 A — STATE.md `Phase:` 라인 파싱 교체 (D-04, D-05, D-06; STATUS-03):
    - 기존 `PHASE_NUM=$(grep -E '^Phase: [0-9]' ... | awk '{print $2}')` 추출 로직과 그 뒤이은 ROADMAP.md `### Phase ${PHASE_NUM}:` 룩업 로직을 모두 폐기.
    - 대신 `PHASE_LINE` 변수를 도입: `grep -E '^Phase:' .planning/STATE.md 2>/dev/null | head -1 | sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//'` 패턴으로 콜론 뒤 전체 문자열을 trim해서 캡처. `\S+` 같은 토큰 1개 캡처 정규식은 **금지** (D-06).
    - STATE.md 파일이 없거나 `^Phase:` 라인이 없으면 `PHASE_LINE="(none)"` 로 설정 (D-05 fallback).
    - 출력은 `Phase: ${PHASE_LINE}` 한 줄 그대로. ROADMAP.md를 phase 이름 룩업용으로 더 이상 읽지 않는다 (STATE.md를 single source of truth로 — D-04). 단, hookify 분기의 next-phase 존재 여부 체크용 ROADMAP 조회는 아래 영역 C에서 그대로 유지.
    - 별도 `PHASE_NUM` 변수가 필요하면(영역 C에서 사용) `PHASE_LINE` 시작 토큰을 정규식으로 검사: `PHASE_NUM=$(echo "$PHASE_LINE" | grep -oE '^[0-9]+' || echo "")`. 비정수 시작이면 빈 문자열.
    - **D-07 forward-compat 표시**: 이 파싱 블록 시작과 끝에 다음 주석을 삽입해 Phase 8 sg-start가 동일 블록을 식별·복제할 수 있도록 한다:
      ```
      # --- BEGIN STATE.md Phase parsing block (D-07: Phase 8 sg-start이 동일 블록을 복제) ---
      PHASE_LINE=$(grep -E '^Phase:' .planning/STATE.md 2>/dev/null | head -1 | sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//')
      [ -z "$PHASE_LINE" ] && PHASE_LINE="(none)"
      PHASE_NUM=$(echo "$PHASE_LINE" | grep -oE '^[0-9]+' || echo "")
      # --- END STATE.md Phase parsing block ---
      ```

    영역 B — Stage 표시 enum 매핑 추가 (D-01, D-02; STATUS-01):
    - 기존 stage 변수명을 `STAGE_RAW` 로 rename (HANDOFF.md `To` 컬럼 값 5-state). 기존 `case "$STAGE" in gsd-plan|superpowers|review|hookify) ;; *) ... corruption ... ;; esac` 스키마 검증 블록은 `STAGE_RAW` 기준으로 그대로 유지.
    - `STAGE_RAW` → `STAGE_DISPLAY` 변환 case 블록을 신규 추가:
      ```
      case "$STAGE_RAW" in
        init)         STAGE_DISPLAY="init" ;;
        gsd-plan)     STAGE_DISPLAY="gsd" ;;
        superpowers)  STAGE_DISPLAY="superpowers" ;;
        review)       STAGE_DISPLAY="superpowers" ;;
        hookify)      STAGE_DISPLAY="hookify" ;;
      esac
      ```
    - HANDOFF.md 데이터 행 0개 / 파일 부재 시 `STAGE_RAW="init"`, `STAGE_DISPLAY="init"`, `LAST_TS="(none)"` (STATUS-02). 현재 코드의 `LAST_ROW` 분기를 그대로 활용하고, `STAGE_RAW` 명명만 적용.
    - 출력 라인은 `Stage: ${STAGE_DISPLAY}`.

    영역 C — Next 명령 분기 보정 (D-03 라우팅 유지 + scenario 6 fallback 결정):
    - 기존 `case "$STAGE" in` 블록을 `case "$STAGE_RAW" in` 으로 rename. 분기 자체(`init|gsd-plan|superpowers|review|hookify`)와 각 분기의 next-command 매핑은 D-03 lock 그대로 유지 — 즉 `review` 분기는 여전히 `/super-gsd:sg-learn`을 emit, hookify 분기도 기존 `NEXT_PHASE_EXISTS` 로직 그대로.
    - `init` 분기 fallback (CONTEXT specifics scenario 6): 기존 `NEXT_CMD="/super-gsd:sg-plan $PHASE_NUM"` 을 `PHASE_NUM` 정수 추출 여부에 따라 분기:
      ```
      init)
        if [ -n "$PHASE_NUM" ]; then
          NEXT_CMD="/super-gsd:sg-plan $PHASE_NUM"
        else
          NEXT_CMD="/super-gsd:sg-plan"
        fi
        ;;
      ```
      (PHASE_NUM 비추출 → 인자 생략한 `/super-gsd:sg-plan`을 emit — CONTEXT specifics 후보 (b) 채택. 사용자가 sg-plan 안에서 phase를 직접 선택하도록 위임.)
    - hookify 분기 안의 `NEXT_PHASE=$((PHASE_NUM + 1))` 도 PHASE_NUM이 정수일 때만 실행 — 이미 기존 코드의 `if echo "$PHASE_NUM" | grep -qE '^[0-9]+$'` 가드가 있으므로 그대로 유지(영역 A 변경이 호환).

    영역 D — `<process>` 코드 블록 외 부속 갱신:
    - Step 1 헤더 텍스트를 D-04에 맞춰 갱신: ROADMAP.md 룩업 언급 제거, "Read `Phase:` line verbatim from STATE.md" 류 1줄 설명으로 교체.
    - Step 6 출력 템플릿(`<process>` 블록 끝의 fenced output sample)에서 `Phase: <PHASE_NUM> (<PHASE_NAME>)` 를 `Phase: <PHASE_LINE>` 로 교체. `Stage: <STAGE>` 를 `Stage: <STAGE_DISPLAY>` 로 교체.
    - 파일 상단 `<objective>` 1줄 갱신: "reads ROADMAP.md for phase-name lookup" 부분 제거 — 단, hookify next-phase 존재 체크용 ROADMAP 조회는 남으므로 "reads ROADMAP.md only to detect whether a following phase exists for the hookify branch" 정도로 정확히 표현.
    - `<success_criteria>` 3개 항목 갱신: D-29 5-라인 형식 그대로(1번), STATUS-02 init fallback(2번), `Next:` 매핑 + display enum 정확성(3번).

    **하지 말아야 할 것:**
    - 새 파일 생성(helper script, test fixture 등) — D-08, Phase 6 D-04 lock 위반.
    - `commands/sg-start.md` 등 다른 명령 파일 수정 — Phase 8 스코프.
    - 출력 라인을 5줄에서 더 늘리거나 줄이기 — D-29 lock.
    - bash 외 언어(Python 등) 인라인 추가 — Phase 6 D-04 lock.
    - 기존 case 블록의 분기 개수 5개를 줄이거나 늘리기 — D-03 lock.
  </action>
  <verify>
    <automated>
    cd /Users/gyuha/workspace/super-gsd \
    &amp;&amp; grep -q 'case "$STAGE_RAW" in' commands/sg-status.md \
    &amp;&amp; grep -q 'gsd-plan)\s*STAGE_DISPLAY="gsd"' commands/sg-status.md \
    &amp;&amp; grep -q 'review)\s*STAGE_DISPLAY="superpowers"' commands/sg-status.md \
    &amp;&amp; ! grep -q "awk '{print \$2}'" commands/sg-status.md \
    &amp;&amp; ! grep -q 'grep -E "^### Phase \${PHASE_NUM}:"' commands/sg-status.md \
    &amp;&amp; grep -q "sed -E 's/\\^Phase:\\[\\[:space:\\]\\]\\*//'" commands/sg-status.md \
    &amp;&amp; PHASE_LINE=$(printf 'Phase: Not started\n' | grep -E '^Phase:' | head -1 | sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//') \
    &amp;&amp; [ "$PHASE_LINE" = "Not started" ] \
    &amp;&amp; PHASE_LINE2=$(printf 'Phase: 6 (sg-health) — Not started\n' | grep -E '^Phase:' | head -1 | sed -E 's/^Phase:[[:space:]]*//' | sed -E 's/[[:space:]]+$//') \
    &amp;&amp; [ "$PHASE_LINE2" = "6 (sg-health) — Not started" ] \
    &amp;&amp; echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    1. `commands/sg-status.md` 에 `case "$STAGE_RAW" in` 라인이 존재한다 (D-03 라우팅 5-state 보존).
    2. `commands/sg-status.md` 에 `gsd-plan` → `gsd`, `review` → `superpowers` 표시 매핑이 포함된 display case 블록이 존재한다 (D-01, D-02).
    3. `commands/sg-status.md` 에서 `awk '{print $2}'` 로 phase 번호를 추출하는 라인이 더 이상 존재하지 않는다 (D-04, D-06 회귀 방지).
    4. `commands/sg-status.md` 에서 `grep -E "^### Phase ${PHASE_NUM}:"` 로 ROADMAP에서 phase 이름을 룩업하는 라인이 더 이상 존재하지 않는다 — 단, hookify 분기의 `NEXT_PHASE` 존재 체크용 grep은 유지된다 (D-04 STATE.md as single source of truth, hookify 분기 D-03 lock).
    5. 회귀 체크: 임시 STATE.md `Phase: Not started` 라인을 sed 파이프라인으로 파싱하면 결과가 정확히 `Not started` 이고 `Not` 만 잘리지 않는다 (STATUS-03; verify automated 라인에서 검증).
    6. 정상 케이스 체크: 현실 STATE.md 라인 `Phase: 6 (sg-health) — Not started` 를 동일 파이프라인으로 파싱하면 결과가 정확히 `6 (sg-health) — Not started` 이다 (D-05).
    7. `<success_criteria>` 섹션이 3개 항목으로 유지되고, 1번이 D-29 5-라인 형식을, 2번이 STATUS-02 init fallback을, 3번이 display 매핑 + Next 매핑 정확성을 각각 명시한다.
    8. 파일 외 변경 없음 — `git status --short` 결과가 정확히 `M commands/sg-status.md` 한 줄이다(.planning 산출물 제외).
  </acceptance_criteria>
  <done>
    `commands/sg-status.md` 한 파일만 수정되었고, 위 8개 acceptance criteria가 모두 충족된다. Display enum 매핑(D-01/D-02), STATE.md Phase 라인 풀 캡처(D-04~D-06), storage-state-기반 Next 라우팅(D-03), HANDOFF.md 데이터 행 0개 init fallback(STATUS-02), PHASE_NUM 비추출 시 `/super-gsd:sg-plan` 인자 생략 fallback(scenario 6 (b))이 코드와 success_criteria 양쪽에 반영되어 있다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 수동 회귀 검증 체크리스트 실행 (D-09)</name>
  <files>.planning/HANDOFF.md (백업/복원 대상; 수정 후 원복), .planning/STATE.md (백업/복원 대상; 수정 후 원복) — 작업이 끝나면 두 파일 모두 원래 상태로 복원되어야 한다 (read-only 효과)</files>
  <read_first>
    - .planning/phases/07-status-accuracy/07-CONTEXT.md (specifics §"수동 검증 시나리오 체크리스트")
    - commands/sg-status.md (Task 1 수정 결과물)
    - .planning/HANDOFF.md (백업 대상)
    - .planning/STATE.md (백업 대상)
  </read_first>
  <what-built>
    Task 1에서 `commands/sg-status.md` bash 블록을 D-01~D-06 규칙에 맞춰 갱신했다. 자동 테스트 자산은 D-08에 따라 추가되지 않았으므로, 사람이 직접 7개 회귀 시나리오를 실행해 출력이 D-29 형식과 D-01~D-06 매핑을 모두 충족하는지 확인한다.
  </what-built>
  <action>
    사람이 7개 회귀 시나리오를 직접 실행해 sg-status 출력이 D-01~D-06 매핑과 D-29 출력 형식을 모두 충족하는지 확인한다. 자동화 자산 추가 없음(D-08). 절차는 `<how-to-verify>`에 명시. 작업 종료 시 HANDOFF.md / STATE.md는 백업으로부터 원복되어야 한다.
  </action>
  <how-to-verify>
    1. 백업: 
       ```
       cp .planning/HANDOFF.md .planning/HANDOFF.md.bak
       cp .planning/STATE.md .planning/STATE.md.bak
       ```
    2. CONTEXT.md `<specifics>` 테이블의 시나리오 1~7을 순서대로 실행:
       - 매 시나리오마다 STATE.md 의 `Phase:` 라인과 HANDOFF.md 마지막 행을 시나리오 fixture로 편집.
       - Claude 세션에서 `/super-gsd:sg-status` 실행.
       - 출력 3줄(`Phase:` / `Stage:` / `Last handoff:`) + 빈 줄 + `Next:` 1줄이 기대값과 정확히 일치하는지 확인.
    3. 특히 주목할 시나리오:
       - **시나리오 1**: HANDOFF.md 데이터 행 0개 → `Stage: init`, `Last handoff: (none)` (STATUS-02).
       - **시나리오 4**: `To=review` → 표시는 `Stage: superpowers`(D-02), Next는 `/super-gsd:sg-learn`(D-03) — 라우팅이 storage 기준으로 유지되는지 확인.
       - **시나리오 6**: `Phase: Not started` → `Phase: Not started` (`Not`만 잘리지 않음; STATUS-03), Next는 `/super-gsd:sg-plan` (인자 생략 fallback (b)).
       - **시나리오 7**: STATE.md 자체 부재 → `Phase: (none)` (D-05 fallback). Next는 `/super-gsd:sg-plan` (인자 생략 — scenario 6과 동일한 fallback (b)의 일관 적용; PHASE_NUM은 `(none)` 시작 토큰이 정수가 아니므로 빈 문자열).
    4. 모든 시나리오에서 출력이 정확히 5줄 형식(D-29)을 유지하는지 확인.
    5. 복원:
       ```
       mv .planning/HANDOFF.md.bak .planning/HANDOFF.md
       mv .planning/STATE.md.bak .planning/STATE.md
       ```
    6. 백업 복원 후 한 번 더 `/super-gsd:sg-status` 실행해 정상 출력 확인.
  </how-to-verify>
  <verify>
    <human-check>7개 시나리오 출력이 모두 CONTEXT.md 기대값과 일치하고, 최종적으로 `git status --short .planning/HANDOFF.md .planning/STATE.md` 결과가 빈 출력(원복됨)이어야 한다.</human-check>
  </verify>
  <done>
    7개 시나리오 출력이 모두 통과 보고되었고, HANDOFF.md / STATE.md 가 백업으로부터 원복되었다.
  </done>
  <resume-signal>
    7개 시나리오 모두 통과하면 "approved" 입력. 시나리오별 출력 불일치가 있으면 시나리오 번호와 실제 출력을 보고 — Task 1로 돌아가 수정.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (no untrusted boundary crossed) | `sg-status`는 로컬 `.planning/HANDOFF.md` / `.planning/STATE.md` / `.planning/ROADMAP.md`만 읽고 stdout으로 출력한다. 사용자 입력, 네트워크, 파일 쓰기 모두 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `.planning/STATE.md` `Phase:` 라인 (악성 콘텐츠) | accept | sg-status는 표시만 하고 실행하지 않음. 셸 인젝션 surface는 이미 bash 변수 인용으로 차단됨(`"$PHASE_LINE"` 형태로만 사용; `eval` 없음). Repo가 이미 침해된 상태에서만 발생 가능 — 별도 mitigation 불필요. |
| T-07-02 | Tampering | `.planning/HANDOFF.md` `To` 컬럼 (악성 stage 문자열) | mitigate | 기존 `case "$STAGE_RAW" in gsd-plan|superpowers|review|hookify) ;; *) ... exit 1 ;; esac` 스키마 검증 블록을 그대로 유지 — 알려진 5-state 외 값은 에러 출력 후 exit. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | Phase 7은 패키지 설치 작업 없음 — RESEARCH.md Package Legitimacy Audit 불필요. |

요약: 읽기 전용 명령, 사용자 입력 없음, 네트워크 없음. 기존 bash 변수 인용 위생을 유지하는 것 외 추가 mitigation은 불필요.
</threat_model>

<verification>
1. Task 1 자동 verify의 grep/sed 회귀 체크가 모두 통과한다 (`PASS` 출력).
2. Task 2 수동 검증 체크리스트 7개 시나리오 모두 통과 — 특히 시나리오 4(review → display 축약, routing 보존), 시나리오 6(`Not started` 잘림 회귀 없음), 시나리오 7(STATE.md 부재 fallback).
3. `git diff --stat commands/sg-status.md` 결과가 한 파일만 변경됨을 확인.
4. `.planning/` 안의 다른 파일은 PLAN/SUMMARY 외 변경되지 않는다 (HEALTH-05 readonly 정신을 sg-status에도 적용).
</verification>

<success_criteria>
- STATUS-01 충족: HANDOFF.md 마지막 데이터 행의 `To`를 읽어 display enum(`init|gsd|superpowers|hookify`)으로 변환해 표시한다 (D-01, D-02).
- STATUS-02 충족: HANDOFF.md 데이터 행 0개 또는 파일 부재 시 `Stage: init`, `Last handoff: (none)`을 오류 없이 출력한다.
- STATUS-03 충족: STATE.md `Phase: Not started` 입력에서 `Phase: Not started` 가 그대로 출력된다 — `Not` 만 잘리던 회귀 버그 제거 (D-04, D-05, D-06).
- D-29 출력 형식 lock 유지: 정확히 3 header lines + blank + Next 1줄.
- D-03 라우팅 lock 유지: Next 명령은 storage 5-state 기준으로 분기 — `review` stage는 표시 축약과 무관하게 `/super-gsd:sg-learn`을 emit.
- D-08 / Phase 6 D-04 lock 유지: helper 스크립트, 테스트 fixture 추가 없음. `commands/sg-status.md` 한 파일만 수정.
</success_criteria>

<output>
Create `.planning/phases/07-status-accuracy/07-01-SUMMARY.md` when done.
</output>
