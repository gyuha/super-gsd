---
phase: 21-sg-ui-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/sg-ui-plan/SKILL.md
autonomous: true
requirements:
  - VC-03
  - VC-04
  - VC-05

must_haves:
  truths:
    - "skills/sg-ui-plan/SKILL.md 파일이 존재하며 슬래시 명령 /super-gsd:sg-ui-plan으로 호출 가능하다"
    - "sg-ui-plan 실행 시 phase resolve 후 superpowers:brainstorming Agent가 실행된다"
    - "sg-ui-plan 실행 후 HANDOFF.md에 To: ui-plan 행이 append된다"
  artifacts:
    - path: "skills/sg-ui-plan/SKILL.md"
      provides: "sg-ui-plan 슬래시 명령 정의"
      contains: "name: sg-ui-plan"
  key_links:
    - from: "skills/sg-ui-plan/SKILL.md"
      to: "superpowers:brainstorming"
      via: "Agent() 호출"
      pattern: "superpowers:brainstorming"
    - from: "skills/sg-ui-plan/SKILL.md"
      to: ".planning/HANDOFF.md"
      via: "bash append"
      pattern: "ui-plan"
---

<objective>
UI 전용 설계 명령 sg-ui-plan의 핵심 SKILL.md 파일을 생성한다.

Purpose: sg-plan의 Visual Companion 분기를 독립 명령으로 분리하여, 사용자가 phase 번호를 전달하면 곧바로 superpowers:brainstorming을 실행하고 결과를 HANDOFF.md에 기록하도록 한다.
Output: skills/sg-ui-plan/SKILL.md — plugin.json의 "skills": "./skills/" 경로 아래에 위치하므로 별도 등록 불필요.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- 패턴 참조 -->
@skills/sg-plan/SKILL.md
@skills/sg-review/SKILL.md

<interfaces>
<!-- sg-plan/SKILL.md에서 발췌한 핵심 패턴 -->

HANDOFF.md append 패턴 (sg-plan Step 2.5 기반):
```bash
HANDOFF_FILE=".planning/HANDOFF.md"
if [ ! -f "$HANDOFF_FILE" ] || ! grep -q "Timestamp.*Phase.*From.*To.*Plan Hash" "$HANDOFF_FILE" 2>/dev/null; then
  mkdir -p "$(dirname "$HANDOFF_FILE")"
  printf '| Timestamp | Phase | From | To | Plan Hash |\n| --- | --- | --- | --- | --- |\n' > "$HANDOFF_FILE"
fi
PHASE_PAD=$(printf "%02d" "${PHASE_NUM:-0}" 2>/dev/null || echo "${PHASE_NUM:-0}")
PHASE_SLUG=$(ls -d .planning/phases/${PHASE_PAD}-* 2>/dev/null | head -1 | xargs basename 2>/dev/null)
[ -z "$PHASE_SLUG" ] && PHASE_SLUG="${PHASE_NUM:-unknown}"
if ! grep -q "| ${PHASE_SLUG} |.*| ui-plan |" "$HANDOFF_FILE" 2>/dev/null; then
  TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)
  FROM_STAGE=$(grep -E '^\| [0-9]{4}-' "$HANDOFF_FILE" | tail -1 | awk -F'|' '{gsub(/ /,"",$5); print $5}')
  [ -z "$FROM_STAGE" ] && FROM_STAGE="init"
  echo "| $TS | $PHASE_SLUG | $FROM_STAGE | ui-plan | - |" >> "$HANDOFF_FILE"
fi
```

brainstorming Agent 패턴 (sg-plan Step 1.5 기반):
```
Agent(
  description="superpowers:brainstorming for Phase $PHASE_NUM UI design",
  prompt="...$PHASE_SECTION...",
  subagent_type="general-purpose"
)
```

ROADMAP phase section 추출:
```bash
PHASE_SECTION_RAW=$(gsd-sdk query roadmap.get-phase "$PHASE_NUM" --pick section 2>/dev/null)
PHASE_SECTION=$(echo "$PHASE_SECTION_RAW" | python3 -c 'import json,sys; v=sys.stdin.read().strip(); print(json.loads(v))' 2>/dev/null || echo "$PHASE_SECTION_RAW")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: skills/sg-ui-plan/SKILL.md 생성</name>
  <files>skills/sg-ui-plan/SKILL.md</files>
  <read_first>
    - skills/sg-plan/SKILL.md — HANDOFF append 패턴, Agent 호출 패턴, phase resolve 패턴 전체
    - skills/sg-review/SKILL.md — SKILL.md 구조(frontmatter + objective + execution_context + process + success_criteria) 확인
    - .claude-plugin/plugin.json — "skills": "./skills/" 경로 확인 (이미 읽음, sg-ui-plan/ 서브디렉토리 생성으로 충족)
  </read_first>
  <action>
    skills/sg-ui-plan/ 디렉토리를 생성하고 SKILL.md를 작성한다.

    YAML frontmatter 필드 (VC-03):
    - name: sg-ui-plan
    - description: Run UI design brainstorming for a phase — resolves phase context from ROADMAP.md and invokes superpowers:brainstorming.
    - argument-hint: "[phase] - optional. Defaults to STATE.md current phase."

    <objective> 블록: ROADMAP.md에서 phase 섹션을 읽어 superpowers:brainstorming Agent를 실행하는 UI 전용 설계 명령임을 서술한다.

    <process> 블록 — 아래 순서로 구성한다:

    Step 1 (Phase resolve): sg-plan Step 1과 동일한 패턴 사용. $ARGUMENTS가 있으면 사용, 없으면 STATE.md에서 추출. PHASE_NUM이 비어 있으면 오류 메시지 출력 후 중단.

    Step 2 (PHASE_SECTION 추출):
    - gsd-sdk query roadmap.get-phase "$PHASE_NUM" --pick section으로 추출
    - JSON parse 실패 시 raw 값 그대로 사용
    - PHASE_SECTION이 비어 있으면 "[sg-ui-plan] WARN: ROADMAP에서 Phase $PHASE_NUM 섹션을 찾을 수 없습니다. 빈 컨텍스트로 brainstorming을 실행합니다." 출력 후 계속

    Step 3 (brainstorming Agent 실행, VC-03):
    - "[sg-ui-plan] Phase $PHASE_NUM UI 설계 brainstorming 시작..." 출력
    - Agent 호출 전 $PHASE_NUM과 $PHASE_SECTION을 실제 값으로 치환
    - Agent prompt에 writing-plans 미호출 억제 지시 포함 (sg-plan Step 1.5 패턴 그대로 적용)
    - Agent 에러 시 "[sg-ui-plan] brainstorming 실패." 출력 후 중단 (sg-ui-plan은 brainstorming 전용이므로 abort)

    Step 4 (HANDOFF.md append, VC-04):
    - brainstorming Agent 완료 후 실행
    - HANDOFF_FILE 존재·헤더 검사 후 ui-plan 행 idempotent append
    - 동일 PHASE_SLUG + ui-plan 조합이 이미 있으면 skip
    - 성공 시 "[sg-ui-plan] HANDOFF.md에 ui-plan 기록 완료." 출력

    <success_criteria> 블록:
    1. PHASE_NUM이 비어 있으면 명시적 오류 메시지 출력 후 종료
    2. PHASE_SECTION 추출 실패 시 경고 출력 후 빈 컨텍스트로 brainstorming 진행
    3. brainstorming Agent가 실행되고 완료 후 Step 4로 이동
    4. brainstorming Agent 에러 시 오류 메시지 출력 후 중단 (HANDOFF 기록 안 함)
    5. brainstorming 완료 후 HANDOFF.md에 To=ui-plan 행이 기록됨 (동일 phase+ui-plan 조합이 있으면 skip)
  </action>
  <verify>
    <automated>
      test -f skills/sg-ui-plan/SKILL.md
      grep -q "name: sg-ui-plan" skills/sg-ui-plan/SKILL.md
      grep -q "superpowers:brainstorming" skills/sg-ui-plan/SKILL.md
      grep -q "ui-plan" skills/sg-ui-plan/SKILL.md
      grep -q "argument-hint" skills/sg-ui-plan/SKILL.md
    </automated>
  </verify>
  <acceptance_criteria>
    - skills/sg-ui-plan/SKILL.md 파일이 존재한다
    - YAML frontmatter에 name: sg-ui-plan, description, argument-hint 세 필드가 모두 있다
    - objective, process, success_criteria 블록이 모두 존재한다
    - process에 superpowers:brainstorming Agent 호출 지시가 있다
    - process에 HANDOFF.md append 로직이 있고 "ui-plan" 문자열이 To 값으로 사용된다
    - writing-plans 미호출 억제 지시가 Agent prompt에 포함된다
    - plugin.json의 "skills": "./skills/" 아래에 skills/sg-ui-plan/ 서브디렉토리가 존재하므로 VC-05 자동 충족
  </acceptance_criteria>
  <done>skills/sg-ui-plan/SKILL.md가 존재하며, sg-plan의 Visual Companion 패턴을 독립 명령으로 구현하고 HANDOFF.md에 ui-plan 행을 기록한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARGUMENTS → PHASE_NUM | 사용자 입력이 phase 번호로 사용됨 — bash injection 가능성 |
| ROADMAP.md → PHASE_SECTION | 파일 내용이 Agent prompt에 삽입됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | ARGUMENTS injection | accept | PHASE_NUM은 숫자 검증 없이 bash 변수로만 사용됨; 로컬 개발 도구이므로 낮은 위험 |
| T-21-02 | Information Disclosure | PHASE_SECTION → Agent prompt | accept | ROADMAP.md는 프로젝트 내부 문서; 외부 노출 없음 |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | 이 plan은 패키지 설치 없음 |
</threat_model>

<verification>
- skills/sg-ui-plan/SKILL.md 존재: `test -f skills/sg-ui-plan/SKILL.md && echo OK`
- frontmatter 필드: `grep -c "name: sg-ui-plan\|description:\|argument-hint:" skills/sg-ui-plan/SKILL.md` 결과 3 이상
- brainstorming 참조: `grep -q "superpowers:brainstorming" skills/sg-ui-plan/SKILL.md && echo OK`
- HANDOFF to ui-plan: `grep -q "ui-plan" skills/sg-ui-plan/SKILL.md && echo OK`
- plugin.json skills 경로 커버: `grep -q '"skills": "./skills/"' .claude-plugin/plugin.json && echo OK`
</verification>

<success_criteria>
VC-03: skills/sg-ui-plan/SKILL.md 생성 완료
VC-04: SKILL.md process에 HANDOFF.md append 로직 포함 (To: ui-plan)
VC-05: plugin.json "skills": "./skills/" 아래에 skills/sg-ui-plan/ 존재 (자동 충족)
</success_criteria>

<output>
Create `.planning/phases/21-sg-ui-plan/21-01-SUMMARY.md` when done
</output>
