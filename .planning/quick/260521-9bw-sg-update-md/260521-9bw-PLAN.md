---
phase: quick
plan: 9bw
type: execute
wave: 1
depends_on: []
files_modified: [commands/sg-update.md]
autonomous: true
requirements: [sg-update-version-display]
must_haves:
  truths:
    - "superpowers 업데이트 완료 후 버전 번호가 괄호 안에 표시된다"
    - "super-gsd 업데이트 완료 후 버전 번호가 괄호 안에 표시된다"
    - "설치 성공 시 버전 없으면 괄호 표시를 생략한다 (graceful fallback)"
  artifacts:
    - path: "commands/sg-update.md"
      provides: "버전 출력 포함 업데이트 스크립트"
      contains: "SUPERPOWERS_VER"
  key_links:
    - from: "claude plugin list"
      to: "SUPERPOWERS_VER / SUPERGSD_VER"
      via: "grep -oE '[0-9]+\\.[0-9]+\\.[0-9]+'"
      pattern: "grep -oE"
---

<objective>
`commands/sg-update.md`의 bash 블록에서 superpowers와 super-gsd 업데이트 후 설치된 버전 번호를 STATUS 문자열에 포함시킨다.

Purpose: 사용자가 업데이트 결과를 확인할 때 어떤 버전으로 변경됐는지 한눈에 알 수 있도록 한다.
Output: `commands/sg-update.md` (수정)
</objective>

<execution_context>
@/Users/gyuha/workspace/super-gsd/commands/sg-update.md
</execution_context>

<context>
수정 대상 파일만 존재한다. 외부 의존성 없음.

현재 출력 예시:
```
- superpowers: updated
- super-gsd: updated
```

목표 출력 예시:
```
- superpowers: updated (5.1.0)
- super-gsd: updated (0.0.22)
```

버전 추출 명령:
```bash
claude plugin list 2>&1 | grep -i 'superpowers' | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1
```

버전을 가져오지 못하면 괄호 없이 `updated`만 출력한다 (graceful fallback).
</context>

<tasks>

<task type="auto">
  <name>Task 1: superpowers / super-gsd STATUS 문자열에 버전 추가</name>
  <files>commands/sg-update.md</files>
  <action>
`commands/sg-update.md`의 bash 블록에서 superpowers와 super-gsd 각각의 성공 분기에 버전 추출 및 STATUS 포맷 변경을 적용한다.

superpowers 성공 분기 (`PLUGIN_EC` -eq 0, updated 케이스):
```
SUPERPOWERS_VER=$(claude plugin list 2>&1 | grep -i 'superpowers' | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)
if [ -n "$SUPERPOWERS_VER" ]; then
  SUPERPOWERS_STATUS="updated (${SUPERPOWERS_VER})"
else
  SUPERPOWERS_STATUS="updated"
fi
```

installed 케이스도 동일 패턴으로 변경 (`SUPERPOWERS_STATUS="installed"` → `updated` 대신 `installed` 워딩 사용).

super-gsd 성공 분기도 동일하게:
```
SUPERGSD_VER=$(claude plugin list 2>&1 | grep -i 'super-gsd' | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)
if [ -n "$SUPERGSD_VER" ]; then
  SUPERGSD_STATUS="updated (${SUPERGSD_VER})"
else
  SUPERGSD_STATUS="updated"
fi
```

installed 케이스도 동일 패턴.

수정 범위: superpowers 블록과 super-gsd 블록의 성공 분기 4곳만. 실패/skipped 분기, GSD 블록, echo 출력 라인은 건드리지 않는다.
  </action>
  <verify>
    <automated>grep -n 'SUPERPOWERS_VER\|SUPERGSD_VER' /Users/gyuha/workspace/super-gsd/commands/sg-update.md</automated>
  </verify>
  <done>
    - `SUPERPOWERS_VER` 변수 추출 및 조건부 STATUS 포맷 코드가 superpowers 성공 분기 2곳(updated, installed)에 존재한다
    - `SUPERGSD_VER` 변수 추출 및 조건부 STATUS 포맷 코드가 super-gsd 성공 분기 2곳에 존재한다
    - 실패/skipped 분기는 변경되지 않았다
  </done>
</task>

</tasks>

<success_criteria>
- `commands/sg-update.md`의 bash 블록에서 superpowers와 super-gsd 업데이트/설치 성공 시 버전 번호가 STATUS에 포함된다
- 버전을 가져올 수 없을 때 STATUS는 버전 없이 `updated` / `installed`만 출력한다 (스크립트 오류 없음)
- GSD 블록, 실패 분기, skipped 분기, echo 출력 라인은 변경되지 않았다
</success_criteria>

<output>
완료 후 `.planning/quick/260521-9bw-sg-update-md/260521-9bw-SUMMARY.md` 생성
</output>
