---
phase: 21-sg-ui-plan
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - README.md
  - docs/COMMANDS.md
autonomous: true
requirements:
  - VC-06
  - VC-07

must_haves:
  truths:
    - "README.md 명령표에 sg-ui-plan이 등재되어 있다"
    - "docs/COMMANDS.md에 sg-ui-plan 전체 설명 섹션이 있다"
  artifacts:
    - path: "README.md"
      provides: "명령표에 sg-ui-plan 행 추가"
      contains: "sg-ui-plan"
    - path: "docs/COMMANDS.md"
      provides: "sg-ui-plan 전체 설명 섹션"
      contains: "sg-ui-plan"
  key_links:
    - from: "README.md"
      to: "docs/COMMANDS.md"
      via: "See docs/COMMANDS.md 링크"
      pattern: "COMMANDS.md"
---

<objective>
README.md 명령표와 docs/COMMANDS.md에 sg-ui-plan 명령을 등재한다.

Purpose: 사용자가 플러그인을 설치했을 때 sg-ui-plan 명령의 존재와 사용법을 README와 COMMANDS.md에서 즉시 확인할 수 있도록 한다.
Output: README.md Commands 테이블에 sg-ui-plan 행, docs/COMMANDS.md에 sg-ui-plan 전체 설명 섹션.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/sg-ui-plan/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md 명령표에 sg-ui-plan 등재 (VC-06)</name>
  <files>README.md</files>
  <read_first>
    - README.md — Commands 섹션 테이블 전체 구조 확인 (현재 14개 행 위치, 삽입 위치 파악)
    - skills/sg-ui-plan/SKILL.md — description 필드 값을 그대로 인용
  </read_first>
  <action>
    README.md의 Commands 섹션 테이블에 sg-ui-plan 행을 삽입한다.

    삽입 위치: sg-plan 행 바로 다음 (sg-plan과 sg-execute 사이).

    추가할 행:
    | `/super-gsd:sg-ui-plan` | UI 설계 전용 brainstorming — `superpowers:brainstorming`을 직접 실행한다 | sg-plan에서 Visual Companion 없이 진행했지만 UI 설계가 필요할 때 |

    수정 범위: Commands 테이블의 sg-plan 다음 행에 한 줄만 삽입. 다른 행, 텍스트, 포맷은 일체 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -q "sg-ui-plan" README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README.md Commands 테이블에 `/super-gsd:sg-ui-plan` 행이 존재한다
    - 행 위치가 sg-plan 바로 다음이다 (grep -n "sg-ui-plan\|sg-plan" README.md로 줄 번호 비교)
    - 행에 superpowers:brainstorming 참조가 포함된다
    - 기존 다른 행 내용은 변경되지 않았다
  </acceptance_criteria>
  <done>README.md Commands 테이블에 sg-ui-plan 행이 sg-plan 다음에 추가된다.</done>
</task>

<task type="auto">
  <name>Task 2: docs/COMMANDS.md에 sg-ui-plan 전체 설명 추가 (VC-07)</name>
  <files>docs/COMMANDS.md</files>
  <read_first>
    - docs/COMMANDS.md — Quick Reference 테이블 위치, sg-plan 섹션 구조(Slash command / Maps to / Arguments / What it does / Example), 전체 섹션 순서
    - skills/sg-ui-plan/SKILL.md — argument-hint, description, process 요약용
  </read_first>
  <action>
    docs/COMMANDS.md에 sg-ui-plan 설명을 두 곳에 추가한다.

    1. Quick Reference 테이블: sg-plan 행 다음에 sg-ui-plan 행 삽입.
       형식: | `/super-gsd:sg-ui-plan` | `superpowers:brainstorming` Skill | `[phase]` | UI 설계 전용 brainstorming 실행 |

    2. 상세 섹션: sg-plan 섹션(## sg-plan) 바로 다음에 ## sg-ui-plan 섹션 삽입.
       섹션 구조는 sg-plan 섹션과 동일한 패턴을 따른다:

       ## sg-ui-plan

       **Slash command:** `/super-gsd:sg-ui-plan`

       **Maps to:** `superpowers:brainstorming` Skill

       **Arguments:** `[phase]` — optional. Defaults to the current phase from `.planning/STATE.md`.

       **What it does:** ROADMAP.md에서 대상 phase 섹션을 읽고, `superpowers:brainstorming` Agent를 실행하여 UI 설계 세션을 진행한다. 완료 후 `.planning/HANDOFF.md`에 `To: ui-plan` 행을 기록한다. `sg-plan`의 Visual Companion 분기를 독립 명령으로 분리한 것이며, brainstorming 완료 후 별도 plan-phase 호출 없이 종료된다.

       **Example:**
       ```
       /super-gsd:sg-ui-plan
       /super-gsd:sg-ui-plan 21
       ```

       ---

    수정 범위: Quick Reference 테이블에 한 행 삽입 + sg-plan 섹션 다음에 sg-ui-plan 섹션 삽입. 다른 섹션, 기존 텍스트는 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -c "sg-ui-plan" docs/COMMANDS.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/COMMANDS.md Quick Reference 테이블에 sg-ui-plan 행이 있다
    - ## sg-ui-plan 섹션이 존재하며 ## sg-plan 섹션 다음에 위치한다
    - 섹션에 Slash command, Maps to, Arguments, What it does, Example 모두 포함된다
    - "superpowers:brainstorming" 문자열이 섹션 내에 있다
    - "HANDOFF.md" 또는 "ui-plan" 참조가 섹션 내에 있다
    - `grep -c "sg-ui-plan" docs/COMMANDS.md` 결과가 3 이상 (Quick Reference 행 + 섹션 제목 + 본문 최소 1)
  </acceptance_criteria>
  <done>docs/COMMANDS.md Quick Reference 테이블과 ## sg-ui-plan 상세 섹션이 모두 추가된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md / COMMANDS.md 편집 | 문서 파일 수정 — 잘못된 삽입 위치가 기존 내용 손상 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | README.md 잘못된 삽입 | mitigate | read_first로 현재 파일 상태 확인 후 정확한 줄 위치에 삽입 |
| T-21-04 | Tampering | COMMANDS.md 섹션 순서 오류 | mitigate | sg-plan 섹션 다음 위치를 grep으로 확인 후 삽입 |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | 이 plan은 패키지 설치 없음 |
</threat_model>

<verification>
- README.md sg-ui-plan 등재: `grep -q "sg-ui-plan" README.md && echo OK`
- COMMANDS.md Quick Reference: `grep -q "sg-ui-plan" docs/COMMANDS.md && echo OK`
- COMMANDS.md 섹션 제목: `grep -q "^## sg-ui-plan" docs/COMMANDS.md && echo OK`
- sg-plan 다음 위치 확인: `grep -n "^## sg-plan\|^## sg-ui-plan" docs/COMMANDS.md`
</verification>

<success_criteria>
VC-06: README.md Commands 테이블에 sg-ui-plan 행 등재 완료
VC-07: docs/COMMANDS.md에 sg-ui-plan Quick Reference 행 + 전체 설명 섹션 추가 완료
</success_criteria>

<output>
Create `.planning/phases/21-sg-ui-plan/21-02-SUMMARY.md` when done
</output>
